ENCARGO · ACTO MAESTRA32-E6 · ETIQUETA-v1_2 + RE-EMPAREJA-LITE
SHA de redacción: `2c0d4c8` (main, merge PR #394) · Redactado: 30/ago/2026, dirección maestra-32 · Instrucciones vigentes: v2.11 · Estado: LISTO PARA LANZAR — primero del carril NUBE; sin ranura de mesa.
ENTORNO ASIGNADO: NUBE (`cloud_default`). NO se lanza en UBUNTU — todos los insumos están versionados (`data/inventario-reactivos-v1_1.tsv`, `data/inventario-fd-v1_0.tsv`, `data/manifiesto.yaml`, `forense/notas/2026-08-28-empareja-spec.md`). No abre ningún payload. Precedente: `MAESTRA31-E7 · ETIQUETA` corrió en NUBE.
CARRILES EN PARALELO (declarado): carril NUBE = `E6 (este) → E7 · CANDIDATOS-MARCO-M`; carril CAJA = `E3 · EXTRACTOR-DTA → E8 · MEDICION-COMPUESTA`. Archivos compartidos entre carriles: solo la cascada (`gobernanza`, `estado-programa`, `registro-rotulos`, `firmas-pendientes`, `tests/check.py` T25). Renumera quien fusiona segundo.
════════ ARRANQUE — hazlo antes de leer el resto del encargo ════════
Reporta las cinco líneas de abajo y NO empieces hasta tenerlas. Si algo no cuadra, PARA y repórtalo: encontrar que el terreno no es el que el encargo supone es entregable, no interrupción.
1 · REPO. Localiza el CLON EXISTENTE. No clones uno nuevo salvo que no haya ninguno, y si clonas, dilo. Reporta: ruta absoluta · git log -1 --format="%h %s" · git status ⚠️ No arranques desde el home. Si el cliente avisa "launched in your home directory", cámbiate al clon antes de nada.
2 · SHA. Confirma contra qué base trabajas y compáralo con el que el encargo declara. Si main se movió: NO es PARO — refresca, re-deriva lo que dependa del perímetro, y reporta la diferencia antes de editar.
3 · data/raw. AUSENTE NO ES PARO. Es raíz integrada, gitignorada, resuelta por código; un clon fresco siempre nace sin ella. Se crea o se enlaza. Reporta: existe / la enlacé a <ruta> / la creé. ⚠️ Si este acto DESCARGA algo: verifica al cerrar que los payloads quedaron en el CORPUS COMPARTIDO y no solo en tu worktree. Es el defecto de PR #77 y no lo atrapa ningún test.
4 · ENTORNO. "Ubuntu" no distingue nada; la política de red sí. CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE → esperado: sin_variable curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://www.inegi.org.mx/ Reporta los dos valores crudos. NUNCA curl -I. Si este acto no toca microdato ni red, dilo y salta este punto. ⚠️ [NUEVO v2.11] Un negativo producido por un comando que no examinó archivos no es un negativo (A.13). Todo veredicto negativo —incluida la sonda de este punto— declara cuántos archivos examinó el comando que lo produjo.
5 · ESPEJO. Prohibido derivar cifras del espejo del proyecto: está versiones atrás del repo y contiene archivos que el repo nunca tuvo. Toda cifra sale del clon de (1), con el comando a la vista.
════════════════════════════════════════════════════════════════════
A.2, tercera parte: `ls data/raw/ 2>/dev/null | head -1` — se espera ausente; no importa aquí. Ambos TSV llevan cabecera comentada con `#`: sáltala antes de `csv.DictReader` (a dirección le costó un `KeyError` hoy). Búsquedas en Python UTF-8, nunca `grep`/`sed` de shell sobre texto con acentos (A.13).
═══ VERIFICACIÓN DE EXISTENCIA — contestada por dirección (maestra-32, 30/ago/2026, contra `2c0d4c8`) ═══
1 · ESTRUCTURA. La familia de inventarios ya tiene entrada en `data/INFRAESTRUCTURA-v1_0.md` desde `MAESTRA32-E2` (`ADR-221`, +1 renglón) — verifica al arrancar con `command grep -c "inventario" data/INFRAESTRUCTURA-v1_0.md` ≥ 1 y reporta el conteo. Tablas gobernantes: los dos inventarios, `data/manifiesto.yaml`, `data/emparejamiento-motor-v1_0.tsv`.
2 · CONTENIDO.

* Capa FD nunca etiquetada: `data/emparejamiento-motor-v1_0.tsv` trae, para `G4.horizonte_temporal` lado θ, `variable_id=P4_8_4` y `P4_6_4` con `instrumento="(raiz)"` y `via=texto` — y en `inventario-reactivos-v1_1` ya no existe ningún `(raiz)` (0 sobre 178,246 filas, contado hoy): ese `(raiz)` viene de `inventario-fd-v1_0.tsv`, que `MAESTRA31-E7` no reparó (su perímetro fue solo el inventario de reactivos). EXISTE-NO-SATISFACE: la etiqueta existe, es placeholder.
* Los 39 payloads `(sin-instrumento-derivable)`: 28,799 filas de 178,246 en `v1_1` (contado hoy). `forense/notas/2026-08-27-etiqueta-regla.md:68-76` los descompone: 16 `<año>trim<N>_csv.zip` ("son ENOE por contexto de dominio, no por el nombre"), 13 `<timestamp>.export.CSV.zip`, 2 `DescargaMasivaOD_*.xml`, 3 `banxico_encuesta_competencias_financieras_*.xlsx`, 3 `cses5_*`, 1 `ucdp_ged261`, 1 `zenodo_electoral_*`. La regla v1_1 (`:54`) prohíbe "conocimiento externo de qué encuesta es" — este acto no la viola: deriva desde los campos declarados del propio `manifiesto.yaml` (procedencia registrada del programa), que es dato interno, no conocimiento externo ni posición en el árbol.
* Tablas destino: `data/inventario-fd-v1_1.tsv`, `data/inventario-reactivos-v1_2.tsv`, `data/emparejamiento-motor-v1_1.tsv` → NO-ENCONTRADO (`ls data`, 63 entradas, 0 coincidencias).

3 · COBERTURA RETROACTIVA. `inventario-fd` nació el 27/ago, el mismo día que E7; E7 selló su etiqueta sobre `v1_0` del inventario de reactivos y declaró su perímetro. Los veredictos de `MAESTRA32-E2` (28/ago) se sellaron sobre esas etiquetas — con la reserva explícita en su tabla de que `(raiz)`/`(sin-instrumento-derivable)` no identifican instrumento. Este acto es el re-sello por etiqueta reparada, mismo universo (A.10, corolario 1); precedente exacto: E7 re-corrió la spec congelada de E5 y movió 15 veredictos.
═══════════════════════════════════════════════════════════════════
0-bis · A.3
Primer commit: este encargo verbatim en `forense/encargos/2026-08-30-MAESTRA32-E6-ETIQUETA-v1_2.md`. Al cerrar, `## CONSUMIDO` con el PR.
Premisas, con procedencia

1. Regla vigente de etiqueta: `forense/notas/2026-08-27-etiqueta-regla.md` — salida `familia + año4` por regex sobre el nombre; sin heurísticas de vecino ni conocimiento externo. Resolvió 80 de 119; 39 quedaron honestos.
2. `G4.horizonte_temporal`: θ real (`P4_8_4`/`P4_6_4`) en instrumento no resuelto, "adyacente al proxy ya conocido ENIF P4_10 de G3" (`ADR-221`). Resolver la etiqueta de la capa FD es lo que decide si esa θ tiene instrumento nombrado.
3. La spec de E2 está congelada en `forense/notas/2026-08-28-empareja-spec.md` y corrige, en su cierre, la co-observación: "exige instrumento identificado" (`ADR-221`). Se re-corre sin tocarla.

Verifica estas premisas antes de ejecutar (v2.1). Si alguna no se sostiene, PARA y repórtalo.
Objeto
Tres cosas, en este orden: (i) etiquetar la capa FD con la regla v1_1 tal cual (nunca se le aplicó); (ii) etiquetar los 39 payloads con una regla v1_2 que lee campos declarados del manifiesto; (iii) re-correr la spec congelada de E2 sobre las tablas reparadas y reportar los deltas por par.
COMMIT-1 — especificación congelada ANTES de escribir ninguna tabla
`forense/notas/2026-08-30-etiqueta-v1_2-spec.md`: (a) regla v1_2, exacta: para cada uno de los 39 `payload_id`, se leen con `yaml.safe_load` los campos del manifiesto que el ejecutor lista primero (nombra cuáles existen: p.ej. `nombre`, `descripcion`, `fuente_programa`, `url_origen`, `nota`); se deriva `familia + año4` solo si un campo declarado contiene una familia canónica del corpus (lista cerrada tomada de los valores ya presentes en la columna `instrumento` de `v1_1`) y un año de 4 dígitos; si no, queda `(sin-instrumento-derivable)` — no se fuerza; (b) para la capa FD: la misma regla v1_1 (regex sobre nombre) + la v1_2 sobre sus placeholders, mismo orden; (c) control positivo (patrón E7): toda fila que en `v1_1` ya tenía instrumento distinto de placeholder se compara línea completa contra `v1_2` y debe ser idéntica — cualquier diferencia es PARO; (d) falsador: si la v1_2 resuelve <50% de los 39, se reporta que el manifiesto no lleva la procedencia que hacía falta (hallazgo sobre el manifiesto, no sobre el corpus) y no se itera; (e) B-bis de la re-corrida: los veredictos de E2 pueden moverse solo de `NO-ENCONTRADO`/`EXISTE-NO-SATISFACE` hacia arriba o quedarse (una etiqueta reparada no puede quitar coincidencias — si alguno baja, es bug y PARO); 0 movimientos es informativo (las etiquetas no eran el cuello de botella); ≥1 `EXISTE-SATISFACE` nuevo habilita un medidor sucesor en caja. Cierra con: "el primer resultado que produzca este procedimiento es el que se reporta."
COMMIT-2 — corrida única
`tools/etiqueta_v1_2.py` (nuevo; no edita `tools/inventario_reactivos.py`) → `data/inventario-fd-v1_1.tsv` y `data/inventario-reactivos-v1_2.tsv` (mismo esquema, cabecera comentada declarando la regla y el acto) → re-corrida verbatim de la spec de E2 → `data/emparejamiento-motor-v1_1.tsv` + tabla de deltas por par (antes/después, universo idéntico declarado). `forense/notas/2026-08-30-etiqueta-v1_2-cierre.md` con A.13 en todo conteo: resueltos/39 por motivo, placeholders restantes en FD (antes/después), control positivo (filas comparadas, diferencias = 0), deltas. Intocables verificados con `git diff --stat` vacío: `inventario-reactivos-v1_1.tsv`, `inventario-fd-v1_0.tsv`, `emparejamiento-motor-v1_0.tsv`, `inventario_reactivos.py`, `empareja-spec.md`.
PERÍMETRO Y CONCURRENCIA
Archivos: `forense/encargos/2026-08-30-MAESTRA32-E6-ETIQUETA-v1_2.md` · `forense/notas/2026-08-30-etiqueta-v1_2-spec.md` · `forense/notas/2026-08-30-etiqueta-v1_2-cierre.md` · `tools/etiqueta_v1_2.py` (nuevo) · `data/inventario-fd-v1_1.tsv` · `data/inventario-reactivos-v1_2.tsv` · `data/emparejamiento-motor-v1_1.tsv` (nuevos) · `data/INFRAESTRUCTURA-v1_0.md` (solo si la entrada de inventarios necesita listar las versiones nuevas) · `forense/firmas-pendientes.tsv` (fila nueva) · cascada. No toca `milpa/**`, `tools/inventario_reactivos.py`, ninguna tabla existente. Concurrencia: carril CAJA en paralelo — ver cabecera. "Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo."
FP pre-asignadas
Rango candidato `FP-180`–`FP-181` (máximo hoy `FP-179`; re-deriva; si el carril CAJA lo tomó, siguiente libre y decláralo). Uso: "mesa recibe etiqueta v1_2 + deltas del re-emparejamiento".
ADR y cascada
Candidato re-derivado (deriva, no heredes; renumera quien fusiona segundo). El ADR declara que es re-sello por etiqueta reparada sobre universo idéntico (A.10), y cita los deltas. `registro-rotulos`: `MAESTRA32-E6` censado (token pelado `E6` colisiona con `MAESTRA31-E6`; se censa, no se reclama).
CONTADOR
Placeholders de instrumento: FD `N_antes → N_después`; reactivos `28,799 → N` (re-derivado) · veredictos de E2 movidos: N de 9 — la cifra es la medición, incluido cero.
Lo que este acto NO hace
No mide, no abre microdato, no cambia `ruta:` en `procedencia.yaml`, no edita la spec de E2 ni sus veredictos originales (quedan como historia; `v1_1` los re-sella), no reabre `MAESTRA32-E4 · RE-EMPAREJA` (ese es el re-sello por universo ampliado tras los extractores; este es por etiqueta reparada sobre el mismo universo — dos cosas distintas, declaradas).
Sucesores declarados, no lanzados
`MAESTRA32-E7 · CANDIDATOS-MARCO-M` (mismo carril, tras este merge) · medidor en caja por cada `EXISTE-SATISFACE` nuevo · `MAESTRA32-E4 · RE-EMPAREJA` tras E3(a).

## CONSUMIDO

Ejecutado en la rama `claude/maestra32-e6-cloud-launch-8qu0hw`. Esta sesión NO abre PR — commitea y pushea a la rama designada, mismo patrón que `MAESTRA32-E2`/`MAESTRA32-E5` para no repetir el defecto ya visto en el acto E9. Número de PR: N/D desde esta sesión; quien fusione (dirección, u otra sesión con permiso de abrir PR) llena este campo al abrirlo. Resultado: capa FD (`data/inventario-fd-v1_1.tsv`) reparada 10 de 10 payloads `(raiz)` (100%, primera vez que recibe la regla de etiqueta de `ACTO MAESTRA31-E7`); inventario de reactivos (`data/inventario-reactivos-v1_2.tsv`) con 16 de 39 payloads `(sin-instrumento-derivable)` resueltos por la regla v1_2 nueva (41.0%, falsador de COMMIT-1 <50% disparado, no iterado); re-corrida verbatim de la spec congelada de `MAESTRA32-E2` sobre las tablas reparadas (`data/emparejamiento-motor-v1_1.tsv`) con **0 de 9** veredictos movidos (resultado informativo, no cuello de botella de etiqueta); `ADR-223`; `FP-180` nueva en `forense/firmas-pendientes.tsv`; `MAESTRA32-E6` censado en `canon/registro-rotulos.tsv`; `tests/check.py --baseline` → 19 FAIL · 136 WARN (sin regresión de FAIL), T25 verde, 1 T03 nuevo declarado y no recongelado (cita verbatim de este mismo encargo a `empareja-spec.md` sin prefijo de fecha — no se edita el texto de dirección para complacer a un test). Dos discrepancias declaradas sin bloquear el acto: el rótulo E6/E7 de este acto quedó intercambiado frente a lo que `MAESTRA32-E5` había previsto; y el payload `DescargaMasivaOD_582026_171540_NACIONAL_7930url.xml` no tiene entrada en `data/manifiesto.yaml` (solo en `data/manifiesto-staging.yaml`, con campos vacíos). Detalle completo: `forense/notas/2026-08-30-etiqueta-v1_2-spec.md` (COMMIT-1) y `forense/notas/2026-08-30-etiqueta-v1_2-cierre.md` (A.13 completo, tabla de deltas, control positivo, script de la re-corrida íntegro).
