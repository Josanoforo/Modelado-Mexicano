**ADR-260923-GEN2-CONTADORES-CONSUMO-2-749c-01**, `ACTO GEN2-CONTADORES-CONSUMO-2`, 23/sep/2026, NUBE, Sonnet 5, MODO ABIERTO, COMPUERTA: ninguna. Sucesor de `NC-260923-GEN2-CONTADORES-CONSUMO-1-988c-01` (P-A de CONTADORES-1, PARO-PREMISA). **P1 (acreditación):** `corrida0.py registro --verifica` mostró 32 usos `NO_APTA`, no 2 filas — `_origen_numerico_decisiones` resuelve por RESULT, igual que el precedente `c45c-01`. 16 son de `CALC-GOB-DIGITAL-EXE-EMISIONES-0002` (el CALC que el encargo autoriza, `GOB.gobierno_digital.encig2025.edad_x_escolaridad`): 16 filas `origen_numerico=NUEVO` en `decisiones.tsv`, mismo razonamiento del precedente (hueco de ETIQUETA vía `PISOS-REJILLA-METADATOS`, no de procedencia). `CALC-GOB-DIGITAL-EXE-ADJUDICACION-0001` verificado sin ningún uso activo hoy (cero referencias en `tramite.yaml`/`procedencia.yaml`/`estimadores-por-segmento.yaml`): no requiere fila. Los otros 16 resuelven a `CALC-ENCIG-DUELO-2025-ADJUDICACION-0001` — un TERCER CALC no nombrado en el encargo ni en `FP-988c-01`, `champion_actual` de `edad_x_sexo`/`escolaridad_x_sexo` — FUERA-DE-PERÍMETRO, declarado en `FP-...-749c-01` (nueva) y `NC-...-749c-01` (nueva, PARO-PREMISA). `FP-988c-01` y `FP-657c-04` → `FIRMADA`. **P2 (línea base):** 5 `T-C2-COMPUESTO` (aserciones de estado del marcador viejo) actualizadas con cita en `test_c2_compuesto.py` (22→19 pares RESERVADA, 206/20→174/52 emitidas/adoptadas, caso ENCIG de unidad condicionado a que exista); 30/30 OK. `T-REPRO(g)` 32→16 tras P1. **P3 (canal):** `verify.yml` deriva el marcador antes del registro y deja de descartar `usos.tsv`; los tres derivados (`usos.tsv`, `marcador-segmento.tsv`, `estimadores-por-segmento.yaml`) entran al `git add` del job; `derivados_protegidos.py` sigue protegiéndolos (cabecera DERIVADO ya presente). **P4 (sufijo `-D-C2` general):** se quita la restricción de prefijo en `celdas_validadas.py`; scoping real sigue siendo `adjudicacion_por_celda`. Verificado sin efecto: `celdas_validadas` 219 → 219 (las 4 celdas TRA con `champion_actual: NINGUNO` no declaran `adjudicacion_por_celda`, no se cuentan). Spec humana actualizada en el mismo commit (D-15). Cierra `NC-260923-GEN2-CONTADORES-CONSUMO-1-988c-01` (realineada a 12 columnas, A.16). `adoptados_activos` (post-merge de `origin/main`): 72 → 103. Suite `--rapido` VERDE, 0 FAIL; `check.py --baseline` completo: 16 FAIL (antes 37), todos T-REPRO(g) sobre el CALC fuera de perímetro.
