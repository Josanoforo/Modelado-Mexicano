**ADR-260922-GEN2-TUBERIA-METRICA-RECTORA-1-9512-01**, `ACTO GEN2-TUBERIA-METRICA-RECTORA-1`, 22/sep/2026, NUBE, Sonnet 5, MODO ABIERTO, COMPUERTA: una (§8, igualdad de JSON antes/después de mover, PASA). `celdas_validadas` deja de vivir sólo en `tablero_programa.py`: se mueve a `tools/celdas_validadas.py` (módulo propio, CLI `--json`/`--linea`, bloque `universo` con SHA y archivos leídos); `corrida0 status`, `digesto --mesa` y el tablero la imprimen desde ahí. `cierre_acto.py` deriva `Δceldas_validadas` (0-bis vs árbol actual) y se niega a cerrar si un acto `cuenta_gen2=SI` cierra con Δ=0 sin declararlo. Spec humana + `spec.yaml` en `forense/prereg-caja/`; dos tests nuevos (`_spec`, `_monotonia`) cableados en `verify.yml` como bloqueantes. Métrica sin cambiar de valor: 92 → 92 (Δ0) @ `77553315`. `adoptadas` queda fuera (sin fuente sellada). Cero microdato, cero adopción, cero WARN nuevo.
