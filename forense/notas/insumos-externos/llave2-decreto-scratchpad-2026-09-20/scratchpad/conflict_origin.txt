
**Validaciones externas materiales: `1` de `1`.** *(Población de conteo **propia**, nacida el 25/ago/2026 con `ADR-165` (`ACTO U2-CRUCE`) y declarada por su encargo bajo `v2.3`; **no toca el denominador 27 del Hito D ni el de coeficientes**. Cuenta cruces de una producción propia contra una cifra oficial publicada con su medida de precisión. Antes de este acto valía `0` de `0`: el programa nunca había contrastado nada suyo contra un tercero. El primer renglón es el cruce de los dos totales nacionales de **ENASIC 2022** contra `enasic2022_ipe_cv_ee_ic` — punto propio dentro del `IC95` oficial en los dos estimandos, que era el criterio pre-declarado, y de hecho reproducción **al peso** de los dos totales (`58 594 471` y `128 857 388`, diferencia cero) y de los dos errores estándar de diseño **a la novena cifra decimal**. Veredicto **propuesto, no firmado** — fila 1 de la escala `B-bis` congelada en `bbis-u2-cruce-v1_0.md`, ante mesa por `FP-135`. Registro: `forense/notas/2026-08-25-u2-cruce-cierre.md`.)*
 **L0 · Gobierno — completo y al día.** 165 ADR, protocolo de cambio con retropropagación bidireccional, severidades S1-S5, casillero de pendientes irresueltos. *(Recifrado 164→165, `ACTO U2-CRUCE`, 25/ago/2026: `ADR-165` corre la primera validación externa material del programa — el pipeline reproduce los dos totales nacionales de ENASIC 2022 al peso y sus dos errores estándar de diseño a la novena decimal; veredicto `PIPELINE CORROBORADO` propuesto ante mesa por `FP-135`, y un hallazgo del lado oficial: el renglón `Población total` publica una cifra que incluye los casos que su etiqueta declara excluidos. Candidateó `ADR-164` y renumeró al fusionar segundo contra `ACTO SELLA-AGO25-E`.)* *(Recifrado 163→164, `ACTO SELLA-AGO25-E`, 25/ago/2026: `ADR-164` es un acto de higiene del tablero (CONTADOR: cero) — palomea seis filas `FIRMADA` con evidencia real ya ejecutada y no marcada (`FP-99`, `FP-100`, `FP-101`, `FP-102`, `FP-110`, `FP-119`, todas reciben `ejecutada_en` con cita verificada), añade `A.12` al tablero (generador de candidatas del marco, encargado a Codex por firma D1 de mesa, ya fusionado en `PR #332`/`codex/generador-marco`, `ejecutada_en` lleno) y surge `FP-24` como `LETRA L6 PARA MESA` en `forense/notas/2026-08-25-sella-e.md` sin decidirla. De las siete filas candidateadas por dirección para el barrido (`FP-99`/`FP-100`/`FP-101`/`FP-102`/`FP-110`/`FP-118`/`FP-119`), seis verificaron con evidencia real citable y se marcan; `FP-118` queda deliberadamente sin tocar -- su `estado` en el árbol es `ABIERTA`, no `FIRMADA`, contradiciendo la premisa del barrido, así que no se le llena `ejecutada_en` pese a que su sustancia (`PASO 0 AGOTADO`, `ADR-157`) ya está resuelta -- desviación declarada, para que mesa reconcilie el `estado`. No mueve `milpa/`, ni Hito D, ni llaves de identificación, ni ningún tier -- CONTADOR declarado cero desde el encargo. Candidateado contra el máximo verificado por `grep -oE '^\*\*ADR-[0-9]+' canon/gobernanza-v1_15.md | sort -t- -k2 -n -u | tail -1` sobre `origin/main` re-fetched al momento de commitear: único `163`, sin huecos -- **renumerado a `ADR-164` al fusionar**, colisión con `ADR-163`/`ACTO BIBLIOTECARIO-56` (`PR #333`), regla de la casa: renumera quien fusiona segundo. Ver `ADR-164`, `forense/notas/2026-08-25-sella-e.md`.)* *(Recifrado 162→163, `ACTO BIBLIOTECARIO-56`, 25/ago/2026: `ADR-163` ejecuta la prueba del bibliotecario de `FP-93` sobre las 56 filas del universo del filtro (i) de `ADV1-M1` y mide que la cuota de publicadas del marco piloto revienta — 24 `SI` nuevos, 28 de 60 = 46.7 % contra un tope de 20 %; `FP-133`/`FP-134` nacen `ABIERTAS`.)* *(Recifrado 161→162, `ACTO EVAL-COMPARTAMOS-LLAVE3`, 25/ago/2026: `ADR-162` da al censo de diseño su quinto valor, `DISENO_EXPERIMENTAL`, con criterio propio de cinco campos escrito dentro de `data/diseno-muestral.yaml` (firma de mesa D2 verbatim, `FP-123` `FIRMADA`+ejecutada); abre `analysis_data_AEJ_pub.dta` a nivel de columna (124 variables, 21,523 filas, sin estimar ningún efecto) y confirma asignación `Treatment`/`BTreatment` constante por conglomerado (238 y 34), unidad de análisis persona, N=16,560; y crea `EXP-COMPARTAMOS-1`, **primer renglón de la clase (iii) de `ADR-57(c)` del programa**, `SELLADA_NO_EJERCIDA` — el conteo previo de esa clase se re-derivó y era `0` (el `1` del `grep` ingenuo es mención, no uso). El amarre al motor que la firma exigió se midió y da **negativo por las dos mitades**: 0 de 37 necesidades de `data/curacion-registro/necesidad-objeto-modelo.tsv` lo nombran, y ninguna de las tres piezas del motor (7 clases de `milpa/procedencia.yaml`, 7 valores de `diseno_datos` del contrato de celda-D, 3 tipos de `milpa/refutations.yaml`) tiene clase donde consumir evidencia experimental de terceros — `FP-131`/`FP-132` `ABIERTA`s, propuesta escrita y no implementada. **Renumerado TRES veces por concurrencia**, misma regla de la casa cada vez (renumera quien fusiona segundo): candidateó `ADR-160` sobre `e8ce5ef`, `PR #328` (`R34-CONDA-V2`) lo tomó y pasó a `ADR-161`, `PR #329` (`BANDAS-DOC-6`) tomó ése y quedó en `ADR-162`, rebasado sobre `a5f1bf6`. Sus dos filas de tablero se renumeraron una vez (`FP-129`/`FP-130` → `FP-131`/`FP-132`, por la colisión con `R34-CONDA-V2`) y sobreviven las otras dos. No mueve `milpa/`, ni ningún tier, ni Hito D (`18 de 27`, sin cambio), ni el numerador de llaves ejercidas. Ver `ADR-162`, `forense/notas/2026-08-25-eval-compartamos.md`.)* *(Recifrado 160→161, `ACTO BANDAS-DOC-6`, 25/ago/2026: `ADR-161` ejecuta `FP-94`/`FP-126` — pre-registra, celda por celda, la banda de las seis candidatas documentales `DOC-01`..`DOC-06` (`forense/prereg-duelo-v2/bandas-doc-01-06-v1_0.md`), conforme a la regla que `ADR-135(e)`/`FP-83` selló. `FP-94` y `FP-126` reciben `ejecutada_en`; corrige el texto de `firmada_en` de `FP-94` (citaba `FP-125`, la sucesora real es `FP-126`). `forense/encargos/2026-08-24-RECENSO-DISENO-2.md` — nunca commiteado al árbol pese a haberse ejecutado y fusionado (`PR #321`) — se archiva `CONSUMIDO` una vez dirección aportó el texto verbatim que faltaba, con el resultado real ya conocido (13 llaves, no 37) citado en su `## Cierre`. Candidateó `ADR-159` y colisionó dos veces al fusionar — `159`→`160` por `ACTO SPEC-R10.1-v2` (`PR #327`), `160`→`161` por `ACTO R34-CONDA-V2` (`PR #328`, independiente, condición A de `R3.4`) — re-verificado sobre el árbol fusionado tras cada colisión, misma regla de la casa (renumera quien fusiona segundo). No mueve ningún contador de Hito D ni de medición sobre México. Ver `ADR-161`, `forense/notas/2026-08-25-bandas-doc-6-cierre.md`.)* *(Recifrado 159→160, `ACTO R34-CONDA-V2`, 24/ago/2026: `ADR-160` re-especifica la condición A de `R3.4` sobre pares de una sola variable con variable dependiente declarada, y **PARA** sobre la razón del par — CoDi en cuentas (257.8 mil) contra SPEI en personas físicas (73.5 M), escalas distintas y función de enlace sin firmar (A-bis 3). Veredicto propuesto fila `A3` «no se puede evaluar»; `FP-104` sigue `ABIERTA`, sin firmar. Candidateó `ADR-159` y **colisionó** con `PR #327` (`ACTO SPEC-R10.1-v2`), fusionado mientras este acto estaba abierto: renumerado a `ADR-160` por la regla de la casa —renumera quien fusiona segundo—, y rebasado sobre `e8ce5ef`. No mueve el Hito D (18 de 27) ni ningún tier. Ver `ADR-160`, `forense/notas/2026-08-24-r34-conda-v2-cierre.md`.)* *(Recifrado 158→159, `ACTO SPEC-R10.1-v2`, 25/ago/2026: `ADR-159` redacta el texto operativo de la spec sucesora v2.0 de `R10.1` (PROPUESTA, no sellada) que `FP-108`/`ADR-150` ya había autorizado por GO de mesa. Candidateado contra el máximo verificado sobre el árbol al arrancar (`158`, `ADR-158`/`ACTO ADQ-CORRE-R74R75`, ya fusionado): único `158`, sin huecos — a re-verificar por quien fusione. No mueve `milpa/` ni ningún tier ni el contador de Hito D (`18 de 27`, sin cambio — este acto no adjudica ninguna ficha). Ver `ADR-159`, `forense/notas/2026-08-25-r10-1-spec-v2-propuesta-cierre.md`.)* *(Recifrado 157→158, `ACTO ADQ-CORRE-R74R75`, 24/ago/2026: `ADR-158` adquiere las tres fuentes reales de eventos que la cola nombraba (GDELT, Mass Mobilization, UCDP — de las cinco filas de la cola, dos resultaron ser el mismo payload que una tercera) y archiva `R7.4`/`R7.5` → `D`, falsador compartido inejecutable por diseño de instrumento en las tres. Candidateado contra el máximo verificado sobre `origin/main = b053491` (`157`, `ADR-157`/`ACTO CAL-G3-PUNTUAL`, ya fusionado, sin movimiento durante todo el acto): único `158`, sin huecos — a re-verificar por quien fusione. No mueve `milpa/` ni ningún tier. Ver `ADR-158`, `forense/notas/2026-08-24-adq-corre-r74r75-cierre.md`.)* *(Recifrado 156→157, `ACTO CAL-G3-PUNTUAL`, 24/ago/2026: `ADR-157` ejerce por primera vez la llave `CAL-G3` (panel ENNViH, clase (i) de `ADR-57(c)`) con diseño intra-persona propio — primeras diferencias ponderadas sobre olas 2-3, θ=horizonte temporal declarado (`pr02`, módulo `PR`, ordinal 1-7)→ahorros (`cr27`), N=6,305, β=+0.0146, IC95%=[+0.0047,+0.0245] bajo supuesto MAS (PASO 0 AGOTADO: ENNViH no trae UPM/estrato de diseño en ningún `.dta` ni documento de las tres olas, reproducido de forma independiente en este acto; espejo académico RAND/ICPSR — 3 URLs — responde 403 real de AWS WAF, confirmado con sandbox de red deshabilitado). Signo opuesto al `−0.60` asignado en el generador; comparación solo en signo por instrucción del encargo, escalas sin enlace declarado. Firma `EJERCIDA_ACOTA`; llaves de identificación ejercidas `2→3` de `3`, sin fila `SELLADA_NO_EJERCIDA` restante. β queda PROPUESTO (`forense/firmas-pendientes.tsv` `FP-127`), no escrito en `milpa/procedencia.yaml`. Candidateado contra el máximo verificado por `grep` sobre el árbol al arrancar (`154`, ya fusionado, `origin/main` con `RECENSO-DISEÑO-2` `f154fd9`): único `157`, sin huecos (colisión doble con `ADR-155`/`ADR-156`, renumerado al fusionar). No mueve ningún contador de Hito D. Ver `ADR-157`, `forense/notas/2026-08-24-cal-g3-puntual-cierre.md`.)*  *(Recifrado 155→156, `ACTO PYPDF-REBARRIDO-B`, 24/ago/2026: `ADR-156` deriva el universo del problema que `FP-111`/`FP-122` dejaron sin derivar -- cuantos veredictos negativos del corpus se apoyan en `pypdf` sin segundo extractor -- barriendo 334 archivos. Universo real: **1**, el mismo que `FP-111` ya nombraba (`data/coef-universo-v1_0.tsv:14`). Re-corrida con union `pdftotext -layout` + `pypdf` sobre los 6 PDF de ENOE (sha256 verificado, 603,465 caracteres): **SOSTIENE**, ningun termino nuevo, cero enmiendas in situ. `FP-111`/`FP-122` reciben `ejecutada_en`. Numerado `ADR-155` al arrancar (`origin/main = ec8da5b`), renumerado a `ADR-156` por la colision con `SELLA-AGO24-C-v2` (PR #322), que fusiono primero y tomo `ADR-155`; regla de la casa, renumera quien fusiona segundo. Verificado sobre el arbol fusionado (`origin/main = df28426`): maximo `155`, unico `156`, sin huecos ni duplicados. No mueve ningun contador de Hito D ni de medicion sobre Mexico. Ver `ADR-156`, `forense/notas/2026-08-24-pypdf-rebarrido.md`.)* *(Recifrado 154→155, `ACTO SELLA-AGO24-C-v2`, 24/ago/2026: `ADR-155` propaga las ocho firmas de mesa del 24/ago que la cola humana se saltó (`FP-112`, `F5`, `FP-109`, `FP-70`, `FP-97`, `FP-101`, `FP-98`, `FP-94`), archiva `R7.3` → `C` — primera fila `C` del programa — y mueve el Hito D de `15` a `16 de 27`. Numerado `ADR-153` al arrancar, renumerado a `ADR-154` por la colisión con `RECENSO-DISEÑO-2` (PR #321) y **de nuevo a `ADR-155`** por la colisión con `MARCO-SATURA-CODEX` (PR #323), que fusionó primero y tomó `ADR-154` para su PARO; regla de la casa, renumera quien fusiona segundo. Verificado sobre el árbol fusionado con `grep -oE '^\*\*ADR-[0-9]+' canon/gobernanza-v1_15.md | sort -t- -k2 -n -u | tail -1`: máximo `154`, único `155`, sin huecos ni duplicados. Detalle: `forense/notas/2026-08-24-sella-c-v2.md`.)* *(Recifrado 153→154, `ACTO MARCO-SATURA-CODEX`, 24/ago/2026: `ADR-154` PARA la ampliación a saturación del marco de candidatas del piloto (`FP-82`/`ADR-135(d)`) — `tools/curador_registro/` existe (confirmado contra `PR #164`/`#244`) pero es un curador de relaciones de adquisición de fuente (terna `necesidad_id`/`fuente_canonica_normalizada`/`objeto_evidencia_id_canonico`), sin solape con las 17 columnas de `marco-candidatas-piloto-v1_0.tsv`; sin `baseline.json`/`relaciones.tsv` vivo en el árbol para correrlo contra ese dominio. El barrido nunca arrancó — hallazgo de instrumento equivocado, no saturación medida. `60` de `60` sin movimiento, `FP-93` sigue `ABIERTA`. Candidateado contra el máximo verificado por `grep` sobre el árbol al arrancar (`153`, ya fusionado, `origin/main = f154fd9`): único `154`, sin huecos. No mueve ningún contador de Hito D ni de medición sobre México. Ver `ADR-154`, `forense/notas/2026-08-24-marco-satura-codex-cierre.md`.)* *(Recifrado 152→153, `ACTO RECENSO-DISEÑO-2`, 24/ago/2026: `ADR-153` re-deriva la cobertura retroactiva de `data/diseno-muestral.yaml` que `FP-117` había cifrado en 37 llaves — la re-derivación por `yaml.safe_load` cruzando `data/manifiesto.yaml` contra `data/diseno-muestral.yaml` da 13, no 37 (5 INEGI + 8 internacionales; 2 buckets eran duplicados de filas existentes — ENSANUT, LAPOP — y ~13 hosts eran documentación/administrativo sin identidad de fuente propia, no llaves). Altas: 2 `MAPEADO` (ENGASTO, WVS7), 3 `SIN_DISEÑO_PUBLICADO` (ISSP/ZA6980, CSES, GPS), 7 `PENDIENTE`, 1 `NO_APLICA_REGISTRO_ADMINISTRATIVO` (OSF). `data/diseno-muestral.yaml`: 43→56 filas. Cierra `FP-117` y `FP-120`; abre `FP-123` (RCT sin vocabulario claro en el censo). Candidateado contra el máximo verificado por `grep` sobre el árbol al arrancar (`152`, ya fusionado): único `153`, sin huecos. No mueve ningún contador de Hito D ni de medición sobre México. Ver `ADR-153`, `forense/notas/2026-08-24-recenso-diseno-2.md`.)* *(Recifrado 151→152, `ACTO ADQ-DISENO-1`, 24/ago/2026: `ADR-152` declara `/microdatos/` ruta canónica de ENOE en adelante y cierra la re-adquisición de las 28 olas post-2019 (2019T1-2026T1, hueco real 2020T2 por COVID), ids sufijados `_microdatos` en `data/manifiesto.yaml`, sin sobreescribir las 20 entradas preexistentes vía `/datosabiertos/` (`FP-110`, historia paralela). T1 confirma sin resolver `FP-118` (sitemap completo de `ennvih-mxfls.org`, 314 URLs, ningún nombre de campo para UPM, solo notación matemática); T2 agota 9 rutas para el diseño muestral ENPOL 2021, `FP-114` sigue `FIRMADA` sin resolver; T3 agota 12 URLs para los FD de ENSAFI 2023/ENSU 2025 (control positivo con ENASIC 2022), ambos `NO-ENCONTRADO`, `FP-115` sigue `FIRMADA` sin resolver. Candidateado contra el máximo verificado por `grep` sobre el árbol al arrancar (`151`, `ADR-151`/`ACTO COMMIT-DOC-COERCION`, ya fusionado, `origin/main = 5bef289`): único `152`, sin huecos. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 152. No mueve ningún contador de Hito D ni de medición sobre México. Ver `ADR-152`, `forense/notas/2026-08-24-adq-diseno-1-cierre.md`.)* *(Recifrado 150→151, `ACTO COMMIT-DOC-COERCION`, 24/ago/2026: `ADR-151` commitea el documento íntegro `forense/COERCION-Y-ADOPCION-rediseno-2026-08-20.md`, adjunto de mesa recibido el 24/ago con `sha256 f77d705e…c107f5cb`, rotulado `PROPUESTA (no sellada)` en cabecera nueva de una línea — la única edición permitida sobre el adjunto. `FP-113` cierra `FIRMADA`, `ejecutada_en` `ADR-151`; la parcial `forense/coercion-adopcion-espec-operativa-v0_1.md` queda marcada `SUPERADA` en cabecera, no borrada. Candidateado contra el máximo verificado por `grep` sobre el árbol al arrancar (`150`, `ADR-150`/`ACTO SELLA-AGO24-D`, ya fusionado, `origin/main = 8791bcf`): único `151`, sin huecos. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 151. No mueve ningún contador de Hito D ni de medición sobre México. Ver `ADR-151`, `forense/notas/2026-08-24-commit-doc-coercion.md`.)* *(Recifrado 149→150, `ACTO SELLA-AGO24-D`, 24/ago/2026: `ADR-150` propaga las trece firmas de mesa del 24/ago sobre `firmas-pendientes.tsv` (siete `FIRMADA+ejecutada`, cinco `FIRMADA` sin ejecutar con cuatro sucesores nuevos `FP-119`-`FP-122`, `FP-113` sigue `ABIERTA`) y encuentra que el clon de partida de este acto no traía las ramas `codex/*` fusionadas hasta `git fetch --unshallow` — hallazgo CODEX, `forense/hallazgos.md`. Candidateado contra el máximo verificado por `grep` sobre el árbol al arrancar (`149`, `ADR-149`/`ACTO RECENSO-DISEÑO-14`, ya fusionado, `origin/main = a664ea5`): único `150`, sin huecos. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 150. No mueve ningún contador de Hito D ni de medición sobre México. Ver `ADR-150`, `forense/notas/2026-08-24-sella-d.md`.)* *(Recifrado 148→149, `ACTO RECENSO-DISEÑO-14`, 24/ago/2026: `ADR-149` ejecuta el re-censo de diseño muestral que `ADR-135(f)` ordenó y `FP-95` dejó pendiente, y encuentra que el universo son **18** fuentes y no las 14 que citaban `FP-84`/`ADR-135` — subconteo en el origen, no deriva del universo: ninguna de las 18 se adquirió después del 20/ago. Recifrado autorizado por adenda de dirección tras `PARA` correcto del ejecutor: el perímetro del encargo no incluía este archivo, que cifra el conteo de ADR en cuatro líneas — misma clase de perímetro corto que `ADR-147(c)` registró el mismo día. Candidateó `ADR-148` al escribir sobre `origin/main = 22d792f` (máximo `147`, sin duplicados ni huecos) y **renumeró a `ADR-149` al fusionar segundo**: `AMPLIA-MARCO-SATURA` (`PR #316`) tomó el `148` mientras este acto estaba en revisión, exactamente la colisión que la cláusula de concurrencia del encargo anticipaba. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 149. No mueve ningún contador de Hito D ni de medición sobre México.)* *(Recifrado 147→148, `ACTO AMPLIA-MARCO-SATURA`, 24/ago/2026: `ADR-148` PARA la ampliación del marco de candidatas a saturación que `ADR-135(d)`/`FP-82` ordenan — la "infraestructura CODEX" que la firma cita no se localiza en el árbol, y el crosswalk pregunta↔regla pasada 1 (el único insumo legible sin red ni microdato) aporta cero candidatas nuevas contra el marco existente. `60 de 60` sin movimiento — ver la línea de candidatas del marco, abajo en esta misma sección. Extensión mínima de perímetro declarada aquí, mismo criterio que `ADR-130` ya fijó para esta misma línea de conteo: el encargo restringe la edición de este archivo a "solo la línea de candidatas del marco", pero el recifrado del conteo de ADR es mecánico y lo arrastra todo ADR, precedente ya sentado por `ADR-147(c)`. Candidateado contra el máximo verificado por `grep` sobre el árbol al arrancar (`147`, `ADR-147`/`ACTO REPARA-PROPAGA-15`, ya fusionado): único `148`, sin huecos. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 148. No mueve ningún contador de Hito D ni de medición sobre México.)* *(Recifrado 146→147, `ACTO REPARA-PROPAGA-15`, 24/ago/2026: `ADR-147` cierra por no-existencia el defecto que `CORROBORA-MOTOR` reportó — README.md y tres citas de `14 de 27` en `canon/modelo-decision-v4_0.md` presuntamente fuera del perímetro de `SELLA-AGO24` — verificando por `grep` que las tres citas vivas ya decían `15 de 27`; el `14 de 27` hallado era narración fechada de la bitácora de cada línea, historia que no se toca. Recifrado autorizado por adenda de dirección tras `PARA` correcto del ejecutor: el encargo original mandaba emitir el ADR sin el recifrado que todo ADR arrastra, perímetro corto de dirección. Candidateado contra el máximo verificado por `grep` sobre el árbol al arrancar (`146`, `ADR-146`/`ACTO EMISOR-M-2`, ya fusionado): único `147`, sin huecos. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 147. No mueve ningún contador de Hito D ni de medición sobre México.)* *(Recifrado 145→146, `ACTO EMISOR-M-2`, 24/ago/2026: `ADR-146` da al emisor dos variables dependientes (`cumplimiento`, `adopcion`) y seis disparadores por componente, ejecutando la reformulación de `FP-104` (`D3`, `ADR-145`); validación dura en `milpa/src/emisor.py` (`valida_dv_celda_m2`), estampa de base extendida (`estampa_base_extendida_m2`: casi ninguno tiene base medida), 7 tests nuevos en `tests/test_emisor_m2.py`. `FP-113` nueva (documento fuente pendiente de mesa). Candidateado contra el máximo verificado por `grep` sobre el árbol al arrancar (`145`, `ADR-145`/`ACTO SELLA-AGO24`, ya fusionado, `origin/main = bcd318f`): único `146`, sin huecos. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 146. No mueve ningún contador de Hito D ni de medición sobre México.)* *(Recifrado 144→145, `ACTO SELLA-AGO24`, 24/ago/2026: `ADR-145` propaga tres firmas de mesa dictadas el 24/ago/2026 sobre `firmas-pendientes.tsv` — adjudica `FP-105` (`R7.1` → `A`, firma D1) y `FP-106` (naturaleza de una fila `C`, caso por caso, firma D2), reformula `FP-104` para `EMISOR-M-2` (firma D3), y registra `D4` como declinación explícita de un pre-registro sobre la celda de filtro (v) de `ADV1-M1`, no como adjudicación. `Hito D`: `14 de 27 → 15 de 27`. Nace `FP-112`. Candidateado contra el máximo verificado por `grep` sobre el árbol al arrancar (`144`, `ADR-144`/`ACTO ADQ-ENOE-PRE2019`, `PR #310`, ya fusionado, `origin/main = bcd318f`): único `145`, sin huecos. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 145.)* *(Recifrado 143→144, `ACTO ADQ-ENOE-PRE2019`, 20/ago/2026: `ADR-144` ejecuta la opción (b) de `FP-64` — 27 payloads ENOE anteriores al decreto de la Zona Libre de la Frontera Norte adquiridos, registrados y verificados `27/27 COINCIDE`; la Razón 1 se extiende al instrumento pre-2019 con universo declarado (16 PDF, 5,021,037 caracteres, `SOLO_PRE = 0` sobre el inventario completo de variables); `R5.1-D2` y `R5.1-D3` adjudicadas clase **(ii)** de `ADR-57(c)`. `FP-64` cierra `ABIERTA`→`FIRMADA`; nacen `FP-109`/`FP-110`/`FP-111`. Candidateado contra el máximo verificado por `grep` sobre el árbol al arrancar (`142`, `ADR-142`/`ACTO INSTRUCCIONES-v2_11`, `PR #309`, ya fusionado): único `143`, sin huecos — y **renumerado de `ADR-143` a `ADR-144` al fusionar `origin/main = 496e1e2`**, porque `PR #311`/`ACTO RETRIAGE-4` fusionó primero y su ADR tomó el `143`; las tres filas de tablero se renumeran por la misma colisión (`FP-105`/`FP-106`/`FP-107` → `FP-109`/`FP-110`/`FP-111`). Re-derivado sobre el árbol fusionado: único `144`, máximo `144`, sin huecos. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 144.)* *(Recifrado 142→143, `ACTO RETRIAGE-4`, 20/ago/2026: `ADR-138` corre las cuatro fichas del Hito D sin triar, archiva `R8.1` → `D` y mueve `13 de 27` → `14 de 27` — el primer contador de medición que se mueve desde el 5/ago/2026. Candidateado contra `origin/main = 54da215` con máximo verificado `137` y **renumerado de `ADR-138` a `ADR-143` al fusionar**: `origin/main` avanzó 13 commits durante el acto y tres actos tomaron `138`…`142`. Re-derivado por `grep` sobre el árbol fusionado (`89c939b`): único `143`, máximo `143`, sin huecos. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 143.)* *(Recifrado 141→142, `ACTO INSTRUCCIONES-v2_11`, 21/ago/2026: `ADR-142` sella `instrucciones-proyecto-v2_11.md` — copia íntegra de v2.10 más `A.13` (un negativo sin conteo de archivos examinados no es un negativo) y la línea nueva del punto 4 del ARRANQUE. `FP-100` cierra `ABIERTA`→`FIRMADA`. Candidateado contra el máximo verificado por `grep` sobre el árbol al arrancar (`141`, `ADR-141`/`ACTO SELLA-OPLUS`, `PR #308`, ya fusionado): único `142`, sin huecos. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 142.)* *(Recifrado 140→141, `ACTO SELLA-OPLUS`, 21/ago/2026: `ADR-141` sella la definición mecánica del operador `⊕` del corredor `E` — `E = mediana_por_cuantil({L-solo, L+corpus, M})`, tres corredores, peso igual, sin entrenar — firma de mesa verbatim «D-a». `FP-99` cierra `ABIERTA`→`FIRMADA`. Candidateado contra el máximo verificado por `grep` sobre el árbol al arrancar (`140`, `ADR-140`/`ACTO SELLA-C`, `PR #307`, ya fusionado): único `141`, sin huecos. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 141.)* *(Recifrado 139→140, `ACTO SELLA-C`, 20/ago/2026: `ADR-140` adjudica `FP-91` con la firma de mesa «C: 2 mueren y ADV1-M6 se reescribe» — los siete umbrales de `ADR-68(c)` mueren (ya `VENCIDO EN ALCANCE`, `ADR-128(b)`) y `ADV1-M6` se reescribe como `ADV1-M6 v2`, cuyos tres criterios de resultado son la nueva compuerta formal de apertura de la fase de cálculo. `FP-91` cierra `ABIERTA`→`FIRMADA`. Candidateado originalmente contra el máximo verificado `137`: único `138`, sin huecos. `origin/main` fusionó `PR #305` (`ACTO LANDING-EMISOR-M1`/`ADR-138`, `ACTO EMISOR-M-1b`/`ADR-139`) mientras esta rama seguía abierta — colisión de numeración, regla de la casa, quien fusiona segundo renumera: re-derivado tras el merge contra el máximo real (`139`, ya fusionado) → `140`, único, sin huecos. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 140.)* *(Recifrado 138→139, `ACTO EMISOR-M-1b`, 21/ago/2026: `ADR-139` cierra el hueco de `ADR-138(c)` — archiva `forense/TRANSFER-EMISOR-M-2026-08-20.md` (los otros tres documentos ya estaban de un commit de seguimiento previo) y sustituye el emisor por la versión con estampa de base empírica. Candidateado contra el máximo verificado por `grep`: único `138` ya fusionado, único `139`, máximo `139`, sin huecos. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 139.)* *(Recifrado 137→138, `ACTO LANDING-EMISOR-M1`, 21/ago/2026: `ADR-138` aterriza el código del emisor del corredor `M` (`milpa/src/emisor.py`, dos suites de test, crosswalk pregunta↔regla v1.0) y ejecuta la firma de mesa `Q1-bis`. `main` fusionó `PR #306`/`ACTO REPARA-T22` como `ADR-137` mientras esta rama seguía abierta — regla de la casa, quien fusiona segundo renumera: candidateado originalmente `137` contra `136`, re-derivado tras el merge contra el máximo real (`137`, ya fusionado) → `138`, único, sin huecos. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 138.)* *(Recifrado 136→137, `ACTO REPARA-T22`, 21/ago/2026: `ADR-137` repara dos defectos medidos de `T22` y sube cuatro decisiones escapadas (`FP-99`-`FP-102`) al tablero. Candidateado contra `origin/main = fc7f0c7` (`PR #304` ya fusionado) con máximo verificado `136`: único `137`, sin huecos, re-derivado por `grep`. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 137.)* *(Recifrado 135→136, `ACTO SELLA-M5-V2`, 20/ago/2026: `ADR-136` sella `ADV1-M5 v2` como capa de cómputo sobre la escala de cinco casillas del piloto (eje unificado, márgenes declarados antes, veredictos por posición del intervalo, secuencia fija) y cumple `D-ii` — la primera celda puntuada del piloto deja de estar bloqueada. Candidateado contra `origin/main = 8b73aee` (`PR #303`) con máximo verificado `135` ya fusionado: único `136`, máximo `136`, sin huecos, re-derivado por `grep`. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 136.)* *(Recifrado 134→135, `ACTO SELLA-MESA-6`, 20/ago/2026: `ADR-135` sella `FP-79`…`FP-84` (las seis filas `ABIERTA` que el marco de candidatas del piloto había dejado abiertas, `ACTO ACT-PIL-2 · MARCO-M1-A`), cerrando su ficha en `forense/encargos/2026-08-20-SELLA-MESA-6.md` y su nota de cierre en `forense/notas/2026-08-20-sella-mesa-6-cierre.md` (renombrada en este mismo acto desde `2026-08-20-sella-mesa-6.md` para no colisionar por nombre normalizado con la ficha de encargo, `T02`). Candidateado contra `origin/main` con máximo verificado `134` ya fusionado en esta rama: único `135`, máximo `135`, sin huecos, re-derivado por `grep`. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 135.)* *(Recifrado 133→134, `ACTO APERTURA-ENFIH-ENSAFI`, 20/ago/2026: `ADR-134` deriva 4 celdas objetivo de `coef-universo-v1_0.tsv` —`N3`/`N4`/`N11`/`N15`— y las busca en `ENSAFI 2023`/`ENFIH 2019` con protocolo pre-registrado: `0` de `8` celdas en `EXISTE-SATISFACE`, ninguna `SIN-RUTA` gana ruta (`4`→`4`); tercera colisión de numeración de este mismo acto al fusionar (`ADR-132`→`ADR-133` contra `T-SELLO`, ahora `ADR-133`→`ADR-134` contra `PR #301`/`ACTO LOTE-MOTOR2`, contenido no relacionado — `MOTOR-2`/`ENIGH`). Candidateado contra `origin/main = 126f1a5` ya fusionado en esta rama: único `134`, máximo `134`, sin huecos, re-derivado por `grep`. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 134.)* *(Recifrado 132→133, `LOTE-MOTOR2`, 20/ago/2026: `ADR-133` descarga la condición de `ADR-100(9)` sobre `M2`/`M4`/`M5` — re-verificadas contra el universo nuevo de `BARRIDO-2` (`data/curacion-universo/`, cerrado por `ADR-108`/`ADR-109`), las tres SOBREVIVEN sin cambio por dominio disjunto (activos INEGI internos vs. instrumento ENIGH + libro de demanda del curador, verificado por comando: `grep` sobre el catálogo de momentos, cruce Python catálogo↔libro de demanda, `cut`/`sort -u` sobre las fuentes del libro de demanda). Sello `MOTOR-2` (`FP-01`..`FP-06`) queda firme sin excepción. `FP-26` cierra: las ocho etapas de su cadena (adjudicación `FP-10`/`FP-12` · re-verificación `M2`/`M4`/`M5` y sello `MOTOR-2` · `E0` · `FP-15` · `ficha-id-g3`/`FP-11` · `E3-TRIAGE`/`FP-14` · `T20`/`FP-18` · descargas `FP-17`) verificadas una por una con evidencia. Re-derivado por `grep` sobre el árbol fusionado (`origin/main = a72ead3`, `PR #299`): único `132`, máximo `132`, sin huecos. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 133.)* *(Recifrado 131→132, `ACTO T-SELLO`, 20/ago/2026: `ADR-132` sella `D-i`–`D-iv` del careo de dirección (`TRANSFER-MAESTRA-FASE-CALCULO-2026-08-19.md` §4 = `CAREO-ADV-DUELO-diseno-v2-2026-08-19.md` §C), declara ABIERTA en canon la Fase de Cálculo bajo `APERTURA-FASE-CALCULO-v1_2.md`, y registra sin resolver la contradicción entre `ADR-128`/`ADV1-M6`/`PLAN-CALCULO-TOTAL` OLA 4 sobre el criterio de cierre de `PILOTO-E1E3`. Este acto **también candidateó `ADR-130` contra `906203a` y renumeró**: `ACT-PIL-2` fusionó primero (`PR #297`, `867948c`) mientras este acto corría — misma regla de la casa que `ADR-130` ya pagó contra `ADR-129`, quien fusiona segundo renumera. Re-derivado por `grep` sobre el árbol ya fusionado dentro de esta rama: único `131`, máximo `131`, sin huecos. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 132.)* *(Recifrado 130→131, `ACTO LOTE-RETRIAGE`, 20/ago/2026: `ADR-131` para `T1`-`T5` ficha por ficha —cinco `PARO` independientes— porque las cinco reglas que su encargo manda correr ya son cinco de las trece que el contador de `Hito D` cuenta, y deriva ese `13 de 27` con receta probada contra tres controles y el oráculo `T18`; encuentra además dos fichas `B-bis` que el encargo no sabía que existían y una cifra falsa (`44` contra `76` medido) en una ficha sellada. Candidateado contra el árbol de `867948c` ya fusionado en esta rama: único `131`, máximo `131`, sin huecos, re-derivado por `grep` y no por aritmética. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 131.)* *(Recifrado 129→130, `ACT-PIL-2 · MARCO-M1-A`, 20/ago/2026: `ADR-130` construye el marco de candidatas del piloto bajo `ADV1-M1` (60 filas, cero de memoria) y localiza en el árbol la cifra del acuerdo `CAC-007/01/2018` que hasta hoy sólo se citaba sin número{cita-historica}. Este acto **candidateó `ADR-129` contra `7d38cb0` y renumeró**: `ACT-PIL-1` corrió en paralelo por exigencia de `ADV1-M1` y fusionó primero (`PR #296`, `906203a`) — regla de la casa, quien fusiona segundo renumera. Re-derivado por `grep` sobre el árbol ya fusionado dentro de esta rama, no por aritmética: único `130`, máximo `130`, sin huecos. `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 130.)* *(Recifrado 128→129, `ACT-PIL-1 · CONTRATO-v0_5`, 20/ago/2026: `ADR-129` sube el contrato celda-D a v0.5 (cinco cambios de vocabulario, D-2/D-4/D-5 de `ADR-128`) y revierte por diseño la ampliación de `T25` al espacio `D` — colisiona con 46+ usos pre-existentes de `Dn` pelado ajenos a `ADR-128`, ver la nota del acto — candidateado contra el HEAD de esta rama (único `129`, máximo `129`, sin huecos). `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 129.)* *(Recifrado 127→128, `ACTO SELLA-ADV`, 20/ago/2026: `ADR-128` sella el aterrizaje de los documentos adversariales del duelo L-vs-M, el vencimiento en alcance de `ADR-68(b)`/`(c)` y las firmas `D-4`/`D-5`/`D-6` — candidateado contra `5a60e98` (único `128`, máximo `128`, sin huecos, `git fetch` re-verificado sin cambio de `origin/main` al escribir). `gobernanza` (cabecera) y `estado` (cabecera y aquí) citan 128.)* *(Recifrado 125→126→127: `LOTE UBUNTU-ADQ-1` (`ADR-126`, `PR #294`) y `LOTE·NUBE-DECISIONES-1` (candidateó `126`, colisionó, renumerado a `ADR-127` al fusionar segundo) corrieron concurrentes contra la misma base `125`; ninguno de los dos tenía la cifra del árbol fusionado — re-derivado sobre el árbol fusionado (`48903d3`), no por aritmética. Corregido 29/jul/2026: decía 32, contradiciendo §0 de este mismo archivo — `censo-integridad-v1_0.md` C1-02.
