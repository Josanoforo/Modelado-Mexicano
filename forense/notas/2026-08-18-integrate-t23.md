# Nota del acto · INTEGRATE-T23 — el integrador deja de abortar y el vigía del cableado nace inactivo

18/ago/2026 · rama `claude/acto-t23-integrador-cableado-is3bv2` · SHA de arranque `3e4c9f7` (verificado contra `origin/main` por `git ls-remote`, no se había movido). Clon des-superficializado al arrancar (`git fetch --unshallow`: `git rev-parse --is-shallow-repository` pasó de `true` a `false`).

## §0 · Arranque, verificado por comando

`data/raw/` ausente — declarado, no paro (este acto no la usa). Sonda INEGI: `curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://www.inegi.org.mx/` → `000` (falla de conexión, allowlist de esta caja de nube, no señal sobre INEGI — A.5). Ninguna de las dos cosas afecta a este acto: código + pruebas, sin corpus.

Suite al arrancar: `python3 tests/check.py` → **19 FAIL · 135 WARN**; `--baseline` → VERDE contra `997482bbda18b52621e24909eedbed0630c7a111` (re-derivado, coincide con lo declarado en el encargo). `python3 tests/check.py --require-cableado` daba, antes de este acto, el mismo `19 FAIL · 135 WARN` — la bandera se ignoraba en silencio, confirmado antes de tocar nada.

## §1 · Verificación de existencia, re-derivada

```
grep -n "def preflight" tools/curador_registro/integrate_barrido2.py → 137:def preflight(  (consumida en :406)
grep -cE "T23|T-CABLEADO|require-cableado" tests/check.py            → 0
grep -nE "^def t[0-9]+" tests/check.py → t01..t22 presentes (t19 como t19a/t19b/t19c); T23 es el siguiente número libre
python3 -c "…real=len(set(nums))…" sobre canon/gobernanza-v1_15.md → 97 ADR, sin huecos, ADR-97 es el más alto
```

`ADR-97` (`T16-HISTÓRICAS`, 18/ago) ya existía al arrancar este acto — el número libre para este cierre es **ADR-98**, no heredado.

## Commit 1 · `integrate_barrido2.py` — una ALTA validada no es un error

**Defecto (C5, `forense/notas/2026-08-18-b2-transfer.md`):** `preflight()` (`:137`) añadía `ALTA_REQUIERE_HIGH_PATH_NO_IMPLEMENTADO` a la lista de errores para toda `PROPUESTA_ALTA` con `estado_supervision=VALIDADA`, y como `preflight` alimenta `checked["ok"]`, eso abortaba el lote completo — ninguna propuesta ordinaria se procesaba, ni siquiera las que no tenían nada que ver con la ALTA.

**Arreglo, acotado a lo que el encargo pide:**
- `preflight()` ya no genera ese error; solo comprueba joins/hashes (que la fila ya pasa).
- `_apply_layer4()` distingue, dentro del grupo agrupado por `relacion_id_actual`, si las filas `ALTA` del grupo están `supervised` (ya calculado ahí mismo para la rama de abajo): si sí, `estado_integracion=PROPUESTA_ALTA` con `razon_integracion` que empieza `WARN:` y no entra a `accepted` (no se integra nada de ella); si no, sigue `RECHAZADA_FAIL_CLOSED` como antes — comportamiento sin cambiar para el caso no cubierto por el encargo.
- `propuestas_altas_validadas` en el camino de éxito estaba hardcodeado a `0` — invisible mientras la ALTA validada siempre abortaba antes de llegar ahí. Corregido a la misma cuenta que ya usaba el camino de aborto.
- `INTEGRATION_STATES` gana `PROPUESTA_ALTA` — documenta el valor nuevo que el módulo ahora puede escribir; no lo consume ningún otro módulo hoy (verificado, `grep -rn INTEGRATION_STATES` solo devuelve su propia definición), pero es el registro correcto de valores válidos de `estado_integracion` para quien construya `build_cableado.py` (C6) después.

**Leído antes de tocar (el encargo lo exige):** el CLI real (`tools/curador_registro/integrate.py:main`, subcomando `--barrido2`) y los dos schemas (`barrido2-semantic-proposal.schema.json`, `barrido2-cableado-row.schema.json`). El segundo ya declara `PROPUESTA_ALTA` en su enum de `estado_integracion` — el valor no se inventa, ya estaba anticipado por el schema congelado; este commit es la primera vez que el integrador lo escribe.

**Pruebas (`tools/curador_registro/tests/test_integrate_barrido2.py`), las cuatro exigidas:**

El fixture gana una segunda relación/tarea/reporte real (`REL-00e7bd28c9c0db01fd89a3be`, N24/ENIF — distinta de la relación "gemela" que ya usaban las dos pruebas del acto anterior), necesaria porque `_apply_layer4` agrupa por `relacion_id_actual`: sin una segunda relación, la ALTA y la propuesta ordinaria caerían en el mismo grupo y contaminarían el resultado la una de la otra. `reports.tsv`/`ledger.tsv`/`tasks.tsv` pasan a llevar dos filas cada uno; el `material_baseline.json` se hashea sobre ambos juntos. Las dos pruebas preexistentes no cambian su comportamiento (la segunda relación queda sin usar en ellas) — corridas de nuevo tras el cambio de fixture, siguen verdes.

- **(a)** `test_validated_alta_does_not_abort_the_batch_and_stays_pending`: lote con una propuesta ordinaria + una ALTA validada → `ok=True`, sin `errors`, `propuestas_altas_validadas=1`, la ordinaria queda `INTEGRADA` (su relación cambia `capa4_apertura_mapeo`).
- **(b)** la decisión de la ALTA es `PROPUESTA_ALTA`; su relación (`relaciones.tsv`) queda **byte a byte igual** a como estaba antes de correr (comparación de fila completa, no solo un campo).
- **(c)** `test_source_specific_historical_case_with_fp24_no_integrates_and_is_idempotent` y `test_any_fp24_dependency_cannot_be_integrated_while_open` (las dos del §22 del acto anterior) corridas junto con la nueva, sin editarlas: verdes las tres.
- **(d)** segunda corrida idéntica del mismo lote (ordinaria + ALTA) → `changed=[]`.

`python3 -m unittest tools.curador_registro.tests.test_integrate_barrido2` → **3/3 OK**. `jsonschema` no estaba instalado en esta caja de nube (`ModuleNotFoundError`); se instaló (`pip install jsonschema`) para poder correr la suite — el propio módulo ya lo importaba desde antes de este acto, no es una dependencia que este acto añada.

## Commit 2 · T23 · T-CABLEADO

**Fuente única, `forense/notas/2026-08-17-b2-derivaciones-c4.md` §4**, implementada tal cual — las 19 condiciones de FAIL, los cuatro WARN de conteo, y la condición de las 17 aperturas absorbidas evaluada contra el registro (`relaciones.tsv` + `data/lista-apertura-enlace2-2026-08-14.tsv`), no contra el cableado.

**Puerta de activación**, la pieza que decide si T23 nace inactivo de verdad: `t23_cableado()` calcula sus rutas (`ROOT`-relativas) **dentro de la función**, no como constantes de módulo — sin eso, monkeypatchear `check.ROOT` en las pruebas sintéticas no tendría efecto y las dos pruebas negativas obligatorias no podrían aislarse del repo real. Sin `data/cableado-universo-v1_0.tsv` y sin `--require-cableado`: `return` inmediato, cero FAIL/WARN. Con la bandera y sin el archivo: un único FAIL ("no existe bajo --require-cableado"), nada más — ninguna de las otras 18 condiciones se evalúa. Con el archivo presente (con o sin bandera): las 19 rigen siempre.

**Rutas fijadas por este acto** (la dependencia dura que el propio §4 señala: "tres de los productos que T23 debe cruzar... no tienen ruta fijada en ningún sitio"), bajo `data/curacion-registro/ejecucion-semantica/barrido2/`, sumadas a la lista «Versionable» del §24 del encargo madre:
- `propuestas-barrido2.tsv`
- `tareas-semanticas-barrido2.tsv`
- `decisiones-integracion-barrido2.tsv`

Ninguna de las tres existe todavía en el árbol real (C4 sigue a medias, C5 nunca corrió contra datos reales) — T23 las trata como ausentes (`None`, distinto de `[]`) y falla cerrado en las condiciones 14/15 que dependen de ellas, si alguna vez el cableado existe sin que ellas existan.

**Dos límites declarados, no disimulados (mismo criterio que pide §4):**
- `evidencia`/`reporte_neutral_ref` no se dereferencian contra el índice E2 privado (no vive en un clon limpio); se exige forma dereferenciable (`algo:sha256`), no resolución real.
- `SIN-DEMANDA-CONFIRMADO` no tiene columna propia en las 26 — su verificación es búsqueda de token en la fila, tal como el §4 la describe.

**Control C4, pegado crudo, corrido antes y después del commit:**

```
python3 tests/check.py            → 19 FAIL · 135 WARN   (idéntico a antes del commit)
python3 tests/check.py --baseline → LÍNEA BASE: VERDE — nada nuevo (git status tests/baseline.json: sin cambios)
python3 tests/check.py --require-cableado → 20 FAIL · 135 WARN, con exactamente una entrada T23 nueva:
    "`data/cableado-universo-v1_0.tsv` no existe bajo --require-cableado"
```

**Dos pruebas negativas obligatorias del §22**, en `tools/curador_registro/tests/test_check_t23.py` — sintéticas a propósito, porque T23 no conoce los 20 IDs históricos (§22 lo prohíbe explícitamente) y una prueba que usara uno de esos IDs estaría verificando justo lo que la espec pide que el checker NO haga. `tests/check.py` no es paquete; se carga por ruta (`importlib.util`) y se monkeypatchea `check.ROOT` a un árbol temporal sintético (cableado + `relaciones.tsv` + las tres tablas fijadas arriba), reseteando `check.FAILS`/`check.WARNS` antes de cada corrida:
- una relación decidible por evidencia específica, `dependencia_fp24=NO`, `INTEGRADA` → cero FAIL.
- la misma fila con `dependencia_fp24=SI` (y `requiere_decision_mesa`/`decision_mesa_id` consistentes) forzada a `INTEGRADA` → FAIL citando exactamente "no puede quedar INTEGRADA mientras FP-24 esté abierta".

Control de cordura adicional (no exigido, corrido a mano): una fila con las 26 celdas vacías produce 27 FAIL — confirma que el checker de verdad evalúa, no que las dos pruebas pasan por vacuidad.

`python3 -m unittest tools.curador_registro.tests.test_integrate_barrido2 tools.curador_registro.tests.test_check_t23` → **5/5 OK**.

## §2 · Lo que este acto NO hace, verificado

No crea `build_cableado.py` ni `data/cableado-universo-v1_0.tsv` (C6 sigue sin empezar — exige decisiones de la fase semántica que este acto no toma). No toca `barrido2_material.py` ni ningún producto de `data/` (`git diff --stat` contra este acto: solo `tests/check.py`, `tools/curador_registro/integrate_barrido2.py`, `tools/curador_registro/tests/*`, y los artefactos de gobernanza/forense de este mismo commit). No cierra ninguna FP. No congela `tests/baseline.json` (`git status` lo confirma sin cambios en todo el acto). Contadores de medición sobre México: cero.

## Commit 3 · Cierre

`ADR-98` — número re-derivado al escribir esta nota (`ADR-97` era el más alto al arrancar) y a re-verificar otra vez al fusionar: `ACTO GATE-DURABLE-V7` corre en paralelo sobre archivos disjuntos (`barrido2_material.py` y productos de `data/`, ninguno tocado aquí), pero ambos actos escriben `gobernanza`/`estado-programa` — la colisión de número de ADR es la esperada y ya tiene protocolo de renumeración con cinco precedentes (`ADR-91`/`92`/`93` lo declaran explícitamente en `estado-programa:101`). Cascada `canon/estado-programa-v1_10.md:27,101` actualizada a 98, con las entradas de `ADR-96`/`ADR-97` que la cadena de `:101` no llevaba (backfill, mismo mecanismo que `ADR-91`/`92`/`93` ya aplicaron cuando encontraron el mismo hueco al tocar esa misma línea — precedente citado en el propio texto de `:101`).
