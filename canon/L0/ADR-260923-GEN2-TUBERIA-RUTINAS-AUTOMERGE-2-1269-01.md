**ADR-260923-GEN2-TUBERIA-RUTINAS-AUTOMERGE-2-1269-01**, `ACTO GEN2-TUBERIA-RUTINAS-AUTOMERGE-2`, 23/sep/2026, NUBE, Sonnet 5, sin sub-agentes, MODO ABIERTO. D4-A (firma de mesa 23/sep/2026: main exige `check.py --baseline` VERDE; merge queue; el token de Actions fusiona solo PR de rutina — `claude/encola-*`, `acto/gen2-tramite-*`, `[deriva]`; lo que mide lo fusiona mesa) construye P2 — `.github/workflows/automerge-rutinas.yml` (cuatro condiciones: clase de rama/commit, CI VERDE sobre el job `suite`, guardia por diff que nunca deja pasar `data/corrida0/CALC-*` ni `forense/prereg-caja/`, PR abierto y mergeable; fusiona por API sin actions de marketplace y registra en `forense/fusiones-automaticas.tsv`) — pero no puede aplicar P1 (protección de main + merge queue): esta sesión no tiene tool de branch-protection ni `gh` CLI, y la red directa a la API de GitHub está `DENEGADA-POR-POLITICA`. Receta para mesa en la nota de cierre y en `FP-260923-GEN2-TUBERIA-RUTINAS-AUTOMERGE-2-1269-01`; el workflow queda instalado pero inerte hasta entonces, y P3 (las pruebas de «Hecho») queda diferido. De paso cierra cuatro objetos de deuda del mismo tema (`NC-…-8e53-04`, `NC-…-e889-01/02/03`, `NC-…-aa3f-02/04`, `FP-…-97dc-01` → FIRMADA) porque D4-A responde, verbatim, las tres líneas que mesa había dejado en blanco dos veces. Cero mediciones; no adopta. Suite `--rapido` VERDE, 0 FAIL.
