# ACTO GEN2-V215 · nota de cierre

`ENTORNO-DERIVADO = NUBE`, que es el que el encargo asigna: coincide, no hay nada que declarar por esa vía.

**Arranque.** Clon existente en `/home/user/Modelado-Mexicano`, no se clonó nada. `git log -1` al abrir: `cb1945ff` (`Merge pull request #918`), `git status` limpio. SHA de redacción declarado por el encargo: `cb1945ff` — **coincide exactamente**; tras `git fetch origin main`, `git rev-list --left-right --count origin/main...HEAD` → `0 0`. El hook de arranque reportó `detras=535 adelante=468 (sin fetch)`, cifra del clon sin refrescar; refrescado, el árbol está en `main`. `corpus montado = NO`, `archivos_examinados = 0`; red `DENEGADA-POR-POLITICA`. El acto no abre microdato, así que ni el corpus ni la red lo gatean. Rama propia: `claude/wonderful-johnson-8qs9qj`.

**Adjuntos (A.3).** Los tres llegaron y los tres verifican al recibir, antes de instalar nada:

| adjunto | sha256/16 declarado | derivado |
|---|---|---|
| instrucciones-proyecto-v2_15-OPERATIVA.md | `237255dbe6da598d` | `237255dbe6da598d` |
| `forense/encargos/PLANTILLA-ENCARGO-v2_0.md` | `3574daacec8c22ac` | `3574daacec8c22ac` |
| ACTO-MD-bloque-ejecucion-v2_15.md | `7d81cce9ef1cefb7` | `7d81cce9ef1cefb7` |

El operativo y la plantilla se verificaron **otra vez ya instalados en el árbol**, no sólo en el directorio de subida: `sha256sum instrucciones-proyecto-v2_15.md` y `sha256sum forense/encargos/PLANTILLA-ENCARGO-v2_0.md` dan los mismos dígitos. Ninguno de los dos se editó: son texto firmado.

## Las dos premisas `[SUPUESTO]`, verificadas — y las dos ciertas

El encargo pedía verificarlas y previó una rama por si resultaban falsas. Ninguna rama hizo falta.

1. **`tests/test_arnes_sesion.py` falla si `CLAUDE.md` no nombra el archivo de instrucciones de versión más alta.** **CIERTA.** `tests/test_arnes_sesion.py:52-77`: `_version_mas_alta()` recorre `instrucciones-proyecto-v*.md` en la raíz, descarta los sufijos `-HISTORIA` y `-DELTA`, ordena por `(mayor, menor)` y compara contra lo que `CLAUDE.md` cita. Es perímetro de cierre real: al instalar v2_15.md sin tocar `CLAUDE.md`, el test habría fallado. Corrido después de P4: **13 pruebas, 0 fallos.**
2. **`tools/tablero_programa.py` ya deriva `instrucciones_vigentes` del árbol.** **CIERTA.** `tools/tablero_programa.py:396-407`, mismo método (glob + versión máxima, sin `-HISTORIA`/`-DELTA`). No hubo que actualizar ningún valor a mano ni dejar NC para derivarlo: `--actualiza` movió `v2.14 → v2.15` solo. E.4 se cumple sin intervención — lo dice el mecanismo.

La tercera premisa relevante, `[EJECUTADO]` y por tanto sólo de comprobación de pasada, **sí tenía un pliegue** (ver «lo que no cuadró»).

## Lo que no cuadró, y qué se hizo

**(a) «17 semillas» son 16 + 1.** El encargo derivó el conteo con `grep -c "PARA-v2.1[56]"`, patrón que funde dos rótulos. Por rótulo exacto: **16** `PARA-v2.15` y **1** `PARA-v2.16` (`hallazgos.md:910`). No cambia ninguna pieza — el cuerpo operativo es verbatim y no era mío de editar — pero sí cambia lo que la HISTORIA debía decir, y ahí quedó asentado, junto con el giro: **el cuerpo v2.15 promovió la entrada rotulada `PARA-v2.16`** (la guarda de una rama irreversible sobre el estado positivo, hoy en E.6). La semilla quedó consumida por una versión anterior a la que su rótulo anunciaba. No se edita el rótulo de la entrada (A.3). Hallazgo asentado, semilla para `v2.16`.

**(b) La premisa de las 27 NC en 18 actos parecía falsa y era mía la falla.** Primera derivación con `awk -F'\t'`: **20** filas en **12** actos, un 26 % por debajo de lo que el encargo declaraba. Segunda, con `csv.DictReader`: **27** en **18** — exactamente lo declarado. La causa es que `forense/no-corrido.tsv` trae campos citados con saltos de línea dentro, que `awk` parte como filas nuevas. **El encargo tenía razón y el instrumento no.** Es el pliegue que más cerca estuvo de producir un reporte falso de «premisa no sostenida», porque una cifra 26 % baja no se ve mal. Asentado en `forense/hallazgos.md` e instrumentado en la herramienta de P5, que lee con `csv` y lo dice en su docstring y en su comentario. La regla que se ganó el sueldo aquí es A.13: fue el conteo declarado de filas examinadas lo que delató la discordancia.

## Piezas

**P1 · Los dos cuerpos.** `instrucciones-proyecto-v2_15.md` ← adjunto operativo, byte a byte (116 líneas; el encargo decía 117, diferencia de si se cuenta la línea final sin `\n` — el sha256 manda y coincide). `instrucciones-proyecto-v2_15-HISTORIA.md` = HISTORIA de v2.14 íntegra + bloque v2.15; `diff <(head -492 instrucciones-proyecto-v2_15-HISTORIA.md) instrucciones-proyecto-v2_14-HISTORIA.md` → **vacío**. El bloque nuevo trae una entrada por regla nueva o enmendada (§0, §2, D-18 a D-23, A.15, E.5, E.6) con su defecto, fecha y hallazgo citados por línea de `forense/hallazgos.md`, más la lista de semillas **no** promovidas con el porqué de cada una, más el costo contado y el falsador. Dos notas de procedencia explícitas en la HISTORIA, porque no todo salió limpio: **D-23 no tiene semilla `PARA-v2.15` propia** (se deriva del linaje de clon superficial, `hallazgos.md:330` y `:366`, más la optimización de verificación de `#901`), y **E.6 promueve una semilla rotulada `PARA-v2.16`**. Se asientan como discordancias en vez de maquillarlas.

instrucciones-proyecto-v2_14.md retirada del árbol por T01, igual que V214 retiró v2_13.md. **`instrucciones-proyecto-v2_14-HISTORIA.md` NO se borra**: «borrar versiones anteriores que V214 no borró» está en la lista cerrada de PAROS, y V214 sólo retiró el cuerpo operativo. Sidecars `sha256` para los dos cuerpos nuevos.

Ningún error detectado en el cuerpo operativo, así que no hubo FP a mesa por esa vía.

**P2 · La plantilla.** `forense/encargos/PLANTILLA-ENCARGO-v2_0.md` byte-idéntica, con sidecar. `forense/encargos/PLANTILLA-LOTE-v1_0.md` gana cinco líneas de cabecera `SUCEDIDA POR…` y no se borra.

**P3 · `/acto`.** El bloque adjunto entra como **`## 4 · EJECUCIÓN — premisas, latitud y paros`** entre `0-bis` y `CIERRE`, que pasa a `## 5`. Ajustes de estilo al del archivo, sin tocar el sentido: los seis puntos pasan de `**4.1**…**4.6**` a lista numerada `1.`–`6.` (que es como numera el resto del archivo), el ancho se ajusta a la caja del documento, el comentario HTML de dirección no se transcribe, y la referencia interna «la cascada de §5» se reescribe como «la cascada de la sección 5» ya renumerada. Cambios adicionales, con antes/después:

| dónde | antes | después | por qué |
|---|---|---|---|
| `.claude/commands/acto.md` frontmatter | `…0-bis A.3 + CIERRE en cascada)` | `…0-bis A.3 + EJECUCIÓN + CIERRE en cascada)` | la skill ahora tiene cinco secciones |
| `.claude/commands/acto.md` ARRANQUE | «Si algo no cuadra, **PARA** y repórtalo: encontrar que el terreno no es el que el encargo supone es entregable, no interrupción.» | «Encontrar que el terreno no es el que el encargo supone es entregable, no interrupción — pero qué hacer con ese hallazgo lo decide la sección 4: si toca qué se mide o una firma de mesa, PARA y repórtalo; si es logística (main movido, `data/raw` sin enlazar, un clon superficial) y el objetivo sigue alcanzable, resuélvelo, síguele y decláralo.» | era un `PARA` general ante cualquier discordancia, incluida la logística que el propio ARRANQUE ya declara que **no** es PARO (pasos 2 y 3). Contradecía §2 y D-19 |
| `.claude/commands/acto.md` CIERRE, paso `NO-CORRIDO` | lista de los siete tokens, sin más | + «`FUERA-DE-PERÍMETRO` **nombra de qué otro acto es la pieza**; si no puedes nombrarlo, no era de otro acto y te toca a ti (D-21). El token va **al principio** de la razón, no en medio de la prosa (A.16): `tools/nc_por_razon.py` cuenta por prefijo exacto.» | lo pide el encargo; la segunda frase cierra el defecto de `hallazgos.md:913` (87 de 108 filas ABIERTAS sin token reconocible) sin reescribir ninguna fila vieja |

**`.claude/commands/tramite.md`: examinado entero, sin cambios, y por qué.** Tiene tres `PARA` (`:70`, `:244`, `:536`) y los tres son legítimos bajo la norma nueva. `:70` es la frase de perímetro que las propias instrucciones mandan (D-12) y no ocurre ante algo reversible y barato, sino ante escribir fuera de la lista. `:244` para **con ese encargo**, no con la corrida, y lo enruta a una fila del digesto: ya tiene la semántica de «una pieza que PARA no tumba el lote» (D-11). `:536` es «un PR de trámite que necesita ADR dejó de ser trámite», que es D-19(f), objetivo que dejó de tener sentido. Ninguno contradice la v2.15; tocarlos habría sido cambio sin defecto.

**P4 · Punteros.** Vivos, actualizados: `CLAUDE.md:5` (`@instrucciones-proyecto-v2_15.md`), `canon/registro-rotulos.tsv` (7 filas de rótulo; 14 sustituciones, porque cada fila cita el archivo en la definición y en la fuente), `tools/tablero_programa.py:9` (docstring — el mismo sitio que V214 actualizó). **No editados, y por qué:** `forense/notas/nota-arnes-sesion-claude-md.md`, `forense/notas/2026-09-19-GEN2-PISOS-ENIF2021-FORMALIDAD-1-cierre.md`, `forense/encargos/2026-09-19-GEN2-V214-instrucciones-v2_14.md` y el texto de `ADR-544` en `canon/gobernanza-v1_15.md` son historia (A.3, A.10). Las citas de `registro-rotulos.tsv` a `instrucciones-proyecto-v2_14-HISTORIA.md:NNN` **se conservan**: esos números de línea son correctos en ese archivo, que sigue en el árbol. `AGENTS.md`: **no cita la versión** — comprobado, sin cambio. Nota aparte: `.claude/commands/acto.md:375` cita instrucciones-proyecto-v2_12.md dentro de una frase histórica sobre una firma de mesa del 2/sep; el archivo existe y la frase es un registro de lo que se decidió entonces, así que no es puntero vivo y no se toca.

**P5 · Línea base del falsador.** `tools/nc_por_razon.py` (comando suelto; no cabía en ≤10 líneas como subcomando una vez resueltas las tildes y los campos multilínea). Clasifica por **prefijo exacto** de la columna `razon` (A.16), normalizando tildes en los dos sentidos, de modo que `FUERA-DE-PERIMETRO` y `FUERA-DE-PERÍMETRO` caen en el mismo token y un token a media frase **no** cuenta. La receta se verificó contra siete casos conocidos antes de usarla (§2: la receta también se verifica) — 0 fallos, incluidos los dos que importan: variante sin tilde y token embebido en prosa. `SIN-TOKEN` se reporta como estado, no como cero.

Ventana **16–20/sep/2026**, universo `forense/no-corrido.tsv`, **398 filas examinadas** (A.13):

```
NC en ventana=160
  PARO-ENTORNO                    2    1.2% *
  PARO-PREMISA                   25   15.6% *
  FUERA-DE-PERÍMETRO             48   30.0% *
  SUSTITUIDO-POR                  4    2.5%
  DIFERIDO-A                      8    5.0%
  NO-VERIFICABLE-AQUÍ            15    9.4%
  DECISIÓN-DE-MESA-PENDIENTE     22   13.8%
  SIN-TOKEN                      36   22.5%
  PARO-* aportados por 18 actos distintos (27 filas)
  (*) suma del falsador          75   46.9%   umbral de revision: < 33.3%
```

Reproduce las tres cifras del encargo (160, 48, 27-en-18) y el 47 % de la plantilla. **Contra esto se mide en tres meses** (`NC-0418`).

**P6 · ADR y A.9.** `ADR-566`, con la reconciliación de conteo en `gobernanza:2`, la tabla y `L0` de `canon/estado-programa-v1_14.md`, más anotación `L0` propia. **Número en disputa, declarado:** `tools/cierre_acto.py` derivó `564` como candidato contiguo **y reportó que dos ramas remotas vivas ya lo traen redactado** (`acto/gen2-celda-d-piloto-3-commit-1-v1_1`, `claude/pensive-keller-7wxjod`). Se toma el contiguo y rige la regla de la casa: **renumera quien fusione segundo**, junto con `NC-0416`-`NC-0418` y `FP-401`. No se inventa un número alto para esquivar la colisión, que es lo que la «lección de aparato» de la HISTORIA de v2.14 desaconseja.

**A.9 CUMPLIDA EN LOS DOS LADOS — y estuvo a punto de no estarlo.** La firma del lado proyecto viajó en el encargo como **propuesta** («mesa la confirma o corrige la fecha al lanzar»), que es exactamente la causa por la que `ADR-544` tuvo que sellar la v2.14 **sólo del lado repo**. Iba camino de repetirse por segunda vez consecutiva. **Mesa la confirmó al cierre**, verbatim: **«La fecha es de hoy 20 de septiembre»** (20/sep/2026), sobre la línea propuesta: *"El cuerpo operativo v2.15 quedó pegado en las instrucciones del proyecto de Claude el 20/sep/2026, y `forense/encargos/PLANTILLA-ENCARGO-v2_0.md` subida al conocimiento del proyecto el mismo día."* Con eso la v2.15 sella completa: `NC-0416` nace y cierra en el mismo acto y `FP-401` queda `FIRMADA` (A.12: la ranura añadió su fila, y la firma la marcó, en el mismo commit).

**Reparto de evidencia, dicho sin disfraz.** El lado repo está **verificado por comando**: sha256 de los dos cuerpos, `instrucciones_vigentes` derivado, suite VERDE, arnés 13/13. El lado proyecto **descansa en la declaración de mesa** — no tengo superficie de escritura ni de lectura fuera del repo, así que no puedo verificarlo y no lo afirmo como verificado. Es lo que A.9 pide («mesa lo declara con fecha; el ADR cita esa línea verbatim») y todo lo que un ejecutor en nube puede aportar. La otra firma del día, también verbatim en el ADR: «Esta ok, dame todos los lugares donde debe de ir, instrucciones, modificar el template de "acto" en el repo, guardarlo aquí en el proyecto. Quiero que quede completamente permeado.»

## La plantilla, usada contra sí misma

El encargo pedía decir si la plantilla estorbó al ejecutarlo. **No estorbó, y dos de sus piezas hicieron trabajo medible en este mismo acto:**

- Los **rótulos de premisa** dirigieron el esfuerzo. Los dos `[SUPUESTO]` se verificaron primero y los dos resultaron ciertos; ninguno de los `[EJECUTADO]` se re-derivó entero salvo el conteo de semillas y el de NC — y ahí, justamente, aparecieron los dos pliegues. Sin los rótulos, el reparto habría sido a ojo.
- La **lista cerrada de paros** evitó dos paradas que la v2.14 habría permitido: el conteo de semillas discordante y la sospecha (falsa) de que la premisa de las 27 NC no se sostenía. Ninguna toca qué se mide; las dos se resolvieron y se declararon.

Lo que sí rozó: la plantilla manda «si `<premisa [SUPUESTO]>` resulta falsa, entonces `<rama prevista>`», pero **no tiene casilla para “la premisa `[EJECUTADO]` de dirección resultó mejor que mi primera derivación”**, que es lo que pasó con las 27 NC. Se resolvió escribiéndolo en la nota. No se propone texto nuevo: un caso no es un defecto repetido (D-14).

## Desviación de procedimiento, declarada

`/acto` manda que el commit `0-bis` (archivar el encargo verbatim) sea el primero y que **ningún paso sustantivo lo preceda**. En esta sesión el orden de *commits* es correcto —`0-bis` es el primero y contiene sólo el encargo—, pero en tiempo real las ediciones de P1–P5 ya estaban en el árbol de trabajo cuando se creó. Además, el `0-bis` se creó primero con `git add -A` y barrió todo el trabajo dentro de un commit cuyo mensaje decía sólo «archiva encargo»; se deshizo con `git reset --soft` **antes de cualquier push** y se rehízo separado. Nada sellado se reescribió: el commit tenía segundos de vida, era local y era mío. Se declara porque el registro debe decir lo que pasó, no lo que habría quedado bonito.

## Contador

`cuenta_gen2 = NO`. **Ningún contador del programa se movió**, y el acto no podía moverlos: no abre microdato, no sella corridas, no adopta nada. El único valor derivado que cambia es `instrucciones_vigentes` (`v2.14 → v2.15`), que es puntero de versión, no medición. Cero mediciones producidas, dicho sin disfraz (§5, pregunta v2.4 del módulo de auditoría).

Este artefacto **no afirma nada sobre México**, así que el módulo de auditoría de rigor extremo no aplica (§5: no en notas, registros ni encargos).

## Estado de la suite

`python3 tests/check.py --baseline --parallel` → **LÍNEA BASE VERDE**, sin FAIL nuevos frente a `tests/baseline.json`. Los **3 FAIL** vigentes (`T06` ×2, `T08`) son de contenido del corpus —valores de Gini y de confianza interpersonal, reports sin mapa de evidencia— y preexisten al acto; están en la línea base congelada. **19 WARN nuevos**, que por **D-16** son estado y no adjudican; ninguna cabecera de este acto aserta totales de WARN. Su desglose, porque el número subió durante el acto y conviene decir por qué. Antes de escribir nota y cascada eran **7**: tres los produce la retirada de instrucciones-proyecto-v2_14.md por T01 —`canon/gobernanza-v1_15.md` (texto de `ADR-544`), el encargo archivado de V214 y una nota de cierre lo citan, y los tres son historia que **no se edita** (A.3/A.10)— y cuatro son ajenos y preexistentes (`EVALUACION-CARRIL-CODEX-*` ×2, v2_13.md, settings.md). Los **11** restantes los añadieron el encargo archivado de este acto, la nota y las entradas de `forense/hallazgos.md`, al citar por nombre archivos que no están en el árbol: los tres adjuntos (que viven en el mensaje de dirección, no en el repo) y las versiones ya retiradas. Una primera versión de la nota levantaba **25**; normalizar sus citas a ruta completa donde el archivo existe bajó a 18, y el merge con `main` sumó uno más (19), y el resto es irreducible sin editar texto verbatim o sin dejar de nombrar aquello de lo que el hallazgo trata. **No se instrumenta exención ni se toca la línea base**: D-16 dice que un WARN no adjudica y D-14 pide un defecto real antes de construir aparato. Que el patrón se repite está medido, no supuesto: `forense/notas/nota-arnes-sesion-claude-md.md` sigue levantando el mismo `T03` por v2_13.md, retirado un día antes.

`python3 tests/test_arnes_sesion.py` → **13 pruebas, 0 fallos**.

## Renumeración al fusionar segundo

El encargo cerró con `ADR-564` **declarado en disputa**: `tools/cierre_acto.py` lo derivó como contiguo y en la misma salida reportó que dos ramas remotas vivas ya lo traían redactado. Se tomó el contiguo, como manda la casa, y se dijo que renumeraría quien fusionara segundo. Pasó exactamente eso.

`claude/pensive-keller-7wxjod` fusionó primero (`PR #925`) y **él mismo renumeró `564→565`**. Traído `origin/main` (`dfb07b9`) a esta rama, los máximos se re-derivaron **contra el árbol real, no contra el encargo**: ADR `565`, NC `0415`, FP `400`. De ahí:

| antes | después | por qué |
|---|---|---|
| `ADR-564` | **`ADR-566`** | `564` y `565` ya ocupados en `main` |
| `NC-0407`-`NC-0409` | **`NC-0416`-`NC-0418`** | `NC-0407`-`NC-0410` los tomó `GEN2-CELDA-D-PILOTO-3-EJECUCION` |
| `FP-399` | **`FP-401`** | `FP-399` y `FP-400` los tomó el mismo acto |

El merge trajo conflicto en los cinco registros de apéndice (`gobernanza`, `estado`, `registro-rotulos`, `no-corrido`, `firmas-pendientes`). Se resolvieron **tomando `main` como base y re-aplicando lo propio renumerado**, nunca al revés: ningún texto ajeno se reescribió al reordenar. Verificado después: **cero IDs duplicados** en las tres series (566 ADR, 410 filas NC, 388 FP) y cero marcadores de conflicto en el árbol.

Que la renumeración era necesaria y no ceremonial lo prueba la fila de `registro-rotulos.tsv` de `GEN2-CELDA-D-PILOTO-3-EJECUCION`, que llegó en el merge citando `FP-399`, `NC-0407..NC-0410` y `ADR-564`: los números viejos de este acto estaban **los tres** ocupados.

## NO-CORRIDO / RESERVAS

- **qué:** el sello pleno de A.9 — la v2.15 pegada en las instrucciones del proyecto de Claude con fecha declarada por mesa. **RESUELTA EN EL MISMO ACTO.**
  **por qué:** `DECISIÓN-DE-MESA-PENDIENTE`. Nació porque la firma del lado proyecto viajó como propuesta con la fecha sin llenar, y este ejecutor no tiene superficie fuera del repo. Precedente idéntico y por la misma causa: `ADR-544`/V214, que por eso selló sólo el lado repo. **Mesa la declaró al cierre: «La fecha es de hoy 20 de septiembre».**
  **impacto:** ninguno pendiente — A.9 queda cumplida en los dos lados y la v2.15 sella completa.
  **sucesor:** ninguno. `NC-0416` **CERRADA** por la firma del 20/sep/2026, citada verbatim en `ADR-566`; `FP-401` **FIRMADA**.

- **qué:** reescribir los encargos ya archivados al formato de la plantilla v2.0.
  **por qué:** `FUERA-DE-PERÍMETRO` — y no es de otro acto: el encargo lo excluye en §10 y A.3 prohíbe editar un encargo archivado. Se asienta para que la ausencia no se lea como olvido (D-21: si usas el token, di de qué acto es; aquí la respuesta honesta es «de ninguno, es deliberado»).
  **impacto:** ninguno — `/acto` §4 define el default de `MODO` para encargos anteriores a v2.15, así que ningún acto queda sin regla aplicable.
  **sucesor:** `SIN-ASIGNAR`; no lo requiere. `NC-0417`, CERRADA en el mismo acto.

- **qué:** la medición del falsador de la plantilla.
  **por qué:** `DIFERIDO-A:` el acto sucesor de diciembre/2026 — la ventana de tres meses no ha transcurrido.
  **impacto:** sin ella no se sabe si la plantilla resolvió el defecto (75/160 = 46.9 %). No bloquea nada hoy; la línea base ya existe y es reproducible por comando.
  **sucesor:** `NC-0418` — `python3 tools/nc_por_razon.py --desde 2026-09-20 --hasta 2026-12-20`.

## CONSUMIDO

Ejecutado sobre la rama `claude/wonderful-johnson-8qs9qj`. `ADR-566`, `NC-0416`-`NC-0418`, `FP-401`.
