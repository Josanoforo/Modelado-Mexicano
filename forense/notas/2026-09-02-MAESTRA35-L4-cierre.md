# ACTO MAESTRA35-L4 · R-v1_2-CIEGA — nota de cierre

2 de septiembre de 2026 · ejecutado con la skill `/acto` (`ADR-237`) sobre el encargo `forense/encargos/2026-09-02-MAESTRA35-L4-R-v1_2-CIEGA.md` (dirección/Fable, SHA de redacción `0fd6b4c`) · entorno **UBUNTU** con corpus montado · caja `/home/pc0/mm-maestra35-l4`, rama `acto/maestra35-l4-r-v1_2-ciega`, base real `9cbd8d8` (`origin/main`, merge `PR #485`) · modelo ejecutor: Fable (el encargo sugería Opus; misma familia de razonamiento, declarado).

Commits, en el orden en que importan: `da20d03` (0-bis A.3, encargo verbatim) → `e96c25e` (`/arbitra` COMMIT-1, specs congeladas + fila `TRA-M-02`) → `deea16f` (COMMIT-2, los cuatro `corridas-R/*.json` + filas de estado) → el commit de esta cascada (`ADR-305` — nació `302` y se renumeró tres veces al fusionar, ver §6.6 —, L0, registro, `FP-253`, hallazgo, censo T22, esta nota, `## CONSUMIDO`).

## §0 · Declaración de ceguera — al inicio, como pide el encargo

**Ninguna cifra de motor de `FAM-M-05`, `FAM-M-06`, `FAM-M-07` ni `TRA-M-02` fue vista por esta sesión antes de escribir y commitear sus `R`. Ninguna celda sale de lote.** Sesión nueva, que no había visto `M` ni el motor.

Lo que sí hay que declarar, por declarar más y no menos: el índice de memoria del ejecutor (que el cliente carga automáticamente al arrancar toda sesión) contiene una línea con una `p` del acto `MAESTRA33-C1` (corresidencia, `RE-SPEC`) — ajena a las cuatro celdas de este lote y al sorteado v1_2. Las notas de memoria propias que se consultaron para el arranque se barrieron antes con el detector de cifras (valores enmascarados, sólo conteos) y se abrieron sólo las que dieron 0 — incluida la nota propia sobre `MAESTRA35-L2`, que dio 0; las cuatro que dieron >0 (`spec congelada degenerada`, `eje anidado`, estado de `MAESTRA35-L3`, estado de `MAESTRA34-L2`) **no se abrieron**.

**Momento exacto en que la sesión dejó de ser ciega:** `2026-09-02T19:27:29-06:00`, al abrir `canon/gobernanza-v1_15.md` (primer archivo de canon) para esta cascada. COMMIT-2 es `deea16f`, `2026-09-02 19:26:50 -0600`: los cuatro `R` llevaban 39 segundos commiteados. Verificable con `git log --format='%h %ci' -3` sobre la rama. Después de ese instante se abrieron, para la cascada, `canon/gobernanza-v1_15.md` (cabecera, conteo por comando, `ADR-301` y `ADR-292` como plantillas — este último con las cifras enmascaradas en el comando, sin necesidad y por costumbre), `canon/estado-programa-v1_11.md` (líneas 27, 105 y 343 y su tabla de artefactos), `canon/registro-rotulos.tsv`, `forense/firmas-pendientes.tsv` (cabecera, `FP-227`, `FP-249`, `FP-251`), `forense/hallazgos.md` (cola), `tests/check.py` (`T15`, `T22`, `_T22_ARCHIVOS_CONOCIDOS`) y el diff de `PR #486` sólo en sus hunks de conteo de ADR. `marco-M-*`, `milpa/`, `corridas-M/`, `corridas-L/`, `scoreboard*` y `L-extraido*` **no se abrieron en ningún momento del acto**, tampoco después de COMMIT-2: no hacían falta.

## §1 · ARRANQUE (cinco líneas)

1. **REPO.** Clon existente `/home/pc0/Modelado-Mexicano` (parado en `L/corridas-v1_2`, `c4bc12d`, como se esperaba de un worktree principal); caja nueva por `git worktree add /home/pc0/mm-maestra35-l4 -b acto/maestra35-l4-r-v1_2-ciega origin/main` → `HEAD 9cbd8d8`, `git status` limpio. (El «`.git/config: Device or resource busy`» del `worktree add` es el sandbox, no git; el worktree quedó bien.)
2. **SHA.** El encargo se redactó contra `0fd6b4c`; `origin/main` iba **100 commits** por delante (`git rev-list --count 0fd6b4c..HEAD`). No es PARO: perímetro, contadores y numeración re-derivados contra `9cbd8d8`.
3. **data/raw.** Ausente en la caja nueva (gitignorado, `.gitignore:5-6`); enlazada: `ln -s /home/pc0/mm-corpus/raw data/raw`.
4. **ENTORNO (tres partes).** `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE` → sin variable · `curl -s -o /dev/null -w "%{http_code}" --max-time 10 https://www.inegi.org.mx/` → `200` · `ls data/raw/ | head -1` → `2005trim1_csv.zip`. Es la firma UBUNTU con corpus que el encargo asigna.
5. **ESPEJO.** Ninguna cifra salió del espejo; todas del clon, con el comando a la vista.

## §2 · COMPUERTA (por producto, contra `origin/main` real)

`git fetch origin main` → `9cbd8d8`. `git cat-file -e origin/main:forense/prereg-duelo-v2/espec-R-ciega-v1_2.tsv` → presente (exit 0). `git show origin/main:tools/arbitra.py | command grep -c '"--produce"'` → `1`. `gh pr view 470` → `MERGED 2026-09-02T21:36:05Z`, merge `792b7ef`. Compuerta cumplida; ningún paso se adelantó.

## §3 · VERIFICACIÓN A.8 re-derivada contra el árbol real

- `ls corridas-R/ | grep -c -E "^(FAM-M-0[567]|TRA-M-02)\.json"` → `0` sobre 38 JSON (control positivo: el directorio tenía 38). NO-ENCONTRADO ×4, como decía el encargo. Hoy: 4 nuevos, 42.
- `grep -c -P '^TRA-M-02\t' codificacion-R-v1_0.tsv` → `0`; `FAM-M-05/06/07` → `1` cada una, `estado=PROPUESTA` del 2026-09-01. EXISTE-SATISFACE ×3, NO-ENCONTRADO ×1. Hoy: `TRA-M-02` 2 filas (PROPUESTA + estado), FAM 2 filas cada una.
- Interfaz del árbitro: `--produce <marco.tsv> <ids>` existe (`tools/arbitra.py:932`), lee de la proyección sólo `id`/`encuesta`/`ola` (`_encuesta_ola_del_marco`) y rehúsa sobreescribir. Regresión previa de L2/L5 no se repitió; la de este acto sobre sus cuatro celdas: 4 de 4 COINCIDE.
- Numeración al arrancar, por comando contra `origin/main`: ADR 301 entradas, máximo 301, sin duplicados ni huecos → candidato `302`; FP máximo `FP-251` → candidato `FP-252`. Al cerrar: `PR #486` (`MAESTRA35-L7`, abierto, `MERGEABLE`) reclama `ADR-302` **y** `FP-252` (verificado contra su diff, no contra su título). ADR: se tomó `302` al cerrar (T15 exige contigüidad) y se renumeró dos veces al fusionar `origin/main`: a `303` porque `PR #487` (`MAESTRA35-L8`) fusionó primero y se llevó el `302`, a `304` porque `PR #486` (`MAESTRA35-L7`) fusionó mientras se resolvía la primera fusión y se llevó el `303`, y a **`305`** porque `PR #492` (`MAESTRA35-N8`) fusionó con el PR ya abierto y se llevó el `304` — las citas tocadas están listadas en el propio `ADR-305`. FP: se toma **`FP-253`** (sin test de contigüidad, mejor no duplicar).
- Cobertura retroactiva: todo lo que toca nació el 31/ago–2/sep; cubierto.

## §4 · Lo que se hizo, por pieza

**P0 (`e96c25e`).** (a) `sha256sum` de la proyección: `b2dacd8a4f66ccb29eb97e448c2d0e9cf1b70002669d0c5770a49def061beb53` (commit `db69b98`). (b) Control de ceguera con valores enmascarados en el propio comando (script `ceguera.py` en el scratchpad, patrones `0.dddd+` y `p=`/`M=`/`L=`/`z=`; imprime conteos por archivo y columna, nunca el valor): proyección **0** en 14 columnas; cabecera + cuatro filas de `codificacion-R` por `grep -P` **0**; también 0 en `.claude/commands/acto.md`, `AGENTS.md`, `instrucciones-proyecto-v2_12.md`, `correr-R.py`, `dbfmini.py`; `.claude/commands/arbitra.md` (3 cifras: los `R` de `DIN-11`/`SFT-04`/`TIC-08` en su tabla de regresión) y `arbitra.py` (3, en comentarios del detector) se leyeron con las cifras sustituidas por `<0.XXXX>`. (c) Fila `TRA-M-02` `PROPUESTA (ACTO MAESTRA35-L4)` en `codificacion-R-v1_0.tsv`, escrita desde la proyección y el FD y nada más: `encuci2020_bd_dbf` (manifiesto:992) · tabla lógica `SEC_4_5` (único miembro `ENCUCI_2020_SEC_4_5.dbf` entre `SD`, `SEC_4_5`, `SEC_6_7_8`, `SEC_9_10`, `VIV`; `testzip` limpio) · variable `AP5_17|AP5_18` · codificación `y=1 si AP5_17=='1' o AP5_18=='1'; y=0 si AP5_17=='2' y AP5_18=='2'` (resto FUERA) · `FAC_SEL` / `EST_DIS` / `UPM_DIS`. Códigos verificados en `FD_ENCUCI2020.pdf` (`pdftotext -layout`, sección 3.3 `ENCUCI_2020_SEC_4_5`, preguntas 5.16–5.18 y «Campos empleados para el diseño muestral»): `AP5_16_x` 1 Sí / 2 No / 9 NS-NR; `AP5_17`, `AP5_18` 1 Sí / 2 No / 9 NS-NR / b blanco; `FAC_SEL` Numérico 1-999999; `EST_DIS` Caracter 001-999 («Estrado de Diseño», no `ESTRATO` 1-4); `UPM_DIS` Caracter 0000001-9999999. El FD dice '1'/'2': nada que corregir. Parser verificado en seco: `parsea_codificacion_binaria` devuelve un callable por `_PATRON_COMPUESTO_OR` y **no** por `_PATRON_BINARIO`/`_SET`/`_UMBRAL`; 12 combinaciones sintéticas de (`AP5_17`, `AP5_18`) con el resultado esperado, sin tocar datos. COMMIT-1 (`notas-arbitra/2026-09-02-lote-fam-m-05-06-07-tra-m-02-ciego.md`): las cuatro filas de la proyección y las cuatro codificaciones verbatim, guardias con valor esperado (§3 de esa nota), comando a correr y las dos frases de sello.

**P1 (`deea16f`).** `python3 tools/arbitra.py --produce forense/prereg-duelo-v2/espec-R-ciega-v1_2.tsv FAM-M-05 FAM-M-06 FAM-M-07 TRA-M-02` → exit 0 en 5 s, cuatro `ESCRITO COMPUTADO`; salida cruda íntegra en §5 de la nota COMMIT-1 y en el mensaje de `deea16f`. Primer y único resultado:

| celda | R | EE_R | IC95 | cv % | n_filas | n_efectivo | no válidos | sin pond. | estratos | UPM | singleton |
|---|---|---|---|---|---|---|---|---|---|---|---|
| FAM-M-05 (ENIGH 2016) | 0.047459 | 0.001262 | [0.0450, 0.0499] | 2.66 | 70311 | 70311 | 0 | 0 | 536 | 7891 | 0 |
| FAM-M-06 (ENIGH 2018) | 0.047285 | 0.001195 | [0.0449, 0.0496] | 2.53 | 74647 | 74647 | 0 | 0 | 543 | 8377 | 0 |
| FAM-M-07 (ENIGH 2020) | 0.043775 | 0.001012 | [0.0418, 0.0458] | 2.31 | 89006 | 89006 | 0 | 0 | 558 | 10118 | 0 |
| TRA-M-02 (ENCUCI 2020) | 0.126025 | 0.005060 | [0.1161, 0.1359] | 4.02 | 21519 | 13412 | 8107 | 0 | 281 | 3003 | 1 |

Guardias (script auxiliar de sólo lectura, corrido **después** de la corrida; salida íntegra en la nota COMMIT-1 §5): ENCUCI **21519** filas y **13435** con contacto — exactas contra la proyección; las 8084 sin contacto traen `AP5_17`/`AP5_18` en blanco (salto del cuestionario) y **ninguna** entra en R, así que el universo queda aplicado por el salto aunque `arbitra.py` no ejecute `universo_filtro` (límite declarado); los 8107 no válidos son esos 8084 + 23 con contacto y código 9 en alguna (6 + 7 + 10), sin reclasificar; `n_efectivo` 13412 = 1426 (y=1: 606 + 586 + 1 + 233) + 11986 (y=0). ENIGH: `remesas` >0 / ==0 = 4128/66183, 4584/70063, 5240/83766, exactas contra las filas de codificación del 1/sep; no válidos 0 en las tres, como el encargo esperaba; `factor`, `est_dis`, `upm` sin vacíos. Cuatro filas de estado `COMPUTADA (ACTO MAESTRA35-L4)` anexadas (copia íntegra de cada codificación con `fuente` y `fecha` nuevas; las `PROPUESTA` intactas — precedente de forma: `DIN-M-01b`; `lee_codificacion()` toma la última fila por id, y ningún test ni herramienta parsea el archivo exigiendo ids únicos: verificado con `git grep` en `tests/` y `tools/`). `tools/arbitra.py --regresion FAM-M-05 FAM-M-06 FAM-M-07 TRA-M-02` (no escribe) → **4 de 4 COINCIDE**, bit a bit en `R`, `EE_R`, `n_efectivo`, `n_estratos`, `n_upm_total`.

**P2 (esta cascada).** `ADR-305` (nacido `302`, ver §6.6) al final de `canon/gobernanza-v1_15.md` (donde viven `ADR-293`…`ADR-301`), cabecera 301→302; `canon/estado-programa-v1_11.md`: línea 27, `L0` (anotación nueva insertada antes de la de `ADR-301`, verificada por reconstrucción: `len(nueva) == len(vieja) + len(insertado)` y la cola byte a byte) y la fila «máximo vigente» de la tabla de verificación de `ADR-301` (línea 343), recifrada del mismo modo que `PR #486` para que la fusión sea simétrica; `canon/registro-rotulos.tsv`: la fila `MAESTRA35-L4` que L2 dejó como «sucesor DECLARADO, no lanzado» se actualiza en su sitio (precedente `MAESTRA35-L1`), `git diff --numstat` → `1 1`; `forense/firmas-pendientes.tsv`: `FP-253` (recibo, ABIERTA); `forense/hallazgos.md`: una línea (el descriptor de campo del DBF); `tests/check.py`: dos entradas en `_T22_ARCHIVOS_CONOCIDOS` (ver §6); `.claude/commands/acto.md`: `grep -c "sesión ciega"` → `1`, la línea de `FP-244` ya estaba (la trajo `PR #470`), **no se duplica ni se toca el archivo**; `## CONSUMIDO` al pie del encargo.

## §5 · Lista completa de archivos abiertos (contrato de `/arbitra`, para quien audite el CIEGO)

**Antes de COMMIT-1 (`e96c25e`):** fuera del repo, el índice de memoria del ejecutor (automático) y las notas de memoria que dieron 0 en el barrido (`project_modelado_mexicano`, `feedback_*` de proceso, estado de `MAESTRA35-L2`); en el repo, `.claude/commands/acto.md`, `.claude/commands/arbitra.md` (enmascarado), `tools/arbitra.py` (enmascarado), `forense/prereg-duelo-v2/corridas-R/correr-R.py`, `tests/dbfmini.py`, `instrucciones-proyecto-v2_12.md` (sólo `grep` de A.3/A.13/D-10…D-12/SELLA-3), `AGENTS.md` (sólo barrido), `forense/encargos/convencion.md`, la **primera línea** de `forense/encargos/2026-09-02-*.md` (títulos, incluidos los de actos vetados: 90 caracteres, sin contenido), `forense/encargos/2026-09-02-MAESTRA35-N6-ESTADO-PROGRAMA-v1_11.md` (cabeza y cola, 0 cifras; N6 no está vetado), `data/manifiesto.yaml` (líneas 985-1035 y `grep` de dos ids), `forense/prereg-duelo-v2/espec-R-ciega-v1_2.tsv` (entera: es el insumo; barrida por columna), `forense/prereg-duelo-v2/codificacion-R-v1_0.tsv` (**sólo** línea 1, las cuatro filas por `grep -P` y las columnas `#id`/`estado`/`fecha` de todas las filas), listados de nombres (`forense/prereg-duelo-v2/`, `corridas-R/`, `notas-arbitra/`, `forense/encargos/`, `forense/encargos/cola/`, `canon/`, `tests/`), `data/raw/BD_ENCUCI2020_dbf.zip` (nombres de miembros y `testzip`), `data/raw/FD_ENCUCI2020.pdf` (texto: índice, preguntas 5.16-5.18 y diseño muestral de `SEC_4_5`), `.gitignore` (`grep data/raw`), `git show origin/main:canon/gobernanza-v1_15.md` **sólo a través de un regex que imprime conteo/máximo/duplicados/huecos** (ningún texto), `git show origin/main:forense/firmas-pendientes.tsv | cut -f1` (sólo ids), metadatos de git/gh (`log`, `pr view 470`, `pr list`). **Entre COMMIT-1 y COMMIT-2:** los cuatro payloads (contenido, ADR-46 declarado) vía `--produce`, `--regresion` y el script de guardias; `tests/check.py:1300-1345` (bloque `_T22_ARCHIVOS_CONOCIDOS`); `tools/calibracion_mordida_encig_serie.py` (contexto de un docstring); los cuatro JSON propios; `gh pr view 486` y los ids `ADR-`/`FP-` de su diff. **Después de COMMIT-2:** lo listado en §0.

## §6 · Desviaciones, hallazgos y cosas que conviene saber

1. **Guardia corregida, no spec ni R (declarado).** La primera corrida del script de guardias comparó `AP5_16_x == '1'` como cadena y dio 0 contactos: en el DBF esos campos son `N` 19,15 y `dbfmini` entrega `1.000000000000000`. Se corrigió la guardia a comparación numérica → 13435 exacto. `AP5_17`/`AP5_18` son `C` 6 en el DBF (el FD dice «Numérico»), por eso la codificación de cadena compara bien. Va a `forense/hallazgos.md` con la regla operativa (leer el descriptor del DBF, no el FD, antes de codificar).
2. **T22 por mención.** El encargo (línea 19) y la nota COMMIT-1 (línea 85) disparan `_T22_MARCADOR_PENDIENTE` porque en la misma línea que `estado=PROPUESTA`
   aparece la palabra `remesas` (la variable de las tres celdas ENIGH), y el patrón del marcador busca `mesa` tras esa palabra clave en la misma línea. La corrida previa a la cascada dio exactamente esos dos FAIL y nada más. Resolución: `FP-253` cita ambos archivos en `dónde` (exención mientras esté ABIERTA/FIRMADA) **y** se censan en `_T22_ARCHIVOS_CONOCIDOS` para que la exención no muera cuando la fila cierre — precedente exacto: el encargo de `MAESTRA35-L2` en la misma lista. Es la única extensión de perímetro del acto (`tests/check.py`, 14 líneas, sólo censo) y se declara aquí.
3. **Un `.claude/` espurio en `corridas-R/`.** Al cambiar el directorio de trabajo del shell a `corridas-R/` para leer los JSON, el cliente creó `forense/prereg-duelo-v2/corridas-R/.claude/` con montajes de `/dev/null` (mecanismo del sandbox, «Device or resource busy» al borrarlo dentro). Se borró fuera del sandbox; nunca se añadió a git ni entró en ningún commit. Se anota para que nadie lo busque.
4. **`canon/registro-rotulos.tsv` tiene la cabecera en la línea 2**, no en la 1 (una fila `C MAESTRA33-C4` quedó antes de `espacio/valor/que_significa/donde_vive`). Preexistente, fuera de perímetro, no se tocó; se deja dicho.
5. **Ids duplicados en `codificacion-R-v1_0.tsv`, a propósito.** Desde este acto `FAM-M-05/06/07` y `TRA-M-02` tienen dos filas cada una (PROPUESTA + COMPUTADA). Es lo que el encargo pide («las filas PROPUESTA no se editan») y es compatible con el único lector de la tabla (`lee_codificacion()`, última fila gana), verificado con `--regresion`. Si algún día un lector exige unicidad, ésta es la nota que lo explica.
6. **Colisión de numeración, declarada y pagada tres veces.** `ADR-302` y `FP-252` los reclamaba también `PR #486` (`MAESTRA35-L7`); ver §3. Al cerrar, `origin/main` había avanzado a `22b563c` (`PR #487`, `MAESTRA35-L8`, fusionado a las 19:35) y se había llevado `ADR-302`: este acto renumeró a `303`. Mientras se resolvía esa fusión, `PR #486` fusionó (78304f3, 19:42), re-derivó a su vez y se llevó `ADR-303` y `FP-252`; el primer intento de fusión se abortó (`git merge --abort`, sin commit) y este acto renumeró de nuevo a **`ADR-304`** — las dos veces verificado contra `git show origin/main:canon/gobernanza-v1_15.md` por comando (entradas, máximo, duplicados, huecos), no por título, y tocando sólo las citas propias: la cabecera de la entrada, `gobernanza:2`, `estado-programa:27`, la fila «máximo vigente» de la tabla de `ADR-301` y la `L0`, la fila del registro, el hallazgo, el `## CONSUMIDO` y esta nota. `FP-253` se conserva: es el contiguo al `FP-252` de main. Los conflictos de fusión (cabecera y cola de `gobernanza`, `L0`/27/343 de `estado-programa`, colas de `hallazgos.md` y `firmas-pendientes.tsv`) se resolvieron por reconstrucción aritmética desde `git show :1:/:2:/:3:` — prefijo + anotación propia + anotaciones ajenas + cola común del ancestro, con la longitud verificada como suma exacta —, nunca editando marcadores a ojo; `registro-rotulos.tsv` auto-fusionó (main añadió la fila `MAESTRA35-L7` al final; este acto sólo cambió la línea 127). **Tercera vez:** con `PR #490` ya abierto y `MERGEABLE`, `PR #492` (`MAESTRA35-N8 · SELLA-L7`, 19:54) fusionó y se llevó `ADR-304`; el PR pasó a `CONFLICTING`, este acto renumeró a **`ADR-305`** con el mismo procedimiento y volvió a fusionar `origin/main` = `e1afa2a`. `FP-253` sigue libre en main (máximo `FP-252`); `PR #491` (`MAESTRA35-L9`) reclama también `ADR-304` y `FP-253`: quien fusione segundo renumera.
7. **`tests/check.py --baseline` sobre el árbol final:** ver el pie de esta nota.

## §7 · CONTADOR (el real, medido sobre `corridas-R/` contra los 14 ids de la proyección)

Celdas del sorteado v1_2 con `R`: 10 → **14 de 14** (`CIV-M-01/02/04/10/12/13`, `DIN-M-01b`, `FAM-M-01/05/06/07`, `TRA-M-02/03/07`, todas `COMPUTADO`) · familia con `R`: 1 → **4** · trámite con `R`: 2 → **3** · cívico 6, dinero 1 (sin cambio) · celdas puntuables L∩M∩R: **+4** (las puntúa `N3`/`N5`, no este acto) · estimaciones fuera de `R`: **0**. Coincide con lo que el encargo anticipaba («10 → hasta 14»); el «R 11 → 14» de `ADR-277` sigue siendo el conteo de filas de codificación, no de R computadas, como L2 ya declaró — no se corrige el tablero.

## §8 · Lo que este acto NO hizo · sucesores

No reparó herramienta; no re-computó ninguna `R` existente; no tocó `M`/`L`/scoreboard, `marco-M-*`, `milpa/`, `corridas-M/`, `corridas-L/`, `data/` ni `tools/arbitra.py`; no leyó el motor antes de COMMIT-2; no editó instrucciones ni `.claude/commands/acto.md`; no puntuó. Sucesores declarados, no lanzados: `/despacha` decide si `N3` se re-corre con 14 `R` o `N5` las lee.

## Suite

`python3 tests/check.py --baseline` sobre el árbol final de este acto (antes de fusionar `origin/main`): exit 0, **LÍNEA BASE: VERDE — nada nuevo frente a `tests/baseline.json`** (HEAD congelado `e4af4ed`). La corrida previa a la cascada (tras COMMIT-2, antes de tocar canon) daba ROJO con exactamente dos entradas nuevas, los dos `T22` por mención de §6.2, y nada más; la primera corrida sobre esta nota añadió tres `T03` por citar `.claude/commands/acto.md`, `.claude/commands/arbitra.md` y el índice de memoria por su nombre pelado, corregidos citando la ruta completa o sin nombre de archivo. El núcleo (`CHECK_SELFCHECK_CHILD=1`) se cita en CI, no aquí. Tras fusionar `origin/main` = `78304f3` (dos renumeraciones, §6.6) la suite se volvió a correr: exit 0, VERDE. Tras la tercera fusión (`origin/main` = `e1afa2a`, `ADR-305`) se corrió de nuevo: exit 0, VERDE. PR: `#490` (https://github.com/Josanoforo/Modelado-Mexicano/pull/490), abierto contra `main`, sin fusionar — el merge es de mesa.
