Contadores movidos: 0.

# Encargo A · Canon v2.3 + ADR-54 (`sens_estatus`) + líneas a `hallazgos` — nota de sesión

**Sin módulo de auditoría** — este artefacto no afirma nada sobre México (v2.3, 3.4). No es un report temático, integrador, modelo de decisión ni validación forense.

## 0 · Entorno

`curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://github.com` respondió `400` (respuesta HTTP real vía proxy del entorno, no bloqueo) — suficiente para operar `git`. Clon no necesario: la sesión ya tenía el repo en disco, en la rama `claude/new-session-akki1f`, con `origin` apuntando a `Josanoforo/Modelado-Mexicano`. `git fetch origin main` trajo `main` a `e761b78`, el mismo HEAD contra el que se redactó el encargo — todas las premisas de abajo se verificaron contra ese HEAD sin necesidad de re-fundar nada. No se tocó `data/raw` ni INEGI.

## 1 · Verificación de premisas (contra HEAD `e761b78`)

- **PR-1** ✔ `head -1 instrucciones-proyecto-v2.md` → `... v2.2`.
- **PR-2** ✔ `grep -n "último" canon/gobernanza-v1_15.md` (línea 8, la cabecera `VERIFICAS ASÍ`) cierra con "el último es **ADR-53**". (El grep literal `"el último es ADR"` que sugería el encargo no matchea porque el texto real trae `**ADR-53**` con marcado bold entre "es" y "ADR"; se re-derivó con un patrón más laxo.)
- **PR-3** ✔ `grep -n "8+2+1+3" canon/modelo-decision-v4_0.md` → línea 272, control de suma. `grep -n "sens_estatus" ...` → línea 269, fila "Sin reactivo verificado — búsqueda abierta", con el texto exacto "ningún acto de búsqueda nombrado todavía". Suma verificada a mano: 8+2+1+3 = 14.
- **PR-4** ✔ `forense/notas/2026-08-04-sens-estatus-examen-descriptor.md` existe; línea 69: "**Ninguno sirve.** Los cinco instrumentos permitidos..."; línea 110 cita el fundamento "descriptor literal, no supuesto".
- **PR-5** ✔ `grep -n "aversion_riesgo" canon/gobernanza-v1_15.md` → línea 527 (dentro de ADR-52 A): "único con candidato examinado y descartado con argumento".
- **PR-6** ✔ `grep -c '^=======$' forense/hallazgos.md` → `0`.
- **PR-7** ✔ `python3 tests/check.py --baseline` (antes de tocar nada) → `19 FAIL · 84 WARN`; línea base ROJO, 1 entrada nueva (T17, `estado-programa-v1_9.md:113=25` vs `hitoD-preregistro-v2_0.md:8=27`), 4 entradas de la línea base congelada ya no aparecen. Coincide con lo declarado en el encargo (cita `:113`, no `:111` — la línea de baseline citaba `:111`, se re-derivó como el mismo hallazgo con corrimiento de línea, no uno nuevo). Salida completa guardada como línea base de esta sesión.
- **PR-8 / PR-9** — de mesa, no verificables contra repo. Transcritas aquí sin ejecutarlas como instrucción independiente: la autoridad del §3 v2.3 es de mesa (sello final: el merge); el mandato de este encargo se lee como aprobación de CP-5, con CP-1…CP-4 sin responder — en particular, cerrar `sens_estatus` por la vía del precedente-examen **no** adjudica la caducidad ante bloqueo de entorno (CP-4), que queda declarada abierta en ADR-54.

No apareció ninguna instrucción de procedencia no identificable en el contexto de esta sesión — no hay nada que transcribir bajo la convención de transferencia del §1.

**Contaminación (ADR-46).** Al leer `forense/notas/2026-08-04-sens-estatus-examen-descriptor.md` (PR-4) esta sesión queda inhabilitada para pre-registrar contra los cinco instrumentos que ese examen recorre. Aceptado — esta sesión es de canon, no de pre-registro.

## 2 · Acto 1 — `instrucciones` v2.2 → v2.3

Anclas verificadas únicas antes de insertar (todas encontradas, todas únicas): título L1, párrafo "Por qué v2.2", "Escribe el reporte final en español." + "`[NUEVO]` Regla de oro", bloque `[NUEVO v2.1]` "Ninguna cifra esperada...", encabezado "Módulo de auditoría de rigor extremo", pregunta v2.2 sobre deudas asumidas, párrafo final de "Nota de alcance" (v2.1). Ninguna faltó, ninguna estaba duplicada — no hubo condición de paro en este acto.

**Clasificación del `git diff` de `instrucciones-proyecto-v2.md`** (7 hunks):
- (a) título/cabecera: 1 hunk (línea 1, v2.2→v2.3).
- (b) bloques 3.1–3.6: 6 hunks (párrafo "Por qué v2.3"; Regla de señal; refinado de "receta de derivación"; "Dónde va, y dónde ya no" del módulo de auditoría; pregunta de contadores movidos; cierre de Nota de alcance).
- (c) cualquier otra cosa: **vacío**.

`python3 tests/bitacora.py --abre` reporta `v2.3` como versión vigente de `instrucciones` tras el delta.

No se tocaron los párrafos "Por qué v2.1"/"Por qué v2.2" existentes — el del repo mandó sobre el resumen distinto que traía la copia de chat, como indicaba 3.0.

## 3 · Acto 2 — ADR-54

**Cascada, derivada (no copiada del encargo):**
- `grep -rn "sens_estatus" canon/` y `grep -rn "búsqueda abierta" canon/` dieron como puntos de contacto reales: `modelo-decision-v4_0.md:269,270,272,278,725` (tabla de reparto, control de suma, nota de reparto, condensado §12) y `gobernanza-v1_15.md:8,527` (cabecera, texto histórico de ADR-52 A — este último **no se edita**, es historia del acto que ADR-52 selló, no una afirmación vigente que ADR-54 deba corregir).
- La lista del encargo (⟨3⟩) coincidió con lo que el grep encontró, salvo que **no incluye una edición de `estado`** para `sens_estatus`/reparto: `grep -n "sens_estatus\|8+2+1+3\|reparto" canon/estado-programa-v1_9.md` no devolvió ninguna afirmación de estado vigente sobre `sens_estatus` o el reparto — solo la línea L0 de conteo de ADR, tratada aparte (ver abajo).

**Motor (`modelo-decision-v4_0.md`):** fila "búsqueda abierta" ahora solo `exposicion_violencia` (cuenta 1); fila "búsqueda cerrada" ahora `aversion_riesgo` + `sens_estatus` (cuenta 2), con racional nuevo citando `PR #64` / la nota de PR-4 y el precedente ADR-52 A; control de suma re-derivado a `(8+1+2+3)` sumando la tabla, no de memoria — 8+1+2+3=14 verificado a mano. Misma cirugía en la "Nota de reparto" (línea 278) y en el condensado §12 (línea 725, antes "2 búsqueda abierta / 1 búsqueda cerrada", ahora "1 búsqueda abierta / 2 búsqueda cerrada").

**`gobernanza-v1_15.md`:** ADR-54 insertado como enmienda in situ tras ADR-53, mismo formato de casa (número de versión NO sube, archivo NO se renombra, excepción declarada a ADR-36); declara explícitamente que la condición de caducidad ante bloqueo de entorno (CP-4) no se decide en este acto. Fila nueva en la bitácora de versiones (§7), al tope (orden más-reciente-primero, igual que las filas de ADR-53/52/51). Cabecera "el último es **ADR-53**" → "... y **ADR-54** (...) — el último es **ADR-54**".

**Efecto lateral encontrado y corregido:** `T15 T-ADR-COUNT` (test existente, no nuevo) deriva el conteo de ADR únicos en `gobernanza` y lo compara contra tres citas literales de "53 ADR": `gobernanza-v1_15.md:2` (cabecera), `estado-programa-v1_9.md:25` (tabla de artefactos) y `estado-programa-v1_9.md:97` (línea L0, que además trae la cadena histórica "a 52 después... a 53 después..."). Al añadir ADR-54 el test pasó a fallar (3 fail nuevos) hasta corregir las tres citas a "54 ADR" y extender la cadena de L0 con "a 54 después, con ADR-54 (...)". Esto **no** es un texto de mesa del §3 — es una cascada mecánica de conteo que el propio test exige y que el mismo patrón de ADR-52/ADR-53 ya establecía; se corrigió en el mismo acto en vez de reportarse como hallazgo.

**`estado-programa-v1_9.md` — resto:** ninguna otra mención de `sens_estatus`/reparto en estado vigente; no se tocó nada más ahí. `hitoE` y las notas fechadas no se tocaron (registros de historia).

## 4 · Acto 3 — `hallazgos.md`

Re-derivado PR-6 en este HEAD (`grep -c '^=======$'` → 0) antes de tocar el archivo. Seis líneas anexadas **textuales**, tal cual venían en el encargo §5, al final del archivo — `git diff --stat` confirma `6 insertions(+)`, cero líneas tocadas ni removidas.

## 5 · Suite — antes/después

**Antes** (antes de cualquier edición, PR-7): `19 FAIL · 84 WARN`; línea base ROJO con 1 entrada nueva (T17) y 4 entradas de baseline ya no presentes.

**Después** (tras la última edición de todo documento que la suite reporta — Acto 3 fue el último): `19 FAIL · 84 WARN`; misma única entrada nueva de línea base (T17, la pre-existente de la mesa #18, cita `:113` vs baseline `:111` por corrimiento de línea en `estado`), mismas 4 mejoras. **Ninguna entrada nueva causada por este PR** — el fail transitorio de T15 (3 fail) que apareció tras insertar ADR-54 se corrigió en el mismo acto (ver §3) y no aparece en esta corrida final. No se usó `--freeze` en ningún momento.

## 6 · Contadores movidos

**0.** Este encargo es trabajo de instrumento/canon: sube una versión de instrucciones, cierra una clase de reparto por precedente ya sellado, y anexa hallazgos de contabilidad. No mide nada sobre México, no toca `milpa/procedencia.yaml`, no re-congela `tests/baseline.json`.
