# ACTO SELLA · las ocho decisiones de mesa del 13/ago que corrieron y nunca se escribieron — sella `ADR-76`

**Acto:** ACTO SELLA · **Entorno:** repo-only, NUBE, `cloud_default`, sin `data/raw`, sin sonda HTTP (no aplica) · **SHA de redacción del encargo v1:** `5f3ee91` · **SHA de redacción del encargo v2:** `5f3ee91` + tres ramas vivas sin fusionar (ver §1) · **SHA real al sellar:** `7ac523e` · **Depende de:** `ACTO REAPERTURA-52A-54` (#207), `ACTO E2` (commit `40ba303`), `ACTO ADJ-4` (#209, commit `9154072`), `BENCHMARK-ENLACE` (#210), `ACTO RECONCILIA-PUERTAS` (#208), `ACTO GDELT-UCDP-RECON` (#212). Este acto no corre ningún diseño, no estima nada, no abre payload — propaga y sella firmas verbatim de mesa ya tomadas.

## §0 · ARRANQUE

1. **REPO.** Clon existente en uso: `/home/user/Modelado-Mexicano`. Ningún clon nuevo.
2. **SHA.** El encargo v1 declaraba `5f3ee91`; confirmado sin divergencia en el primer intento. El encargo v2, recibido tras el primer PARO, ya declaraba que `main` podía haber avanzado y que el número de ADR debía re-derivarse — así fue: `main` avanzó a `7ac523e` durante este acto (tres PRs fusionados, ver §1), y el número de ADR se re-derivó dos veces (75 en el primer intento, 76 en el segundo, tras el GATE).
3. **data/raw.** Ausente, no requerida por este acto. Confirmado y saltado, dos veces (v1 y v2).
4. **ENTORNO.** `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=cloud_default`. Sonda HTTP saltada — este acto no toca red ni microdato.
5. **ESPEJO.** Toda cifra de esta nota y de `ADR-76` sale del clon de (1), con el comando a la vista donde aplica.

## §1 · Dos paros antes de escribir una sola línea de canon

**Primer intento (encargo v1, sella incisos (a)-(g) con inciso (b) sobre la reserva de género de `P7_12_7`).** El PARO DE CONCURRENCIA del propio encargo instruía: `git ls-remote --heads origin`, y si existía una rama viva de "ACTO RES" que sellara el mismo inciso (b), parar. Verificado: `claude/new-session-b0pwaf` estaba viva, con un commit `fb4bade` — "ACTO RES: sella ADR-75 -- RESUELTA-ACOTA la reserva de encuadre de genero de P7_12_7, firma enmienda de ENASIC-SPLIT". Colisión real, no hipotética: mismo inciso, mismo número de ADR derivado (75, contra el `5f3ee91` de ese momento). Parado y reportado sin escribir ningún archivo. Diligencia adicional, no exigida por el encargo pero barata: se verificaron las otras dos ramas vivas del `ls-remote` (`ra76ye`, `ytzzm9`) — ninguna colisionaba, ninguna tenía PR abierto en ese momento.

**Segundo intento (encargo v2, recibido tras el primer PARO — corrige el v1: retira el inciso de la reserva de género por completo, ya sellado por `ADR-75`; añade un GATE explícito).** El GATE de v2 nombraba las tres ramas (`ytzzm9`/ACTO E2, `ra76ye`/ACTO CEP, `b0pwaf`/ACTO RES) y decía, verbatim: *"Si `b0pwaf` no ha fusionado, PARA."* Verificado en el momento de recibir v2: las tres ramas seguían vivas en `ls-remote`, `b0pwaf` con un PR recién abierto (#215) pero sin fusionar (`merged: false`). Segundo PARO, reportado, sin escritura. **Retomado solo cuando el usuario pidió explícitamente verificar si ya habían fusionado** (`git ls-remote --heads origin` → solo queda `main`; `git merge-base --is-ancestor fb4bade origin/main` → sí; `origin/main` avanzó de `5f3ee91` a `7ac523e`). **Discrepancia encontrada y no heredada:** la API de PRs de GitHub marcaba las tres PRs (#213, #214, #215) como `state: closed, merged: false` — pero las tres están, verificablemente, en la ancestría real de `origin/main` (fusión local + push directo a `main`, no el botón de fusión de GitHub, consistente con la convención de este repo de prohibir el editor web de conflictos). Se confirmó contra `git log`/`git merge-base`, no contra el campo `merged` de la API. Con las tres fusionadas, la receta de T15 contra `gobernanza-v1_15.md` real dio `únicos:75 · max:75 · huecos:[]` → **ADR-76**, no 75 como calculó el primer intento contra una base ya superada.

Rama de trabajo (`claude/acto-sella-ocho-decisiones-5fx2ft`) actualizada por fast-forward a `7ac523e` antes de escribir nada — sin commits propios todavía en ese punto, así que el fast-forward no publicó contenido propio, solo sincronizó con lo que `origin/main` ya tenía público.

## §2 · Los ocho incisos — resumen; texto completo en `ADR-76` (`canon/gobernanza-v1_15.md`)

Cada inciso trae su firma de mesa verbatim citada en el propio `ADR-76`; esta sección no la repite, registra qué se verificó y qué correcciones de premisa encontró este acto antes de escribir.

**(a) Reapertura de ADR-52 A / ADR-54.** Verificado contra `forense/notas/2026-08-13-reapertura-52a-54-commit1-preregistro.md` y `forense/notas/2026-08-13-reapertura-52a-54-commit2-barrido.md`: universo 104 payloads (no 5), 94 diccionarios abiertos, 2,477 coincidencias crudas, 208/208 `NO-ENCONTRADO`. **Dos correcciones de premisa encontradas, ninguna inventada por este acto** — ya las había atrapado `ACTO E2` al cerrar la Entrada 2 (`forense/notas/2026-08-13-e2-cierre.md`), y este acto las re-verificó de forma independiente contra la fuente antes de escribirlas: la cifra de LAPOP es **79 de 81**, no 81; y son **seis** casos documentados con detalle propio (commit2-barrido.md §5), no tres — el propio transfer solo nombraba dos de los seis. Enmienda in situ escrita sobre ambos ADR, no repetición: la reapertura ocurrió, no acotada, y las dos búsquedas se sostienen cerradas.

**(b) `R5.1-D3`.** Verificado: `ADJ-4` = commit `9154072` (hash completo `91540723d8d6395e6e098caa4c82dc5fad02c743`), PR #209, ancestro confirmado de `origin/main`. La cabecera de `forense/encargos/2026-08-13-r5-1-d3.md` decía que `ADJ-4` no existía — cierto cuando se escribió, falso desde ese commit. Corregida con tachado + nota fechada, mismo mecanismo que el resto del corpus usa para premisas vencidas. **Nota de perímetro, declarada:** ese archivo no está en la lista corta de cuatro archivos que el encargo v2 declara como perímetro — se editó porque el propio inciso (b) lo instruye por nombre de archivo y por texto exacto a corregir. Es una extensión de perímetro de un archivo, hecha a propósito y declarada aquí, no una desviación silenciosa.

**(c)/(d) `D-ABC` e invarianza ENCUCI↔ENBIARE.** Ambos textos ya redactados en `forense/benchmark-enlace-invarianza-v1_0.md` ("Las dos propuestas de sello, listas para firma de mesa"), copiados sin reescribir. Verificado que la Propuesta 1 (D-ABC) trae, explícita, la corrección de la premisa que el propio benchmark refuta — que colapsabilidad implica que una inversión de signo es señal real; esa lectura está marcada *non sequitur* por dos auditorías adversariales del propio repo (`auditoria_adversarial_benchmarks.md`, `red-team-auditoria-benchmarks.md`), verificado contra esos dos archivos, no solo contra el resumen del benchmark.

**(e) Precedencia del puntero de puertas.** Ésta es la corrección de premisa más grande que encontró este acto. El propio encargo v2 cita, como si fuera la regla a sellar, la frase *"la más reciente cuyo `universo_declarado` cite un portal, no una tabla interna"* — verificado contra `PROPUESTA-reconciliacion-universo-puertas.md:26`, esa frase es la **candidata original**, y el propio documento la reemplaza explícitamente por una regla distinta (Regla 1: `clase_origen ≠ gap_mapeo_map_b` gobierna sin importar fecha; Regla 2: empate → fecha más reciente). Si este acto hubiera copiado la cita del encargo sin verificarla contra la fuente, `ADR-76(e)` habría sellado una regla que su propia fuente ya había descartado. Segunda corrección, menor: la cifra "14 de 16 parejas" que el encargo cita como verificada no reproduce contra la tabla que la propia nota (`forense/notas/2026-08-13-reconcilia-puertas.md` §4.2) trae como evidencia — recontada directamente, son **15 de 16** con fecha vieja posterior, y la pareja 16 (`ISSP`) está empatada, no invertida. Tercera verificación, de estado: el diff de 16 filas (verificado con `git apply --check`, aplica limpio contra el momento en que se escribió) **no se ha aplicado** — `data/universo-puertas-2026-08-12.tsv` creció de 114 a 120 filas desde entonces y las 16 filas contradictorias siguen ahí, las 16 confirmadas presentes por nombre. `ADR-76(e)` sella la regla, no el diff — `data/` no está en el perímetro de este acto.

**(f) Recongelado de línea base.** Verificado: `ADJ-4` revirtió `tests/baseline.json` al contenido de `e7cd99d` (campo `head` restaurado a `2cb39c9ceff4abe76d895a9739242c2e5d056516`, deshaciendo el recongelado no autorizado de `ACTO ENLACE-1` Commit 4 a `948ad70...`), y ese revert destapó una cita colgante en `forense/encargos/2026-08-13-enlace1-mapeo-id-manifiesto.md:7` (backticks alrededor de PLANENLACECAPA220260813.md, un documento externo, nunca un archivo del repo) — arreglada quitando los backticks. La formulación que sella `ADR-76(f)` es la que trae el propio encargo v2, verbatim, sin condiciones añadidas por este acto: recongelar exige ADR de mesa.

**(g) GDELT/UCDP.** Verificado contra `forense/notas/2026-08-13-gdelt-ucdp-recon.md` íntegro: las cifras del encargo (factor de extrapolación ×7.4, 417,968 filas UCDP totales, 25,714 de México) reproducen exactas contra la nota. `RECORTE-VIABLE` en las dos, `EXISTE-NO-SATISFACE` sin cambio.

**(h) A.7.** Verificado el estado real de `forense/registro-recalculo-v1_0.md`: E0/E1/E2/E4 cerradas, coincide con el encargo. **Corrección encontrada:** el encargo dice "E3 y E5 abiertas" — hoy son **tres** las abiertas, no dos: existe una Entrada 6 (censo-explotación vs. consumo real de producción), creada por `ACTO CEP` después de que el encargo se redactara. Los dos obstáculos de sellado se verificaron letra por letra: la colisión de tres vías del rótulo A.7 es real (`instrucciones-proyecto-v2_6.md:265`, `forense/encargos/2026-08-13-A7-indice-infraestructura.md:18`, y el borrador de MAP-B) y `forense/hallazgos.md:214` en efecto solo registraba dos. Corrección de cita: la frase "el texto completo... vive en la sesión/acto que sí las ejecuta" no está en la línea 87 de `2026-08-12-veredicto-pr185-mapeo-universo-map-b.md` (ésa es solo el título de §5) — está en la línea 93, y cubre §2/§4/§5 en conjunto, no solo §5.

## §3 · Cascada

Conteo de ADR (75→76) derivado con la receta de T15 contra `origin/main` real (`7ac523e`): únicos 75, max 75, huecos ninguno → siguiente 76. Sitios: `gobernanza-v1_15.md:2` (cabecera) · `estado-programa-v1_10.md:27` (tabla) · `estado-programa-v1_10.md:101` (las **dos** citas de esa línea — la cifra de apertura de la oración y el cierre del listado histórico; `ACTO RES` ya documentó, en su propia nota, haber cometido el error de tocar solo el cierre y perderse la apertura la primera vez — no se repite aquí). Contadores que NO se mueven, verificados uno por uno contra su fuente, no heredados: `13 de 27` (Hito D, `estado-programa:95`) · `9 de 14` (condicionales — misma razón que `ADR-75` ya dejó escrita: T19b deriva de `milpa/procedencia.yaml`, fuera de perímetro) · `0 de 15` (coeficientes) · `1 de 2` (llaves, `registro-llaves-identificacion-v1_0.md`) · `4 de 144` (congelado desde 31/jul). Ninguno.

## §4 · Cierre — verificación de línea base

`python3 -c "import json;print(json.load(open('tests/baseline.json'))['head'])"` → `2cb39c9ceff4abe76d895a9739242c2e5d056516`, sin cambio (este acto no toca `tests/baseline.json`).

`python3 tests/check.py --baseline`, corrido antes de editar y después de editar todos los archivos:

- **Antes:** `18 FAIL · 104 WARN` — LÍNEA BASE: VERDE. T15 `[ok]`.
- **Después:** `18 FAIL · 104 WARN` — LÍNEA BASE: VERDE. T15 `[ok]`, T19b `[ok]` (no se movió, verificado explícitamente). Idéntico byte a byte en el resumen de cifras.

Ningún archivo fuera del perímetro declarado de `ADR-76` (cierre del propio ADR, arriba) fue escrito, salvo la extensión de un archivo declarada en (b) y esta misma nota + `forense/hallazgos.md`, ambos dentro del perímetro original.
