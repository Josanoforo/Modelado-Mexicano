Contadores movidos: 0 en Hito D (una ficha no es una corrida). Fichas: 25 → 27.

# Encargo O · Fichas R3.1 y R3.4 — cierra T17 escribiendo lo que faltaba

**Mesa #18, 4/ago/2026.** Sesión LIMPIA de Claude Cloud: no abrió ENCIG, ningún cuestionario de `§3.3`, ni series de SPEI/CoDi de Banxico. Solo leyó `canon/`, `forense/hitoD-preregistro-v2_0.md` (incluidas sus Notas fechadas), `milpa/` y `corpus/reports/`.

## 0 · Entorno y discrepancia de HEAD

`git log` local mostraba `HEAD` en `a0d8fd5` (branch `claude/fichas-r3-1-r3-4-t5a02g`, ya creada desde la sesión que emitió el encargo), coincidente con el `main` real verificado vía la API de GitHub (`mcp__github__list_commits`, tip `a0d8fd5`, PR #81). Un primer `git fetch origin main` a través del proxy local devolvió un árbol muy distinto y muy anterior (tip `9301e59`, "Corpus inicial: 60 artefactos") — **discrepancia del proxy de red, no del repositorio real**: verificada y descartada contra la API de GitHub antes de proceder. Se procedió desde `a0d8fd5`, el HEAD real, documentando la discrepancia aquí en vez de fusionar contra el árbol falso del proxy.

## 1 · Premisas verificadas contra archivo (Bloque de la mesa, PO-1 a PO-7)

- **PO-1.** `grep -cE "^## R[0-9]"` da **25** antes del acto (no 26 — ese conteo viene de `grep -c "^## R"` sin `[0-9]`, que además cuenta el encabezado `## Registro de veredictos archivados`). Verificado con ambos comandos.
- **PO-2.** Confirmado contra `canon/estado-programa-v1_9.md:113` (declaración canónica §4·S2, antes de esta sesión): faltan exactamente `R3.1` y `R3.4`; `R3.2` ya tenía ficha (Nota 4); `R3.3` es `[MEDIA]`, fuera del perímetro.
- **PO-3.** Confirmado contra `canon/modelo-decision-v4_0.md:659` ("Decisión: los IDs son un registro CONGELADO, no una fórmula") y la tabla de §7 (líneas 674-677).
- **PO-4.** Nota 3 de `hitoD-preregistro` (29/jul/2026) leída completa e incorporada en la ficha de `R3.4` (Nota 15) **antes** de su escala, como exige.
- **PO-5.** ADR-37 verificado en `canon/gobernanza-v1_15.md:261-272`: tres condiciones (A reproducción, B prueba de mecanismo ≥70% colapso, C anti-confusión <30% reducción), B y C marcadas `ASIGNADO` en el propio archivo (línea 271).
- **PO-6.** Nota 4 (`R3.2`, líneas 396-452 antes de este acto) leída completa y usada como plantilla de formato para ambas fichas nuevas.
- **PO-7.** Confirmado: las dos fichas se añadieron como Notas 14 y 15, al final, sin tocar el cuerpo de arriba ni las Notas 1-13.

Ninguna premisa (1) falló. No hubo PARO.

## 2 · Lo escrito

Dos Notas fechadas nuevas en `forense/hitoD-preregistro-v2_0.md`:

- **Nota 14 · `R3.1`** — trámite presencial discrecional sin registro → mordida `[FUERTE]`. Hallazgo declarado antes de diseñar el falsador: esta regla comparte fuente y población con `R3.2` (ya corrida, veredicto B, Nota 6), cuyo techo medido de incidencia presencial (13.38%, ENCIG2023, seis cómputos) hace que cualquier lectura de "alta" como mayoritaria (≥50%, la lectura que combina con el `p=0.62` asignado del motor) sea **ya conocida como gate inalcanzable por construcción** — mismo modo de falla que `R3.2` encontró, declarado aquí antes de abrir nada (disciplina `CAL-X` de `CAL-G3`, Nota 8). Por eso el falsador de esta ficha no se fija sobre nivel absoluto de incidencia (ese camino ya está pre-cerrado por un hallazgo archivado del propio corpus), sino sobre una dimensión que `R3.2` no aisló: el grado de discrecionalidad/anonimato **dentro** de la rama presencial — literalmente lo que el `SI` de la regla nombra y que el confundidor 1 de `R3.2` ya señalaba sin resolver. Umbral ASIGNADO (20pp, mismo número que `R3.2`/`R7.2` por consistencia interna, no por derivación). Serie/año/corte/variable fijados (ENCIG2023, submuestra presencial). Cuatro confundidores estructurales, cuatro filas de escala mutuamente excluyentes con regla de precedencia explícita.
- **Nota 15 · `R3.4`** — el gate: gobierno digital coercitivo (CoDi) vs. útil (SPEI) `[MEDIA-FUERTE]`. El límite de Nota 3 ("el gate no separa coerción de fricción") se incorpora **antes** de la escala, con la lectura que queda prohibida si el gate pasa declarada explícita: un veredicto A no licencia leer que el mecanismo *es* riesgo fiscal percibido y no fricción — el gate nunca apagó la fricción de forma independiente del riesgo fiscal, porque ambas covarían por diseño del propio producto CoDi (confundidor 2). Ancla del tier verificada: a diferencia de `R3.1`/`R3.2`, esta construcción sí tiene entrada positiva y propia en un mapa de evidencia (`corpus/reports/Adopción_y_Resistencia_Tecnológica...md:59-68`, EVIDENCIA FUERTE) y en el glosario (`canon/glosario-v5_6.md:152`, §6, tier Media-Fuerte, alcance citado literal). Las tres condiciones de ADR-37 declaradas con sus umbrales marcados `ASIGNADO`, citando `gobernanza-v1_15.md:263-265,271` línea por línea. **Hallazgo colateral, sin resolver:** discrepancia numérica entre `tramite.yaml:61` (backtest "3.09M cuentas") y el report ancla (21.8M cuentas registradas, 17.8M transacciones, misma ventana de seis años) — no se abrió Banxico para resolverla; queda registrada para que la calibración de Fase 1 (ya exigida por `gobernanza:271`) no la descubra tarde. Regla de selección de pregunta con Respaldo 2 pre-registrando, como desenlace más probable, que ninguna fuente pública separe coerción/riesgo fiscal de fricción de uso — degradando B y C a inejecutables aunque A sí sea medible. Cuatro filas de escala mutuamente excluyentes con regla de precedencia estricta (A→B→C→D, "no evaluado" y "evaluado y falló" declarados estados disjuntos por definición).

Ninguna ficha ejecuta ningún falsador. Ambas quedan pre-registradas, sin corrida.

## 3 · Cascada

- `canon/estado-programa-v1_9.md:113` — declaración canónica de cobertura corregida de 25 a 27 fichas, desglose por tier re-derivado (20 `[FUERTE]` + 5 `[MEDIA-FUERTE]` + 1 `[FUERTE como correlación]` + 1 `[FUERTE / MEDIA]`), "Faltan 2" cerrado in situ con marca fechada, sin editar el texto original (tachado + corrección, mismo molde que el resto del archivo).
- `canon/estado-programa-v1_9.md:190` — "`R3.4` sigue sin ficha" cerrado in situ, mismo molde.
- `canon/modelo-decision-v4_0.md:674,677` — columna "¿Ficha?" de la tabla de IDs pasa a "Sí" para `R3.1` y `R3.4`, con cita a la Nota que la sostiene.
- **No se tocó:** la cabecera línea 8 del pre-registro (append-only; con 27 fichas reales deja de ser falsa sola, sin editarla), la Nota 2 (narra la historia real: 24 antes del 29/jul), ningún forense fechado previo, `milpa/tramite.yaml`, `milpa/procedencia.yaml`.

## 4 · Módulo de auditoría de rigor extremo

Esta ficha no afirma nada sobre México — pre-registra dos falsadores, no ejecuta ninguno. Aun así, sí hay un juicio propio que vale declarar: el hallazgo de que `R3.1`, leído literal contra el nivel absoluto de incidencia, comparte fuente y población con un veredicto ya archivado (`R3.2` → B) y por tanto arriesgaba producir una ficha redundante disfrazada de nueva si no se declaraba antes de diseñar el falsador. Se declaró antes de escribir el resto de la ficha, no después de notarlo por accidente — mismo principio que el propio archivo exige para R1.3/R1.4 en su cierre del Paso 1 ("declarado antes de buscar: encontrarlo en el Paso 2 habría parecido hallazgo y habría sido recuerdo").

## 5 · HECHO

1. `grep -cE "^## R[0-9]" forense/hitoD-preregistro-v2_0.md` → **27**.
2. `estado` dice 27 con desglose re-derivado.
3. T17 verde (ver suite abajo).
4. Ambas fichas con serie/año/corte/variable/umbral/confundidores/escala y regla de precedencia entre filas.
5. `R3.4` incorpora el límite de Nota 3 y las tres condiciones de ADR-37 (con umbrales ASIGNADO).
6. Tabla de IDs del motor actualizada.
7. Suite corrida tras la última edición (ver abajo).
8. PR abierto, no fusionado por esta sesión.
