# REGION · ENIF 2024 · spec humana v1.0

El primer resultado que produzca este procedimiento es el que se reporta.

R1 y R2 aprobadas en sesión de 23/sep/2026. ENIF 2024 publica seis regiones oficiales de diseño, códigos `REGION=1..6`; no se fuerza una fila estatal. La ficha `enif_2024_fd.xlsx`, hoja TMODULO, filas 1471–1483, documenta esos códigos, `FAC_PER`, `EST_DIS` y `UPM_DIS`. Las seis regiones son 1 Noroeste, 2 Noreste, 3 Occidente y Bajío, 4 Ciudad de México, 5 Centro Sur y Oriente, 6 Sur. La tabla TMODULO no incluye entidad actual de residencia; `P3_15_EPC` se refiere al lugar de residencia hace cinco años y no la sustituye. Este CALC mide una conducta inicial y no cierra todavía el universo U5.

Conducta `dinero.ahorro.tiene_ahorros` / `tiene_ahorros_enif2024`, exactamente como `tools/medidor_ahorro_enif24.py`: unidad persona elegida 18+, numerador alguna de `P5_1_1..P5_1_6` o `P5_6_1..P5_6_9` = 1, denominador al menos una respuesta 1/2 en esas quince variables. El blanco por secuencia no equivale a ahorro. Factor `FAC_PER`; diseño `EST_DIS×UPM_DIS`. p_g = Σ FAC_PER·1(D,g)·Y / Σ FAC_PER·1(D,g), nunca media simple de tasas.

Plan común de 1 000 réplicas de UPM con reposición dentro de estrato en toda la ola, PCG64(20260923); módulo compartido por SHA256 `4df2c630179c194345594d959d012b7dd18d94ac93fab3f48b8f6683753dafd6`, con adaptador de clave sintética solo para la guarda. IC95 percentil. Estado `SUPRIMIDA-N` si n<200, `VARIANZA-NO-ESTIMABLE` si una réplica carece de denominador, menos de dos UPM de dominio o dispersión nula. Se conservan las seis filas y el n. Requisitos oficiales más estrictos tienen precedencia. `n_efectivo_kish` es diagnóstico, no una regla de supresión nueva. No se calibra persistencia en este CALC; RETROSPECTIVA; `adopta: NO`.

## Auditoría de rigor extremo

El ahorro observado mezcla oferta, ingreso, restricciones y preferencias. La región no identifica clase ni pertenencia indígena. El universo de persona elegida y sus seis regiones no permite nombrar una entidad concreta. Una comparación de 2024 con otras olas exige armonizar las preguntas y la cartografía oficial antes de atribuir cambio. Todo este análisis es RETROSPECTIVA.
