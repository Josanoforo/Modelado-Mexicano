**ADR-260921-GEN2-ENIGH2024-SERIE-Y-COMMIT-1-7492-01** (raíz de acto: `7492` = 4 hex del commit de 0-bis `74921d1a`; no se renumera al fusionar), `ACTO GEN2-ENIGH2024-SERIE-Y-COMMIT-1 · LAS SERIES DE ENIGH SE CONSTRUYEN SOBRE OLAS ABIERTAS: TRES MEDIDORES QUE SOLO TENÍAN 2022 GANAN 2016-2022 CON ORO BYTE-EXACTO, LA COMPARABILIDAD 2022→2024 SE CONFIRMA POR TEXTO SIN ABRIR EL DATO RESERVADO, Y EL DUELO 2024 QUEDA CONGELADO EN PROCEDIMIENTO (NO EN ADOPCIÓN: FALTA LA FIRMA b7ae-02)`, 21/sep/2026, entorno **CAJA** (`ENTORNO-DERIVADO = CAJA`, corpus montado, `sin_variable`, sonda 200), Sonnet 5 por mandato de mesa (presupuesto, cabecera del encargo), 2 sub-agentes fork de solo lectura (verificación de premisas contra el árbol; reconocimiento del patrón de commits de los precedentes ENUT/ENCIG — ningún commit propio, cero escritura). Encargo archivado verbatim (A.3) con sello de cuerpo: `forense/encargos/2026-09-21-GEN2-ENIGH2024-SERIE-Y-COMMIT-1.md` + `.cuerpo.sha256` (`f5913666…`). SHA de redacción `99a43faf` (= `origin/main` al abrir, guard 0.a-0.d en verde, sin rótulo duplicado); `main` se movió 30 commits durante el acto (PR #987 y mantenimiento de CI incluidos) — fusionado en `e9c00c9e` sin conflicto, suite `--rapido` VERDE después. **MODO ABIERTO hasta cada COMMIT-1, RÍGIDO después** (cumplido: ningún sello se tocó tras congelar).

**Premisa del encargo corregida contra el árbol (A.8, antes de escribir nada):** el encargo afirma que los cuatro sellados de ENIGH nombrados (incluido `CALC-ENIGH-0001`) están "medidos en una sola ola". **Falso para uno de los cuatro:** `remesas>0` (`CALC-ENIGH-0001`) ya está sellado en **cinco olas** (2014-2022) por dos CALC ajenos (`CALC-B-0001`, `CALC-B-MARCO-ENIGH-0001`) — no se re-corre; el trabajo real de P1 son los otros tres.

**CONTADOR:** `N_corridas_selladas` +10 (`CALC-ENIGH{2016,2018,2020}-{PERFIL-ESTRUCTURAL,INTENSIDAD-REMESAS,REMESAS-CONTEXTO}-0001` ×9, `CALC-ENIGH-DUELO-ORIGEN-MOVIL-0001` ×1); `cuenta_gen2 = SI` en las 10 (cabecera del encargo, «sella las series»; `CALC-ENIGH-DUELO-EMISIONES-0001` queda PREVISTO sin `ejecucion.json`, `cuenta_gen2 = NO` declarado en su propia spec); **no adopta** ninguna (`estado_adopcion: NO-ADOPTADO` en las 10); `adoptados_activos` sin cambio.

**P1 (series sobre olas abiertas).** Ventana 2016-2022 confirmada por dos vías independientes: `url_origen` del manifiesto (`_ncv_` en 2012/2014 vs `_nueva_serie_`/`_ns_` en 2016+) y `RESULT-BM-ENIGH-2014-METADATO-VERSION` (sellado, ajeno) — 2012 comparte metodología NCV con 2014 y también queda `VENCIDO EN ALCANCE`, corrigiendo la duda que el propio encargo dejaba abierta. Único cambio de código, verificado antes de generalizar: `poblacion.csv` de 2016/2018/2020 no trae `factor`/`est_dis`/`upm` (2022 sí) — tomados de `concentradohogar` por `folioviv+foliohog`, probado matemáticamente equivalente contra 2022 (1667/1667 personas de 500 hogares muestreados, cero discordancias). **Oro:** el `medidor.py` congelado de cada CALC nuevo, reconfigurado para leer 2022, reproduce byte a byte (`P1-MARGINALES-SHA256`/`P2-CONJUNTA-SHA256`/`P2-MARGINALES-COMPLETOS-SHA256`) y bit a bit (bootstrap de 2000 réplicas) los tres `resultados.json` ya sellados de 2022 (`tests/test_enigh_serie_oro.py`); sin re-sellar un CALC-2022 duplicado (D-14). Nueve `preflight`/`run` VERDE, sello `COINCIDE` en las nueve. Serie de `remesas_media` entre receptores: 7249 (2016) → 8331 (2018) → 9507 (2020) → 14455 (2022, sellado 2022).

**P2 (comparabilidad 2022→2024 por texto).** `data/enigh-comparabilidad-texto-v1_0.tsv` (misma forma que `credito-comparabilidad-texto`, adaptada: unidad de fila `variable`, no `conducta`×`ola` — ENIGH documenta por diccionario fijo, no por flujo de cuestionario): 14 variables, 13 `MISMO-INSTRUMENTO` / 1 `CAMBIO-MENOR` (`factor`: 2024 tampoco trae `factor`/`est_dis`/`upm` en `poblacion.csv` — mismo join que P1, verificado sin abrir el zip reservado por listado de miembros, A.7). `remesas` (P041) confirma por texto, con su código de reactivo intacto entre 2022 y 2024, por qué `remesas>0` ya pasaba la regla de entrada sin este acto. Cero microdato de `enigh2024_ns_csv.zip` abierto — sólo el diccionario (`enigh2024_descripcion_base_pdf`, ya en el corpus por `#977`) y el listado de miembros del zip (A.7).

**P3 (spec del duelo).** `forense/prereg-caja/DUELO-PROSPECTIVO-ENIGH2024-spec-v1_0.md` formaliza `DISENO-duelo-prospectivo-ENIGH2024-v1_0.md` (propuesta de `ACTO GEN2-ENIGH2024-RESERVA-Y-DISENO-1`, firma `FP-260921-GEN2-ENIGH2024-RESERVA-Y-DISENO-1-b7ae-02` **PENDIENTE**) a contrato ejecutable, sin repetir su prosa: estimando y ventana sin cambio, nivel cruce `NO-CONSTRUIBLE` confirmado (los `REMESAS-CONTEXTO` de las 4 olas ahora selladas por este acto son marginal por eje dentro de UNA ola cada uno, no historia de un cruce entre olas — la condición del diseño §3 sigue sin cumplirse), regla de adjudicación y B-bis sin cambio.

**P4 (código, ensayo, guardia, D-22).** `tools/enigh_duelo_nacional.py` (contendientes `C-PISO/C-T2/C-T3/C-TS/C-MEDIA`, código propio: `tools/duelo/` está declarado **ajeno** al perímetro de este encargo, mismo diseño matemático que el molde de ENVIPE). `CALC-ENIGH-DUELO-ORIGEN-MOVIL-0001` (sellado, oro «2022 como ola nueva» = la fila `ola=2022`, los 5 construibles a la vez): en la ventana retrospectiva de 3 olas, `C-MEDIA` MAE=0.1406pp vence numéricamente a `C-PISO` MAE=0.1867pp — **declarado como evidencia cruda, no como adjudicación** (B-bis-1 es sobre el duelo real de 2024, que este acto no abre). `tools/enigh_duelo_guardian.py`: único código autorizado a tocar `concentradohogar` de ENIGH 2024 antes de COMMIT-3 (E.6) — sin ejes (el diseño no los tiene), la guardia es que solo se leen 6 columnas y `nacional()` rechaza un marco `reservada=True`; 6 pruebas contra zip sintético (`tests/test_enigh_duelo_guardian.py`), nunca contra el real. `CALC-ENIGH-DUELO-EMISIONES-0001` (COMMIT-2): spec + medidor congelados, probado contra zip sintético propio, `preflight` **VERDE** sobre el input real por hash (`enigh2024_nc_csv` `[COINCIDE]`, identidad sin contenido — A.7) — **`run` nunca invocado**: PARO (a) del encargo prohíbe abrir `enigh2024*`.

**Estado de "congelado" (F3, quien congela no ejecuta; declarado con precisión para que mesa no tenga que releer el hilo):** el procedimiento (spec, medidor, guardia) está congelado en el sentido de D-15/D-18. La declaración fuerte «CONGELADO» del diseño (que autorizaría COMMIT-2/3) espera la firma `b7ae-02`, **PENDIENTE** — este acto no la asienta ni la asume: hacerlo tocaría una firma de mesa, PARO por lista cerrada. Se pregunta a mesa en `## NO-CORRIDO / RESERVAS` del encargo archivado, y se sigue con lo demás (D-19, latitud).

`tests/check.py --rapido`: VERDE, 0 FAIL, 296 WARN (tras el merge). Perímetro respetado: `tools/duelo/`, `tools/celda_d/`, sellos y celdas-D ajenos, intocados; `tests/check.py`, `tools/cierre_acto.py`, `tools/tablero_programa.py`, `.github/workflows/`, `.claude/commands/` (TUBERÍA) intocados. **Pendiente de mesa:** `FP-260921-GEN2-ENIGH2024-SERIE-Y-COMMIT-1-7492-01` (firma `b7ae-02`, adopción del diseño, condición para que un acto futuro corra COMMIT-2/3). Nota: `forense/notas/2026-09-21-GEN2-ENIGH2024-SERIE-Y-COMMIT-1-cierre.md`. `## NO-CORRIDO / RESERVAS`: ver encargo archivado. Rótulo censado. **El PR no se fusiona en este acto: queda propuesto, mesa fusiona.**
