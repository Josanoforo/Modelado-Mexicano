# ENCARGO · T20-LLAVES — instrumentar el marcador T20 para "llaves de identificación ejercidas"

**SHA de redacción:** `93a4dd9` (`origin/main`, tras `ADR-100`/`ADR-101`, ACTO MESA-18AGO, 18/ago/2026)
**Entorno asignado:** **NUBE** (`cloud_default`, repo-only). Toca `tests/`, no toca microdato.
**Estado:** VIVO
**Origen:** `FP-18`, firmada `ADR-91` (`PR #246`, 17/ago) — *"Incluyela."* sobre *"T20 se instrumenta en el primer acto que toque tests tras el cierre de BARRIDO-2."* Disparador: `ADR-101(d)`, DISPARADOR-A de `FP-26` (`ADR-101(h)`).

## Verificación de existencia (A.8), contestada por quien escribe

```
BARRIDO-2 cierre material:     GATE-DURABLE-V7 (PR #255) fusionado, contenido en ae25137   SATISFECHO
INTEGRATE-T23 (toca tests/):   PR #256 fusionado                                            SATISFECHO
T20 en tests/check.py hoy:     grep -n "T20" tests/check.py                                  A VERIFICAR
                                (si ya existe otro T20, este encargo colisiona -- re-deriva
                                el número de marcador libre antes de escribir, misma receta
                                que T21/T22/T23 ya usaron)
contador "llaves ejercidas":   forense/registro-llaves-identificacion-v1_0.md, hoy 1 de 2
                                (movido por R5.1-D2, ACTO ADJ-4, 13/ago) -- este encargo NO
                                mueve el contador, solo instrumenta el vigía
```

## Perímetro

ESCRIBE: `tests/check.py` (el marcador T20 nuevo, número re-derivado contra `main` real al ejecutar) · `tests/baseline.json` **solo si** el marcador nuevo introduce entradas que exijan congelar, y solo con el mismo criterio de `ADR-76(f)`/`FP-51`: si el vigía nuevo dispara sobre estado ya existente (no regresión), es candidato a `SENAL` o ADR de mesa antes de recongelar, no recongelado automático · `forense/firmas-pendientes.tsv` (marca `FP-18` `ejecutada_en`) · nota del acto. NO ESCRIBE: `canon/`, `data/`, `milpa/`.

## Tarea

1. Re-deriva contra `main` real el siguiente número de marcador libre (`grep -oE "^T[0-9]+" tests/check.py` o equivalente — no asumas T20 sigue libre).
2. Instrumenta el vigía sobre la población **"llaves de identificación ejercidas"** — fuente: `forense/registro-llaves-identificacion-v1_0.md`, la receta de conteo al pie de ese archivo. El vigía observa el contador, no lo mueve.
3. Corre `python3 tests/check.py --baseline` antes y después; si el vigía nuevo dispara sobre estado no-regresivo, sigue el precedente de `SENAL`/`FP-51` (`ADR-96`, `ADR-101(c)`) en vez de recongelar sin ADR.
4. Marca `FP-18` con `ejecutada_en` = el PR de este encargo.

## Cierre

`python3 tests/check.py --baseline` VERDE (o `SENAL` declarado, no recongelado silencioso) · línea en `hallazgos.md` · `firmas-pendientes.tsv` actualizado · encargo `CONSUMIDO`.
