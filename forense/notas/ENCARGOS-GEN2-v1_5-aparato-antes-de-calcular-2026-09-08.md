# ENCARGOS · GEN2 · v1.5 — TODO EL APARATO ANTES DE CUALQUIER CÁLCULO
### dirección (Fable, Maestra 48) · 8/sep/2026 · contra `origin/main = df9336c5` (PR #608, E3.1 READINESS fusionado) · ADR máx y FP máx: derívalos en el 0-bis (`cierre_acto.py`), no se heredan · sustituye a v1.4 y a los cuerpos encolados de E5-0/E5/E6/E7

**Estado verificado.** #605 fusionado (absorbe `historico/`; T-CRON desacoplado de la línea base), #606 (cascada del acto del cron), **#607 T8** (A.14, cero ramas, `forense/no-corrido.tsv` con 8 filas: NC-0001 y NC-0002 ABIERTA y a decisión de mesa; NC-0007 y NC-0008 ABIERTA propias de T8), **#608 E3.1 READINESS** (`YA-SELLADO`, `CONTEXTO`, `CALC-SMOKE-0002`; cierra NC-0003). `para_v2_13_entradas = 4`. Cero ramas remotas. Los cuerpos hoy encolados de E5-0/E5/E6/E7 traen las compuertas del orden viejo (cálculos antes de E6): V213 los reemplaza por estos.

**Firmas que gobiernan (verbatim):** 6/sep «mi merge manual es la firma de las decisiones» · 7/sep «manda todos los cálculos al final (E5) y dejemos toda la automatización etc, primero» · 8/sep «no quiero que se mezclen cálculos de Gen1 […] asegúrate de que pueda correr los encargos de automatización y demás antes de que calculemos nada».

**Orden, y ya no se mueve:** **V213** (nube, sella v2.13) → **E6** (nube: registro, status, tablero, T-REPRO en modo aviso) → **E7** (nube + caja: readiness del marcador y verificación adversarial de INFRA) → **E5-0** (caja, solo metadato) → **E5** (caja, los cálculos). Todo encargo cierra con `## NO-CORRIDO / RESERVAS` y con su rama fusionada o borrada (A.14, ya exigido por T34).

---

## V213 · ACTO GEN2-V213 · SELLA INSTRUCCIONES v2.13 Y REENCOLA EL ORDEN NUEVO

Cabecera: **NUBE** · **Sonnet** · COMPUERTA: mesa declara en el lanzamiento, con fecha, que v2.13 ya está pegada en el proyecto de Claude (A.9). NO se lanza en UBUNTU.
Qué hace: (1) `instrucciones-proyecto-v2_13.md` = v2.12 íntegra + el delta adjunto (`instrucciones-proyecto-v2_13-DELTA-2026-09-08.md`, verbatim), con sidecar; `instrucciones_vigentes` del tablero → v2.13 (receta ya deriva por versión máxima). (2) Enmienda a A.1 aplicada como el delta la declara (no se reescribe v2.12; v2.13 la lleva). (3) Reencola: reemplaza en `forense/encargos/cola/` los cuerpos de E5-0, E5, E6 y E7 por los de este documento, verbatim, con estados `LISTO-` (E6) y `GATED` (E7, E5-0, E5) según sus compuertas. (4) `hallazgos.md`: entrada «v2.13 entregada íntegra; las cuatro PARA-v2.13 quedan absorbidas; fecha de pegado en el proyecto: <la que mesa declare>». (5) ADR con la línea de A.9 verbatim. Sección `## NO-CORRIDO / RESERVAS` y `## CONSUMIDO`.
Perímetro: `instrucciones-proyecto-v2_13.md` + `.sha256` · `forense/encargos/cola/` (cuatro reemplazos) · `forense/hallazgos.md` · cascada (ADR, L0, rótulos, `tests/check.py` solo exenciones). **No toca `corrida0.py`, `milpa/**`, canon de reglas, specs.** Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.
Contador: `instrucciones_vigentes` v2.12 → v2.13; `para_v2_13_entradas` deja de contar (absorbidas).

---

## E6 · ACTO GEN2-E6 · AUTOMATIZA-GEN2-2 — registro, status, tablero, T-REPRO en modo aviso (antes de cualquier cálculo)

Cabecera: **NUBE** · **Opus** · COMPUERTA por producto: `git show origin/main:instrucciones-proyecto-v2_13.md | grep -c "Bloque E"` ≥ 1 (V213 fusionado). NO se lanza en UBUNTU: trabaja sobre `demanda-*.tsv`, `decisiones.tsv`, `CALC-SMOKE-0001/0002` y fixtures; no necesita corpus ni corridas reales.
Por qué antes de E5 y no después (cambio respecto a v1.4): mesa manda todo el aparato antes de calcular. El esquema endurecido ya existe (E3.1) y hay dos corridas selladas bajo él (los smokes) que sirven de caso real para `registro`/`status`. Lo único que no puede congelarse sin una corrida GEN2 verdadera es el **FAIL** de T-REPRO: por eso entra en **modo aviso** y pasa a FAIL en el cierre de E5, declarado aquí y allá.
Piezas: (1) `cmd_registro`: une `demanda-*.tsv` + `decisiones.tsv` + carpetas `CALC-*/` → `corridas.tsv · resultados.tsv · usos.tsv` (`# DERIVADO — NO EDITAR`), campos `spec_yaml_sha256 · input_sha256 efectivos · resultado_replay · contexto_replay · estado (…SELLADA | SUPERADO→sucesor)`; validaciones que paran: ids duplicados, RESULT sin CALC, uso a RESULT inexistente, CALC sin spec, RESULT sin sello, hash ausente, ciclo, consumidor activo apuntando a LEGACY; avisan: RESULT sin consumidor, CALC sin consumidor activo, legacy sin sucesor; `CALC-SMOKE-*` entran con `cuenta_gen2 = NO` y **no suman**. (2) `cmd_status`: `N_corridas_requeridas · N_corridas_selladas · N_resultados_activos · N_resultados_sellados · N_resultados_pendientes · dependencias_numericas_legacy_activas · resultados_con_validacion_independiente · diferencias_materiales · no_corrido_abiertas`, todo derivado; hoy debe leer `0 / 79`, `0 / 162`, `162` legacy activas — y eso es correcto, no un error. (3) `tools/tablero_programa.py` lee `status` y **elimina el hardcode histórico** (marco anterior al v1.3; tarjetas narradas). (4) **T-REPRO** en `tests/check.py` (`T35`, modo `WARN`): cada RESULT activo GEN2 con cadena completa; consumidor→RESULT→CALC resolubles; `valor materializado == RESULT` dentro de tolerancia por tipo para todo valor con `corrida0_resultado_id`; (11.1) `spec_yaml_sha256`, `script_blob_sha256`, `input_sha256` completos; (11.2) inmutabilidad estructural — artefactos sellados coinciden con su sello, sin reejecutar microdato; **más un check nuevo: ningún consumidor GEN2 lee un valor sin `corrida0_resultado_id`** — es lo que impide que un cálculo GEN1 se cuele. Sobre los smokes debe pasar limpio. Se declara en la nota que pasa a FAIL en E5. (5) Tests de `registro`/`status` de punta a punta sobre fixtures (no exigen árbol limpio). (6) Cierra NC-0007 y NC-0008 si su verificación end-to-end cabe aquí (`fuera_de_politica` contra PRs reales; diff de `NC-` entre digestos); si no, quedan ABIERTA con sucesor nombrado.
Perímetro: `tools/corrida0.py` (`cmd_registro`, `cmd_status`) · `tools/tablero_programa.py` · `tests/test_corrida0.py` · `tests/check.py` · `data/corrida0/{corridas,resultados,usos}.tsv` · `forense/no-corrido.tsv` (NC-0007/0008) · cascada. **No toca `preflight/run/verify`, `milpa/**`, canon, specs.** Si te encuentras escribiendo fuera de esta lista, PARA.
Contador: cero directo; el tablero pasa a derivar GEN2 con `0/N` explícito. Cierre A.14; rama borrada.

---

## E7 · ACTO GEN2-E7 · READINESS-2 — marcador (M, R, L, agregado) y verificación adversarial de INFRA (antes de cualquier cálculo)

Cabecera: dos piezas de nube y una de caja, **un encargo, dos PR** (D-11: el lote es una corrida coherente; aquí son dos entornos) · **Opus** · COMPUERTA por producto: `git show origin/main:tools/corrida0.py | grep -c "def cmd_registro\|def cmd_status"` → 2 (E6 fusionado). Pieza C (caja) NO se lanza en NUBE; piezas A y B NO se lanzan en UBUNTU.
Firmas: D11/D12 del ADR de E3.1 (`motor.py` y familia son scaffold; la readiness del marcador es E7); NC-0001 `DIFERIDO-A:E7`.
**Pieza A (nube) · Corredores M, R, L y agregado, envueltos para GEN2.** (A1) Wrapper M: `marco vigente → cada celda → tools/emite_m.py::emite_celda → RESULT-M`, sin `regenera_todos()` histórico, con `grado_DD` por conducta (ya en `emite_m.py` tras M13) y `ciego_a_R` verificable; salida como `CALC-M-<marco>` bajo `corrida0.py` (spec.yaml + contrato; el marco es input de repo). (A2) R: `tools/arbitra.py` envuelto o adaptado para recibir `payload_id` exacto + spec + ruta resuelta por `resolver_payload`, **sin heurística de «mejor archivo»**; cada celda R como `CALC-R-<celda>` con inputs de manifiesto. (A3) L: corredor sucesor para `L-spec-v1_2.json` (14 celdas) en lugar de `runner_l_cli.py` + `carga_l_v1_1.py` (loader del universo v1.1, verificado el 7/sep); las capturas existentes de `corridas-L/` son GEN1 y no se releen como GEN2. (A4) Agregado sucesor que consuma **solo** `RESULT-R/M/L` por celda como corrida derivada (`CALC-AGG-<marco>`), con `ci_replayable: true` si sus inputs quedan versionados; `agregado_v1_3.py` queda LEGACY y smoke. (A5) Verificar el paso 3 del marco como **diseño**, no como código: la unidad de celda (regla × ola × instrumento vs regla × segmento) es decisión de mesa; este acto entrega la tabla de opciones con el diagnóstico de M13 (`diagnostico-14-celdas`) y no la toma. Tests: uno por wrapper sobre fixtures, `verify` `REPRODUCE/IDENTICO` de cada CALC nuevo. **Nada de esto produce un número nuevo del modelo: envuelve lo que existe para que el primero que se produzca ya nazca con cadena.**
**Pieza B (nube) · Go/No-Go del marcador.** Checks mecánicos: `MARCO-VIGENTE-ÚNICO` (una sola fuente del marco) · `M-DESDE-CONTRATO` · `R-SIN-HEURÍSTICA` · `L-SPEC-v1_2` · `AGREGADO-DERIVADO` · `LEGACY-NO-LEÍDO` (ningún wrapper abre `corridas-R/M/L` ni `agregado-v1_3` salvo como `valor_legacy` para `delta`). Salida: `GO-MARCADOR` o lista de fallos.
**Pieza C (caja) · Verificación adversarial de INFRA con muestra propia — cierra NC-0001 (V1·P3, 4/sep).** Exactamente lo que V1 pedía y nadie hizo: (C1) `tests/test_alta_relacion.py` cinco casos con fixture propio; (C2) dos escritores simultáneos sobre un manifiesto de fixture (`--registra` concurrente) → el lock de `tests/manifiesto.py` debe impedir la doble escritura; (C3) `tools/arbitra.py` contra fixture (no contra corpus); (C4) `git worktree add` sin `data/raw` y `resolver_payload` → `RAIZ_NO_CONFIGURADA`, no excepción; (C5) el reporte de C1–C4 con salida cruda, y NC-0001 → `CERRADA` con este PR o `ABIERTA` con lo que falló y sucesor. No relee la «verificación post-hoc» del 4/sep: la contrasta.
Perímetro A/B: `tools/emite_m.py` (solo envoltura; la emisión no cambia) · `tools/arbitra.py` (adaptador o wrapper nuevo `tools/arbitra_gen2.py`) · `forense/prereg-duelo-v2/` (corredor L sucesor, nuevo archivo; nada existente se edita) · `tools/corrida0.py` (solo si un wrapper necesita un tipo de input nuevo, declarado) · `tests/` · `data/corrida0/CALC-M-*/`, `CALC-R-*/`, `CALC-AGG-*/` **solo si se ejecutan sobre insumos de repo** (M y agregado sí; R no: R necesita corpus y va a caja en E5 o después) · cascada. Perímetro C: `tests/test_alta_relacion.py` (nuevo) · fixtures · `forense/no-corrido.tsv` (NC-0001) · nota · cascada. **No toca `tramite.yaml`, `procedencia.yaml`, tiers, `corridas-R/M/L` ni `agregado-v1_3.py`.** Si te encuentras escribiendo fuera de esta lista, PARA.
Contador: cero GEN2 del modelo (los CALC-M/AGG sobre insumos de repo nacen `cuenta_gen2 = SI` solo si mesa lo firma en su merge; por defecto `PENDIENTE-DE-MESA`). `no_corrido_abiertas` −1 si C cierra NC-0001. Cierre A.14; ramas borradas.

---

## E5-0 · ACTO GEN2-E5-0 · SPECS EJECUTABLES — códigos y ponderadores desde metadato; congelar `spec.yaml`

Cabecera: **UBUNTU (caja)**, worktree nuevo desde `origin/main` · **Opus** · COMPUERTA por producto: E7 fusionado con `GO-MARCADOR` en su nota (`git show origin/main:forense/notas/<nota-E7> | grep -c GO-MARCADOR` ≥ 1); `python3 tools/limpia_arbol.py --reporta` sin árboles ni ramas fuera de política; gates de spec en `main`: S12 `870522a3…`, S13 `c41235b8…`, S6 v1.2 `c1cd3b63…` (íntegros del sidecar). NO se lanza en NUBE.
Principio (E.5 de v2.13): «spec conceptual pre-registrada → abrir **solo** codebook/metadato → resolver códigos y ponderador → congelar `spec.yaml` → COMMIT-1 → (E5) abrir microdato → calcular». **Termina en el COMMIT-1; no abre microdato ni calcula.** Adivinar códigos para lograr un `preflight` VERDE está prohibido.
**CALC-0001 / S12 (CIDE-CSES 2015).** Codebook y metadatos del `.sav` (etiquetas y códigos, sin valores): `pcyc13`, `pcyc14`, `pvoto1/2/3`, desenlace §2, ponderador. Si el desenlace no existe con esos códigos → `NO-CONSTRUIBLE` declarado (outputs `null` solo en ese brazo). `spec.yaml` (D-15), `spec-check` y `preflight` VERDE, COMMIT-1.
**CALC-0002 / S13 (LAPOP 2019/2021/2023, R10.3).** **No se convierte ausencia de desenlace en veredicto D2-h.** (a) Confirmar en los codebooks de las tres olas si existe desenlace comparable al de la corrida 2004 (leer en la nota de LOTE-LAPOP cuál usó); (b) si existe: escribir **S13 v1.1** (md + sidecar, antes de cualquier microdato) y `spec.yaml` contra v1.1; (c) si no: `spec.yaml` contra v1.0 con `veredicto_D2h: NO-CONSTRUIBLE` y solo outputs descriptivos pre-registrados. Las dos rutas están autorizadas; el acto reporta cuál y por qué, con codebook citado.
**CALC-0003 / S6 v1.2 (ENNViH 2002).** Riesgo técnico: `seed {aplica: true, valor, rng}`, `dependencias_materiales: [zipfile-deflate64, …]`, `ponderador` por brazo (`fac_3b`; `fac_3b_px` y `fac_3a_px` en pareja), rutas de los ocho `.dta` por `resolver_payload`, ventanas de `es09`/`ce01` leídas de `ennvih1_2002_hogar_q` y citadas con página **en el yaml**, outputs con tipo/unidad y `NO-ESTIMABLE` permitido por celda. `spec-check` y `preflight` VERDE, COMMIT-1.
Común: un COMMIT-1 por CALC con «el primer resultado que produzca este procedimiento es el que se reporta»; sidecars; nota con codebooks abiertos (id de manifiesto, página) y **ningún número**. Cierre A.14; rama fusionada o borrada.
Perímetro: `data/corrida0/CALC-000{1,2,3}/spec.yaml` (+ `spec.md` local citando la spec sellada) · `forense/prereg-caja/S13-R10-3-spec-v1_1.md` + sidecar (solo ruta b) · `forense/notas/` (1) · `forense/firmas-pendientes.tsv` (recibo) · cascada. **No toca microdato, `tramite.yaml`, canon ni S12/S6.** Si te encuentras escribiendo fuera de esta lista, PARA.
Contador: cero; tres CALC en `PRE-FLIGHT-VERDE`.

---

## E5 · ACTO GEN2-E5 · CALC-0001..0003 — correr, sellar, verificar (los cálculos, al final de todo)

Cabecera: **UBUNTU (caja, corpus montado)**, worktree nuevo desde `origin/main` con E5-0 fusionado · **Opus** · COMPUERTA por producto (**el GO**): los tres `spec.yaml` y sidecars en `main`; los tests de E3.1 en verde en `origin/main` (salida pegada); `preflight` VERDE en los tres CALC desde `origin/main`; `cmd_status` en `main` (E6) y `GO-MARCADOR` (E7); base congelada (si `main` avanza durante el acto, no se auto-mezcla); caja limpia (`limpia_arbol --reporta` vacío). NO se lanza en NUBE.
Por CALC, en orden: `preflight` → `run` (contrato y rutas resueltas) → `verify` (`REPRODUCE/IDENTICO` esperado; cualquier otra combinación se reporta como salga) → COMMIT-2 (`ejecucion.json`, `resultados.json`, `sello.sha256`; entradas `PENDIENTE-DE-MESA` en la propuesta citando `corrida0_resultado_id`) → `registro` y `status` (deben subir `N_corridas_selladas` y `N_resultados_sellados`; si no suben, algo del cableado falló y es hallazgo, no se fuerza). Spec mal contra el dato: **no se edita**; nace el siguiente id con `sucesor_de` y un tercer commit lo declara. Una pieza que PARA no tumba las otras; PARO de entorno A.2 detiene todo. Al cierre: **T-REPRO pasa de WARN a FAIL** (`tests/check.py`), declarado en E6 y ejecutado aquí, con línea base que congela GEN1. La sustancia metodológica es la de los `spec.yaml` congelados en E5-0: este acto no decide nada. Cierre A.14; rama borrada.
Perímetro: `data/corrida0/CALC-000{1,2,3}/{ejecucion.json,resultados.json,sello.*}` · `data/corrida0/{corridas,resultados,usos}.tsv` (re-derivados) · `forense/notas/<fecha>-GEN2-E5-*.md` · `milpa/tramite-ola5-propuesta-v0.yaml` (solo `append`) · `tests/check.py` (T35 WARN→FAIL) · `forense/firmas-pendientes.tsv` (recibo) · `forense/no-corrido.tsv` · cascada. **No toca specs, `spec.yaml`, `tramite.yaml` ni canon.** Si te encuentras escribiendo fuera de esta lista, PARA.
Contador: **medición: sí** — `N_corridas_selladas 0→3`, `N_resultados_sellados 0→k`, `dependencias_numericas_legacy_activas` 162 → 162−k. Sucesores: `SELLO-4` (mesa), C0-B lote 1 (specs para los 7 coeficientes y las 4 conductas de D10, sobre `demanda-corridas.tsv`), C0-C, C0-D.

**Contadores movidos por este documento: cero.** Declarado. **NO-CORRIDO / RESERVAS de este documento:** Ninguno.
