# ENCARGO · ACTO GEN2-CELDA-D-PILOTO-3-COMMIT-2-3-v1_3 · EL PILOTO 3 CORRE: EMISIONES SELLADAS, PIN, REALIDAD Y VEREDICTO SOBRE EL v1.3 CONGELADO EN #958

> ENTORNO: **CAJA** (corpus montado) — el hook imprime ENTORNO-DERIVADO; si no dice CAJA, PARA en una línea. Worktree nuevo sin `data/raw` ni `data/raices.local.yaml`: se enlazan, no es PARO. NO es NUBE.

CABECERA · SHA de redacción `33d97e12` (= merge de `#958`); re-deriva al abrir · una sola sesión, rama `acto/gen2-celda-d-piloto-3-commit-2-3-v1_3` · MODELO: Opus · MODO: **RÍGIDO** — reserva de evaluación y spec congelada: tu latitud es sobre logística, nunca sobre el procedimiento · **F3:** ejecuta la sesión de `#951` (`c0ab3df1`, caja ya montada) u otra nueva. NO ejecutan: `165ce648` (congeló el v1.3), la de `#944`, `pc0-77`, ni ninguna con los diseños A/B o el careo en contexto — si eres una de ésas, PARA y dilo · CONTADOR: sella dos corridas (`N_corridas_selladas` +2); `cuenta_gen2 = SI` para ambas, ya asentado (`decisiones.tsv:184-185`); **no adopta**: `adoptados_activos` no debe moverse; `celdas_validadas` antes y después · FP/ADR/NC: raíz de acto; busca por objeto.

## 1 · OBJETIVO
Que el tercer piloto pre-registrado produzca su veredicto: ¿se transporta a ENCIG 2025 la interacción edad × escolaridad de «pagar la luz por canal digital», estable en 2021 y 2023, cuando el nivel subió once puntos? Es la primera de las tres pruebas que alimentan la regla de salida de la capa θ, y habilita hasta 15 celdas validadas en un tercer dominio y el informe v1.2. Lleva seis actos sin abrir el cruce; los cinco paros fueron del conducto, ninguno del dato, y el v1.3 los cerró con un ensayo que pasa por el validador que sella.
«Hecho» significa: EMISIONES y ADJUDICACION selladas por `corrida0 run`, en ese orden y cada una en `origin` antes de la siguiente; veredicto con las palabras de la spec (B-bis); las dos corridas en la vista con asiento de replay (E.7); celda-D registrada con unidad `evento`; marcador re-derivado; las FP y NC del piloto cerradas por objeto.

## 2 · FIRMAS DE MESA — verbatim; no se piden otra vez
FP-389 · F1-bis (20/sep): rejilla de edad y coherencia sobre el universo sin residuo; elegibilidad con tolerancia de ⅓; 18–29 × hasta primaria FUERA-DE-SOPORTE ex ante. FP-399 · S2 (20/sep): «El código 97 es edad real censurada y se reconoce como tal. […] Para 2025, el COMMIT-2 cuenta los trámites con código 97 con una sola variable de agrupación y lo reporta. Siguen fuera del universo del cruce […]. Si superan el 1 % de los trámites de la banda 60+, el veredicto lleva la marca RESERVA-S2; no detiene el piloto.» FP-400 (20/sep): «[…] Hereda VERBATIM de la spec v1.0: estimando, rejilla, las 15 PUNTUADA y la FUERA-DE-SOPORTE ex ante, candidatos y fórmulas, lambda = 0.8937949410086089, soporte, adjudicación, lectura secundaria y B-bis. […] Los COMMIT-2 y 3 los ejecuta OTRA sesión (F3). Nadie ha visto ENCIG 2025: revisar el COMMIT-1 es legítimo (E.6).» FP-407 y firma del v1.3 (21/sep, en `forense/encargos/2026-09-21-GEN2-CELDA-D-PILOTO-3-COMMIT-1-v1_3.md:24`): «El v1.3 cambia solo declaraciones de los dos `spec.yaml`; `medidor.py`, `adjudicacion.py`, la spec humana y su sidecar no cambian. Los 8 controles 60-96 se declaran anulables y se recuperan al cierre como aritmética entre sellados. […] Re-correr EMISIONES es legítimo: procedimiento y semilla intactos, nadie vio cifras. `cuenta_gen2 = SI`, sea cual sea el veredicto, se hereda.» FP-393 (21/sep, 4.2): «unidad «evento» — a».

## 3 · LO QUE DIRECCIÓN SABE (contra `33d97e12`, contenedor SIN corpus)
- `[EJECUTADO]` `corrida0 preflight CALC-GOB-DIGITAL-EXE-EMISIONES-0002` → `VERDE`. En caja debe además mostrar el payload `encig25_base_datos_csv` `COINCIDE`.
- `[EJECUTADO]` `preflight` de ADJUDICACION → `BLOQUEADO` **exactamente** por los cuatro de las emisiones que aún no existen (`input_repo_ausente` / `input_repo_no_commiteado` de `emisiones_resultados` y `emisiones_sello`): es el estado que su `spec.yaml` declara esperado antes del COMMIT-3a.
- `[EJECUTADO]` `permite_no_estimable: true` → 292 en EMISIONES, 174 en ADJUDICACION. `[LEÍDO]` `spec.yaml` de ADJUDICACION, bloque `secuencia_commits`: trae `commit_2`, `commit_3a`, `commit_3` y `commit_3_cierre_control_60_96`. **La secuencia vive ahí; si este encargo y ese bloque difieren, manda el bloque y me lo reportas.**
- `[EJECUTADO]` FP-393 `FIRMADA` en main; `decisiones.tsv:184-185` trae `cuenta_gen2=SI` para los dos CALC.
- `[LEÍDO]` nota de `#958`: ocho caminos del ensayo pasan `_valida_outputs` (`tests/test_piloto3_v13_conducto.py`); cero no finitos. **Riesgo residual que el congelador dejó escrito** (§3 de su nota): `adjudicacion.py:185,188-189` emite los puntos de C2/S½/Sλ sin guardia de finitud; serían NaN solo si una marginal entera de 2025 tuviera masa cero — y en ese caso EMISIONES ya habría emitido `null` en ese `-MARGINAL-…-P`. → compuerta de P2.
- `[REPORTADO]` en `#951` el medidor ya corrió sobre el payload real de 2025 (una variable, en proceso, `corrida0` descartó todo, nadie vio cifras) y produjo exactamente los 40 nulos que el v1.3 declaró. Esa ejecución previa **se declara en tu nota**; no es un primer resultado distinto: procedimiento y semilla no cambiaron.

## 4 · YA HECHO
Por objeto («PILOTO-3», «GOB-DIGITAL-EXE», «commit-2-3-v1_3») en `data/corrida0/`, encargos, notas y ramas remotas (3 vivas, ninguna del piloto): los dos CALC no tienen `ejecucion.json`, `resultados.json` ni `sello.*`; nadie ha corrido el COMMIT-2 sobre el v1.3. **Repítela tú.**

## 5 · PIEZAS — orden estricto
**P0 · Antes de correr.** Sidecar de la spec OK · `pytest tests/test_piloto3_v11.py tests/test_piloto3_v13_conducto.py` en verde aquí, con la de oro sobre 2023 · los dos `preflight` como en §3, con el payload `COINCIDE` · censo de rastros: nadie abrió `encig25*` por dos variables.
**P1 · COMMIT-2 · emisiones.** `python3 tools/corrida0.py run CALC-GOB-DIGITAL-EXE-EMISIONES-0002`, sin argumentos que la spec no declare. Sella. Reporta el conteo del código 97 y si dispara RESERVA-S2; **lista cada nulo emitido, por familia de id**. Vista y replay aquí (E.7). Commit, push, `git ls-remote`: el orden del diff es el sello.
**P2 · Compuerta sobre lo sellado, sin abrir nada.** En el `resultados.json` recién sellado: ¿los 9 `-MARGINAL-…-P` y los 48 puntos de candidato son todos no nulos? Si sí, la rama NaN de ADJUDICACION es inalcanzable: sigue. Si alguno es nulo: **PARO antes de derivar R**, con el id — el cruce sigue sin gastarse.
**P3 · COMMIT-3a.** Los dos `sha256` de `emisiones_resultados` y `emisiones_sello` en el `spec.yaml` de ADJUDICACION, en commit propio y nada más. `preflight` ADJUDICACION → `VERDE`. Push.
**P4 · COMMIT-3 · la realidad.** `corrida0 run CALC-GOB-DIGITAL-EXE-ADJUDICACION-0001`. Veredicto con las palabras de la spec: corroborada · acotada · falsador débil (manda si caben dos) · un retador vence (si vence Sλ y no S½, o al revés, el hallazgo es sobre cuánto encoger). ≥ 5 de 15 sin soporte → FUERA-DE-SOPORTE global, y ése es el veredicto. Si una celda `PUNTUADA` perdió su IC de R y pasó a `NO-PUNTUADA` (`adjudicacion.py:210-211`), el veredicto lo dice. Lista los nulos por familia. Lo no pre-registrado va después, en `EXPLORATORIO — NO ADJUDICA`.
**P5 · Cierre.** Las cuatro restas |C2 del piloto − `-60-X-` de la casa| como aritmética entre sellados, rotuladas así · celda-D con `evento` · marcador re-derivado (el par deja de estar RESERVADA) · `celdas_validadas` antes/después · FP y NC del piloto cerradas por objeto.
**Nota para mesa, una página:** qué se probó · qué salió · con qué certeza · qué NO significa (es un cruce de un trámite —pago de luz—, no «gobierno digital» ni confianza en el Estado; el +11 pp de nivel no queda explicado; canal responde a bancarización y cobertura antes que a preferencia) · **una línea para la regla de salida de θ:** ¿algún retador con interacción venció al piso, sí o no?

## 6 · LATITUD
Decides tú: enlazar `data/raw`, instalar dependencias, nombres, reintentar un paso que falló por entorno SIN haber producido salida, arreglar lo adyacente de ≤ 10 líneas **fuera** de los dos CALC y de la spec (p. ej. `tools/asienta_replay_aislado.py` escribe incluso con `--help`: no lo invoques a ciegas). Replantea y sigue si main se movió o un id fue renumerado.

## 7 · PAROS — lista cerrada
a) agrupar `encig25*` por dos variables fuera de `adjudicacion.py`, o antes de que las emisiones estén selladas y en `origin` · b) editar spec humana, sidecar, un `.py` de los dos CALC, o un `spec.yaml` fuera de las dos líneas de P3 · c) **`corrida0 run` no sella** → no se parcha, no se relanza; diagnóstico exacto y el id que falló — y dilo así: el falsador del v1.3 se cumplió y D-22 ampliada pasa a exigir enumeración por herramienta · d) la compuerta de P2 encuentra un nulo · e) salida en scratch · f) repetir una corrida que ya selló · g) la de oro no pasa · h) entorno o sesión equivocados · i) rastro de 2025 abierto por dos variables · j) adoptar o mover `adoptados_activos`.
No es paro: main movido · `cuenta_gen2` rebajada por el registro (reporta el campo que decide) · un veredicto que no te guste · RESERVA-S2.

## 8 · COMPUERTAS
«P0 en verde» protege: **abrir dato**. «Emisiones selladas y en `origin`» y «P2 sin nulos» protegen: **abrir dato** (el cruce). Ninguna más.

## 9 · PERÍMETRO
Propio: `ejecucion.json`, `resultados.json`, `sello.*` de los dos CALC · las dos líneas de P3 · la celda-D GOB · filas propias en `corridas.tsv`, `resultados.tsv`, `replay-evidencia.tsv` · derivados y marcador por comando · nota · cascada. Ajeno: todo lo demás de los dos CALC · spec y sidecar · `tools/` · tests · payloads que no sean ENCIG 2021/2023/2025 · los otros dos cruces de ENCIG 2025, que siguen RESERVADA. Si te encuentras escribiendo fuera de esta lista, PARA.

## 10 · NO HACE · SUCESORES · AUDITORÍA · CIERRE
No cambia el procedimiento · no adopta · no abre otros cruces. Sucesores: informe v1.2 y lectura de la regla de θ (dirección). Auditoría: una interacción estable entre edad y escolaridad en el canal de pago es compatible con acceso a banca y conectividad —estructura— tanto como con disposición; el piloto predice el patrón, no identifica su causa (A-bis). Universo: quien paga luz a su nombre; sub-representa hogares donde otro paga. Peligroso leído simplista: «los mayores con poca escuela no adoptan lo digital» como rasgo, cuando puede ser oferta. `## NO-CORRIDO / RESERVAS` · `## CONSUMIDO`.

## NO-CORRIDO / RESERVAS

| qué | por qué | impacto | sucesor |
|---|---|---|---|
| `NC-260921-GEN2-CELDA-D-PILOTO-3-COMMIT-2-3-v1_3-3619-01` · P5 «marcador re-derivado (el par deja de estar RESERVADA)»: `python3 tools/marcador_segmento.py --escribe` corrió sin diff; el par `CRUCE-GRUPO::tramite.gobierno_digital.util_sin_coercion_ejes_encig2025::edadxescolaridad` sigue `RESERVADA` porque la herramienta sólo levanta la reserva con `champion_actual: C2` y este acto no adopta. Un estado `CONSUMIDA-POR-PILOTO` exige > 10 líneas en `tools/` (incluida la aserción «emitir no consume») y una decisión de vocabulario A.16: ensayado y revertido sin commitear. | DECISIÓN-DE-MESA-PENDIENTE: (a) adoptar el piso C2 no vencido (A-bis 6) — el marcador lo levanta solo, recomendada · (b) vetar y encargar el estado nuevo a un acto de aparato · (c) dejar RESERVADA como registro del cruce gastado (E.6). | el marcador no refleja que el cruce está consumido; `adoptados_activos` no mueve (vedado por el encargo). | MESA — al fusionar este PR; NC-0410 y NC-0432 re-apuntadas aquí. |
| `NC-260921-GEN2-CELDA-D-PILOTO-3-COMMIT-2-3-v1_3-3619-02` · P5 «`celdas_validadas` antes y después»: 73 → 73. `tools/tablero_programa.py` deriva `cruce_vs_R` del marcador (ADOPTADO-POR-FIRMA) y de dos CALC nombrados, no de celdas-D adjudicadas; las 15 PUNTUADA del piloto 3 están selladas con R e IC y no entran. `tools/` es ajeno (§9). | DECISIÓN-DE-MESA-PENDIENTE: la misma de `-3619-01`, o un acto de aparato que cuente celdas-D adjudicadas sin adoptar. | `celdas_validadas` no mueve aunque el tercer dominio ya tiene 15 celdas con R e IC. | MESA; NC-0432. |

A.8 contra medición ya corrida (`T-YAMEDIDO`; la cita de arriba es el `celda_id` del marcador, no clasifica ni sella la regla): `python3 tools/ya_medido.py tramite.gobierno_digital.util_sin_coercion_ejes_encig2025` → `MEDIDA-EN: tramite-ola5-propuesta-v0.yaml`; `python3 tools/ya_medido.py tramite.gobierno_digital.util_sin_coercion` → `MEDIDA-EN: 2026-09-09-GEN2-LOTE-ENCIG-1-cierre.md, CALC-ENCIG-0001, tramite-ola5-propuesta-v0.yaml, tramite.yaml`.

Corrido entero lo demás: P0, P1 (COMMIT-2 sellado y en `origin`), P2 (sin nulos), P3 (COMMIT-3a, preflight VERDE), P4 (COMMIT-3 sellado, veredicto FALSADOR DÉBIL con las palabras de la spec), P5 (aritmética 60-96, celda-D con `evento`, contadores, FP/NC por objeto), nota para mesa. `## CONSUMIDO` se añade en el commit siguiente con el número real del PR.
