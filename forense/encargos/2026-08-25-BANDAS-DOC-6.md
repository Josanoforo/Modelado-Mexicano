# ENCARGO 2 · ACTO BANDAS-DOC-6 — pre-registrar las bandas antes del árbitro

- **SHA de redacción:** commit de `origin/main` al arrancar este acto (rama `claude/bandas-doc-prereg-wzwm1q`), sin cambios locales previos al arranque.
- **Entorno asignado:** NUBE (`cloud_default`; sin red/microdato — declarado, sonda saltada). NO Ubuntu.
- **Estado:** `CONSUMIDO` — ejecutado el 25/ago/2026, `ADR-160` (candidateado `ADR-159`, renumerado al fusionar `main` — `ACTO SPEC-R10.1-v2`, `PR #327`, fusionó primero y tomó `ADR-159`), nota `forense/notas/2026-08-25-bandas-doc-6-cierre.md`.

## Bloque VERIFICACIÓN DE EXISTENCIA

- **Estructura:** el marco que trae las seis celdas objetivo (`forense/marco-candidatas-piloto-v1_0.tsv`, filas `DOC-01`..`DOC-06`, líneas 56–61) ya existía en el árbol al arrancar; no se creó ninguna fila nueva de marco.
- **Contenido:** las seis cifras que este acto banda ya vivían en `corpus/forense/compass-4-e29a28d4-credito-popular-2026.md` (archivo `verbatim`, `FP-61`), citadas en la columna `publicada` del marco. `DOC-05` es la excepción declarada: el denominador de su razón (cartera total promedio del ejercicio de Compartamos) **no** se localizó en ese corpus — verificado por lectura completa del archivo (126 líneas) y `grep` dirigido sobre "cartera total"/"balance"/"activos totales", cero coincidencias.
- **Cobertura retroactiva:** la regla que este acto ejecuta (`FP-83`/`ADR-135(e)`) ya estaba `FIRMADA` desde el 24/ago/2026 (`ADR-155`, «FP94: GO.»); la fila operativa `FP-126` («Sucesora de FP-94... pre-registrar, celda por celda, la banda...») ya estaba `FIRMADA` sin `ejecutada_en` al arrancar este acto — verificado leyendo `forense/firmas-pendientes.tsv` completo, no por memoria del encargo.

## Texto completo tal como se lanzó

> ENCARGO 2 · ACTO BANDAS-DOC-6 — pre-registrar las bandas antes del árbitro (FP-94, GO de mesa 24/ago)
>
> ENTORNO: NUBE (cloud_default; sin red/microdato — dilo y salta la sonda) · Opus · CONTADOR: cero (v2.3). EXISTENCIA (dirección): la regla de bandas está sellada (FP-83, firmada); lo que falta —textual de FP-94— es pre-registrar celda por celda la banda de las seis documentales DOC-01..DOC-06 antes de que el árbitro corra. Las seis celdas viven en el marco/prereg del duelo (forense/prereg-duelo-v2/ y el marco v1_0 — deriva dónde está cada una y pega el comando). TAREAS: (1) para cada DOC-0x: la banda con su derivación a la vista (la fuente de la cifra, el ancho y POR QUÉ ese ancho — regla de FP-83, sin inventos nuevos); (2) todo en un solo documento pre-registrado forense/prereg-duelo-v2/bandas-doc-01-06-v1_0.md que cierra con «el primer resultado que produzca el árbitro contra estas bandas es el que se reporta»; (3) FP-94 → FIRMADA+ejecutada; (4) higiene heredada: archiva forense/encargos/2026-08-24-RECENSO-DISENO-2.md como CONSUMIDO (el ejecutor no pudo — quedó declarado en su nota) citando PR #321. PERÍMETRO: forense/prereg-duelo-v2/bandas-doc-01-06-v1_0.md (nuevo) · forense/encargos/ (el CONSUMIDO heredado + el propio) · tablero · gobernanza · estado · nota.

## Cierre

- **Bandas derivadas:** las seis, en `forense/prereg-duelo-v2/bandas-doc-01-06-v1_0.md` — cinco completas (`DOC-01`, `DOC-02`, `DOC-03`, `DOC-04`, `DOC-06`), una incompleta por diseño y declarada como tal (`DOC-05`, denominador no localizado en el corpus).
- **`FP-94`** → `FIRMADA`, `ejecutada_en = 2026-08-25`. **`FP-126`** (fila operativa real) → `FIRMADA`, `ejecutada_en = 2026-08-25`. Corregido el texto de `firmada_en` de `FP-94` (citaba `FP-125` como sucesora; la sucesora real es `FP-126`, verificado por lectura directa de ambas filas).
- **Tarea (4) de higiene — resuelta en dos pasos.** Primer paso de este acto: `forense/encargos/2026-08-24-RECENSO-DISENO-2.md` **no existía** en el árbol (`ls forense/encargos/` — cero coincidencias). Lo que existía era la nota de salida `forense/notas/2026-08-24-recenso-diseno-2-cierre.md` (`ACTO RECENSO-DISEÑO-2`, `ADR-153`), cuyo cierre declara verbatim que el propio encargo instruyó no marcarse `CONSUMIDO` ("instrucción explícita del encargo... queda con los cambios sin commitear para revisión del supervisor"), y `FP-123` confirma que el sucesor de `RECENSO-DISEÑO-2` corrió "instruccion directa sin encargo .md propio" — el `.md` del encargo nunca se había commiteado, incumpliendo `forense/encargos/convencion.md`. Este acto no fabricó ese texto (violaría "sin inventos nuevos"); lo declaró como hueco abierto. Segundo paso, misma conversación: dirección aportó el texto verbatim del encargo original. Con el texto en mano, se archivó `forense/encargos/2026-08-24-RECENSO-DISENO-2.md` con cabecera completa (SHA `754eb86`, entorno UBUNTU, bloque de verificación de existencia propio del encargo) y `## Cierre` citando el resultado real ya conocido (`forense/notas/2026-08-24-recenso-diseno-2-cierre.md`/`FP-117`/`FP-120`/`FP-123`: 13 llaves reales, no 37) y `PR #321` (`3bae09f`). Detalle en `ADR-160(c)` (corregido tras renumerar) y en la nota de este acto.
- **Merge de `main` (post-push, mismo acto):** al fusionar `origin/main` la rama traía un conflicto real en `canon/gobernanza-v1_15.md`/`canon/estado-programa-v1_10.md` — `ACTO SPEC-R10.1-v2` (`PR #327`) había fusionado primero y tomado `ADR-159`; resuelto renumerando este acto a `ADR-160` (regla de la casa, renumera quien fusiona segundo). El merge introdujo, propias de este acto, una autocolisión `T02` (este mismo encargo contra la nota preexistente de `RECENSO-DISEÑO-2`, resuelta renombrando la nota a `...-cierre.md`) y dos `T15` (citas a `159 ADR` desincronizadas, recifradas a `160`) — las tres cerradas antes de empujar, verificadas con `python3 tests/check.py --baseline`: `LÍNEA BASE: VERDE`.
- **Perímetro:** respetado — se tocó `forense/prereg-duelo-v2/bandas-doc-01-06-v1_0.md` (nuevo), `forense/encargos/` (este archivo, nuevo; `2026-08-24-RECENSO-DISENO-2.md`, nuevo, archivado `CONSUMIDO` en el segundo paso — ver arriba), `forense/notas/2026-08-24-recenso-diseno-2-cierre.md` (renombrada, sin editar contenido), `forense/firmas-pendientes.tsv` (tablero), `canon/gobernanza-v1_15.md` (`ADR-160`), `canon/estado-programa-v1_10.md` (conteo de ADR y recifrado de la suite), `forense/notas/2026-08-25-bandas-doc-6-cierre.md` (nueva). No se tocó `forense/marco-candidatas-piloto-v1_0.tsv` (se leyó, no se editó), `scoring-adv1-m3.py` ni ningún otro script del duelo (el árbitro no corrió), `milpa/`, `data/`, `corpus/` (solo lectura), `data/diseno-muestral.yaml` ni la fila `FP-117`/`FP-120`/`FP-123` del tablero (ya reflejaban el resultado real desde el 24/ago), ni ningún `resultado.tsv`.
