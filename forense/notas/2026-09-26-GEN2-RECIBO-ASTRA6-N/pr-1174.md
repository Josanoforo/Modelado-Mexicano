# Recibo técnico PR #1174 · ASTRA6-C2-ENIF-1 · carril C2 (familias 2027)

Revisor: Claude (subagente, solo lectura, entorno NUBE sin data/raw) · 26/sep/2026
Criterios: `scratchpad/recibo/criterios.md` (K1–K6) + §1 C2 de `forense/encargos/2026-09-26-GEN2-RECIBO-ASTRA6-N.md` (L19–L23).

## Identidades

- HEAD del PR: `722cdb5149f5d765b3d06f0d4f7d3fbd61b7bc99` (ref `recibo-pr-1174`, rama `codex/astra6-c2-enif-1`).
- origin/main al revisar: `acf62514c857f23f003b33c70d8162d2feaa6937`; merge-base `948f024a80a04cae48e5d4fca828da58f94a6b62`.
- Vista previa: worktree `scratchpad/wt-1174` = origin/main + `git merge --no-ff --no-commit recibo-pr-1174` → «Automatic merge went well» (auto-merge limpio en `forense/no-corrido.tsv` y `forense/replay-evidencia.tsv`). Worktree ya retirado.
- Diff del PR: 79 archivos, +3941 / −0 (`git diff --numstat 948f024a..722cdb51 | awk '$2>0'` → vacío: cero líneas borradas en todo el PR).
- Encargo propio: `forense/encargos/2026-09-26-ASTRA6-C2-ENIF-1.md` (archivado por #1166; el PR solo añade NO-CORRIDO/CONSUMIDO al pie) + `…-ADENDA-1.md` (con sidecar cuerpo).
- Historial (EJECUTADO, `git log --reverse 948f024a..recibo-pr-1174`):
  `cca0d551` arranque → `a6504860` **COMMIT-1** (specs v1.3, spec/medidor de 2 EMISIONES y ORO-0001, lector, auditoria, contrato futuro, 3 archivos de tests) → `f4776e15` potencia-metodo (solo doc) → `0c74afd2` **COMMIT-2a** (sello emisión FORMAL) → `c617b1df` **COMMIT-2b** (sello emisión HORIZONTE) → `5bbe117f` replay + `auxiliar-primer-intento.txt` (ORO-0001 rechazado) → `1335b5e3` → `d6fd8a06` **COMMIT-1 sucesor** (ORO-0002 spec/medidor, ADENDA-1, lector v2, tests) → `e2612b9c` censo → `f7d26f12` **COMMIT-2 auxiliar** (sello ORO-0002 + tablas) → `09ee29cd` evidencia → merges de main y cierre.

## Tabla criterio × veredicto

| # | Criterio | Veredicto | Cómo (E=EJECUTADO, L=LEÍDO) · salida recortada |
|---|---|---|---|
| K1 | Cifras propias con RESULT | CUMPLE | E: re-derivé 41 cifras de nota/potencia.md/recibo desde `CALC-FAMILIA-2027-ENIF-ORO-0002/tablas/potencia.json` (sha `d2d8d11c…` = REF) y resultados.json: tabla Δ 0/2/3/4 pp (24 celdas), deriva (9), MDE80 `[0.0365, 0.034]`, corr `0.8255931…`, N/estratos/UPM `13502/190/2164`, deltas oro−piso; `potencia.tsv` 108 filas, 0 discordantes. 41/41 trazadas. Las 3 CALC nuevas están «selladas en disco, no registradas» (`consulta.py` aún no las ve; declarado, E.7). |
| K2 | Etiquetas y sellos | CUMPLE | E: `sello.json` de las 3 CALC nuevas: todos los archivos cubiertos coinciden y `sello.sha256` casa (ORO-0002 `e80ab72d…`). 0 líneas borradas en todo el PR. `verifica_sidecars.py` → `FAIL: 0 — VERDE`, 1 WARN (H6). Rótulos: emisiones `PROSPECTIVA-EMISION-SIN-R`, oro `HISTORICO-AUTORIZADO`; `cuenta_gen2: 'NO'` por etiqueta de spec. milpa/, decisiones, manifiesto intactos. |
| K3 | Perímetro | CUMPLE-CON-NC | E: 79 rutas ⊂ perímetro §9 + cierre D-21; grep de prohibidas (milpa/, .github/, manifiesto, derivados, corpus/reports/, tablero, catálogo, decisiones) → 0. Excepción: `tests/check.py` +5 líneas (H5), declarada en la nota L38. |
| K4 | Olas | CUMPLE | E/L: inputs por id = `enif_2024_enif_2024_bd_csv` (manifiesto L5595, sha `00e4b0b4…`, COINCIDE en la corrida) + `resultados.json` sellados de `CALC-ENIF-0001` y `CALC-HORIZONTE-VIA-DERIVADOS-0001-v1_1`. ENIF 2024 es la última ola publicada, pero su módulo de ahorro ya está visto: `CALC-ENIF-0001` (GEN2, cuenta_gen2=SI) lee las mismas columnas P5_1_*/P5_6_* del mismo payload (`spec.yaml` L43/L49), y los marginales de ahorro ENIF 2024 están en `catalogo-del-mexicano-v1_1.md` L56/L152. El oro es nacional, sin cruces (guardia `agrupacion is None`), así que no toca los cruces reservados del lote ENIF2024. Ninguna ola 2027 se lee: `R_futura: null` en todos los contratos. |
| K5 | §3 México (specs/hojas) | CUMPLE-CON-NC | L: las specs v1.3 dicen «declaraciones de ahorro… no preferencias psicológicas» y declaran unidad y escala. Les faltan la etiqueta de evidencia (a), el módulo de auditoría con las preguntas [v2.16] y la medida de exclusión por oferta al lado del marginal de ahorro formal (§3, «oferta antes que preferencia»). Es un defecto heredado: 0/12 specs FAMILIA-2027 en main lo tienen (grep `auditor`/`oferta` → 0). H7. |
| K6 | Suite sobre el merge | CUMPLE | E: `python3 tests/check.py --baseline` sobre la vista previa → `LÍNEA BASE: VERDE — sin FAIL nuevos` · `EXIT=0` (3 FAIL heredados, 149 WARN nuevos: T03 y T13 de encargos y canon ya en main; ninguno viene de archivos del PR). `pytest -q tests/test_astra6_enif.py` → `1 passed`; `pytest -q tools/familias-2027/enif/tests` → `32 passed`. |
| C2.1 | COMMIT-1 completo antes de COMMIT-2 (orden por historial) | CUMPLE-CON-NC | E: `a6504860` (13:58) trae spec.yaml, medidores, guardia, auditoría, mutación y terminales **antes** de `0c74afd2`/`c617b1df` (14:41). Para ORO-0001 el COMMIT-1 no cumplía D-22(2): `test_terminales` validó solo `_valida_outputs` y no `_fallas_run`/`_problemas_valor_largo`, así que el conducto real rechazó la corrida (H1). Lo corrigió el COMMIT-1 sucesor `d6fd8a06`, que va antes de `f7d26f12`. |
| C2.2 | Guardia de una variable | CUMPLE | L: `lector.py` L30–39: `guardia()` lanza error con cualquier `agrupacion` ≠ None (solo nacional, más estricto que una variable) y con `reservada and not autorizada`. `evaluar_futuro` la llama antes de abrir el zip. E: `test_guardias_mutacion`, `test_guardia_piso_antes_lectura` pasan. |
| C2.3 | Auditoría del código | CUMPLE-CON-NC | E: `audita(lector.py)` → `[]`. En cambio `audita(lector-futuro-serializacion-v2.py)` → `['importación ajena']` y `audita(ORO-0002/medidor.py)` → `['importación ajena']` (pathlib no está en la lista blanca). `cierre.py` solo audita v1 (H2). |
| C2.4 | Prueba por mutación | CUMPLE | E: `test_mutacion_codigo_columna_ajena` (r["SEXO"] detectado), `test_guardias_mutacion` (FAC_HOG, edad 17, llave duplicada, input ajeno) pasan. |
| C2.5 | Preflight sobre sintético y oro histórico; dos ORO | CUMPLE-CON-NC | L/E: sintético con 7 ramas terminales + `_fallas_run` completo (`test_sucesor_ref`, `test_futuro_ref_v2`). Oro: ORO-0001 dio `PRE-FLIGHT: VERDE` y luego `RUN: FALLO -- nada se sella` (VALOR-LARGO 15508 y 157608 bytes). ORO-0002 es la sucesora que mesa autorizó en ADENDA-1 («Autorizar sucesora de serialización»). El diff de medidores solo añade `_tabla()` y cambia 2 líneas de salida; `test_algoritmo_congelado_identico` y la igualdad de valores contra el medidor 0001 sobre sintético pasan. Se trata de una **ejecución previa descartada por el conducto, con procedimiento y semilla intactos** (§6, precedente #951→#961), declarada en nota L16 y en ADENDA-1; no es un segundo primer resultado, porque el primer intento no emitió valores (salida cruda solo con tamaños en bytes). Reserva: la nota no usa la fórmula literal y el nombre no declara ORO-0001 como «congelado sin sello, sustituido» (H1). |
| C2.6 | Ids nulos declarados | CUMPLE | L: contrato futuro v1/v2: LO, HI y FRACCION-BANDA con `permite_no_estimable: true`; E: `test_terminales_real_yaml[diseno-vacio]` confirma LO=None y lo acepta. El oro no emite nulos top-level; `auditoria.finitos` descarta NaN. |
| C2.7 | Ningún hash sobre archivo vivo | CUMPLE | L: inputs: zip del manifiesto (inmutable) y `resultados.json` de CALC sellados (`2a90c52e…`, `16a03f13…`, coinciden hoy). `PISOS_HASH` fijado en el código. |
| C2.8 | Ninguna ola reservada | CUMPLE | Ver K4. |
| C2.9 | Familia ≠ ola, conteos y dependencia | CUMPLE | E: `potencia-resumen.json`: estimandos 2, olas_futuras 1, retadores 0, R_futura null. La dependencia se declara con la correlación muestral 0.8256 (re-derivada) y la frase «dos resultados dependientes / no duplican evidencia independiente» (potencia.md, calendario.md L24). |
| C2.10 | Calendario: fecha oficial o ventana con fuente | CUMPLE | L: `calendario.md`: calendario INEGI 1S-2027 (URL + sha `320a481c…`) sin ENIF, así que la fecha queda NO-CONFIRMADA y CONDICIONAL. La ventana 23/sep/2026–23/mar/2028 viene de `calendario-y-exclusiones.md` L3 y está rotulada como ventana operativa, no como anuncio. |
| C2.11 | Potencia con escenarios rotulados | CUMPLE | E: 54 escenarios (escala SE .75/1/1.5 × deriva 0/.5/1 pp × Δ 0–5 pp), cada uno rotulado como supuesto. MDE80 re-derivado. Se declara «no potencia acreditada». |
| C2.12 | Estados de atestación sin colapsar | CUMPLE | E: `inventario-sellos.json`: estado `SELLADO-INTERNAMENTE`, `comprobante_envio/ots: null`, `atestaciones_externas_verificadas: 0`. NC-…-cca0-01 abierta. |
| C2.13 | Retador externo ≤1 y estructural; familias nuevas como propuesta | CUMPLE | L: cero retadores; ninguna familia nueva. HORIZONTE se re-describe como «intersección de vías» con INTERPRETACIÓN-DECLARADA, sin cambiar el sello. E: la identidad F+I−U = F∩I se verifica con el oro: 0.2047673204233842 frente a AMBAS 0.2047673204233843; UNION del oro = TIENE-AHORROS-P (0.6420795665818781, N 13502). |
| C2.14 | Frase de producto con N/M/K derivados | CUMPLE-CON-NC | E: re-derivado: 2 CALC de emisión sellados y COINCIDE, 1 ola, 0 R futura, 0 atestaciones, así que la frase es correcta. Pero `cierre.py` L135 la imprime como **literal fija** («VERDE; 2 familias, 1 ola futura…»), no derivada por conteo (H4). |
| Hecho propio | `cierre.py --verifica` reproducible | NO-VERIFICADO-AQUÍ + defecto | E (nube): `CIERRE: INCOMPLETO`. Dos causas: (a) `verify ORO-0002` da NO-EJECUTABLE porque falta el input (`[3/5 INPUT AUSENTE]`, entorno: esperado en nube); (b) 3 FAIL «inventario discordante» por `__pycache__/medidor.cpython-314.pyc`, que **no están en el árbol** (27/30 entradas COINCIDEN, 3 fuera). (b) falla en cualquier clon limpio (H3). |

## Hallazgos

1. **H1 · ORO-0001: COMMIT-1 incompleto por D-22(2), corregido por sucesora autorizada.** `tools/familias-2027/enif/tests/test_terminales.py:39` y `test_lector.py:74-81` ejercen `_valida_outputs` y no `_fallas_run` (`tools/corrida0.py:2396-2405`, que añade `_problemas_valor_largo`, vigente desde 566fb049 del 24/sep). Por eso el run real falló (`forense/analisis/familias-2027/astra6-enif/auxiliar-primer-intento.txt:28-30`). El tratamiento es correcto: ADENDA-1 de mesa, CALC nuevo 0002, 0001 intacto y sin sello, algoritmo y semilla idénticos (verificados por AST y por valores en `test_sucesor_ref.py:30-90`). Está declarado en `forense/notas/2026-09-26-ASTRA6-C2-ENIF-1-cierre.md:16`. Queda `data/corrida0/CALC-FAMILIA-2027-ENIF-ORO-0001/` como CALC congelado permanentemente sin sello: un `corrida0 run` futuro repetiría el rechazo. No lo marca ninguna fila NC.
2. **H2 · La auditoría estática no cubre el código que corre.** `tools/familias-2027/enif/auditoria.py:5` no incluye `pathlib` en IMPORTS. `audita()` da `importación ajena` sobre `lector-futuro-serializacion-v2.py` (el lector que el COMMIT-3 usaría, porque v1 choca con VALOR-LARGO en D-K) y sobre `ORO-0002/medidor.py`. `cierre.py:89` solo audita `lector.py` v1. Además, `Path.read_bytes/write_bytes` esquivan la prohibición de `open`. E.6 exige «auditoría automática del código antes de abrir el dato» sobre el código autorizado: hay que resolverlo antes del COMMIT-3, no ahora.
3. **H3 · El «Hecho» propio no se reproduce fuera de la máquina del ejecutor.** `forense/analisis/familias-2027/astra6-enif/inventario-sellos.json` incluye 3 `__pycache__/*.cpython-314.pyc` (gitignorados, dependen del intérprete y del mtime). `cierre.py:111-114` falla con `inventario discordante` en todo clon limpio. El inventario también se entrega para el circuito de atestación (P5), con 3 archivos que no existen en el repo.
4. **H4 · Frase de producto fija.** `tools/familias-2027/enif/cierre.py:135` imprime N/M/K como constantes, condicionadas solo a «sin fallos». Los valores son correctos hoy (re-derivados), pero la frase no se deriva por conteo.
5. **H5 · `tests/check.py` editado (+5 líneas, T02).** Añade el par `{astra5-genero-endireh, familias-2027/astra6-enif}/recibo-para-claude.md` a `EXCEPTED_NAME_GROUPS` (check.py L217-221 en la vista previa). Está justificado por D-21 (defecto adyacente de ≤10 líneas que impedía terminar, declarado en la nota L38), y C3 ya editó T02 con el mismo objeto (identidad por ruta para reports-v2). Es frágil: MEMORIA §1 impone ese nombre de archivo a todo recibo, así que cada unidad nueva vuelve a chocar y necesita otra excepción por par (ya hay 4 rutas `*recibo-para-claude.md` en el merge).
6. **H6 · Cuerpo de nota sellado y luego ampliado.** `verifica_sidecars.py` avisa con `WARN_ENCABEZADO_AJENO`: después del cuerpo sellado de `forense/notas/2026-09-26-ASTRA6-C2-ENIF-1-cierre.md` sigue «## Complemento final de cierre» (L36-40), no NO-CORRIDO/CONSUMIDO. Solo se lista (D-a5).
7. **H7 · §3 en las specs v1.3.** `forense/prereg-caja/FAMILIA-2027-ENIF-{AHORRO-FORMAL,HORIZONTE-AHORRO}-spec-v1_3.md` no tienen módulo de auditoría [v2.16], etiqueta (a)/(b)/(c) ni medida de exclusión por oferta junto al marginal de ahorro formal. El defecto viene heredado de toda la serie FAMILIA-2027.
8. **H8 · Contrato futuro v1 vigente, pero el conducto lo rechazaría.** `tools/familias-2027/enif/contrato-futuro.yaml` (estado `CONTRATO-DE-ACEPTACION-NO-EJECUTABLE-HOY`, script `lector.py`) emite D-K como JSON de 2000 floats, que `_problemas_valor_largo` rechazaría. `test_futuro.py:43` solo usa `_valida_outputs`. Conviven v1 y v2 sin marca de cuál gobierna el COMMIT-3.
9. **H9 · Defecto ajeno de `corrida0 verify` (menor).** `tools/corrida0.py:3013` llama `_problemas_valor_largo(valores, None)`; con `d=None` toda REF sale como `VALOR-REF-FUERA-DE-TABLAS`, así que el WARN espurio aparece en `replay-oro.txt`. No es del PR.
10. **H10 · `git_status: SUCIO` (1 línea) en `ORO-0002/ejecucion.json`,** aunque el preflight decía LIMPIO. INFERIDO, no verificado: el medidor crea `tablas/` antes de que se capture la firma. No afecta RESULT.
11. **Observación sin defecto:** `EDAD_V ∈ 18..95 ∪ {97,98}` (lector L76) coincide con el FD («18-95, 97=97 y más, 98=no especificada», `CALC-ENIF-0001/spec.yaml:73`), aunque las specs v1.3 digan «18…98».

## Recomendación

**FUSIONAR-CON-NC.** No hay condición de DEVOLVER:
- ninguna cifra propia queda sin RESULT (41/41);
- no se leyó ninguna ola reservada;
- no se reescribió ningún sello (0 borrados);
- no hay adopción;
- el orden COMMIT-1 → COMMIT-2 se respeta en los dos pares (a6504860 → 0c74afd2/c617b1df; d6fd8a06 → f7d26f12);
- el ORO-0001 descartado queda declarado como ejecución previa rechazada por el conducto, con autorización de mesa;
- K6 está VERDE sobre el merge.

Lista exacta para mesa (no bloquea el merge):
1. Abrir las NC propuestas NC-A…NC-F.
2. Antes del COMMIT-3 ENIF, resolver NC-B (auditoría sobre v2) y NC-D (qué contrato futuro gobierna).
3. Firmar o vetar `FP-260926-GEN2-ASTRA6-C2-ENIF-1-cca0-01` (sigue ABIERTA; la cca0-02 ya consta FIRMADA por ADENDA-1).

## NC propuestas

| Id sugerido | Qué | Razón (token A.14) | Impacto | Sucesor |
|---|---|---|---|---|
| NC-A | Quitar los 3 `__pycache__/*.pyc` de `inventario-sellos.json` (o reemitirlo sin ellos) y excluir `__pycache__` en `cierre.py` | DIFERIDO-A: acto de cierre ENIF o circuito de sellos. El recibo lee y no corrige | `cierre.py --verifica` falla en todo clon limpio. El inventario para atestación lista archivos inexistentes | Circuito de sellos de mesa / sucesor ASTRA6-C2-ENIF |
| NC-B | Ampliar `auditoria.py` (pathlib, prohibir `read_bytes/write_bytes` fuera de tablas/) y auditar v2 y ORO-0002 desde `cierre.py` | DIFERIDO-A: COMMIT-3 ENIF 2027 | Sin esto, el código que abriría la ola reservada no pasa su propia auditoría automática (E.6) | Acto COMMIT-3 ENIF |
| NC-C | Derivar la frase de producto de `cierre.py` por conteo (CALC de emisión sellados, olas, comprobantes) | DIFERIDO-A: sucesor ENIF / plantilla C2 | Riesgo de frase desalineada si cambian las familias | Sucesor ASTRA6-C2 |
| NC-D | Declarar `contrato-futuro-serializacion-v2.yaml` como el que gobierna el COMMIT-3 y marcar v1 como SUSTITUIDO (archivo nuevo, sin editar v1) | SUSTITUIDO-POR: contrato v2 (ADENDA-1) | v1 pasa su test pero el conducto real lo rechazaría (VALOR-LARGO en D-K) | Acto COMMIT-3 ENIF |
| NC-E | Excepción genérica por ruta para `recibo-para-claude.md` en T02 de check.py (como la de reports-v2), en vez de un par por PR | FUERA-DE-PERÍMETRO: suite/CI | Cada recibo nuevo obliga a editar check.py | Acto de tubería/CI |
| NC-F | Módulo de auditoría [v2.16], etiqueta (a)/(b)/(c) y medida de oferta en las specs FAMILIA-2027 (serie completa) | DECISIÓN-DE-MESA-PENDIENTE: si §5 aplica a specs prereg-caja de familias | Las specs afirman marginales de ahorro sin la medida de exclusión por oferta (§3) | Mesa / lote de diseño FAMILIA-2027 |
| (opcional) NC-G | `corrida0 verify` pasa `d=None` a `_problemas_valor_largo` y da un WARN espurio con toda REF | FUERA-DE-PERÍMETRO: tools/corrida0.py | Ruido en replays (confunde la evidencia de oro) | Acto de tubería |

## Qué no verificaste

- **Replay del oro (ORO-0002) sobre microdato:** NO-VERIFICABLE-AQUÍ (nube, sin data/raw). `verify` → `INPUT AUSENTE` · NO-EJECUTABLE. Doy por LEÍDO el `replay-oro.txt` del ejecutor (REPRODUCE/IDENTICO en WSL CAJA), sin reproducirlo.
- Que el primer intento de ORO-0001 no mostró valores al ejecutor: solo LEÍ la salida archivada (tamaños en bytes). No tengo el log de terminal completo.
- Los enlaces del calendario INEGI y su sha (`320a481c…`): no descargué nada.
- Los 149 WARN nuevos de check.py: no los comparé contra una corrida de origin/main solo. Por grep no vienen de rutas del PR.
- Firma de entorno CAJA del ejecutor (WSL con data/raw enlazado): LEÍDA en `arranque.md`, no verificada.
- No leí completos los adjuntos embebidos del encargo (MISION y ADENDA de la misión) ni la cláusula de autonomía, más allá del pie.
- Nota: el clon principal muestra ` M forense/analisis/cableado/bloqueos.tsv` (registro del hook de lectura). Yo no hice ningún commit ni checkout ahí.
