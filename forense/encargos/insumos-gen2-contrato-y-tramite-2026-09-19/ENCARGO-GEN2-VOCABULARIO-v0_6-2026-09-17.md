# ENCARGO · ACTO GEN2-VOCABULARIO-v0.6 · EL CONTRATO CELDA-D DICE LO QUE DOS PILOTOS YA HICIERON: UN PISO NO VENCIDO ES EL ESTIMADOR DE LA CELDA

**CABECERA** · redactado contra `9eff694` (merge de #860); re-deriva al abrir · **ENTORNO: NUBE** — cero microdato · COMPUERTA: ninguna · MODELO SUGERIDO: Opus (edita el contrato; cada cambio se justifica con un defecto ya ocurrido) · FP/ADR/NC: deriva al cierre, no heredes (máximos al redactar: FP-385, NC-0332, ADR-543; TRÁMITE-5 y REPLAY-ASIENTOS-1 pueden fusionar antes) · vehículo: `/acto` · **una sola sesión** (precedente #845/#848).

**FIRMA DE MESA, verbatim (17/sep/2026; su archivo aquí la sella; FP nace FIRMADA):**
> "Un piso no vencido en su celda-D es el estimador adjudicado de esa celda y se adopta salvo veto de mesa. Se adoptan las 20 celdas de ADR-538 y ADR-542 (piso C2) como estimadores por celda de sus reglas consumidoras. El vocabulario celda-D v0.6 lo escribe. Los retadores son credencial para emitir donde no hay piso, no sustitutos del piso donde lo hay."

**VERIFICACIÓN DE EXISTENCIA (A.8, dirección, contra `9eff694`):**
- (1) Estructura: `propuesta-motor-adaptativo-celda-v0_5.md` (contrato vigente; v0.4 §3 regla 3: los conjuntos cerrados se citan por versión), `data/curacion-registro/celdas-d/*.yaml` (5), `tests/test_celdas_d.py`, `milpa/theta-esquema-e1-v1_0.yaml` (`vocabulario_identificacion`). Cubren.
- (2) Contenido, medido: v0.5 §3(a) — `rol: BASELINE | CHALLENGER | COMPLEMENTO | BASELINE_INGENUO | ENSAMBLE`; `champion_actual` sin definición que admita un piso: los dos pilotos cerraron `champion_actual: NINGUNO` con veredicto `SIN-CANDIDATO-SUPERIOR` **y** con el piso C2 primero en las dos (notas de cierre §2). `estrategia` (v0.3 §3): `pseudo_panel | momentos | composicion | transversal_con_seleccion` — "persistencia" no existe; el piloto 1 la registró como `transversal_con_seleccion` + `regla_composicion` y abrió **NC-0305** (`FUERA-DE-PERIMETRO`, sucesor "vocabulario v0.6"). Tokens de identificación de la capa E1 (`AUSENCIA_DE_FACTO`, `ASOCIACION-MEDIDA·*`) viven en la capa y **no** en el contrato. Una v0.6: **NO-ENCONTRADA** (`ls propuesta-motor-adaptativo-celda-v0_6.md` → no existe).
- (3) Cobertura retroactiva: v0.5 es del 20/ago; los dos pilotos (16–17/sep) son los primeros usos con `BASELINE_INGENUO` — el contrato nunca vio un piso ganar.

### PIEZAS

**P1 · v0.6 del contrato**, archivo nuevo `propuesta-motor-adaptativo-celda-v0_6.md`, v0.5 intacta. Solo cuatro cambios, cada uno con el defecto que lo motiva:
- (a) **Adjudicación del piso.** Regla nueva en §3: *"si ningún challenger vence a un piso bajo el criterio declarado, el piso es el estimador adjudicado de la celda; `champion_actual` toma su id; se adopta por merge de mesa salvo veto"*. Defecto: dos celdas-D con estimador ganador y `champion_actual: NINGUNO`.
- (b) **`estrategia: persistencia`** en el enum, definida como *"valor de la ola anterior de la misma serie, mismo reactivo, mismo universo"*, con `regla_composicion` obligatoria (identidad temporal o ajuste declarado). Defecto: NC-0305.
- (c) **`rol: PISO`** como alias formal de `BASELINE_INGENUO` cuando el corredor es un piso del marcador (marginales sin interacción o persistencia por eje), con la nota de que los dos son el mismo conjunto cerrado; los tres yaml de agosto no se tocan.
- (d) **Referencia cruzada a la capa E1:** el contrato no define tokens de identificación; cita `milpa/theta-esquema-e1-v1_0.yaml` como su fuente (`vocabulario_identificacion`). Defecto: dos vocabularios paralelos sin puente.
`vocabulario_version: 0.6` se cita por número (v0.4 §3 regla 3).

**P2 · Los dos pilotos, enmendados y no reescritos.** En `DIN.ahorro_solo_informal.enif2024.localidad_x_edad.yaml` y `TRA.evade_norma.envipe2025.escolaridad_x_dominio.yaml`: `champion_actual` → id del `RESULT` de C2 (leído de `resultados.json` del CALC de emisiones, citado por id, no tecleado); `vocabulario_version: 0.6`; enmienda fechada en comentario (*"NINGUNO → C2 por firma de mesa 17/sep y v0.6 §3(a); veredicto SIN-CANDIDATO-SUPERIOR intacto"*). El veredicto no cambia: cambia lo que el veredicto implica.

**P3 · Adopción, hasta donde el instrumento llegue.** Fila en `data/corrida0/decisiones.tsv` (objeto `adopcion:piso-C2-20-celdas`, verbatim). La adopción **efectiva** (`ADOPTADO_ACTIVO` = sellado citado por consumidor activo, `tools/corrida0.py:4256`) exige que los `RESULT` estén **registrados en la vista** — hoy no lo están (NC-0329; `GEN2-REPLAY-ASIENTOS-1`). Si al abrir ya lo están: enlazar los 20 `RESULT` de C2 al consumidor por el mecanismo de la casa (`corrida0.py demanda`/relevo; si el mecanismo no admite adopción por celda, **PARA en P3**, repórtalo y propón, sin implementar). Si no lo están: P3 queda `DIFERIDO-A: GEN2-MARCADOR-REDISENO-1` con la decisión ya en `decisiones.tsv`. **No se fuerza el registro.**

**P4 · Tests.** `tests/test_celdas_d.py`: acepta `champion_actual` = id de un `BASELINE_INGENUO`/`PISO` solo si `estado_decidibilidad` ∈ {`PUNTUADA`, `INDECIDIBLE`} y el veredicto es `SIN-CANDIDATO-SUPERIOR` — un piso no se adjudica cuando un challenger ganó. `estrategia: persistencia` válida. NC-0305 → CERRADA.

**PERÍMETRO Y CONCURRENCIA:** `propuesta-motor-adaptativo-celda-v0_6.md` (nuevo) · los dos yaml de los pilotos (P2) · `data/corrida0/decisiones.tsv` · `tests/test_celdas_d.py` · `forense/no-corrido.tsv` · `forense/firmas-pendientes.tsv` · `forense/hallazgos.md` (línea PARA-v2.14: *"un piso no vencido es el estimador adjudicado"*) · nota de cierre · cascada. **No toca** `tramite.yaml`, `procedencia.yaml`, la capa E1, el catálogo, `corrida0.py` ni los CALC. En paralelo: `GEN2-REPLAY-ASIENTOS-1` (caja), `GEN2-TRAMITE-5` (nube; tablero) — sin archivo común salvo el tablero al cierre; **quien fusione después renumera**. «Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.»

**CONTADOR:** cero mediciones; `N_resultados_gen2_adoptados_activos` 24 → 44 **solo si P3 corre efectiva**; si no, dicho. **LO QUE NO HACE:** no cambia veredictos · no fuerza registros · no rediseña el marcador. **SUCESOR:** `GEN2-MARCADOR-REDISENO-1`. **CIERRE:** cascada + `## NO-CORRIDO / RESERVAS` + `## CONSUMIDO`.
