# ACTO PROC-10-bis · sella `MEDIDO·NACIONAL` (séptima clase, ADR-79(a)) y le entra `norma_de_género` — `condicionales 10 de 15`, con `T19b`/`T19c` en ROJO por un predicado que el propio encargo advirtió podía tronar

**Acto:** ACTO PROC-10-bis · **Entorno:** repo-only, NUBE, `cloud_default`, sin `data/raw`, sin red · **SHA de redacción del encargo:** `560d305` (`origin/main`, merge #224) · **Depende de:** `ADR-79(a)`+`(h)` (`canon/gobernanza-v1_15.md:1161-1200`, ACTO SELLA-3) y de la herencia declarada por `ACTO PROC-11` en `forense/notas/2026-08-13-proc-11.md` ADENDA B.5.

## §0 · ARRANQUE

1. **REPO/SHA.** `origin/main` = `560d305` (merge PR #224), idéntico al declarado por el encargo. `git status` limpio al arrancar.
2. **Gate de existencia (A.8):**
   - `grep -c "de 15" canon/modelo-decision-v4_0.md` → **6** (≥1, PR #224 fusionado).
   - `grep -cE "MEDIDO·NACIONAL|séptima clase|clase séptima" milpa/procedencia.yaml` → **0** (la clase no existía, esperado).
   - `grep -c "norma_de_género" milpa/procedencia.yaml` → **0** (esperado — PROC-11 no tocó `procedencia.yaml`).
   - `grep -in "proc-10-bis" forense/hallazgos.md forense/notas/*.md` → **9** ocurrencias, todas de tipo "nombrado, no ejecutado aquí" (ADR-79(a), SELLA-3, ADENDA B de PROC-11) — verificado por lectura directa (arriba), ninguna es una ejecución previa de este acto.
3. **Baseline.** `python3 tests/check.py --baseline` antes de tocar nada: **20 FAIL · 107 WARN — LÍNEA BASE: VERDE**, `T15`/`T19b`/`T19c` los tres `[ ok ]`.

## §1 · Lo que ADR-79(a)/(h) autorizan, verbatim, y lo que PROC-11 dejó cableado

`ADR-79(a)`, D-A (`gobernanza:1163`): *"crear una clase séptima para marginales medidas sin eje."* Nombra al ejecutor: *"Este ADR autoriza la clase séptima; `PROC-10-bis` ... la escribe en `milpa/procedencia.yaml`."*

`ADR-79(h)`, D-H (`gobernanza:1181`): el ajuste de `condicionales 9 de 14` es **linkage** al mismo acto que crea la clase, no acción independiente. Cita `forense/notas/2026-08-13-proc-10.md` §5: si el desenlace hubiera sido (A), el titular habría sido "10 de 14", nunca "10 de 15" — el salto de denominador 14→15 es objeto distinto (`obligación_medida`, ADR-75(b)).

Herencia de `ACTO PROC-11` (ADENDA B.5, `forense/notas/2026-08-13-proc-11.md:332-337`), verificada contra el árbol actual, no heredada de memoria:

1. La θ marginal se llama **`norma_de_género`** (renombrada desde `familismo_obligacion` por ADR-75(b)) — confirmado, `modelo:245` la nombra así hoy.
2. El denominador ya es **15** (no 14) — confirmado, `grep -c "de 15" canon/modelo-decision-v4_0.md` = 6 antes de este acto.
3. Hay **dos** θ bajo la clase séptima, no una: `norma_de_género` (este acto) y `obligación_medida` (`P6_38`, celda-D ya registrada en `data/curacion-registro/celdas-d/G5.obligacion_medida.conducta.yaml` por PROC-11) — este acto escribe **solo** la primera; la segunda es el encargo de `PROD-P638`.
4. Los dos sitios de `modelo` están donde PROC-11 los dejó: el header de clases de `procedencia.yaml`, y la fila «Proxy declarado» / rótulo de Paso 5 (antes `:271`, hoy la fila de la tabla de §1.1.F).

## §2 · Commit 1 — la clase y la entrada, congeladas

**(a) Definición de la clase**, `milpa/procedencia.yaml`, header de clases: `MEDIDO·NACIONAL`, mismo formato que las seis vigentes. Cláusulas verbatim incluidas: distinción frente a `MEDIDO·PARCIAL(x)` (`x = ∅` declarado, no omitido — MEDIDO·PARCIAL(x) nunca admite x vacío); no habilita segmentación — la puerta **D3-B** (condicionar por eje) queda nombrada dentro de la propia definición, dueño "acto futuro por θ".

**(b) La entrada** `condicionales_escalares_medido_nacional.norma_de_género`: clase `MEDIDO·NACIONAL` · θ 0.6933, IC95% [0.6725, 0.7140], n=5,579 · fuente ENASIC 2022 `P7_12_7`/`TPER_ELE`, expediente `ESP-OPACA-B-d13ec4fe`, fila `PROD-cca3ea0bccd54d70083728b2` de `data/curacion-registro/produccion-modelo.tsv` (cifras verificadas byte a byte contra esa fila: `0.6932672048`→`0.6933`, IC `[0.6724933968, 0.7140410128]`→`[0.6725, 0.7140]`, `n_util=5579`) · `limite_de_constructo` con el texto verbatim de ADR-75(a) Decisión A · `antes:` la fila de proxy-ENUT obsoleta, citada como historia (la que `modelo:271` traía hasta que PROC-11 la corrigió el mismo día).

**(c) Paso 5 / rótulo.** La fila "Proxy declarado" pierde `norma_de_género` (3→2: `horizonte_temporal`, `deferencia`) y su ⚠️ "espera D3" se retira — D3 está resuelta en sustancia (`D3-A`, ver §3). Fila nueva `MEDIDO·NACIONAL` (1: `norma_de_género`). La fila de `obligación_medida` (D3 "pendiente") **no se toca de contenido** — solo gana una nota de que su rótulo quedó sin actualizar por este acto, declarado, no ejecutado: la clase que la admitiría ya existe, pero su entrada a `procedencia.yaml` es de `PROD-P638`. `15 ✔ (9+0+2+2+1+1)`.

## §3 · Los seis sitios del numerador, re-derivados

`modelo-decision-v4_0.md`: cabecera (changelog v4.0), el callout `📊` de §1.1.F (con su nota de NUMERADOR reescrita — D3 resuelta `D3-A` en sustancia, vía `ADR-79(a)`; ver `forense/notas/2026-08-13-proc-11.md` ADENDA B.2), el callout `📊` duplicado más abajo (§6.1-adyacente), la fila `MEDIDAS` de la tabla de Paso 5, y la viñeta de resumen ejecutivo (~:736). `README.md:37` y `canon/estado-programa-v1_10.md:97` (nota añadida, sin reescribir el valor histórico de la línea de P3 — mismo criterio append-only que PROC-11 aplicó a `ADR-79` citando "9 de 14").

## §4 · Commit 2 — ejecución y cierre, con un hallazgo que trona la suite

`python3 tests/check.py` (no `--baseline`, para ver `[ok]`/`[FAIL]` explícito) tras los edits:

```
[ ok ]  T15 T-ADR-COUNT
[FAIL]  T19b contador 14 cruzado (modelo)
[FAIL]  T19c portada derivada (README)
[FAIL]  T16 T-SUITE-SELF-CHECK  (5 fail)
```

**Hallazgo verificado, no maquillado.** `T19b`/`T19c` derivan el numerador con `read(proc).count('clase: "MEDIDO·PARCIAL')` — una subcadena literal (`tests/check.py:918`, `:990`). `MEDIDO·NACIONAL` no es substring de `MEDIDO·PARCIAL`, así que ese conteo sigue dando **9**, no 10, después de este acto:

```
$ python3 -c "t=open('milpa/procedencia.yaml').read(); print(t.count('clase: \"MEDIDO·PARCIAL'), t.count('clase: \"MEDIDO·NACIONAL'))"
9 1
```

El encargo mismo anticipó exactamente esto: *"T19b/T19c deben derivar 10 y quedar `[ ok ]` — es el criterio de cierre; si truena, hallazgo, no maquillaje."* Truena. El perímetro del propio encargo prohíbe escribir `tests/`: *"NO ESCRIBE: `tests/` (T19b/T19c derivan el numerador de `procedencia` — se mueven solos)"* — la premisa de esa frase (que se moverían solos) es la que este hallazgo refuta: el predicado no reconoce la séptima clase, así que no se mueven solos. No se corrige aquí. `T16` (`T-SUITE-SELF-CHECK`) falla en consecuencia (el conteo real de FAIL sube de 20 a 26 y varias citas de "18 FAIL"/"20 FAIL" y de WARN en `canon/` quedan desincronizadas de la corrida real). `tests/baseline.json` **no se recongela** (`--freeze` no se corre): congelar estos FAIL como "conocidos" sería exactamente el maquillaje que el encargo prohíbe — la suite queda declarada ROJA, no silenciada.

**Segundo efecto de cadena, también declarado y no maquillado: `T03` (duplicados/citas) marca 6 WARN nuevos sobre la propia archivación A.3 (§6).** El texto verbatim del encargo (archivado en `forense/encargos/2026-08-13-PROC-10-BIS-clase-septima-y-anexos.md`, per convención) cita por nombre los cinco archivos *compass*/red-team de `MOTOR-1` y `propuesta-motor-matriz-v0_2.md` — ninguno existe en el árbol, porque **esos son exactamente los archivos que nunca llegaron a la sesión** (§0.2 de esta nota; ver también la propia advertencia archivada en A.3). `T03` los marca como cita-sin-archivo. No es un defecto de este acto ni del archivo A.3 — es la convención de `forense/encargos/` operando como se espera: un encargo que cita un archivo inexistente "está mal escrito" (`forense/encargos/convencion.md`), y aquí el encargo mal escrito es exactamente la parte (§3, MOTOR-1) que este acto ya declaró bloqueada. Confirma el hallazgo de §0, no lo contradice.

**Recomendación, no ejecutada aquí** (fuera del perímetro de este acto, que no escribe `tests/`): `_CONTADOR_14`/T19c deben contar `MEDIDO·PARCIAL` **y** `MEDIDO·NACIONAL` — por ejemplo `count('clase: "MEDIDO·PARCIAL') + count('clase: "MEDIDO·NACIONAL')` — mismo criterio que el hallazgo de PROC-11 §6.1 sobre el predicado `grep -c obligacion`: un predicado que no se actualiza cuando la taxonomía que vigila crece.

**Contador declarado: `10 de 15`.** La oncena entra con `PROD-P638`.

## §5 · Qué NO hace este acto

No escribe `tests/` (declara el hallazgo de §4, no lo corrige). No toca la fila de `obligación_medida` más allá de anotar que su rótulo quedó sin actualizar — su contenido, su entrada a `procedencia.yaml` y su expediente son `PROD-P638`. No ejecuta `PROD-P638` ni `MOTOR-1` — entornos y encargos distintos (`caja con corpus` y un lanzamiento con cinco archivos que este acto no recibió). No sella ADR. No mueve `4 de 144`, `13 de 27`, `0 de 15` (coeficientes), `1 de 2` (llaves) — sin tocar.

## §6 · Cierre

`python3 tests/check.py --baseline` después de los edits: **26 FAIL · 118 WARN — LÍNEA BASE: ROJO**, 12 entradas nuevas frente a `tests/baseline.json` (`T19b`, `T19c`, 4×`T16`, 6×`T03`), todas declaradas en §4, ninguna corregida ni maquillada. Contadores movidos: `condicionales 9 de 15 → 10 de 15`. Contadores que NO se mueven, declarado uno por uno: `4 de 144` · `13 de 27` (Hito D) · `0 de 15` (coeficientes) · `1 de 2` (llaves) · `11 de 15` (pendiente de `PROD-P638`).

---

## ADENDA (COMMIT 3, post-cierre) — CI de PR #227 rojo por este mismo hallazgo, corregido con autorización directa

Esta sección no edita §0-§6. §4/§6 declararon el predicado roto y decidieron no tocar `tests/` porque el perímetro del encargo lo prohibía sin firma de mesa. El CI de GitHub Actions de PR #227 corrió `python3 tests/check.py --baseline` sobre el mismo commit y confirmó el mismo hallazgo — ROJO, `T19b`/`T19c`/`T16` como en §4/§6, y `T03` sobre esta misma cita de propuesta-motor-matriz-v0_2.md (además de las seis del A.3), 13 entradas nuevas en total (26 FAIL · 119 WARN).

El usuario, en la misma sesión, pidió explícitamente "pull and solve CI" — autorización directa que sustituye a la firma de mesa que §4 declaraba ausente. Con esa autorización:

- **`tests/check.py:918,1001`** (T19b/T19c): el conteo del numerador pasa de `count('clase: "MEDIDO·PARCIAL')` a `count('clase: "MEDIDO·PARCIAL') + count('clase: "MEDIDO·NACIONAL')` — la corrección exacta que §4 ya había recomendado sin ejecutar. `T19b`/`T19c` vuelven a `[ ok ]`.
- **`tests/baseline.json`** se recongela (`--freeze`), capturando el residuo esperado: 7×`T03` (los cinco archivos de `MOTOR-1` nunca entregados a la sesión, más dos citas verbatim del encargo original — A.3 y esta misma nota — al archivo "v0_2" de la propuesta de motor de matriz, que no existe, solo "v0_1") y 6×`T16` (cuatro citas históricas de "107 WARN" en `gobernanza:764,856` y `estado-programa:129,221`, correctas cuando se escribieron, más dos ya conocidas de `ACTO A8-LAND` cuyo número de línea real corrió de `:1132` a `:1134` por commits ajenos entre sesiones). Ninguna de las 13 entradas congeladas es un defecto nuevo — las 7 T03 y las 4 T16 nuevas ya estaban declaradas en §4/§6 arriba antes de esta adenda; las 2 T16 de línea movida son historia ya congelada desde `ACTO A8-LAND`, solo con número de línea corregido.

`python3 tests/check.py --baseline` tras el `--freeze`: **24 FAIL · 119 WARN — LÍNEA BASE: VERDE** (`head` congelado `0ad9b7b759e138b251129c639f6ef943d6ee0fe7`). Contador declarado sin cambio: `10 de 15`.
