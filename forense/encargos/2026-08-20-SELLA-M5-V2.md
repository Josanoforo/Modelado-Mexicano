**SHA de redacción:** declarado en la cabecera del encargo como `8b73aee` (PR #303, 20/ago 17:20 −0600). Verificado al arrancar: `git log -1 --format="%h %s"` → `8b73aee Merge pull request #303 from Josanoforo/claude/bibliotecario-test-mechanizable-13ksz0`. Coincide, sin drift.
**Entorno asignado:** NUBE (repo-only; no toca corpus, microdato ni red de datos). Verificado: `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=cloud_default`.
**Estado:** `CONSUMIDO` por `ACTO SELLA-M5-V2`, 20/ago/2026, `ADR-136`, rama `claude/sella-m5-v2-benchmark-fh3759`.
**Nota de archivo, no del original:** este documento llegó a la sesión como texto de tarea, con un adjunto (nombre de subida ADV1M5v2propuesta20260820.md) entregado en un turno posterior al lanzamiento — la compuerta de arranque paró correctamente la primera vez (el adjunto no llegó con el lanzamiento) y pidió el archivo en vez de reconstruirlo, per doctrina `ADR-124`. El texto que sigue es exactamente el recibido, sin editar.

---

```
Contra ⟦SHA al lanzar; hoy 8b73aee (PR #303, 20/ago 17:20 −0600)⟧.
ENTORNO ASIGNADO: NUBE (repo-only; no toca corpus, microdato ni red de datos).
NO Ubuntu — deja la caja libre.
Modelo: Opus.  🚫 --freeze.  Dueña única: pgrep -af claude al arrancar, y reporta.
Concurrencia: ninguna declarada. Si hay otro acto en vuelo, mesa lo dice al lanzar.
```

Compuerta de arranque. El lanzamiento trae adjunto `ADV1-M5-v2-propuesta-2026-08-20.md`. Si no llega, PARA y pídelo — no lo reconstruyas (precedente `ADR-124`; ese paro es correcto y no cuenta como sesión perdida).
Firma de mesa que autoriza este acto, 20/ago/2026, verbatim: «Vamos con 1» — sobre la opción (1) del benchmark: eje unificado, márgenes declarados antes, veredictos por posición del intervalo, todos los corredores como filas de la misma tabla, y secuencia fija declarada al sellar. Y «Venga!!!!» autorizando el sello.

ARRANQUE
1 · REPO. Localiza el clon existente; no clones uno nuevo salvo que no haya. Reporta ruta absoluta · `git log -1 --format="%h %s"` · `git status`. No arranques desde el home.
2 · SHA. Contra el declarado. Si `main` se movió, no es PARO: refresca, re-deriva ADR y máximo de tablero, y reporta la diferencia antes de editar.
3 · `data/raw`. Este acto no la usa. Dilo y salta.
4 · ENTORNO. `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE`, esperado `cloud_default`. Sin sonda: no toca red de datos. Reporta el valor crudo.
5 · ESPEJO. Prohibido derivar cifras del proyecto de Claude. Toda cifra sale del clon con el comando a la vista.

⚠️ Regla del `grep`, y esta semana mordió tres veces. Cuando un comando te devuelva un conteo, abre los archivos antes de concluir. Un negativo producido por un comando que no examinó archivos no es un negativo, y un negativo acotado a N documentos no se ensancha a «el corpus». Los tres defectos: «los pobres no pagan» (el documento ya estaba en el árbol) · `LOTE-RETRIAGE` (las cinco fichas ya estaban archivadas) · «falsador no existe» (existe en 119 archivos). Usa `command grep` y `iconv -f utf-8 -t utf-8 -c` en las salidas.

VERIFICACIÓN DE EXISTENCIA (A.8, contestada por quien escribe)
(1) Estructura. Las tablas gobernantes son `forense/escala-cinco-casillas-piloto-v1_0.md` (el archivo que hoy declara `NO SELLADO — COMPUERTA B-bis ACTIVADA`), `canon/gobernanza-v1_15.md` y `forense/firmas-pendientes.tsv`. El párrafo fuente vive en `forense/adv-duelo/CAREO-ADV-DUELO-diseno-v2-2026-08-19.md` §B.
(2) Contenido. Derivado contra `8b73aee`:
* `forense/escala-cinco-casillas-piloto-v1_0.md` existe, 33 líneas, `NO SELLADO`, con la compuerta B-bis activada y la frase «este documento no autoriza a ningún acto sucesor a tratar `ADV1-M5` como sellada».
* `ADV1-M5 v2` → NO-ENCONTRADO. `forense/prereg-duelo-v2/mesa-pendientes.md` §2 sigue con las tres opciones abiertas y ninguna elegida.
* `scoring-adv1-m3.py` ya calcula las cinco condiciones por separado y las deja sin componer, con la composición como parámetro pendiente de mesa (su propio encabezado, líneas 24-26). Re-verifícalo en tu SHA y lee los archivos, no solo el conteo.
(3) Cobertura retroactiva. El careo aterrizó el 20/ago (`ACTO SELLA-ADV`), la escala el 20/ago (`DUELO-PREREG-V2`), el tablero es de agosto. Nada que este acto toca es anterior a sus tablas gobernantes. Sin brecha.

T1 · Aterrizaje de la propuesta
`forense/adv-duelo/ADV1-M5-v2-propuesta-2026-08-20.md` — el adjunto verbatim, con cabecera de procedencia (dirección + benchmark web del 20/ago, cuatro campos, fuentes en su §9), `sha256` en la nota y `diff` contra el adjunto. Única diferencia admisible: la cabecera de procedencia y el salto de línea final (doctrina `FP-57`).

T2 · `escala-cinco-casillas-piloto` sube a `v2_0` — y el `v1_0` NO se edita
Nace `forense/escala-cinco-casillas-piloto-v2_0.md`. `v1_0` se conserva íntegro y gana un banner de una línea apuntando aquí — patrón exacto de `v0.4` sobre `v0.3` del contrato celda-D, y `A.10` corolario 1: «un sello vencido se reactiva por re-sello contra el universo nuevo, nunca por edición del viejo».
El `v2_0` contiene, en este orden:
1. El párrafo original de `ADV1-M5`, verbatim, citado por ruta y línea desde el careo. No se toca una palabra. Sigue siendo la fuente.
2. La capa de cómputo de la propuesta: eje único (§2), márgenes (§3), secuencia fija (§4), tabla de veredictos por posición del intervalo (§5).
3. Las cinco cláusulas anti-sobrelectura del original, verbatim, atadas a su posición — son lo que `D-ii` firmó, incluida la cláusula de alcance que gobierna sobre las seis: «ningún resultado autoriza abandonar L ni M para usos no muestreados — el duelo mide estimación de cantidades encuestables y nada más.»
4. La declaración B-bis explícita, que es la que abre la compuerta: qué significa que el falsador no refute. Con la escala de §5 son `(3a) EQUIVALENTES` —corroboración, hallazgo positivo— y `(3b) INDETERMINADO` —el falsador fue demasiado débil—, separadas y no fundidas. Escríbelo con esas palabras, que son las de `instrucciones-proyecto-v2_10.md:113`.
5. La regla de precedencia, que es la secuencia de §4, con la frase obligatoria: fijada al sellar y no después.

⚠️ Rótulo: nunca el rótulo pelado a secas — colisiona con cuatro habitantes vivos del mismo rótulo pelado (`ADR-100(5)` · el defecto homónimo de `RONDA-M:61`, cableado en `milpa/src/matriz.py:21` · el homónimo histórico de `hallazgos:65` · y este). El vigía `T25` lo atrapa.

T3 · ADR
Número derivado al escribir Y al fusionar; máximo hoy 135. Cláusulas:
(a) Sella `ADV1-M5 v2` como la capa de cómputo vigente, citando la firma de mesa verbatim de la cabecera de este encargo.
(b) `D-ii` queda CUMPLIDA. Su letra es «Firma la tabla de cinco casillas de ADV1-M5 antes de la primera celda» (`TRANSFER §4`, FIRMADA 19/ago). Con este sello, la primera celda puntuada del piloto deja de estar bloqueada. Dilo explícitamente: es el desbloqueo que este acto compra.
(c) Declara qué se conserva y qué se traduce, con la tabla del §1 de la propuesta. El original no queda refutado, no se borra, y sigue siendo la fuente — las tres cosas, `A.10`.
(d) Procedencia del diseño, declarada y no escondida: benchmark web del 20/ago sobre cuatro campos (no-inferioridad y equivalencia · multiplicidad y secuencia fija · hubs de pronóstico · equivalencia combinada con significancia). Fuentes en el §9 de la propuesta. Marca esto como procedencia (c) del Bloque A: marco importado, adoptado con crítica y no por reflejo — y di qué se adoptó y qué no.
(e) Estampa `A.10`: tu SHA · el adjunto con su `sha256` · el párrafo fuente por ruta y línea · las rutas de `v1_0` y `v2_0` · fecha.
(f) Lo que este ADR NO decide, declarado: no fija `Δ` (`D-iv` lo ordena al acto de pre-registro, mesa lo firma) · no adjudica `FP-91` (los siete umbrales; la casilla (4) es uno de ellos y esta capa no lo resuelve) · no decide qué emite el corredor `M` (sesión `EMISOR-M`) · no toca el marco ni el sorteo.

T4 · Tablero
Máximo derivado hoy: `FP-95`. Próxima `FP-96`, re-derivada al fusionar. Estado hoy: 95 filas · 68 `FIRMADA` · 16 `CERRADA` · 10 `ABIERTA` · 1 `CERRADA POR PREMISA REFUTADA`. No heredes estas cifras: re-derívalas y pega el comando.
* Fila nueva `FIRMADA` por `ADV1-M5 v2` y el cumplimiento de `D-ii`, con la firma verbatim.
* Fila nueva `ABIERTA`: «¿La composición de `scoring-adv1-m3.py` se implementa en este acto o en el sucesor?» — el script deja la composición como parámetro configurable y este acto no escribe código; el sucesor lo hace. `gatea`: la primera celda puntuada.
* ⚠️ Fila nueva `ABIERTA` por el hallazgo de §2 de la propuesta, y va con reserva escrita. La propuesta sostiene que con el eje unificado el intervalo del veredicto agregado sale del remuestreo pareado entre celdas, no del `EE` del árbitro de cada celda — y que por tanto `FP-83` deja de bloquear el veredicto del piloto y queda acotada a la etiqueta por celda. `FP-83` ya está `FIRMADA` (`ADR-135`), así que no la reabras: abre fila propia que pregunte si esa lectura se adopta. Es propuesta de dirección, no derivación verificada, y así se escribe.

T5 · Hallazgos
Al menos dos líneas en `forense/hallazgos.md`:
1. Las cinco casillas de `ADV1-M5` se solapaban porque medían tres comparaciones sobre tres ejes (L vs M · L/M vs B · L/M vs R), y la casilla (2) ya traía escrita la precedencia que nadie había leído como tal: «NO licencia "M es bueno" salvo skill material sobre B». La secuencia no la inventa: la implementa.
2. El corredor `E` no tenía casilla en una tabla de consecuencias que mesa adoptó con cuatro corredores, y el adversarial que la escribió predijo que `E` ganaría — «que es lo que suele pasar con ensembles en pronóstico» (`forense/adv-duelo/ADV-1_demolicion_duelo_L_vs_M.md:355`). La solución no fue una sexta casilla: es una fila más en la misma tabla, como en los hubs de pronóstico.

Cierre
Encargo archivado en `forense/encargos/` y marcado `CONSUMIDO` con su PR (A.3). Nota del acto con tabla T1–T5 y los `sha256`. `python3 tests/check.py --baseline` VERDE, salida pegada.
Contador: medición sobre México = 0, dicho (v2.3). Lo que este acto compra es el desbloqueo de `D-ii` — la primera celda puntuada del piloto deja de estar bloqueada, y con ella el marcador.

PERÍMETRO Y CONCURRENCIA
`forense/adv-duelo/ADV1-M5-v2-propuesta-2026-08-20.md` · `forense/escala-cinco-casillas-piloto-v2_0.md` · banner de una línea en `…-v1_0.md` · `canon/gobernanza-v1_15.md` (ADR) · `forense/firmas-pendientes.tsv` · `forense/hallazgos.md` · `forense/encargos/` · nota del acto.
No toca: `milpa/` · `data/` · `corpus/` · `tests/` (ni siquiera `scoring-adv1-m3.py` — la composición es del sucesor) · `forense/marco-candidatas-piloto-v1_0.tsv` · `forense/prereg-duelo-v2/` salvo lectura · el párrafo original del careo.
Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.

## CONSUMIDO

Derivado por auditoría 2026-09-03 (ACTO MAESTRA37-N9 · AUDITA-ENCARGOS-166). `grep -Fc -- "2026-08-20-SELLA-M5-V2.md" canon/gobernanza-v1_15.md` → 1: citado bajo ADR-136 en canon/gobernanza-v1_15.md, con lenguaje de ejecución (archivado/ejecutado) en el bloque correspondiente. Marca ausente en el archivo era defecto de trámite, no evidencia de no-ejecución.
