# ENCARGO · CONF-07-CIERRE (v2) — sellar la partición ya ejecutada de §3.7 + higiene de cinco rótulos

**SHA de redacción:** `57984b5` (`PR #262`, `origin/main`) · **Entorno asignado:** NUBE, repo-only · **Modelo:** Opus
**Estado:** CONSUMIDO — ACTO CONF-07-CIERRE, rama `claude/conf-07-cierre-partition-labels-lfu6k7`, 18/ago/2026. Sella `ADR-106`. Firma de mesa, verbatim: *"Adopto"*. Ejecución completa, comando por comando: `forense/notas/2026-08-18-sello-conf07-y-rotulos.md`.
**Gate:** ninguno (CONSOLIDA-2 fusionó en `#259`).
**Deriva contra el SHA declarado, registrada y no escondida:** `main` avanzó a `f3d3f95` (`PR #263`, `COND-ATRIB`) mientras el encargo se redactaba. No fue PARO: `57984b5` es ancestro de `HEAD` y toda premisa se re-derivó por contenido. Dos cifras del encargo resultaron **desmentidas por el clon** y se corrigieron en el sello: el máximo de ADR era **105**, no 104 (este acto sella el **106**); y la distribución de tiers de `§3.7` es **3F · 3M · 2MF · 1H**, no `5F·1H·2MF·3M` — el `5` era artefacto de `grep` sobre dos `[FUERTE]` de prosa histórica en el bullet 7.

> Archivado bajo A.3 por la sesión que lo ejecutó. Texto verbatim tal como se lanzó; lo único añadido es esta cabecera.

---

## Texto verbatim del encargo

ENCARGO · CONF-07-CIERRE (v2) — sellar la partición ya ejecutada de §3.7 + higiene de cinco rótulos
SHA: `57984b5` (#262) · Entorno: NUBE, repo-only · Modelo: Opus · Estado: VIVO · Gate: ninguno (CONSOLIDA-2 fusionó en #259). Delta v1→v2: ADR máximo hoy 104 · `glosario:399` ya fue re-derivada por #259 — deriva tú su estado actual antes de tocarla · líneas por contenido, no por número · se añade el C0 de higiene. 🚫 Sin `--freeze` · ola 1 en paralelo: colisión de ADR esperada, re-derivar dos veces · `estado-programa:101` cláusula por cláusula salvo que ESTADO-SPLIT ya haya fusionado (entonces la cascada va donde el split la dejó — derívalo).
════ ARRANQUE ════ los 5 estándar: clon no-superficial, `main` local actualizada antes de cualquier checkout; SHA contra `57984b5`; data/raw no se usa; firma A.2 de tres partes (nunca `curl -I`); toda cifra del clon con comando.
VERIFICACIÓN DE EXISTENCIA (contra `57984b5` — re-córrela tú)

```
partición hecha:   modelo-decision:437 "se parte en dos (conf.07)" + tiers por regla (5F·1H·2MF·3M)  EXISTE-SATISFACE
conf.08:           glosario "✅ Corregido en modelo v2.3"                                             EXISTE-SATISFACE
fila conf.07:      glosario ~:323 "⚠️ Abierto — nuevo en v5.1"                                        EXISTE-NO-SATISFACE
deudas §15:        estado ACTUAL post-#259 — derívalo (grep "conf.07" glosario)                       A DERIVAR
ADR que la selle:  grep "conf.07" gobernanza → ninguno la SELLA al redactar                           NO-ENCONTRADO
requisito salida:  "no citarse como [FUERTE] completa" — nunca verificado                             NO-ENCONTRADO
rotulos C0:        5 encargos del 17/ago con Estado:VIVO rancio (lista abajo)                          EXISTE-NO-SATISFACE
```

PERÍMETRO
`glosario` (fila conf.07, deudas §15, §8/§11 si citan el empaquetado) · `gobernanza` (ADR) · `estado-programa` solo cascada de conteo · `forense/encargos/` (C0: solo bloques de Estado, ni una línea más) · notas · `hallazgos.md` (append) · tablero solo si C1 abre fila. NO toca `modelo-decision`, `hitoD`, `milpa/`. Fuera: PARA.

C0 · Higiene de rótulos — cinco encargos, evidencia citada, cero ejecución
Para cada uno: lee su cuerpo, busca la evidencia de su destino en el árbol, y actualiza solo su bloque de Estado con cita:

* `2026-08-17-EA10-a10-estampa.md` → la estampa corrió: las instrucciones v2.10 (enmiendas in situ, ACTO A10-ESTAMPA) y el texto de A.10 en canon lo prueban → `CONSUMIDO` citándolas.
* `2026-08-17-B2-RELEVO…md` → superado materialmente por #255/#260 → `SUPERADO` con las dos citas.
* `EDEC` · `EHIG` · `RUTA-SELLO` → deriva su destino real (grep de sus entregables en el árbol); si hay evidencia de ejecución/supersesión → rótulo con cita; si genuinamente vivo → déjalo `VIVO` con razón fechada y, solo si gatea algo, fila. No inventes destinos.

C1 · Requisito de salida — el entregable duro
`grep -rn "3\.7"` sobre canon/corpus/forense/milpa (filtra `v3.7`): clasifica cada cita — (a) regla partida con tier propio → OK; (b) "§3.7 [FUERTE]" en bloque → corrígela con el tier de la mitad citada; (c) ficha del Hito D cuyo tier dependa del empaquetado → PARA en esa cita (ADR-60(b): veredictos y perímetro no se tocan). Tabla completa en la nota.

C2 · El sello
Prompt único a mesa (mecanismo ADR-86/88/90): las dos mitades con su tier + resultado de C1 → "adopto / corrijo:[texto]". Con respuesta: ADR que sella la partición ya ejecutada (cita modelo v2.1 y glosario v5.1 como actos materiales) · fila conf.07 → resuelta · deudas §15 re-derivada (no editada a mano) · citas (b) enumeradas · conf.08 no se reabre (solo verifica que ningún texto vivo repita el "ni broker" viejo; si alguno, corrígelo citando modelo v2.3).

C3 · Cierre
ADR (re-derivado ×2, base 104) · cascada · nota con tabla C1 + los 5 rótulos y su evidencia · `hallazgos.md` · encargo `CONSUMIDO`. Auditoría: contadores México: cero, una línea. Mueve: última `conf` sin sello adjudicada + cinco cadáveres de rótulo enterrados con acta. Nada tecleado. NO hace: no re-tieriza mitades · no toca veredictos · no ejecuta EDEC/EHIG/RUTA-SELLO — solo los rotula con evidencia.
