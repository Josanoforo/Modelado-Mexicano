**ADR-260921-GEN2-MARCADOR-E-INFORME-1-48d4-01**, `ACTO GEN2-MARCADOR-E-INFORME-1`, 21/sep/2026, NUBE (`cloud_default`, corpus **NO** montado, cero microdato), Opus 5, sin sub-agentes, MODO ABIERTO, LOTE de cuatro piezas, COMPUERTA: ninguna. **P0** asienta verbatim las tres decisiones de mesa del 21/sep a MOTOR (`FP-260921-GEN2-MARCADOR-E-INFORME-1-48d4-01/-02/-03`), verificadas por objeto antes de asentar: cero coincidencias sobre 414 filas de `firmas-pendientes.tsv` + 185 de `decisiones.tsv`. **P1** `celdas_validadas` **73 → 88**: la clase 1 de la métrica rectora era una lista de DOS celdas-D tecleada dentro de `tools/tablero_programa.py` y por eso el piloto 3 no la movió; ahora se deriva de toda celda-D con veredicto sellado (validada = predicha antes y comparada después, **no** acertada: `FALSADOR-DEBIL` cuenta igual que «vence»), con la escala derivada contra el `margen_material` sellado de cada celda-D y la celda **sin contar** si la escala no se deriva. Desglose por tipo (**35** cruce / **53** marginal) y por instrumento en el tablero. **P2** `tools/prospectividad.py` publica en el marcador la columna `prospectividad` + `prospectividad_cita`, derivada de los sellos (`ejecucion.json:fecha`) y **no de `git`** —el clon es `shallow` y ahí la fecha de alta del yaml de la ola5 cae *después* de una corrida sellada que ya lo declara como input—: **20 PROSPECTIVA · 59 RETROSPECTIVA · 89 IDENTICO-EMISOR-ES-ARBITRO · 30 SIN-EMISION · 16 EMITIDA-SIN-R · 0 ORDEN-NO-DERIVABLE** sobre 214 filas; ninguna vista suma las dos primeras. **P3** (firma F8) declara la ola de calibración de `tramite.evasion_norma` = **ENVIPE 2025**, derivada de `CALC-EVASION-NORMA-0001-v1_1/spec.yaml` y cerrada por hash (el `sha256` del payload de esa spec es el `sha256_payload` de la regla en `milpa/tramite.yaml:535`): `test_10` y `test_11` pasan. **P4** `canon/informe-programa-v1_1.md` (la v1.0 **no se edita**, queda VENCIDA EN ALCANCE por A.10), con marcador en columnas que no se mezclan, los tres pilotos con error y **cobertura del IC95 por candidato** (R dentro del IC del candidato, **no** el campo `ARB-DENTRO-IC-R` del piloto 2, que mide lo contrario) e intervalo de Wilson con la advertencia de dependencia entre celdas de una misma ola —**piso C2: 7/8 · 11/12 · 8/15 = 26/35, [0.579, 0.858]**, peor que el 18/20 de dos pilotos—, y el módulo de auditoría de §5 completo. `tools/informe_pilotos.py` deriva todas las cifras de §2 y se verifica contra los MAE sellados: **COINCIDE en los 12 candidatos**. `cuenta_gen2 = NO`: no sella, no adopta, no adjudica. Guardia propia `tests/test_marcador_metrica_y_prospectividad.py` (11 casos, celdas-D sintéticas de cada veredicto), cableada en CI. Vista completa: `python3 tools/l0_vista.py`.
