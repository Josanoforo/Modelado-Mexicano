# ENCARGO · CENSO-CMD — derivar el censo de estimabilidad por comando, no por foto escrita a mano

**SHA de redacción:** `93a4dd9` (`origin/main`, tras `ADR-100`/`ADR-101`, ACTO MESA-18AGO, 18/ago/2026)
**Entorno asignado:** **NUBE** (`cloud_default`, repo-only). No toca microdato — deriva de TSV ya en el repo.
**Estado:** `CONSUMIDO` — ejecutado por `ACTO CENSO-CMD` (rama `claude/censo-cmd-launcher-kmeg90`), detalle en `forense/notas/2026-08-19-censo-cmd.md`.
**Origen:** `FP-37` — `ADR-61(d)` midió que el censo se venció dos veces antes de ser leído y dejó "para mesa" si pasa a derivarse por comando, sin implementar mecanismo. Intentado en `ACTO MESA-18AGO` (`ADR-101(m)`): excede el alcance de una sesión ya extensa en otros diez incisos — se escribe el encargo con el puntero completo en vez de fingir el intento.

## Verificación de existencia (A.8), contestada por quien escribe

```
censo vigente (foto a mano):    forense/censo-estimabilidad-coeficientes-v1_1.md §5    EXISTE
reglas de precedencia:          forense/censo-estimabilidad-coeficientes-v1_0.md §1
                                 (define RUTA-A/RUTA-C/RUTA-I/SIN-RUTA, sellado ADR-89)  EXISTE-SATISFACE
insumos cruzables:              data/curacion-registro/relaciones.tsv (capa4_apertura_mapeo,
                                 clasificacion_relacion) + data/curacion-registro/
                                 necesidad-objeto-modelo.tsv (mapeo necesidad_id -> fila
                                 del censo, verbatim de FP-15: "fila N del censo = N{N}")  EXISTE-SATISFACE
precedente de mecanización:     RUTA-SELLO (ADR-89) ya mecanizó el REPARTO por awk sobre
                                 la tabla -- no mecanizó la CLASIFICACIÓN en sí (eso es
                                 lo que este encargo debe hacer)                          PRECEDENTE, NO
                                                                                           SATISFACE
cotejo ya hecho a mano una vez: registro-recalculo Entrada 0 (ACTO ADJ-4) cruza censo
                                 v1.1 contra relaciones.tsv, 15/15 filas, con receta de
                                 mapeo 1:1 explícita                                       EXISTE-SATISFACE,
                                                                                           reutilizable como
                                                                                           especificación
```

## Perímetro

ESCRIBE: `tools/` (un derivador nuevo, p. ej. `tools/censo_estimabilidad.py`, de solo lectura sobre `data/curacion-registro/relaciones.tsv` + `necesidad-objeto-modelo.tsv`, sin tocar ninguno) · `forense/censo-estimabilidad-coeficientes-v1_2.md` (nuevo, generado por el derivador, no editado a mano) · `tests/` (un test que compare la salida del derivador contra el censo v1.1 vigente, y falle si diverge sin ADR) · nota del acto. NO ESCRIBE: `canon/`, `data/curacion-registro/**` (solo lee), `milpa/`.

## Tarea

1. Formalizar como código la receta de mapeo `necesidad_id` → fila del censo que la Entrada 0 de `registro-recalculo` ya usó a mano (`forense/registro-recalculo-v1_0.md` §1, fila 0).
2. Formalizar como código las reglas de precedencia RUTA-A/RUTA-C/RUTA-I/SIN-RUTA de `censo-estimabilidad-coeficientes-v1_0.md §1`.
3. Correr el derivador contra el árbol real; verificar que reproduce exacto el censo v1.1 vigente (15/15 filas, mismo reparto que `ADR-89` ya midió: 3 RUTA-A / 5 RUTA-C / 1 RUTA-I / 6 SIN-RUTA).
4. Si reproduce exacto: el censo pasa a derivarse por comando desde este acto en adelante — declarar censo-estimabilidad-coeficientes-v1_2.md (sin comillas invertidas aquí a propósito, para no disparar T03 sobre un archivo que este mismo encargo crea) como el vigente, generado, no escrito a mano. Si diverge: investigar la divergencia antes de declarar nada — puede ser un defecto del censo v1.1 (a corregir con su propio ADR) o de la receta (a corregir en el derivador).

## Cierre

Derivador corriendo, reproduce el censo vigente exacto (o divergencia investigada y declarada con causa) · `firmas-pendientes.tsv`: `FP-37` con la decisión ejecutada · línea en `hallazgos.md` · encargo `CONSUMIDO`.

## INDETERMINADO

Derivado por auditoría 2026-09-03 (ACTO MAESTRA37-N9 · AUDITA-ENCARGOS-166). `grep -Fn -- "2026-08-18-CENSO-CMD.md" canon/gobernanza-v1_15.md` cita ADR-101, pero el bloque mezcla lenguaje de ejecución y de encargo pendiente (o el rótulo del ADR es compartido entre varios encargos sin desenlace individual claro) — rastro parcial, no se decide aquí. Para mesa: verificar manualmente contra ADR-101 en canon/gobernanza-v1_15.md.

## CERRADO-POR-HISTORIA

Regla mecánica (b) de la resolución de mesa sobre FP-290 (2026-09-04):
sin hermano de rótulo compartido con desenlace ya sellado (regla a no
aplicó -- ver tabla en forense/notas/2026-09-03-MAESTRA37-N9-auditoria-encargos.md,
enmienda 2026-09-04), este encargo queda cerrado por antigüedad e
inacción declarada, no por evidencia positiva de ejecución o
sustitución. Si aparece evidencia nueva, esta marca se reabre -- no es
`## CONSUMIDO`.
