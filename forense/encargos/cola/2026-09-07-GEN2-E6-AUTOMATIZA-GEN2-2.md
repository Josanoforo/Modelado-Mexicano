ESTADO: CONSUMIDO — PR #611 (`claude/e6-registro-status-tablero-82umwv`). Sincronizado por `ACTO GEN2-E7` pieza D (D2a): seguía `LISTO-NUBE` con el PR ya fusionado y su homónimo archivado (`forense/encargos/2026-09-08-GEN2-E6-AUTOMATIZA-GEN2-2.md`) ya con `## CONSUMIDO`.
ENTORNO: NUBE
ENCOLADO: 2026-09-08 · ACTO GEN2-V213 reencola: reemplaza el cuerpo de este archivo, verbatim (texto técnico sin cambios respecto de la versión que `ACTO GEN2-T7-CIERRE` encoló), y recomputa la COMPUERTA contra `origin/main = df9336c5` bajo la regla nueva E.5 de `instrucciones-proyecto-v2_13.md` ("el aparato se prueba con replays").
BITACORA:
- 2026-09-08 · **LISTO-NUBE** (recomputado por `ACTO GEN2-V213`, cambia de `GATEADO`). La compuerta ORIGINAL de esta pieza (`git ls-tree -d --name-only origin/main data/corrida0/ | grep -c "^data/corrida0/CALC-000"` ≥ 2) sigue sin cumplirse por el patrón literal: verificado hoy contra `origin/main = df9336c5`, la salida es **0** (`data/corrida0/` solo trae `CALC-SMOKE-0001` y `CALC-SMOKE-0002`, ninguno con el prefijo `CALC-000`). Pero E.5 de `instrucciones-proyecto-v2_13.md` — «los cálculos van después del aparato, y el aparato se prueba con replays» — autoriza exactamente este caso: `CALC-SMOKE-0001`/`CALC-SMOKE-0002` ya están sellados con el esquema endurecido completo de `ADR-394` (`spec_yaml_sha256`, `script_blob_sha256`, `input_sha256`, `sello.sha256`, `verify` → `REPRODUCE`/`IDENTICO`) y sirven de fixture legítimo para construir y probar `registro`/`status`/`tablero`/`T-REPRO` — la propia regla lo dice: el aparato se valida contra replays antes de que exista una corrida GEN2 real. **COMPUERTA (v213, vigente, reemplaza la anterior): ≥2 directorios `CALC-*` sellados en `origin/main` con esquema completo (`spec_yaml_sha256` + `sello.sha256` válidos), sin exigir el prefijo `CALC-000` ni que sean GEN2 real — cumplida hoy (2).** Nota explícita de A.13/E.4: `T-REPRO`/`registro`/`status` deben, aun así, excluir estos `CALC-SMOKE-*` de los contadores GEN2 reales (`cuenta_gen2 = NO`, ya declarado por E3.1/`ADR-394`) — usarlos como fixture de prueba no los vuelve medición GEN2.

──── CUERPO VERBATIM DEL ENCARGO (A.3) · el despachador NO lo edita ────

## E6 · ACTO GEN2-E6 · AUTOMATIZA-GEN2-2 · v1.3 — registro, status, tablero, T-REPRO sobre el esquema endurecido

Cabecera: **NUBE** · **Opus** · COMPUERTA por producto: `git ls-tree -d --name-only origin/main data/corrida0/ | grep -c "^data/corrida0/CALC-000"` ≥ 2. NO se lanza en UBUNTU. **COMPUERTA reemplazada (ACTO GEN2-V213, 8/sep/2026, E.5 de `instrucciones-proyecto-v2_13.md`, "el aparato se prueba con replays"): ≥2 directorios `data/corrida0/CALC-*` en `origin/main` con sello completo (`spec_yaml_sha256`+`sello.sha256` válidos), sin exigir prefijo `CALC-000` ni GEN2 real — cumplida hoy con `CALC-SMOKE-0001`/`CALC-SMOKE-0002` (`ADR-394`). El propio texto de abajo ya declaraba `CALC-SMOKE-* con cuenta_gen2 = NO` fuera de los contadores GEN2 reales; esta compuerta solo autoriza usarlos como fixture para construir/probar el aparato, no los cuenta.**
Qué hace: `cmd_registro` (une `demanda-*.tsv` + `decisiones.tsv` + `CALC-*/` → `corridas.tsv · resultados.tsv · usos.tsv`, `# DERIVADO — NO EDITAR`; campos del esquema de E3.1: `spec_yaml_sha256 · input_sha256 efectivos · resultado_replay · contexto_replay · estado (…SELLADA | SUPERADO→sucesor)`; validaciones que paran: ids duplicados, RESULT sin CALC, uso a RESULT inexistente, CALC sin spec, RESULT sin sello, hash ausente, ciclo, consumidor activo apuntando a LEGACY; avisan: RESULT sin consumidor, CALC sin consumidor activo, legacy sin sucesor; `CALC-SMOKE-*` con `cuenta_gen2 = NO`) · `cmd_status` (§9 del plan) · `tools/tablero_programa.py` deriva de `status` y **elimina el hardcode histórico** (marco anterior al v1.3) · **T-REPRO** en `tests/check.py` (`T33`): cada RESULT activo GEN2 con cadena completa; consumidor→RESULT→CALC resolubles; `valor materializado == RESULT` dentro de tolerancia por tipo; **más** (11.1) todo CALC activo con `spec_yaml_sha256`, `script_blob_sha256`, `input_sha256` completos y (11.2) inmutabilidad estructural — artefactos sellados coinciden con su sello, sin reejecutar microdato. Línea base congela GEN1; si E5 pidió cambios de esquema, se declaran y el gate entra en la versión siguiente.
Perímetro: `tools/corrida0.py` (`cmd_registro`, `cmd_status`) · `tools/tablero_programa.py` · `tests/test_corrida0.py` · `tests/check.py` · `data/corrida0/{corridas,resultados,usos}.tsv` · cascada. Si te encuentras escribiendo fuera de esta lista, PARA.
Contador: cero directo; el tablero pasa a derivar GEN2.
