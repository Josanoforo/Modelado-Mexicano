# NOTA · Verificación de premisas — SESIÓN EMISOR-M · 20/ago/2026

**Sesión de diseño (transfer `TRANSFER-EMISOR-M-2026-08-20.md`), ejecutada en la conversación de dirección por directiva de mesa en el lanzamiento** (*"trabajar en el diseño desde acá; la otra maestra Opus está en otros temas"*). **Desviación declarada respecto del transfer:** modelo **Fable** (no Opus) y contenedor de dirección (egreso solo-GitHub), no la caja NUBE del programa — equivalente en lo que importa: **repo-only, sin microdato, sin red de datos.** Destino de esta nota: `forense/notas/2026-08-20-emisor-m-verificacion-premisas.md`.

## 0 · ARRANQUE (Bloque D, condensado de la casa)
1 **REPO**: clon existente `/home/claude/repo` (no se clonó nuevo); `git fetch` + `reset --hard origin/main`.
2 **SHA**: transfer declara `c6fe985` (#302); **main real al arrancar = `8b73aee` (#303, 20/ago 17:20 −0600)** — avance limpio, `c6fe985` es su ancestro; re-derivado todo contra `8b73aee`.
3 **`data/raw`**: AUSENTE — este acto no toca microdato; declarado y saltado.
4 **ENTORNO**: `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=[sin_variable]` · sin sonda (sin red de datos, permitido por el propio punto 4) · `type grep` envuelve — se usó `command grep` con conteo de examinados en todo negativo.
5 **ESPEJO**: ninguna cifra del espejo; todo del clon con comando.
**Suite:** `python3 tests/check.py --baseline` → **LÍNEA BASE: VERDE** (HEAD congelado `e24d033`), salida pegada al final del acto. 🚫 sin `--freeze`.

## 1 · PREMISA CERO — "¿existe ya un emisor?" → **CONFIRMADA: NO existe**
`git ls-files "*.py" | wc -l` → **123 archivos examinados**; `xargs command grep -l "disparador\|SI-ENTONCES\|riesgo_fiscal_percibido\|simulad"` → **0**. La afirmación de `pipeline-L-adv1-m2.py:33` (*"M ya vive en el motor de decisión"*) es **falsa como afirmación sobre código**: lo que vive es el canon en prosa + la rebanada mínima, cuya propia cabecera (`milpa/src/motor.py`) declara *"produce… un VEREDICTO DE ESTADO, no un número… no calibra, no estima"*. No es el cuarto negativo mal ensanchado: es el positivo que el transfer temía haber ensanchado, verificado con universo.

## 2 · Premisas §3, una por una

| § | Premisa del transfer | Veredicto | Evidencia (comando propio) |
|---|---|---|---|
| 3.1 | `tramite.yaml` 127 líneas, 5 reglas, forma ejecutable con `si`/`entonces[].p`/`clase`/`tier`/`falsable_si` | **CONFIRMADA, y corta** | Leído COMPLETO (no grep). 5 reglas ✔, 127 líneas ✔. **Hallazgo a favor del diseño que el transfer no vio:** el esquema **ya separa los dos niveles** — `si.disparadores` (globales: `sancion_creible`, `quien_observa`, `cobertura_formal`) vs `si.contexto_producto`/`contexto_norma` (**palancas de dominio**, incl. `riesgo_fiscal_percibido`, `coercitivo`). La brecha de ADR-26 es **del bucle, no del esquema**. Además: la regla espejo del gate está restituida con su nota anti-`NO_COVERAGE`, y `nota_calibracion` declara que el criterio del gate (B<10% de A) *"no depende del split exacto"*. |
| 3.1 | "11 probabilidades `clase: ASIGNADO`" | **REFUTADA — son 10** | `command grep -c "p: 0\." milpa/tramite.yaml` → **10**; y el propio header v0.3.0 lo dice: *"Las 10 probabilidades de este archivo (5 reglas × 2)"*. Cifra del transfer tecleada u obtenida con otra receta; no cambia nada del diseño, se corrige por higiene. |
| 3.2 | 49 reglas en prosa; ¿son las mismas 49 que las refutaciones? | **CONFIRMADA / RESUELTA: conjuntos DISTINTOS** | Reglas: `grep -oE 'R[0-9]+\.[0-9]+'` sobre `modelo-decision-v4_0.md` → **49 únicas** (R1.1…R10.4, lista pegada en sesión). Refutaciones: `meta.refutaciones_compiladas: 49` (`refutations.yaml:26,115`) — pero su espacio de ids es `ref.A/B/C.NN` y su semántica es **anti-mecanismo** ("pruebas que el simulador debe FALLAR"), no reglas de conducta. Coincidencia numérica, cero traslape de ids. El crosswalk del diseño liga preguntas↔**reglas**, nunca ↔refutaciones. |
| 3.3 | procedencia: clase 18 · valor 10 · n_util 19 · ic95 2 · ee 0 · error_estandar 0 · intervalo 1 | **CONFIRMADA EXACTA, campo por campo** | Siete greps propios sobre `milpa/procedencia.yaml` (1 132 líneas), mismos siete números. **Cero errores estándar: el modelo afirma puntos.** |
| 3.4 | Ningún `.py` implementa reglas; `milpa/src` = rebanada de veredictos; curador y `svystat` fuera de perímetro; `milpa-spec` es mapa viejo | **CONFIRMADA** | §1 arriba; cabecera de `motor.py` leída y citada; `milpa/src` = 9 módulos, **1 119 líneas** ✔; banner de `milpa-plan`/`ADR-62` visto en árbol. |
| 3.5 | ADR-26: dos niveles, 7 globales + 42 palancas, booleanos de contexto | **CONFIRMADA verbatim** | `gobernanza:216` y `:275` — con la frase que gobierna el harness: apagar el disparador sin la regla espejo *"caería en NO_COVERAGE y el gate pasaría por pérdida de cobertura, no por prueba de mecanismo"*. |
| 3.6 | Consumidor ya escrito; salida de M debe ser consumible sin modificar scoring; E depende de la forma de M | **CONFIRMADA, con el contrato extraído** | `scoring-adv1-m3.py`: trae `interval_score` (*"sustituto cerrado del CRPS cuando el corredor solo emite un [intervalo]"*), `crps_normal_aprox` (punto+intervalo→Normal), `skill` vs B, `es_indecidible`, `calibracion_80` por corredor L/M/B/E, y capa distribucional (KS/Wasserstein/razón de varianzas). `corredor-E`: dataclass **`PrediccionCorredor`** = `{tipo_escala ∈ continua/binaria/ordinal · valor_punto · valor_categoria · intervalo_lo/hi · confianza_declarada}`; `combinar_continua` **solo exige `valor_punto`** (intervalo opcional, envolvente si ambos lo traen) → **E es implementable con un M de solo-punto** — dato decisivo para §4. `⊕` sigue PROPUESTA sin sellar (`mesa-pendientes §3`), fuera de mi perímetro. |
| 3.7 | Marco 60×17, todas columnas del árbitro, ninguna liga al modelo | **CONFIRMADA** | header leído (17 columnas, pegado en sesión), `60 filas`; `pipeline-L` lee specs del marco (*"NUNCA se edita ese archivo"*). Techo 60 = por construcción (`FP-82`, FIRMADA: ampliar a saturación antes de sortear). **El crosswalk es entregable del diseño.** |
| 3.8 | Congelamiento ADR-68(a); 6 tests de motor intactos; baseline VERDE; sin `--freeze`; clase obligatoria | **CONFIRMADA, con alcance precisado** | `gobernanza:952` (enmienda nombrada): el congelado literal es **`tools/curador_registro/` desde la apertura del piloto**; la cláusula *"el piloto NO edita reglas del motor"* vive en `canon/APERTURA-FASE-CALCULO-v1_2.md:42`. Los 6 `tests/test_motor_*.py` existen (`ls` pegado). Suite VERDE ✔. **Queda UNA ambigüedad real** (¿compilar reglas prosa→YAML cuenta como "editar reglas"?) → pregunta a mesa, no decisión de ejecutor (diseño §6). |
| 3.9 | R3.4: gate ADR-37 A∧B∧C; el interruptor no existe en código; FP-86 la señala; mueve Hito D | **CONFIRMADA, con un matiz que el transfer omite** | ADR-37 verbatim (`gobernanza:267+`): A (<10%), B (colapso ≥70%), C (persistencia, reducción <30%), *"pasa solo si A y B y C"*; interruptor en `milpa/src` → 0 archivos ✔; `FP-86` [ABIERTA] la nombra *"la única cuyo desbloqueo no exige adquirir nada"* ✔. **Matiz (hitoD:370, Nota 2/Nota 3): R3.4 está desbloqueada pero NO TIENE FICHA en el pre-registro** — el dominio §3.3 entero quedó fuera del Paso 1 (R3.1/R3.2/R3.4 sin ficha). Mover 13/27 exige, además del emisor: **escribir la ficha R3.4 pre-registrando el límite de Nota 3** (*"el gate no separa coerción de fricción; `riesgo_fiscal_percibido` captura solo el primer componente"*) y los umbrales B/C declarados `ASIGNADO`. El emisor habilita; no basta solo. |

## 3 · Estado de lo pendiente ajeno (verificado, no tocado — §5 del transfer)
`ADV1-M5` quedó **NO SELLADA** por su propia compuerta B-bis (`escala-cinco-casillas-piloto-v1_0.md`: ni "falsador" ni "precedencia" existen en los cinco documentos fundacionales — verificado por el acto con comando); las 4 lecturas de "falsador no refute" y las 3 opciones de precedencia esperan en `mesa-pendientes.md §1-§2`, junto con `⊕` (§3). `FP-91` [ABIERTA]: contradicción GO/NO-GO de OLA 4. **El diseño de abajo es robusto a cualquier resolución de las tres** (emite; no adjudica casillas, no combina, no gatea).

## 4 · Contador de esta sesión
**Medición sobre México: 0** (sesión de diseño). Lo que habilita: `R3.4` corrible → ficha → **Hito D 13/27→14/27**, condicionado a las dos firmas del diseño §3/§6 y a un acto de implementación.
