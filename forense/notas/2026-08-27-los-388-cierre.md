ACTO MAESTRA31-E8 · LOS-388 — cierre

27/ago/2026, entorno UBUNTU, worktree `/home/pc0/mm-maestra31-e8-los-388`, base `origin/main` = `9578ee6` (incluye `PR #387`/`ACTO MAESTRA31-E6 · DICCIONARIOS-FD`, que libera la caja). `E7 · ETIQUETA` (FP-174) no había fusionado a esta hora — usado `data/inventario-reactivos-v1_0.tsv` **v1_0**, declarado en COMMIT-1.

## ARRANQUE (resumen; comandos completos en el mensaje de dirección al lanzar el acto)

1 · REPO: clon existente en `/home/pc0/Modelado-Mexicano` (rama `acto/cal-g3-puntual` al llegar, `ea22bdd`); worktree nuevo creado sobre `origin/main` en `/home/pc0/mm-maestra31-e8-los-388`, rama `acto/maestra31-e8-los-388`.

2 · SHA: main se había movido de `f1b0d79` (base del encargo) a `9578ee6` — el merge exacto de `PR #387`/`ACTO MAESTRA31-E6 · DICCIONARIOS-FD`, es decir, el gate del encargo se cumplió entre que dirección escribió el texto y que este acto arrancó. Reportado, no PARO.

3 · `data/raw`: existe en el clon principal como symlink a `/home/pc0/mm-corpus/raw` (321 entradas de primer nivel, 720 payloads recursivos). En el worktree nuevo no existía — se enlazó a la misma ruta (`ln -s /home/pc0/mm-corpus/raw data/raw`).

4 · ENTORNO: `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE` = `sin_variable` (coincide con UBUNTU). `curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://www.inegi.org.mx/` → `200` (red alcanzable; este acto no la usa — cero descargas, cero API). `ls data/raw/ | head -1` → `2005trim1_csv.zip`. Ciclo `data/raw/raw` confirmado presente (`data/raw/raw -> /home/pc0/mm-corpus/raw`), excluido por comando (`enumerar_universo()`), no seguido.

5 · ESPEJO: ninguna cifra de este acto sale del espejo; todas del worktree de (1), comando a la vista en `forense/notas/2026-08-27-los-388-commit1-escala.md`.

## Q1 — de qué está hecho el 388 (+16)

Partición re-derivada (no heredada del `.meta`), verificada 316+388+16=720 (COMMIT-1). Causa de los 404 no-OK, clasificada por extensión más — solo para `.zip`/`.xlsx` — listado de miembros (`zipfile.ZipFile.namelist()`, sin abrir contenido tabular):

| causa | n | qué es |
|---|---|---|
| **A** — por naturaleza sin variables | 204 | 139 pdf informe/cuestionario/boletín · 36 html · 23 zip solo doc/imagen/código · 5 html enmascarados por sufijo `.2a` · 1 json `"cero_resultados"` (respuesta vacía de API CKAN) |
| **B** — microdato/diccionario en formato sin rama de despacho | 178 | 125 zip con miembro `.dta`/`.sav`/`.rdata`/`.dbf` · 32 pdf ficha-descriptiva (`fd_*`/`*_fd`) · 8 dta Stata sueltos (`osf_interacting_as_equals/`) · 7 xls diccionario · 4 json tipo `*.metadata.json`/`base_completa` · 1 tab (`MASSMOB`, 15.5MB) · 1 tar.gz (GDELT, 96 miembros `*.export.CSV.zip`, doblemente anidado) |
| **C** — formato con despacho probado en el resto del universo, esta instancia en 0 | 22 | 8 xlsx ficha-descriptiva (`n_sheets` 1-16, `sharedStrings` presente en las 8 — hay texto, no se extrajo) · 6 zip-de-zips (segundo nivel no desempaquetado) · 5 zip con único miembro `.xlsx` no reconocido · 2 csv Banxico SIE (series CoDi/SPEI) · 1 zip real enmascarado por sufijo compuesto `.zip.2a` (`votar_entre_balas_base.zip.2a`: contiene `political_violence.csv` + 2 pdf, formato SI soportado, el despachador por sufijo nunca lo alcanza) |

Empirismo, no la tabla estática del extractor: `FORMATOS_SIN_CAMPOS` de `tools/inventario_reactivos.py` declara `.xml` sin campos, pero los 5 `.xml` del universo **sí** produjeron variables (100% OK) — la tabla del código es aspiracional, no lo que de hecho ocurrió; la clasificación de este acto usa el resultado empírico (qué extensión tuvo ≥1 éxito en los 720), no la tabla declarada.

Hallazgo de nomenclatura, tres archivos: el sufijo compuesto `.2a` (6 payloads, `ADQ15_*`) no es un formato — es un artefacto del proceso de adquisición que antepone `.html.2a`/`.zip.2a` al nombre real, y por eso el despachador por sufijo de `inventario_reactivos.py` nunca los alcanza. De los 6, 5 son páginas HTML reales (causa A de todos modos) pero 1 es un ZIP real con un CSV de datos adentro (causa C, alto valor cero por manifiesto — ver Q2).

## Q2 — cuáles importan

Escala congelada en COMMIT-1 (`hitoD` > `n_notas` > `manifiesto` > `milpa`, empate por `payload_id` alfabético), aplicada mecánicamente:

- **10/404 (2.5%)** citados en `forense/hitoD-preregistro-v2_0.md` — la señal más fuerte.
- **194/404 (48.0%)** con ≥1 cita en `forense/notas/*.md` (incluye los 10 de arriba).
- **201/404 (49.75%) más** dependen *solo* de `manifiesto.yaml` `usado_para` — señal deliberadamente puesta en tercer lugar por su historial de quedarse obsoleta.
- **9/404 (2.2%)** sin ninguna señal en ninguna de las cuatro fuentes: los 6 `.2a` (5 html + 1 zip-con-CSV) más 3 zip explícitamente `DECLARADO_NO_USADO` en manifiesto (`enut2002_bd_dbf.zip`, `enut2014_bd_dbf.zip`, `enut2019_der.zip`).
- `milpa/procedencia.yaml` + `milpa/tramite.yaml`: **0/404** — ninguno de los 404 aparece citado por nombre en ninguno de los dos, verificado por comando.

No es el resultado B-bis excelente (cero señal). Tampoco es "casi todo cero" ni "casi todo señal fuerte": la mitad (194) tiene evidencia independiente del campo débil, la otra mitad (201) depende solo de él. Se reportan las dos cifras por separado precisamente para no inflar el hallazgo con la fuente que se sabe ruidosa.

## Q3 — cuánto ganaría cada rama de extractor

Cruzando causa B/C (200 payloads — los únicos donde un extractor nuevo podría cambiar algo; los 204 de causa A no rendirían variables aunque el formato tuviera despacho) contra alto valor estricto (`hitoD` o `n_notas≥1`, sin depender de manifiesto):

| rama | N | alto valor (estricto) | solo manifiesto | sin señal |
|---|---:|---:|---:|---:|
| zip: miembro en formato sin despacho (.dta/.sav/.rdata/.dbf) | 125 | 40 | 85 | 0 |
| pdf: ficha-descriptiva/diccionario (`fd_*`) | 32 | 25 | 7 | 0 |
| xlsx: ficha-descriptiva no reconocida | 8 | 6 | 2 | 0 |
| dta (Stata, sueltos) | 8 | 1 | 7 | 0 |
| xls (Excel legado) | 7 | 6 | 1 | 0 |
| zip: desempaquetar 2do nivel (zip-de-zip) | 6 | 1 | 3 | 2 |
| zip: miembro xlsx no reconocido | 5 | 3 | 2 | 0 |
| json: sin lógica de extracción de campos | 4 | 0 | 4 | 0 |
| csv: formato Banxico SIE no reconocido | 2 | 2 | 0 | 0 |
| tab (delimitado, extensión no listada) | 1 | 1 | 0 | 0 |
| tar.gz (contenedor no soportado) | 1 | 0 | 1 | 0 |
| zip: sufijo compuesto enmascara despacho (`.zip.2a`) | 1 | 0 | 0 | 1 |
| **TOTAL** | **200** | **85** | **112** | **3** |

Lectura para mesa, sin adjudicar cuál se implementa (fuera de perímetro de este acto): la rama con más payloads es "zip con miembro en formato sin despacho" (125, 40 de alto valor estricto); la rama con mayor proporción de alto valor es "pdf ficha-descriptiva" (32, 78% de alto valor). Las dos xlsx (`ficha-descriptiva no reconocida` y `miembro xlsx no reconocido`, 13 payloads, 9 de alto valor) ya están en formato soportado — no requieren un parser nuevo, requieren corregir la detección de encabezado de `inspect_one`, el mismo defecto que la enmienda F1 de `ORDEN-SUPERIOR` ya documentó para el patrón `.xlsx` (`forense/hallazgos.md`, 2026-08-27).

## A.13 — conteos del veredicto negativo

Ningún veredicto de este acto es "no hay señal" sin comando: los 404/404 con `milpa` = NO se verificaron contra las **1187** líneas de `milpa/procedencia.yaml` + **127** líneas de `milpa/tramite.yaml` completas (búsqueda de texto sobre el archivo completo, no una muestra). Los 9/404 sin ninguna señal se verificaron contra las cuatro fuentes completas: `data/manifiesto.yaml` (794 entradas parseadas con `yaml.safe_load`, no grep de texto), `forense/hitoD-preregistro-v2_0.md` (1190 líneas), `milpa/procedencia.yaml` + `milpa/tramite.yaml` (1187+127 líneas), `forense/notas/*.md` (354 archivos, todos leídos). Cero contenido tabular de ningún payload no-OK fue abierto (REGLA DE TOPE punto 1 y 2, verificado: ningún `pandas.read_*`, `dbfread`, `pyreadstat` ni similar corrió en este acto — solo `zipfile.ZipFile.namelist()`/`.getinfo().file_size`, `Path.stat()`, `tarfile`/`gzip` metadata).

## Lo que este acto no hizo (verificado, no solo declarado)

`git diff --stat origin/main` al momento de escribir esto toca únicamente los archivos del perímetro declarado (ver PR). `data/inventario-reactivos-v1_0.tsv`, su `.meta`, `tools/inventario_reactivos.py`, `tools/curador_registro/**`, `milpa/**` y `data/manifiesto.yaml`: sin cambios, verificado por `git status`/`git diff` antes de abrir el PR.

Detalle completo, comando por comando: este archivo más `forense/notas/2026-08-27-los-388-commit1-escala.md` (COMMIT-1). Tabla: `data/cobertura-composicion-v1_0.tsv` (404 filas) + `.meta`. Ranking ante mesa: `FP-175`. Enmienda: `FP-171`.
