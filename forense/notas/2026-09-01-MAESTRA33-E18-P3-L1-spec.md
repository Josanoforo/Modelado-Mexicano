# Nota · ACTO MAESTRA33-E18-P3 · REGLAS-OLA6-ACTIVOS-L1 — spec congelada

> | | |
> |---|---|
> | **ARCHIVO** | `2026-09-01-MAESTRA33-E18-P3-L1-spec.md` |
> | **QUÉ ES** | Spec por regla del lote `REGLAS-OLA6-ACTIVOS-L1`, con los `PENDIENTE-VERIFICACIÓN-EN-ACTO-SUCESOR` del encargo resueltos contra microdato real. |
> | **ENCARGO** | `forense/encargos/2026-09-01-MAESTRA33-E18-P3-REGLAS-OLA6-ACTIVOS-L1.md` (archivado por A.3 en `86e893d`, PR #443 — no se re-escribe) |
> | **RÉGIMEN** | Dos commits por regla (precedente `MAESTRA33-C4`, PR #424): COMMIT-1 congela la spec, COMMIT-2 mide. |

---

## ARRANQUE (`/acto` §1) — las cinco líneas

1. **REPO.** Clon existente `/home/pc0/Modelado-Mexicano`; caja de este acto
   `/home/pc0/mm-e18-p3-ola6` (worktree nuevo, rama
   `acto/maestra33-e18-p3-reglas-ola6-activos-l1`), `git log -1` =
   `39e832d Merge pull request #443`, `git status` limpio al abrir.
2. **SHA.** El encargo declara base `b827824` (refrescado contra `ce5e10d`).
   `origin/main` real al arrancar = **`39e832d`** — main se movió
   (`ce5e10d..39e832d`, 193 archivos, +3160/−6: corridas-L de `L-TRA-M-05/07`,
   `scoreboard-v1_1-AGREGADO.md`, `tests/check.py` +20). **No es PARO**
   (`/acto` §1.2): se refrescó y este acto trabaja contra `39e832d`. Nada del
   perímetro de este lote (`data/raw/**`, `milpa/`) fue tocado por esa
   diferencia — verificado con `git diff --stat ce5e10d 39e832d`.
3. **`data/raw`.** Existía en el clon principal como symlink a
   `/home/pc0/mm-corpus/raw` (328 entradas). **La enlacé** igual en la caja:
   `ln -sfn /home/pc0/mm-corpus/raw data/raw` → 328 entradas. Este acto **no
   descarga nada** (los 8 payloads ya están en el corpus compartido y en
   `data/manifiesto.yaml`), así que el anti-PR#77 no aplica más que como
   verificación de que no escribí en `data/raw` — no lo hice.
4. **ENTORNO** (tres partes, A.2):
   - `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE` → `sin_variable` ✅ (esperado)
   - `curl -s -o /dev/null -w "%{http_code}" --max-time 15 https://www.inegi.org.mx/` → **`200`**
   - `ls data/raw/ | head -1` → `2005trim1_csv.zip`; **corpus compartido montado**, 328 entradas.
   Entorno asignado por el encargo: Ubuntu con microdato. Coincide.
5. **ESPEJO.** No se usó. Toda cifra de esta nota sale del clon de (1) con el
   comando a la vista.

**COMPUERTA** (`/acto` §2): el encargo declara `**COMPUERTA:** ninguna` — es
la declaración explícita de que no hay compuerta, no dispara verificación.
Cero rótulos pelados en la línea, así que tampoco aplica la regla de rótulo
ambiguo.

**0-bis A.3** (`/acto` §3): el encargo **ya existe en el árbol**
(`forense/encargos/2026-09-01-MAESTRA33-E18-P3-REGLAS-OLA6-ACTIVOS-L1.md`,
creado por `86e893d` / PR #443, el propio acto P2/P3 que lo redactó). Por
`/acto` §3 no se re-escribe. Se le añade `## CONSUMIDO` en la cascada y nada
más.

---

## Lo que este acto le debe al encargo

El encargo P3 dejó explícito que **no midió nada** y que los nombres de
ponderador y diseño que trae son *"los estándar declarados por el propio
instrumento […] no confirmados contra el microdato real"*, marcados
`PENDIENTE-VERIFICACIÓN-EN-ACTO-SUCESOR`, con la instrucción de que
*"el acto que ejecute este lote los confirma con `head -1`/lector real antes
de calcular nada"*. Esta nota es esa confirmación. **Tres de los supuestos
del encargo resultaron falsos**; los tres se corrigen abajo, citando el
descriptor oficial, no el juicio del ejecutor.

| # | Lo que declaró el encargo | Lo que dice el microdato | Regla |
|---|---|---|---|
| 1 | Ponderador `factor` en las 6 olas de ENIGH | `factor` solo en 2016-2022; en **2012 y 2014 la columna es `factor_hog`** | R1 |
| 2 | `V_AFORE` = "indicador de tenencia", codificación *"típicamente 1=Sí/2=No"* | `V_AFORE` es el **monto en pesos** (0 – 108 264 000). La tenencia es **`C_AFORE`**, codificada **0/1 crudo** | R2 |
| 3 | Universo ENFIH2019 = "personas 18+" | `TCONCENTRADORA.csv` es tabla de **hogar** (`FOLIO`+`VIV_SEL`+`HOGAR`, 17 765 filas, ponderador `FAC_HOG`) | R2 |

Ninguna de las tres es reproche al encargo: las tres son exactamente lo que
él mismo mandó verificar. Se registran porque el encargo pidió que se
confirmaran "antes de calcular nada", y calcular sobre `V_AFORE` habría
producido una `p` sin significado (proporción de hogares con saldo > 0, que
no es tenencia).

---

## Regla 1 · `familia.seguro.volatilidad_ausencia_estado`

### Enunciado verbatim del modelo

`canon/modelo-decision-v4_0.md` §3.5 — el `ENTONCES` nombra tres conductas
(corresidencia, pooling, **remesas**); este lote congela **remesas**, la
única con candidata `EXISTE-SATISFACE` en P2. Las otras dos quedan fuera del
lote y **no se les inventa candidata**.

### (a) Encuesta / olas / payloads

Seis olas bienales de ENIGH, las seis con `id` en `data/manifiesto.yaml` y
`sha256` que coincide con el archivo en disco (verificado, A.1):

| ola | `id` de manifiesto | archivo | `sha256` |
|---|---|---|---|
| 2012 | `enigh2012_nc_csv` (`manifiesto.yaml:327`) | `data/raw/enigh2012_nc_csv.zip` | `2ed45acf…5e44` |
| 2014 | `enigh2014_nc_csv` (`:342`) | `enigh2014_nc_csv.zip` | `fffec78d…da7e` |
| 2016 | `enigh2016_nc_csv` (`:357`) | `enigh2016_nc_csv.zip` | `95e30780…eca1` |
| 2018 | `enigh2018_nc_csv` (`:372`) | `enigh2018_nc_csv.zip` | `5026cd95…d636` |
| 2020 | `enigh2020_nc_csv` (`:387`) | `enigh2020_nc_csv.zip` | `47417cac…01d4` |
| 2022 | `enigh2022_nc_csv` (`:402`) | `enigh2022_nc_csv.zip` | `3b2b0bc9…9c06` |

Tabla interna, **distinta por serie** (confirmado con `zipfile.namelist()`):

- 2012/2014 (serie propia, `ncv`): `concentradohogar_enigh<AAAA>ncv/conjunto_de_datos/concentradohogar.csv`
- 2016-2022 (Nueva Serie, `_ns`): `conjunto_de_datos_concentradohogar_enigh<_>AAAA_ns/conjunto_de_datos/conjunto_de_datos_concentradohogar_enigh<_>AAAA_ns.csv`

### (b) Variable y dicotomización

`remesas` — ingreso corriente monetario del hogar por remesas, columna de
`concentradohogar`. Presente en las 6 olas, **cero nulos en las 6**
(verificado: `pd.to_numeric(...).isna().sum() == 0` en cada ola).

`recibe_remesas = 1` si `remesas > 0`, `0` si `remesas == 0`.

El encargo dejó abierto *"vacío/NA tratado como 0, a confirmar con el
diccionario de cada ola — puede variar 2012→2022"*. **Resuelto: la rama de
NA no existe.** Las seis olas traen `remesas` numérica completa, sin blancos;
el hogar sin remesas trae `0`, no vacío. La dicotomización queda por tanto
sin ambigüedad y **no varía** entre 2012 y 2022.

### (c) Universo

Hogares — la unidad de observación de `concentradohogar` es el hogar
(`folioviv`+`foliohog`), no la persona. Universo **completo** de cada ola,
sin filtro adicional: la regla no declara ninguno. `n` sin ponderar por ola:
9 002 · 19 479 · 70 311 · 74 647 · 89 006 · 90 102.

### (d) Ponderador — **corrige al encargo**

| ola | columna real | fuente |
|---|---|---|
| 2012 | **`factor_hog`** | `head -1` de `concentradohogar.csv` |
| 2014 | **`factor_hog`** | idem |
| 2016 · 2018 · 2020 · 2022 | `factor` | idem |

El encargo declaró `factor` para las seis. En 2012 y 2014 esa columna **no
existe**; el nombre es `factor_hog`. Un medidor que hubiera confiado en el
encargo habría reventado en las dos primeras olas (o, peor, las habría
saltado en silencio). Población expandida resultante, como control de
sanidad: 31.56M · 31.67M · 32.97M · 34.40M · 35.75M · 37.56M hogares —
monótona creciente y del orden correcto.

### (e) Diseño — **verificado presente, y usado**

`est_dis` (estrato) y `upm` (unidad primaria de muestreo) están presentes en
**las 6 olas** (el encargo lo dejó como `PENDIENTE-VERIFICACIÓN`). Conteos:

| ola | estratos | UPM |
|---|---|---|
| 2012 | 108 | 1 111 |
| 2014 | 200 | 2 626 |
| 2016 | 536 | 7 891 |
| 2018 | 543 | 8 377 |
| 2020 | 558 | 10 118 |
| 2022 | 560 | 10 211 |

En consecuencia **este acto no usa el bootstrap simple por fila** de
`tools/tasas_base_fase1.py`. Usa el **bootstrap por conglomerado
estratificado** ya precedentado en la casa —
`tools/medicion_familismo.py:274-301` (ACTO MAESTRA32-E16, `ADR-235`):
remuestrea UPMs *con reemplazo dentro de cada estrato*, recalcula la
proporción ponderada en cada réplica, IC95 por percentiles. Esto es
exactamente lo que la spec de fase 1 previó y no pudo hacer
(`2026-08-31-reglas-fase1-spec.md` §(c)4: *"si el tiempo del acto lo permite
se usa diseño real"*).

- Réplicas: 10 000. `seed = 42` (mismo seed de casa).

### (f) Escala y clase

Binaria `recibe_remesas ∈ {0,1}`; `p` = proporción ponderada de hogares con
`remesas > 0`. Escala [0,1]. `clase: "MEDIDO·p(tasa base ponderada)"` —
misma clase que las cinco de fase 1, y por la misma razón: el dato da una
**tasa base incondicional** sobre el universo de hogares, no la condicional
del disparador (`segsoc=2 ∨ residencia ∈ {EUA, Otro país} ∨ remesas`).

⚠️ **El disparador es circular en esta candidata y se declara.** El
`SI` de §3.5 incluye *"hogar con remesas P041"* entre sus disparadores, y el
desenlace que aquí se mide es *recibir remesas*. Medir `p(remesas | remesas)`
sería vacuo. Lo que se mide es la **tasa base de la conducta-remesas en el
universo completo de hogares**, con `si.disparadores: PENDIENTE-DE-MESA` —
igual que las cinco de fase 1 y por el mismo motivo declarado allí. Mesa
decide si esta tasa base sirve de `p` para la regla o si la regla necesita
un disparador no-circular (`segsoc`/`residencia`), que `concentradohogar`
**no** trae y que exigiría cruzar con el módulo de personas — fuera del
perímetro de este lote.

### (g) `ola_calibracion`

Seis olas, no una. Se reporta `p` **por ola** y se declara la ola de
calibración = **ENIGH 2022** (la más reciente), con las otras cinco como
serie de contexto en la misma entrada. Ninguna se promedia: promediar seis
olas de un panel repetido de corte transversal sin ponderación entre olas no
tiene interpretación declarada, y no se inventa una.

### (h) RESULTADO — COMMIT-2

`python3 tools/tasas_base_ola6_activos.py --regla 1`, corrida única, 1/sep/2026:

| ola | `p` | IC95 | `n` | estratos | UPM | ponderador | hogares expandidos |
|---|---|---|---|---|---|---|---|
| 2012 | **0.044704** | [0.039340, 0.050420] | 9 002 | 108 | 1 111 | `factor_hog` | 31 559 379 |
| 2014 | **0.040784** | [0.036551, 0.044978] | 19 479 | 200 | 2 626 | `factor_hog` | 31 671 002 |
| 2016 | **0.047459** | [0.045192, 0.049807] | 70 311 | 536 | 7 891 | `factor` | 32 974 661 |
| 2018 | **0.047285** | [0.045093, 0.049555] | 74 647 | 543 | 8 377 | `factor` | 34 400 515 |
| 2020 | **0.043775** | [0.041877, 0.045680] | 89 006 | 558 | 10 118 | `factor` | 35 749 659 |
| **2022** *(calibración)* | **0.045694** | **[0.043754, 0.047711]** | 90 102 | 560 | 10 211 | `factor` | 37 560 123 |

**Lectura.** La serie es **plana**: rango 0.67 pp en diez años (mín 4.08% en
2014, máx 4.75% en 2016), y los IC95 de las cuatro olas grandes (2016-2022)
se traslapan entre sí. Es la primera regla del programa con `p` medida en
**seis** olas de la misma encuesta.

Control de sanidad, declarado: la tasa **sin ponderar** de 2022 es 5.78% y la
ponderada 4.57%. La diferencia va en la dirección esperada (los hogares
receptores de remesas se concentran en estratos rurales, sobremuestreados y
por tanto con factor de expansión menor); no es un error de ponderación.

Entrada escrita en `milpa/tramite-ola5-propuesta-v0.yaml` — archivo nuevo,
**que el motor no carga**: verificado en este acto con
`grep -rn "tramite-ola" milpa/src/ tests/` → **0 líneas**, contra el control
positivo `grep -rn "tramite.yaml" milpa/src/` → **3 líneas**
(`emisor.py:18,48,87`). A.13: el negativo se declara con su control positivo.

---

## Regla 2 · `dinero.planeacion.formal_estable`

### Enunciado verbatim del modelo

`canon/modelo-decision-v4_0.md` §3.1 — `SI` empleo formal e ingreso estable
(`segsoc`=1 ∧ `contrato` ∧ `pres_8`) `ENTONCES` planeación larga: **afore**,
seguro, hipoteca. Este lote congela **solo AFORE**, por mandato explícito del
encargo (*"seguro/hipoteca quedan fuera de este lote, no se inventa candidata
para ellos"*).

### (a) Encuesta / ola / payload

ENFIH 2019 — `id` de manifiesto `enfih2019_bd_csv_zip`
(`data/manifiesto.yaml:4005`), archivo
`data/raw/enfih2019/enfih_2019_base_de_datos_csv.zip`,
`sha256 = be372533d5043920892142e8bf792b7293a5f20ab466a6441bc89925b42ef4d5`
(verificado contra el manifiesto, A.1). Tabla interna `TCONCENTRADORA.csv`,
88 columnas, 17 765 filas. Descriptor oficial:
`data/raw/enfih2019/enfih_2019_fd.xlsx`, hoja `TConcentradora`.

### (b) Variable — **corrige al encargo (dos veces)**

El encargo declaró: *"**Variable:** `V_AFORE` […] indicador de tenencia de
cuenta AFORE […] **Dicotomización:** `tiene_afore` = 1/0 directo de `V_AFORE`
(a confirmar codificación exacta — típicamente 1=Sí/2=No en INEGI, no 0/1
crudo)"*. El FD oficial dice otra cosa, en dos puntos:

| nemónico | pregunta (FD, verbatim) | códigos válidos |
|---|---|---|
| **`C_AFORE`** | *"Condición de tenencia de cuenta de ahorro para el retiro o Afore"* | `0` = No tiene · `1` = Sí tiene |
| `V_AFORE` | *"**Monto** de cuenta de ahorro para el retiro o Afore"* | `0` = sin valor específico · `1 – 108 264 000` = monto |

1. La tenencia es **`C_AFORE`**, no `V_AFORE`. `V_AFORE` son **pesos**.
2. La codificación es **0/1 crudo**, no `1=Sí/2=No`.

Comprobado también contra el dato: `C_AFORE` tiene exactamente dos valores
(`0`: 7 568 filas, `1`: 10 197); `V_AFORE` tiene 828 valores distintos y
máximo 108 264 000. Y **12 504** filas traen `V_AFORE = 0` frente a solo
**7 568** con `C_AFORE = 0`: las ~4 900 de diferencia son hogares que **sí**
tienen AFORE y no declararon saldo. Medir sobre `V_AFORE > 0` habría
producido una `p` de 29.6% sin ponderar en vez de la tenencia — un número
distinto, de otra cosa, y sin ninguna señal de error.

**Congelado:** `tiene_afore = C_AFORE` (ya es 0/1, no requiere recodificación).

### (c) Universo — **corrige al encargo**

El encargo declaró *"personas 18+ (marco estándar de la Encuesta Nacional de
Financiamiento de Hogares…), **PENDIENTE-VERIFICACIÓN** el filtro exacto por
edad/PEA"*. `TCONCENTRADORA.csv` **no es una tabla de personas**: su llave es
`FOLIO`+`VIV_SEL`+`HOGAR` y su ponderador es `FAC_HOG` (*"FACTOR DE
EXPANSIÓN"*, FD fila 214). Es **tabla de hogar**. No hay filtro de edad ni de
PEA que aplicar, porque no hay personas que filtrar.

**Congelado:** universo = **hogares**, los 17 765 de `TCONCENTRADORA`, sin
filtro adicional (la regla no declara ninguno) — mismo criterio que la
Regla 1.

*Sensibilidad declarada:* la tabla trae `H_PPAL` (hogar principal): 17 386
principales (35 836 258 expandidos) y 379 no principales (808 422, 2.2% del
total expandido). La `p` se reporta sobre el universo completo y, como
robustez, restringida a `H_PPAL=1` en (g).

### (d) Ponderador — **corrige al encargo**

El encargo declaró `FACTOR`/`FAC_PER`. Ninguna de las dos existe. La columna
real es **`FAC_HOG`** (FD fila 214, *"FACTOR DE EXPANSIÓN"*, rango 24–22 850).
Suma expandida = **36 644 680 hogares**.

### (e) Diseño — **verificado presente, y usado**

`EDIS` (estrato de diseño) y `UPM_DIS` (unidad primaria de muestreo de
diseño) están ambos en `TCONCENTRADORA` — FD filas 213-214. El encargo lo
dejó como `PENDIENTE-VERIFICACIÓN`.

> Corrección de método dentro de este mismo acto, declarada: mi primer
> barrido buscó `EST_DIS` (el nombre que usa ENIGH) y devolvió "sin estrato".
> El nombre en ENFIH es `EDIS`. El negativo era del patrón, no del dato.

- **407 estratos**, mediana de 4 UPM por estrato, mínimo 2, máximo 59.
- **Cero estratos con una sola UPM** — el bootstrap por conglomerado
  estratificado corre sin caso degenerado.

Mismo estimador que la Regla 1: remuestreo de UPMs dentro de estrato,
10 000 réplicas, `seed = 42`.

### (f) Disparador — **no alcanzable en este instrumento, con negativo A.13**

El `SI` de §3.1 pide `segsoc` ∧ `contrato` ∧ `pres_8`. Barrido exhaustivo de
**las 16 tablas / 664 columnas** del payload con el patrón
`SEG|IMSS|ISSSTE|PREST|CONTRAT|FORMAL|AFIL`:

- **2 columnas**, y las dos son `C_SEGVID`/`V_SEGVID` — *seguro de vida*, un
  activo financiero, no una variable de formalidad laboral.
- **Control positivo (A.13):** el mismo barrido con el patrón `AFORE` sobre
  las mismas 16 tablas devuelve **2 columnas**. El barrido sí examina; el
  cero de formalidad es del dato, no del comando.

**ENFIH 2019 no contiene ninguna variable de formalidad laboral.** No es un
límite de perímetro de este acto — es una ausencia del instrumento, y por
tanto un acto sucesor tampoco podrá construir la condicional aquí.
`si.disparadores: PENDIENTE-DE-MESA`, y la razón queda medida, no supuesta.

*Hallazgo lateral, para el acto sucesor y no para este lote:* el mismo
barrido encontró **`C_SEGVID`** (tenencia de seguro de vida) y `SERV_HIPO`
(servicio de deuda hipotecaria) en la misma tabla — candidatas para los otros
dos brazos del `ENTONCES` de §3.1 ("seguro, hipoteca"), que el encargo dejó
fuera de este lote. Se anotan; **no se miden aquí**.

### (g) Escala, clase y evidencia de dirección

Binaria `tiene_afore ∈ {0,1}`, `p` = proporción ponderada de hogares con
AFORE, escala [0,1], `clase: "MEDIDO·p(tasa base ponderada)"`.

### (h) RESULTADO — COMMIT-2

`python3 tools/tasas_base_ola6_activos.py --regla 2`, corrida única, 1/sep/2026:

| | `p` | IC95 | `n` | estratos | UPM |
|---|---|---|---|---|---|
| **ENFIH 2019, universo completo** *(sellado)* | **0.538502** | **[0.526700, 0.550616]** | 17 765 | 407 | 3 270 |
| robustez: `H_PPAL=1` | 0.540091 | [0.528375, 0.552262] | 17 386 | — | — |

La restricción a hogar principal mueve la cifra **0.16 pp**, muy dentro del
IC95 de ambas: la elección de universo no cambia el resultado.

**Descriptivo — NO es la `p` de la regla y NO se sella.** Tasa ponderada de
AFORE por `CAT_POS` (posición en el trabajo de la persona de referencia):

| `CAT_POS` | etiqueta (FD) | `n` | `p` |
|---|---|---|---|
| 1 | Empleado(a) u obrero(a) | 8 154 | **0.7230** |
| 3 | Patrón(a) o empleador(a) | 390 | 0.5688 |
| 4 | Trabajador(a) por su cuenta | 3 428 | 0.4161 |
| 0 | Persona no ocupada | 4 354 | 0.3831 |
| 5 | Trabajador(a) familiar sin pago | 452 | 0.3519 |
| 2 | Jornalero(a) o peón(a) | 987 | **0.2940** |

Brecha de **42.90 pp** entre empleado/obrero y jornalero/peón, **en la
dirección que §3.1 predice**. Por qué no se sella: `CAT_POS` es *posición en
la ocupación*, no *formalidad* — un empleado u obrero puede ser informal, y
usar `CAT_POS=1` como el `segsoc=1 ∧ contrato ∧ pres_8` de la regla sería
inventar exactamente la operacionalización que (f) acaba de declarar ausente
del instrumento. Se registra como evidencia de dirección para que mesa la
tenga a la vista, no como la condicional.

---

## Regla 3 · `civico.participacion.contingente`

### Enunciado verbatim del modelo — la pieza que el encargo parafraseó

`canon/modelo-decision-v4_0.md:551`, verbatim:

> **SI** el votante percibe que el acto **pesa** —resultado abierto **y**
> consecuencia palpable— **ENTONCES** participa; **SI** lo percibe **decidido
> de antemano o sin consecuencia** **ENTONCES** se abstiene — PORQUE cálculo
> del peso del acto — `[FUERTE]` **(a)**. […] **Efecto sobre el falsador:** se
> vuelve comprobable con **elecciones locales concurrentes vs. no
> concurrentes** —mismo electorado, mismo año, distinto peso percibido—, en
> vez de esperar a que la historia produzca otro caso.

Dos cosas que la paráfrasis del encargo no arrastró y que deciden el caso:

1. El disparador es una **conjunción** (`resultado abierto` **∧**
   `consecuencia palpable`), y su negación una **disyunción** (`decidido de
   antemano` **∨** `sin consecuencia`). No es un constructo simple.
2. El modelo **ya declaró cuál es su falsador**, y es un **diseño
   comparativo** — elecciones concurrentes vs. no concurrentes, mismo
   electorado, mismo año —, no un reactivo de actitud en un corte
   transversal.

### (a) Censo — `tools/censo_encup_participacion.py`, 1/sep/2026

Payload `encup_2012_base_datos_xlsx` (`data/manifiesto.yaml:3490`),
`sha256 = 50341d97a3e305e6ed3348757632e5b23ab2e87b2052304961905619d85612e4`.
Hoja `BaseDatos_ENCUP_2012_Final`: **282 columnas, 3 750 filas**.

Tres ejes, cada uno con ≥3 formulaciones declaradas antes de correr (A.4).
**A.13 — columnas examinadas por cada formulación: 282.** Control positivo
(mismo barrido, mismas 282 columnas, patrón `P\d`): **239 columnas** — el
barrido sí examina.

| eje | unión de formulaciones | veredicto |
|---|---|---|
| **D1 · resultado abierto** | **1** columna, y es **falso positivo** | **NO-ENCONTRADO** |
| **D2 · consecuencia palpable** | 12 columnas, 2 utilizables | EXISTE-SATISFACE |
| **DES · desenlace participa/abstiene** | 9 columnas, 2 utilizables | EXISTE-SATISFACE |

**D1 — el hallazgo que decide la regla.** El único hit de las tres
formulaciones es `P67_10` — *"…¿usted aceptaría o no que su hijo… **Se fuera
a vivir con su pareja**"* —, que pegó por `parej[ao]` en un patrón pensado
para "elección pareja/reñida". Es falso positivo. **Verificado además por
lectura completa de las 282 columnas, una por una:** ENCUP 2012 no pregunta
si las elecciones son limpias, si son competidas, si el resultado está
decidido de antemano, ni si los votos se cuentan o se respetan. Lo más
cercano es `P30_15` (confianza en el IFE, escala 0-10), que mide confianza en
la autoridad electoral — **no** que el resultado esté abierto.

**D2 — utilizables:** `P80` (*"Votar es la única manera que tienen las
personas como yo para decir si el gobierno hace bien o mal las cosas"*,
4 puntos + NS/NC) y `P28C` (*"A los funcionarios públicos no les preocupa lo
que piensa la gente como yo"*, eficacia externa clásica). También `P51_2`
(influencia ciudadana general), no específica del voto.

> ⚠️ **Las tres formulaciones pre-declaradas de D2 fallaron las dos mejores
> candidatas del eje.** `P80` dice *"para **decir** si el gobierno hace bien
> o mal"* — «decir», no «decidir» —, así que el regex
> `vot\w+.{0,60}(…|decid|…)` pasó de largo; `P28C` no contiene ni «voto» ni
> «influir». Ambas salieron de la **lectura completa** de las 282 columnas,
> no del patrón. Se añadió una 4.ª formulación `lectura-completa` al censo,
> etiquetada como tal, en vez de reescribir las tres primeras para que
> parecieran haber acertado. Un censo que solo hubiera reportado las tres
> declaradas habría dejado el eje D2 **más vacío de lo que está**.

**DES — utilizables:** `P77_1` (*"Del año 2000 a la fecha, ¿acudió a votar…
Presidente de la República"*: Sí 3 282 · No 466 · NC 2) y `P78` (*"…si en la
pasada elección presidencial"*, con `No voto` = 288 entre opciones que en
realidad describen **cuándo decidió** el voto, no la asistencia).

### (b) Adjudicación

El desenlace existe. La mitad `consecuencia palpable` del disparador existe.
La mitad **`resultado abierto` no existe en el instrumento** — cero reactivos
en 282 columnas, con control positivo.

Medir `p(participa | P80)` y llamarlo la regla sería sellar **media
conjunción como si fuera la conjunción**. Y aun con las dos mitades, ENCUP
2012 es **un solo corte transversal de un año**: no puede producir el
falsador que el propio modelo declara (concurrentes vs. no concurrentes,
mismo electorado, mismo año). El obstáculo no es de perímetro ni de esfuerzo:
es de **diseño de la fuente**.

**Veredicto: `EXISTE-NO-SATISFACE`** — exactamente la salida que el encargo
previó (*"el acto sucesor la completa o la regresa a `EXISTE-NO-SATISFACE` si
ninguna candidata del log resulta suficiente al leerla completa"*). Se leyeron
completas. No alcanzan. **La Regla 3 no recibe entrada en
`milpa/tramite-ola5-propuesta-v0.yaml`.**

### (c) Descriptivo — NO es la `p` de la regla y NO se sella

Solo para que mesa vea el tamaño de lo que la mitad operacionalizable
produce, antes de decidir si vale la pena buscar la otra mitad en otra
fuente. `python3 tools/censo_encup_participacion.py`:

| | `n` | reportó haber votado |
|---|---|---|
| percibe que el acto pesa (`P80` de acuerdo / muy de acuerdo) | 2 921 | **0.9115** |
| no lo percibe (en desacuerdo / muy en desacuerdo) | 709 | **0.7705** |
| **brecha** | | **+14.10 pp** |

Ponderador `factor`; `n` utilizable 3 630 de 3 750. La brecha va **en la
dirección de la regla**.

**Dos reservas sobre esta misma cifra, declaradas:**

1. **Media conjunción no es la conjunción.** `P80` es solo D2.
2. **La participación autorreportada está inflada.** 91% y 77% están muy por
   encima de la participación real de la presidencial de 2012 (≈63%): sesgo
   de sobre-reporte clásico de encuesta. Aun la brecha, que es menos sensible
   al sesgo de nivel, se apoya en un desenlace que sabemos desplazado.
3. **El diseño es proxy.** La base pública de ENCUP 2012 trae `factor` y
   `POND` pero **no** variables de diseño declaradas; lo más cercano es
   `Punto` (375 puntos de muestreo) dentro de `Estado`. Por eso arriba va la
   proporción ponderada sin IC: un IC con diseño inventado sería peor que no
   darlo.

### (d) Ruta

Al registro del curador por la vía de **A5**, junto a las 20
`EXISTE-NO-SATISFACE`/`NO-ENCONTRADO` que P2 ya mandó allí
(`2026-09-01-MAESTRA33-E18-P2-mapeo-tabla.md`, sección «Ruta»). **Este acto no
escribe en `data/curacion-registro/`:** P2 declaró esa escritura fuera de
perímetro y esa frontera se respeta. El veredicto queda registrado en tres
sitios auditables: esta nota, el bloque de no-entrada de
`milpa/tramite-ola5-propuesta-v0.yaml`, y el cierre.

**Lo que le falta a la regla, nombrado** — para que el acto sucesor no repita
el barrido: una fuente que cruce, **sobre el mismo elector**, (i) percepción
de que el resultado está abierto, (ii) percepción de consecuencia, y (iii)
participación; o, mejor y más barato, el diseño que el propio modelo declara:
**resultados electorales de comicios locales concurrentes vs. no concurrentes
del mismo año y estado**, que no requiere encuesta ninguna.

---

## Corrección de dirección llegada a mitad de acto — nombre del lote y destino de las entradas

`origin/main` se movió **dos veces más** mientras este acto corría: de
`39e832d` a `75747db` (`PR #445`, `ACTO MAESTRA33-S2 · SELLA-2-REGLAS-ACTIVOS`)
y de ahí a `84433c7` (`PR #446`, `ACTO MAESTRA33-E21 · L-EXTRAE-v1_1`). Se
detectó al correr el **control de T27** (abajo), no por casualidad. `git merge
origin/main`: sin conflicto. Trajo dos consecuencias reales, no cosméticas:

**1 · El lote no se llama «OLA6».** El encargo de `S2`, Mensaje 2, verbatim:

> «en P3 los lotes se nombran **REGLAS-ACTIVOS-L1/L2/L3 (no
> REGLAS-OLA6-ACTIVOS)**: son reglas nuevas dentro de los 4 dominios ya
> ACTIVOS, no apertura de dominio (eso lo evaluó E14 y es otra cosa).
> **Añade esa aclaración en la cabecera de cada lote.**»
> «ACLARACIÓN: "ACTIVOS" = reglas nuevas dentro de los 4 dominios ya activos
> (trámite, cívico, dinero, familia). **No es apertura de Ola 6**; E14/ADR-265
> sigue vigente.»

El archivo del encargo conserva el nombre viejo — A.3 no edita un verbatim —
pero el lote es `REGLAS-ACTIVOS-L1`. La aclaración quedó **en la cabecera del
bloque de este lote** dentro del YAML acumulador, como dirección pidió.

**2 · Las entradas no van a un archivo propio.** Este acto había creado
`milpa/tramite-ola6-propuesta-v0.yaml`. Estaba **mal por dos razones**: (i) el
nombre afirma una apertura de Ola 6 que dirección acaba de decir que no
ocurre, y (ii) el cierre de `S2` declara que al relanzarse buscará las
entradas `MEDIDO·p` en **`milpa/tramite-ola5-propuesta-v0.yaml`**
(`2026-09-01-maestra33-s2-sella-2-reglas-activos-cierre.md:47-52`, condición
(a) de relanzamiento). Un archivo aparte habría dejado a `S2` sin encontrar lo
que espera. Las dos entradas y el bloque de no-entrada se movieron al
acumulador — que es donde `MAESTRA33-C1` ya había añadido la suya — y el
archivo propio se borró. **8 reglas en el acumulador, sin `id` duplicado.**

**3 · Este PR es el «PR de ejecución» que `S2` esperaba.** `S2` corrió con
`P1` **SIN INSUMO** porque lo único fusionado era el lote *redactado*
(`PR #443`, ninguna `p` medida): *"la compuerta se cumplió por letra, no por
sustancia"*. Este acto aporta la sustancia. Queda pendiente la condición (b)
de `S2`: **firma de mesa por regla**, fuera de corchetes — que este acto no
puede dar. Por eso las dos entradas van `situacion: PENDIENTE-DE-MESA`.

**4 · El ADR se renumeró dos veces.** Candidato derivado contra `39e832d` era
`ADR-271`; `PR #445` (`S2`) lo tomó y `PR #446` (`E21`) tomó `ADR-272`
—renumerándose él mismo desde `271`—. Contra `84433c7` el máximo es **272**,
sin huecos ni duplicados, así que este acto toma **`ADR-273`**. Regla de la
casa aplicada tal cual: renumera quien fusiona segundo.
