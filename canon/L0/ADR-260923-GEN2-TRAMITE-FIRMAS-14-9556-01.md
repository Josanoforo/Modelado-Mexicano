**ADR-260923-GEN2-TRAMITE-FIRMAS-14-9556-01**, `ACTO GEN2-TRAMITE-FIRMAS-14`, 23/sep/2026, NUBE, Sonnet 5, MODO ABIERTO, COMPUERTA: ninguna. Asienta las decisiones de mesa sobre las once FP abiertas al 23/sep (tarde) — ninguna firma de mesa llegó con el lanzamiento, así que aplica el fallback textual de §2 del encargo: `forense/firmas-pendientes.tsv` marca B (`657c-04`) y C (`988c-01`) con `estado = EJECUTA:CONTADORES-CONSUMO-2`, G (`cfce-01`) con `estado = EJECUTA:SELLO-EXTERNO-2`; J (`3d56-01`) y K (`c3fa-05`) sin cambio; A (`657c-02`), D (`1f30-01`), E (`1f30-02`), F (`e422-01`), H (`4296-01`) e I (`1269-01`) quedan `ABIERTA`, declaradas en `## NO-CORRIDO / RESERVAS` con `DECISIÓN-DE-MESA-PENDIENTE`. Verificado que `657c-03` (ya cerrada) no sigue `ABIERTA` en ninguna de las siete vistas que la mencionan — no se edita nada por esa pieza. Corrige de paso una cita de logística del encargo (`657c-03` cerró con PR `#1086`, no `#1078`). `ADENDA-1` de dirección (corrige el punto I; sin firma de mesa) archivada y sellada aparte. No ejecuta ninguna de las once decisiones. `celdas_validadas` sin cambio (219). CONTADOR: cero mediciones. Suite `--rapido` VERDE, 0 FAIL.
**Enmienda 23/sep/2026 (ADENDA-2, post-cierre):** dirección reporta ventana 26-27/sep para tres acciones manuales de mesa (G, H; J sin cambio); sin cita verbatim ni adjuntos recibidos -- anotado como contexto en firmas-pendientes.tsv/no-corrido.tsv, no adoptado como firma; ningún estado FP ni NC cambia. Detalle en canon/gobernanza-v1_15.md.
