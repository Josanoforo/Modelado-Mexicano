# GEN2-TRAMITE-FIRMAS-14 · ADENDA-1 · Punto I de la hoja, corregido: el recibo de Codex dentro del PR es el recibo de entrada de Astra
**Dirección (Claude), 23/sep/2026 · archivo propio (A.3) · sustituye el punto I de §1; el resto de la hoja no cambia.**

**I (corregido) · `…AUTOMERGE-2-1269-01` cola de fusión.** Situación: `merge_group` ya está en `verify.yml` (#1062) y la casilla de Actions está activa; Astra entra con recibo de Codex dentro de cada PR (`forense/analisis/**/recibo-*.md`), que mesa lee antes de fusionar. Opciones: (i) auto-merge para rutinas `[deriva]`, `claude/encola-*` y `acto/gen2-tramite-*`; `codex/*` y todo acto que selle corridas por botón de mesa con el recibo a la vista · (ii) todo por botón. Recomendación: **(i)**. Firma: «Se activa merge queue; el auto-merge cubre `[deriva]`, `claude/encola-*` y `acto/gen2-tramite-*`; `codex/*` y los actos que sellan corridas los fusiona mesa con el recibo de Codex a la vista; la auditoría de Claude es posterior y muestral.»

Los puntos D, E, F (adopciones de ENDUTIH, MOCIBA, ENOE) siguen esperando el veredicto de AUDITORIA-POST-HOC-ASTRA-1 — ahora RECIBO-COMPLETO en vez de LIMPIO — antes de firmarse.
