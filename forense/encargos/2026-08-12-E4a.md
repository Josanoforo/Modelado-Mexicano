- **SHA de redacción:** `385884e` (merge de #173, `origin/main`, 11/ago/2026)
- **Entorno asignado:** CAJA LOCAL Ubuntu CC
- **Estado:** CONSUMIDO — PR de la rama `e4a/radio-celda-d`

---

ENCARGO E4a · CAJA LOCAL Ubuntu CC · Celda-D semilla de radio_confianza + resolución del estado de las 8 producciones
SHA de redacción: 385884e (merge de #173, origin/main, 11/ago/2026)
Entorno asignado: CAJA LOCAL Ubuntu CC. NO la nube. Razón verificada: si este acto llega a correr el motor, tools/curador_registro/integrate_production.py:173 recalcula sha256(input_path) contra hash_microdato, y input_path de las 8 filas es /home/pc0/mm-corpus/raw/enbiare2021/enbiare_2021_base_de_datos_csv.zip, ruta que solo existe en la caja local.
Estado: VIVO · Depende de: solo del sellado (ADR-67(a), en main). No espera al piloto — ADENDA A5.
Concurrencia: e4c/r5-1-d2 está viva con su commit 1 hecho. Perímetros disjuntos salvo forense/hallazgos.md — si al rebasar hay conflicto ahí, re-aplica tu entrada al final y jamás resuelvas borrando la ajena. Ningún otro acto de E4 corre en esta caja mientras este esté abierto.

PROCEDENCIA. Derivado con comando contra un clon de 385884e el 11/ago/2026.

Las 8 producciones de radio_confianza ya traen su número calculado: estimacion con proporciones y n_categoria, incertidumbre con error estándar e IC95, n=31166, suma_pesos=84449936, estado=CALCULO_REPRODUCIBLE, verificacion_supervisor=REPRODUCIDA_EN_TEMP_Y_COMPARADA_BYTE_A_BYTE. Este acto no calcula nada.

Y el mecanismo de estado no es el que parece. integrate_production.py:284-289, verbatim:

```python
if result["estado"] != "CALCULO_REPRODUCIBLE":   model_state = "NO_LISTA_CALCULO_NO_DETERMINADO"
elif link["requiere_decision"] == "NO":          model_state = "LISTA_PARA_USO_MODELO"
else:                                             model_state = "NO_LISTA_DECISION_HUMANA_PENDIENTE"
```

model_state no consulta decisiones-humanas.tsv. Depende de supervisor_link.requiere_decision en la especificación maestra, que para ESP-OPACA-C-9ecb5c61 está en "SI" (verificado; comparar con ESP-OPACA-A-7baf278d, que está en "NO" y por eso sus dos filas de familismo_apoyo sí están LISTA_PARA_USO_MODELO). Re-correr el motor sin tocar ese campo reproduce el mismo estado. No lo descubras corriendo.

Y reserva viene de result["reserva"] — lo genera produce.py::taylor_distribution a partir del cálculo. No es un campo libre donde llevar la marca de PROXY_PARCIAL.

════════ ARRANQUE — hazlo antes de leer el resto del encargo ════════

Reporta las cinco líneas de abajo y NO empieces hasta tenerlas. Si algo no cuadra, PARA y repórtalo: encontrar que el terreno no es el que el encargo supone es entregable, no interrupción.

1 · REPO. Localiza el CLON EXISTENTE. No clones uno nuevo salvo que no haya ninguno, y si clonas, dilo. Reporta: ruta absoluta · git log -1 --format="%h %s" · git status ⚠️ No arranques desde el home. Si el cliente avisa "launched in your home directory", cámbiate al clon antes de nada.

2 · SHA. Confirma contra qué base trabajas y compáralo con el que el encargo declara. Si main se movió: NO es PARO — refresca, re-deriva lo que dependa del perímetro, y reporta la diferencia antes de editar. (Main se ha movido cuatro veces en las últimas dos horas: cuenta con ello.)

3 · data/raw. AUSENTE NO ES PARO. Es raíz integrada, gitignorada, resuelta por código; un clon fresco siempre nace sin ella. Se crea o se enlaza. Reporta: existe / la enlacé a <ruta> / la creé. ⚠️ Este acto solo necesita el microdato si termina corriendo el motor (rama A del PASO 3). Si el ZIP de ENBIARE 2021 no está donde las 8 filas lo declaran, eso es PARO en esa rama y es lo que hay que reportar — no cambies input_path para que cuadre.

4 · ENTORNO. CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE → esperado sin_variable · curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://www.inegi.org.mx/ → esperado 200 · ls data/raw no vacío. Reporta los tres valores crudos. NUNCA curl -I. Si la sonda da 403 con x-deny-reason: host_not_allowed, estás en la nube: PARA.

5 · ESPEJO. Prohibido derivar cifras del espejo del proyecto. Toda cifra sale del clon de (1), con el comando a la vista.

════════════════════════════════════════════════════════════════════

PERÍMETRO. SOLO escribe: data/curacion-registro/celdas-d/G5.radio_confianza.encuci_vs_enbiare.yaml (nuevo) · forense/notas/2026-08-12-e4a-radio-celda-d.md (nuevo) · forense/hallazgos.md (append) · forense/encargos/2026-08-12-E4a.md (copia literal de este encargo) · y solo bajo la rama A del PASO 3: data/curacion-registro/especificaciones-produccion.json y data/curacion-registro/produccion-modelo.tsv (este último por el motor, jamás a mano). NO toca canon/, milpa/, tools/, tests/, baseline.json, decisiones-humanas.tsv, cola-residual.tsv, la celda-D de familismo_obligacion, ni las producciones de familismo_*. Si te encuentras escribiendo fuera de esta lista, PARA.

PASO 1 · Verificación de premisas
```bash
git log -1 --format="%h %s"                                                        # esperado 385884e o posterior
grep -c "PROXY_PARCIAL" canon/gobernanza-v1_15.md                                  # ADR-67(a); reporta el valor
grep -c "NO_LISTA_DECISION_HUMANA_PENDIENTE" data/curacion-registro/produccion-modelo.tsv   # esperado 8
awk -F'\t' 'NR>1 && $10=="RESUELTA"' data/curacion-registro/decisiones-humanas.tsv | wc -l  # esperado 2
ls data/curacion-registro/celdas-d/                                                # esperado: solo G5.familismo_obligacion.actitud.yaml
python3 -c "import json;d=json.load(open('data/curacion-registro/especificaciones-produccion.json'));print(json.dumps(d,ensure_ascii=False)[:0] or '')" # carga sin error
grep -n "requiere_decision" data/curacion-registro/especificaciones-produccion.json # reporta los tres valores crudos
sed -n '284,290p' tools/curador_registro/integrate_production.py                    # confirma el bloque citado arriba
```

Si sed no muestra el bloque if/elif/else que este encargo cita, PARA: el motor cambió y todo el diagnóstico de abajo hay que rehacerlo.

PASO 2 · La celda-D de radio — esto se hace pase lo que pase

Es registro, no ejecución, y ADR-68(f) ya la autoriza. No depende del PASO 3.

Escribe data/curacion-registro/celdas-d/G5.radio_confianza.encuci_vs_enbiare.yaml conforme al contrato de propuesta-motor-adaptativo-celda-v0_3.md §3, y conforme a §4 donde §3 y el resumen de ADR-68(f) difieran — §4 manda, (f) es listado. Usa como plantilla la celda que ya existe (G5.familismo_obligacion.actitud.yaml, PR #173): mismo esquema, mismo nivel de detalle, misma disciplina de comentarios.

clave: estimando × poblacion_objetivo. La fuente nunca dentro de la clave.
tipo_adjudicacion: COMPARACION
candidato rol: BASELINE → ENCUCI, ítems AP5_1_1/2/3, resultado vigente — MEDIDO·PARCIAL(x), modelo-decision-v4_0.md:253
candidato rol: CHALLENGER → ENBIARE 2021, ítems PB1_01, PB1_02, PF1_1..6, clase PROXY_PARCIAL por ADR-67(a), registrado sin adjudicar
champion_actual: BASELINE (ENCUCI) — explícito
criterio_adjudicacion: acto de vinculación con prueba de invarianza sobre celdas compartidas, con lógica de ítems ancla, declarado antes de cualquier resultado del challenger. Su escala aún no existe: escribe "por declarar en el acto de vinculación" y dilo así; no inventes un umbral.
universo_instrumento y escala por candidato — ENCUCI y ENBIARE tienen poblaciones y escalas distintas y ese es justamente el punto de la celda. Las 8 producciones de ENBIARE traen su universo derivable del TSV (poblacion, dominio, unidad, edicion, periodo_levantamiento); cítalo de ahí, no de memoria.
universo_candidatos: qué se barrió, con qué mecanismo, en qué fecha (A.4 — una clasificación sin universo es una afirmación sobre el buscador). Declara que ISSP/GPS/LAPOP siguen en cola como operacionalizaciones futuras, tal como ADR-67(a) advierte.
commit_declaracion: el SHA del commit 1 de este acto.
Y una línea, explícita: ningún estado de esta celda mueve contadores de canon (ADR-68(a)).
PASO 3 · El estado de las 8 producciones — decisión de mesa, dos ramas

El problema, planteado sin rodeo. ADR-67(a) dice que las 8 "se desbloquean como segunda operacionalización, marcada — Θ-candidatas de proxy parcial", que NO sustituyen la condicional vigente de ENCUCI, y que el uso paramétrico sustitutivo queda condicionado a un acto de vinculación con prueba de invarianza. El vocabulario de estado_uso_modelo tiene tres valores y ninguno expresa "disponible como proxy parcial marcado, no sustitutiva": LISTA_PARA_USO_MODELO, NO_LISTA_DECISION_HUMANA_PENDIENTE, NO_LISTA_CALCULO_NO_DETERMINADO.

Rama B — la que mesa recomienda por defecto. No se toca nada. La decisión humana que requiere_decision: SI espera no es la de ADR-67(a): es la del acto de vinculación-invarianza, que no ha corrido. Bajo esa lectura el estado actual es correcto, no obsoleto, y lo que falta no es un cambio de campo sino la línea que lo explique. En esta rama: no toques especificaciones-produccion.json, no corras el motor, y escribe en la nota y en hallazgos por qué el estado no se mueve, con la cita de ADR-67(a) y el bloque de integrate_production.py. La aparente contradicción entre decisiones-humanas.tsv (RESUELTA) y las 8 filas queda explicada y cerrada, que es lo que un lector futuro necesita.

Rama A — solo si mesa la firma por escrito en el hilo. Cambia requiere_decision de "SI" a "NO" en el supervisor_link de ESP-OPACA-C-9ecb5c61, re-corre integrate_production.py por la vía del motor (nunca editando el TSV a mano), y verifica que exactamente las 8 filas esperadas cambian y nada más — pega el diff crudo en la nota. En esta rama la marca de PROXY_PARCIAL vive solo en la celda-D del PASO 2 y en el ADR, y eso hay que declararlo como riesgo asumido: quien lea el TSV sin leer la celda-D verá LISTA_PARA_USO_MODELO y puede usarlas como sustitutas de ENCUCI, que es precisamente lo que ADR-67(a) prohíbe.

Sin firma explícita de mesa en el hilo, corre la rama B. No interpretes silencio como autorización, y no ejecutes la rama A porque parezca "lo que el encargo quería".

PASO 4 · El hallazgo que este acto entrega pase lo que pase

forense/hallazgos.md, una entrada. Lo sustantivo, sin narrar el proceso:

Qué celda-D se abrió, con qué commit_declaracion.
Que estado_uso_modelo no puede expresar la decisión que mesa tomó. integrate_production.py:284-289 decide sobre supervisor_link.requiere_decision, un binario, y ADR-67(a) autorizó un estado intermedio —desbloqueada como proxy parcial marcado, no sustitutiva— que el vocabulario del registro no tiene. Es un hallazgo de mecanismo, no de dato, y es el que explica por qué decisiones-humanas.tsv y produccion-modelo.tsv parecen contradecirse sin contradecirse: uno registra la decisión, el otro un estado derivado de un campo distinto.
Qué rama del PASO 3 se ejecutó y con qué autorización.
Y la línea de contadores como manda la casa.
PASO 5 · Archivar el encargo

Copia este archivo completo y literal a forense/encargos/2026-08-12-E4a.md, con la cabecera de forense/encargos/convencion.md (SHA · entorno · estado VIVO).

PASO 6 · Suite, git, PR — NO FUSIONAR

python3 tests/check.py --baseline. Línea base congelada; entradas nuevas se reportan en el PR, jamás se silencian — una celda-D nueva puede levantar WARN de nomenclatura, y si aparece va con su texto crudo. Ojo también: data/curacion-registro/baseline.json arrastra desajustes conocidos desde #169 y está fuera de tu perímetro; si tu cambio añade uno nuevo, dilo en el PR y no lo toques.

Rama e4a/radio-celda-d. Commits: (1) la celda-D y la nota, (2) hallazgos, encargo archivado, y —solo en rama A— el cambio de spec y el TSV re-derivado. PR: "E4a: celda-D semilla de radio_confianza + resolución del estado de las 8 producciones — NO FUSIONAR sin mesa".

PASO 7 · Cierre — siete líneas

Qué cambió · por qué importa · qué habilita (la celda de radio queda registrada; su adjudicación espera al acto de vinculación-invarianza, que es acto propio futuro) · qué falta · pruebas (suite cruda y, en rama A, el diff del TSV y el hash del microdato) · reservas (el vocabulario que no alcanza; la rama que se ejecutó y por qué) · y di explícitamente qué contadores no se movieron: 13 de 27, 15 coeficientes cero medidos, 9 de 14, 4 de 144 y llaves ejercidas siguen donde estaban.

## INDETERMINADO

Derivado por auditoría 2026-09-03 (ACTO MAESTRA37-N9 · AUDITA-ENCARGOS-166). `grep -Fc -- "2026-08-12-E4a.md" canon/gobernanza-v1_15.md` → 0 (sin cita en ningún ADR). Rastro fuera de gobernanza, sin nota de cierre propia: canon/registro-rotulos.tsv, tests/check.py. Insuficiente para CONSUMIDO, insuficiente para NO-EJECUTADO — rótulo/evidencia parcial, se lista para mesa.
