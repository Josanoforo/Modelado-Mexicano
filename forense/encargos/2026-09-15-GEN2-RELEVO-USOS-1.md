# ACTO GEN2-RELEVO-USOS-1 · CABLEAR LA ÚLTIMA MILLA

Encargo de mesa, 15/sep/2026, NUBE (Opus, integral; corre en paralelo con
FIRMAS-MESA-1). Archivado verbatim (0-bis A.3) por el propio acto.

---

NUBE — ACTO GEN2-RELEVO-USOS-1 · CABLEAR LA ÚLTIMA MILLA (Opus, integral; corre en paralelo con FIRMAS-MESA-1)

* P1 · Derivar, no contar a mano: con `corrida0.py demanda`, para cada uno de los 207 slots: ¿su corrida natural ya tiene CALC sellada? (el cruce CORR↔CALC vive en `demanda-corridas.tsv`; mi sonda de hoy no lo resolvió por nombre y por eso lo manda a derivar, no a heredar). Entregable: la tabla slot → RESULT GEN2 candidato, o SIN-CANDIDATO con razón.
* P2 · Delta por script: `corrida0 delta` legacy vs GEN2 para cada par, con hashes y contrato explícito (el patrón de NC-0048). Nunca a mano.
* P3 · Materialidad por consumidor (E.4: `reglas_impacto`, no por spec): cada relevo clasificado MATERIAL (cambia signo, tier, clasificación o coeficiente central) o NO-MATERIAL. Escribe las filas de uso en el registro derivado para los relevables, vía escritor canónico.
* P4 · Adopción en los consumidores — GATED al merge de FIRMAS-MESA-1, porque ese acto está escribiendo `milpa/tramite.yaml` y la propuesta ahora mismo; escribir citas ahí en paralelo es pelearse por el mismo archivo. Al abrirse la compuerta: los NO-MATERIALES entran en bloque al PR; los MATERIALES se sirven a mesa como lista, uno por uno, con su delta.
* Perímetro: `usos.tsv`/registro derivado vía escritor, `demanda-*`, nota; `milpa/` solo en P4 tras la compuerta. Concurrencia declarada con FIRMAS-MESA-1 (decisiones, milpa, propuesta) y el servicio adq (cola, manifiesto). Contador: es el único acto de la semana que puede mover `N_resultados_pendientes` y `dependencias_numericas_legacy_activas` (191), que no se han movido desde que abrió GEN2.
