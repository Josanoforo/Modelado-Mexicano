# Nota forense · ENCARGO CRUCE-1 (parte 1) · 2026-08-09

**Cuántos contadores movió este acto: 0.** No abre microdato, no adjudica, no toca manifiesto ni mapa. Es re-derivación de denominadores + declaración de universo de atributos + alineación de cuatro referencias.

**Alcance de esta entrega — CRUCE-1 parte 1, no el encargo completo.** La compuerta de dependencias (ARRANQUE 6) falló: 2 de 3 entregas duras (BARRIDO-1, ABRIR-4) no están disponibles como artefacto usable. Mesa autorizó explícitamente una desviación acotada y escrita: la compuerta se respeta para TAREA B (el registro) y TAREA D (la cola de re-examen) — ninguna de las dos se ejecuta aquí. Se ejecutan §1, TAREA A y TAREA C, que no dependen del universo de fuentes y no quedan invalidadas cuando BARRIDO-1/ABRIR-4 aterricen. CRUCE-1 parte 2 (TAREA B + TAREA D) se relanza como acto separado cuando ambas existan.

Encargo archivado en `forense/encargos/2026-08-09-cruce-1.md` (commit `92e32ca`, junto con los dos documentos de mesa del PASO 0-bis, Estado `VIVO` al arrancar). Ejecutado en worktree `/home/pc0/wt-cruce1-1786051624`, rama `cruce1-1786051624`.

## ARRANQUE (crudo)

```
$ git -C /home/pc0/Modelado-Mexicano status
On branch sesion/cal-conf-faseb-pos4-envipe-paso1
Untracked files: data/raw, data/secretos.local.yaml
$ git -C /home/pc0/Modelado-Mexicano log -1 --format="%h %s"
302ac5a Merge origin/main into sesion/cal-conf-faseb-pos4-envipe-paso1
```
Clon existente confirmado en `/home/pc0/Modelado-Mexicano`, no se clonó uno nuevo. Base no confiable (rama de sesión ajena) — todo lo posterior sale de `origin/main` refrescado, nunca de este checkout.

```
$ git fetch --all --prune
$ git log -1 --format="%h %s" origin/main
3d43c8b Merge pull request #154 from Josanoforo/explora2-1786042858
```
**ARRANQUE 2 · SHA:** declarado `9fd49db`; real `3d43c8b` — 10 commits por delante (incluye el merge de PR #154 ENCARGO EXPLORA-2, el fix T03 de `9f94103`, y el addendum Banxico `12c5b84`). No es paro (§ARRANQUE.2 lo anticipa explícitamente).

Worktree creado sobre `origin/main` real: `git worktree add -b cruce1-1786051624 ../wt-cruce1-1786051624 origin/main` (con dos `error: could not write config file .git/config: Device or resource busy` transitorios — mismo síntoma ya documentado por EXPLORA-2/BARRIDO-1, causado por creación concurrente de otro worktree en la misma máquina; verificado íntegro con `git worktree list` + `git status` limpio).

**ARRANQUE 3 · data/raw:**
```
$ ls data/raw/
ls: cannot access 'data/raw/': No such file or directory
```
Ausente. No es paro para este acto de escritorio — no se enlaza.

**ARRANQUE 4 · entorno (dos crudos; la sonda de red no aplica per el propio encargo):**
```
$ echo "${CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE:-sin_variable}"
sin_variable
$ ls data/raw/ 2>/dev/null | head -1
(vacío/ausente)
```

**ARRANQUE 5 · espejo:** ninguna cifra de este acto sale del espejo del proyecto. Todo lo citado abajo sale de `git show origin/main:<ruta>` / `git show <rama>:<ruta>` contra el clon de (1), con comando a la vista.

**ARRANQUE 6 · DEPENDENCIAS — compuerta evaluada con evidencia, no de memoria:**

| Entrega | Estado real | Evidencia |
|---|---|---|
| **ÍNDICE-2** | Entregado. `data/indice-canastas-2026-08-08.tsv` (17,163 URLs) + `tests/indice.py`, **PR #156** (abierto, no fusionado), rama `sesion/indice2-1786050152`, commit `685d4cf` (+ posteriores hasta `5c95dd4`). | `gh pr list` refrescado; `git log --all --diff-filter=A` sobre el nombre del archivo |
| **BARRIDO-1** | No entregado como artefacto usable. Solo PASO 0 quedó commiteado (`484ced5`, rama `barrido1-1786050583`, sin PR). Existe una nota forense sustancial y no trivial (Tarea A completa, N1-N33) en `forense/notas/2026-08-08-barrido1.md` dentro de ese worktree — **nunca commiteada**. La Tarea B (el censo de puertas en sí, `data/universo-puertas-2026-08-08.tsv`) **nunca empezó** — la nota corta exactamente al abrir el encabezado de §2. Cero puertas censadas. | `git log barrido1-1786050583 --oneline` (tope = solo PASO 0); `git -C /home/pc0/wt-barrido1-1786050583 status --short` (un solo archivo untracked) |
| **ABRIR-4** | No entregado en absoluto. El worktree `/home/pc0/wt-abrir4-1786051186` existe pero es idéntico a `origin/main` (`3d43c8b`) — ni el PASO 0 se hizo. El candidato más cercano por contenido es **PR #153 (VERIF-3)**, que literalmente "abre" 4 archivos (ACLED_HDX/PUB/CONTRALORIA_SOCIAL_SICS/ECCO) y ya trae columna `clasificacion_a4` — pero ningún texto (ni el encargo de VERIF-3 ni su nota) lo vincula a "ABRIR-4" por nombre. **Hipótesis por contenido, no hecho verificado** — no se usa como sustituto en esta entrega. | `git -C /home/pc0/wt-abrir4-1786051186 log --oneline -1` = `3d43c8b`; `git -C /home/pc0/wt-abrir4-1786051186 status --short` (solo `data/raw` untracked) |

2 de 3 faltan como entregable usable → compuerta fallida per la regla literal del encargo ("Si falta más de una, PARA y repórtalo"). Mesa autorizó desviación acotada (ver arriba) en vez de parar el acto entero.

---

## PASO 0-bis · nota de fuente sobre los dos documentos de mesa

Provistos por el usuario tras confirmarse que no estaban en el árbol (no venían pegados con el encargo, como PASO 0-bis asume). `forense/AUDITORIA-PREMISAS-SELLADOS-2026-08-07-v1_0.md` se commiteó verbatim, sin discrepancia de nombre. `forense/REVERIFICACION-DEMANDA-vs-UNIVERSO-2026-08-07-v1_0.md`: el archivo real en Downloads se llama `REVERIFICACION-COMPLETA-DEMANDA-vs-UNIVERSO-2026-08-07-v1_0 (3).md` (con "COMPLETA" y un sufijo de duplicado de Windows) — commiteado al path exacto que pide PASO 0-bis, contenido sin alterar. Declarado aquí, no silenciado.

---

## §1 · Denominadores, re-derivados

| capa | valor citado por el encargo | valor re-derivado hoy | comando |
|---|---|---|---|
| índice Descarga Masiva | 7,930 URLs · 135 programas limpios | **7,930 URLs, exacto** · 136 valores de `programa` distintos (135 programas reales + 1 duplicado de caja `Cnije`/`cnije`, 20+346 URLs — confirmado con `tolower()`) | `tail -n +2 data/indice-descarga-masiva-2026-08-05.tsv \| wc -l` |
| `en_manifiesto` en esa canasta | 69 sí · 7,861 no | **69 sí · 7,861 no, exacto** | `awk -F'\t' '{print $7}' ... \| sort \| uniq -c` |
| canastas U2 | ~16,270 URLs (estimado) | **17,163 URLs, real** — ÍNDICE-2 (PR #156) ya corrió; ver nota de cambio abajo | `git show sesion/indice2-1786050152:data/indice-canastas-2026-08-08.tsv \| tail -n +2 \| wc -l` |
| fuentes únicas del catálogo | 128 | **128, exacto** | `python3 tests/catalogo.py` |
| operables | 43 · 27 en disco · 16 sin bajar | **43 exacto · 28 en disco · 15 sin bajar** — un payload cruzó de "sin bajar" a "en disco" desde que se escribió el encargo | `python3 tests/dedup.py` |
| payloads en manifiesto | 492 (514 con EXPLORA-2) | **514, exacto** (EXPLORA-2/PR #154 ya está fusionado) | `grep -c '^- id:' data/manifiesto.yaml` |
| fuentes del mapa | 134 · Σn_urls_portal = 3,754 | **134 filas · Σ=3,754, exacto** | `wc -l data/mapa-fuentes-2026-08-06.tsv`; `awk -F'\t' '{s+=$8} END{print s}'` |

**La aritmética de los 7,930, re-derivada con join real (no asumida):** usando los 34 `slug_portal` reales de `data/inventarios/alias-fuentes.yaml` (35 líneas que matchean `slug_portal:` — 1 es el comentario de cabecera, 34 reales) contra `data/indice-descarga-masiva-2026-08-05.tsv$programa` (case-insensitive):

```
covered_URLs (34 programas con fuente)      = 3,754
uncovered_URLs (102 valores de programa, = 101 programas reales + el duplicado Cnije/cnije) = 3,756
blank_URLs (programa vacío)                 = 420
TOTAL                                       = 7,930  ✔ cierra exacto
```

El "101 programas sin slug" del encargo es correcto una vez se colapsa el duplicado de caja; el join crudo sin colapsar da 102 valores de string. El mapa es internamente consistente; el hueco de 4,176 URLs jamás atribuidas (7,930−3,754) se reconfirma.

**⚠️ Precisión de `en_manifiesto`, verificada, no solo citada:** 69/7,861 es un *join por URL* contra la canasta del portal — no mide cobertura de disco. Hay 514 payloads íntegros en `data/manifiesto.yaml`; ambas cifras son ciertas y no colapsables (mismo caso ENIGH que cita el encargo: 960 URLs en esta canasta, 0 en "sí", pero R5.1 corrió sobre microdato ENIGH real llegado por otra vía — ver `forense/AUDITORIA-PREMISAS-SELLADOS-2026-08-07-v1_0.md` §3, fila `R5.1`).

**Lo que cambió desde que se escribió el encargo (el objetivo real de "no heredes, re-derívalos"):**
1. **Canastas U2: 17,163 reales, no ~16,270.** La cifra ~16,270 del encargo viene de `REVERIFICACION-DEMANDA-vs-UNIVERSO-2026-08-07-v1_0.md` §0 — escrita **antes** de que ÍNDICE-2 corriera ("Canastas U2 parseadas: 0 — DESC-2 nunca corrió"), como estimación. ÍNDICE-2 (PR #156, 6/ago) ya las parseó de verdad: denue 970 · indicadores 557 · tabulados 15,563 · sala_de_prensa 70 · inv 3 = 17,163, cero solapamiento con las 7,930 (confirmado por la propia nota de ÍNDICE-2).
2. **Universo real de puertas: 25,093, no ~24,200.** 7,930 + 17,163 = 25,093 — casi 900 URLs por encima de la aproximación que trae el propio encargo en su preámbulo ("el universo real de puertas ronda las 24,200 URLs"), que a su vez hereda la estimación pre-ÍNDICE-2 de REVERIFICACION. Esto es exactamente el tipo de cambio que §1 pide detectar, no heredar.
3. **Operables sin bajar: 15, no 16.** Ya señalado por la propia nota de ÍNDICE-2 (514/99/15 vs. las cifras citadas 492/82/16) — confirmado de nuevo aquí, de forma independiente.
4. **MANIFEST_SIN_MATCH: 99, no 82** (mismo origen: ÍNDICE-2 ya lo había re-derivado; reconfirmado aquí).

---

## TAREA A · Universo de atributos

**Los tres conteos, verificados directo contra archivo** (BARRIDO-1 ya había llegado a resultados equivalentes en su nota no commiteada — usada como segunda receta de verificación, no como fuente heredada):

**A. `forense/censo-estimabilidad-coeficientes-v1_0.md` §5 — 15 coeficientes θ, confirmado fila por fila.** Su propia cabecera (línea 7) declara el universo exacto: *"Para cada uno de los 15 coeficientes de generador (`milpa/procedencia.yaml:612-639`)"* — es decir, este censo se autodeclara como **vista 1:1** sobre un bloque específico de `procedencia.yaml` (`coeficientes_generador`, líneas 612-639), no como universo independiente. Reparto de rutas contado directo de la tabla (líneas 65-79): RUTA-A = filas 1,2,7 (3) · RUTA-C = filas 8,9 (2) · RUTA-I = fila 5 (1) · SIN-RUTA = filas 3,4,6,10,11,12,13,14,15 (9). **9 SIN-RUTA · 3 RUTA-A · 2 RUTA-C · 1 RUTA-I confirmado, exacto.** Auto-declaración de alcance en línea 21: *"No rige nada hasta que una mesa la selle con ADR; hoy es etiqueta de censo, no compuerta del motor"* — confirma lo que `AUDITORIA-PREMISAS-SELLADOS-2026-08-07-v1_0.md` §4 ya caracterizó: *"No es un sello"*.

**B. `milpa/tramite.yaml` — 10 valores `clase: ASIGNADO`, no 11.** `grep -n 'clase: ASIGNADO' milpa/tramite.yaml` da **10** líneas exactas (45,46,57,58,70,71,84,85,111,112 — 5 reglas × 2 valores cada una), no las 11 que cita el encargo. El propio archivo lo confirma en su changelog v0.3.0 (líneas 8-18, 4/ago/2026): *"Las 10 probabilidades de este archivo (5 reglas × 2)... son ASIGNADAS por mesa, sin calibración"* — la cifra 10 está declarada por el propio archivo, con receta ("conteo derivado con comando, ver mensaje del commit"). La cifra 11 del encargo es la que no se sostiene; corrección: **10**, no 11 — y BARRIDO-1 (nota no commiteada) ya lo tenía bien.

**C. `milpa/procedencia.yaml` — 16 bloques `clase: "..."`, confirmado exacto, con el desglose completo por línea.** `grep -n 'clase: "' milpa/procedencia.yaml` da 16 líneas: 9 `MEDIDO·PARCIAL(...)` (135,146,156,180,192,203,232,252,317) · 5 `MEDIDO·β̂(...)` (661,710,742,770,801) · 1 `PENDIENTE` (289) · 1 `GATE·ID-X` (836). **9 MEDIDO·PARCIAL · 5 MEDIDO· · 1 PENDIENTE · 1 GATE·ID-X, exacto.**

**El gotcha reproducido, con mecanismo probado, no solo el número:** `grep -c 'MEDIDO·PARCIAL' milpa/procedencia.yaml` (sin anclar a `clase: "`) da **23**, no 9 — confirmado. Las 14 líneas de más no son entradas reales: son comentarios de leyenda/cabecera (línea 26: *"MEDIDO·PARCIAL(x) — sexta clase, sellada 3/ago/2026..."*; líneas 122,125: cabeceras de sección) y anotaciones/prosa que repiten el término al lado de una entrada real ya contada (p. ej. línea 186: *"MEDIDO·PARCIAL(x), por institución: 4 instituciones separadas..."*, comentario bajo la entrada de línea 180; línea 804: el campo `fuente:` de otra entrada cruzando por prosa hacia `condicionales_confianza_institucional.justicia-policía`). La receta correcta ancla a `clase: "`; la receta suelta cuenta leyenda.

**Respuesta a la pregunta central — ¿tres vistas o tres universos?, con archivo y línea:**
No son ni completamente lo mismo ni completamente distintos; son dos universos, no tres, y la frontera es precisa:
- **Censo-estimabilidad y los 15 θ de `procedencia.yaml:612-639` son el MISMO universo** — 1:1, autodeclarado (arriba, censo línea 7).
- **Los 16 bloques `clase:` de `procedencia.yaml` son una capa evidencial PARCIAL del mismo universo, no un tercero.** Varias entradas se cruzan explícitamente contra filas del censo por número: `procedencia.yaml:771` cita *"RUTA-C en el censo de estimabilidad (fila 8, forense/censo-estimabilidad-coeficientes-v1_0.md:72)"*; `:802` cita fila 9; `:837` cita fila 5. Pero el mapeo no es 1:1: 16 condicionales contra 15 θ, y la mayoría de las 9 filas SIN-RUTA del censo no tienen ninguna entrada `clase:` correspondiente (no hay nada que medir todavía). Es sub-conjunto evidencial, no censo paralelo.
- **`milpa/tramite.yaml` es un universo genuinamente distinto — sin relación de calibración declarada en ningún archivo.** `grep` cruzado en ambos sentidos: `tramite.yaml` no menciona `procedencia.yaml` ni `censo-estimabilidad` ni una sola vez (`grep -n 'procedencia\.yaml\|censo-estimabilidad' milpa/tramite.yaml` → 0 líneas). `procedencia.yaml` y el censo sí usan nombres de regla `tramite.*` — pero solo como **etiqueta de desenlace candidato** para algunos de los 15 θ (censo fila 1: *"`tramite.mordida.discrecional` — ENCIG 2023..."* como desenlace de `G1·confianza_institucional`), nunca como fuente de calibración. El propio `tramite.yaml` (líneas 8-18) declara sus 10 probabilidades como *"ASIGNADAS por mesa, sin calibración"* — es decir, el archivo mismo niega que exista una vía documentada de generador→probabilidad. **Ningún archivo del repo declara el mecanismo θ→p (cómo un coeficiente de generador se compilaría en una probabilidad de `tramite.yaml`).** Esa ausencia es el hallazgo, tal como anticipa el propio encargo, y se escribe así: no hay relación documentada, solo naming compartido en un puñado de filas del censo.

**El hueco de `milpa-plan-v0_1.md`, confirmado y más amplio de lo que el encargo nombraba.** `ls milpa/` da exactamente: `procedencia.yaml`, `refutations.yaml`, `tramite.yaml` (más 3 `.md` de plan/spec/whitepaper) — confirmado. `milpa/milpa-plan-v0_1.md:32` nombra `world/generators.yaml` para "Modelo §2 (generadores G1–G6)" y `:40` nombra `rules/comunicacion.yaml` para "Meta-auditoría de comunicación" — ninguno de los dos existe (`find` confirma que no hay directorios `world/` ni `rules/` en ningún lugar del repo). Pero la tabla completa de arquitectura (`milpa-plan-v0_1.md:28-42`, "Artefacto existente → Alimenta → Forma concreta") nombra **más artefactos ejecutables que los dos que el encargo señaló**: `rules/`, `pop/profiles.yaml`, `world/generators.yaml`, `rules/<dominio>.yaml` (uno por cada uno de "10 dominios" del motor SI-ENTONCES, línea 33), `engine/triggers.py`, `world/scope_mask.geojson`, `epistemics/confidence.yaml` — de ninguno de estos existe el directorio contenedor. La propia cabecera de `milpa/tramite.yaml:1` dice `# rules/tramite.yaml` — el archivo se concibió para vivir bajo `rules/` (consistente con la fila 33 del plan, un archivo por dominio) y hoy vive en `milpa/` en su lugar: **1 de los ~10 `rules/<dominio>.yaml` diseñados existe, bajo una ruta distinta a la diseñada.** Confirmado: el motor está diseñado para múltiples yaml de dominio repartidos en varios namespaces (`rules/`, `pop/`, `world/`, `engine/`, `epistemics/`) de los que hoy solo un fragmento (`milpa/tramite.yaml`, `milpa/procedencia.yaml`) existe, bajo un único namespace no diseñado (`milpa/`). El universo de parámetros declarado en los tres conteos de arriba es, por esta vía, más incompleto de lo que las cuentas por sí solas sugieren — tal como el propio encargo advertía que podía ser el caso.

---

## TAREA C · Alineación de referencias (las cuatro verificadas)

**1. `canon/cola.yaml` — no es defecto: renombrada/congelada por ADR-48, declarada como tal en el propio código de suite.** Historia completa: creada el 30/jul/2026 (`git log --all --diff-filter=A`) y borrada el mismo día (`--diff-filter=D`) — ADR-48 (R0, `canon/gobernanza-v1_15.md:409-430,915`) la sacó del canon y la congeló como `forense/hallazgos-congelados-2026-07-30.yaml` (40 entradas, sin añadir ni borrar). `gobernanza-v1_15.md:422` declara explícitamente por qué las citas vivas no se reapuntaron: *"las citas a `canon/cola.yaml` que quedan vivas en bitácora, pre-registros y documentos recogidos verbatim **no** se reapuntaron, y el nombre `cola.yaml` entra a la lista `HISTORICOS` de `tests/check.py` como borrado declarado"* — confirmado en código, `tests/check.py:165`, `"cola.yaml"` vive dentro del set `HISTORICOS`. Los tres citantes que nombra el encargo están los tres confirmados: `canon/gobernanza-v1_15.md` (414, 422, 430, 915), `canon/protocolo-sesion-v1_0.md:82`, y `forense/hitoD-preregistro-v2_0.md` — este último con **seis** citas, no una (575, 599, 628, 642, 646, 653), incluyendo dos decisiones de falsación de G3 literalmente "registradas en `cola.yaml` como decisión abierta, sin resolver" (575, 599) — vivas hoy solo como prosa histórica, su resolución real está archivada en `forense/hallazgos-congelados-2026-07-30.yaml`. Veredicto: **no hay nada que reparar aquí** — es la referencia colgante mejor documentada del repo, con su propia entrada de test-suite.

**2. `run_r5_2.py` — reproducibilidad rota sobre un veredicto ya sellado.** Confirmado ausente en todo el árbol (`find` vacío); `tests/` solo tiene `r5_1_pension_bienestar.py`. Citado en `data/diseno-muestral.yaml:239` ("script `run_r5_2.py` ejecutado") y `data/manifiesto.yaml:3062` ("este payload alimentó `run_r5_2.py`"). `forense/AUDITORIA-PREMISAS-SELLADOS-2026-08-07-v1_0.md` §3 sella `R5.2` con veredicto **`A`** ("Reserva verbatim Nota 18: el punto satisface la fila 'pero el IC95% no despeja el umbral' (23.98%, IC [14.39, 33.57] vs 20)"). Las dos cosas juntas: el script que produjo ese número sellado no existe en el árbol bajo el nombre que dos archivos distintos citan como su procedencia — el veredicto `A` no se puede reproducir hoy corriendo el script que el propio manifiesto dice que lo generó. No se investiga más (fuera de perímetro); se deja nombrado con su conexión al sello.

**3. `data/catalogo-fuentes-v1_0.md` — no existe (es v2_0); 18 archivos lo citan, no ~13, y cuatro citas a la misma línea fantasma sostienen un veredicto sellado.** Confirmado ausente; solo `data/catalogo-fuentes-v2_0.md` existe. 18 archivos citan `catalogo-fuentes-v1_0` (más que el "~13" del encargo — otro caso de "no heredas, re-derivas"): `forense/hitoD-preregistro-v2_0.md`, `canon/gobernanza-v1_15.md`, `forense/hallazgos.md`, `forense/cruce-catalogo-fichas-v1_0.md`, `data/inventarios/README-inventarios.md`, y 13 notas fechadas de `forense/notas/`. De estas, **seis citas apuntan a un número de línea específico, permanentemente inverificable**: cuatro apuntan a la misma línea, `:123` (DGIS — Otros subsistemas) — `forense/hitoD-preregistro-v2_0.md:954`, `forense/notas/2026-08-04-z6-veredicto-r9-2.md:9`, `forense/notas/2026-08-04-z3-declaracion-fuente-r9-2.md:12`, y **`canon/gobernanza-v1_15.md:603`**, donde esa cita a `:123` es explícitamente la evidencia de la mitad "sin fuente" del veredicto sellado `R9.2 → D` ("la única disponible, DGIS (`data/catalogo-fuentes-v1_0.md:123`), es el propio prestador, excluido por la ficha misma"). Las otras dos son `forense/cruce-catalogo-fichas-v1_0.md:68` (cita `:72`, ENCUP) y `:133` (cita `:41`, ENASEM), y `forense/hallazgos.md:102` también cita `:41`. **Conexión con `AUDITORIA-PREMISAS-SELLADOS-2026-08-07-v1_0.md` §3**: ese documento ya marca `R9.2` como `EXPUESTO`, por razones de vocabulario/universo — esta Tarea C añade una segunda exposición, estructural: parte de su evidencia documental vive en una línea de un archivo que ya no existe bajo ese nombre.

**4. `tests/catalogo.py` SIN_RESOLVER — 4 confirmados, ninguno nuevo.** `python3 tests/catalogo.py` da `SIN_RESOLVER: 4` de 201 entradas (197 resueltas), consistente con el "128 fuentes únicas" de §1. Los cuatro, con archivo y línea:
- `inventario_fuentes_clase-fuente-mexico.md:215` — sugerencia `'ENCUESTA NACIONAL DE CALIDAD D'`, título real `Encuesta Nacional de Calidad de la Atención del Servicio de Salud (ENCAL, IMSS)`
- `inventario_fuentes_cultura_valores_opinion_mexico.md:234` — misma sugerencia truncada, título real `Encuesta Nacional de Calidad de la Ciudadanía`
- `inventario_fuentes_trabajo_ingreso_formalidad_mexico.md:36` — sugerencia `'REGISTROS ADMINISTRATIVOS DE A'`, título real `Registros administrativos de aseguramiento — IMSS`
- `inventario_fuentes_trabajo_ingreso_formalidad_mexico.md:48` — misma sugerencia, título real `Registros administrativos de aseguramiento — ISSSTE **[no verificado]**`

Son exactamente los dos pares que `MAP-1`/PR #147 ya había encontrado y dejado `SIN_RESOLVER` **a propósito** (ENCAL vs. "Calidad de la Ciudadanía"; IMSS vs. ISSSTE de aseguramiento — dos identidades genuinamente ambiguas, no un defecto del parser). Nada nuevo: la decisión de MAP-1 sigue vigente, sin resolver, tal como se dejó.

**5. `corpus/forense/Apuestas_Conductuales_…md` — confirmado, no es defecto.** Existe: `corpus/forense/Apuestas_Conductuales_sobre_el_Consumidor_Mexicano__Validación_Forense_de_Supuestos_contra_Desenlaces_Reales.md`. Confirma la explicación del encargo: el barrido de mesa del 30/jul lo marcó como colgante solo porque su lista de rutas candidatas no incluía `corpus/forense/`. No se vuelve a levantar.

---

## TAREA B y TAREA D — explícitamente diferidas

No ejecutadas en esta entrega, por instrucción de mesa. La compuerta de ARRANQUE 6 falló (2/3), y "no se construye el registro ni se cruza la cola de re-examen sin BARRIDO-1 y ABRIR-4" — mesa, verbatim. Cuando ambas entregas existan como artefacto real (BARRIDO-1 necesita retomar su Tarea B desde donde murió, o relanzarse; ABRIR-4 necesita al menos su PASO 0), CRUCE-1 parte 2 construye `data/registro-demanda-universo-*.tsv` y cruza `forense/AUDITORIA-PREMISAS-SELLADOS-2026-08-07-v1_0.md` §5 (ya commiteado en este acto, listo para usarse) contra ese registro.

**Addendum, descubierto durante la verificación de suite (no durante ARRANQUE 6, ya cerrado arriba) — la compuerta se movió en vivo mientras este acto corría:** al usar el worktree `wt-abrir4-1786051186` como control limpio para aislar qué ROJO era propio de este acto (ver abajo), resultó que ya no estaba idéntico a `origin/main` como en ARRANQUE 6. Estado real al cerrar este acto, no al abrirlo:
- **BARRIDO-1 se completó durante esta sesión**: retomó, censó 27 puertas en las 5 clases de §2, se fusionó con ÍNDICE-2/PR#156 en su propia rama, y abrió **PR #157** ("censo de 27 puertas más allá de INEGI en 5 clases"), abierto, sin fusionar.
- **ABRIR-4 sigue en vuelo, no cerrado**: dos commits reales (`f0f48a6` PASO 0, `3c5a8dc` "cruce necesidad x instrumento de los 4 payloads sin abrir: ENSAFI/ENFIH/ENASIC/ENBIARE" — exactamente los candidatos que `REVERIFICACION-DEMANDA-vs-UNIVERSO-2026-08-07-v1_0.md` §3 ya señalaba como el hallazgo mayor), pero con cambios sin commitear todavía (`forense/hallazgos.md`, `forense/notas/2026-08-08-abrir4.md` modificados, `data/raw` sin trackear) — sesión activa, no entrega estable.

No se actúa sobre esto en esta entrega — construir el registro ahora sería competir con una sesión de ABRIR-4 todavía en vivo, y mesa no ha re-autorizado el alcance con el estado nuevo. Queda declarado para que la decisión de cuándo lanzar CRUCE-1 parte 2 se tome con la compuerta real, no con la de ARRANQUE 6.

---

## Suite ×3 (antes del push)

Corrida `python3 tests/check.py --baseline` tres veces en este worktree — con corpus (`data/raw` enlazado a `/home/pc0/mm-corpus/raw`), sin `data/raw`, y sin gitignorados de config (el worktree nunca tuvo `data/raices.local.yaml`/`data/secretos.local.yaml`, al ser `git worktree add` fresco — condición verificada, no asumida). **Resultado idéntico en las tres corridas: `21 FAIL · 95 WARN`, `LÍNEA BASE: ROJO`, con las mismas 3 entradas nuevas frente a `tests/baseline.json` (HEAD congelado `837d5fe`):**

- **T02, self-causado por este mismo acto**: `forense/notas/2026-08-09-cruce1.md` colisiona con `forense/encargos/2026-08-09-cruce-1.md` bajo la normalización de T02 (que descarta todo carácter no alfanumérico antes de comparar). Mismo mecanismo, mismo tipo de causa, que `[[project_modelado_mexicano_indice2_status]]` ya documentó para sus propios dos nombres mandados por su encargo — aquí igual: PASO 0 y §6 mandan los dos nombres verbatim, y renombrar cualquiera violaría el perímetro de escritura. No se corrige.
- **T16 ×2, verificado como preexistente, no causado por este acto**: `canon/estado-programa-v1_10.md` y `canon/gobernanza-v1_15.md` declaran "18 FAIL · 95 WARN vigente" contra una corrida real de 19 (antes de sumar el T02 propio de arriba). Verificado con control limpio: la misma corrida en `wt-abrir4-1786051186` (`origin/main` + solo los commits de ABRIR-4, cero relación con este acto) da el mismo mensaje exacto — el desfase ya vive en `origin/main` desde antes de que este acto empezara. `canon/**` está fuera de perímetro; no se toca.

No hay CI que verificar (Actions permanece en `major_outage`, causa ya establecida por `PR #155`/`forense/hallazgos.md` 2026-08-08) — verificación puramente local, cruda arriba.
