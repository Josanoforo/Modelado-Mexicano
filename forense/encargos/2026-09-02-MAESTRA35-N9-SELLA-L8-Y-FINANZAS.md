ENCARGO · ACTO MAESTRA35-N9 · SELLA-L8-Y-FINANZAS — invoca /acto SHA de redacción: e1afa2a (merge PR #492). Redacta dirección (Fable), 2/sep/2026, contra v2.12. Estado: LISTO PARA LANZAR — PR #487 (MAESTRA35-L8) y #488 (`[ADQ] 2026-09-02`) ya están en `origin/main`; verificación por producto: `grep -c "relanzamiento_l8" milpa/tramite-ola5-propuesta-v0.yaml` ≥ 1 (línea 1328 en e1afa2a). COMPUERTA: ninguna. ENTORNO ASIGNADO: NUBE (`cloud_default`). NO en UBUNTU. MODELO SUGERIDO: Sonnet (propagación; SELLA-3).
CARRILES: PR #490 (MAESTRA35-L4, cuatro R) y #491 (MAESTRA35-L9, seis reglas) pendientes de fusión — tocan `corridas-R`, propuesta (append `civico.voto.*` etc.), tablero: disjuntos de este acto salvo tablero (re-aplica quien fusiona segundo). Cron `mm-adq` (registro del curador): este acto edita UNA fila de ese registro (P3) — re-lee el archivo antes de escribir, por línea, nunca con el módulo `csv` (defecto medido por el cron: un round-trip alteraba el citado de 3 filas ajenas).
FIRMAS DE MESA — verbatim, 2/sep/2026. El ejecutor propaga, no decide (SELLA-3).

* Decisión 2: «e1.» Letra tal como dirección la presentó: sellar la entrada `civico.participacion.tipo_boleta_federal_2016_2024` como ACOTADA, tier MEDIA, y autorizar a dirección a presentar a mesa la conversión a regla del motor con un solo disparador (concurrencia con presidencial → +4.0 pp; la traducción de pp a probabilidad es firma de mesa con el texto a la vista, L6 §5). Con e1 se cierra la línea cívica electoral: lo que falta (Hidalgo 2024, Veracruz 2025) no mueve el veredicto.
* Decisión 1 (FINANZAS): «no recuerdo a qué se refiere pero si nos hace falta data la obtenemos si no explotamos la que ya tenemos.» → se propaga como f1: cerrar FINANZAS como código sin identidad propia; lo que PNIF publica (ENIF, ENAFIN, ENSAFI 2023) ya está en el corpus bajo su nombre; si una regla necesita algo que no esté, se abre fila nueva con identidad.

═══ VERIFICACIÓN DE EXISTENCIA (A.8) — contestada por dirección contra e1afa2a ═══ (1) ESTRUCTURA. Propuesta: entrada única con dos cuerpos — L3 (línea 1172, `situacion: PENDIENTE-DE-MESA` 1174, `veredicto_falsador: NO-DISCRIMINA` 1195, `tier: PENDIENTE-DE-MESA` 1197) y bloque `relanzamiento_l8` (1328+: 9 entidades, 489 municipios, 864 observaciones, 19 transiciones, 9 conglomerados, 7 tratadas medibles, STAY 4; β_pres +4.0167 [+0.0492, +7.8874] / municipio [+3.3467, +4.6839]; β_int +0.2864 [−1.2216, +1.7945]; veredicto ACOTADA). Registro del curador `data/curacion-registro/cola-adquisicion-registro.tsv` línea 73 (`FINANZAS · PENDIENTE · FP17-sin-N`); vista `data/cola-adquisicion-v1_0.tsv` (generada). Tablero: FP-245 ABIERTA («Sellar la entrada nueva … que L3 deja PENDIENTE-DE-MESA»). (2) CONTENIDO. `grep -c "firma e1" milpa/tramite-ola5-propuesta-v0.yaml` → 0; `grep -c "CERRADA-PREEXISTENTE" data/curacion-registro/cola-adquisicion-registro.tsv` → contar (el vocabulario existe en el tablero: 1 fila). AHORRO_FINANCIERO_Y_FINANCIAMIENTO: `grep -c -P "\tAHORRO_FINANCIERO_Y_FINANCIAMIENTO\t"` en el registro → 0 en e1afa2a (la fila vive bajo otro nombre canónico o en la vista; el ejecutor la localiza por `grep -n -i "AHORRO_FINANCIERO"` en registro y vista y reporta dónde; si no existe fila propia, sólo se anota en la nota de FINANZAS). (3) COBERTURA RETROACTIVA. Todo del 2/sep; cubierto.
PIEZAS (cero estimaciones) P1 · e1 en la propuesta: `situacion: PENDIENTE-DE-MESA` (1174) → `situacion: SELLADA-SIN-CARGA # firma e1 (mesa, 2/sep/2026), ACTO MAESTRA35-N9 -- ACOTADA; conversion a regla del motor pendiente de presentacion de direccion y firma de mesa (L6 §5)`. `tier: PENDIENTE-DE-MESA` (1197) → `tier: MEDIA # firma e1`. Campo nuevo `veredicto_vigente:` con: `ACOTADA (MAESTRA35-L8, 9 entidades / 7 tratadas medibles de 8; beta_pres +4.0167 pp con IC wild cluster [+0.0492, +7.8874] y municipio [+3.3467, +4.6839], positivo en 8/8 sensibilidades; beta_int +0.2864 [-1.2216, +1.7945]); reservas: secuencia rota declarada en spec §0.0 de L8, Hidalgo sin pata 2024, 3 municipios de Veracruz 2017 con cero votos reales; el NO-DISCRIMINA de L3 queda como historia de su universo (A.10)`. Campo `linea_cerrada: "firma e1: no se adquiere mas para esta linea; Hidalgo 2024 y Veracruz 2025 no mueven el veredicto"`. Estampa A.10 con SHA e1afa2a. El bloque `relanzamiento_l8` y el cuerpo de L3 quedan intactos. P2 · Tablero: FP-245 → EJECUTADA (firmada_en «e1 (mesa, 2/sep/2026)», ejecutada_en = ADR de este acto). Fila nueva ABIERTA: «Dirección presenta a mesa la conversión de tipo_boleta_federal a regla del motor con un disparador (concurrencia con presidencial); la traducción pp → probabilidad es firma de mesa (L6 §5). Autorizada por e1.» vence: con el siguiente lote de propagación. P3 · f1 en el registro del curador (línea 73, edición por línea): `estado_A4A5` `PENDIENTE` → `CERRADA-PREEXISTENTE`; nota con enmienda fechada: «2026-09-02, firma de mesa verbatim: "no recuerdo a qué se refiere pero si nos hace falta data la obtenemos si no explotamos la que ya tenemos". Código sin identidad propia desde el 14/ago (FP-17); la puerta pnif.cnbv.gob.mx abrió el 2/sep (cadena TLS, [ADQ] 2026-09-02) y publica ENIF/ENAFIN/ENSAFI 2023, las tres ya registradas (grep de ids: 22/10/3). Si una regla pide algo que no esté, se abre fila con identidad.» Si existe fila propia de AHORRO_FINANCIERO_Y_FINANCIAMIENTO con acción pendiente «cadena TLS CNBV», misma enmienda: la acción quedó resuelta por el [ADQ] del 2/sep; estado según lo que la fila diga de su payload (no se cambia a OBTENIDO sin id de manifiesto). Regenerar la vista: `python3 tools/vista_cola_adquisicion.py`; T26 verde; `via_capa2.py --root .` en lectura sin diff. P4 · Hallazgos: `2026-09-02 · MAESTRA35-N9 · civica electoral: tipo_boleta ACOTADA sellada MEDIA (e1), linea cerrada, conversion pendiente de firma; FINANZAS cerrada como codigo sin identidad (f1). El cron dejo de caminarla.`
PERÍMETRO Y CONCURRENCIA: `milpa/tramite-ola5-propuesta-v0.yaml` (una entrada, campos nuevos + dos marcas) · `data/curacion-registro/cola-adquisicion-registro.tsv` (una o dos líneas) · `data/cola-adquisicion-v1_0.tsv` (solo regenerada) · `forense/firmas-pendientes.tsv` (FP-245 + una fila) · `forense/hallazgos.md` · A.3 · cascada. NO toca `milpa/tramite.yaml`, `corridas-*`, `data/l3-*`/`l8-*`, `tools/`, manifiesto. Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.
FP/ADR CANDIDATOS: una fila nueva (conversión pendiente); primer FP/ADR libres al arrancar (252/304 en e1afa2a; #490 y #491 reclaman antes si fusionan).
CONTADOR: S5 −1 (entrada cívica sellada) · filas de la cola PENDIENTE −1 · cero estimaciones.
Lo que este acto NO hace: no carga la cívica al motor (eso es la conversión, firma aparte); no mide; no toca L4/L9; no edita instrucciones.
