# 2026-08-13 · ACTO A8-LAND — nota de cierre (reconciliada: `ADR-78`, no `ADR-77`)

Renombrada desde `forense/notas/2026-08-13-a8-land.md` — ese nombre lo ocupa ahora la nota de la otra sesión que selló `ADR-77` (§2, abajo). Mismo patrón de colisión que `convencion.md` ya documenta ("un encargo y una nota con el mismo tema... colisionan — ha ocurrido en cinco actos"), aquí entre dos notas de dos sesiones distintas sobre el mismo tema.

Entorno: **NUBE**, `cloud_default` (confirmado, `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=cloud_default`), sonda HTTP saltada per el propio encargo. Repo-only: no abrió `data/raw`, no tocó `milpa/**`, `tests/**` ni `tools/**`.

---

## 1 · ARRANQUE — un hallazgo de terreno antes de escribir nada

El clon existente estaba **shallow** (límite en `ed46c26`, 8/ago/2026). Contra un clon shallow, `git log --diff-filter=A -- instrucciones-proyecto-v2_6.md` da **2026-08-08** como fecha de nacimiento — no coincide con el **2026-08-05** que la VERIFICACIÓN DE EXISTENCIA del encargo declaraba. `git fetch --unshallow origin` (860 commits reales contra 264 visibles) resuelve la discrepancia: la fecha real es 2026-08-05 (`661ee90`), exactamente la que el encargo citaba — el desajuste era del clon, no del encargo.

SHA de redacción `8b30306` confirmado idéntico contra `origin/main` tras `git fetch origin --prune`; **cero ramas vivas** en ese momento además de `main`. Esto resultó ser el punto ciego real: la otra sesión (rama `claude/new-session-dh6rm9`) ya había arrancado, y su PR aún no existía cuando este acto verificó "cero ramas vivas" — el ARRANQUE del encargo no puede ver una rama que otra sesión todavía no ha empujado. No hay chequeo que lo hubiera atrapado antes; el propio encargo lo anticipa como riesgo estructural ("la colisión que ya ocurrió cuatro veces") sin poder prevenirlo por completo.

---

## 2 · La colisión con `ADR-77`/PR#217, y cómo se resolvió

Al pedirse "haz match con main para resolver CI" (tras abrir el PR #218 original de este acto, commit `5f4095f`), `git fetch origin main` mostró que `origin/main` había avanzado de `8b30306` a `a97dc28` — merge de PR #217, título *"v2.7: Add A.8 seal rule and incorporate instructions update"*, rama `claude/new-session-dh6rm9`.

**Lo que PR #217 resultó ser, verificado leyendo su diff completo, no asumido por el título:** otra sesión ejecutando el mismo encargo A8-LAND, pero una versión **anterior y más corta** — redactada contra `origin/main=7ac523e` (antes de que `ACTO SELLA`/PR #216 fusionara `ADR-76`), con **solo A.8**, sin la sustitución de línea 158 ("los DOS bloques"), sin el bloque VERIFICACIÓN DE EXISTENCIA que va después del ARRANQUE, y sin A.9. Su propia nota (`forense/notas/2026-08-13-a8-land.md`, ahora la de esa sesión) documenta que también tuvo que refrescar contra `8b30306` a mitad de ARRANQUE y re-derivar el número de ADR — la misma disciplina que este encargo exige, aplicada de forma independiente por la otra sesión.

**Por qué llegó primero:** simplemente terminó antes. No hay evidencia de que su encargo fuera "más correcto" — es un encargo más corto, verificado contra su propia base, ejecutado con el mismo rigor. `ADR-77` es una decisión de mesa tan válida como cualquier otra ya sellada.

**La resolución, mecánica.** `git merge origin/main` (local, nunca el editor web de conflictos — mismo criterio que el propio encargo y `ADR-76(b)` ya fijan) produjo tres conflictos reales:

1. `canon/gobernanza-v1_15.md` — auto-merge sin marcar CONFLICT, pero dejó **dos entradas `**ADR-77`** consecutivas, contenido distinto, mismo número. Git no lo vio como conflicto porque las inserciones no se solapaban línea a línea; el conflicto era semántico, no textual.
2. `canon/estado-programa-v1_10.md` — CONFLICT real: ambas ramas editaron la misma oración de la narrativa histórica de L0, cada una añadiendo su propia cláusula "a 77 después...".
3. `instrucciones-proyecto-v2_7.md` — CONFLICT real (add/add): ambas ramas crearon el archivo, contenido distinto.
4. `forense/notas/2026-08-13-a8-land.md` — CONFLICT real (add/add): ambas ramas crearon una nota con el mismo nombre.

**Resuelto así, y declarado explícitamente en `ADR-78`:**

- `ADR-77` (de `PR#217`) **queda intacto, sin una palabra editada** — reescribir una decisión ya fusionada y sellada la falsearía, mismo principio que `ADR-76(f)` fija para la línea base ("un ejecutor que encuentre drift lo reporta y no lo recongela"; aquí, un ejecutor que encuentre una decisión ya sellada la respeta y no la reescribe).
- Mi propia entrada `ADR-77` (A.8+A.9 juntas) se **elimina por completo** del documento — no es un ADR paralelo válido, es la misma decisión de gobierno (el número 77) que ya está tomada por otra parte.
- `instrucciones-proyecto-v2_7.md` de esta rama se **descarta**; se adopta el de `origin/main` verbatim (299 líneas, A.8 sola).
- A.9 se reconstruye como `instrucciones-proyecto-v2_8.md` — **archivo nuevo**, v2.7 (de main) + sustitución de línea 158 + bloque VERIFICACIÓN DE EXISTENCIA + nota de numeración/"Cuatro reglas" dentro de Bloque D-ter + A.9 al final. Mismo patrón append-only que v2.4→v2.5→v2.6→v2.7: **nadie edita el v2.7 ya sellado.**
- Nueva decisión de mesa, `ADR-78`, sella A.9 sola — no re-sella A.8, no re-declara sus incisos, solo cita `ADR-77` donde hace falta.
- La nota de esta rama se renombra a este archivo (`2026-08-13-a8-land-adr78.md`); la de la otra sesión ocupa `2026-08-13-a8-land.md` sin tocarse.
- `canon/estado-programa-v1_10.md`: la narrativa de L0 queda con **ambas** cláusulas, en orden — "...a 77 después, con ADR-77... (sella A.8...); a 78 después, con ADR-78... (sella A.9...)". El primer intento de resolución dejó un párrafo duplicado completo (mi versión vieja + la fusionada) — encontrado con `grep -c "^\*\*L0 · Gobierno"` dando 2 en vez de 1, corregido antes de commitear.

**Lo que esto NO decide:** cuál de las dos sesiones "debió" llegar primero. Ninguna — es una carrera entre dos ejecuciones legítimas del mismo encargo, y el criterio de este repo para exactamente esta situación (visto ya en `ADR-70`/`ADR-71`, ambos renumerados por la misma razón) es: la que fusiona primero se queda con el número, la segunda se reconcilia. `ADR-78` lo declara así en vez de disputarlo.

---

## 3 · Sitios de cascada tocados, con línea (estado final, post-reconciliación)

- `instrucciones-proyecto-v2_8.md` — archivo nuevo. `instrucciones-proyecto-v2_7.md` — **no tocado** en este acto (es el de `origin/main`, ya sellado por `ADR-77`).
- `canon/gobernanza-v1_15.md:2` — cabecera, `77 ADR` → `78 ADR`.
- `canon/gobernanza-v1_15.md` (después del cierre de `ADR-77`, antes del `---`) — entrada nueva `**ADR-78 · ...**`. `ADR-77` **no editado**.
- `canon/estado-programa-v1_10.md:27,101` — `77 ADR` → `78 ADR` (dos citas) + cláusula "; a 78 después, con ADR-78..." añadida a la narrativa histórica de L0, después de la cláusula de `ADR-77` que ya traía `origin/main`.
- `forense/hallazgos.md` — mi entrada original (A.8+A.9 juntas, `ADR-77`) reescrita para reflejar la colisión y `ADR-78`; la entrada de la otra sesión (`ADR-77`) no tocada.
- `forense/encargos/convencion.md` — mi línea duplicada se retira; la línea de `origin/main` (misma idea, otra redacción) queda como única.
- `forense/notas/2026-08-13-a8-land.md` → contenido de esta rama movido a este archivo; el nombre original queda con el contenido de `origin/main`.
- `forense/encargos/2026-08-13-A8LAND-instrucciones-v2_7.md` — Estado actualizado para narrar la reconciliación.

**NO tocado:** `data/`, `milpa/`, `tests/`, `README.md`, `canon/modelo-decision-v4_0.md`, `instrucciones-proyecto-v2_6.md`, `instrucciones-proyecto-v2_7.md` (de `origin/main`). **NO tocado, deliberadamente**, dentro de `canon/gobernanza-v1_15.md`: líneas 1104 (`ADR-76(f)`) y 1132 (`ADR-77`, Cascada) — ver §5.

---

## 4 · La cifra de suite — tres momentos, ninguno copiado

**Momento 1 — antes de que este acto tocara nada** (`HEAD = 8b30306`):
```
18 FAIL · 104 WARN
LÍNEA BASE: VERDE
```

**Momento 2 — PR #218 original, antes del merge con `origin/main`:**
```
19 FAIL · 107 WARN
LÍNEA BASE: ROJO — 2 entradas nuevas (T03 archivo de encargo, T16 gobernanza:1104)
```

**Momento 3 — final, después del merge y la reconciliación completa:**
```
20 FAIL · 107 WARN
LÍNEA BASE: ROJO — 2 entradas nuevas (agrupadas por archivo; 3 hallazgos reales, ver §5)
```

WARN no se movió entre el momento 2 y el 3 (107 en ambos) — el contenido de `PR#217` no introdujo citas colgantes nuevas. FAIL subió de 19 a 20 porque `ADR-77` (fusionado desde `PR#217`) trae su propia línea `**18 FAIL · 104 WARN**` en su párrafo de Cascada — un segundo caso del mismo patrón que `ADR-76(f)` ya tenía, ahora por partida doble, cada uno un registro histórico legítimo de lo que esa decisión midió al sellarse.

---

## 5 · Tres hallazgos declarados al cierre, ninguno absorbido por freeze unilateral

`ADR-76(f)` fija la regla: recongelar la línea base exige ADR de mesa. Ninguno de los tres se resuelve editando texto sellado ni congelando por decisión unilateral.

**(i) T03 — tres citas verbatim de "v2_6.md" sin prefijo**, en `forense/encargos/2026-08-13-A8LAND-instrucciones-v2_7.md` (líneas 53, 61, 74). Texto del encargo archivado tal como se lanzó (A.3); no se edita. Precedente ya en baseline: `2026-08-12-M6-sello.md`.

**(ii) T16 — `gobernanza-v1_15.md:1104`, dentro de `ADR-76(f)`.** Registro histórico de lo que `ADR-76` midió al sellarse (`8b30306`, antes de que existiera el archivo del hallazgo (i)) — correcto entonces, correcto como historia hoy. Cambiarlo falsearía el registro.

**(iii) T16 — `gobernanza-v1_15.md:1132`, dentro de `ADR-77`, su propia Cascada.** Mismo mecanismo que (ii): `ADR-77` midió `18 FAIL · 104 WARN` al sellarse (verificado — esa cifra es exacta contra `8b30306`, la base de `ADR-77`, antes de que este acto añadiera ningún archivo), y ese texto ahora sellado por otra sesión no se toca por la misma razón que `ADR-76(f)` no se toca.

Los tres son instancias del mismo límite, ya documentado en el propio código de T16 (`tests/check.py:578-592`, docstring): solo reconoce como histórico el formato `> **vX.Y — fecha.**`, y ninguna de las dos citas de "104 WARN" lo usa. No es un bug de este acto ni de `ADR-77` — es el costo conocido de que dos ADR distintos, sellados por dos sesiones distintas el mismo día, cada uno correctamente midiendo su propio momento, ya no coinciden entre sí una vez que un tercer archivo (el hallazgo (i)) cambia la cifra real después de ambos.

---

## 6 · La instrucción "Tres reglas"/"Cuatro reglas" — sigue igual, ahora en `instrucciones-proyecto-v2_8.md`

Sin cambio respecto al hallazgo original: el encargo especifica dos sustituciones exactas (`Tres reglas.`→`Cuatro reglas.`, `manda sobre las tres`→`manda sobre las cuatro`) en la línea de cierre de Bloque D-ter, dejando sin tocar una tercera ocurrencia del mismo conteo ("Las tres salen del mismo defecto..."). Se preserva exactamente esa misma decisión en `instrucciones-proyecto-v2_8.md` (Bloque D-ter no cambió de lugar ni de contenido al reconstruir sobre v2.7 de `origin/main` — main tampoco lo tocó). Reportado, no completado por inferencia.

---

## 7 · Contadores que NO se mueven, y el estado de A.9

`13 de 27` (Hito D) · `9 de 14` (condicionales) · `0 de 15` (coeficientes) · `1 de 2` (llaves) · `4 de 144`. **Ninguno**, ni por `ADR-77` (verificado en su propio texto) ni por `ADR-78`.

A.9 (inciso de `ADR-78`) queda **`PENDIENTE`**: `instrucciones-proyecto-v2_8.md` no se ha pegado en el proyecto de Claude — el pegado es a mano, fuera del repositorio, y ningún acto con herramientas de repo puede ejecutarlo. Es la propia regla A.9 aplicada a sí misma en su primer uso real (el intento en `ADR-77`, que no sella A.9 en absoluto, no cuenta como el primer uso). El texto íntegro de v2.8, listo para pegar, va en el reporte de cierre de este acto.

---

## 9 · Resolución de §5 — mesa autorizó el freeze, mismo día

Los tres hallazgos de §5 se reportaron al usuario (fuera de este repositorio, en el reporte de cierre del PR) sin resolverse, exactamente como §5 los dejó — CI en rojo por diseño, no por descuido. Mesa respondió con instrucción explícita: *"solve to merge"* — mismo mecanismo de autorización que ya usó `ENCARGO ADR-PROVISIONALIDAD` §9 (*"solve Pr cant merge"*, PR #199), dada fuera del marco del encargo original (que reservaba la decisión) y por encima de él.

**Implementado:** `tests/check.py` gana dos clasificaciones nuevas — un bucket propio para el T03 del encargo archivado (`_classify`, patrón `2026-08-12-M6-sello.md`) y un bucket propio para los T16 de hoy (distinto del bucket viejo, hardcodeado, que describía el refreeze del 30/jul/2026 — usarlo sin editar habría sido tan impreciso como no clasificar). La `nota` de `_freeze_note()` se extiende (no se reescribe) con el evento de hoy, mismo patrón que ya usa para narrar 29/jul→30/jul. `python3 tests/check.py --freeze` (HEAD `3d0d1e5`) → `tests/baseline.json` recongelado. `python3 tests/check.py --baseline` → **VERDE**.

**Por qué esto y no otra cosa.** Editar las citas de "104 WARN" dentro de `ADR-76`/`ADR-77` habría falsificado dos decisiones ya selladas (§5). Editar el encargo archivado habría violado A.3. La única vía que no reescribe nada ya sellado es aceptar los tres hallazgos con `--freeze`, bajo autorización de mesa — exactamente el mecanismo que `ADR-76(f)` reservó para esto. Enmienda in situ correspondiente en `ADR-78` (`gobernanza-v1_15.md`).

**Alcance, declarado.** Toca `tests/**` — fuera del `NO toca` original del encargo. Autorizado explícitamente por mesa en el hilo de la sesión, no decisión unilateral de este acto; mismo criterio que `ADR-PROVISIONALIDAD` §9 ya sentó para el mismo tipo de cambio.

---

## 10 · La línea honesta

"Haz match con main para resolver CI" resultó no ser un conflicto mecánico de texto — fue una colisión real de contenido: dos sesiones, mismo encargo, dos decisiones de gobierno distintas peleando por el mismo número. Resolverlo bien significó tres cosas a la vez: no reescribir una decisión ya sellada por otra sesión (aunque fuera "menos completa" que la de esta rama), no perder A.9 en el proceso de ceder A.8, y declarar la colisión misma en vez de dejar que un `git merge` silencioso la enterrara en una entrada de ADR duplicada que nadie notaría hasta que T15 (o un lector humano) tropezara con ella. Los tres hallazgos de T03/T16 de §5 no eran nuevos defectos de este acto — eran la huella mecánica, ya anticipada por el propio código de la suite, de que dos ADR honestos sobre su propio momento pueden dejar de coincidir con el presente sin que ninguno de los dos esté mal. Y "resolver CI" no significó forzar un verde silencioso — significó pedir la autorización que la propia regla exige, y dejar el porqué escrito donde el próximo lector lo vea.

---

**Requisito de salida.** Este archivo (renombrado); `forense/hallazgos.md` (entrada reescrita + addendum de §9); `forense/encargos/2026-08-13-A8LAND-instrucciones-v2_7.md` con la reconciliación narrada en su Estado; `tests/check.py --baseline` corrido en los tres momentos de §4 y VERDE tras §9; enmienda in situ en `ADR-78` (`gobernanza-v1_15.md`); texto íntegro de `instrucciones-proyecto-v2_8.md` entregado en el reporte de cierre, listo para pegar en el proyecto de Claude (A.9, vía `ADR-78`).
