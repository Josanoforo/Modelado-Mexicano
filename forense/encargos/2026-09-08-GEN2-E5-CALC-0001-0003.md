# E5 · ACTO GEN2-E5 · CALC-0001..0003

- **SHA de redacción (base del encargo):** `df9336c5` (reencolado por `ACTO GEN2-V213`, 8/sep/2026). **SHA de ejecución:** `origin/main = fbce146d` (`PR #629`, `ACTO GEN2-E5-0`, fusionado). La compuerta se re-derivó contra este segundo SHA, no contra el de redacción.
- **Entorno asignado:** UBUNTU (caja, corpus montado), worktree nuevo desde `origin/main`, Opus. **NO se lanza en NUBE** (lo dice el propio cuerpo).
- **Estado:** VIVO.


## E5 · ACTO GEN2-E5 · CALC-0001..0003 — correr, sellar, verificar (los cálculos, al final de todo)

Cabecera: **UBUNTU (caja, corpus montado)**, worktree nuevo desde `origin/main` con E5-0 fusionado · **Opus** · COMPUERTA por producto (**el GO**): los tres `spec.yaml` y sidecars en `main`; los tests de E3.1 en verde en `origin/main` (salida pegada); `preflight` VERDE en los tres CALC desde `origin/main`; `cmd_status` en `main` (E6) y `GO-MARCADOR` (E7); base congelada (si `main` avanza durante el acto, no se auto-mezcla); caja limpia (`limpia_arbol --reporta` vacío). NO se lanza en NUBE.
Por CALC, en orden: `preflight` → `run` (contrato y rutas resueltas) → `verify` (`REPRODUCE/IDENTICO` esperado; cualquier otra combinación se reporta como salga) → COMMIT-2 (`ejecucion.json`, `resultados.json`, `sello.sha256`; entradas `PENDIENTE-DE-MESA` en la propuesta citando `corrida0_resultado_id`) → `registro` y `status` (deben subir `N_corridas_selladas` y `N_resultados_sellados`; si no suben, algo del cableado falló y es hallazgo, no se fuerza). Spec mal contra el dato: **no se edita**; nace el siguiente id con `sucesor_de` y un tercer commit lo declara. Una pieza que PARA no tumba las otras; PARO de entorno A.2 detiene todo. Al cierre: **T-REPRO pasa de WARN a FAIL** (`tests/check.py`), declarado en E6 y ejecutado aquí, con línea base que congela GEN1. La sustancia metodológica es la de los `spec.yaml` congelados en E5-0: este acto no decide nada. Cierre A.14; rama borrada.
Perímetro: `data/corrida0/CALC-000{1,2,3}/{ejecucion.json,resultados.json,sello.*}` · `data/corrida0/{corridas,resultados,usos}.tsv` (re-derivados) · `forense/notas/<fecha>-GEN2-E5-*.md` · `milpa/tramite-ola5-propuesta-v0.yaml` (solo `append`) · `tests/check.py` (T35 WARN→FAIL) · `forense/firmas-pendientes.tsv` (recibo) · `forense/no-corrido.tsv` · cascada. **No toca specs, `spec.yaml`, `tramite.yaml` ni canon.** Si te encuentras escribiendo fuera de esta lista, PARA.
Contador: **medición: sí** — `N_corridas_selladas 0→3`, `N_resultados_sellados 0→k`, `dependencias_numericas_legacy_activas` 162 → 162−k. Sucesores: `SELLO-4` (mesa), C0-B lote 1 (specs para los 7 coeficientes y las 4 conductas de D10, sobre `demanda-corridas.tsv`), C0-C, C0-D.
---

## NO-CORRIDO / RESERVAS

| qué (verbatim del encargo) | por qué | impacto | sucesor |
|---|---|---|---|
| «Por CALC, en orden: `preflight` → `run` → `verify` → COMMIT-2» — la pieza `CALC-0003` | `PARO-PREMISA` | `CALC-0003` no selló: `run` falló con `MergeError` contra el dato real (`c_portad.dta` trae 3 filas totalmente vacías; `_llave` las vuelve `<NA>`, `duplicated` cuenta `NA==NA` y `validate="m:1"` levanta). Tampoco nació aquí el id sucesor con `sucesor_de` que el encargo contempla: escribir un `spec.yaml` nuevo es sustancia metodológica de la clase de `E5-0`, está fuera del perímetro («No toca specs, `spec.yaml`») y el encargo dice que este acto «no decide nada». `N_corridas_selladas` se queda en 2 de 3 y `N_resultados_sellados` en 83 en vez de 83+128; `S6-L16 v1.2` no gana medición | acto sucesor que haga nacer el id sucesor de `CALC-0003` con `sucesor_de` — `FP-355` · `NC-0044` |
| Contador: «`N_corridas_selladas 0→3`, `N_resultados_sellados 0→k`» | `DECISIÓN-DE-MESA-PENDIENTE` | Los contadores siguen en 0 con dos CALC sellados, y **no es fallo de cableado**: los tres `spec.yaml` congelados por `E5-0` declaran `etiquetas.cuenta_gen2 = PENDIENTE-DE-MESA`, valor que sobrevive hasta que mesa firme en `data/corrida0/decisiones.tsv` (precedencia 1, D-1). Las 83 cifras GEN2 ya están selladas en el árbol; lo que falta es que cuenten. Control positivo con la firma simulada en memoria y nada escrito: 0→2 corridas, 0→83 resultados, 0→5 pendientes de adopción | mesa: dos filas `cuenta_gen2=SI` en `decisiones.tsv` — `FP-356` · `NC-0045` |
| «`T-REPRO` pasa de WARN a FAIL (`tests/check.py`) … con línea base que congela GEN1» | `DECISIÓN-DE-MESA-PENDIENTE` | El cambio **sí** se hizo (18 `warn`→`fail`, `[aviso]` retirado, dos falsadores migrados, suite en LÍNEA BASE VERDE), pero el FAIL **no se ha ejercido** sobre una cadena GEN2 real: mientras no exista la firma de arriba, los ramales (a)(b)(c) y (11.1) siguen con universo vacío. El único ramal con universo hoy es (11.2), inmutabilidad estructural, que sí cubre los dos sellos nuevos | el mismo de `NC-0045` — `FP-356` · `NC-0046` |
| COMPUERTA del cuerpo: «caja limpia (`limpia_arbol --reporta` vacío)» | `NO-VERIFICABLE-AQUÍ` | No se cumplió y no es cumplible desde el ejecutor: 11 worktrees vivos, 9 ramas locales ya fusionadas y 1 rama remota `fuera_de_politica` (`claude/new-session-98j16e`), **todas ajenas a esta sesión**, y `/acto` §1.0.d prohíbe decidir su borrado desde aquí. Además la cláusula es literalmente inalcanzable: el reporte nunca sale vacío, siempre imprime al menos el worktree en curso. Una de las siete cláusulas del GO queda sin cumplir; las otras seis se re-derivaron y pasaron | mesa: o `E4`/Fase IV limpia el remoto, o la cláusula se reescribe acotada a las ramas del propio acto — `SIN-ASIGNAR` · `NC-0047` |
| Perímetro: «`tests/check.py` (T35 WARN→FAIL)» | `FUERA-DE-PERÍMETRO`, declarado y ejecutado igual | Se editó además `tests/test_corrida0.py`: el propio cambio mandatado de T35 rompía sus dos falsadores, que leían `chk.WARNS`. Repararlos para que lean `FAILS + WARNS` es la terminación del cambio pedido, no alcance nuevo — siguen exigiendo lo mismo sin depender de la severidad vigente. Se declara en vez de absorberse | mesa, en el merge: si `test_corrida0.py` no debía tocarse, el cambio de T35 no era ejecutable sin dejar la suite rota |

**Premisas del encargo que no se reprodujeron contra el árbol.** (1) `dependencias_numericas_legacy_activas` vale **205**, no los `162` que el encargo declara, y **no baja por sellar**: por diseño (`ACTO GEN2-PRE-E5` P3) mide sólo lo que un consumidor **activo** lee de verdad. (2) `cierre_acto` Fase A reportó el rótulo del acto como ya censado; era falso positivo por subcadena de `GEN2-E5-0`. `E · GEN2-E5` lo censa este PR.

**`NC-0042` → `CERRADA`.** Su reserva era «los tres `medidor.py` escritos y no ejecutados»: los tres se ejecutaron aquí contra microdato real — dos corrieron y sellaron, el tercero corrió y falló, que es también ejecutarlo.

**Recibos abiertos:** `FP-353` · `FP-354` · `FP-355` · `FP-356` · `FP-357`.

## CONSUMIDO

`PR #631` — `ACTO GEN2-E5 · CALC-0001..0003`, 8/sep/2026, entorno **UBUNTU
(caja), corpus montado, microdato abierto**, rama `acto/gen2-e5-calc-0001-0003`,
`ADR-410`.

Ejecutado: `CALC-0001` **SELLADA** (54 `RESULT`, `CALC-0001--174c269a07b6`) y
`CALC-0002` **SELLADA** (29 `RESULT`, `CALC-0002--f57ad1cd8f96`), las dos con
`ejecucion.json` + `resultados.json` + `sello.sha256` y su entrada
`PENDIENTE-DE-MESA` en la propuesta citando `corrida0_resultado_id`.
`CALC-0003` **PARÓ** sin sellar. `registro` re-derivó las tres vistas
(`resultados.tsv` 390 → 473). `T-REPRO` (T35) pasó de WARN a FAIL.
**83 `RESULT` GEN2 sellados en el árbol y `N_resultados_gen2_sellados` en 0**:
falta la firma de mesa en `data/corrida0/decisiones.tsv`, que este acto no
escribe. `NC-0042` **CERRADA**. Reservas: `NC-0044`, `NC-0045`, `NC-0046`,
`NC-0047`. Firmas abiertas a mesa: `FP-353`, `FP-354`, `FP-355`, `FP-356`,
`FP-357`.
