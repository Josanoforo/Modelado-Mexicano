# ENCARGO · FP60-ADJUDICA — las dos expectativas rancias de `test_produccion_correctiva`

**Estado: `CONSUMIDO`** · SHA de redacción: `20c7dee` (dirección, contra #283 fusionado) · Ejecutado contra `20c7dee` (re-derivado al arrancar, sin movimiento) · PR: **este PR** · Cierre: `ADR-118` (re-derivado al escribir, máximo `117`; a re-derivar al fusionar) · `FP-60` → `CERRADA` · Origen: `forense/firmas-pendientes.tsv` fila `FP-60`, abierta por `ACTO REFIRMA-OPACA` (`forense/notas/2026-08-19-refirma-opaca.md` §6).

Archivado bajo `A.3` al cierre: el encargo llegó **inline** de dirección y no tenía archivo en el árbol; se archiva aquí para que el acto sea auditable contra su instrucción.

## Instrucción recibida (verbatim, tal como llegó)

> ENCARGO · FP60-ADJUDICA — las dos expectativas rancias de `test_produccion_correctiva`
>
> Redactado por dirección el 19/ago/2026 contra `20c7dee` (#283). Re-deriva al arrancar. ENTORNO ASIGNADO: NUBE (repo-only). NO lanzar en UBUNTU. Modelo: Opus. 🚫 `--freeze`.
>
> **Ley de fondo**
>
> Fila `FP-60` (abierta por REFIRMA-OPACA/#276, renumerada al fusionar): "Las dos expectativas de `test_produccion_correctiva` que quedaron rancias cuando el programa produjo su primer resultado real, y que ningún acto ha adjudicado desde entonces. El test espera 11 filas y 1[…]" — lee la fila completa y la nota `forense/notas/2026-08-19-refirma-opaca.md` (sección que las encontró): esa es tu ley. Este acto adjudica, no parcha.
>
> **VERIFICACIÓN DE EXISTENCIA (dirección, 19/ago)**
>
> 1 · ESTRUCTURA. Gobiernan: la fila FP-60, la nota de #276, y el propio test (localízalo: `git grep -l "produccion_correctiva" tests/`). 2 · CONTENIDO. ¿Alguien ya lo adjudicó? La fila sigue ABIERTA por comando (derivado hoy) y ningún encargo la reclama → NO-ENCONTRADO (universo: tablero + `forense/encargos/`, términos "FP-60", "produccion_correctiva"; 19/ago). 3 · RETRO. Las expectativas nacieron antes del primer resultado real del programa; esa asimetría es el objeto, no una brecha.
>
> ════════ ARRANQUE ════════ 1 · REPO: clon existente; ruta · `git log -1` · `git status`. 2 · SHA: contra `20c7dee`; si se movió, refresca y reporta. 3 · data/raw: no se toca — dilo y salta. 4 · ENTORNO: solo la variable, cruda; sin sonda. 5 · ESPEJO: nada del espejo; el "11 filas y 1…" de arriba se re-deriva del test y de la corrida, no de este encargo. ═════════════════════════
>
> **Tarea**
>
> 1. Deriva por comando: qué esperan hoy las dos expectativas (valores exactos, ruta:línea) y qué produce la realidad vigente (corre el test, pega la salida cruda).
> 2. Adjudica cada una con vocabulario A.4 aplicado a expectativas: RANCIA-SE-ACTUALIZA (la realidad nueva es la correcta: actualiza el valor derivándolo, con el comando en el commit — esto NO es freeze: freeze congela un fallo, esto corrige el criterio con su derivación a la vista) · VIGENTE-SE-CONSERVA (la expectativa tenía razón y la realidad tiene un defecto: entonces el defecto es el entregable, repórtalo y NO toques el test) · MAL-PLANTEADA (la expectativa compara cosas de universos o escalas distintas: se reescribe el predicado, con A-bis regla 3/4 citada).
> 3. Si las dos adjudicaciones divergen entre sí, no las colapses: una fila del test por expectativa.
> 4. Fila `FP-60` → ejecutada con este PR · ADR corto (número derivado al escribir Y al fusionar) · `hallazgos.md` una línea · nota corta · encargo `CONSUMIDO`.
> 5. Contadores de medición sobre México: 0 — dilo. Gate de cierre: `tests/check.py --baseline` VERDE en tu árbol final.
>
> **Perímetro (fuera, PARA)**
>
> El archivo del test (solo las dos expectativas adjudicadas) · tablero (FP-60) · gobernanza (ADR) · estado-programa (cascada) · hallazgos · nota · este encargo.
>
> **Concurrencia**
>
> FP58-PROPAGA-CANON corre en paralelo en nube (colisión gobernanza/tablero esperada). En la caja: COEF-UNIVERSO y CAJA-RESIDUOS — no los esperas ni te esperan.

## Ejecución — desviaciones y hechos derivados

- **Base real:** `origin/main` en `20c7dee` al arrancar, sin movimiento durante el acto.
- **`data/raw`:** no tocado — repo-only, sin microdato.
- **Entorno:** `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=cloud_default` (crudo, sin sonda).
- **Dependencia faltante, sin relación con este acto:** `jsonschema` no estaba instalado en la caja (lo importa `integrate_production.py`, no algo que este acto añadiera); instalado vía `pip install jsonschema` para poder correr el módulo de test.
- **Adjudicación:** las dos expectativas, `RANCIA-SE-ACTUALIZA`. Detalle comando por comando en `forense/notas/2026-08-19-fp60-adjudica-cierre.md`.
- **Las dos adjudicaciones no divergieron** — ambas `RANCIA-SE-ACTUALIZA`; el test ya las tenía en dos métodos separados y así quedan.
- **Contadores de medición sobre México movidos: 0.**
- **Gate de cierre:** `tests/check.py --baseline` VERDE, 21 FAIL · 117 WARN, sin `--freeze`.
