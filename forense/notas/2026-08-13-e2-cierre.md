# ACTO E2 · cierre formal de la Entrada 2 del registro de recálculo (ADR-52 A / ADR-54)

Ejecuta el encargo transferido el 13/ago/2026 ("ACTO E2 · cierre formal de la Entrada 2..."). Repo-only: no caja, no red, no `data/raw`.

## 0 · ARRANQUE — comandos y salida cruda

**REPO.** Clon existente, no se clonó uno nuevo.

```
$ pwd
/home/user/Modelado-Mexicano
$ git log -1 --format="%h %s"
fd788a9 Merge pull request #211 from Josanoforo/claude/registro-efimeros-forense-mm9sra
$ git status
On branch claude/new-session-ytzzm9
nothing to commit, working tree clean
```

**SHA.** `fd788a9` coincide exacto con la base declarada por el encargo (`origin/main`, verificado 13/ago/2026 17:12 UTC). Sin deriva que re-derivar.

**data/raw.** Ausente (`ls data/raw` → no existe). No es PARO — este acto no la necesita, confirmado.

**ENTORNO.** `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=cloud_default`. Este acto no toca red — sonda saltada, tal como el encargo instruye.

**ESPEJO.** Toda cifra de esta nota sale de este clon (`/home/user/Modelado-Mexicano`), comando a la vista en cada sección.

## 1 · Verificación de las cuatro notas fuente — no se firma desde el encargo

Leídas íntegras las cuatro: `forense/notas/2026-08-13-apertura-issp.md`, `forense/notas/2026-08-13-reapertura-52a-54-commit1-preregistro.md`, `forense/notas/2026-08-13-reapertura-52a-54-commit2-barrido.md`, y `data/apertura-issp-variables-2026-08-13.tsv`. Cada cifra citada en el registro se re-derivó del artefacto, no del encargo de cierre ni de esta nota:

```
$ python3 -c "
import csv
from collections import Counter
with open('data/reapertura-52a-54-variables-2026-08-13.tsv', encoding='utf-8') as f:
    rows = list(csv.DictReader(f, delimiter='\t'))
print('filas:', len(rows))
print(Counter(r['clasificacion_a4'] for r in rows))
print('payloads únicos:', len(set(r['id_manifiesto'] for r in rows)))
print(Counter(r['sha256_verificado'] for r in rows))
"
filas: 208
Counter({'NO-ENCONTRADO': 208})
payloads únicos: 104
Counter({'COINCIDE': 188, 'NO_VERIFICADO_EN_ESTE_ACTO': 20})
```

208 filas, 208/208 `NO-ENCONTRADO`, 104 payloads únicos (2 necesidades cada uno = 208 celdas), 188 con `sha256` verificado en este barrido (94 payloads nuevos × 2) y 20 `NO_VERIFICADO_EN_ESTE_ACTO` (los 10 payloads citados, no reabiertos, × 2) — coincide exacto con `forense/notas/2026-08-13-reapertura-52a-54-commit1-preregistro.md` §2.1-§2.3 y `forense/notas/2026-08-13-reapertura-52a-54-commit2-barrido.md` §3. Vocabulario prohibido (`no existe`/`inexistente`/`no hay fuente`) buscado sobre las 208 filas: **cero coincidencias**.

`data/apertura-issp-variables-2026-08-13.tsv` (gate): 14 filas confirmadas (7 necesidades × 2 módulos ISSP), existe en el árbol junto con `forense/notas/2026-08-13-apertura-issp.md` — gate "reporte de APERTURA-ISSP fusionado" cumplido.

SHA contra el que corrió el barrido: `1e6e6a9` (declarado en `forense/notas/2026-08-13-reapertura-52a-54-commit1-preregistro.md` §0, confirmado sin remerge de `origin/main` antes de COMMIT 2 — el merge de `origin/main` a la rama de PR #207 ocurrió *después* de COMMIT 2, trayendo trabajo concurrente ajeno al perímetro de este barrido, verificado con `git show 0d2a949 --stat`).

### 1.1 · Dos imprecisiones del encargo de cierre frente a la fuente — reportadas, no ocultas

Por instrucción explícita del propio encargo ("deriva cada cifra del archivo... si alguna no cuadra, PARA y repórtalo"), dos cosas no cuadran exactamente:

1. **"208 términos evaluados" (encargo §1) no es preciso — son 208 *celdas*** (104 payloads × 2 necesidades), no 208 términos de búsqueda. Los términos de búsqueda son una lista mucho más corta (~59 entre español e inglés, `forense/notas/2026-08-13-reapertura-52a-54-commit1-preregistro.md` §3, ninguna cuenta oficial de un solo número). El propio encargo usa correctamente "188 celdas nuevas" en su §0 pero desliza "términos" en su §1 — se usa aquí y en el registro el término correcto: **celdas**.
2. **"Los tres casos más cercanos" (encargo §0) no coincide con la fuente** — `forense/notas/2026-08-13-reapertura-52a-54-commit2-barrido.md` §5 documenta **seis** casos revisados con detalle propio por ser los más plausibles ("Estos seis se revisaron..."): Módulo de Movilidad Social/INEGI, WVS7 México, `za5900_questionnaire_development_report`, codebook CSES5, LAPOP AmericasBarometer, MOCIBA (9 ediciones). El encargo solo nombra dos. Además, la cifra de LAPOP citada por el encargo ("las 81 coincidencias... son percepción de inseguridad") es aproximada: la fuente dice **79 de 81**, no las 81. El registro (fila 2) usa las cifras exactas y cita los seis casos.

Ninguna de las dos imprecisiones cambia el resultado sustantivo (208/208 `NO-ENCONTRADO`, verificado directo del TSV arriba) ni el veredicto — son errores de resumen en el encargo de cierre, exactamente el tipo de cosa que su propia instrucción ("este encargo es el índice, no la fuente") anticipa que hay que cazar leyendo la fuente. No son PARO: no afectan ninguna cifra que entra en la línea del registro, que ya sale corregida.

## 2 · Veredicto

**`RECALCULADO — SIN CAMBIO`.** El re-examen contra un universo declarado ~20 veces mayor (104 payloads vs. los 5 instrumentos del régimen original, 0.52% de 958 programas) no encontró candidato para `sens_estatus` ni `aversion_riesgo` — 208/208 `NO-ENCONTRADO`, 2,477 coincidencias crudas revisadas término por término, cero sobrevivientes tras revisión de contexto, seis casos a priori más plausibles examinados y descartados con argumento propio (no por conteo). El cierre original de ADR-52 A/ADR-54 ("búsqueda cerrada, límite de régimen" — frase verificada en `forense/notas/2026-08-08-abrir4.md:217` y `forense/notas/2026-08-13-censo-v1_1-abrir4-enasem.md:116`, no inventada por este acto) se sostiene, ahora con universo declarado. No es `INDECIDIBLE` — el resultado es un null claro y exhaustivo sobre el universo declarado, no una imposibilidad de decidir. No es `CAMBIA` — ningún coeficiente se reclasifica, ningún candidato se promueve.

Fila `estado` de la Entrada 2 escrita en `forense/registro-recalculo-v1_0.md` con el veredicto y el universo completo en la misma línea, per §1 de ese registro.

## 3 · §2 del encargo — no se sella ADR

Veredicto `SIN CAMBIO` → por ADR-72 "Criterio de salida" y el precedente de las Entradas 0 y 4 (cerradas `SIN CAMBIO` sin ADR propio), **este acto no sella ningún ADR nuevo**. No se tocó `canon/`.

## 4 · Lo que este cierre desbloquea

La Entrada 3 del registro (los 7 veredictos `D` del Hito D) declaraba como gate "entradas 1 y 2". La Entrada 1 ya había cerrado (`ADR-74(b)`). **Con el cierre de la Entrada 2 aquí, la Entrada 3 queda desbloqueada.** Este acto no la lanza — solo lo declara.

**Contador que mueve este acto:** `registro-recalculo`, entradas cerradas **3 de 6 → 4 de 6** (antes: 0, 1, 4; ahora: 0, 1, 2, 4 — verificado releyendo la tabla completa de §1 del registro tras la edición). Ningún contador de medición sobre México se mueve por este acto: `8 de 550` (§2 del registro), `13 de 27`, `9 de 14`, `0 de 15`, `1 de 2`/`0 de 2` (llaves de identificación), `4 de 144` — todos quedan donde estaban. Este acto no tocó ningún TSV de `data/`, ni `relaciones.tsv`, ni `censo-estimabilidad-*`.

## 5 · Tres encargos archivados corregidos (`VIVO` → `CONSUMIDO`)

Detectados por el encargo de cierre, verificados aquí antes de corregir (PR fusionado + ancestro confirmado de `fd788a9`, no solo confiado del texto del encargo):

```
$ git log --oneline --grep="#205" -i | head -1
b7aa67c Merge pull request #205 from Josanoforo/vp/verifica-puertas
$ git log --oneline --grep="#198" -i | head -1
184882b Merge pull request #198 from Josanoforo/censo-v1_1-enasem
$ git log --oneline --grep="#200" -i | head -1
19d885d Merge pull request #200 from Josanoforo/wt-apertura-issp-1786589980
$ for c in b7aa67c 184882b 19d885d; do git merge-base --is-ancestor $c fd788a9 && echo "$c ancestro OK"; done
b7aa67c ancestro OK
184882b ancestro OK
19d885d ancestro OK
```

- `forense/encargos/2026-08-13-VP-verifica-puertas.md`: `VIVO` → `CONSUMIDO — PR #205`.
- `forense/encargos/2026-08-13-censo-v1_1.md`: `VIVO` → `CONSUMIDO — PR #198` (añadido como actualización, sin editar el texto original de archivo).
- `forense/encargos/2026-08-13-AI-apertura-issp.md`: `VIVO` → `CONSUMIDO — PR #200`.

Corregidos en este mismo PR, per la opción que el propio encargo de cierre ofrece ("corrígelos en este mismo PR o repórtalo") — se eligió corregir porque la verificación (PR fusionado + ancestro) ya estaba hecha y el costo declarado era de un minuto.

## 6 · Cierre

```
$ python3 tests/check.py --baseline
18 FAIL · 104 WARN
LÍNEA BASE: VERDE — nada nuevo frente a tests/baseline.json (HEAD congelado 2cb39c9ceff4abe76d895a9739242c2e5d056516)
(3 entradas de la línea base ya no aparecen — mejora, no bloquea, no baja la cifra congelada sin --freeze explícito)
```

Perímetro respetado: este acto escribió únicamente `forense/registro-recalculo-v1_0.md` (celda `estado`, fila 2), esta nota, `forense/hallazgos.md`, y los tres encargos de §5 (extensión de perímetro explícitamente ofrecida por el encargo de cierre, no una desviación silenciosa). No se tocó `canon/`, ningún TSV de `data/`, ni se selló ADR.
