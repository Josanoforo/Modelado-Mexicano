# ENCARGO · ACTO GEN2-CORTE-EDAD-1 · EL CORTE DE `edad` PARA CELDAS, POR DEFINICIÓN Y CON FIRMA — DEJA DE CITAR FP-53

**CABECERA** · redactado contra `e4f5f77`; re-deriva al abrir · **ENTORNO: NUBE** — lee FD/catálogos del repo y del inventario; no abre microdato; si un FD necesario solo está en `data/raw`, esa pieza se declara NO-ACCESIBLE-AQUÍ y se rutea a caja, no se adivina · **COMPUERTA:** el merge de `GEN2-TRAMITE-4` (tablero y `gobernanza` compartidos) · MODELO SUGERIDO: Opus (equivalencia por definición sobre tres FD es juicio) · FP/ADR/NC: deriva al cierre, no heredes · vehículo: `/acto`.

**FIRMA DE MESA, verbatim (cerrada en conversación de dirección el 16/sep/2026; su archivo aquí la sella):**
> D4 · Corte de `edad` para celdas: "Se adoptan como corte del modelo los cuatro tramos del árbitro — 18-29 / 30-44 / 45-59 / 60+ — sobre edad en años cumplidos, verificados por definición en ENVIPE 2025, ENCIG 2025 y ENIF 2024. Es un objeto distinto de FP-53 (umbral binario 'joven' 15-29, firmado el 18/ago para otro uso): `CORTES_C1.edad` deja de citar FP-53 como bloqueador. Se autoriza editar el dato sellado bajo ADR-100(2) con este ADR como fuente."

**VERIFICACIÓN DE EXISTENCIA (A.8, dirección, contra `e4f5f77`):**
- (1) Estructura: `milpa/src/celdas.py:74-84` (`CORTES_C1`, dato sellado bajo ADR-100(2), "el catálogo es su sitio de sello": `milpa/catalogo-momentos-v0_1.md` §3), `data/crosswalk-ejes-arbitro-modelo-v1_0.tsv` (fila `edad`), Dominio 7 (ADR). Cubren.
- (2) Contenido: `celdas.py:78` → `"edad": None,  # PENDIENTE — FP-53` (sellado el 17/ago, ADR-100). FP-53 → **FIRMADA el 18/ago** (ADR-111(b): joven = 15-29, convención INEGI/ENOE; ejecutada por ADR-116, PR #281): el bloqueador citado está resuelto desde hace un mes y es **otro objeto**. Crosswalk fila `edad` → `PENDIENTE-FP-53`, tramos del árbitro `18-29 | 30-44 | 45-59 | 60+` en ENIF 2024 / ENCIG 2025 / ENVIPE 2025. `grep "tramos_edad\|corte de edad\|CORTES_C1.*edad" forense/no-corrido.tsv forense/firmas-pendientes.tsv` → 0 sobre 2 archivos: **NO-ENCONTRADA** ninguna fila sobre el corte de celdas. Inventario: `envipe2025` tiene `catalogos/edad.csv` (20 filas con "edad"); la función del árbitro vive en `tools/ejes_maestra35_l1.py::tramos_edad`.
- (3) Cobertura retroactiva: `CORTES_C1` (17/ago) es anterior a FP-53 (18/ago) y al crosswalk (16/sep); el comentario `PENDIENTE — FP-53` nunca se actualizó porque ningún acto posterior tocó el archivo sellado.

### PIEZAS

**P1 · Por definición, en tres FD (A.15c: por archivo, no por nombre).** Para ENVIPE 2025 (persona, la base que `CORTES_C1` corta), ENCIG 2025 y ENIF 2024: variable de edad, unidad (años cumplidos), rango válido, códigos de no-respuesta, y la función exacta de `tools/ejes_maestra35_l1.py::tramos_edad` aplicada a cada una. Veredicto por instrumento: `EQUIVALENTE` (misma variable en años cumplidos, mismos cortes, mismos no-respuesta) / `MAPEO-N-A-1` (tabla explícita) / `NO-EQUIVALENTE` (con la diferencia escrita). Si un instrumento reporta edad en tramos y no en años, se dice y ese instrumento queda `MAPEO` o `NO-EQUIVALENTE`.

**P2 · El dato sellado, editado con firma.** `celdas.py`: `"edad": ("18-29", "30-44", "45-59", "60+")` con comentario que cite el ADR de este acto y la variable ENVIPE 2025 de P1; la línea `PENDIENTE — FP-53` no se borra: recibe enmienda fechada al lado ("resuelto por ADR-<este>; FP-53 era otro objeto"). `firma_m2` se extiende con el ADR nuevo, sin borrar el anterior. Tests de `celdas` en verde; si `pi.py`/`motor.py` consumen el número de categorías de `edad`, se reporta qué cambia y se deja **sin tocar** (sucesor), no se adapta en silencio.

**P3 · El crosswalk.** Fila `edad`: `corte_modelo` = los cuatro tramos; `veredicto` = el de P1 (por instrumento, si difieren); `evidencia` = archivo:variable de cada FD; `firma` = la D4 verbatim. Nota al pie desambiguando FP-53 (joven 15-29, 9 sitios, ADR-111/116) del corte de celdas.

**P4 · Filas y hallazgo.** Fila `decisiones.tsv` (objeto `corte-edad-celdas`); FP nueva **nace FIRMADA** con D4; línea en `hallazgos.md`: "`edad espera FP-53` se heredó un mes después de resuelta (17/ago → 16/sep, tres documentos); patrón: un bloqueador citado por nombre no se re-verifica cuando cambia de estado" (A.13 de clase distinta: no un negativo, un positivo vencido). NC-0239 recibe enmienda fechada: las 16 celdas de `edad` del árbitro pasan de "sin correspondencia" a consumibles por el marcador **cuando** NC-0275 (emisor = árbitro) esté resuelta.

**PERÍMETRO Y CONCURRENCIA:** `milpa/src/celdas.py` · `tests/test_celdas*.py` (solo si el cambio los rompe; se reporta) · `data/crosswalk-ejes-arbitro-modelo-v1_0.tsv` (fila `edad`) · `data/corrida0/decisiones.tsv` · `forense/firmas-pendientes.tsv` · `forense/no-corrido.tsv` · `forense/hallazgos.md` · `canon/gobernanza-v1_15.md` (ADR) · nota de cierre · cascada. **No toca** `pi.py`, `motor.py`, `matriz.py`, el marcador, `tramite.yaml`, FP-53, las celdas-D ni el piloto (que usa los mismos tramos del árbitro sin necesitar el corte del modelo). En paralelo: **GEN2-CELDA-D-PILOTO-1** (caja) — sin archivo común salvo el tablero al cierre; **quien fusione segundo renumera**. «Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.»

**CONTADOR:** cero mediciones, dicho sin disfraz; convierte 16 celdas del árbitro de `SIN-CORRESPONDENCIA` a consumibles (condicionadas a NC-0275). **LO QUE NO HACE:** no toca FP-53 · no rediseña el marcador · no adapta `pi.py`/`motor.py` · no mide nada. **SUCESOR:** el marcador por segmento, gated a NC-0275 resuelta y al piloto. **CIERRE:** cascada + `## NO-CORRIDO / RESERVAS` + `## CONSUMIDO`.

---

## NO-CORRIDO / RESERVAS

- **qué:** «P1 · … códigos de no-respuesta …» — la cláusula «**mismos no-respuesta**» que el propio encargo pone como condición de `EQUIVALENTE`, en los **tres** instrumentos.
  **por qué:** `NO-VERIFICABLE-AQUÍ` — entorno NUBE, `tools/entorno.py` → `acceso_corpus.montado=NO`, `archivos_examinados=0`. Negativo con su universo declarado (A.13): **0** filas con `texto_reactivo` para una variable `EDAD*` de `envipe2025`/`encig2025` sobre los **cuatro** inventarios de reactivos; `data/inventario-fd-v1_1.tsv` cubre **30** payloads y no incluye ninguno de los dos (control positivo: `envipe2013` → 22, `enif2024 EDAD*` → 2). El catálogo `tsdem_envipe2025/catalogos/edad.csv` está `PRESENTE_EN_DATA_RAW`. Censo de raíz vigente (`ADR-326`): `forense/censo-raiz/2026-09-15-cron-1047.txt` (524 en disco · 62 nuevos · 462 ya registrados). La cabecera del encargo ordena esta ruta: se declara y se rutea, no se adivina.
  **impacto:** los tres veredictos `EQUIVALENTE` quedan **con reserva escrita, no cerrados**. Si en caja 97/98 resultaran edades reales y no códigos de no especificada, el tope de 96 de `tramos_edad` estaría truncando población viva y ese instrumento bajaría a `NO-EQUIVALENTE`, con impacto sobre el corte sellado hoy y sobre `FP-383`.
  **sucesor:** `NC-0299` — acto de CAJA que abra los tres catálogos y confirme código por código. Mismo patrón que `NC-0270`.

- **qué:** «`tests/test_celdas*.py` (solo si el cambio los rompe; se reporta)» — el test que el sello **sí** rompe no es uno de ésos.
  **por qué:** `FUERA-DE-PERÍMETRO` — quien rompe es `tests/test_motor_clases.py::test_corte_pendiente_no_se_inventa` (`pendientes` pasa de `('edad','migracion')` a `('migracion',)`; `celda(edad='30-44')` deja de lanzar `CortesNoSellados`, que es exactamente lo que el acto autoriza). Medido: `python3 tests/test_motor_clases.py` → «T-MOTOR-CLASES: 1 fallo(s)», `RC=1`. Ese archivo **no está** en el perímetro, y `tests/test_celdas_d.py` —el que sí está— sigue en verde y **ni siquiera menciona** `edad` ni `CORTES_C1`. El encargo manda PARAR antes que escribir fuera de la lista, así que se deja **sin tocar** y se reporta: «no se adapta en silencio».
  **impacto:** **la rotura es SILENCIOSA para la maquinaria de cierre.** `tests/check.py` **no** ejecuta ese archivo (`grep motor_clases tests/check.py` → **0**) y `.github/workflows/verify.yml` tampoco lo cablea, así que ni la línea base ni el CI se ponen en rojo por esto. Un desarrollador que corra el archivo a mano **sí** lo ve rojo.
  **sucesor:** `NC-0300` — acto con `tests/test_motor_clases.py` en perímetro que reescriba la aserción contra el estado sellado citando `ADR-536`; y, aparte, decisión de mesa sobre si ese archivo debe entrar a la lista cableada de `verify.yml`, dado que hoy **nadie lo corre en automático**.

- **qué:** «si `pi.py`/`motor.py` consumen el número de categorías de `edad`, se reporta qué cambia y se deja **sin tocar** (sucesor)» — ejecutado como manda, sobre **tres** consumidores, no dos.
  **por qué:** `FUERA-DE-PERÍMETRO` — el encargo declara explícitamente «**No toca** `pi.py`, `motor.py`, `matriz.py`», y `corrida0.py`/`rutas_sin_candidato.py` no aparecen en la lista. Medido: **(i)** `milpa/src/pi.py:62-64` — `construir_pi` **sigue parando** (`migracion` sigue pendiente), pero su mensaje cita `FP-53` para cualquier eje pendiente y ahora dice «cortes PENDIENTE: `('migracion',)`. `FP-53` tiene abierta esa deuda», cuando `FP-53` **nunca** fue el bloqueador de `migracion` — **es el mismo defecto que este acto nombra, en el archivo que el perímetro prohíbe tocar**; **(ii)** `tools/corrida0.py::_consumidores_cortes_pi` lee `CORTES_C1` en vivo y la fila derivada de `edad` pasa de `clase_legacy=PENDIENTE·FP-53` a `SELLADO·M2` (verificado corriéndolo), pero su `acto_legacy` está cableado a `'ACTO LANE-A-E0-E5 C1'`, de modo que el registro derivado **atribuye el sello al acto equivocado**, y su docstring sigue diciendo «cuatro ejes con corte sellado y dos `None`» cuando ya son cinco y uno; **(iii)** `tools/rutas_sin_candidato.py:198` trae cableada la cadena «`CORTES_C1` declara cuatro cortes y deja edad/migración pendientes», falsa desde este acto.
  **impacto:** el registro derivado de consumidores y las rutas sin candidato quedan con prosa y **atribución** vencidas. Ninguna cifra de medición se mueve; ningún test se pone rojo por esto.
  **sucesor:** `NC-0301` — acto con los tres archivos en perímetro que los alinee con `ADR-536`.

- **qué:** «NC-0239 recibe enmienda fechada: … consumibles por el marcador **cuando** `NC-0275` (emisor = árbitro) esté resuelta» — la fila citada por número **no existía**.
  **por qué:** `PARO-PREMISA` — re-derivado contra `e4f5f77` al abrir: el máximo real de `forense/no-corrido.tsv` era **`NC-0274`**, y «emisor = árbitro» no aparece como vocabulario en ese archivo (grep → 0). No se resolvió por inferencia (A.4) ni se dejó colgando: se **abre** `NC-0298` con el bloqueador **ya medido** que el encargo presupone —`RESULT-AGGOLA-MARCO-TIENE-COLUMNA-SEGMENTO = NO`, el marco del árbitro no publica columna de segmento—, citado de `NC-0239`/`FP-376`, donde hasta hoy vivía sin id propio. La enmienda a `NC-0239` **sí** se escribió, y la condición se conserva íntegra.
  **impacto:** las 16 celdas de `edad` **no** son consumibles hoy pese al corte sellado. El corte levanta **uno de tres** bloqueadores; quedan `FP-376`/`NC-0269` (firma de mesa sobre el crosswalk) y `NC-0275`. El conteo baja de tres a dos, **no a cero**.
  **sucesor:** `NC-0298` — acto que haga que el marco del árbitro publique columna de segmento, o decisión de mesa sobre por qué vía se marca sin ella. **Concurrencia:** `GEN2-CELDA-D-PILOTO-1` corre en paralelo (caja) y puede tomar ese número primero — **renumera quien fusiona segundo**.

  **Adenda al cerrar (renumeración):** entre la apertura y el cierre, `main` avanzó **69 commits** y `ACTO GEN2-CELDA-D-CAREO-1` tomó `NC-0275`…`NC-0282`. Las cuatro filas de este acto se renumeran a **`NC-0298`–`NC-0301`** (y `FP-378`→**`FP-383`**, `ADR-533`→**`ADR-536`**) — *renumera quien fusiona segundo*. Efecto secundario que conviene dejar dicho: la cita `NC-0275` del encargo, que al abrir era **colgante**, al cerrar resuelve a una fila **real pero de otro acto**. El bloqueador del lado emisor es **`NC-0298`**.


- **qué:** `COMPUERTA: el merge de GEN2-TRAMITE-4` — el rótulo citado **no existe**.
  **por qué:** `PARO-PREMISA`, **resuelto sin paro y sin inferencia**. `canon/registro-rotulos.tsv` tiene `MAESTRA38-TRAMITE-3/4/5/6` y, en la serie `GEN2`, `GEN2-TRAMITE-FIRMAS-1`, `GEN2-TRAMITE-TABLERO-1`, `GEN2-TRAMITE-TABLERO-2` y `GEN2-TRAMITE-BANDEJA` — **ningún `GEN2-TRAMITE-4`**. No se eligió «el más reciente que calce»: se verificó **por producto** que la compuerta se cumple **bajo toda lectura candidata**, que es prueba más fuerte que escoger una — `git cat-file -e origin/main:<ruta>` da presentes los cinco encargos archivados; `ADR-369` (`MAESTRA38-TRAMITE-4`, `PR #573`) presente en `gobernanza` de `origin/main`; `forense/tablero/TABLERO-PROGRAMA.md` presente; cero `GEN2-TRAMITE-4` en `forense/encargos/cola/`; y **cero PR abiertos de trámite** (`#833`, `#832`, `#827`, ninguno lo es). El propósito declarado —que nada de trámite quede sin fusionar sobre el tablero y `gobernanza` compartidos— **está satisfecho**.
  **impacto:** ninguno sobre el acto; el acto arrancó. Lo que queda es que el encargo cita un rótulo inexistente y eso **no se corrige en el encargo**, que es verbatim por A.3.
  **sucesor:** asentado en `ADR-536` (bloque **Gate verificado**) y en `forense/hallazgos.md`. `SIN-ASIGNAR` en cuanto a acto propio: no hay deuda material que perseguir, sólo el registro.

  **Enmienda fechada 17/sep/2026, antes de fusionar — el terreno cambió durante la ejecución:** `GEN2-TRAMITE-4` **ya existe**, ejecutado por otra sesión en `PR #848`, **abierto y sin fusionar**. Al abrir este acto el rótulo no era hallable en ninguno de los cuatro sitios donde se busca, porque esa sesión aún no había empujado — el `0.c` del ARRANQUE no puede ver un acto en vuelo sin rama remota. **Tomada al pie de la letra, la compuerta NO está cumplida**, y se dice en vez de disimularse. Los dos PR comparten **ocho** archivos más `data/crosswalk-ejes-arbitro-modelo-v1_0.tsv`, que es el que la `P3` de este acto escribe: **renumera quien fusiona segundo**, la misma regla que este acto ya pagó dos veces. Asentado en `ADR-536` (bloque **Gate verificado**), que es donde vive el registro, no sólo en un comentario del PR.

- **qué:** la línea base de la suite — **fila corregida al cierre: mi primer diagnóstico era falso y el CI lo falsó.**
  **por qué:** `NO-VERIFICABLE-AQUÍ` en su primera lectura, **y esa lectura estaba mal**. Se declaró «el `ROJO` de `T16` es pre-existente en `origin/main`» apoyándose en un control que parecía fuerte: un worktree temporal sobre `origin/main` **limpio**, que dio `4 FAIL · 4361 WARN` con la misma entrada `T16`. **El CI lo refutó** (`PR #846`, run `35173913746`): `origin/main` está **verde** en CI y el rojo era **de este PR**. Causa, reproducida: a este contenedor le faltaba `jsonschema` —declarada en `requirements.txt`—, así que `T-ALTA-RELACION` salía `NO-CORRIDO` y sumaba **un WARN que el repo no tiene**; el control heredó ese sesgo porque corría en el mismo contenedor, aunque el control en sí estuviera bien diseñado. Instalada la dependencia, la corrida local reproduce la de CI **exactamente**.
  **impacto:** el rojo **sí era atribuible a este acto**: sus 4 `NC` y 1 `FP` suben el total de `4360` a `4365` WARN, y la declaración «vigente» de `ADR-533` quedaba vencida por exactamente esas 5. **Corregido en el mismo acto, no diferido:** la declaración de `ADR-533` se marca `{cita-historica}` y la cifra vigente pasa a `ADR-536`, **medida**. No se silenció nada — `tests/baseline.json` sin tocar, `T38` sin desactivar. Estado final: `python3 tests/check.py --baseline` → **`3 FAIL · 4365 WARN`, `LÍNEA BASE VERDE`**.
  **sucesor:** dos, ninguno de ellos este acto. (1) `PARA-v2.13` ya abierto en `forense/hallazgos.md` — que `T03` ignore los archivos gitignorados, o que declare que no lo hará. (2) **`tools/entorno.py` no censa `jsonschema`** aunque `requirements.txt` la declare y la suite la use: por eso el ARRANQUE de este acto reportó el entorno como sano y el sesgo pasó desapercibido hasta el CI. Mismo hallazgo que `ACTO GEN2-EMISOR-ESTADO-1` (`PR #844`) el mismo día, en sesión independiente — **encontrado dos veces, luego no es anécdota**. `SIN-ASIGNAR` a acto propio.

---

## CONSUMIDO

Ejecutado por **`ACTO GEN2-CORTE-EDAD-1`** (`ADR-536`), 17/sep/2026, entorno **NUBE** (`cloud_default`, Opus; `acceso_corpus.montado=NO`, `archivos_examinados=0`, red `000`), vehículo `/acto`.

**PR [#846](https://github.com/Josanoforo/Modelado-Mexicano/pull/846)** · rama `claude/new-session-p176hs` · base declarada por el encargo `e4f5f77`, base real al abrir `e4f5f77` (sin divergencia), fusionada `origin/main` (+69 commits) antes de cerrar.

**Piezas:** P1 `EJECUTADA` (verificación por definición y por archivo sobre los tres FD; veredicto `EQUIVALENTE` para los tres, con la cláusula de no-respuesta declarada `NO-ACCESIBLE-AQUÍ` y ruteada a caja) · P2 `EJECUTADA` (`CORTES_C1.edad` sellado; la línea `PENDIENTE — FP-53` enmendada, no borrada; `firma_m2` extendida sin borrar `ADR-100(2)`) · P3 `EJECUTADA` (fila `edad` del crosswalk a `EQUIVALENTE`, con la firma D4 verbatim y la nota al pie que desambigua `FP-53`) · P4 `EJECUTADA` (`decisiones.tsv` objeto `corte-edad-celdas`; `FP-383` nace `FIRMADA`; línea en `hallazgos.md`; `NC-0239` enmendada).

**Numeración final** (renumerada al fusionar segundo): `ADR-536` · `FP-383` · `NC-0298`/`NC-0299`/`NC-0300`/`NC-0301`.

**Contador: cero mediciones.** `FP-53` intacta y no re-abierta. `pi.py`, `motor.py`, `matriz.py`, el marcador, `tramite.yaml`, las celdas-D y el piloto, sin tocar.

**Sucesor:** el marcador por segmento, gated a `NC-0298` resuelta y al piloto (`GEN2-CELDA-D-PILOTO-1`).

**Adenda al cerrar, segunda sincronización (17/sep/2026, `origin/main = 8d2a985`).** `ACTO GEN2-TRAMITE-4` **fusionó** (`PR #848`), así que: **(a)** la `COMPUERTA` de este encargo queda **cumplida al pie de la letra**, verificada por producto — el encargo de ese acto está en `origin/main` y su rótulo censado en `registro-rotulos.tsv`. Las tres lecturas sucesivas del rótulo —inexistente al abrir, existente y sin fusionar a media ejecución, fusionado al cerrar— quedan las tres escritas, en orden y sin reescribir ninguna: *el estado de una compuerta es una película, no una foto*. **(b)** Este acto fusionó segundo y **renumeró**: `ADR-536` · `FP-383` · `NC-0298`–`NC-0301`. **(c)** Efecto sustantivo que **mejora** el contador y se dice medido: `#848` trae la firma **D1** de mesa sobre `FP-376`, que pasa a `FIRMADA` y **cierra `NC-0269`**. De los **tres** bloqueadores de las 16 celdas de `edad` queda **UNO** — sólo `NC-0298`, el lado emisor. **De tres a uno en un solo día, por dos actos independientes que no se conocían al empezar.** **(d)** Suite sobre el árbol fusionado: **`3 FAIL · 4374 WARN`, `LÍNEA BASE VERDE`**.

El encargo no se borra ni se edita en ningún otro punto: su cuerpo (líneas 1-25) es **verbatim por A.3**, verificado idéntico al original recibido. Las secciones `## NO-CORRIDO / RESERVAS` y `## CONSUMIDO` son del ejecutor, añadidas al cerrar.
