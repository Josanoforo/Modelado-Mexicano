# ACTO PROD-P638 — el expediente de la θ ya calculada: reproducción independiente de `obligación_medida` por el motor formal

**Acto:** ACTO PROD-P638 · **Entorno:** CAJA con corpus (Ubuntu/WSL local) · **SHA de redacción:** el encargo declara «al fusionar #1» (PROC-11) como gate; verificado contra el árbol real abajo · **Depende de:** `data/curacion-registro/celdas-d/G5.obligacion_medida.conducta.yaml` (ACTO PROC-11) y `forense/notas/2026-08-13-enasic-split-verificacion.md` §1.5/§2.2 (ENASIC-SPLIT).

Este commit (1) fija la especificación de producción congelada, ANTES de ejecutar `produce.py`. No corre el motor. Ningún resultado vive en este commit.

## §0 · ARRANQUE

1. **Cuerpo del encargo.** `forense/encargos/2026-08-13-MOTOR-COND-v2-encargos-finales.md` §3 (ACTO PROD-P638 v2). Más dos ajustes vigentes recibidos en el veredicto de dirección posterior al archivo de PROC-11 (no re-escritos en el archivo, verbatim en el encargo de dirección): (a) el GATE literal del encargo (`ls .../celdas-d/ | grep -c obligacion`) está roto — corregido a `ls data/curacion-registro/celdas-d/G5.obligacion_medida.conducta.yaml` + `grep -c MEDIDO·NACIONAL`; (b) la ranura D3, "corre sin D3" al cerrar PROC-11, quedó **RESUELTA** después (`ADR-79(a)`, ejecutada por `PROC-10-bis`) — al reproducir, este acto escribe también la entrada de `obligación_medida` con clase `MEDIDO·NACIONAL` → contador `11 de 15`.
2. **REPO.** Worktree dedicado `~/mm-prod-p638`, rama `prod-p638`, creado con `git worktree add ... origin/main` desde `/home/pc0/Modelado-Mexicano`. `git worktree add` emitió el error conocido de contención de `.git/config` en esta máquina (`Device or resource busy`, ver `[[project_modelado_mexicano_git_config_contention]]`) — verificado sin pérdida: `branch 'prod-p638' set up to track 'origin/main'` completó, `HEAD` = `84b2acf` = `origin/main` exacto, sin divergencia.
3. **Gate, verificado por comando, no heredado del encargo original:**
   ```
   $ ls data/curacion-registro/celdas-d/ | grep -c obligacion_medida
   1
   $ grep -c "MEDIDO·NACIONAL" milpa/procedencia.yaml canon/modelo-decision-v4_0.md
   milpa/procedencia.yaml:5
   canon/modelo-decision-v4_0.md:8
   ```
   Ambos ≥1. **PROC-11 fusionado** (`PR #224`, commit de merge `560d305`, confirmado ancestro de `origin/main` por `git merge-base --is-ancestor`) y **PROC-10-bis fusionado** (`PR #227`, merge `f3f05d3`, mismo método) — la clase séptima `MEDIDO·NACIONAL` existe, sellada, con la primera entrada (`norma_de_género`) ya dentro. Contador actual, verificado en `README.md:37`: **`10 de 15`**.
4. **Colisión — ninguna detectada.** `grep -in "prod-p638" forense/hallazgos.md forense/notas/*.md` → 15 apariciones, las quince son referencias hacia adelante de `PROC-11`/`PROC-10-bis` nombrando a este acto como su sucesor pendiente (verificado por lectura directa, ninguna es una ejecución previa). Ningún `forense/notas/2026-08-13-prod-p638.md` existía antes de este commit. `git branch -a | grep -i p638` → solo la rama propia de este acto.
5. **Línea base, corrida ANTES de tocar nada salvo la especificación de §1:** `python3 tests/check.py --baseline` → **24 FAIL · 119 WARN — LÍNEA BASE: VERDE**, contra `tests/baseline.json` head congelado `0ad9b7b759e138b251129c639f6ef943d6ee0fe7` (idéntico al cierre de `PROC-10-bis`).
6. **Concurrencia.** El encargo declara «concurrencia con INV-DESCMX declarada: archivos disjuntos — él toca `tests/corpus.py`, tú no tocas `tests/`». Verificado: `INV-DESCMX` (`PR #221`) ya está `MERGED`; este acto no toca `tests/` en ningún punto de su perímetro.
7. **Qué encuentra la celda-D, leído directo (no heredado de memoria):** `data/curacion-registro/celdas-d/G5.obligacion_medida.conducta.yaml` — `estado_operativo: PENDIENTE`, razón única «expediente por la vía -- PROD-P638»; `requiere_decision_mesa: false` (ADR-75(b) ya firmó todo lo que había que firmar sobre esta medida); `production_spec_refs: []` **vacío a propósito** — el propio archivo declara que poblarlo con una ruta inventada sería el defecto que `documentacion_fuente` (ADR-70(b)) existe para impedir. El campo `resultado` cita, verbatim de `forense/notas/2026-08-13-enasic-split-verificacion.md` §2.2, las cinco proporciones e IC que este acto debe reproducir:

   | código | razón | n | proporción | IC95 |
   |---|---|---|---|---|
   | 1 | obligación | 479 | 0.0765 | [0.0661, 0.0868] |
   | 2 | decisión familiar | 366 | 0.0671 | [0.0579, 0.0763] |
   | 3 | ser la (el) única(o) que podía | 193 | 0.0356 | [0.0286, 0.0426] |
   | 4 | petición de quien necesita el cuidado | 72 | 0.0123 | [0.0083, 0.0162] |
   | 5 | iniciativa propia | 4,567 | 0.8086 | [0.7918, 0.8254] |

   n=5,677, suma de pesos (`FAC_CUI`) = 31,652,127, 148 estratos (0 singleton), 861 UPM.

## §1 · Especificación de producción congelada — commit 1, antes de correr `produce.py`

**Advertencia epistémica, declarada por lo que es.** Este acto no es un pre-registro ciego de un resultado nuevo: las cinco proporciones de §0.7 ya son conocidas, calculadas y verificadas por `ENASIC-SPLIT` (`tests/svystat.py::prop_ultimate_cluster`, declarado en su §1.5 antes de ver el dato en ESE acto). Lo que PROD-P638 pre-registra aquí es el **método de reproducción independiente** — el motor formal `tools/curador_registro/produce.py::taylor_distribution` — y el **criterio de éxito**, ambos fijados ANTES de ejecutar `produce.py` en este acto. La independencia no es de los números (ya vistos), sino del camino de cómputo: código distinto (`produce.py` vs. `svystat.py`), arquitectura distinta (blindaje de campos semánticos + verificación de hash + reproducción bit a bit por un "supervisor" que ignora `hashes.json` del analista — `hashes_analista_confiados: false`, constante fija de `integrate_production.py`, no un flag que este acto active).

**Especificación registrada** en `data/curacion-registro/especificaciones-produccion.json` como `ESP-OPACA-D-d800e103` (cuarta entrada del archivo — las tres previas, `A`/`B`/`C`, re-derivadas byte a byte por `prepare_production.py` en este mismo commit, `git diff` confirma **cero cambios** en sus `especificacion-recibida.json`). Campos materiales:

- **Variable:** `P6_38`, tabla `TPOB_CUI`, ENASIC 2022. **Codificación:** 5 categorías (obligación / decisión familiar / única que podía / petición / iniciativa propia), verificado de nuevo contra el microdato en este acto (`Counter` sobre las 5,677 filas de `TPOB_CUI.csv` dentro de `enasic_2022_bd_csv.zip`): `{'5': 4567, '1': 479, '2': 366, '3': 193, '4': 72}` — coincide exacto con `n_categoria` de §0.7, verificación independiente antes de correr el motor formal.
- **Población/universo:** población cuidadora, `FILTRO 6.10` — verificado `FILTRO6_10='1'` en el 100% de las 5,677 filas en este acto (no heredado).
- **Ponderador:** `FAC_CUI`. **Diseño:** `EST_DIS`/`UPM_DIS`, viven directo en `TPOB_CUI`.
- **`input_path`:** `/home/pc0/mm-corpus/raw/enasic2022/enasic_2022_bd_csv.zip` — **mismo archivo** que usó `norma_de_género` (`ESP-OPACA-B`), verificado por hash: `8a5e8c5ed2dcda6e25dfe2dd630c0ac7273e0736e7b99662a15a4ef68c3ab36e`, idéntico. `input_member`: `TPOB_CUI.csv` (vs. `TPER_ELE.csv` de la celda hermana).
- **Evidencia neutral:** reutiliza `data/curacion-registro/expedientes-produccion/evidencia-neutral-produccion.json#ENASIC_2022` **sin editar ese archivo** — `hash_evidencia_neutral` idéntico al de `ESP-OPACA-B` (`94427f99…`), verificado que el archivo no cambió. Razón de no editarlo: `ADR-70`, nota de alcance sobre (d), documenta que incluso una regeneración puramente aditiva de un artefacto compartido (`snapshot-t0.json`, mismo principio aplica a `evidencia-neutral-produccion.json`) puede tronar la reproducción supervisada de expedientes ya sellados (`ESP-OPACA-A/B/C`) — no se toca. La cobertura de P6_38 (contenido nuevo, no solo el hash del zip) se documenta en su lugar vía `documentacion_fuente` (ADR-70(b), campo nuevo, poblado por primera vez en este acto: ficha RNM `inegi.org.mx/rnm/index.php/catalog/922`, consultada de nuevo en vivo hoy — `curl` devolvió `200`).
- **`supervisor_link`:** `relacion_id: REL-fe202a3fa76f0516a6e27f8b` (misma relación que `norma_de_género` — `data/curacion-registro/necesidad-objeto-modelo.tsv` confirma `N13 → G5.familismo_obligacion` como necesidad única, no desdoblada; la propia nota de esa relación cita `P6_38` como candidato secundario de la misma evidencia) · `objeto_modelo_origen: G5.familismo_obligacion` (mismo criterio) · `requiere_decision: NO` (`requiere_decision_mesa: false` en la celda-D — ADR-75(b) ya cerró toda reserva sobre el constructo).
- **`snapshot_t0_sha256`/`baseline_sha256`:** inyectados por `prepare_production.py` desde `data/curacion-universo/snapshot-t0.json`/`data/curacion-registro/baseline.json` **sin regenerarlos** — idénticos a los que ya usan `ESP-OPACA-A/B/C` (`89f4c3a49c00c0e1…`), mismo directorio `t0-89f4c3a49c00c0e1/`.

**Criterio de éxito, fijado antes de correr `produce.py`:** las cinco proporciones e IC que produzca el motor formal deben coincidir, a los decimales que `forense/notas/2026-08-13-enasic-split-verificacion.md` §2.2 declara (4 decimales), con la tabla de §0.7. Aviso pre-declarado sobre una fuente de discrepancia NO descalificante: `produce.py::taylor_distribution` usa cuantil normal `z=1.96` (fijo, línea 132/133 del propio motor); `svystat.py::prop_ultimate_cluster` (el método que sí produjo §0.7) usa `z=1.959963985` — diferencia de `0.00004`, declarada por adelantado en el propio §1.5 de `ENASIC-SPLIT` para que un desacuerdo en la última cifra del error estándar/IC no se lea, por sí solo, como no-reproducción; se evalúa si esa diferencia efectivamente mueve algún dígito a 4 decimales.

**REPRODUCE ⇒** celda `LISTO` + fila 12 de `produccion-modelo.tsv` (`CALCULO_REPRODUCIBLE`) + entrada `obligación_medida` en `milpa/procedencia.yaml` bajo `MEDIDO·NACIONAL` + contador `11 de 15` — la firma condicional D1 ya dada en el encargo autoriza escribir esto sin volver a mesa. **NO-REPRODUCE ⇒** celda permanece `PENDIENTE`, la discrepancia exacta es el entregable, expediente a mesa, contador no se toca.

**El primer resultado que produzca `produce.py` sobre esta especificación es el que se reporta — no se reintenta si no coincide.**

---

## §2 · Commit 2 (resultado) — sin editar §0-§1 arriba

### §2.1 · Verificación independiente contra el microdato, antes de correr el motor formal

Antes de ejecutar `produce.py`, este acto abrió `TPOB_CUI.csv` de primera mano (`zipfile`/`csv.DictReader`, mismo mecanismo que el motor) y contó `P6_38` directo: `{'5': 4567, '1': 479, '2': 366, '3': 193, '4': 72}` — coincide exacto con los `n_categoria` de §0.7. `FILTRO6_10='1'` en el 100% de las 5,677 filas, confirmado de nuevo.

### §2.2 · Ejecución del pipeline

```
$ python3 tools/curador_registro/prepare_production.py --config data/curacion-registro/especificaciones-produccion.json \
    --snapshot data/curacion-universo/snapshot-t0.json --baseline data/curacion-registro \
    --output-root data/curacion-registro/expedientes-produccion/t0-89f4c3a49c00c0e1
{"ok": true, "specifications": [...4 rutas, A/B/C re-derivadas byte a byte...]}

$ python3 tools/curador_registro/produce.py --spec .../ESP-OPACA-D-d800e103/especificacion-recibida.json \
    --output-dir .../ESP-OPACA-D-d800e103
{"ok": true, "especificacion_id": "ESP-OPACA-D-d800e103", "variables": 1, "calculos_reproducibles": 1, "no_determinado": 0, "cegado": true}

$ python3 tools/curador_registro/integrate_production.py --config especificaciones-produccion.json \
    --snapshot snapshot-t0.json --baseline data/curacion-registro --analyst-root expedientes-produccion \
    --output produccion-modelo.tsv
{"ok": true, "productos": 12, "reproducibles": 12, "no_determinado": 0,
 "RELACIONES_CON_CALCULO_DESCRIPTIVO": 3, "RELACIONES_LISTAS_PARA_USO_MODELO": 2,
 "hashes_analista_confiados": false}
```

Segunda pasada, `--validate-existing`, sobre el archivo ya escrito: idéntico (`ok: true, productos: 12, reproducibles: 12`) — confirmación independiente de que lo commiteado es exactamente lo que el supervisor deriva.

### §2.3 · Resultado — REPRODUCE

`resultado.tsv` de `ESP-OPACA-D-d800e103`, `estado=CALCULO_REPRODUCIBLE`:

| código | razón | n | proporción (motor formal) | proporción (nota, `svystat.py`) | IC95 (motor formal) | IC95 (nota) |
|---|---|---|---|---|---|---|
| 1 | obligación | 479 | 0.0765 | 0.0765 | [0.0661, 0.0868] | [0.0661, 0.0868] |
| 2 | decisión familiar | 366 | 0.0671 | 0.0671 | [0.0579, 0.0763] | [0.0579, 0.0763] |
| 3 | única que podía | 193 | 0.0356 | 0.0356 | [0.0286, 0.0426] | [0.0286, 0.0426] |
| 4 | petición | 72 | 0.0123 | 0.0123 | [0.0083, 0.0162] | [0.0083, 0.0162] |
| 5 | iniciativa propia | 4,567 | 0.8086 | 0.8086 | [0.7918, 0.8254] | [0.7918, 0.8254] |

n=5,677, suma de pesos=31,652,127.000000 — idéntico. **Coincidencia exacta a los cuatro decimales que la nota declara, en las cinco proporciones y en los diez límites de IC.** La diferencia de cuantil pre-declarada (z=1.96 del motor formal vs. z=1.959963985 de `svystat.py`, §1.5) no mueve ninguna cifra a esta precisión — verificado, no solo anticipado. **REPRODUCE.**

### §2.4 · Escrito, por la firma condicional D1 ya dada en el encargo (no vuelve a mesa)

- `data/curacion-registro/celdas-d/G5.obligacion_medida.conducta.yaml`: `estado_operativo` → `LISTO`, `production_spec_refs`/`resultado`/`output_nativo.valor_ref` apuntan al expediente real.
- `milpa/procedencia.yaml`: nueva entrada `obligación_medida` bajo `condicionales_escalares_medido_nacional` (clase `MEDIDO·NACIONAL`, segunda de la clase), header de la clase actualizado.
- `data/curacion-registro/produccion-modelo.tsv`: fila 12, `PROD-ea28cc177894157ca113388b` — ver §2.5 sobre por qué las otras 9 filas de `B`/`C` NO se tocan pese a que la integración completa las regeneraría distinto.
- `canon/modelo-decision-v4_0.md`: 7 sitios (cabecera, Paso 5 — fila `MEDIDO·NACIONAL` 1→2, fila "D3 pendiente" 1→0 con nota de cierre —, fila `MEDIDAS` ~~9~~→~~10~~→11, dos callouts 📊, bullet §7).
- `README.md:37`, `canon/estado-programa-v1_10.md:97` (addendum fechado, no reescribe el valor histórico).
- **Contador: `condicionales 10 de 15 → 11 de 15`.** `T19b`/`T19c` se mueven solos (cuentan por clase, no por nombre) — `tests/` no se toca, dentro del perímetro declarado.

### §2.5 · Hallazgo fuera de perímetro, declarado y NO corregido aquí — deriva entre `especificaciones-produccion.json` y `produccion-modelo.tsv` para dos medidas ajenas

Al correr `integrate_production.py` sin `--validate-existing` (que regenera las 12 filas desde cero contra la maestra), las 9 filas existentes de `norma_de_género` (`ESP-OPACA-B-d13ec4fe`) y `radio_confianza` (`ESP-OPACA-C-9ecb5c61`) salieron con `requiere_decision=SI`/`estado_uso_modelo=NO_LISTA_DECISION_HUMANA_PENDIENTE` — un retroceso frente a lo commiteado (`NO`/`LISTA_PARA_USO_MODELO`). Diagnóstico, verificado por comando, no inferido:

- `especificaciones-produccion.json` dice `requiere_decision: "SI"` para ambas specs **desde su creación** (`8df61b0`, 11/ago, para `B`; sin cambio en `0edc008` ni en ningún commit posterior — `git log -p` confirma cero ediciones a ese campo).
- `produccion-modelo.tsv` muestra `NO`/`LISTA_PARA_USO_MODELO` para `B` desde el commit `fb4bade` (`ACTO RES`, 13/ago — el mismo acto que resolvió `RESUELTA-ACOTA` la reserva de encuadre de género) y para las 8 filas de `C` desde `84a943e` (`ENCARGO 9`, veredicto de invarianza ENCUCI↔ENBIARE) — es decir, la tabla se editó (a mano o por una vía que no vuelve a correr `integrate_production.py` sobre la especificación completa) para reflejar la decisión ya tomada, sin propagar el cambio a la especificación maestra.
- **Confirmado con `--validate-existing` contra el estado exacto anterior a este acto (`HEAD~1`): ya fallaba.** `produccion-modelo.tsv` llevaba en un estado que su propio verificador rechazaría desde `ACTO RES`/`ENCARGO 9` — no detectado hasta esta corrida porque nadie había vuelto a ejecutar una integración completa desde entonces.
- La propia celda `G5.familismo_obligacion.actitud.yaml:118-119` afirma, por escrito, que `ACTO RES` cambió `requiere_decision=SI -> NO` en la maestra "misma fecha" — **esa afirmación es falsa contra el árbol real**, verificado arriba. Cita interna incorrecta, mismo patrón que `[[feedback-verify-injected-claims-against-source]]`.

**No se corrige en este acto**: editar `ESP-OPACA-B`/`ESP-OPACA-C` es escribir la especificación de otra medida — fuera del perímetro declarado de `PROD-P638` (mismo criterio que `PROC-11` §6.1 usó para el gate roto que encontró y no arregló). El `produccion-modelo.tsv` que este acto commitea preserva las 9 filas de `B`/`C` **byte a byte** como estaban en `HEAD~1` (no las regenera, no las mueve hacia atrás) — solo la fila 12 (`D`, nueva) y las 2 filas de `A` (únicamente `hash_especificacion_fuente`, refresco inocuo del hash del archivo compartido, ya precedentado y declarado esperado por el propio commit `0edc008`) usan la salida fresca del motor. Verificado campo por campo que ninguna otra columna de `A` cambió.

**Queda nombrado, sin dueño asignado por este acto:** actualizar `supervisor_link.requiere_decision` a `"NO"` en `ESP-OPACA-B-d13ec4fe` y, si el veredicto de `ENCARGO 9` en efecto resuelve su reserva, en `ESP-OPACA-C-9ecb5c61`; correr `integrate_production.py` completo de nuevo para que `produccion-modelo.tsv` deje de estar en un estado que `--validate-existing` rechaza.

## §3 · Qué NO hace este acto

No toca `ESP-OPACA-B-d13ec4fe` ni `ESP-OPACA-C-9ecb5c61` (§2.5). No toca `evidencia-neutral-produccion.json` (reutilizado sin editar, mismo criterio que ADR-70 aplicó a `snapshot-t0.json`). No toca `tests/`. No sella ADR. No decide la pregunta del nombre de generador de G5 (reservada a MOTOR-1/MOTOR-2). No mueve `4 de 144`, `13 de 27`, `0 de 15` (coeficientes), `1 de 2` (llaves).

## §4 · Cierre

`python3 tests/check.py --baseline`: **24 FAIL · 119 WARN — LÍNEA BASE: VERDE**, idéntico al cierre de `PROC-10-bis` (head congelado `0ad9b7b759e138b251129c639f6ef943d6ee0fe7`), sin recongelar. `T19b`/`T19c`/`T15`/`T20` todos `[ok]`. **Contador declarado: `11 de 15`.**
