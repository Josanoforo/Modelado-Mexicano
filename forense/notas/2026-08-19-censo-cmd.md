# Nota del acto · CENSO-CMD

`forense/encargos/2026-08-18-CENSO-CMD.md` · `FP-37` · 19/ago/2026 · NUBE (`cloud_default`, repo-only)

## Qué se hizo

Se formalizó como código la receta de mapeo `necesidad_id` → fila del censo (`forense/registro-recalculo-v1_0.md` §1 Entrada 0) y las reglas de precedencia RUTA-A/RUTA-I/RUTA-C/SIN-RUTA de `censo-estimabilidad-coeficientes-v1_0.md` §1, en `tools/censo_estimabilidad.py`.

El derivador **no re-audita el corpus desde cero** — lee la clasificación base de v1.0 ya volcada en forma tabular por `milpa/procedencia.yaml:rutas_estimabilidad_coeficiente` (congelada ahí a `version_censo: "1.0"`, escrita por el propio Encargo E-CE), y aplica sobre esa base la única regla de reclasificación que produjo v1.1: una necesidad `SIN-RUTA` sube a `RUTA-C` si `data/curacion-registro/relaciones.tsv` trae, para la misma necesidad, una fila con `capa4_apertura_mapeo=EXISTE-SATISFACE` y `clasificacion_relacion=CONFIRMADA` — exactamente el criterio que `registro-recalculo-v1_0.md` §1 Entrada 0 verificó a mano contra las 15 necesidades (positivo en N12/N13/N14, negativo declarado en las 12 restantes).

## Corrida contra el árbol real

```
$ python3 tools/censo_estimabilidad.py --reparto
      3 RUTA-A
      5 RUTA-C
      1 RUTA-I
      6 SIN-RUTA
```

Reproduce exacto el reparto que `ADR-89` selló sobre `censo-estimabilidad-coeficientes-v1_1.md` (`3 RUTA-A · 5 RUTA-C · 1 RUTA-I · 6 SIN-RUTA`, 15/15 filas) — y, fila por fila, la misma ruta que trae v1.1 §5 para cada una de las 15 (verificado por `tests/test_censo_derivado.py`, no solo por el agregado: dos censos distintos pueden compartir reparto sin compartir fila).

Por el punto 4 del encargo ("si reproduce exacto: el censo pasa a derivarse por comando... declarar `censo-estimabilidad-coeficientes-v1_2.md` como el vigente"): **reprodujo exacto** — `forense/censo-estimabilidad-coeficientes-v1_2.md` queda declarado vigente, generado por comando, desde este acto en adelante. No hubo divergencia que investigar.

## Qué se escribió

- `tools/censo_estimabilidad.py` — el derivador. Solo lee `milpa/procedencia.yaml` y `data/curacion-registro/{relaciones,necesidad-objeto-modelo}.tsv`; no toca ninguno.
- `forense/censo-estimabilidad-coeficientes-v1_2.md` — generado por `python3 tools/censo_estimabilidad.py --write forense/censo-estimabilidad-coeficientes-v1_2.md`, no editado a mano. Cada fila trae, además de la ruta, el comando puntual que reproduce su evidencia — insumo directo para fichas F3 que necesiten citar instrumento+comando por coeficiente (delta 2 del encargo).
- `tests/test_censo_derivado.py` — compara el derivador contra el reparto y la ruta-por-fila sellados por `ADR-89`, y contra el contenido en disco de v1.2; falla si diverge sin que medie un ADR. No depende de `tests/check.py`, corre solo.
- `forense/firmas-pendientes.tsv` — `FP-37` pasa de `ABIERTA` a `FIRMADA`, ejecutada por este acto (sin mesa: el propio encargo pre-autorizó la decisión en su punto 4, condicionada a que el derivador reprodujera exacto — y reprodujo).
- Esta nota, y la línea de cierre en `hallazgos.md`.

## Qué NO se hizo

No se tocó `censo-estimabilidad-coeficientes-v1_1.md` (el defecto de comillas de T03 que el propio encargo señala como ya corregido en ese archivo se dejó intacto — no se "re-arregló"). No se escribió en `data/curacion-registro/**` ni en `milpa/procedencia.yaml` — el derivador solo lee. No se re-auditaron las 12 filas no reclasificadas contra el corpus crudo — se heredan de `procedencia.yaml`, que a su vez hereda de v1.0 §5; ese trabajo ya está hecho y sellado. No se abrió microdato. No se creó ningún ADR nuevo — no hizo falta, no hubo divergencia.

## Verificación de la suite — un WARN menos, cascada fuera de perímetro, declarada no editada

`python3 tests/check.py` antes de este acto: `19 FAIL · 129 WARN`, línea base verde (coincide con lo que `canon/estado-programa-v1_10.md:129,221` cita como vigente). Ninguno de los archivos nuevos de este acto (`tools/censo_estimabilidad.py`, `forense/censo-estimabilidad-coeficientes-v1_2.md`, `tests/test_censo_derivado.py`) aparece citado en ningún FAIL/WARN — el derivador y su test no introducen ningún defecto propio.

**Efecto de segundo orden, medido y cascada — CI lo atrapó.** Pasar `FP-37` de `ABIERTA` a `FIRMADA` en `forense/firmas-pendientes.tsv` (requerido por el Cierre del encargo) retira la señal `T22` que `tests/check.py` emite por cada firma `ABIERTA` con su antigüedad — una menos. Al fusionar `main` (que ya traía su propia cadena de recifrados hasta `127 WARN`), el efecto neto midió `127 → 126 WARN`, desincronizando las dos citas de `T16` (auto-consistencia) en `canon/estado-programa-v1_10.md:196,288` (`127 WARN` / `19 FAIL · 127 WARN` citados, `126` real) — `LÍNEA BASE: ROJO` en el check corrido por CI (`PR #270`, `check_run` fallido, 2 entradas nuevas frente a `tests/baseline.json`).

**Se corrigió aquí, después de todo — no es una decisión de mesa.** El perímetro del encargo dice `NO ESCRIBE: canon/`, escrito antes de que este efecto de segundo orden fuera visible; pero el propio `canon/estado-programa-v1_10.md` trae, en la misma línea, más de una decena de "recifrado N→M" previos (`T20-LLAVES`, `LANE-A-E0-E5`, `ESTADO-SPLIT`, `CONSOLIDA-2`, `MESA-18AGO`, …) — todos bajadas/subidas aritméticas de una cifra, sin ADR, ejecutadas por el mismo acto que movió el contador, como bookkeeping de cascada, no como contenido de doctrina. Se sigue el mismo patrón: recifrado `127→126` en las dos líneas, citando este acto y `python3 tests/check.py --baseline` como derivación (no tecleado). `python3 tests/check.py --baseline` tras la corrección: `19 FAIL · 126 WARN`, `LÍNEA BASE: VERDE`, cero `--freeze`, `tests/baseline.json` intacto.
