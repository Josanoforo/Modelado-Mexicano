# `filter-repo/` de PURGA-PRIVACIDAD (10/ago/2026) — copia verbatim

Copia byte-idéntica (6 de 6, sha256 en `MANIFIESTO-PURGA.tsv`) de los artefactos que `git-filter-repo` emitió al reescribir el repositorio el 10/ago/2026; se rescatan aquí porque vivían **solo** en el espejo `~/mm-purga.git`, fuera de todo control de versiones.

De los seis, `ref-map` (167 refs old→new) es el único cuyo contenido no existía ya en el repo; `commit-map` es redundante al 100 % con `canon/remapeo-shas-purga-2026-08-10.tsv` — 603 de 603 pares idénticos, cero diferencia en ambos sentidos, verificado por comando en `ACTO CAJA-RESIDUOS` — y se conserva en su forma cruda por ser contra la que un auditor externo diffea.
