ENCARGO · ACTO GEN2-TRAMITE-FIRMAS-1 · PROPAGA LAS SEIS DECISIONES DE MESA DEL 8/SEP · listo para lanzar; el merge de mesa es la firma final
Cabecera: NUBE (repo-only) · MODELO SUGERIDO: Sonnet (propagación mecánica de decisiones dictadas + un cruce derivable; subir permitido, D-13) · redactado contra `origin/main = 7e2a608a` (PR #624 fusionado) · ADR/FP: deriva en cierre · COMPUERTA: ninguna · NO se lanza en caja. Autocontenido: todo lo que necesita está pegado aquí (A.3); nada se copia de la conversación. CONCURRENCIA: E5-0/E5 corren en caja — cero archivos comunes; ninguna pieza de este acto toca `data/corrida0/`, `milpa/**` ni la cola.
FIRMAS DE MESA, verbatim del 8/sep/2026, que este acto propaga (el ejecutor propaga, no decide — ADR-76/79):

* D-1: «sí corremos hoy» — no la ejecuta este acto (va en el lanzamiento de E5-0); se cita para el registro.
* D-2: «la idea central de todo este proyecto es un LLM puede superar a LLM con dato (todo el corpus), si esto no está claro ahorita entonces no estamos comparando correctamente.»
* D-3: «está bien que hagamos la corrección en otro encargo, solo que si ese requisito es de Gen1 entonces ya no es lo que necesitamos pues estamos en gen 2.»
* D-4: «siempre y cuando estemos súper seguros que no se usan o usarán o contienen algo relevante.»
* D-5: «revisa primero el repo para entenderlo, ya nos ha pasado muchas veces que queremos re-descubrir la rueda cuando la solución ya está.»
* D-6: «firmamos todo de un plumazo.»

═══ VERIFICACIÓN DE EXISTENCIA (A.8) — contestada por quien escribe, 8/sep, contra `7e2a608a`; cada pieza abajo cita el hecho que la condiciona ═══

* El agregado vigente solo compara `L_SOLO_vs_M` (primaria, 3 apariciones) y `L_CORPUS_vs_M` (2); `L_SOLO` vs `L_CORPUS` — la tesis de D-2 — no existe como comparación pareada (`grep -o` sobre `agregado-v1_3-resultado.json`). El diagnóstico D4 vigente: L_SOLO 11.69 pp · L_CORPUS 19.60 pp · M 4.51 pp.
* `forense/prereg-duelo-v2/corredor_l_v1_2.py` (el corredor GEN2) no lee `aviso_M`: 0 apariciones. `L-spec-v1_2.json` no trae campo `generacion`.
* La causa de FP-342 ya estaba diagnosticada en `forense/notas/2026-08-13-w-limpieza-worktrees.md:11`: el bind-mount `devtmpfs` sobre `.git/config` lo impone el sandbox del propio agente (harness), no el sistema de mesa; el rodeo documentado allí mismo resolvió la escritura. FP-342 (7/sep) redescubrió el síntoma sin buscar.
* Las 28 filas de FP-286 se clasifican en `forense/notas/2026-09-03-MAESTRA37-A2-revision-cola.md` §Recomendaciones: 6 BAJAR · 12 MESA-DECIDE · 10 NO-BAJAR-PORQUE — con sustancia viva (p. ej. HOMESCAN/NielsenIQ costo ALTO; credencial WB; limpieza de la relación N21↔SE). Posteriores a esa nota, A4/A6 (6/sep) adquirieron 25+34 objetos: parte de las 28 puede estar ya superada, y eso no está cruzado.
* NC-0027: `CALC-MOTOR-celdas-semilla-v2` existe con el medidor corregido (1 `dirname`) y el registro lo marca `SUPERADO→v2` (PR #622). NC-0031: el PR #620 ya fusionó — la aceptación ocurrió. `GO-E5-0=SI` sigue con 0 apariciones en gobernanza y notas.

PIEZAS P1 · FP-348 → SELLADA con la firma D-2 y su directiva de diseño. El campo `estado` pasa a token FIRMADA (A.16) y la glosa registra, con la firma verbatim: el selector B se adopta como piso común, y C0-D queda instruido a que su comparación de primera clase sea la pareada L_SOLO ↔ L_CORPUS (la tesis del programa: el LLM con todo el corpus contra el LLM a secas), con M como tercer contendiente y B como piso de los tres, todo bajo el mismo corte informativo del selector. Se asienta además, como hallazgo que C0-D debe adjudicar y no enterrar: el diagnóstico vigente muestra al corpus EMPEORANDO al LLM (19.60 vs 11.69 pp) — hoy eso es marginal sin comparación pareada sellada; C0-D lo convierte en veredicto o lo explica (¿capturas L_CORPUS viejas? ¿pp la métrica equivocada? ¿señal real?). El ensayo remesas ENIGH 2016→2022 queda autorizado para C0-B. Una línea en `forense/hallazgos.md` deja la directiva citable para quien redacte C0-D. P2 · FP-347 → CERRADA, VENCIDO-EN-ALCANCE, sin acto sucesor. Aplicando D-3 al hecho verificado: el `aviso_M` es prosa histórica que ningún consumidor GEN2 lee (corredor: 0 apariciones) y DIN-M-01 ya emite (p=0.174804, ADR-398). El JSON sellado no se edita (E.3); la fila cierra con la cita y la estampa A.10. Ni siquiera viaja en C0-B: no hay nada que corregir para GEN2. P3 · FP-343/FP-286 — el cruce que D-4 exige, y solo entonces cerrar. NO se cierra en bloque: la condición de mesa no se cumple a ciegas y la nota A2 contiene decisiones vivas. Este acto deriva, con comando y conteo (A.13), el cruce de las 28 filas contra `data/manifiesto.yaml` y la cola vigente de hoy: (a) toda fila cuyo objeto quedó OBTENIDO o superado por A4/A6/A5 → se cierra con la cita exacta (id_manifiesto/sha); (b) las NO-BAJAR-PORQUE cuya razón es «el dato no existe/nadie lo compone» y que ningún consumidor del modelo referencia hoy (`grep` sobre `milpa/`, `canon/modelo-decision`, specs vigentes — pegar conteos) → se cierran como historia con la razón; (c) lo que quede vivo — típicamente los MESA-DECIDE con costo (HOMESCAN, credencial WB, N21) — NO se cierra: se devuelve como lista corta numerada en la nota del acto, redactada para decisión (qué es, qué cuesta, qué desbloquea), y mesa la recibe en conversación. FP-286/FP-343 quedan FIRMADA-PARCIAL con el desglose (cerradas: N; vivas: M) hasta esa segunda pasada de mesa. P4 · FP-342 → CERRADA por la rueda ya inventada (D-5 ejecutada). Cita: `forense/notas/2026-08-13-w-limpieza-worktrees.md:11` — la causa es el harness del sandbox del agente (montajes `devtmpfs` sobre `.git/config` y afines), no la caja de mesa; el rodeo está documentado y en uso desde agosto. La glosa lo dice con las dos fechas: diagnosticado 13/ago, redescubierto sin búsqueda 7/sep — y una línea en `hallazgos.md` deja el precedente para la próxima vez que un síntoma «nuevo» aparezca en la caja. P5 · La pila D-6, de un plumazo. (a) NC-0027 → CERRADA: la corrección vive en `CALC-MOTOR-celdas-semilla-v2` (medidor con raíz correcta; registro `SUPERADO→v2`, PR #622) — el v1 sellado queda como historia, E.3. (b) NC-0031 → CERRADA: el merge de #620 fue la aceptación de la convención de rama. (c) Asentar en `forense/hallazgos.md`: «APARATO-GEN2-PRE-E5=READY · GO-E5-0=SI (PR #614, cuerpo del PR; asentado por firma D-6 del 8/sep)». (d) `.claude/commands/despacha.md`: añadir la re-verificación del candado inmediatamente antes de escribir la huella (no solo al arrancar), y si el candado cambió a media corrida, la huella lo dice así — la mejora que el `[REVISA]` de #619 propuso tras medir tres hechos caducados en 100 minutos. La nota de ese revisor vive en la rama `claude/revisa-post-hoc-619`; fusionarla es de mesa, este acto no la toca — solo deja la mejora instalada.
PERÍMETRO: `forense/firmas-pendientes.tsv` · `forense/no-corrido.tsv` (solo columnas de estado/cierre) · `forense/hallazgos.md` · `.claude/commands/despacha.md` · la nota del acto en `forense/notas/` · `forense/encargos/` (0-bis) · cascada (tablero re-derivado incluido — hoy rancio en 402 contra 404 real). Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.
CONTADOR: cero GEN2 — trámite. Lo que mueve: 3 FP cerradas + 1 sellada + 1 parcial con lista corta de vuelta a mesa · 2 NC cerradas · `GO-E5-0` por fin en el árbol · `/despacha` deja de poder asentar huellas caducadas.
NO-CORRIDO / RESERVAS (plantilla mínima que el acto completa)
La fusión de la nota `revisa-post-hoc-619` — DECISIÓN-DE-MESA-PENDIENTE (es un clic de mesa) — sucesor: mesa. La lista corta de P3(c) — DIFERIDO-A: conversación de mesa — sucesor: la propia mesa con la lista a la vista.
Lo que NO hace: no fusiona nada · no lanza E5-0 (eso viaja en su propio mensaje de lanzamiento, firma D-1) · no toca CALC, cola, milpa ni specs · no reabre FP-166 ni ningún cierre previo (A.10 gobierna hacia adelante).
