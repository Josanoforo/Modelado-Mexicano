# Nota · ACTO SELLA-AGO25-F — las ocho letras, cero verbatim

25/ago/2026. Entorno **NUBE** (`cloud_default`). Modelo Opus. Rama `claude/encargo-1-sella-ago25-f-75gu88`. Encargo: `forense/encargos/2026-08-25-SELLA-AGO25-F.md`.

## 1 · ARRANQUE

1. **REPO:** clon existente, `/home/user/Modelado-Mexicano`.
2. **SHA:** `git fetch origin main` refresca `origin/main` a `8aff7cb` (desde `62e60e9`). CONTADOR: cero directo (v2.3), higiene declarada desde el lanzamiento.
3. **`data/raw`:** no aplica — este acto no toca microdato ni corpus.
4. **ENTORNO:** NUBE, sin red/microdato — declarado, sonda saltada (boilerplate, no hay sonda real que hacer).
5. **ESPEJO:** nada.

## 2 · Las ocho letras — búsqueda dirigida, cero rulings verbatim

El lanzamiento de este acto (`forense/encargos/2026-08-25-SELLA-AGO25-F.md`) describe, para cada letra `L1`–`L8`, **qué hacer condicionado a una opción de mesa** (`a`/`b`/`c`, "sí"/"no", una cadena verbatim, o una adjudicación caso por caso) — pero **el propio texto del lanzamiento no trae adjuntas las opciones elegidas**: no hay "hoja" de resoluciones anexa con las ocho letras resueltas, pese a que el encabezado del encargo la da por adjunta ("adjunta la hoja al lanzar").

Regla del propio lanzamiento, aplicada: *"Ejecuta solo las letras que vengan verbatim en el lanzamiento; las ausentes se saltan y se dicen."* Antes de asumir que la hoja simplemente no llegó, se buscó además en el árbol (mismo método que `ACTO SELLA-AGO25-E` §4 ya usó): `grep -rn` sobre `forense/notas/`, `forense/bitacora.md`, `canon/gobernanza-v1_15.md` de "L1"–"L8" y de `FP-127`/`FP-128`/`FP-129`/`FP-130`/`FP-63`/`FP-24`/`FP-131`/`FP-132`/`FP-118` en contexto de ruling de mesa verbatim.

**Resultado: cero coincidencias nuevas.** Las únicas apariciones de `L1`–`L6` en el árbol son las que `ACTO SELLA-AGO25-E` ya dejó documentadas el mismo día — su propio hallazgo de "cero rulings verbatim" para esas seis letras, no una resolución nueva. No existe ningún ruling `L7` u `L8` en ningún archivo. Letra por letra:

- **L1/`FP-127`** — sin opción (`a`/`b`/`c`) verbatim en el lanzamiento ni en el árbol. `estado` de `FP-127` en el tablero: `ABIERTA`. **No se ejecuta.** El registro de llaves (`forense/registro-llaves-identificacion-v1_0.md`) y el sitio del `−0.60` (misma tabla, fila `CAL-G3`) quedan sin tocar.
- **L2/`FP-128`** — el lanzamiento no trae la resolución "PROPUESTA→SELLADA" como un hecho ya decidido; describe el procedimiento a seguir *si* mesa la sella, pero no incluye la firma misma. `estado` de `FP-128`: `ABIERTA`. **No se ejecuta.** `forense/hitoD-R10_1-spec-v2_0-PROPUESTA.md` sin tocar.
- **L3/`FP-129`** y **L4/`FP-130`** — ninguna opción firmada llega adjunta. `estado` de ambas: `ABIERTA`. **No se ejecutan.** `forense/ficha-r34-conda-v2-spec.md` sin tocar; `FP-104` sin tocar (sigue `ABIERTA`).
- **L5/`FP-63`** — búsqueda exacta de la cadena verbatim `AUTORIZO DESTRUIR mm-purga.git` en todo el árbol: aparece únicamente dentro de la definición de la propia compuerta de `FP-63` (`forense/notas/2026-08-19-fp63-verificacion-espejo.md:267`, `forense/notas/2026-08-19-caja-residuos-cierre.md:93`, `canon/gobernanza-v1_15.md:2349,2409`), nunca como autorización dada — mismo hallazgo que `ACTO SELLA-AGO25-E` ya reprodujo horas antes. **No se ejecuta ninguna acción destructiva**, y no se crea la mini-fila UBUNTU que L5 pediría *si* la cadena apareciera como autorización.
- **L6/`FP-24`** — la "PROPUESTA de ENLACE-2" (texto completo en `forense/notas/2026-08-25-sella-e.md` §5, copiado verbatim de `forense/firmas-pendientes.tsv`) fija la **regla** de pares (ya sellada, `ADR-93`), pero no adjudica las 20 filas concretas de `data/curacion-registro/relaciones.tsv` (ENSAFI 9 · ENFIH 8 · ENBIARE 3) — esa adjudicación caso por caso es exactamente lo que el texto reserva para "acto propio" (`FP-46`), y el lanzamiento de este acto no trae esa adjudicación fila por fila. No hay firma de mesa que "difiera de la propuesta" que propagar: la propuesta no contiene una decisión ejecutable sobre las 20 filas, solo la regla. **No se toca `relaciones.tsv`.**
- **L7/`FP-131`** — el lanzamiento no trae un "sí" o "no" verbatim de mesa sobre si el motor gana el conducto. `estado` de `FP-131`: `ABIERTA`. **No se ejecuta**; la propuesta mínima del hook permanece donde ya vivía, `forense/notas/2026-08-25-eval-compartamos.md` §7.1, sin promoverse a "PROPUESTA-SELLADA".
- **L8/`FP-132`** — el lanzamiento no trae el mapeo firmado (qué necesidad/θ reclama la evidencia de Compartamos); lo único que existe es el hallazgo `NO-ENCONTRADO` ya registrado en `forense/notas/2026-08-25-eval-compartamos.md` §6 (0 de 37 filas de `data/curacion-registro/necesidad-objeto-modelo.tsv` nombran el paquete). Sin una firma de mesa que asigne el mapeo (o que declare explícitamente "sin consumidor"), no hay nada verbatim que escribir en `necesidad-objeto-modelo.tsv` ni en el registro de llaves. **No se ejecuta.**

**Ninguna de las ocho letras ejecuta.** Cero cambios a `forense/hitoD-R10_1-spec-v2_0-PROPUESTA.md`, `forense/ficha-r34-conda-v2-spec.md`, el registro de llaves, `data/curacion-registro/relaciones.tsv`, `data/curacion-registro/necesidad-objeto-modelo.tsv`, ni `milpa/` (que en cualquier caso el encargo prohíbe tocar).

## 3 · `FP-118` — segunda mitad de L8, contradicción de premisa (de nuevo)

El lanzamiento pide cerrar `FP-118` citando "PASO 0 agotado + corrida bajo plan B, ADR-157", con el argumento de que quedó fuera del palomeo de `SELLA-AGO25-E` por estar `ABIERTA` y no `FIRMADA`.

Verificado contra el árbol real, no asumido: **la fila `FP-118` de hoy ya no trata ese tema.** `ACTO SELLA-AGO25-E` (mismo día, horas antes) documentó en su §2 que la premisa "PASO 0 agotado / `ADR-157` / plan B" sí correspondía a `FP-118` en ese momento, pero se abstuvo de tocarla porque el `estado` era `ABIERTA`, no `FIRMADA`. Entre ese acto y este, `ACTO RECENSO-DISENO-14` (fusionado después, aunque escrito antes, 24/ago) **reescribió por completo el contenido de la fila `FP-118`** en `forense/firmas-pendientes.tsv:119`: hoy pregunta si ENNViH admite error estándar basado en diseño para la corrida `CAL-G3` (sin UPM/estrato en los 425 `.dta` examinados), con tres opciones nuevas para mesa — nada que ver con "PASO 0 agotado".

**Decisión de este acto: no se cierra `FP-118`.** Cerrarla citando la firma original de mesa sobre un texto que la fila ya no contiene sería exactamente el defecto que `SELLA-AGO25-E` existió para impedir — fabricar una ejecución sobre una premisa que el árbol ya contradice, esta vez no solo por `estado` sino porque **el contenido mismo cambió de tema**. `FP-118` queda `ABIERTA`, sin tocar, con este hallazgo para mesa: la cita de dirección ("PASO 0 agotado", `ADR-157`) describe una versión superada de la fila; la versión vigente (`RECENSO-DISENO-14`) necesita su propio ruling.

## 4 · Actualizaciones al tablero (`forense/firmas-pendientes.tsv`)

Ninguna. Las ocho filas (`FP-127`, `FP-128`, `FP-129`, `FP-130`, `FP-63`, `FP-24`, `FP-131`, `FP-132`) y `FP-118` quedan exactamente como estaban al arrancar este acto.

## 5 · ADR y cascada

`ADR-165` (`canon/gobernanza-v1_15.md`), candidateado contra el máximo verificado por `grep -oE '^\*\*ADR-[0-9]+' canon/gobernanza-v1_15.md | sort -t- -k2 -n -u | tail -1` sobre `origin/main` re-fetched inmediatamente antes de commitear: único `164` (`ACTO SELLA-AGO25-E`, ya fusionado), sin huecos → `165`, a re-verificar por quien fusione. `canon/estado-programa-v1_10.md`: conteo de ADR recifrado `164→165`.

## 6 · Tests

`CHECK_SELFCHECK_CHILD=1 python3 tests/check.py --baseline`, corrida final tras la edición:

```
19 FAIL · 141 WARN
LÍNEA BASE: VERDE — nada nuevo frente a tests/baseline.json
```

Sin movimiento neto: este acto no cierra ni abre ninguna fila de `T22` (cero letras ejecutadas, `FP-118` sin tocar), y no toca ningún archivo que `T03`/`T09`/`T05`/`T02`/`T06`/`T08`/`T11`/`T10`/`T13` vigilen salvo el propio recifrado de `T15`/`T16` (conteo de ADR, converge dentro de este mismo acto). `milpa/` no se tocó en ningún momento.

## 7 · Perímetro respetado

Tocado: `canon/gobernanza-v1_15.md`, `canon/estado-programa-v1_10.md`, `forense/notas/2026-08-25-sella-f.md` (este archivo), `forense/encargos/2026-08-25-SELLA-AGO25-F.md`.
No tocado (correctamente, porque ninguna letra verificó verbatim): `forense/firmas-pendientes.tsv`, `forense/hitoD-R10_1-spec-v2_0-PROPUESTA.md`, `forense/ficha-r34-conda-v2-spec.md`, el registro de llaves, `data/curacion-registro/relaciones.tsv`, `data/curacion-registro/necesidad-objeto-modelo.tsv`, `milpa/`.

## 8 · Para mesa

- **Las ocho letras (`L1`–`L8`) siguen sin ruling verbatim** — ni en el lanzamiento (la "hoja" que el encabezado da por adjunta no llegó al repo) ni en ningún archivo del árbol. Si mesa ya las decidió fuera de este medio, hace falta transcribirlas verbatim (mismo patrón que `FP-101` ya documentó para "documentos de mesa fuera del repo", y que `ACTO SELLA-AGO25-E` reportó horas antes para `L1`–`L5`).
- **`FP-118`** cambió de tema entre `SELLA-AGO25-E` y este acto (`RECENSO-DISENO-14` lo reescribió): la cita de dirección sobre "PASO 0 agotado" ya no corresponde a la fila vigente. Necesita ruling nuevo sobre su contenido actual (ENNViH / error estándar de diseño), no el cierre que este lanzamiento pedía.
- **`L6`/`FP-24`** — las 20 filas con par de `data/curacion-registro/relaciones.tsv` (ENSAFI 9 · ENFIH 8 · ENBIARE 3) siguen sin adjudicar caso por caso; la regla general ya está sellada (`ADR-93`), falta la adjudicación fila por fila que `FP-46` heredaría.
