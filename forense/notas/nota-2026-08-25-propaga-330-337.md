# Nota · `ACTO PROPAGA-330-337`, 25/ago/2026

Entorno **NUBE** (`cloud_default`). Sin red externa ni microdato — declarado en cada tarea que lo requería, no fallado en silencio. Encargo archivado: `forense/encargos/2026-08-25-PROPAGA-330-337.md` (`CONSUMIDO`).

## 1 · Scope del agregado adjudicante (`escala-cinco-casillas-piloto-v2_0.md`)

Verificación de la línea citada (`:29` en el encargo):

```
$ sed -n '29p' forense/escala-cinco-casillas-piloto-v2_0.md
**Eje único (§2 de la propuesta).** Ya implementado en `forense/prereg-duelo-v2/scoring-adv1-m3.py:74` (`def skill`): ...
```

La línea sigue siendo la `:29` correcta (no se movió), pero la cita interna a `scoring-adv1-m3.py:74` está vencida:

```
$ grep -n "^def skill" forense/prereg-duelo-v2/scoring-adv1-m3.py
359:def skill(error_corredor: float, error_baseline: float) -> float | None:
```

`PR #330` reescribió el archivo (1255 líneas nuevas/tocadas, `git show --stat c79b9bf`) y movió `def skill` de `:74` a `:359`. Corregido en la línea `:29`.

Añadida enmienda fechada (2026-08-25) declarando el scope del agregado que `PR #330` implementó sin anunciarlo aquí: **{L predeclarada, M}** vía `comparacion_principal_id` (`scoring-adv1-m3.py:85,102,205-322`), universos propios por comparación (`_construir_universo:581`, `construir_universo_marginal/pareado:618/624`), más el corredor **E** y la **L auxiliar no-gating**. La reserva de cuál `L` se predeclara (L-solo vs. L+corpus) queda declarada **viva/abierta** — este acto no la cierra.

## 2 · `canon/estado-programa-v1_10.md` y `PR #337`

Grep de los tres marcadores relevantes, ejecutado antes de escribir, conteo = 0 en los tres:

```
$ grep -c "#337" canon/estado-programa-v1_10.md
0
$ grep -c "generar_marco\|marco_e2_adapter" canon/estado-programa-v1_10.md
0
```

`PR #337` (`ACTO generador de marco: adaptador E2 real y diagnóstico fail-closed`, commit de merge `55c2b8b`, ya en `origin/main`) existe en el historial local (`git show --stat 55c2b8b`: `tools/curador_registro/generar_marco.py`, `tools/curador_registro/marco_e2_adapter.py` y sus tests) pero no tenía mención en `canon/estado-programa-v1_10.md`. Añadido paréntesis fechado en el párrafo de "Candidatas del marco del piloto" (línea 99) declarando:

- El generador existe y es fail-closed por construcción: `python3 tools/curador_registro/generar_marco.py --help` declara verbatim *"La corrida real puede emitir cero candidatas; el diagnóstico describe cobertura e insuficiencias y nunca declara saturación"*.
- **No se ejecutó la corrida real** en esta sesión: exige `--indice-e2` privado real (microdato), ausente en NUBE sin red — declarado explícitamente en vez de fallar en silencio o simularlo.
- **Discrepancia declarada:** las cifras que trae el encargo (1 833 802 registros E2 leídos, 113 013 uniones exactas, 16 832 ausentes) se citan tal como se recibieron pero **no se pudieron verificar ni reproducir de forma independiente** en este entorno. La cifra `1 833 802` sí coincide con el universo E2 ya documentado en el corpus (`forense/hallazgos.md:375`, `forense/notas/2026-08-18-b2-v7.md:200`, `forense/encargos/2026-08-18-B2-SEMANTICO-C4-C5-C6.md:54`), pero `113 013` y `16 832` no aparecen en ningún artefacto local (`grep -rn "113.013\|113013\|16.832\|16832"` sobre `*.md`/`*.tsv`/`*.json` del repo: cero coincidencias) — se dejan citadas con esta advertencia, no como hecho verificado por este acto.
- `0` candidatas por insuficiencia semántica del adaptador E2, consistente con el comportamiento fail-closed documentado por el propio `--help`.
- `AUTORIDAD-SEMANTICA-MARCO` está pendiente y la planea otro agente/rol (referido en el encargo como "ChatGPT"), no este acto.

## 3 · Tablero — `FP-96`, `FP-97`, `FP-118`, `FP-133`, `FP-134`

Localización: `forense/firmas-pendientes.tsv` es el tablero real de firmas (no hay un archivo separado con nombre genérico de "tablero" en el árbol).

**`FP-96`/`FP-97`.** Ambas `FIRMADA` con `ejecutada_en` vacío antes de este acto (verificado leyendo la fila completa con el parser de 9 columnas, no con grep aislado). `PR #330` (commit de merge `c79b9bf`, "ADV1-M3: scoring pareado y scope L predeclarado") ejecutó la composición de `scoring-adv1-m3.py` que ambas filas sellan/designan. `ejecutada_en` rellenado citando `PR #330` en ambas; `estado` no se toca (siguen `FIRMADA`).

**`FP-133`/`FP-134`.** Ambas `ABIERTA` antes de este acto. El ruling ya está dado en el árbol: `canon/gobernanza-v1_15.md` `ADR-168(h)` (línea `3351`, `L9`/`FP-133`, opción `c`: *"el marco de candidatas no se poda ni se re-congela; `ACT-PIL-3` debe diseñarse para que su sorteo respete el tope del 20% sin tirar candidatas. `FP-133` cierra `FIRMADA`"*) y `ADR-168(i)` (línea `3353`, `L10`/`FP-134`, opción `a`: *"se extiende el universo de búsqueda del filtro (i)... `FP-134` cierra `FIRMADA`"*). `ADR-168` es el ADR que el commit de merge de `PR #338` (`26ea239`) trae — verificado con `git show --stat 26ea239` y `git log --oneline` (el merge de `#338` es el commit base de esta rama). Movidas a `FIRMADA`, `firmada_en` citando `PR #338`/`ADR-168(h)`/`ADR-168(i)` con el texto del ruling; se anota en ambas que la ejecución real vive en `FP-145` y `FP-146` respectivamente (que ya existían en el tablero, `FIRMADA`, sin ejecutar) — **no se marcan `FP-133`/`FP-134` como ejecutadas**, solo firmadas.

**`FP-118` — DISCREPANCIA DECLARADA, NO TOCADA.** El encargo pide cerrarla citando "PASO 0 agotado + plan B, `ADR-157`". Verificado contra el árbol: esa cita corresponde al contenido que `FP-118` tenía cuando `ACTO SELLA-AGO25-E` la examinó (`canon/gobernanza-v1_15.md:3385-3395`), pero `ACTO RECENSO-DISENO-14` reescribió por completo el contenido de la fila después — hoy `FP-118` (leída íntegra en `forense/firmas-pendientes.tsv`, fila `id=FP-118`) pregunta si ENNViH admite error estándar de diseño para `CAL-G3`, sin UPM/estrato en los 425 `.dta` examinados, con tres opciones nuevas (i/ii/iii), y sigue `ABIERTA`. `canon/gobernanza-v1_15.md:3373` ya documenta este defecto exacto verbatim: *"Cerrarla con la cita vieja fabricaría una ejecución sobre un texto que la fila ya no contiene."* Cerrar `FP-118` ahora con la cita que trae este encargo repetiría ese mismo defecto. Conforme a la regla del acto ("si algo requiere referencia que no se puede verificar localmente, declara la discrepancia y sigue"): `FP-118` **se deja `ABIERTA`, sin tocar**, y se reporta esta discrepancia en vez de ejecutarla. Archivos examinados para esta verificación negativa: `forense/firmas-pendientes.tsv` (fila `FP-118` completa), `canon/gobernanza-v1_15.md` (líneas `3373`, `3385-3395`).

## 4 · `ArbitroR.ee` en `scoring-adv1-m3.py`

```
$ grep -n "ArbitroR" forense/prereg-duelo-v2/scoring-adv1-m3.py
337:class ArbitroR:
390:    valor_L: float, valor_M: float, arbitro: ArbitroR
$ grep -c "ArbitroR" forense/prereg-duelo-v2/scoring-adv1-m3.py
2
```

`class ArbitroR` conserva el campo `ee` (dataclass congelado, comentario de cabecera *"Primitivas M3 preservadas"*, líneas `327-354`). El símbolo `ArbitroR.ee` que `forense/prereg-duelo-v2/banda-tost-margen-v1_0.md:45` cita **sigue existiendo sin cambio**. Conforme a la tarea 4: no se toca `banda-tost-margen-v1_0.md`; se declara el resultado aquí, con el conteo literal del grep arriba.

## 5 · Lo que este acto NO hace

No toca `milpa/`. No cierra `FP-118` (discrepancia declarada en §3). No ejecuta ningún sorteo ni corrida real de `generar_marco.py` (sin microdato/red en NUBE). No toca ningún directorio de "espejo" — no existe tal directorio en el árbol (`find . -iname "*mirror*" -o -iname "*espejo*"` solo encuentra `forense/notas/2026-08-19-fp63-verificacion-espejo.md`, una nota, no un directorio).
