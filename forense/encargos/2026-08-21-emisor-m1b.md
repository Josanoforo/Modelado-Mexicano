**CONSUMIDO** — `ADR-138` (`canon/gobernanza-v1_15.md`), `ACTO EMISOR-M-1b`, 21/ago/2026. Precisión de base: `PR #305`/`claude/emisor-m1-landing-qjq81p` seguía **abierto** (no fusionado a `main`) al llegar este encargo; se aplicó directamente sobre esa misma rama en vez de sobre `main` post-merge — el contenido es idéntico al que el encargo esperaba (el branch ya trae exactamente `ADR-137`/commit `2fcfb00`, más el commit de seguimiento que ya había archivado 3 de los 4 documentos de §2 tras una ronda de CI), así que el delta ejecutado es el mismo que el encargo especifica. Ejecutado: §1 (los dos archivos sustituidos, hashes verificados exactos), §2 (el cuarto documento pendiente, `TRANSFER-EMISOR-M-2026-08-20.md`, archivado — los otros tres ya estaban), §3 (`13 passed, 1 xfailed`, `ESTAMPA` visible, `LÍNEA BASE: VERDE`), §4 (`ADR-138`), §5 (una línea en `hallazgos.md`, ninguna fila nueva de tablero).

# ENCARGO · EMISOR-M-1b — estampa de base empírica + cierre de la revisión del aterrizaje
**Entorno: NUBE (repo-only), Opus. Correr DESPUÉS de fusionar el PR del aterrizaje (`claude/emisor-m1-landing-qjq81p`, commit `2fcfb00`, ADR-137) — la revisión de dirección del 21/ago lo declaró FUSIONABLE: batería `12 passed, 1 xfailed` y `check.py --baseline` VERDE, verificados por dirección corriendo sobre la rama, no leyendo el ADR. Base esperada: main con ese merge; si se movió más, refresca y re-deriva.**

**Autorización, verbatim de mesa (21/ago/2026):** *"lo que se calculó aunque pueda estar bien no significa necesariamente que se hizo con la data suficiente […] si hay algún ajuste que hacer entonces hagámoslo."* Este acto es ese ajuste.

## 0 · ARRANQUE (Bloque D)
Las cinco líneas del arranque estándar; este acto no toca microdato ni red de datos — dilo y salta el punto 4. **A.8:** (2) contenido — los dos archivos de §1 EXISTEN en main con sha `d2abaff8…`/`4d4ef938…` (versión M-1) y este encargo los SUSTITUYE por M-1b; los cuatro docs de §2 son NO-ENCONTRADO en el árbol (universo: rama `2fcfb00` completa, `git cat-file -e`, 21/ago — es el hueco que ADR-137(c) declaró).

## 1 · Sustituir por las versiones M-1b (adjuntas) y verificar sha256
```
cd2d4776cd8dbec0827b45e8935cae3347ee031986dc1304c8b2435550cc0cdd  milpa/src/emisor.py
5bc2067e1d02c73f25d1cfb1b95d3a6300b4fd97de687f1915401b41b88cd342  tests/test_emisor_fidelidad.py
```
Qué cambia (delta exacto sobre M-1, tres cosas): (a) **estampa de base empírica** — `GateR34` gana `insumos_clase` + `estampa`; el gate DERIVA la clase de procedencia de cada probabilidad consumida y estampa en el resultado: hoy `{'ASIGNADO': 2}`, `base medida: 0 de 2`, "B y C son propiedades estructurales del par ASIGNADO, no hallazgos empíricos", con universo declarado. El test nuevo truena el día que un coeficiente del par gradúe a MEDIDO — la advertencia se actualiza sola. (b) `construir_crosswalk` escribe **LF** (`lineterminator="\n"`): regeneración byte-estable contra el repo, verificada en origen (`ce1135d8…` == árbol). (c) `__main__` imprime la estampa. `tests/aceptacion_r3_4.py` y el crosswalk del árbol **no se tocan**.

## 2 · Archivar los cuatro documentos de sesión (cierra el hueco de ADR-137(c))
Adjuntos, con destino y sha256 — verificar tras colocar:
```
2bff007c…79ee4c0c  → forense/notas/2026-08-20-emisor-m-verificacion-premisas.md
5721007b…a8b62c17  → forense/DISENO-EMISOR-M-v1_0.md
96792aad…f883a6ba8d → forense/DISENO-EMISOR-M-v1_1-DELTA.md
b6760b01…ff87697f  → forense/BENCHMARK-INTERVALO-CORREDOR-M-2026-08-20.md
```
(sha256 completos en la salida de la sesión de dirección; verifica contra el adjunto, no contra esta tabla truncada — si difiere, PARA.) Con esto las tres citas colgantes de `T03` del encargo archivado dejan de colgar sin editar el encargo (A.3 intacto).

## 3 · Correr y pegar crudo
Batería completa (harness + fidelidad + 6 motor): esperado **13 passed, 1 xfailed**. `python3 -m milpa.src.emisor`: la línea `ESTAMPA ·` visible. `check.py --baseline`: VERDE, sin `--freeze`; los 3 WARN de T03 del encargo deben desaparecer del reporte (mejora, no bloquea).

## 4 · ADR (candidato 138; renumera al escribir Y al fusionar) — puntos
(a) La autorización verbatim de arriba. (b) La estampa: qué es, por qué (un cálculo correcto sobre insumos ASIGNADO no debe poder confundirse con uno medido — A-bis(3)/A.10 aplicadas a resultados computados), y que se deriva, no se teclea. (c) **Adjudicación A.7 del crosswalk de la revisión**: el TSV de la rama (`ce1135d8…`) y el generado en origen (`21a336b3…`) eran contenido idéntico con envoltura distinta (LF vs CRLF, default del módulo `csv`); la frase "hashes verificados exactos" del CONSUMIDO queda precisada — 3 de 4 exactos, el cuarto igual por contenido — y el fix (b) de §1 la vuelve exacta hacia adelante. (d) Archivado de §2 ejecutado, hueco de ADR-137(c) cerrado. (e) Lo que este acto NO hace: no resuelve H1/H2 (`FP-100` sigue ABIERTA), no escribe FICHA-R3.4, no mueve Hito D, contadores de medición sobre México: 0.

## 5 · Tablero y hallazgos
Ninguna fila nueva. `forense/hallazgos.md`, una línea: la adjudicación A.7 del crosswalk (contenido vs envoltura, CRLF del `csv` de Python, fix en M-1b) con este acto como referencia. Marcar este encargo CONSUMIDO al cerrar.

## 6 · Perímetro
Toca SOLO: los 2 archivos de §1, los 4 de §2, `forense/encargos/2026-08-21-emisor-m1b.md` (este archivo), ADR en gobernanza, cabeceras de estado que el recifrado exija, hallazgos. "Si te encuentras escribiendo fuera de esta lista, PARA."
