ENCARGO · ACTO GEN2-RECIBO-CODEX-3 · LO QUE #866 DEJÓ SIN CASCADA, LOS SIETE DEFECTOS DE LOS PISOS Y LOS CIERRES DE MESA QUE TRÁMITE-5 NO PROPAGÓ
CABECERA (D-12) · SHA de redacción `843a5f97` (merge de #867; premisas leídas en `782293a` y re-leídas aquí); re-deriva al abrir, main movido no es PARO · ENTORNO ASIGNADO: NUBE (no abre microdato) — NO caja · una sola sesión; rama propia; `/acto` PARA si este encargo ya está archivado en otra rama viva (D-17) · COMPUERTA: cumplida — `GEN2-V214` fusionó (#867, ADR-544) y la rama contrato también (#865); se lanza hoy, en paralelo con `GEN2-MARCADOR-REDISENO-1` · MODELO SUGERIDO: Sonnet (receta sin juicio; si P2 exige juicio sobre un cierre, PARA esa pieza, no subas de modelo a mitad) · vehículo `/acto` · FP/ADR/NC: deriva al cierre, no heredes; renumera quien fusione segundo · CONTADOR: `cuenta_gen2 = NO` — este acto no mide; mueve `no_corrido_abiertas` y nada más; dilo en la primera línea de la nota.
FIRMAS DE MESA (verbatim; tu lanzamiento con este archivo es el sello)
Ya dadas (17/sep, archivadas en `ENCARGO-GEN2-TRAMITE-5-2026-09-17.md`, sha256/16 `2cf6b3d8be23fc99`, líneas 8–13; van inline por A.3):
NC-0274: "Rige el 16/sep/2026 como fecha de la firma de ADR-531/FP-377; lo sellado no se edita; la discrepancia queda registrada (ADR-534 P3b). Cierra." NC-0328: "`edad × dominio` (ENVIPE 2025) no se usa como celda-D por ahora; queda `RESERVA-CONSUMIDA-SIN-PILOTO`; cierra." NC-0227: "Opción (a): #682, #719, #726 y #728 quedan huérfanos aceptados; la excepción se anota una vez en `hallazgos.md` y no se fabrica encargo retroactivo. Cierra." NC-0255 / NC-0256: "MEDICION-DEMANDA-3 se redefine al universo real de `relevo-usos-v1_0.tsv`: 153 `SIN-CANDIDATO`, con los 12 `CANDIDATO-GEN2` primero. Ambas cierran con ese sucesor." NC-0237: "R01-MOCIBA (¿misma medida en dos olas?) queda `DIFERIDO-A: F6` — no se decide mientras F6 esté en espera de acreditación (FP-374 VENCIDA-EN-ALCANCE)." NC-0254: "RES-0043 y RES-0044 no cierran por corroboración: el pin EDER 2017 (tipo de primera unión) mide otro estimando que el `p` GEN1 (ENADID 2023, situación conyugal actual). Pasan a `SIN-CANDIDATO` en relevo-usos con sucesor = corrida GEN2 sobre ENADID 2023. Cierra la NC, no el slot."
Nuevas, dadas en conversación de dirección el 19/sep/2026 (verbatim de mesa entre «»; el texto operativo es el que mesa respondió):
D2 — FIRMADA. Texto propuesto: "Veto de mesa a la adopción de `CALC-PISOS-ENVIPE2024-EJES-0001`, `CALC-PISOS-ENCIG2023-EJES-0001`, `CALC-PISOS-ENCIG2023-EJES-0001-v1_1` y `CALC-PISOS-ENIF2021-EJES-0001` como estimadores de celda, hasta que `GEN2-PISOS-REJILLA-CLI-1` entregue sucesores en la rejilla y el universo del árbitro. No se reescriben; se suceden." Mesa: «si vetado». D1 — FIRMADA. Mesa: «El dueño del marcador es acá». Alcance: `GEN2-MARCADOR-REDISENO-1` (Claude) lleva construcción y adopción por celda; `GEN2-MARCADOR-ADOPCION-CLI-1` queda cancelado. D3 — FIRMADA. Mesa: «si deja de contar pero necesitamos encontrar el sustituto, no se puede quedar así». Alcance: `CALC-PISOS-ENCIG2023-EJES-0001` deja de contar; el sustituto es el sucesor ENCIG de `GEN2-PISOS-REJILLA-CLI-1` (desenlace `P7_3 ∈ {4,5}` y universo del árbitro), que Codex mide primero. El `-v1_1` no es el sustituto: corrigió el numerador y dejó el denominador sin las exclusiones del árbitro; queda vetado con los demás. A.9 — pegado de la v2.14 operativa. Mesa: «la fecha del pegado fue hoy 10 de Septiembre». La fecha del sistema al decirlo es 19/sep/2026 (y el transfer que abrió la conversación está fechado 19/sep). Rige 19/sep/2026; el verbatim se conserva; la discrepancia es una línea en `hallazgos.md`, mismo trato que NC-0274. No detiene nada. Ritmo. Mesa: «Dejemos de retrasar encargos por meros trámites […] necesitamos movernos con más agilidad y estrategia.» — en este acto: una sub-pieza que se atora se asienta NO-CORRIDO en una línea y el lote sigue.
VERIFICACIÓN DE EXISTENCIA (A.8; dirección, contra `782293a`, 19/sep)

* (1) Estructura: gobiernan `forense/no-corrido.tsv` (326 filas examinadas), `forense/firmas-pendientes.tsv` (374), `data/corrida0/decisiones.tsv`, `forense/hallazgos.md`, `canon/gobernanza-*` (ADR), tablero. Cubren.
* (2) Contenido — lo pedido no existe ya: `git diff --name-status 09681abe..782293a | grep -E "firmas-pendientes|no-corrido|hallazgos|gobernanza"` → vacío: #866 no tocó ninguno. Las 18 NC nombradas abajo: todas `ABIERTA` (lectura fila por fila por `id`, A.17). Rama contrato (`9fbeefce`): no toca `no-corrido.tsv`; `grep -c FP-379` y `FP-385` en su `decisiones.tsv` → 0 y 0.
* (3) Cobertura retroactiva: las tablas son anteriores al trabajo; no hay recibo previo de #866 (`git log --oneline 09681abe..782293a` → solo commits Codex).

PIEZAS (lote D-11; una pieza que PARA no tumba el lote)
P1 · ADR de recibo de #866, una línea por producto: `tools/verifica_aislada.py`, `tools/asienta_replay_aislado.py`, `tools/pisos_ejes.py`, `tests/test_verifica_aislada.py`, cuatro specs, cuatro CALC, `corridas.tsv`, `replay-evidencia.tsv`. `status` antes/después derivado (corre `status` en `09681abe` y en HEAD con `git worktree`, sin tocar el árbol de trabajo). Reporta sellos físicos, filas de la vista y `N_corridas_selladas` por separado; no afirmes causalidad entre publicar y el contador.
P2 · NC de replay, una por una, por producto: NC-0315, NC-0284, NC-0285, NC-0286, NC-0287, NC-0288, NC-0329. Para cada una: lee su fila, lee el asiento correspondiente en `replay-evidencia.tsv`, y cierra solo si el asiento existe y sostiene lo que la NC pedía; si no, enmienda fechada y sigue abierta. NC-0315 cierra con el texto «remedio superado: asentar, no excluir» si el registro ya no está bloqueado (pruébalo: `corrida0.py registro` sin escritura). Prohibido el cierre en bloque. Añade a `hallazgos.md` la línea: «un veredicto de replay no se publica sin asiento en la fuente; verify que imprime y no asienta es media verificación». Pregunta a contestar: los cuatro `CALC-PISOS-*` figuran `resultado_replay=NO-VERIFICADO` en `corridas.tsv` — ¿tienen asiento? Si no: NC (E.7(2)).
P3 · Los siete defectos de los pisos → hallazgos + NC. Re-verifica cada uno con su comando (están en `BRIEF-ASTRA-01-PISOS-REJILLA-2026-09-19.md` §2, adjunto; archívalo con sha256). Por cada defecto que se sostenga: una línea en `hallazgos.md`; una sola NC para el conjunto, `razón: SUSTITUIDO-POR`, `sucesor: GEN2-PISOS-REJILLA-CLI-1`, impacto = "los cuatro CALC no son consumibles como estimador de celda; MARCADOR-REDISEÑO-1 no se lanza sobre ellos". El defecto 5 (¿`BP1_20` en blanco?) y la pregunta de `P5_6_k` 2021 son `NO-VERIFICABLE-AQUÍ`: se asientan como pregunta, no como hecho. Si un defecto no se sostiene al re-verificar, eso se reporta y el brief se corrige por enmienda. Aplica D2 (firmada) y D3 solo si viene firmada: fila en `decisiones.tsv` (objetos `veto:pisos-866` y `sucesion:pisos-encig2023`). Asienta como hecho verificado por dirección y re-verificado por ti: los 8 `RESULT-PISOS-*` tienen `n_usos = 0` en `resultados.tsv` y ningún archivo de `milpa/`, `canon/`, celdas-D ni `decisiones.tsv` cita `CALC-PISOS` — el veto no desadopta nada, por el mecanismo de la casa; si el instrumento no admite `SUPERADO→` sin re-registro en caja, NC con `NO-VERIFICABLE-AQUÍ` y sucesor PISOS-REJILLA.
P4 · Semillas `PARA-v2.15` en `hallazgos.md`, una línea cada una, de esta jornada: (a) congelar spec sin congelar medidor no es COMMIT-1 (#866: spec 10:33, medidor 10:43); (b) un sello que hashea un shim no fija el código; (c) una spec que promete una celda obliga a medirla o dictaminarla — el silencio no es un valor; (d) la rejilla del piso se lee del árbitro, no se teclea; (e) "el tablero de firmas se busca por objeto, no por frase"; (f) "cuando una sesión rompe una reserva, pide la categoría del defecto, nunca su contenido". No instrumentes nada: son semillas.
P5 · Siete cierres de mesa y cuatro enmiendas (TRÁMITE-5 P2b y P3a). Sub-compuerta: ninguna — las firmas viajan inline arriba. Cada NC: `estado → CERRADA`, `cerrado_por` = este acto, `fecha_cierre`, verbatim en `decisiones.tsv` con objeto `cierre:<NC>`. NC-0237 no cierra: su firma la difiere a F6 — enmienda fechada y sigue abierta (el texto de TRÁMITE-5 la lista entre "cierres"; la firma dice otra cosa: manda la firma, y se anota). NC-0254: además `relevo-usos` RES-0043/0044 → `SIN-CANDIDATO` por comando de la casa; si no existe el comando, PARA esa sub-pieza. NC-0227: línea única en `hallazgos.md` con los cuatro PR. NC-0024, NC-0076, NC-0239, NC-0300 no se tocan aquí: las cierra `GEN2-MARCADOR-REDISENO-1`, que corre en paralelo (enmendarlas para cerrarlas una hora después era trámite).
P6 · Recibo de la rama contrato (#865, ya en main). ADR de recibo; `decisiones.tsv` filas FP-379, su enmienda D9 y FP-385 (TRÁMITE-5 P2a, verbatim de cada FP leída de `firmas-pendientes.tsv`); compara §13 de `estado-programa-v1_14.md` contra `seccion-13.md` y lista, una línea cada una, las afirmaciones del insumo que Codex quitó o cambió — no las restaures: llévalas a mesa como FP ("¿se acepta la corrección del ejecutor al insumo firmado?"); `INFRAESTRUCTURA:159` + tablas nuevas y huella de rutinas (TRÁMITE-5 P3b–c); recibo #851–855 (P4 de TRÁMITE-5) solo si sigue sin ADR — verifícalo antes.
P7 · A.9 y tablero. Enmienda fechada a ADR-544 (no se edita el ADR): "lado proyecto completado; pegado de la v2.14 operativa declarado por mesa, rige 19/sep/2026". `instrucciones_vigentes` → `v2.14` en el tablero vigente (hoy dice `v2.12` en `forense/tablero/TABLERO-PROGRAMA-v1_1.md:117`; verifica cuál archivo es el vigente antes de escribir).
PERÍMETRO Y CONCURRENCIA
`forense/no-corrido.tsv` · `forense/hallazgos.md` · `forense/firmas-pendientes.tsv` · `data/corrida0/decisiones.tsv` · `canon/gobernanza-*` (ADR) · tablero al cierre · `forense/rutinas.tsv` y `data/INFRAESTRUCTURA-v1_0.md` (solo P6) · `relevo-usos` (solo por comando) · nota de cierre · archivo de este encargo y del brief adjunto. No toca: ningún `CALC-*` · `corridas.tsv` ni `replay-evidencia.tsv` (si falta un asiento es NC, no se escribe aquí) · `tools/` · `tests/` · specs · `estado-programa-*` · `milpa/`. En paralelo: Codex en `PISOS-REJILLA` (caja; sin archivo común) y `GEN2-MARCADOR-REDISENO-1` (nube; comparte `decisiones.tsv`, `no-corrido.tsv`, `hallazgos.md` y ADR: quien fusione segundo re-deriva y renumera). «Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.»
LO QUE NO HACE
No mide · no adopta ni desadopta por cuenta propia (ejecuta firmas) · no corrige los pisos · no relanza TRÁMITE-5, VOCABULARIO, REPLAY ni PISOS · no sella el informe v1.1.
SUCESORES
`GEN2-PISOS-REJILLA-CLI-1` (Codex) → `GEN2-MARCADOR-REDISENO-1` (Claude, Opus; construye y adopta por celda) · sello del informe v1.1 después de este recibo (el informe afirma que el motor tiene segmento; debe poder citar el estado real de los pisos) · la rama `codex/gen2-marcador-adopcion-cli-1` la archiva como insumo y la suelta el acto del marcador (su P0), no éste.
CIERRE
Cascada D-10 · `## NO-CORRIDO / RESERVAS` ("Ninguno." si aplica) · `## CONSUMIDO` con PR · cero ramas.

## NO-CORRIDO / RESERVAS

| qué | por qué | impacto | sucesor |
|---|---|---|---|
| P3 — re-verificar los siete defectos de los pisos con su comando (`BRIEF-ASTRA-01-PISOS-REJILLA-2026-09-19.md` §2) | PARO-PREMISA: el brief no existe en el repo ni llegó adjunto a este acto (búsqueda exhaustiva, `find . -iname "*ASTRA*"` / `*pisos-rejilla*` → 0 fuera de notas ya citadas); `NC-0336` | ningún defecto se pudo confirmar ni negar; no se abrió la NC de conjunto `SUSTITUIDO-POR` que P3 pedía porque no hay defecto sostenido que la funde | SIN-ASIGNAR — quien archive `BRIEF-ASTRA-01-PISOS-REJILLA-2026-09-19.md` con su sha256 vuelve a lanzar esta sub-pieza |
| P3 — marcar `SUPERADO→` en el registro (`spec.yaml`/`corridas.tsv`) para las 4 `CALC-PISOS-*` vetadas por D2 | FUERA-DE-PERÍMETRO: el perímetro de este acto excluye explícitamente `tools/`, specs y `corridas.tsv`; asentado como `NC-0334` | las 4 `CALC-PISOS-*` siguen sin marca de supersesión visible en `corridas.tsv` hasta que un acto en perímetro la escriba (el veto ya opera por decisión, D2/D3, con independencia de esta marca) | `GEN2-PISOS-REJILLA-CLI-1` |
| P5/NC-0254 — mover RES-0043/RES-0044 a `SIN-CANDIDATO` en `data/corrida0/relevo-usos-v1_0.tsv` por comando de la casa | PARO-PREMISA: `relevo-usos-v1_0.tsv` es `# DERIVADO — NO EDITAR` y `tools/relevo_usos.py` no lee `decisiones.tsv` ni conoce `RES-0043`/`RES-0044` por `id` (`grep -n` → 0 líneas) — no existe el comando que P5 pedía | el slot sigue sin moverse aunque la NC-0254 ya cerró (cierra la NC, no el slot, tal como el propio texto de mesa anticipaba) | SIN-ASIGNAR — pieza de mantenimiento que enseñe a `tools/relevo_usos.py` a leer decisiones de corroboración por `id`, o acto de CAJA que re-derive el TSV a mano con la decisión citada |
| P2/NC-0287 — decidir si "el propio recibo en lote" ya satisfizo su propio sucesor y cerrar la fila | DECISIÓN-DE-MESA-PENDIENTE: cerrarla exige juzgar si el patrón de recibo por tanda ya cumplió lo que la fila pedía — juicio de mesa, no verificación mecánica; el encargo instruye parar la sub-pieza que exija juicio sobre un cierre | `NC-0287` sigue `ABIERTA`, sin cambio de sucesor | mesa decide en el próximo trámite o recibo de tanda |

## CONSUMIDO

PR #872.
