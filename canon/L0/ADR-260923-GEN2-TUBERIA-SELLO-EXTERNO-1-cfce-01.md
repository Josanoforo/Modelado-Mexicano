**ADR-260923-GEN2-TUBERIA-SELLO-EXTERNO-1-cfce-01**, `ACTO GEN2-TUBERIA-SELLO-EXTERNO-1`, 23/sep/2026, NUBE, Sonnet 5, sin sub-agentes, MODO ABIERTO. Manifiesto (`tools/sello_externo.py manifiesto`) de los 342 sellos vivos (226 `sello.json` CALC + 116 sidecars `.sha256` SPEC) en `forense/sellos/manifiesto-sellos-2026-09-23.tsv`, con sha256 propio, commit de origen, PR y fecha de merge derivados localmente (sin API de GitHub, determinista) y la firma GPG del merge (342/342 con la misma llave `B5690EEEBB952194`, estado `E`: no verificable sin llave pública local). Atestación externa (P2): (a) OpenTimestamps y (b) TSA RFC 3161 intentadas, ambas `DENEGADA-POR-POLITICA` (egress denegado a los cuatro calendarios/TSA probados, salida cruda en la nota) — el propio encargo ya preveía esta rama; corre el fallback (c): receta para que mesa firme un tag GPG desde su máquina, con `docs/sello-externo.md` como receta para un tercero (probada desde un clon limpio en esta sesión). `NC-260923-…-cfce-01` abierta para (a)/(b), sucesor `GEN2-TUBERIA-SELLO-EXTERNO-2`. `FP-260923-…-cfce-01` a mesa: si `stamp` (el subcomando de delta, P4) entra a la cascada de `/acto`. Cero mediciones movidas, no adopta, ningún sello reescrito o re-hasheado — solo lee y censa. Suite `--rapido` VERDE, 0 FAIL.
