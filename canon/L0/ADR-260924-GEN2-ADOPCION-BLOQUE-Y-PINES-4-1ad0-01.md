**ADR-260924-GEN2-ADOPCION-BLOQUE-Y-PINES-4-1ad0-01** (24/sep/2026, NUBE, MODO AUTÓNOMO — cláusula de autonomía v1.0): sucesor de #1114/PINES-3 (PARO-PREMISA). Las tres reglas de la propuesta ola 5 que citan los diez RESULT pendientes (`civico.contexto_institucional_victimas.lapop`, `dinero.credito.atraso_y_dano_por_producto_banxico`, `trabajo.prestaciones.valoracion_seguridad_social_motral`) se crean en `milpa/tramite.yaml` (22→25 reglas) por `tools/escribe_relevo_consumo.py --crear-desde-propuesta`: `p`/`entonces`/campos medidos copiados VERBATIM de la propuesta, verificados por comando contra el RESULT sellado; los cuatro campos pendientes (`situacion`/`si.disparadores`/`porque`/`tier`/`falsable_si`) redactados por el ejecutor y rotulados PROPUESTO-POR-EJECUTOR. `tools/corrida0.py numera-res --escribe` asigna RES-0227..RES-0236 a los diez slots nuevos de la demanda. Cierra `FP-...-d164-01`/`FP-...-e0db-02` (FIRMADA, INTERPRETACIÓN-DECLARADA: ninguna opción a/b/c pre-redactada se firmó verbatim) y `NC-...-369b-01`/`NC-...-ec71-01`/`NC-...-e0db-02`/`NC-...-d164-01` (CERRADA). NO-CORRIDO: la regeneración de `usos.tsv`/`resultados.tsv`/`corridas.tsv` (y por tanto `pendientes_adopcion`/`adoptados_activos` en `status`) — `corrida0.py registro --escribe` PARA por un defecto de esquema pre-existente en `corridas.tsv` (falta la columna `tolerancia` que el código exige desde PR #1113/VISTA-NORMALIZADA-2), verificado AJENO a este acto (mismo PARO reproducido con `milpa/tramite.yaml` sin modificar, vía `git stash`). `check.py --rapido` VERDE (0 FAIL, 415 WARN preexistentes).
