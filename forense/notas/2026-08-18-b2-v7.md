# Nota del acto · ACTO B2-V7 — generación v7 y las tres cifras del bloque 2

**Fecha:** 2026-08-18 · **Rama:** `acto-b2-v7` · **Base al arrancar:** `origin/main = 997482b` (merge #244)
**Base al cerrar:** `origin/main = 3e4c9f7` (merges #253 y #254, integrados por merge local)
**ADR:** `ADR-98` (nació 96, renumerado dos veces — ver §7)

---

## 1 · ARRANQUE, las cinco líneas

Salida cruda, del worktree, con el comando a la vista.

```
1 · REPO   /home/pc0/Modelado-Mexicano-barrido2
           9941adf hallazgos: T22(a) dispara por diseno...
           On branch codex/barrido-2 · up to date · nothing to commit, working tree clean
           .barrido2/ está dentro. No PARO.

2 · SHA    git ls-remote origin refs/heads/main
           → 997482bbda18b52621e24909eedbed0630c7a111  ·  coincide con el SHA de redacción
           9941adf ya es ancestro de main → rama fusionada, se arranca desde main

3 · ls data/raw/ | head -3      → 20260813130000.export.CSV.zip
                                  20260813131500.export.CSV.zip
                                  20260813133000.export.CSV.zip
   du -sh .barrido2/            → 19G
   ls snapshot-v4 / ledger-v7   → los dos presentes  (snapshot-v4, no v2 → no PARO)
   ls -d tasks-v7 staging-v7    → los dos presentes  (672 y 672)
   ls -lh e2-neutral-index.jsonl → No such file or directory

4 · ENTORNO  [sin_variable] · 200 · 20260813130000.export.CSV.zip
             firma esperada de esta caja, completa. Corpus montado. No PARO.

5 · ESPEJO   aceptado.
```

**Desviación del punto 3, reportada en el arranque y no después.** El índice E2 que el
encargo pide listar no existe con ese nombre; en disco está `e2-neutral-index-v5.jsonl`
(2.1 GB, 17/ago 21:27). La explicación la da el propio transfer (`§3`, líneas 117-118):
`e2-neutral-index.jsonl` es **salida** de la generación en curso, no precondición, y el
`-v5` es la generación anterior conservada para comparar. No es PARO; se declara porque
el encargo lo pedía como si fuera un insumo.

---

## 2 · PASO 0 · el driver de olas

`correr-olas-v7.py` **estaba en disco**, en el scratchpad de la sesión difunta:

```sh
find /tmp -maxdepth 6 -name "*.py" | grep -iE "ola|wave|driver|barrido"
→ /tmp/claude-1000/-home-pc0/37c3147b-…/scratchpad/correr-olas-v7.py   (y v3, v4, v5, v6)
```

Archivado **verbatim** en `tools/curador_registro/correr-olas-v7.py`, commit `8df0c73`:

```sh
sha256sum <origen> tools/curador_registro/correr-olas-v7.py
→ ef5026f7ab3069be441446a96fbb736ee1d76f1aaa60dfa2c84c0e8221eeaf22   (los dos)
diff <origen> tools/curador_registro/correr-olas-v7.py → IDÉNTICO
```

**Reparto verificado contra el LEDGER, no contra el log ni contra el encargo:**

```sh
awk -F'\t' 'NR>1{print $8}' .barrido2/private/t0/ledger-v7.tsv | sort | uniq -c
→ 26 W1 · 246 W2 · 396 W3 · 4 W4      total 672
```

Reproduce 26 · 246 · 396 · 4 = 672. No hay PARO.

**La corrida ya había ocurrido.** `olas-v7-resumen.json` y `olas-v7.log` estaban junto al
script, fechados 18/ago 01:10: 672/672 ok, cero fallas, 3 998 s (W1 11 s · W2 444 s ·
W3 3 192 s · W4 352 s), de 00:03:30 a 01:10:09. El encargo instruye «commitéalo antes de
correrlo»; se llegó tarde a esa consigna porque la corrida la hizo la sesión difunta, no
ésta. Lo que el commit cierra es la infracción de A.3, no la secuencia.

**Tensión declarada y no resuelta en silencio.** El docstring del propio archivo afirma
que no entra a `tools/` «porque la lista cerrada de ADR-92(a) no lo incluye». El número
está rancio (era el provisional de `ADR-95`) y el fondo no se sostiene: `ADR-95(a)`
autoriza «(2) inspección E2», que es exactamente lo que el driver hace. Se obedeció a la
mesa y **no se tocó el docstring**, porque su `sha256` es la única prueba de que lo
archivado es lo que corrió.

---

## 3 · La secuencia real corrida, paso por paso

| # | paso | estado |
|---|---|---|
| 1 | materializar `tasks-v7` + `ledger-v7` desde `snapshot-v4` | ya hecho antes de esta sesión — verificado, no repetido |
| 2 | las cuatro olas | ya hecho antes de esta sesión — 672/672, 0 fallas |
| 3 | **segundo `--barrido2-materialize` con `--staging-root`** | **corrido en esta sesión** |
| 4 | `write_barrido2_w0.py` | **NO corrido, deliberadamente** — §5 |
| 5 | `write_barrido2_material.py` | **bloqueado por el gate** — §4 |

Paso 3, salida cruda (respaldo del ledger tomado antes: `ledger-v7.tsv.pre-mat2.bak`,
`sha256 e043a561…9c45b`):

```sh
unshare -Urn -- python3 -m tools.curador_registro.inspect_assets --barrido2-materialize \
  --snapshot .barrido2/private/t0/snapshot-v4.json \
  --contract data/curacion-universo/contrato-barrido2-v1_0.json \
  --task-root .barrido2/tasks-v7 --ledger .barrido2/private/t0/ledger-v7.tsv \
  --staging-root .barrido2/staging-v7
→ {"W1": 26, "W2": 246, "W3": 396, "W4": 4, "ok": true, "tasks": 672}
```

---

## 4 · El gate, y por qué la secuencia se detuvo ahí

El paso 3 promovió **296 de 672** a terminal, no 672. El encargo pide correr el gate
contra tres o cuatro expedientes antes de la ola completa; aquí la ola ya estaba corrida,
así que el remedio de método se aplicó al revés — se diagnosticó la cláusula exacta antes
de tocar nada más.

**Primer corte: cuál de las 16 cláusulas de `compatible` falla.**

```
{"fallos": {"matches_task": 376}, "ejemplos": {"matches_task": "REP-009ade8f…"}}
```

Las otras 15 pasan en los 672. El fallo es único y está en
`_completed_expediente_matches_task` (`barrido2_material.py:2243`).

**Segundo corte: qué error devuelve su auditor.**

```
{"sin_parser": 0, "auditados": 672,
 "clases_de_error": {"E2_PII_NO_REDACTADA": 13953, "E2_ERRORES_TRUNCADOS": 51}}
```

**Tercer corte: qué campo y qué patrón, uno por uno.**

| campo | patrón | activaciones | qué es |
|---|---|---|---|
| `value_labels` | `\d{11,18}` | 100 841 | `codigo_hex=3120202020202020`, código IEEE-754 |
| `value_labels` | teléfono, 10 dígitos | 31 555 | `codigo_hex=0000000000c05840` |
| `definicion` | teléfono, 10 dígitos | 18 418 | `crc=2719796586;zip_slip=NO` |

**Cero activaciones en cualquier otro campo y en cualquier otro patrón. Ni un solo dato
personal real.**

**Gate oficial, de sólo lectura, que confirma el diagnóstico:**

```sh
python3 tools/curador_registro/validate.py --barrido2-material \
  --snapshot .barrido2/private/t0/snapshot-v4.json \
  --contract data/curacion-universo/contrato-barrido2-v1_0.json \
  --task-root .barrido2/tasks-v7 --ledger .barrido2/private/t0/ledger-v7.tsv \
  --staging-root .barrido2/staging-v7 --require-complete
→ rc=1 · ok:false · representations 672 · completed 672 · e2_records 1 833 802
  E2_PII_NO_REDACTADA 13 953 · LEDGER_NO_TERMINAL 376 · E2_ERRORES_TRUNCADOS 51
```

**Lectura.** El validador aplica los once patrones completos a `value_labels` y a
`definicion`, y sólo exime `nombre`/`hoja`/`tabla` vía `exento_estructural()`. Es decir:
`exento_estructural()` cerró el eje **estructural** y dejó abierto el eje **durable**.
Los dos campos que el gate rechaza son exactamente los dos que las cifras 1 y 2 miden.
Tercera instancia de la misma clase de defecto que ya costó dos reejecuciones.

**No se arregló, y es PARO de perímetro, no incapacidad.** El arreglo vive en
`tools/curador_registro/barrido2_material.py`; el encargo abre `tools/` sólo para su §1, y
editarlo cambiaría `MATERIAL_BUILD_SHA256`, invalidando los 672 expedientes y costando
otra corrida de 67 minutos. Se propone acto propio.

---

## 5 · Por qué W0 no se corrió

Verificado antes de decidirlo:

```sh
awk -F'\t' 'NR==1{for(i=1;i<=NF;i++) if($i=="grado_inspeccion") c=i} NR>1{print $c}' \
  data/curacion-universo/ledger-inspecciones-barrido2.tsv | sort | uniq -c
→ 672 E2
```

El ledger durable vigente tiene **672 filas en `grado_inspeccion=E2`**. `W0` lo reescribe
degradado a tronco `E0` (`estado_terminal=NO`, `parser=NO-APLICA`, `objetos_e2=0`) y pisa
el PRISMA con la versión `PRELIMINAR-W0`. La regla «W0 antes que material» supone que
`material` corre detrás y lo reconstruye; con `material` bloqueado por el gate, correr W0
sólo destruye. Los tres productos están versionados y limpios en `997482b`, así que la
destrucción habría sido recuperable — pero inútil, y se prefiere no ensuciar el árbol.

---

## 6 · Las tres cifras

Especificación y procedimiento congelados en `65122e6` (COMMIT A) **antes de abrir un solo
resultado**; cifras en `b8d5bb8` (COMMIT B), que no edita el COMMIT A. El procedimiento
corrió con el `sha256` exacto que COMMIT A congeló, verificado antes de invocarlo. Primera
corrida, `rc=0`, y es la que se reporta.

Tabla completa con escala y denominador: `data/curacion-registro/b2-v7-tres-cifras.tsv`.
Salida cruda del procedimiento: `data/curacion-registro/b2-v7-tres-cifras-salida.json`.
Lectura y matices: `ADR-98(b)` a `(e)`.

**Estampa de universo (A.10):** `v7` · 672 expedientes · 1 833 802 registros · `65122e6`.

Tres cosas que la tabla no dice y hay que leer con ella:

1. **El residuo de la cifra 1 es la defensa funcionando.** Las 5 944 entradas de SAV
   redactadas tienen **una sola forma en el universo entero**,
   `codigo_hex=<HEX>;label=[REDACTADO-PRIVACIDAD]`: el código sobrevive en el 100 % de los
   casos y sólo se redacta el texto de etiqueta. No es pérdida de metadato.
2. **El 100 % de la cifra 3 es casi tautológico** — `cifrado=SI-EXTRAIBLE` ya codifica el
   desenlace. Lo informativo es **628 de 630**, y que **los 2 que no abrieron no están
   cifrados**: dos PDF de 10 páginas dentro de
   `zenodo_electoral_precinct_level_mexico_municipal.zip`. Ninguno es `enut2002_fd.pdf`.
3. **Cinco miembros ZIP declaran `zip_slip=SI`** (cuatro en `wb2661_*`, uno con ruta
   redactada). Antes del arreglo del bloque 2 eran invisibles.

**Desviación declarada antes de medir:** los tres marcadores que el control cita para la
cifra 3 —`rc=`, `stderr=`, `bytes_texto=`— **no existen** en el índice v7. Los dos últimos
tienen cero apariciones y las 25 713 de `rc=` son falso positivo de subcadena (es el `crc=`
de los miembros ZIP). El v7 declara la apertura con
`lineas_texto=<n>;texto_extraible=SI|NO;cifrado=NO|SI-EXTRAIBLE`.

---

## 7 · Renumeración del ADR, y la cascada que no se escribió

El número se re-derivó **dos veces**, las dos en vivo y no de memoria:

- contra `origin/main = 997482b`, al arrancar: máximo 95, sin huecos → habría sido `ADR-96`;
- contra `origin/main = 3e4c9f7`, justo antes de escribirlo: **máximo 97**, sin huecos →
  **`ADR-98`**. `PR #253` selló `ADR-96` (CI-CATEGORÍA) y `PR #254` selló `ADR-97`
  (T16-HISTÓRICAS) mientras este acto corría.

**`CASCADA NO ESCRITA — perímetro.`** `canon/estado-programa-v1_10.md:27` y `:101` quedan
citando **97** y no 98. Ese archivo no está en el perímetro de este acto. Las dos líneas
que le tocarían, redactadas aquí para quien pueda escribirlas:

- `:27` → `| **`gobernanza`** | `gobernanza-v1.15.md` | 98 ADR, protocolo de cambio |`
- `:101` → `**L0 · Gobierno — completo y al día.** 98 ADR, protocolo de cambio con …`

**FP-48, revisado cláusula por cláusula como exige.** El merge local de `origin/main` sí
tocó `canon/estado-programa-v1_10.md`. Revisado: entraron **tres** líneas, las tres de la
rama ajena (`95 ADR`→`97 ADR` en `:27` y `:101`, y la corrida de la suite de 14/ago a
18/ago con `19 FAIL · 132 WARN`→`19 FAIL · 135 WARN`). **Ninguna cláusula se descartó**;
no hubo automerge quedándose con un lado entero, porque este acto nunca escribió ese
archivo. Merge local, cero conflictos.

---

## 8 · Lo que este acto NO hizo, y por qué

- **No corrió `--freeze`.** Prohibición explícita. `ACTO CI-CATEGORÍA` trabajaba
  `tests/baseline.json` en paralelo y fusionó como `#253` mientras esto corría.
- **No arrancó C5.** `integrate_barrido2.py` sigue con su defecto abierto: una
  `PROPUESTA_ALTA` validada aborta el lote como error de preflight.
- **No arrancó C6.** No existen `data/cableado-universo-v1_0.tsv`, `build_cableado.py` ni
  el test de cableado.
- **No cerró la muestra adversarial.** Build 1.0 contra 1.1, **0 de 41 hashes coinciden,
  sin veredicto**. La exigencia 4 del §15 **no está satisfecha**. Decirlo así es el
  entregable; fabricar un veredicto para cerrarla, no.
- **No absorbió las 17 de M-APERTURA.** Siguen **17 de 17** en `INDEXADO-NO-DESCARGADO`,
  que es lo que §18.8 prohíbe. Quedó fuera por el tiempo que consumió el diagnóstico del
  gate, que no estaba previsto en el encargo.
- **No escribió `forense/corridas/`.** Se creó por un momento para archivar el log de las
  olas y se deshizo al ver que ese directorio no está en el perímetro. La bitácora de la
  corrida queda citada aquí, en §2, que sí lo está.

---

## 9 · Contadores movidos

Los cinco contadores de medición sobre México **no se mueven, uno por uno**: `13 de 27`
(Hito D) · `11 de 15` (condicionales) · `0 de 15` (coeficientes en escala del modelo) ·
`1 de 2` (llaves ejercidas) · `4 de 144`. Este acto no mide México.

Lo que sí se mueve, y es la primera vez que existe: **siete filas de cifra con escala y
denominador propios** en `data/curacion-registro/b2-v7-tres-cifras.tsv` — las tres del
bloque 2 más sus cuatro subsidiarias. Población nueva; no se suma a ninguna de las cinco.
