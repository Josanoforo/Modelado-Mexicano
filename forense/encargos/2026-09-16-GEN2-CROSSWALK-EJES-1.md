ENCARGO · ACTO GEN2-CROSSWALK-EJES-1 · F-17 CORREGIDA, PROPAGADA Y VERIFICADA POR DEFINICIÓN
CABECERA · redactado contra `1366d65b` (post-#815; re-deriva al abrir) · ENTORNO: NUBE (lee codebooks/FD del repo y del inventario; no abre microdato) — si un FD necesario solo está en `data/raw`, esa pieza se declara NO-ACCESIBLE-AQUÍ y se rutea a caja, no se adivina · COMPUERTA: ninguna · MODELO SUGERIDO: Opus (la equivalencia por definición es juicio sobre codebooks) · FP/ADR/NC: deriva al cierre, no heredes.
FIRMA DE MESA, verbatim (adentro; su merge sella). Corrige la referencia de la firma de ayer, que citaba "NC-0226 renumerada" (NC-0226 ya existía CERRADA con otro objeto; FIRMAS-MESA-3 la registró sin propagar, correctamente). El texto sustantivo es idéntico:
FIRMA DE MESA, mesa, 16 de septiembre de 2026 — OBJETO (F-17, NC-0240): ningún eje del árbitro se declara equivalente a un corte del modelo por parecido de nombre. Se ratifica `formalidad ≡ formalidad`. Se autoriza verificar por definición, no por nombre, los dos únicos pares candidatos (`dominio_urbano_rural` ↔ `urbanizacion`, `localidad` ↔ `urbanizacion`) y traer el resultado a firma; `edad` espera FP-53; el resto queda SIN-CORRESPONDENCIA declarado. Con esto, el marcador por segmento tiene hoy un eje comparable, y se dice.
VERIFICACIÓN DE EXISTENCIA (A.8, dirección, contra `1366d65b`):

* `forense/no-corrido.tsv` → NC-0240 `ABIERTA`; pieza: "Se MIDE el cruce y NO se decide"; sucesor: "firma de mesa sobre el crosswalk eje-árbitro ↔ eje-modelo; en particular si `localidad` y `dominio_urbano_rural` son `urbanizacion`". NC-0226 → `CERRADA`, objeto "hoja de firmas 2".
* `forense/encargos/2026-09-16-GEN2-FIRMAS-MESA-3.md` → F-17 registrada verbatim, no propagada (P1 de ese acto lo dice). Este encargo es su propagación.
* Corte del modelo, sellado como dato: `milpa/src/celdas.py:74` `CORTES_C1` — `formalidad: (segsoc=1, segsoc=2)` · `urbanizacion: (1,2,3,4) # tam_loc` · `ingreso: (1..4) # est_socio` · `acceso_digital: (1,2)` · `edad: None (FP-53)` · `migracion: None`. Firma M2: ADR-100(2).
* Ejes del árbitro y su cruce medido: `forense/notas/2026-09-15-GEN2-MARCADOR-C0-D-A8-hueco.md` §6 — `formalidad` 6 puntos sobre corte sellado; `edad` 16 PENDIENTE; `localidad` 4, `dominio_urbano_rural` 3, `escolaridad` 12, `sexo` 8, `sexo_edad` 10, `cuenta_formal` 4, `escolaridad_proxy` 4, `cohorte_nacimiento` 4, `cobertura_seguro` 2, `reparto_hogar` 1 — todos "sin correspondencia".
* La definición de cada eje del árbitro vive en las celdas R (registro del árbitro y `celdas.tsv` de `forense/prereg-duelo-v2/F5-aprendizajes-sucesor-v1_0/`) y en los codebooks/FD de sus instrumentos (ENVIPE para `dominio_urbano_rural`; ENIF/ENIGH para `localidad`): el acto los localiza con el inventario canónico y cita archivo y variable (A.15c: los mapas de códigos se verifican por archivo).
* NO-ENCONTRADO (patrón `crosswalk-ejes`, universo `data/ canon/ forense/`, 16/sep): no existe hoy una tabla sellada de crosswalk eje-árbitro ↔ corte-modelo. Este encargo la crea.

PIEZAS
P1 · Propagar la firma. Fila en `decisiones.tsv` con la firma verbatim; NC-0240 recibe la firma como enmienda fechada (no se cierra todavía: cierra en P4 con el resultado de P2). El registro de FIRMAS-MESA-3 queda como historia; se enlaza, no se edita.
P2 · Los dos pares, por definición. Para cada par:

1. `localidad` ↔ `urbanizacion`: leer en el FD/codebook del instrumento del árbitro la variable que produce `localidad` (¿es `tam_loc`? ¿con los mismos cuatro cortes: ≥100 000 / 15 000–99 999 / 2 500–14 999 / <2 500?) y compararla, categoría por categoría, con el `tam_loc` de las bases ENVIPE 2025 que el modelo corta (A.15c: por archivo, no por nombre — `hs02g` fue CENTRO RURAL en un libro y DIF en otro). Veredicto: `EQUIVALENTE` (mismos cortes, mismo umbral) / `MAPEO-N-A-1` (tabla explícita) / `NO-EQUIVALENTE` (con la diferencia escrita).
2. `dominio_urbano_rural` ↔ `urbanizacion`: ENVIPE codifica `dominio` en tres (urbano / complemento urbano / rural). Tres no es cuatro: el único veredicto posible es `MAPEO-N-A-1` (p. ej. rural ↔ tam_loc=4; urbano+complemento ↔ tam_loc 1–3) si y solo si los umbrales coinciden en el FD, o `NO-EQUIVALENTE`. Nunca `EQUIVALENTE`. Cada veredicto lleva: archivo del FD, variable, cortes literales, y qué pierde el marcador si se usa el mapeo (celdas colapsadas).

P3 · La tabla sellada. `data/crosswalk-ejes-arbitro-modelo-v1_0.tsv` (o donde `data/INFRAESTRUCTURA-v1_0.md` diga; si el índice no cubre "crosswalk de ejes", ese hueco es entregable y se reporta): una fila por eje del árbitro con `corte_modelo`, `veredicto` (`EQUIVALENTE` / `MAPEO-N-A-1` / `NO-EQUIVALENTE` / `SIN-CORRESPONDENCIA` / `PENDIENTE-FP-53`), `evidencia` (archivo:variable), `firma` (vacía salvo `formalidad`, que se ratifica aquí). `formalidad`: verificar que el árbitro también corta por `segsoc` en las mismas dos categorías antes de ratificar; si no, es `MAPEO-N-A-1` y se dice.
P4 · Lo que va a mesa. Los dos veredictos de P2 como propuesta de firma, no como decisión: el acto trae "`localidad` ↔ `urbanizacion`: EQUIVALENTE por definición (evidencia)" y "`dominio_urbano_rural` ↔ `urbanizacion`: MAPEO-N-A-1 con tabla (evidencia)" — o sus negativos — y mesa firma o devuelve. NC-0240 cierra citando esta nota y el PR; la firma de mesa sobre los pares es una fila nueva en el tablero (A.12), abierta por este acto.
PERÍMETRO Y CONCURRENCIA: `data/crosswalk-ejes-arbitro-modelo-v1_0.tsv` (nuevo) · `data/INFRAESTRUCTURA-v1_0.md` (solo si hay hueco) · `decisiones.tsv` · `forense/no-corrido.tsv` (NC-0240 + la fila nueva de firma) · `forense/firmas-pendientes.tsv` (la fila nueva) · nota en `forense/notas/`. No toca `milpa/src/celdas.py` (los cortes son dato sellado bajo ADR-100), el marcador, specs ni resultados. Actos en paralelo: la rutina `/deriva` (derivados, `data/curacion-universo/derivados/`), el servicio de adquisición (cola/manifiesto) — sin archivo común. «Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.»
CONTADOR: cero mediciones, dicho sin disfraz; entrega la primera tabla de crosswalk sellada y convierte en decisión firmable lo que hoy es "sin correspondencia" por nombre. LO QUE NO HACE: no declara equivalencias sin FD a la vista · no abre microdato · no toca FP-53 · no cambia cortes del modelo · no construye el marcador por segmento (eso es sucesor de E1 y de esta firma). SUCESOR: el acto de marcador por segmento, gated a Θ cargable (D-θ) y a la firma de P4. CIERRE: cascada + `## NO-CORRIDO / RESERVAS` + `## CONSUMIDO`.
