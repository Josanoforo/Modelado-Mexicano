# ENCARGO · ACTO GEN2-RELEVO-CONSUMIDORES-2 · Los otros cuatro consumidores legacy (procedencia 40 · catálogo de momentos 23 · marco del duelo 43 · celdas-D 6) con el esquema de cita ya decidido, la partición categórica fuera del contador y el escritor extendido: `legacy_activas` de 137 hacia cero, o dictamen por fila

> ENTORNO: **NUBE** — escritor, `milpa/`, RESULT sellados; re-mediciones, si las hay, en CAJA declaradas. Hook imprime ENTORNO-DERIVADO.

CABECERA · SHA de redacción `9632a880` (re-deriva al abrir) · una sola sesión, rama propia (la que fije la plataforma; se declara) · MODELO: Opus · MODO: **AUTÓNOMO** · ids con raíz de acto (D-24) · D-21 aplica · «Si te encuentras escribiendo fuera de la lista de §9, PARA.» · cierre por /acto: `## NO-CORRIDO / RESERVAS` («Ninguno.» obligatorio si aplica) y `## CONSUMIDO` al pie.
CONTADOR: mueve `dependencias_numericas_legacy_activas_por_consumidor__{procedencia,catalogo_de_momentos,marco_del_duelo,celdas_D}` hacia 0 (o residuo con NC por fila); `adoptados` sube por cada cita que el escritor materialice. Escribir `milpa/` es adopción: **merge de mesa**.

## 1 · OBJETIVO
RELEVO-MOTOR-34-1 (#1129) dejó el motor en camino y tres decisiones que FIRMAS-16 da (B1–B3); CATALOGO-CONTRATO-Y-TEST-1 (#1115) hizo consistentes test y contrato del catálogo y propuso el esquema de procedencia (B4, opción a). Con eso: (P1) **procedencia (40)**: por fila, RESULT GEN2 por conducta y texto de pregunta → el escritor escribe `corrida0_resultado_id`, `corrida0_generacion: GEN2`, `clase_respaldo` en la propia entrada (B4); sin RESULT → re-medición en caja o NO-CONSTRUIBLE con la pregunta buscada. (P2) **catálogo de momentos (23)**: valor GEN2 + cita + `discrepancia_gen1` según el contrato consistente; momento 08 unidad DELITO (N); momentos 01–02 por cotejo. (P3) **marco del duelo (43)**: las lecturas que son parámetros de diseño (umbrales, λ, listas) se dictaminan HISTÓRICO-SIN-RELEVO con firma (propuesta al cierre, una fila) o se relevan si tienen RESULT; las particiones (`CORTES_C1`) salen del contador por B3. (P4) **celdas-D (6)**: las seis referencias de código se relevan por cita al RESULT del champion vigente; los YAML no se tocan a mano. (P5) Tabla final consumidor × fila × vía × estado; `status` en árbol antes/después.
«Hecho»: los cuatro contadores en árbol re-derivado bajan (el residuo con NC por fila y razón A.14) · `milpa/*.yaml` sin edición manual (escritor, diff seco) · tests por llave · `check.py --baseline` VERDE.

## 2 · FIRMAS DE MESA — dadas o en FIRMAS-16
4.1 (vías de relevo), D6 (eje RESULTADO), D7 (motor con perímetro), N (escritor extendido; momento 08), FIRMAS-16 B1–B5 (por id). Un contrato que exija una decisión de contenido no cubierta: se redacta rotulado PROPUESTO-POR-EJECUTOR (cláusula §3) y se sigue.

## 3 · LO QUE DIRECCIÓN SABE
`[EJECUTADO]` legacy 137 (motor bajó de 34; los otros cuatro sin cambio). `[LEÍDO]` contratos en `forense/analisis/astra4-relevo/contratos-otros-consumidores.md`; nota de #1129 (ocho ASIGNADO, cuatro históricos, seis cortes); nota de #1115 (esquema de procedencia, T-REPRO(c) consistente). `[SUPUESTO]` que el escritor (`tools/escribe_relevo_consumo.py`) ya crea reglas y etiqueta en `tramite.yaml` (ADOPCION-4) y hay que extenderlo a `procedencia.yaml` y `catalogo-momentos`: es parte del acto.

## 4 · YA HECHO / YA DECIDIDO
`git ls-remote --heads origin | grep -i relevo` → 0. Pines firmados en `pines-de-mesa.tsv` no aplicados: se aplican aquí.

## 5 · PIEZAS
P1 procedencia → P2 catálogo → P4 celdas-D → P3 marco (dictámenes) → P5 tabla.

## 6 · LATITUD — CLÁUSULA DE AUTONOMÍA v1.0 (`3fbc487684b77b7f`, verbatim en el ADR)
1. Discrepancias encargo↔repo las resuelve el ejecutor y las declara. 2. Firma con letra en choque e intención clara: INTERPRETACIÓN-DECLARADA, se sigue. 3. Lo redactable se redacta, rotulado PROPUESTO-POR-EJECUTOR, con fuente; mesa adopta al fusionar. 4. Bifurcación con opción recomendada: se ejecuta la recomendada. 5. PARO solo por D-19 estricta. 6. Nunca: cifra tecleada, sello reescrito, reserva abierta, fuera de §9 sin declarar; merge de mesa cuando se sella o se escribe el motor. 7. El «Hecho» no se rebaja. Pregunta a mesa prevista: **ninguna**.

## 7 · PAROS — lista cerrada (D-19 estricta)
a) abrir ola reservada · b) editar `milpa/` a mano, reescribir sellos, cambiar un valor sin RESULT · c) mover contadores a mano · d) cambiar procedimiento congelado · e) —

## 8 · COMPUERTAS
«Escritor, diff seco, test por llave, merge de mesa» protege: **adoptar / congelar**. «Cita solo con replay afirmativo» protege: **adoptar**.

## 9 · PERÍMETRO Y CONCURRENCIA
Propio: `milpa/procedencia.yaml`, `milpa/catalogo-momentos-v0_1.tsv`, marco del duelo (archivo que el inventario señale), referencias de código de celdas-D, `tools/escribe_relevo_consumo.py` + tests, `pines-de-mesa.tsv`, `forense/analisis/relevo-consumidores/`, TSV de gobierno, nota, L0, cascada. Ajeno: `tramite.yaml` (motor: hecho), YAML de celdas-D, CALC. En vuelo: FIRMAS-16 (B1–B5 gatean: si no ha fusionado, se toma lo firmado y se declara), CATALOGO-V1-1-1, PISOS-GEN2-2.

## 10 · LO QUE NO HACE · SUCESORES
No toca el motor (hecho), no adopta lo de Astra. Sucesor: `-3` para el residuo; FIRMAS-17 asienta HISTÓRICO-SIN-RELEVO del marco.
