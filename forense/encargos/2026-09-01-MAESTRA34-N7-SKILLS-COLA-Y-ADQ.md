ENCARGO · ACTO MAESTRA34-N7 · SKILLS-COLA-Y-ADQ — invoca /acto
SHA de redacción: e4af4ed (merge PR #455). Redacta dirección (Fable), 1/sep/2026, contra v2.12. Estado: LISTO PARA LANZAR — firma DC-a dentro; A1/A2 no requieren firma de contenido (tu merge lo es).

ENTORNO ASIGNADO: NUBE. NO se lanza en UBUNTU (edita skills y runbooks; el script de caja se escribe aquí y se prueba en caja por L3 o por mesa). MODELO SUGERIDO: Opus (edita dos skills en uso; regresión contra su comportamiento actual).
CARRILES: N4 y N6 pueden estar corriendo en nube — perímetros disjuntos (.claude/commands/{encola,despacha}.md, forense/agente-adquisicion-v1_0.md, tools/adquiere_cron.sh vs prereg-duelo-v2/milpa/acto.md y tests/curacion-registro). Si N4 abrió acto.md, este acto NO lo toca.
COMPUERTA: ninguna.

FIRMAS DE MESA — verbatim, 1/sep/2026: «DC - a, DM2-a, MAESTRA34-E1- Escríbelo.» (mesa escribió el rótulo pelado; serie añadida por dirección para T25, ADR-128) y antes: «DC- pero eso solo puede correr en ubuntu, cloud claude code no tiene acceso a hacer esas revisiones y nos topamos con falla por eso migramos a ubuntu.» → DC-a = rutina de adquisición en la caja Ubuntu/WSL de mesa vía cron que lanza `claude -p` con el runbook (mismo mecanismo sin API que runner_l_cli.py, firma MAESTRA33-E17). El ejecutor propaga, no decide (SELLA-3).

═══ VERIFICACIÓN DE EXISTENCIA (A.8) — dirección contra e4af4ed ═══
(1) ESTRUCTURA: .claude/commands/ (9 skills, versionadas; D-10); forense/agente-{tramite,despacho,revisor}-v1_0.md (runbooks con §1 prompt exacto, §3 falsador — el digesto los lee y deriva su fecha de revisión); forense/encargos/cola/ (estados); canon/registro-rotulos.tsv (T25).
(2) CONTENIDO:
  - `/encola`: `ls .claude/commands/` → 9 archivos, ninguno encola → NO-ENCONTRADO. Defecto medido: PR #454 omitió 1 de 3 piezas de la instrucción [COLA] (la enmienda a N3) y requirió perdón T25 manual; PR #455 lo corrigió a mano.
  - `/despacha` §2: toma el LISTO-NUBE más antiguo y marca EN-CURSO antes de que /acto verifique la compuerta; no lee `COMPUERTA:` → EXISTE-NO-SATISFACE. Defecto medido: la sesión de N3 paró dos veces el 1/sep con cero commits por compuerta no cumplida; en cola, N3 (gateado a PR [L]) precede a N5 (gateado a N3).
  - Runbook de adquisición: `ls forense/agente-*` → tramite, despacho, revisor → NO-ENCONTRADO. Skill /adquiere EXISTE (A1/A3/A5) y es la que el runbook invoca.
(3) COBERTURA RETROACTIVA: no aplica.

PIEZAS (un commit por pieza)
P1 · SKILL `/encola`. Nueva `.claude/commands/encola.md`. Entrada: lista de rutas destino ← origen (archivos nuevos a forense/encargos/ o cola/, y ENMIENDAS a appendear al pie de un archivo existente). Hace, en orden: git fetch; copia/append; censa rótulos nuevos en registro-rotulos.tsv (y solo los que el texto trae con serie MAESTRA<nn>-); `tests/check.py --baseline`; **verificación de completitud**: cada ruta de la lista aparece en `git diff --stat` — si falta una, no abre el PR y reporta cuál (A.13: n listadas / n en diff); abre PR `[COLA] …`. No inventa perdones de test: si T25 falla por rótulo pelado en el texto, PARA y reporta el rótulo — la corrección es de dirección (cita con serie), no de la skill. Prueba: corre en seco sobre los tres archivos de PR #455 y muestra que habría detectado la omisión de #454.
P2 · `/despacha` §2-bis PRE-CHECK DE COMPUERTA. Antes de marcar EN-CURSO: lee la última línea `COMPUERTA:` del encargo (la de una ENMIENDA DE DIRECCIÓN al pie manda sobre la del verbatim); verifica por PRODUCTO lo que ahí se cite (archivo, conteo, PR fusionado por `git merge-base`/`git show origin/main:<ruta>`, o fecha ≥ NO-LANZAR-ANTES-DE); si no se cumple, NO marca nada, pasa al siguiente LISTO-NUBE y reporta «<rótulo> gateado: falta <qué>» con el comando. `grep` del asunto de commit queda prohibido como prueba (ADR-277). Prueba: dry-run contra la cola actual → debe reportar N3 gateado (falta PR [L] v1_2) y N5 gateado (falta N3), y tomar nada.
P3 · RUNBOOK Y CRON DE ADQUISICIÓN (DC-a). `forense/agente-adquisicion-v1_0.md` con §1 prompt exacto (invoca /adquiere sobre las 5 filas más antiguas del registro sin intento en ≥7 días, 4 rutas, registro por las tres capas por la vía que N6 documenta, recetas nuevas al PAQUETE-RECETAS-<fecha>, PR `[ADQ] <fecha>` para firma de mesa, cero commits si nada cambió), §2 entorno = UBUNTU/WSL de mesa exclusivamente con la razón medida (A.2, 5/ago), §3 falsador «a un mes» (si en un mes ninguna corrida registra un payload ni produce una receta que mesa ejecute, se retira). `tools/adquiere_cron.sh`: script que hace `cd` al clon, `git fetch && git checkout main && git pull`, verifica corpus montado (`ls data/raw | head -1`, A.2 tercera parte) y sonda `curl … inegi.org.mx` (A.13: reporta valores crudos), y lanza `claude -p "$(cat forense/agente-adquisicion-v1_0.md#§1)"` con log fechado en `forense/adq-log/` (gitignorado). Línea de crontab sugerida en el runbook (lun–vie 07:30 GMT-6), instalación manual por mesa — no se instala desde aquí. El digesto tomará su falsador automáticamente (lee forense/agente-*.md).
P4 · TRÁMITE. hallazgos.md: tres líneas (omisión #454, ciclo N3, adquisición sin dueño). Tablero: recibo; fila «cron instalado por mesa» ABIERTA, gatea a mesa, vence 2026-09-08.

PERÍMETRO Y CONCURRENCIA: .claude/commands/encola.md (nuevo) · .claude/commands/despacha.md (§2-bis) · forense/agente-adquisicion-v1_0.md (nuevo) · tools/adquiere_cron.sh (nuevo) · forense/hallazgos.md · tablero · notas · A.3 · cascada. NO toca acto.md (N4). Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.
FP/ADR CANDIDATOS: deriva al arrancar (N4/N6 pueden fusionar antes).
CONTADOR: skills 9→10 · rutinas documentadas 3→4 · cero estimaciones, declarado.
LO QUE NO HACE: no instala el cron (es de mesa, en su máquina); no corre /adquiere; no toca la cola ni sus estados; no edita acto.md.
SUCESORES: mesa instala el cron y fusiona su primer PR [ADQ] · `/encola` se usa desde la tanda siguiente.

## CONSUMIDO

Ejecutado por `PR #460` (`MAESTRA34-N7 · SKILLS-COLA-Y-ADQ`), 1/sep/2026, `ADR-281`. P1 `.claude/commands/encola.md` · P2 `.claude/commands/despacha.md` §2-bis · P3 `forense/agente-adquisicion-v1_0.md` y `tools/adquiere_cron.sh` · P4 `forense/hallazgos.md` y `forense/firmas-pendientes.tsv` (`FP-233`). Cascada: `ADR-281`, `L0` 280→281, fila `MAESTRA34-N7` de `canon/registro-rotulos.tsv` ampliada. `python3 tests/check.py --baseline` VERDE.
