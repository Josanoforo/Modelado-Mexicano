# ENCARGO · ACTO GEN2-TUBERIA-VISTA-NORMALIZADA-3 · COMMIT-B: `camino_linaje` sale de `resultados.tsv` por derivación bajo demanda, la guarda de CI baja a "falla a 50 MB"

> ENTORNO: **NUBE**. Hook imprime ENTORNO-DERIVADO; si dice CAJA, PARA.

CABECERA · SHA de redacción: `07896580` (`origin/main` al abrir, re-derivado — 0 commits de diferencia) · una sola sesión, rama propia `claude/charming-mccarthy-gdv74u` (D-17) · MODELO: Sonnet (el encargo original de dirección sugería Opus para -2; D-13 permite declarar, no exige repetir la sugerencia en el sucesor de una pieza ya diagnosticada) · MODO: ABIERTO · ids con raíz de acto (D-24) · D-21 aplica · «Si te encuentras escribiendo fuera de la lista de §9, PARA.» · cierre por /acto: `## NO-CORRIDO / RESERVAS` («Ninguno.» obligatorio si aplica) y `## CONSUMIDO` al pie.
CONTADOR: cero mediciones; no adopta; ningún valor de ningún RESULT cambia (la columna que sale es texto de trazabilidad, no un RESULT — `origen_numerico`, que sí es RESULT-adjacente, no se toca).

## 1 · OBJETIVO
Terminar lo que `GEN2-TUBERIA-VISTA-NORMALIZADA-2` dejó `NO-CORRIDO` a propósito (`NC-260924-GEN2-TUBERIA-VISTA-NORMALIZADA-2-f1b2-01`): sacar `camino_linaje` de `resultados.tsv` (cardinalidad medida 1.0, regla del encargo -2: ≥0.5 → bajo demanda, no dedup), derivarlo resultado → corrida → spec → inputs en `tools/vista.py`, adaptar sus consumidores directos y bajar la guarda de tamaño de CI a "falla a 50 MB" — cumpliendo la firma de mesa que -2 ya trae.
«Hecho» significa: los 4 tests nuevos de `tests/test_vista.py` (incluida la reproducción de 500 filas reales, huérfana) en VERDE · `git grep -n camino_linaje tools/corrida0.py` no lo muestra en `COLS_VISTA_RESULTADOS` · `.github/workflows/verify.yml` falla a 50 MB sin tramo de aviso a 100 · `data/INFRAESTRUCTURA-v1_0.md` actualizado antes del cierre · `python3 tests/check.py --baseline` VERDE sin `--force` sobre el commit final con `origin/main` fusionado — nunca una revisión de dirección sesión por sesión.

## 2 · FIRMAS DE MESA
Verbatim, ya en el repo (A.12 — este acto la asienta, la ejecuta y no la re-pide): `forense/encargos/2026-09-24-GEN2-TUBERIA-VISTA-NORMALIZADA-2.md` §2 (línea 15): «Las vistas derivadas se normalizan: los campos constantes por corrida viven una vez por corrida; `camino_linaje` sale de `resultados.tsv` por deduplicación o cálculo bajo demanda según su cardinalidad medida; la guarda de CI falla a 100 MB y avisa a 50 hasta que `camino_linaje` salga, y desde entonces falla a 50; ningún archivo derivado vuelve a superar 50 MB; sin Git LFS y sin sacar la vista del canal. Publicar a ~70 MB entre COMMIT-A y COMMIT-B está autorizado.» Sin firma nueva: este acto es exactamente "desde entonces" (la cardinalidad ya midió bajo demanda, no dedup).

## 3 · LO QUE DIRECCIÓN SABE
- `[LEÍDO]` `forense/encargos/2026-09-24-GEN2-TUBERIA-VISTA-NORMALIZADA-2.md` completo: COMMIT-A cerrado (PR #1113), COMMIT-B declarado `NO-CORRIDO` con sucesor este acto; §5-P3 fija el criterio exacto (cardinalidad → dedupe o bajo demanda, spec, consumidores, guarda a 50, test de 500 filas).
- `[LEÍDO]` `canon/gobernanza-v1_15.md:10132-10151` (ADR-260924-GEN2-TUBERIA-VISTA-NORMALIZADA-2-f1b2-01): cardinalidad ya medida ahí, `65 287/65 287 = 1.0`, no re-medida por este acto — se cita.
- `[LEÍDO]` `forense/no-corrido.tsv:719` (`NC-260924-GEN2-TUBERIA-VISTA-NORMALIZADA-2-f1b2-01`, `ABIERTA`, sucesor este acto): mismo criterio, mismos 3 archivos citados como "al menos" (`tests/test_corrida0.py`, `tests/test_f5_sin_fugas.py`, `milpa/src/emisor.py`).
- `[EJECUTADO]` `git grep -l 'resultados\.tsv\|camino_linaje\|funciones_dependencia' -- tools tests` → 22 archivos (no 18: la cifra de -2 era previa a consumidores nuevos de otros actos entre el 24/sep y hoy — `tools/delta_pin_superado.py`, `tools/escribe_relevo_consumo.py`, `tests/test_registro_lote_estricto.py`, `tests/test_relevo_encuci_f2.py`, `tests/test_astra5_dominios.py`, `tests/test_corrida0_oro.py`, `tests/test_guardas_res.py`, `tests/test_motor_usos_complementos.py`, `tools/relevo_candidatos_delta.py`, `tools/relevo_usos.py`, `tools/resuelve_citas.py` se sumaron). Censados uno por uno (rótulo A.4 por archivo, no por lote): 4 ya pasan por `tools/vista.py::join_resultado`/`leer_resultados_join` (`milpa/src/emisor.py`, `tools/adq_investigacion.py`, `tools/delta_pin_superado.py`, `tools/escribe_relevo_consumo.py` — se benefician del join extendido sin editarse, salvo un comentario); 2 leen `camino_linaje` de un objeto YA construido por el join (`tools/consulta_gen2.py`, `tools/snapshot_motor_gen2.py` — pass-through, sin cambio); el resto matchea por `resultados.tsv`/`funciones_dependencia` como substring de `demanda-resultados.tsv` o referencia a `usos.tsv` (columna homónima, tabla distinta, fuera de perímetro) o testea `corrida0._propaga_envuelto` directamente sobre oferta sintética (no la vista TSV) — cero cambios, verificado por lectura de cada uno.
- `[EJECUTADO]` reproducción del cálculo completo (`corrida0._lee_oferta(False)` + `_propaga_envuelto`) contra las 65 287 filas de `resultados.tsv` en `origin/main` en una sola pasada consistente: 65 240/65 287 reproducen byte a byte su `camino_linaje` publicado; 47 (tres `spec_id`, todas de corridas activamente en curso hoy por otros actos) difieren porque `decisiones.tsv` — edición manual de mesa, no insumo sellado — recibió filas nuevas entre el momento en que `resultados.tsv` se publicó y esta lectura. No es un defecto del cálculo: es la naturaleza de derivar bajo demanda contra un insumo vivo, declarada en el módulo y en `data/INFRAESTRUCTURA-v1_0.md`.
- `[EJECUTADO]` `python3 tools/corrida0.py registro --escribe` (con `--lote` para 13 corridas ajenas que `_para_si_pisa_replay` exigió nombrar, evidencia de replay fresca de otros actos, no de este) contra el código YA cambiado (columna quitada): `resultados.tsv` sale en 96.7 MB, 71 301 filas (subió de 65 287 en las horas transcurridas — actos concurrentes activos). **Hallazgo fuera de perímetro, declarado y no perseguido aquí**: el campo `valor` pasó de 1.4 MB (21.5 bytes/fila) a 68.8 MB (965 bytes/fila) por varias `CALC-*-PISOS-*`/`CALC-ENOE-PERSISTENCIA-0001` que serializan tablas completas en una sola celda (`RESULT-ENOE-PERSISTENCIA-TABLA` sola pesa 11.56 MB). Quitar `camino_linaje` seguía siendo necesario y correcto (ahorra ~35-40 MB que de otro modo se sumarían a este mismo problema) pero **no basta hoy, por una causa que este acto no toca ni rediseña** (no es dedupe/bajo-demanda de `camino_linaje`, es una columna distinta con datos distintos). El commit NO trae un `resultados.tsv` regenerado a mano (el derivador automático de `verify.yml`, con su propio `--lote`, es quien re-deriva sobre `main` real tras el merge — regenerar aquí con datos de hace horas sería publicar una vista ya vieja). Confirmado con mesa (pregunta directa, 24/sep/2026): bajar la guarda a 50 MB de todos modos, aceptando que puede volver a fallar por esta causa nueva, ya documentada.
- `[SUPUESTO]` que ningún consumidor de `camino_linaje` toma una decisión de adopción/clasificación con él (es trazabilidad, per `_propaga_envuelto`'s docstring y el propio encargo -2). Verificado en el censo de arriba: ninguno de los 22 lo usa para bifurcar lógica, todos lo usan para mostrar/propagar texto.

## 4 · YA HECHO / YA DECIDIDO
`git ls-remote --heads origin | grep -i vista-normalizada` → 0 (ninguna rama viva con este rótulo). `grep -n "camino_linaje" tools/corrida0.py` antes de este acto → aparecía en `COLS_VISTA_RESULTADOS` (línea ~3032 de esa versión) y siete sitios más (construcción en memoria, `COLS_VISTA_CORRIDAS`, `COLS_VISTA_USOS` — los tres últimos, ajenos, no se tocan). `tools/vista.py` (COMMIT-A) ya existía con `join_resultado`/`leer_resultados_join`/`corridas_por_id`; este acto lo EXTIENDE (no lo reescribe) con `linajes_por_resultado`, `_linajes_cache`, `_deriva_camino_linaje`.

## 5 · PIEZAS
- **P1 · `tools/vista.py`.** `linajes_por_resultado(oferta=None, decisiones=None)` reejecuta `corrida0._lee_oferta(False)` + `corrida0._propaga_envuelto` (la misma máquina que escribía la columna, no una segunda) y devuelve `{(corrida_id, resultado_id): camino}`. `join_resultado`/`leer_resultados_join` lo consumen: campo propio manda; fila `DEMANDA` es el patrón mecánico gratis; fila `OFERTA` usa `linajes` explícito o el cache de proceso (`_linajes_cache`, un solo cálculo caro — ~70s medido, ~310 `CALC-*/` — por proceso, nunca por fila); fila sin `origen` reconocible (fixture legacy) no dispara nada, igual que antes de este acto. Resultado esperado: cero cambio de comportamiento para un consumidor que no pide `camino_linaje`; el que lo pide lo recibe igual, sin columna.
- **P2 · `tools/corrida0.py`.** `camino_linaje` sale de `COLS_VISTA_RESULTADOS` (una línea). El cómputo en memoria (`_propaga_envuelto`, `filas_resultados`) NO se toca: `usos.tsv` sigue necesitándolo para encadenar su propio `camino_linaje` (columna homónima, ajena, `COLS_VISTA_USOS`). Resultado esperado: `_escribe` deja de proyectar la columna; nada más cambia.
- **P3 · Consumidores.** Censo por archivo (§3): cero ediciones de comportamiento donde el join ya cubre el campo; un comentario en `milpa/src/emisor.py` documentando el cache de proceso. Si `<consumidor lee camino_linaje sin pasar por join_resultado>` resulta falso en algún archivo no censado, se declara y se adapta ahí mismo (≤10 líneas, LATITUD).
- **P4 · Guarda de CI.** `.github/workflows/verify.yml`: colapsa el tramo de aviso a 100/50 a un solo `if` que falla a 50 MB, cita la firma.
- **P5 · `data/INFRAESTRUCTURA-v1_0.md`.** Fila de `resultados.tsv` (22 columnas, no 23) y de `corridas.tsv` (aclara que su `camino_linaje` es ajeno) actualizadas ANTES de este commit final (D-15).
- **P6 · Test de reproducción.** `tests/fixtures/vista-camino-linaje-muestra500.json`: muestra de 500 filas reales (semilla fija 20260924) con su `camino_linaje` verificado en una sola pasada consistente contra el cálculo fresco, ANTES de que la columna saliera. `tests/test_vista.py::PruebaCaminoLinajeBajoDemanda` la reproduce vía `vista.linajes_por_resultado()` real — huérfana (D-21), no entra a `check.py --rapido`.

## 6 · LATITUD
Nombres de función/variable en `tools/vista.py`: tuyos. Formato del fixture (JSON vs TSV): tuyo. Un consumidor no censado que sí decide con `camino_linaje`: se adapta y se declara, no PARA. El hallazgo de `valor` (P3 de §3): declarado aquí y en el hallazgo de cierre; no se investiga más a fondo ni se corrige — es de otro acto.

## 7 · PAROS — lista cerrada
a) no aplica · b) tocar un sello, un RESULT, `--force`, `--excluye`, comprimir o binarizar, Git LFS, sacar la vista del canal, o intentar corregir el hallazgo de `valor` (P3) reabriendo el esquema · c) mover contadores a mano · d) no aplica · e) CAJA · f) el objetivo (sacar `camino_linaje`, bajar la guarda) sigue alcanzable y se alcanzó — el hallazgo de `valor` no lo invalida, lo declara.

## 8 · COMPUERTAS
«Test de reproducción de 500 filas reales antes de bajar la guarda» protege: **congelar** (no se declara terminado sin verificar que el texto sigue siendo el mismo). «Guarda en CI a 50 MB» protege: **borrar** (un GH001 silencioso es una publicación perdida — igual que en -2).

## 9 · PERÍMETRO Y CONCURRENCIA
Propio: `tools/vista.py`, `tools/corrida0.py` (solo `COLS_VISTA_RESULTADOS` y comentarios adyacentes), `milpa/src/emisor.py` (solo el comentario de P1), `tests/test_vista.py`, `tests/fixtures/vista-camino-linaje-muestra500.json`, `data/INFRAESTRUCTURA-v1_0.md` (sección de vistas), `.github/workflows/verify.yml` (guarda), `no-corrido.tsv` (cierre de la fila `f1b2-01`), nota, L0, cascada. Ajeno: CALC, specs, `decisiones.tsv`, `data/corrida0/{corridas,resultados,usos}.tsv` (el derivador automático los regenera tras el merge, no este acto a mano), el problema de `valor` (§3, declarado, no perseguido). En vuelo verificado (`git ls-remote --heads origin`): `acto/gen2-adopcion-bloque-y-pines-4` (fusionada, PR #1119, ya incorporada por fast-forward antes de empezar) y actividad continua sobre `decisiones.tsv`/corridas nuevas (de ahí el drift de 47 filas en §3) — ninguna toca `tools/vista.py` ni `COLS_VISTA_RESULTADOS`.

## 10 · LO QUE NO HACE · SUCESORES
No corrige el hallazgo de `valor` (§3/§6) — sucesor sin nombrar todavía (necesita triage propio: qué corridas, si el patrón `-TABLA`/`-JSON` completo debe seguir viviendo en `valor` o normalizarse aparte). No adopta, no mide, no toca microdato. No re-abre dedupe-vs-bajo-demanda (ya resuelto por la cardinalidad medida en -2). Sucesor de este acto: ninguno si «Hecho»; uno nuevo solo si un consumidor no censado resulta decidir con `camino_linaje`.
