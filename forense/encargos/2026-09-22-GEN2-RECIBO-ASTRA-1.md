# ENCARGO · ACTO GEN2-RECIBO-ASTRA-1 · Los seis CALC de Astra (PR #1030 ENCIG, PR #1031 ENVIPE) recibidos con veredicto por CALC: ENTRA / NO-ENTRA a los pilotos 4 y 5, con la razón verificada por comando

> ENTORNO: **NUBE** — cero microdato: lee `spec.yaml`, `ejecucion.json`, `sello.json`, historial `git`. Hook imprime ENTORNO-DERIVADO; si dice CAJA, PARA en una línea.

CABECERA · SHA de redacción `619748f5` (re-deriva al abrir; main movido no es PARO) · una sola sesión, rama propia `acto/gen2-recibo-astra-1` (D-17) · MODELO: Sonnet, puede subir a Opus si (b) exige leer historial largo · MODO: **ABIERTO** · CONTADOR: cero mediciones, cero corridas propias; **no adopta**; no mueve `cuenta_gen2` de ningún CALC · ids con raíz de acto (D-24).

## 1 · OBJETIVO
Que exista en `origin/main` un recibo (nota) que diga, por cada uno de los seis `CALC-ASTRA-*`, si cumple lo que la ADENDA-1 del piloto 4 exige para entrar como candidato externo — (a) mismas celdas que la spec del piloto, (b) congelado antes de cualquier lectura de la ola de evaluación, (c) punto, IC y tipo de incertidumbre por celda — más las tres etiquetas de contador y el asiento de replay; y que mesa tenga una recomendación de fusión por PR con su razón. Habilita: la decisión de mesa sobre si C-ASTRA entra al piloto 4 (ENVIPE) antes de su COMMIT-1 y al piloto 5 (ENCIG) al lanzarse.
«Hecho» = sobre el commit final con `origin/main` fusionado: `ls forense/notas/ | grep -c RECIBO-ASTRA-1` → 1; `grep -c 'CALC-ASTRA' forense/notas/<nota-recibo>.md` → ≥ 6; `awk -F'\t' '$3=="GEN2-RECIBO-ASTRA-1"' forense/no-corrido.tsv` lista las NC que Astra no abrió (o «Ninguno.» en la nota); `grep -c 'GEN2-RECIBO-ASTRA-1' forense/firmas-pendientes.tsv` → ≥ 1 (la fila de fusión para mesa).

## 2 · FIRMAS DE MESA
Ya selladas, se citan: ADENDA-1 del piloto 4 (rama `acto/gen2-celda-d-piloto-4-encogida-1`, archivo `forense/encargos/2026-09-22-GEN2-CELDA-D-PILOTO-4-ENCOGIDA-1-ADENDA-1.md`, sellada al recibirse 20:08) — criterios (a)–(c) verbatim ahí; REPARTO-DOBLE-MANO (Astra investiga, Claude gobierna y adjudica; mesa 22/sep, transfer de dirección). Ninguna nueva. La fusión de #1030/#1031 es de mesa: este acto la recomienda, no la ejecuta.

## 3 · LO QUE DIRECCIÓN SABE
- `[EJECUTADO]` `git ls-remote --heads origin` → `codex/astra-encig-1` (5 commits adelante de main) y `codex/astra-interaccion-dinamica-1` (8). `git grep -o 'pull/[0-9]*'` en sus recibos → #1030 (ENCIG) y #1031 (ENVIPE).
- `[EJECUTADO]` `git diff --stat origin/main...origin/<rama>`: ENCIG trae `CALC-ASTRA-ENCIG-EDADXSEXO-0001`, `CALC-ASTRA-ENCIG-ESCOLARIDADXSEXO-0001`, dos specs humanas en `forense/prereg-caja/`, `tools/astra/encig/emitir.py`, +2 filas en `forense/replay-evidencia.tsv`, 18 archivos. ENVIPE trae `CALC-ASTRA-ENVIPE-{DOMINIOXSEXO, EDADXESCOLARIDADPROXY, EDADXSEXO, ESCOLARIDADPROXYXSEXO}-0001`, cuatro specs humanas, `tools/astra/envipe/{model,scaffold}.py`, +4 filas en replay-evidencia, 39 archivos. Ninguna de las dos toca tablero, `decisiones.tsv` de main ni celdas-D (diff leído; **re-verifícalo tú**).
- `[EJECUTADO]` `ejecucion.json` de los seis: `input_ids` ENCIG = `historico_2021`, `historico_2023`, `marginales_publicos_2025`; ENVIPE = `envipe2023_csv`, `envipe2024_csv`, `MARGINALES-PUBLICOS`. Ningún id nombra microdato 2025/2026. **No sé** si los sha256 corresponden a payloads del manifiesto ni si «marginales públicos 2025» son tabulados de una ola reservada (E.6: los tabulados de una ola reservada tampoco se leen). Eso lo decide P2.
- `[LEÍDO]` Recibo de Astra en `codex/astra-encig-1:forense/analisis/astra-encig/mandato.md`: dice que «el encargo posterior de Jonás exige CALC en main antes de COMMIT-2» y sustituye el plazo «al abrir/en COMMIT-1». Es `[REPORTADO]` por Astra; la ADENDA-1 dice «al abrir el acto». Si chocan, manda la adenda sellada y la diferencia va a mesa como pregunta.
- `[EJECUTADO]` Piloto 4 (`acto/gen2-celda-d-piloto-4-encogida-1`): tres commits (0-bis 20:03; adenda+firmas 20:08; spec humana `TRA-evade-norma-cruces-encogida-spec-v1_0.md` 20:28). **Sin COMMIT-1 al escribir.** Su lista final de cruces vive en esa spec humana (rama, tipo (3): léela ahí, cita líneas).
- `[EXISTE]` `forense/encargos/2026-09-19-GEN2-RECIBO-CODEX-3.md` … `-6.md`: recibos previos; sirven como patrón de veredicto, no como norma.
- ADJUNTOS: ninguno. Todo lo citado está en el repo o en las dos ramas.

## 4 · YA HECHO / YA DECIDIDO
`ls forense/encargos | grep -ci RECIBO-ASTRA` → 0. `git grep -l 'CALC-ASTRA' origin/main -- forense canon data` → reporta (dirección: los CALC no están en main; el token aparece en 0 encargos). `firmas-pendientes.tsv`: 0 filas con `ASTRA` en estado ABIERTA (625 NC y todas las FP examinadas por lector CSV). Ramas vivas con este objeto: ninguna aparte de las dos de Astra. **Repítelo tú.**

## 5 · PIEZAS
- **P1 · Etiquetas y asiento, por CALC (seis).** `cuenta_gen2`, `adopta`, `origen_numerico` presentes en `spec.yaml` y coherentes con «candidato externo, no adopta»; `sello.json`/`sello.sha256` cierran; fila en `replay-evidencia.tsv` con `resultado_replay` y `contexto_replay` (E.7). Un CALC sin las tres etiquetas o sin asiento = NO-ENTRA por forma, aunque el número sea bueno.
- **P2 · Orden spec→dato y olas (criterio b).** Por historial: `git log -p -S <sha256 de cada input>` y `-S <payload>` sobre la rama; el commit que congela `spec.yaml` precede a cualquier commit que lea la ola de evaluación. Cruza cada `input_sha256` contra `data/manifiesto.yaml` por id y sha (A.15, con conteo). Si algún input es tabulado o comunicado de ENCIG 2025 / ENVIPE 2025 / ENVIPE 2026 en estado reservado: **NO-ENTRA**, NC con categoría del defecto (nunca el contenido, E.6) y aviso a mesa en la misma nota. Si el orden no es derivable del historial (squash, rebase): RETROSPECTIVA-MECÁNICA como máximo, y se dice.
- **P3 · Celdas (criterio a) y forma (criterio c).** ENVIPE: contra la lista final de cruces de la spec humana del piloto 4 (rama, líneas citadas). ENCIG: contra `marcador-segmento.tsv` filas `RESERVADA`/`EMITIDA-SIN-R` de ENCIG 2025 por eje, porque el piloto 5 no está lanzado — el veredicto ENCIG es «entraría si el piloto 5 pide estas celdas». Por celda: punto, IC y tipo de incertidumbre declarados; unidad (persona / trámite / delito) rotulada, y que no se promedie con otra (§4 v2.16). Proxy de escolaridad en ENVIPE: si la spec del piloto define escolaridad de otra forma, es NO-ENTRA por (a), no por calidad.
- **P4 · Recibo y recomendación.** Nota `forense/notas/2026-09-2X-GEN2-RECIBO-ASTRA-1-recibo.md` con tabla: CALC · (a) · (b) · (c) · etiquetas · asiento · veredicto ENTRA/NO-ENTRA · razón por comando. Recomendación por PR: FUSIONAR / FUSIONAR-CON-NC / NO-FUSIONAR, y una fila en `firmas-pendientes.tsv` por PR para que mesa selle. NC por cada defecto que Astra no abrió (D-21). Comentario en el PR de GitHub solo si la sesión tiene credencial; si no, la nota basta y se dice.
- **P5 · Si `[SUPUESTO]` «el piloto 4 sigue sin COMMIT-1 al cerrar este recibo» resulta falso** (ya congeló): el veredicto ENVIPE se rotula «para duelo posterior», no para el piloto 4, y la nota lo dice en la primera línea. No se pide al piloto reabrir nada.

## 6 · LATITUD
DECIDES TÚ: orden de las piezas, formato de tabla, si comentas en GitHub. Remover obstáculos reversibles: `git fetch --unshallow`, instalar `pyyaml`, regenerar un derivado por comando. Defecto adyacente ≤ 10 líneas que impida terminar: arréglalo y decláralo.
PREGUNTAS A MESA (con 2–3 opciones y recomendación; sigues con lo demás): (i) si la adenda («al abrir») y el mandato citado por Astra («antes de COMMIT-2») chocan, ¿cuál rige para el piloto 4? Recomendación de dirección: rige la adenda sellada; C-ASTRA entra al piloto 4 solo si #1031 está en main antes de su COMMIT-1. (ii) si un CALC cumple (a)–(c) pero le falta una etiqueta: ¿FUSIONAR-CON-NC o devolver? Recomendación: devolver — «etiquetas completas o no cuentan» (transfer §6).
NO DECIDES: nada de §7.

## 7 · PAROS — lista cerrada
a) abrir, derivar o imprimir dato de ENCIG 2025, ENVIPE 2025 o ENVIPE 2026 fuera del código congelado de un piloto (este acto no tiene ninguno autorizado) · b) borrar, forzar o reescribir un sello de Astra o de cualquier CALC · c) adoptar, o poner `cuenta_gen2` a un CALC de Astra · d) editar `spec.yaml` de Astra para que cuadre con (a)–(c) — un CALC se recibe como está o se devuelve · e) CAJA · f) los PR ya no existen o ya están en main sin recibo → PARO, y eso es el entregable.

## 8 · COMPUERTAS
«Ningún commit de este acto toca `data/corrida0/CALC-ASTRA-*/`» protege: **borrar/reescribir sellado**. «El veredicto ENTRA para ENVIPE exige (b) verificado por historial, no por declaración de Astra» protege: **adoptar** (un candidato que entra sin (b) sería retrospectivo rotulado prospectivo). Todo lo demás es orden sugerido: P2 antes de P3 (si falla (b), (a) y (c) sobran).

## 9 · PERÍMETRO Y CONCURRENCIA
Propio: la nota del recibo · `forense/no-corrido.tsv` (append) · `forense/firmas-pendientes.tsv` (append) · `forense/hallazgos.md` (append) · `canon/L0/<ADR-raíz>.md` · cascada de /acto. Ajeno que no se toca: las dos ramas de Astra y sus CALC (se leen, no se editan; una corrección es una devolución) · `data/corrida0/marcador-segmento.tsv` (derivado) · celdas-D · `tramite.yaml`.
Archivos que OTRO ACTO EN VUELO está tocando ahora: piloto 4 en CAJA escribe `forense/firmas-pendientes.tsv` (append), `forense/prereg-caja/TRA-evade-norma-*`, `data/corrida0/CALC-TRA-EVADE-NORMA-*` (por nacer) — no los toques; append con union y `git merge-file --union` local si choca (`.gitattributes:16`), nunca el editor web. Astra sigue empujando a sus dos ramas: fija el SHA de cada rama que recibes en la nota; lo que llegue después es de un recibo siguiente.
«Si te encuentras escribiendo fuera de esta lista, PARA.» Perímetro de cierre permanente (D-21) aplica sin enumerarlo.

## 10 · LO QUE NO HACE · SUCESORES · CIERRE
No mide, no adopta, no fusiona, no edita a Astra, no lanza el piloto 5. Sucesores: mesa fusiona o devuelve #1030/#1031 con la fila FP; el piloto 4 copia por id lo que ENTRA si su COMMIT-1 aún no existe; `GEN2-CELDA-D-PILOTO-5-ENCOGIDA-ENCIG-1` copia por id lo que ENTRA de ENCIG; `GEN2-RECIBO-ASTRA-2` para lo que Astra empuje después del SHA fijado aquí. Auditoría de rigor extremo: no carga (el recibo no afirma sobre México; los CALC de Astra la traerán cuando un piloto los adjudique). Cierre por /acto: `## NO-CORRIDO / RESERVAS` («Ninguno.» obligatorio si aplica) y `## CONSUMIDO` los añade /acto al pie; adendas de mesa como `<este-encargo>-ADENDA-N.md`.
