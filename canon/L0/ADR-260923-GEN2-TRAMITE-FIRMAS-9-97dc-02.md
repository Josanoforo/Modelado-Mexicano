**ADR-260923-GEN2-TRAMITE-FIRMAS-9-97dc-02**, `ACTO GEN2-TRAMITE-FIRMAS-9 · ADENDA-1`, 23/sep/2026, NUBE, Sonnet 5, sin sub-agentes, MODO ABIERTO. Dos hallazgos sobre la marcha del acto original (mismo PR #1032). (1) P2 tuvo que corregirse: CI rechazó commitear `marcador-segmento.tsv`/`estimadores-por-segmento.yaml` directamente (firma `GEN2-TUBERIA-EFICIENCIA-1`, 21/sep: los `# DERIVADO — NO EDITAR` no viajan en PR); revertidos a `origin/main`, el job de push a `main` los re-deriva solo tras fusionar — mismo mecanismo que `#1026` ya usó. (2) La rama se sincronizó limpio con `main` tras la fusión de `#1033`. Adenda-1 (P4/P5) llegó con el acto ya `## CONSUMIDO`: P4 (`adoptados_activos` 72→87) verificado por dry-run sin commitear, mismo motivo que (1); P5 (10 filas de `no-corrido.tsv` corridas, según la adenda) resultó ser 16, verificadas con lector de CSV — 15 con `sucesor`=`ABIERTA`/`estado` vacío, una más corrida un paso; texto real de `sucesor` irrecuperable sin inventar, se dejan como están. NC `…-673c-01/02`. Adjunto `PENDIENTES-PROGRAMA-v3.md` de la adenda nunca llegó a esta sesión. Suite `--rapido` VERDE, 0 FAIL; `derivados_protegidos.py --toca` limpio.
