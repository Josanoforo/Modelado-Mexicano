# ENCARGO · ACTO PURGA-2 — destrucción autorizada del segundo espejo

*(Archivado verbatim por regla A.3. **Estado: `CONSUMIDO`** — ejecutado en `ACTO PURGA-2`, `ADR-176`, 25/ago/2026; ver `forense/notas/2026-08-25-purga-2-cierre.md`. La cadena que la compuerta `ORDEN` exige se verificó en el árbol (`ADR-173`, `canon/gobernanza-v1_15.md:3571`, fusionado en `origin/main` vía `PR #347`/`c502a43`) — no de memoria ni de este encargo. Re-verificación en fresco sin diferencia frente a `FP-151`: `count-objects -v` in-pack=3053, `fsck --unreachable --dangling` 0 líneas, 167 refs, sin `logs/` ni `alternates`. `rm -rf ~/BACKUP-mm-mirror-2026-08-10.git` ejecutado; ausencia confirmada por `ls`/`test -e`. Una desviación declarada, no silenciosa: el verbatim real de mesa que `FP-151` registra no es la cadena exacta letra por letra — es *"ahora dame los encargos incluyendo: autorizo destruir backup etc etc"* — sino una inferencia que `ACTO SELLA-G` hizo sobre ese mensaje ("el referente inequívoco es la cadena que `FP-151` exige"). La compuerta de este encargo pedía la cadena "en el árbol", condición que sí se cumple; no pedía verbatim de mesa sin mediar interpretación, a diferencia de la compuerta histórica de `mm-purga.git` que dos actos anteriores pararon por esa razón exacta. Detalle completo en la nota.)*

---

Redactado por    dirección, 25/ago/2026 · Firma que ejecuta: L15 de mesa, registrada verbatim por SELLA-G con su referente inequívoco — la cadena AUTORIZO DESTRUIR BACKUP-mm-mirror-2026-08-10.git (FP-151)
⛔ ORDEN    Tras fusionar SELLA-G — la cadena debe estar en el árbol; este acto la cita del ADR, no de memoria ni de este encargo.
ENTORNO    UBUNTU (el espejo vive en el disco de la caja). NO NUBE. · Modelo: Sonnet (mecánico) · 🚫 --freeze · pgrep -af claude · iconv
CONTADOR    Cero — higiene de disco con autorización destructiva. Declarado (v2.3).

════════ ARRANQUE ════════ 1 · REPO: clon existente; ruta · git log -1 · git status. 2 · SHA: la base debe INCLUIR SELLA-G fusionado — verifica que su ADR contiene la cadena; si no está: PARA (una destrucción sin cadena en el árbol no se ejecuta). 3 · data/raw: enlaza al corpus; este acto no la usa — repórtala igual. 4 · ENTORNO tres partes (A.2): sin_variable · sonda INEGI · ls data/raw/ | head -1. ⚠️ [v2.11] A.13 en todo negativo. 5 · ESPEJO del proyecto: nada; y el espejo a destruir se verifica, no se asume. ══════════════════════════

═══ VERIFICACIÓN DE EXISTENCIA (dirección) ═══ FP-151 midió el objetivo con el método de FP-143: ~/BACKUP-mm-mirror-2026-08-10.git, 13M, in-pack 3053 (1081 blob · 603 commit · 1369 tree), 167 refs; solo 2 objetos no viven en el clon base y ninguno es contenido: un commit sintético de GitHub (refs/pull/164/merge, instante-anterior de un PR ya fusionado en main) y un tree con cero blobs exclusivos. La cadena de mesa llega por SELLA-G. ═══════════════════════════

Tareas
Cita la cadena desde el ADR de SELLA-G (archivo:línea) y el verbatim original de mesa que ese ADR registra.
Re-verifica la premisa en el momento de ejecutar (el disco pudo cambiar): git -C ~/BACKUP-mm-mirror-2026-08-10.git count-objects -v y fsck — pega salidas; si la forma difiere de la medida por FP-151 (conteos u huérfanos distintos), PARA y reporta: una autorización sobre un objeto medido no cubre un objeto que ya no es ése.
Si coincide: destruye (rm -rf), verifica la ausencia (ls), pega salidas crudas.
FP-151 y la mini-fila de SELLA-G → ejecutada_en con las salidas; una línea en forense/hallazgos.md; nota 2026-08-25-purga-2.md; ADR (re-derivado; renumera si colisiona); recifrado estándar; suite VERDE con tail; encargo CONSUMIDO.
PERÍMETRO

Tablero · canon/gobernanza-v1_15.md · canon/estado-programa-v1_10.md · forense/hallazgos.md (una línea) · forense/notas/2026-08-25-purga-2.md · forense/encargos/2026-08-25-PURGA-2.md · el directorio ~/BACKUP-mm-mirror-2026-08-10.git (fuera del repo, destino declarado). Nada más. Fuera de la lista: PARA. Concurrencia: NUBE/Codex en paralelo permitido; renumera quien fusiona segundo. Los packs NUBE-2 y UBUNTU-2 anteriores siguen vigentes tal cual y pueden correr en sus cajas cuando mesa quiera.

## CONSUMIDO

Derivación mecánica (`/tramite`, §3.3): único `Merge pull request #N` cuyo mensaje cita el rótulo `PURGA-2` — `PR #348` (`66e7eac`), y ese merge toca este archivo además de otros 5 (`git diff --stat 66e7eac^1 66e7eac`).
