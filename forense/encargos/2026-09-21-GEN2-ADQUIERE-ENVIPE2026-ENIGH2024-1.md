# ENCARGO · ACTO GEN2-ADQUIERE-ENVIPE2026-ENIGH2024-1 · LOS DOS PAYLOADS QUE EL DUELO Y LA RESERVA ESPERAN ENTRAN AL CORPUS CON SU HASH, SIN QUE NADIE LOS ABRA — Y EL DUELO ENVIPE 2026 QUEDA CONGELADO SI SU PR YA ESTÁ EN MAIN

> ENTORNO: **CAJA** (es donde vive el corpus y el agente de adquisición). Arranque de `/adquiere` §0: `data/raw` enlazada, CAJA por tres señales, sonda a INEGI. Si el hook no dice CAJA, PARA en una línea. NO es NUBE: desde `cloud_default` el egreso a INEGI está DENEGADO y desde `milpa-inegi` el payload no cae en el corpus compartido.

CABECERA · SHA de redacción `b86c40e0`; re-deriva al abrir · una sola sesión, rama `acto/gen2-adquiere-envipe2026-enigh2024-1` · MODELO: Sonnet (receta; se puede subir) · MODO: **ABIERTO** · vehículo: `/adquiere`, dentro de `/acto` · CONTADOR: ninguno numérico; si P4 aplica, tampoco (congelar no sella) · FP/ADR/NC: raíz de acto.
**Si al fusionar `main` choca la línea L0 de `canon/estado-programa-v1_14.md`: NO conserves los dos lados; toma la de `main` y re-inserta solo tu anotación.** `canon/L0/` ya existe: tu anotación va ahí como fragmento.
**Candado del agente:** el launcher de `/adquiere` toma un lock; no corras otra descarga a la vez. `[LEÍDO: nota de #974]` `mm-corpus/raw/raw -> raw` es un symlink en bucle: no lo sigas (`--no-links` / `followlinks=False`); no lo toques.

## 1 · OBJETIVO
Dos payloads esperan en la cola desde `#960` y ninguno ha entrado: **ENVIPE 2026** (prioridad 1) es lo único que impide congelar el duelo prospectivo —construido, ensayado y sellable en `#968`, `NO-CONGELADO` solo por `input_manifiesto_AUSENTE=envipe2026_csv` `[LEÍDO: forense/notas/2026-09-21-GEN2-DUELO-ENVIPE2026-COMMIT-1-nota.md:24,29]`— y cada semana sin sello degrada la ceguera (fecha límite del COMMIT-2: 31/oct). **ENIGH 2024** (prioridad 2) ya tiene reserva asentada en `#964` y diseño en papel. «Hecho» significa: los dos payloads en el corpus compartido, con entrada en `data/manifiesto.yaml` (id, archivo, sha256, tamaño, `url_origen`, `descargado_por`), verificados por hash; sus descriptores y cuestionarios registrados; las dos filas de la cola en `OBTENIDO`; y, si `#968` ya está en `main`, el duelo congelado (P4).

## 2 · FIRMAS DE MESA — verbatim
Reserva ENVIPE 2026 (21/sep; `data/corrida0/decisiones.tsv`, clave `reserva:envipe2026`): «ENVIPE 2026 queda RESERVADA desde hoy, entera: nacional, marginales por eje y cruces. Bajar el payload está permitido; abrirlo, derivar de él o leer sus tabulados, no, fuera del código que se congele para ello. Rige E.6.»
Reserva ENIGH 2024 (21/sep, asentada por `#964`; si aún no está en `main`, rige igual por este encargo): «ENIGH 2024 queda RESERVADA desde hoy, entera, en los mismos términos que ENVIPE 2026. Y como regla: toda ola nueva de una encuesta con historia en el corpus nace RESERVADA al entrar al manifiesto; la levanta solo el código congelado de una prueba pre-registrada, o mesa por escrito.»
Mandato (21/sep): «Todo lo que sea descargas se baja […] lancemos el agente.» «Descargas se hacen desde caja, siempre ha sido así.»
**Precisión de dirección sobre la reserva, para que el agente no pare por su propia herramienta:** `tests/manifiesto.py` calcula `miembros_zip` —nombre, CRC32 y tamaño de cada miembro, sin extraer— como parte de la identidad A.7 (`:1571-1580`). Esa lista es envoltura, no dato: **está autorizada**. Lo que NO: descomprimir, leer una fila, abrir un CSV, leer tabulados, comunicados o presentaciones de resultados de 2026 o de 2024.

## 3 · LO QUE DIRECCIÓN SABE (contra `b86c40e0`, sin corpus)
- `[EJECUTADO]` `python3 tools/adq_doctor.py --selecciona` → **ELEGIDOS (0)**; `ENVIPE_2026` y `ENIGH_2024_NC` salen **EXCLUIDOS: «estado 'SOLICITUD-PRE-CONFIRMADA' fuera del contrato de elegibilidad»**. `[LEÍDO: tools/adq_doctor.py:650-730]` el contrato solo camina `PENDIENTE` (y `NO-OBTENIDO-POR-ESTE-AGENTE` tras el plazo); el token `AUTORIZADA:` se exige solo a filas con `SONDA-LATERAL-RECOMENDADA` en la nota, que no es el caso. **`#960` escribió un estado que el agente no reconoce: sin P1, `/adquiere` vuelve con caminata vacía.**
- `[EJECUTADO sobre data/curacion-registro/cola-adquisicion-registro.tsv]` las dos filas traen `url_conocida` por patrón (`…/envipe/2026/datosabiertos/conjunto_de_datos_ENVIPE_2026_csv.zip`; `…/enigh/nc/2024/datosabiertos/` — directorio, no archivo), `ids_manifiesto = SIN-ID-MANIFIESTO`, `RESIDUAL-ADQ-V1` con objeto, cobertura y residual («la URL NO está confirmada — se deriva por patrón»). Es el protocolo de rutas múltiples de `/adquiere` §3 (i)–(iv).
- `[LEÍDO: .claude/commands/adquiere.md:57-64]` arranque: `ln -s /home/pc0/mm-corpus/raw data/raw` si falta; CAJA por tres señales; `curl` a INEGI. `[LEÍDO: :83-96]` la selección se emite antes de caminar con `tools/adq_doctor.py --selecciona` y su salida se pega en el cierre. El escritor canónico de la cola es `tools/curador_registro/tsv_crudo.py::upsert_fila`.
- `[LEÍDO: nota de #968, §3 y sucesor]` «al entrar `envipe2026_csv` al manifiesto con sha, la única edición autorizada es `preflight` VERDE sobre ese input — ningún parámetro, fórmula ni umbral». Los CALC del duelo llevan `input_manifiesto` con el id `envipe2026_csv`: **usa ese id exacto** al registrar, o el preflight no lo encuentra.
- `[REPORTADO por la nota de #960]` desde `milpa-inegi` la URL de 2026 no se verificó con GET; la de ENIGH 2024 tampoco.

## 4 · YA HECHO
Por objeto («envipe2026», «ENIGH 2024», «adquiere») en manifiesto, cola, encargos y ramas vivas: filas en cola desde `#960`, 0 entradas en el manifiesto, ninguna caminata posterior. **Repítela tú** (`grep -c envipe2026 data/manifiesto.yaml` debe dar 0 al abrir).

## 5 · PIEZAS — en orden
**P1 · Habilitar las dos filas.** Por el escritor canónico (`upsert_fila`), `estado_A4A5 → PENDIENTE` en `ENVIPE_2026` y `ENIGH_2024_NC`, y en la nota de cada una la regla de reserva de §2 verbatim. Nada más de la fila cambia. Luego `python3 tools/adq_doctor.py --selecciona` debe listar **ELEGIDOS (2)** y nada más nuevo; pega la salida. Si lista otra cosa, no la camines: `/adquiere 2` y `--nombrada` acotan.
**P2 · ENVIPE 2026.** `/adquiere` sobre `ENVIPE_2026`: microdato CSV (zip), descriptor de archivos, cuestionario, diseño muestral; protocolo (i)→(iv) si la URL por patrón falla; registro en el manifiesto con id **`envipe2026_csv`** para el zip y los ids hermanos por la convención de 2025 (`envipe2025_*`); sha256 y tamaño reales; fila de cola → `OBTENIDO`. **Cuestionario, descriptor y diseño sí se leen; el zip no se abre** (§2). Verifica al cerrar que el payload quedó en el corpus compartido (`data/raw` → `/home/pc0/mm-corpus/raw`), no en un worktree (defecto PR #77).
**P3 · ENIGH 2024.** Ídem sobre `ENIGH_2024_NC`, localizando el archivo dentro del directorio de datos abiertos: ids por la convención de 2022 (`enigh2024_nc_csv`, `enigh2024_descripcion_base_pdf`, `enigh2024_nota_tecnica_pdf`, y el cuestionario si INEGI lo publica aparte). Misma regla: documentos sí, microdato no.
**P4 · Congelar el duelo — solo si `#968` está en `main` al llegar aquí.** `python3 tools/corrida0.py preflight` sobre los CALC del duelo que llevan `envipe2026_csv`: debe pasar de `BLOQUEADO input_manifiesto_AUSENTE` a `VERDE` (en ADJUDICACION quedan solo los bloqueos de las emisiones aún inexistentes, por cadena 3a). Si VERDE: commit que declara el COMMIT-1 del duelo CONGELADO (D-22 ampliada, requisito 1 cumplido; 2–4 ya demostrados en `#968`), cierra la NC «congelar cuando el payload entre» y actualiza la fila de sucesor. **Ninguna otra edición a esos CALC.** Si no VERDE: no toques nada; NC con el diagnóstico exacto. Si `#968` no está en `main`: NC con sucesor y dilo.
**P5 · Cierre.** Salida cruda de `--selecciona` antes y después; por payload: URL final, bytes, sha256, id; lo NO OBTENIDO con «NO OBTENIDO POR ESTE AGENTE EN N INTENTOS» + receta de un minuto (A.5); nota de media página.

## 6 · LATITUD
Decides tú: orden entre P2 y P3, rutas alternas del protocolo, ids hermanos. Replantea y sigue si INEGI cambió el patrón de URL, si el zip viene partido en varios, o si `--selecciona` exige algo más en la nota (dilo y hazlo por el escritor canónico). Pregunta a mesa, siguiendo con lo demás: si ENVIPE 2026 no está publicada en datos abiertos (solo tabulados) — entonces **no leas los tabulados**: es NO-OBTENIDO con receta.

## 7 · PAROS — lista cerrada
a) descomprimir, abrir, leer una fila, o leer tabulados/comunicados/presentaciones de ENVIPE 2026 o ENIGH 2024 · b) bajar un payload fuera del conducto (`/adquiere`, `tests/manifiesto.py --descarga`) o registrarlo sin sha · c) editar un CALC del duelo más allá de lo que P4 autoriza · d) tocar el symlink en bucle · e) correr con el candado del agente tomado · f) entorno equivocado.

## 8 · COMPUERTAS
«`#968` en `main`» protege: **congelar spec** (P4). Ninguna más.

## 9 · PERÍMETRO
Propio: las dos filas de la cola (por el escritor canónico) y su vista regenerada · entradas nuevas de `data/manifiesto.yaml` · el corpus compartido (solo añadir) · P4: el commit de congelado y la NC del duelo · nota · cascada. Ajeno: todo lo demás; en especial `tools/`, `tests/`, `.claude/commands/`. Si te encuentras escribiendo fuera de esta lista, PARA.

## 10 · NO HACE · SUCESORES · CIERRE
No abre ninguna ola · no corre el duelo · no camina el resto de la cola (las 7 `SOLICITUD-PREPARADA` son de otro contrato). Sucesores: COMMIT-2/3a/3 del duelo ENVIPE 2026 (otra sesión, antes del 31/oct) · comparabilidad ENIGH 2022→2024 en `milpa-inegi` (relanzamiento de `#964` P2) · hallazgo para TUBERÍA: `SOLICITUD-PRE-CONFIRMADA` no es un estado del contrato de `/adquiere`; o entra al contrato, o `/tramite` no lo escribe. Auditoría §5: no aplica. `## NO-CORRIDO / RESERVAS` · `## CONSUMIDO`.
