ENCARGO · ACTO GEN2-C0-D · EL MARCADOR — la pareada de primera clase, B como piso de los tres, y la adjudicación del hallazgo que lleva todo el día esperando

Cabecera: CAJA (UBUNTU), Opus · COMPUERTA: PRIMERA-SILLA fusionado (consume su patrón de adopción y sus contadores verdaderos) · ORDEN DE CAJA: después de PILOTO-SONDA si ambos coinciden · candidatos: deriva, no heredes.

FIRMAS DE MESA que gobiernan, ya selladas — se citan, no se re-piden:

D-2 / FP-348 (FIRMADA, verbatim de mesa): «la idea central de todo este proyecto es un LLM puede superar a LLM con dato (todo el corpus), si esto no está claro ahorita entonces no estamos comparando correctamente.» → la comparación de primera clase es la pareada L_SOLO ↔ L_CORPUS; M tercer contendiente; B piso común de los tres bajo el mismo corte informativo.
El hallazgo a adjudicar, asentado por dirección el 8/sep: el diagnóstico vigente (D4, escala pp) muestra L_CORPUS 19.60 · L_SOLO 11.69 · M 4.51 — el corpus empeorando al LLM. Hoy es marginal sin pareada sellada; este acto lo convierte en veredicto o lo explica (¿capturas L_CORPUS de corpus viejo? ¿métrica? ¿señal real?), y lo que salga se reporta primero.

PIEZAS P1 · Spec del marcador GEN2 (prereg-caja-C0D-MARCADOR-v1_0 + yaml, COMMIT-1 antes de mirar nada): la pareada L_SOLO↔L_CORPUS como comparación primaria (por celda, misma escala, mismo universo — A-bis 3 y 4 gobiernan), L↔M y M↔B secundarias, B de CALC-B-0001 como piso citado por corrida0_resultado_id (el patrón de PRIMERA-SILLA, estrenándose a escala), y pre-declaración B-bis completa: qué significa que la pareada dé corpus-ayuda, corpus-estorba, o no-discrimina — las tres ramas escritas antes del dato. Las fuentes L son las capturas vigentes del duelo (se citan con su fecha y su corpus de origen; si el corpus que vieron es anterior a GEN2, la spec lo declara como límite de alcance — no se re-capturan aquí). P2 · CALC-C0D corre, sella, verifica. Todos los RESULT con tipo/unidad/escala; multiplicidad declarada como en S6 (primaria decide; secundarias se reportan como tales). P3 · Adjudicación escrita. La nota abre con el veredicto de la pareada y el destino del hallazgo corpus-empeora: CONFIRMADO-CON-ALCANCE (y qué alcance), EXPLICADO (y por qué), o NO-DISCRIMINA — con la rama B-bis que cayó, citada. Si el límite de alcance de P1 muerde (capturas pre-GEN2), la adjudicación lo dice en la primera línea y nombra el sucesor: re-captura L_CORPUS con corpus GEN2 (fase 5 del plan de dirección), sin ejecutarlo aquí. P4 · Adopciones del marcador. Los RESULT del marcador que el tablero/§2 consuma se adoptan con el patrón de PRIMERA-SILLA (cita + merge de mesa = adopción); los que no tengan consumidor quedan en el WARN envejeciendo como deben. NC-0024/0026 → CERRADAS con cita; NC-0053 → el precedente pasa a patrón en uso.

PERÍMETRO: forense/prereg-caja/C0D-* · data/corrida0/CALC-C0D*/** + TSV re-derivados · milpa/+tablero solo las citas de P4 · forense/{firmas-pendientes,no-corrido}.tsv · nota · 0-bis · cascada. No toca capturas L, motor M, CALC previos ni sus sellos. Si escribes fuera, PARA. CONTADOR: sí — el marcador es corrida sellada con cadena completa, y sus adopciones bajan el WARN en N declarado. ## NO-CORRIDO obligatoria; dos commits mínimo; cuenta_gen2 del CALC-C0D viaja como FIRMA en el mensaje de lanzamiento (mismo estándar: autoridad, fecha, objeto) o el contador no lo cuenta y se dice.

---

## NO-CORRIDO / RESERVAS

| qué (verbatim del encargo) | por qué | impacto | sucesor |
|---|---|---|---|
| **«NC-0024/0026 → CERRADAS con cita»** | `FUERA-DE-PERÍMETRO` | **No se cierran.** Las dos filas nombran a `C0-D` como sucesor, pero su objeto está fuera del perímetro que este mismo encargo declara («No toca capturas L, motor M, CALC previos ni sus sellos»). `NC-0024` (marcador **por segmento**, `M` por motor matricial) declara depender de **`C0-C`, que no existe en el árbol** (`gh pr list --state all`: ningún acto `C0-C`; `git ls-remote --heads origin`: sólo `main` y esta rama), y las capturas del duelo no traen segmentación — las 14 celdas tienen `x = vacío` y ningún insumo de este marcador puede llenarlo. `NC-0026` (envoltura de `tools/emite_m.py` cableada a `emite_celda`) dice **en su propia fila** que hacerlo *«sería salirse del perímetro»* y cambiaría las `corridas-M` ya selladas. Cerrarlas «con cita» sin ejecutar su objeto sería cerrar deuda por decreto. | `NC-0075`; y `NC-0024`/`NC-0026` siguen **ABIERTAS**, con sucesor re-apuntado: `C0-C` primero, y después un acto de marcador por segmento |
| **«Los RESULT del marcador que el tablero/§2 consuma se adoptan»** · **«NC-0053 → el precedente pasa a patrón en uso»** | `NO-VERIFICABLE-AQUÍ` | **Cero adopciones, medido y no supuesto.** `_ids_corrida0_declarados()` abre sólo `milpa/tramite.yaml` y `milpa/procedencia.yaml`, y las filas que `cmd_demanda` enumera son de cuatro clases: `p` de conducta, coeficiente de generador, corte `CORTES_C1`, momento. El marcador produce **errores en pp, diferencias pareadas, `MAE`, IC y veredictos**: ninguno encaja, y forzar la cita sería miscategorizar para mover un número. **El tablero no es superficie de adopción**: `forense/tablero/*.md` es Markdown que el registro no lee. `NC-0053` **sigue CERRADA** y el patrón de `PRIMERA-SILLA` **no** pasa a «en uso» desde aquí: un patrón entra en uso cuando se usa. Confirma `NC-0072` sobre un caso nuevo. | `NC-0076` — un CALC cuya cantidad **sea** una `p` de conducta, un coeficiente, un corte o un momento; o un acto de mesa que amplíe la superficie que el registro reconoce |
| **«CONTADOR: sí — el marcador es corrida sellada con cadena completa»** | `DECISIÓN-DE-MESA-PENDIENTE` | **El contador no se mueve, y por DOS razones distintas.** (1) El propio encargo condicionó: *«`cuenta_gen2` viaja como FIRMA en el mensaje de lanzamiento … o el contador no lo cuenta y se dice»* — **no viaja ninguna**; la única firma citada es D-2/`FP-348`, que autoriza la *comparación* y **no nombra `CALC-C0D-MARCADOR` como objeto**. `decisiones.tsv` no se toca: escribir ahí sería falsificar una firma de mesa. (2) Y ésta no la levanta ninguna firma sola: **regla E.1** clasifica las dos corridas `envuelto_legacy = SI` porque sus insumos **son** los artefactos GEN1 del duelo — estructural, no accidental. `N_corridas_selladas` 5 → 5; `N_resultados_gen2_sellados` 315 → 315; único contador que se mueve: `corredores_envueltos_legacy` **8 → 10**. | `FP-367` — mesa decide **dos** cosas por separado: (a) si `CALC-C0D-MARCADOR-v2` cuenta, y (b) si un CALC con insumos GEN1 puede contar pese a `E.1` |
| **«B piso común de los tres bajo el mismo corte informativo»** | `FUERA-DE-PERÍMETRO` | **`B` cubre 2 de 14 celdas, no 14** — congelado en §0.2 de la spec **antes** de correr, no descubierto al cerrar. `MAE_pp(B)` no se calcula y `B` no entra a ninguna ordenación (`NO-ADJUDICA-POR-N`, `n = 2`). Lo que sí se midió, y es incómodo: en `FAM-M-06` la línea base tonta yerra `+0.0173 pp` donde `M` yerra `−0.1591`. | `NC-0079` — acto propio que extienda `B` fuera de la serie ENIGH × `recibe_remesas` (un `B` nuevo por serie, no éste) |
| **«si el corpus que vieron es anterior a GEN2 … nombra el sucesor: re-captura `L_CORPUS` con corpus GEN2 (fase 5)»** | `DIFERIDO-A:acto de re-captura L_CORPUS con corpus GEN2` | **No se ejecuta aquí, y este veredicto no la exige:** `RESULT-C0D-ADJUDICACION-SUCESOR = NO-APLICA`, escrito por la máquina — la re-captura está pre-declarada sólo para la rama `CONFIRMADO-CON-ALCANCE`, que **no cayó**. Queda como deuda por razón propia y medida: **435 payloads** con `fecha_descarga > 2026-09-02` que el brazo `L+corpus` nunca vio. | `NC-0077` |
| **P1 — la fuente del punto `L` en la spec `v1.0`** (reserva del propio acto, no del encargo) | `NO-VERIFICABLE-AQUÍ` | La spec `v1.0` citó verbatim la regla del módulo **base** `agregado_v1_1`, que `agregado_v1_2.py` deroga por monkeypatch. Costó **una corrida**: `CALC-C0D-MARCADOR` selló 151 `RESULT` y su guardia lo paró (`DIVERGE` 28/28, `maxdif 0.0`). Sin esa guardia el acto habría reportado `n = 0` como falta de cobertura y nadie habría mirado el lector. | `NC-0074` — **CERRADA por este mismo acto**: spec `v1.1` (`COMMIT-3`) + `CALC-C0D-MARCADOR-v2` |
| **`RESULT-C0D-ALCANCE-CORPUS-CAPTURA` salió pobre** (reserva del propio acto) | `DECISIÓN-DE-MESA-PENDIENTE` | Dice `fecha_congelacion=sin-params · modelo=None` porque muestrea **una** captura y `CIV-M-01` pertenece a la familia con `modelo_real` (nulo) en vez de `params`. Ninguna cifra del acto depende de ese campo —los metadatos correctos están medidos sobre las 424 capturas y citados en §0.3 de la sellada— pero el `RESULT` es más pobre de lo que debería. **Sellado está y sellado se queda.** | `NC-0078` |
| **Suite: cuarto `FAIL` sin `TZ=UTC`** (ajeno al acto) | `FUERA-DE-PERÍMETRO` | `T-YAMEDIDO` sobre `forense/encargos/2026-09-08-GEN2-TRAMITE-BANDEJA.md`, **encargo ajeno**. Con `TZ=UTC`: `3 FAIL · LÍNEA BASE VERDE`. Se declara para que nadie lea el rojo local como daño de este PR. | `NC-0080` |
