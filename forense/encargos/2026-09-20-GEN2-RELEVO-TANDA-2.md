ENCARGO · ACTO GEN2-RELEVO-TANDA-2 · DOCE SLOTS PASAN A PROCEDENCIA GEN2 POR FIRMA DE MESA, UNO POR UNO Y POR NOMBRE — EL `p` NO CAMBIA, CAMBIA QUIÉN MANDA
ENTORNO: NUBE — NO caja. Todo insumo es un `RESULT` sellado; cero microdato.
CABECERA (D-12) · SHA de redacción `8a842afc`; re-deriva al abrir · una sola sesión, rama propia; `/acto` PARA si ya está archivado en otra rama viva · COMPUERTA: #905 (`GEN2-RELEVO-TANDA-1`) en main — cumplida · MODELO: Opus (escribe en `milpa/tramite.yaml`) · FP/ADR/NC: deriva al cierre; renumera quien fusione segundo · CONTADOR: `cuenta_gen2` NO-APLICA; mueve `dependencias_numericas_legacy_activas` (hoy 182) y `adoptados_activos` (hoy 46) — reporta lo derivado, antes y después, con worktree. La adopción la ejecuta el merge de mesa de este PR (E.2).
FIRMAS DE MESA (20/sep/2026, verbatim: «Ok las dos firmas», a los dos textos que dirección propuso en la misma conversación; texto operativo de cada una:)
FP-392 — adopción, por nombre. "Se adoptan, por nombre: RES-0025, 0026, 0031, 0032, 0033, 0034, 0053, 0054, 0056, 0066 y RES-0055. Mismo estimando verificado por texto, |Δ| ≤ 5e-07. El `p` no cambia; cambia la procedencia a GEN2. RES-0028 no se adopta: va por FP-391." FP-391 — RES-0028. "Se conserva el estimando sobre personas (U4). RES-0028 se aparea con el complemento sobre personas, no con el de delitos." Esto es la firma "uno por uno" que el inciso (iii) de la ADENDA del 15/sep reserva a mesa para bin 2, y el bloque con tabla para bin 3. No deroga la ADENDA: la cumple.
VERIFICACIÓN DE EXISTENCIA (A.8; dirección contra `8a842afc`)

* `relevo-usos-v1_0.tsv`: `LEGACY-GEN1` 182; los 12 siguen `CANDIDATO-GEN2`. `milpa/tramite.yaml` no tiene `corrida0_resultado_id` en esas conductas: no está hecho. FP-391 y FP-392 abiertas en `firmas-pendientes.tsv`.
* Mecanismo (leído en el informe de TANDA-1 §P4 y en el yaml): la adopción se escribe como `corrida0_resultado_id: <RESULT>` + `corrida0_generacion: GEN2` en la conducta, sin tocar `p`; `usos.tsv` y `relevo-usos` son derivados y se regeneran por comando. Ejemplo vivo: `tramite.yaml:62`.
* Evidencia ya sellada que este acto cita y no repite: `forense/relevo-usos/candidatos-delta-1/tabla-decision.tsv` (deltas, reproducidos bit a bit el 20/sep) y `forense/analisis/relevo-tanda-1/P2-mismo-estimando.tsv` (11 `CASA` en cinco dimensiones).
* Lo que TANDA-1 no vio, y cambia FP-391: el complemento sobre personas ya existe sellado — `CALC-ENVIPE-RES0028-U4-DERIVADO-0001` (`SELLADA`, `REPRODUCE`, `cuenta_gen2 = PENDIENTE-DE-MESA`): `RESULT-ENVIPE-RES0028-Q-C2-U4 = 0.7056870125…`, `DELTA-VS-LEGACY = 1.25e-08`, `COMPATIBILIDAD = COINCIDE-AL-GRANO-U4`, n = 13 023 personas, con IC heredado del padre. La vista propone otro candidato (`…COMPLEMENTO-C2-U1`, delitos) porque es el que el CALC padre declara. No hace falta ninguna corrida nueva.

PIEZAS
P1 · Re-verifica antes de escribir, slot por slot (12 filas). Para cada uno: el `RESULT` candidato existe y su CALC está `SELLADA` y `REPRODUCE`; el valor al grano de `milpa/` coincide con el `p` vigente; la fila de P2 de TANDA-1 dice `CASA` (para RES-0028: haz tú la lectura de cinco dimensiones contra `CALC-ENVIPE-RES0028-U4-DERIVADO-0001/spec.yaml` — TANDA-1 la hizo contra el candidato equivocado). RES-0055 entró a bin 3 "sin `se_mueve_si` ni IC legacy propio": di en una línea qué le falta y si eso impide anotar procedencia; si lo impide, ese slot va a `NO-CORRIDO` y los otros once siguen. Un slot que no pase, no se adopta; no tumba el lote. P2 · Escribe la procedencia. En cada conducta: `corrida0_resultado_id` + `corrida0_generacion: GEN2`. Ni un `p`, ni un `tier`, ni un nombre, ni un `clase` cambian. RES-0028 → `RESULT-ENVIPE-RES0028-Q-C2-U4`, con un comentario de una línea: unidad persona (U4), complemento derivado de padre sellado, sin información muestral independiente (lo dice el propio CALC: el IC es el del padre). `complemento_de` se conserva. El renombre que ADR-509 propuso no es de este acto. P3 · Regenera por comando `usos.tsv`, `relevo-usos`, demanda y lo que la casa derive; `status` antes/después. Reporta cuántos de los 12 quedaron `YA-ADOPTADO` y el valor nuevo de los dos contadores — no te doy la cifra esperada. P4 · La vista y el candidato equivocado. Deja asentado, sin arreglarlo aquí, por qué `relevo_usos.py` propone para RES-0028 el complemento sobre delitos habiendo uno sobre personas sellado (¿el CALC derivado no declara pin al slot?). Si es una línea en la spec de un CALC sellado, no se edita: NC con sucesor. P5 · Trámite. FP-391 y FP-392 → `FIRMADA` con ADR/PR (A.12); `decisiones.tsv`: `adopcion:relevo-tanda-2` y `estimando:RES-0028-personas`; `cuenta_gen2` de `CALC-ENVIPE-RES0028-U4-DERIVADO-0001` no se decide aquí (sigue `PENDIENTE-DE-MESA`; dilo). Recifrado L0 y `check.py --baseline` verde.
PERÍMETRO
`milpa/tramite.yaml` — solo los campos de procedencia de esas 12 conductas · `milpa/procedencia.yaml` si el mecanismo lo exige · derivados por comando · `decisiones.tsv`, `firmas-pendientes.tsv`, `no-corrido.tsv`, `hallazgos.md`, ADR, L0, tablero · nota. No toca ningún CALC ni spec · `tools/relevo_usos.py` · marcador · ninguna otra conducta. «Si te encuentras escribiendo fuera de esta lista, PARA.»
LO QUE NO HACE
No mide · no cambia cifras · no renombra `denuncia_por_otra_razon` · no toca los 7 `LISTADO-PARA-MESA` ni los 2 `CONFLICTO` · no decide nada sobre el marco M.
MÓDULO DE AUDITORÍA (son lecturas del motor sobre México)
Pasar a GEN2 da trazabilidad, no validez: cada cifra hereda su universo y sus sesgos. `evasion_norma` es por delito; `tiene_ahorros` y `via_informal` son de personas 18+ en ENIF, que sobre-representa a quien tiene contacto financiero; RES-0066 es de no trabajadores. RES-0028 es la razón residual de no denunciar entre personas víctimas: "otra razón" es una bolsa — trámites largos, pérdida de tiempo, delito de poca monta — y no debe leerse como ausencia de miedo ni de desconfianza. Clase (a) en los doce. Peligroso leído simplista: "12 cifras validadas en GEN2".

---

## NO-CORRIDO / RESERVAS

| qué | por qué | impacto | sucesor |
|---|---|---|---|
| **P2 · escribir la procedencia de `RES-0028`** (`corrida0_resultado_id: RESULT-ENVIPE-RES0028-Q-C2-U4` + `corrida0_generacion: GEN2` en `milpa/tramite.yaml:637`) | `FUERA-DE-PERÍMETRO` — `NC-0380`. La firma `FP-391` está acreditada por texto (cinco dimensiones CASA contra `CALC-ENVIPE-RES0028-U4-DERIVADO-0001/spec.yaml`; la conducta padre de la misma regla ya está adoptada al mismo `RESULT` persona/U4). Escrita la línea, `python3 tools/corrida0.py status` **PARA**: `USO-NO-APTO · uso=MEDICION-GEN2 · origen=INDETERMINADO`. El `RESULT` hereda `origen_numerico = INDETERMINADO` porque dos de los cuatro `inputs` del CALC derivado son el `sello.json` y el `sello.sha256` de su padre, clasificados `FUNCION-INDETERMINADA`. La corrección vive en la lista `inputs` de una `spec.yaml` **SELLADA**, y el perímetro dice «No toca ningún CALC ni spec». La línea quedó byte a byte como estaba. | `dependencias_numericas_legacy_activas` queda en **171** en vez de 170; `adoptados_activos` en **57** en vez de 58; `RES-0028` sigue `CANDIDATO-GEN2` —el único que queda de los doce— y sigue citando el `p` legacy `0.705687` como autoridad. `FP-391` queda `FIRMADA` y ejecutada sólo en su parte declarativa. | Acto sucesor que emita un CALC sucesor de `CALC-ENVIPE-RES0028-U4-DERIVADO-0001` con los `inputs` de sello clasificados (u origen numérico acreditado por otra vía) y entonces escriba la procedencia. Alternativa de mesa: declarar para este slot un `uso` de los que admiten herencia (`milpa/src/linaje.py::USOS_CON_HERENCIA`) — decisión de mesa, no de ejecución, porque cambia qué significa la cifra. |
| **P4 · corregir que `relevo_usos.py` proponga para `RES-0028` el complemento sobre delitos** | `DIFERIDO-A:SIN-ASIGNAR` — `NC-0381`. El encargo pedía asentarlo sin arreglarlo, y así se hizo; **su conjetura queda corregida**: el CALC derivado **sí** declara pin (`spec.yaml:102`, «candidato **para** RES-0028»), pero `RE_ENLACE` (`tools/relevo_usos.py:150-152`) acepta una lista cerrada que no incluye «candidato para». El padre escribe «el candidato **a** RES-0028» sobre el complemento de unidad delito y sí casa. Es una línea en la spec de un CALC sellado: no se edita. El arreglo alternativo —ampliar `RE_ENLACE`— tampoco se ejecuta: `tools/relevo_usos.py` está en el perímetro sólo como lectura. | La vista seguirá proponiendo un candidato de unidad **delito** para un slot de unidad **persona** mientras `RES-0028` siga `CANDIDATO-GEN2`. Hoy sólo lo delata que el valor propuesto (`0.7327566`) no coincida con el `p` vigente (`0.705687`); ningún test lo atrapa. | El mismo acto sucesor de `NC-0380` (emitirá la spec sucesora y puede escribir el pin con la preposición que `RE_ENLACE` acepta), o un acto de herramienta que amplíe `RE_ENLACE` y añada regresión. Semilla `PARA-v2.15` anotada en `forense/hallazgos.md`. |
| **P5 · `cuenta_gen2` de `CALC-ENVIPE-RES0028-U4-DERIVADO-0001`** | `DECISIÓN-DE-MESA-PENDIENTE` — `NC-0382`. El encargo lo excluye explícitamente («no se decide aquí … dilo»). **Se dice: sigue `PENDIENTE-DE-MESA`**, en `data/corrida0/corridas.tsv` y en `etiquetas.cuenta_gen2` de su spec. | `N_corridas_selladas` no se mueve por este acto (**102**) y el CALC derivado sigue sin contar en el marcador GEN2. No bloquea `NC-0380`: son dos cosas distintas — el contador es contabilidad, el guard de linaje es aptitud. | Mesa, por la vía de recibo (los actos `GEN2-RECIBO-CODEX-*` son los que han venido firmando `cuenta_gen2` por lote). |

Fuera de tabla, por no ser reservas: `forense/relevo-usos/candidatos-delta-1/tabla-decision.tsv` y `forense/analisis/relevo-tanda-1/P2-mismo-estimando.tsv` **no se re-derivan** — el encargo los declara «evidencia ya sellada que este acto cita y no repite». `milpa/procedencia.yaml` **no se tocó**: el mecanismo no lo exige (ninguna adopción previa escribe ahí; ejemplo vivo `milpa/tramite.yaml:62`). El renombre de `denuncia_por_otra_razon` que `ADR-509` propuso **no es de este acto**, y `complemento_de` se conserva.
