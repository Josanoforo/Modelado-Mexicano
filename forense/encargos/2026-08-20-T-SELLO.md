**SHA de redacción:** No declarado explícitamente por quien escribió el encargo; el propio texto lo marca `⟦SHA al lanzar⟧` y exige derivarlo al arrancar. Este acto lo derivó: `906203a0a732b1e138427bfa2b4dfe284cf51e35` (`origin/main`, `PR #296`, `ACT-PIL-1 · CONTRATO-v0_5` — el gate `ACT-PIL-1 fusionado` que la cabecera exige, ya cumplido).
**Entorno asignado:** NUBE (repo-only). NO Ubuntu — textual en la cabecera del encargo.
**Estado:** `CONSUMIDO` por `ACTO T-SELLO`, 20/ago/2026, `ADR-131`, rama `claude/tsello-nube-opus-arranque-vad18w`. Durante la ejecución, `origin/main` avanzó dos veces (`PR #296`→`PR #297`, `ACT-PIL-2`) — ninguna colisión de contenido, dos colisiones de numeración (`ADR-130`, `FP-79`…`FP-84`), resueltas por la regla de la casa (quien fusiona segundo renumera) y documentadas en `ADR-131` y en la nota de cierre de este acto.
**Nota de archivo, no del original:** este documento llegó a la sesión como texto de tarea (sin adjuntos propios) junto con tres archivos adjuntos, verificados uno a uno contra la compuerta de arranque que el propio encargo declara (APERTURA-FASE-CALCULO v1.1, `TRANSFER-MAESTRA-FASE-CALCULO-2026-08-19.md`, PLAN-CALCULO-TOTAL v1.0 — los tres llegaron, ninguno faltó). El texto que sigue es exactamente el recibido, sin editar.

---

1 · `T-SELLO` — NUBE · Opus · gate: `ACT-PIL-1` fusionado

```
Contra ⟦SHA al lanzar⟧.  ENTORNO: NUBE (repo-only). NO Ubuntu.
Modelo: Opus.  🚫 --freeze.  Dueña única: pgrep -af claude, y reporta.
```

Compuerta de arranque — trae adjuntos. `APERTURA-FASE-CALCULO-v1_1.md` (`sha256 a35b5452…`, 8 870 bytes, 54 líneas) · `TRANSFER-MAESTRA-FASE-CALCULO-2026-08-19.md` · `PLAN-CALCULO-TOTAL-v1_0.md`. Si falta alguno, PARA y pídelo — no lo reconstruyas (`TRANSFER §7`, textual).
ARRANQUE. Cinco puntos. P4: esperado `cloud_default`, sin sonda, crudo. P5: espejo prohibido — ⚠️ `PLAN-CALCULO-TOTAL` viene del espejo y por eso llega como adjunto, no se lee de `/mnt/project/`.
VERIFICACIÓN A.8, contestada aquí. (1) `forense/adv-duelo/` NO existe; los cuatro adversariales viven en `forense/` raíz y `compass-4` en `corpus/forense/` — eso es `FP-78`, `ABIERTA`. (2) `APERTURA · FASE DE CÁLCULO v1.1` → 0 archivos; `TRANSFER · dirección → MAESTRA OPUS` → 0; `git ls-files | grep -i calculo-total` → vacío. Universo: árbol menos `.git`, 1 717 archivos. Re-verifica y lee los archivos que devuelva el grep, no solo el conteo. (3) Sin brecha retroactiva.
T1 · Aterrizaje. `canon/APERTURA-FASE-CALCULO-v1_2.md` = el v1.1 adjunto verbatim, con `§5` marcado SUSTITUIDO por el careo (banner de una línea; el §5 original no se borra — A.10, corolario 1). `canon/PLAN-CALCULO-TOTAL-v1_1.md` = el v1.0 verbatim + la tabla `§1` de APERTURA como delta fechado, original intacto. `forense/TRANSFER-MAESTRA-FASE-CALCULO-2026-08-19.md` verbatim. Los tres con cabecera de procedencia, `sha256` en la nota y `diff` contra el adjunto — única diferencia admisible: cabecera y salto final.
T2 · `forense/adv-duelo/` y `FP-78`. Crea el directorio y mueve ahí los cuatro adversariales de `forense/` raíz (`git mv`, la historia se conserva). `compass-4` se queda donde está y se cita — moverlo rompería `ADR-125`. Cierra `FP-78` con la regla que el TRANSFER `§8` ya fija verbatim: «lo que gobierna un acto → repo, siempre; lo que consulta dirección → proyecto, además». Anota que los compass además van al proyecto de claude.ai, como los compass 1-3.
T3 · ADR. Número derivado al escribir y al fusionar (máx hoy 128). (a) Sella `D-i`, `D-ii`, `D-iii`, `D-iv` verbatim del `TRANSFER §4`, las cuatro FIRMADA 19/ago/2026. Incluye la prohibición operativa de `M4`: la palabra «supera» queda prohibida en toda comunicación del piloto. (b) Declara abierta la Fase de Cálculo con `APERTURA v1.2` como su ley. (c) ⚠️ Registra la contradicción; NO la resuelvas. `ADR-128` declaró `ADR-68(c)` —los siete umbrales— vencido en alcance salvo el umbral 1. `M6` del careo, que `D-4` adoptó, dice «De los 7 umbrales, ≥3 de resultado» — o sea los conserva y endurece tres. Y `PLAN-CALCULO-TOTAL` OLA 4 hace de ese GO la apertura formal de la fase. Los tres textos están vigentes y dicen cosas distintas sobre el mismo objeto. El ejecutor propaga, no decide (`ADR-76`/`ADR-79`): va a fila `ABIERTA`, con las tres citas y sus rutas. (d) Estampa A.10: tu SHA · los tres documentos con `sha256` · árbol completo menos `.git` con su conteo · fecha.
T4 · Tablero. Filas por (a) —nacen y cierran `FIRMADA`—, por `FP-78` —`ABIERTA`→`FIRMADA`— y una `ABIERTA` nueva por (c), que gatea `PILOTO-E1E3`. Re-deriva el máximo; no heredes 78.
Cierre. Nota · hallazgos · `CONSUMIDO` · `--baseline` VERDE con salida pegada. Contador: medición sobre México = 0, dicho. La fase queda abierta en el canon y no en una conversación. PERÍMETRO. `canon/APERTURA-FASE-CALCULO-v1_2.md` · `canon/PLAN-CALCULO-TOTAL-v1_1.md` · `forense/TRANSFER-*` · `forense/adv-duelo/` (creación + `git mv`) · tablero · gobernanza · `canon/estado-programa-v1_10.md` (cascada) · hallazgos · nota · encargo. No toca `milpa/`, `data/`, `tests/`, `corpus/`.
