# ENCARGO · ACTO GEN2-ESTADO-v1_15-1 · El estado del programa deja de describir el 17/sep: §14 derivada del árbol para el 18–22/sep, con cada cifra atada a su comando, y v1.14 retirada por T01

> ENTORNO: **NUBE** — cero microdato. Hook; si no coincide, PARA.

CABECERA · SHA `ccd7c0eb` (re-deriva al abrir: 130 PR entraron en cuatro días y seguirán entrando) · una sola sesión · MODELO: Opus (derivar y retratar es juicio) · MODO: **ABIERTO** · CONTADOR: cero mediciones; ningún contador se mueve · ids raíz de acto.

## 1 · OBJETIVO
Que `estado` (nombre estable) retrate el 18–22/sep con la misma pieza y las mismas reglas que §13 retrató el 14–17: cabecera nueva, registro L0 y §0–§13 verbatim, **§14 nueva** derivada. «Hecho» = `canon/estado-programa-v1_15.md` existe y `v1_14` no (T01); `diff` del cuerpo heredado vacío; §14 trae el bloque íntegro de `corrida0.py status` al abrir y una tabla afirmación → comando con ≥ 20 filas; ninguna cifra de §14 sin comando o cita; `grep -c "NO-DERIVADO" §14` ≥ 1 (lo que no se pudo derivar se declara, no se omite).

## 2 · FIRMAS DE MESA
Ninguna nueva: el estado se sella con el merge de mesa («PROPUESTA — se sella con el merge»). Dirección revisa el PR antes del merge; si el acto y dirección discrepan en una cifra, manda el comando.

## 3 · LO QUE DIRECCIÓN SABE
- `[EJECUTADO]` `canon/estado-programa-v1_14.md` existe; §13 (17/sep) retrata hasta `9eff694`. Desde entonces (`git log --merges 09681ab..HEAD`): piloto 3 con veredicto `FALSADOR DÉBIL` (#961), V215 y V216 (instrucciones v2.16, plantilla v2.1, ids con raíz de acto D-24), MARCADOR-E-INFORME-1 (`celdas_validadas` 73 → 92, #969), ARBITRO-MARGINALES-1/2 (57 marginales evaluadas; cobertura del IC del piso: ENIF 0.19, ENVIPE 0.53, ENCIG 0.00), duelo ENIGH 2024 con reserva (`C-PISO-ADOPTADO`, #994), duelo ENVIPE 2026 congelado (#968/#982), pisos por eje (#866/#871/#874), lote ENIF 2024 (COMMIT-1 y 2-3), crédito 2024 (COMMIT-1 y 2-3; eje escolaridad defectuoso), siete tandas de relevo, RECIBO-CODEX-5/6, TRÁMITE-FIRMAS-3/5, TUBERÍA-CI-MEDICION/PREFLIGHT/RES-LLAVE, LIMPIEZA-RAMAS-LOCALES-1..6, adquisición de ENVIPE 2026 y ENIGH 2024 bajo reserva, informe v1.2, contrato celda-D v0.6. `status` hoy: 154 selladas · 44 177 RESULT GEN2 · 72 adoptados · 215 validados · 212 NC abiertas · 17 FP abiertas.
- `[LEÍDO]` §13 (cabecera-v1_14 + seccion-13, archivados en `forense/encargos/insumos-gen2-contrato-y-tramite-2026-09-19/`): su formato es el que §14 copia: «Retrata, no opina. Cada afirmación trae comando o cita de nota sellada; nada tecleado», bloque de status íntegro, un párrafo por tema con cifra derivada, tabla afirmación → comando, «Lo que este estado deja de decir, a propósito», «NO-DERIVADO».
- `[SUPUESTO]` Nada de lo listado arriba cambió de veredicto después de fusionar. Si resulta falso, §14 lo dice con la cita del cambio.
- ADJUNTOS: ninguno (todo en el árbol).

## 4 · YA HECHO / YA DECIDIDO
`ls canon | grep estado-programa` → solo `v1_14`; `grep -c "^## 14" canon/estado-programa-v1_14.md` → 0. Ramas vivas: ninguna.

## 5 · PIEZAS
- **P1 · Derivación:** todos los conteos por comando al abrir (status íntegro; `ls -d CALC-* | wc -l` vs vista; FP/NC abiertas; celdas-D; catálogo; reserva de cada ola: `decisiones.tsv` `reserva:*`; ADR con raíz: conteo por patrón D-24; instrucciones vigentes derivadas).
- **P2 · §14**, en el formato de §13, con estos temas como mínimo: régimen v2.16 e ids con raíz (qué costó la numeración antes, con el `grep -ci renumer` de la semana); tres pilotos y sus tres veredictos con vocabulario cerrado; el duelo con reserva ENIGH 2024 y el ENVIPE 2026 en cola; marginales: 57 evaluadas, coberturas por instrumento, ninguna adoptada al abrir (o las que E4 haya adoptado); métrica rectora `celdas_validadas` y sus dos sub-cifras (plan P2); el emisor fuera del marcador; crédito 2024 con el eje roto y su sucesor; lo que la vista sigue sin ver (corridas selladas en disco vs vista, con el comando); el carril Codex (PR recibidos por recibo, PR sin recibo); rutinas. Y la sección «Lo que este estado deja de decir» y «NO-DERIVADO».
- **P3 · T01 y cabecera:** `v1_15` = cabecera nueva (era: 18–22/sep, v2.16, plan de aceleración 20/sep, régimen ADR-531/FP-383/A-bis 6) + registro L0 y §0–§13 verbatim + §14; `v1_14` retirada; citas vivas del nombre de archivo actualizadas por grep (históricas no); cabecera de gobernanza y `registro-rotulos` por T25.

## 6 · LATITUD
DECIDES TÚ: orden de temas, extensión (§13 son 78 líneas; §14 puede ser más: cuatro días de 130 PR), regenerar la vista `--mesa`. PREGUNTAS A MESA: ninguna prevista; dirección revisa el PR. NO DECIDES: §7.

## 7 · PAROS
a) no aplica · b) editar §0–§13 o el registro L0 · c) no aplica · d) no aplica · e) caja · f) inalcanzable.

## 8 · COMPUERTAS
Ninguna. Orden sugerido: lanzar después de que E4 (adopción de marginales) y TRÁMITE-FIRMAS-6 fusionen, para que §14 retrate firmas y adopciones hechas y no en vuelo — no es compuerta: si fusionan a mitad, §14 lo cita al sellar.

## 9 · PERÍMETRO
Propio: `canon/estado-programa-v1_15.md` (nuevo), `v1_14` (retirado), punteros vivos por grep, `canon/registro-rotulos.tsv`, `canon/L0/<raíz>.md`, cascada. Ajeno: `hallazgos.md` (solo una línea propia), tablero de firmas/NC (lectura). Otro acto en vuelo: E4 y FIRMAS-6 tocan `firmas-pendientes.tsv` y `decisiones.tsv` — este acto solo los lee. «Si te encuentras escribiendo fuera de esta lista, PARA.»

## 10 · NO HACE · SUCESORES · CIERRE
No opina, no adopta, no cierra NC ni FP (las cuenta). Sucesor: informe v1.3 (dirección) con §14 como fuente. Auditoría: la de §13, contestada de nuevo en §14 (afirma sobre el aparato; las cifras sobre México se citan de notas selladas). Cierre por /acto.



## NO-CORRIDO / RESERVAS

- **qué:** verificación individual del número de PR exacto de `TRÁMITE-FIRMAS-6` (rama `acto/gen2-tramite-firmas-6`, existe en `origin` sin fusionar). **por qué:** `NO-VERIFICABLE-AQUÍ` — `gh` no está disponible en este entorno de sesión y no se adivina el número. **impacto:** §14 declara la rama viva y su efecto (§8 no era compuerta) sin citar el número de PR. **sucesor:** `GEN2-TRAMITE-FIRMAS-6` — el acto que fusione esa rama, o cualquier acto posterior con acceso a `gh`/al tool de GitHub, lo cita.
- **qué:** conteo exacto de PR con recibo del carril Codex vs sin recibo en la ventana 18–22/sep (P2 del encargo, "el carril Codex fuera del marcador"). **por qué:** `NO-VERIFICABLE-AQUÍ` — los recibos no se anuncian en el título de merge; cruzar uno por uno los 357 merges de la ventana contra los actos de recibo excede lo que esta sesión pudo verificar con rigor. **impacto:** §14 reporta los recibos confirmados por nombre y declara el negativo del grep sobre títulos de merge, sin un conteo agregado recibo/sin-recibo. **sucesor:** el próximo `RECIBO-CODEX-N`.
- **qué:** revisión celda por celda de las 92 filas de `celdas_validadas` para detectar adopciones de mesa posteriores a `ccd7c0eb` y anteriores a `02eda84`, más allá de lo ya reflejado (adopción del piso del piloto 3 vía `TRAMITE-FIRMAS-5`). **por qué:** `FUERA-DE-PERÍMETRO` — repetirla a mano encima de lo que `celdas_validadas`/`marcador_segmento.py` ya derivan por comando sería tecleada, no derivada; es del perímetro de quien mantenga esos derivadores, no de este acto de retrato. **impacto:** ninguno sobre §14, que ya cita las cifras vigentes por comando. **sucesor:** `GEN2-ESTADO-v1_16` (el próximo retrato).

## CONSUMIDO

Ejecutado por `PR #1006` (https://github.com/Josanoforo/Modelado-Mexicano/pull/1006), rama `acto/gen2-estado-v1_15-1`. `ADR-260922-GEN2-ESTADO-V15-1-7e23-01`. Estado: propuesto; mesa fusiona.
