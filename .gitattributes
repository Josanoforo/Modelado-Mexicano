* text=auto eol=lf

# merge=union: driver interno de git, no personalizado (verificado empíricamente
# en ramas locales, Encargo CU 5/ago/2026 -- ver forense/notas/2026-08-05-cu-cascada-vigilada-union.md).
# Reduce a mecánico lo que la receta manual de fusión ya hacía a mano: "ambas
# tandas, main arriba, la línea del empalme una sola vez". Requiere que el
# archivo SIEMPRE termine en salto de línea -- verificado hoy en los dos; si
# alguna vez deja de cumplirse, dos ramas que apendicen a la vez duplican en
# silencio la última entrada compartida en vez de conflictuar (reproducido en
# la prueba empírica citada arriba).
# VERIFICADO 12/ago/2026: el botón "Merge pull request" de GitHub NO honra
# este driver del lado servidor -- dos PRs (#175, #179) reportaron conflicto
# en forense/hallazgos.md en la interfaz mientras el mismo merge resolvía
# limpio en clon local. La ruta garantizada, y la única, es el merge local
# (git merge con este archivo en el árbol), fusionando main HACIA la rama.
# NO usar el editor web de conflictos en archivos union: resuelve a mano y
# es la vía por la que se borra la entrada ajena. Receta completa en
# forense/notas/2026-08-12-union-vs-boton-github.md.
forense/hallazgos.md merge=union
forense/bitacora.md merge=union

# P-D (`ACTO GEN2-TUBERIA-CIERRE-SIN-CHOQUE-1`, 21/sep/2026): cuatro
# candidatos probados por MUTACIÓN REAL con git (`tests/test_tuberia_ids_
# union.py`, caso G) contra el caso exacto -- una rama edita EN SU SITIO
# la última fila/entrada (marca FIRMADA, cierra una NC, enmienda un ADR);
# otra rama AÑADE debajo; se fusiona con union. "Entra" sólo si una guarda
# YA EXISTENTE atrapa el resultado (nunca al revés):
#   forense/no-corrido.tsv         -- T47 atrapa (id repetido, ya vivía
#                                      aquí desde el defecto real FP-406).
#   forense/firmas-pendientes.tsv  -- T47 atrapa, misma guarda.
#   canon/gobernanza-v1_15.md      -- T15 atrapa (ADR repetido por número,
#                                      aunque el texto de las dos copias
#                                      difiera -- es justo lo que produce
#                                      esta mutación).
# canon/registro-rotulos.tsv se probó con la MISMA mutación y quedó FUERA:
# el archivo SÍ termina duplicado con contenido contradictorio (el riesgo
# es real), pero NINGUNA guarda de la suite lo vigila hoy (T25 vigila
# OTROS archivos citando rótulos pelados, no filas repetidas de éste) --
# se sigue fusionando a mano hasta que exista esa guarda (sucesor).
#
# P1 (`ACTO GEN2-TUBERIA-CIERRE-SIN-CHOQUE-2`, 21/sep/2026): ese sucesor
# corrió. `canon/registro-rotulos.tsv` ENTRA -- la guarda que faltaba es
# `T51` (T-ROTULOS-PAR-UNICO), unicidad del par `(espacio, valor)`, que es
# la identidad natural de este registro; `T47` no servía tal cual porque su
# llave es un id al principio de la línea. La MISMA mutación de P-D (una
# rama edita en su sitio la última fila, otra añade debajo, union) la
# atrapa: `tests/test_tuberia_ids_union.py`, caso G3-bis. `G3` se conserva
# como el registro de por qué quedó fuera el 21/sep por la mañana. Los 7
# pares hoy repetidos quedan congelados como EXENCIÓN declarada en `T51`
# -- no se borró ninguno; la guarda falla sólo sobre repeticiones nuevas.
forense/no-corrido.tsv merge=union
forense/firmas-pendientes.tsv merge=union
canon/gobernanza-v1_15.md merge=union
canon/registro-rotulos.tsv merge=union

# ACTO GEN2-TUBERIA-CANAL-PUBLICACION-1 (22/sep/2026), P3, firma de mesa
# opción (a) (`…PENDIENTES-CAJA-1-c09b-02`, `…ESCOLARIDAD-2-0af9-01`). Los
# tres archivos de append que el canal de publicación (el job del push a
# `main`) escribe en cada corrida registrada: una fila nueva por objeto,
# nunca edición en su sitio de la anterior -- exactamente el patrón que la
# misma mutación de P-D (dos ramas apendican filas DISTINTAS, une sin
# conflicto) ejercita, ahora contra append+append en vez de edita+apendica
# (`tests/test_tuberia_ids_union.py`, caso I). Sin `union`, cada tanda de
# más de dos actos que asientan replay/censo/decisión a la vez cuesta un
# sync manual por PR (medido: 7 de 9 el 22/sep, misma simulación
# `git merge-tree` del encargo). La guarda de líneas repetidas (T50, ya
# genérica sobre TODO archivo `merge=union`) cubre los tres sin tocar
# `tests/check.py`: una fila de ≥200 caracteres que dos ramas asienten
# IDÉNTICA (la misma corrida, el mismo veredicto) dispara T50 igual que en
# los cuatro candidatos de arriba.
forense/replay-evidencia.tsv merge=union
forense/analisis/ci-guardias/censo-tests.tsv merge=union
data/corrida0/decisiones.tsv merge=union

# La guarda de líneas repetidas (T50, tests/check.py): en todo archivo de
# arriba declarado merge=union, ninguna línea de >= 200 caracteres aparece
# dos veces o más -- riesgo residual DECLARADO, no instrumentado más allá
# de esto (P-D.4): dos ramas que enmiendan A LA VEZ la misma línea con
# textos DISTINTOS no lo dispara (son líneas distintas, no repetidas).

# hitoD-preregistro-v2_0.md es append-only pero NO entra a union, a propósito:
# T18/T19c/T20 (tests/check.py) cuentan reglas/líneas de su bloque designado
# "## Registro de veredictos archivados". Con union, dos ramas que archiven
# veredictos a la vez duplicarían en silencio la línea de una de ellas -- sin
# conflicto, sin aviso, inflando el contador que mide el programa entero. Un
# merge manual que conflictúa es ruidoso; un union que duplica no lo es. Este
# archivo se sigue fusionando a mano.
