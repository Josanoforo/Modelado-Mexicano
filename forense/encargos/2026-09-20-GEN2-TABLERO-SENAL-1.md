ENCARGO · ACTO GEN2-TABLERO-SENAL-1 · EL TABLERO MUESTRA LO QUE HOY GOBIERNA: MARCADOR, FIRMAS DE CONTADOR PENDIENTES Y RAMAS — Y SE REFRESCA SOLO
CABECERA (D-12) · SHA de redacción `4dedab48`; re-deriva al abrir · ENTORNO: NUBE; NO caja · una sola sesión, rama propia; `/acto` PARA si ya está archivado en otra rama viva · compuerta: ninguna · MODELO: Sonnet (receta; si una pieza pide juicio sobre el cuerpo curado, déjala como propuesta y sigue) · CONTADOR: `cuenta_gen2 = NO`; no mide; cero contadores del programa — mueve la legibilidad de todos · FP/ADR/NC: deriva al cierre.
Gate D-14, contestado: ¿defecto real ya observado? Sí, tres el 19/sep: (a) `N_corridas_selladas` bajó de 86 a 85 con seis CALC nuevos y nadie pudo leer por qué sin abrir `corridas.tsv` — eran corridas `PENDIENTE-DE-MESA`, invisibles en el tablero; (b) una rama Codex sin PR (`…marcador-adopcion-cli-1`) fue invisible para mesa durante horas; (c) el bloque vivo del repo está en `4a35529`, decenas de merges atrás, porque `/tramite` no lo regenera (T0 del propio tablero: "sigue sin hacerse tras 153 PR"). ¿Puede cambiar una decisión? Sí: las firmas de contador y el borrado de ramas son decisiones de mesa. ¿Cuesta menos que corregirlo a mano cada vez? Sí.
VERIFICACIÓN DE EXISTENCIA (contra `4dedab48`)
`grep -c` en `tools/tablero_programa.py` (494 líneas): `marcador_segmento` → 0 · `cuenta_gen2` → 0 · `EMITIDA-SIN-EVALUAR` → 0. `ramas_remotas_vivas` sí se deriva (`--help` lo lista) pero el bloque renderizado no lo pinta. `grep -c tablero_programa .claude/commands/tramite.md` → 0. La línea Corredor del bloque reporta `marco-M-v1_3` (14 celdas), que el diseño firmado del marcador declaró "deja de ser el marco" (`MARCADOR-SEGMENTO-diseno v1.0 §1`). NO-ENCONTRADO: nada de lo pedido existe ya.
PIEZAS
P1 · Tres líneas nuevas en el bloque derivado, cada una desde su fuente única, sin teclear cifras:

* Marcador por segmento ← `tools/marcador_segmento.py --json` y `marcador-segmento.tsv`: filas por `estado` (`ADOPTADO-POR-FIRMA`, `SOLO-PISO`, `SIN-PISO`, `RESERVADA`, `CONSUMIDA-SIN-PILOTO`, `IDENTICO`, `DIAGNOSTICO`), `cobertura_de_piso`, `valor_anadido / evaluadas`, celdas con `emision = EMITIDA-SIN-EVALUAR`, y `veto_pisos_activo`. Cada número con su denominador al lado (la lección de 9.6: dos contadores que parecen fracción y no lo son).
* Corridas selladas que no cuentan todavía ← `corridas.tsv`: conteo de `SELLADA` por valor de `cuenta_gen2` (`SI`, `NO`, `PENDIENTE-DE-MESA`, `PENDIENTE-DE-CASCADA-DIFERIDA`, `PENDIENTE-DE-INTEGRACION-SERIAL`) y la lista de las pendientes de mesa con su `resultado_replay`. Hoy son 16; la cifra la da el comando.
* Ramas presentes en origin ← lo que ya deriva `estado_comun.ramas_remotas_presentes()`: nombre, commits delante/detrás de main, fecha del último commit. Política de cero ramas: si hay > 0, la línea lo dice en negrita. P2 · Dos correcciones de rótulo en el bloque. La línea "Corredor" pasa a llamarse "Corredor LEGACY (eje x = ∅, GO-MARCADOR)" con la frase "el marcador por segmento es la línea de arriba". `instrucciones_vigentes` se deriva del archivo `instrucciones-proyecto-v*.md` de versión más alta presente en el árbol sin sufijo `-HISTORIA`/`-DELTA`, y el cuerpo curado deja de escribir la versión a mano. P3 · La cola de encargos deja de ocupar 64 líneas. El bloque lista solo los estados ≠ `CONSUMIDO` y da el conteo de consumidos. P4 · T0, por fin. Una línea en `.claude/commands/tramite.md` (y en la rutina equivalente si existe): `python3 tools/tablero_programa.py --actualiza`. Es derivación mecánica; no decide nada. P5 · NC por razón, con el parser bien hecho. Una lectura gruesa de dirección da 88 de 108 NC abiertas cuya `razon` no empieza por un token de A.14 — es pregunta, no hecho: mi parser cortaba por espacio y dos puntos. Deriva bien (token por prefijo, A.16), reporta conteo por token y cuántas son prosa; si la prosa pasa de un tercio, una línea en hallazgos `PARA-v2.15` y no las reescribas. P6 · Cuerpo curado, mínimo: §6 Bloqueadores G3–G5 citan cifras y FP viejas (FP-349 a 352; "una rama viva") → sustitúyelas por "ver bloque derivado". Ventana nueva §9.7 "19–20/sep": una frase por hecho, cada una con su comando, sin narrativa: pisos en rejilla, marcador, 46 adoptados, error de persistencia por instrumento, corrección de `familia.union.libre`, emisión C2 compuesta, PARO del piloto 3, salida del carril Codex. P7 · Regenera y commitea el bloque contra main; `check.py --baseline` verde por FAIL.

PERÍMETRO
`tools/tablero_programa.py` · `tools/estado_comun.py` solo si P1 lo exige · `forense/tablero/TABLERO-PROGRAMA.md` · `.claude/commands/tramite.md` · `tests/test_tablero_programa.py` (un caso por línea nueva) · cascada. No toca `tools/corrida0.py`, `marcador_segmento.py`, ningún TSV fuente, `milpa/`. «Si te encuentras escribiendo fuera de esta lista, PARA.»
LO QUE NO HACE
No decide firmas de contador ni borra ramas: las hace visibles. No reescribe razones de NC. No toca los snapshots históricos del tablero.
