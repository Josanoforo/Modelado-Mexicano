# Gobernanza del programa · Psicología del Mexicano Contemporáneo
### `gobernanza` · **v1.15** · 30 de julio de 2026 · **62 ADR**

> | | |
> |---|---|
> | **ARCHIVO** | `gobernanza-v1.15.md` |
> | **REEMPLAZA A** | `gobernanza-v1.14.md` — **borrar** |
> | **VERIFICAS ASÍ** | ADR-36 tiene **adenda (c)** sobre series numeradas · §2 lista los tres `milpa-*` · §4 (registro del perímetro del Hito D) trae la corrección de RÓTULO fechada 29/jul — el perímetro sigue en **27** · detalle ADR-44 a ADR-58 en **§0.1**, abajo — el último es **ADR-61** (§4, no está en §0.1) |
> | **NOMBRE ESTABLE** | **`gobernanza`** — cítalo así, **nunca por nombre de archivo** |

### 0.1 Detalle de verificación (ADR-44 a ADR-58)

§4 trae ADR-44 (publicación del repositorio, sin registro previo), ADR-45 (vocabulario de "prueba de falsación", D-05), ADR-46 (unidad de contaminación es la SESIÓN, no la máquina — corrige `forense/hallazgos-congelados-2026-07-30.yaml` E-03) , ADR-47 (falsar una regla ≠ calibrar un coeficiente — cierra `forense/hallazgos-congelados-2026-07-30.yaml` D-09/D-10), ADR-48 (R0 congela el aparato de auditoría; la cola sale del canon, `bitacora.py --cierra` se apaga, `check.py` en CI se conserva), ADR-49 (tres decisiones de mesa: se retira `unico_calibrable_hoy`, se sella la clase `AJUSTADO`, G1a homogeneidad de pendientes declarada), ADR-50 (el motor se calibra por AJUSTE, no por identificación causal; MILPA Fase 1 deja de estar POSPUESTA; **29 libres del ajuste = 14 probabilidades + 15 coeficientes** — cifra corregida por ADR-51, ver abajo) ADR-51 (reescribe ADR-50: el ajuste sobrevive, se retira la calibrabilidad de los libres sin reactivo; **corregido 3/ago/2026: eran "29 = 14+15", son 22 = 7+15** — las 13 reglas de `asignados_probabilidad` suman 1.0 cada una, así que sus valores no son grados de libertad independientes; registra M2/M3 con veredicto de C, cierra `D-12`) y **ADR-52** (dos decisiones de mesa, 4/ago/2026: A — el reparto de las 14 sella "sin reactivo verificado — búsqueda abierta" / "sin reactivo — búsqueda cerrada", `exposicion_violencia`/`sens_estatus` abierta, `aversion_riesgo` cerrada; B — `G1a` nombra su componente, `confianza_institucional[financiera] −0.60`, no se desdobla) y **ADR-53** (sella el veredicto D5-INESTABLE de P3, descarta D6 con razón escrita, habilita el LCA multinivel de `p3-lca-preregistro-v1_0.md` §2.5 sin correrlo) y **ADR-54** (`sens_estatus` sale de "búsqueda abierta" y pasa a "búsqueda cerrada (límite de régimen)", vía el precedente de `aversion_riesgo` en ADR-52 A, fundado en el examen de descriptor de `PR #64`) y **ADR-55** (Encargo Y: propaga `R4.2` → `D` al bloque append-only, 3 de 27 → 4 de 27; `R5.2`/`R1.2` corren completas, no se archivan) y **ADR-56** (Encargo Z: propaga `R4.1`/`R9.1`/`R4.3` mitad A/`R4.3` mitad B/`R9.2` → `D` al bloque append-only, 4 de 27 → 8 de 27) y **ADR-57** (asociar ≠ identificar: los tres β̂ marginales del Encargo W no representan coeficientes de generador — ninguno sobrevive condicionar sin matiz, X §6; compuerta de identificación sobre afirmaciones de intervención, tres llaves con estado verificado, ninguna ejercida; el reactivo genérico ENCIG `P11_1_23` de confianza en servidores públicos se registra como escala propia fuera del vector de ADR-28.b) y **ADR-58** (la escala de falsación gana su fila `E` de corroboración prospectiva y su regla de precedencia, declaradas al sellar; adjudica `R5.1`/`R5.2` → `A` con reserva estadística escrita en la línea, y — MESA-R12=SÍ — `R1.2` → `E` como excepción acotada y nombrada; contador del bloque append-only de `hitoD-preregistro`, 8 de 27 → 11 de 27) — el último es **ADR-58**.

---

*Documento vivo. Registra **qué se decidió, por qué, y qué se rompe si cambia**.
No repite el contenido del corpus: lo gobierna.*

**Versión de este documento:** 1.15 · **Estado del programa:** Ronda 4 cerrada y **auditada**; modelo v2 y glosario v5 consolidados; Fase 1 del simulador **ya NO pospuesta — es la arquitectura objetivo (ADR-50/51)**. *(Corregido 29/jul/2026: decía 1.1, contradiciendo la cabecera de este mismo archivo, que en ese momento ya decía 1.9 — hallazgo nuevo de esa sesión, no catalogado por `censo-integridad-v1_0.md`. Corregido de nuevo 30/jul/2026: decía 1.12, contradiciendo la cabecera de este mismo archivo, que ya decía 1.14 y ahora dice 1.15 — reapareció en el propio commit que registró el defecto como `forense/hallazgos-congelados-2026-07-30.yaml` I-12; ver esa entrada. Corregido 3/ago/2026: decía "Fase 1 del simulador **pospuesta**" — ADR-50 (31/jul) ya había cambiado ese estado y declaró la cascada pendiente sin ejecutarla; esta línea no se tocó entonces. ADR-51 la cierra.)*

---

## 1. Qué gobierna esto (y qué no)

**Gobierna:** qué artefacto es fuente de verdad, cómo se propagan los cambios, qué decisiones están tomadas y cuáles siguen abiertas.

**No gobierna:** el contenido sustantivo (eso vive en los reports, el glosario y el modelo) ni las reglas de rigor (esas viven en `instrucciones-proyecto-v2.md`, que es un artefacto aparte y **canónico**).

**Por qué existe.** El programa creció de 10 reports a un corpus de 30 + glosario + integrador + modelo + validación forense + simulador, con varias decisiones **revertidas o refinadas sobre la marcha**. Sin registro, esas reversiones se ven como incoherencias y alguien las "corrige" de vuelta.

---

## 2. Cadena de dependencia y fuentes de verdad

```
instrucciones-proyecto-v2.md   ← CANÓNICO. Gobierna todo lo demás.
            │
            ▼
   31 reports temáticos         ← CANÓNICO (evidencia primaria)
            │
            ├── meta-auditoría comunicación   ← parche a un report
            ▼
   glosario-v5-consolidado      ← CANÓNICO. Donde los tiers se LEEN (ADR-02).
            │                     Único punto legítimo de entrada de un tier al programa.
            ▼
   integrador                   ← DERIVADO de reports + glosario
            │
            ▼
   modelo-decisiones-v2         ← CANÓNICO OPERATIVO
            │                     ▲
            │                     └── validaciones forenses (retropropagación, ADR-29)
            ▼
            │                     (la ficha derivada se ELIMINÓ — ADR-36.b:
            │                      era superficie de desincronización pura)
            ├──────────────┬──────────────────┐
            ▼              ▼                  ▼
      milpa/rules/*   masterclass.html   prompts verticales
      (ejecutable)     (enseñanza)        (operativo)
```

**Regla de oro de la cadena:** un artefacto **derivado** nunca se edita directamente. Se edita su fuente y se regenera. Si alguien edita la ficha canónica a mano, el modelo y la ficha divergen en silencio — el fallo exacto del vertical V1.

⚠️ **Cambio de v1.0:** el **glosario entra a la cadena**. En v1.0 no aparecía, pese a que ADR-02 lo hace depositario de los tiers. Esa omisión permitió que **el modelo creara reglas sin pasar por él** — la ruta por la que la regla estrella migró de dominio sin que nada rechinara (Hito 2). **Toda regla nueva del modelo debe tener constructo tierizado en el glosario o declararse explícitamente como propuesta sin tier.**

| Artefacto | Estado | Versión | Se regenera desde |
|---|---|---|---|
| `instrucciones-proyecto-v2.md` | **CANÓNICO** | v2 | — (se edita con ADR) |
| **31 reports** en `/mnt/project/` | **CANÓNICO** | Ronda 3. **3 parchados 28/jul** con nota fechada (consumidor · comunicación · foundational). *Eran "30" por conteo de memoria; verificado contra disco el 28/jul. Y los parches se registraban como hechos el 27/jul sin estarlo* | — |
| `glosario-v5.6.md` | **CANÓNICO** | **v5.6**, autocontenido | reports (mapas de evidencia) |
| `integrador-psicologia-mexicano.md` | DERIVADO | 31 reports | reports + glosario |
| `meta-auditoria-comunicacion.md` | CANÓNICO (parche) | 1.0 | — |
| `modelo-decision-v4.0.md` | **CANÓNICO OPERATIVO** | **v4.0, autocontenido** *(absorbe la ficha)* | integrador + glosario + validaciones |
| ~~`ficha-canonica-modelo.md`~~ | **ELIMINADA 28/jul** *(ADR-36.b)* | — | absorbida en `modelo` §0.1, §0.2 y §9 |
| **5 validaciones forenses** | **CANÓNICO** *(ADR-29.b)* | Ronda 4 | — |
| ~~`CHECKPOINT-v2.md`~~ · ~~`mapa-y-roadmap.md`~~ · ~~`inventario-corpus.md`~~ | **BORRADOS 28/jul** | — | **fusionados en `ESTADO-PROGRAMA.md`** |
| `estado-programa-v1.10.md` | **CANÓNICO (estado)** | v1.10, 4/ago | única fuente de estado |
| `ADR-30.md` | **BORRADO 28/jul** | — | incorporado a §4; además contenía la versión **superada** (retiraba `familismo` de G3, corregido en mesa) |
| `milpa/` (Fase 0) | DERIVADO | 0.1.0 | ⚠️ **ausente salvo 3 archivos** |
| `masterclass-mexico.html` | DERIVADO | foto Ronda 4 | ⚠️ **ausente** |
| **`milpa-whitepaper`** | CANÓNICO (programa) | v0.1 | — |
| **`milpa-spec`** | CANÓNICO (programa) | **v0.2** | — |
| **`milpa-plan`** | CANÓNICO (programa) | v0.1 | — |
| `modelo-decisiones-mexicano.md` v1 | **BORRADO** 27/jul | — | superado por v2 |
| `glosario` v2 / v3 / v4 | **BORRADOS** 27/jul | — | consolidados en v5 |
| `estado-proyecto-...md` | **BORRADO** 27/jul | — | superado |

⚠️ **Corrección 29/jul/2026 (sesión de correcciones):** la tabla de arriba citaba glosario v5.5, modelo v3.0 y estado v1.1 (los nombres de archivo exactos viven en el diff de este commit) pese a que este mismo documento ya iba en v1.9 — congelada desde una versión bastante anterior de `gobernanza`, nunca actualizada en los saltos de versión intermedios. Corregido a las versiones vigentes (glosario v5.6, modelo v3.3, estado v1.9). *(`censo-integridad-v1_0.md` C5-02.)*

**Tres nombres citados por `curaduria-archivos.md` (27/jul) que ningún documento declaró borrados o renombrados** *(gap encontrado por `tests/check.py` T03, bucket `nombrado_sin_borrado_explicito`; declarado aquí porque este §2 es donde vive el Registro de artefactos — el resto de borrados de esta sección ya sigue ese patrón)*:

| Nombre citado (27/jul) | Qué pasó | Certeza |
|---|---|---|
| `gobernanza-programa.md` v1.0 | Renombrado bajo la convención de ADR-36 (28/jul) a la serie versionada punto-menor de `gobernanza` (hoy `gobernanza-v1.15.md`) — es este mismo documento, con linaje continuo (ADR-26 a 29 incorporados, tal como `curaduria-archivos.md:37` pedía para su v1.1) | Alta — el propio ADR-36 describe la migración de nombre de todo archivo canónico |
| `glosario-v5.md` | Renombrado bajo ADR-36 a la serie versionada punto-menor de `glosario` (hoy `glosario-v5.6.md`) | Alta, mismo mecanismo |
| `CHECKPOINT-programa-psicologia-mexicano.md` | Probable antecesor de `CHECKPOINT-v2.md`, que esta misma tabla ya declara **BORRADO 28/jul, fusionado en `ESTADO-PROGRAMA.md`** (fila de arriba) | **Media** — el nombre no coincide exacto y no hay commit localizable que confirme el renombre `CHECKPOINT-programa-psicologia-mexicano.md` → `CHECKPOINT-v2.md`; se declara aquí como la lectura más probable, no como hecho verificado |

**Regla de borrado** *(aprendida el 27/jul):* un artefacto superado se borra **solo después** de que su sucesor sea autocontenido. El glosario se hizo bien; el modelo se borró antes de consolidar el v2 y se salvó por una copia temporal. **Consolidar primero, borrar después** — no es detalle de proceso, es lo único que separa "versión limpia" de "pérdida".

---

## 3. Protocolo de cambio (lo que evita la deriva)

Cuando algo cambia, se clasifica y se propaga. **Sin excepción.**

| Severidad | Qué es | Qué obliga |
|---|---|---|
| **S1 · Regla rota** | Una validación rompe o degrada una regla | ADR nuevo → modelo §7 → regenerar ficha → recompilar `milpa/rules` → validador → masterclass → **+ retropropagación (§3.1)** |
| **S2 · Cambio de alcance** | Entra o sale algo del modelo | ADR nuevo → revisar **todos** los derivados → verificar backtests → **+ retropropagación** |
| **S3 · Evidencia nueva** | Se añade un report o una validación confirma algo | Glosario → integrador → modelo → regenerar ficha |
| **S4 · Redacción** | Claridad, formato, ejemplos | Sin propagación; sin ADR |
| **S5 · Pendiente irresuelto** ⭐ | Una contradicción que **no** se resuelve | **Nuevo en v1.1** *(ADR-27)*. Se registra en §5.1 con su estado. No dispara propagación, pero **debe tener casillero** |

**Por qué existe S5.** El protocolo v1.0 solo sabía registrar *cambios*, y "seguimos sin saber esto" no es un cambio. Sin casillero, un pendiente se cae: de tres choques que registró el integrador, dos llegaron a ADR y **el tercero se perdió** porque no había dónde ponerlo.

### 3.1 Retropropagación *(ADR-29 — nuevo en v1.1)*

El protocolo v1.0 propagaba en **una sola dirección**: corpus → integrador → modelo → derivados. No había protocolo para cuando una validación rompe algo **aguas arriba**. Costó seis casos, uno irreparable.

Cuando una validación rompe o degrada una afirmación:

1. **El report dueño recibe nota de corrección fechada** *(29.a)*, en el archivo, no en un documento aparte.
2. **El artefacto forense se archiva como canónico** junto a los reports, **completo e incluyendo los casos descartados con su motivo** *(29.b)*.
3. **El modelo se sincroniza internamente** *(29.c)*: §3 no puede cargar tiers que §7 ya superó.
4. **El validador rechaza un artefacto forense sin tabla de descartes**, igual que rechaza un número sin procedencia *(adenda)*.

*Por qué el punto 4:* el defecto nunca fue que faltara el protocolo — fue que **nadie estaba obligado a ejecutarlo**. Con la misma plantilla, V1 archivó sus descartes y V3 no archivó ninguno. **Un principio necesita un artefacto de salida que falte visiblemente si no se cumple.**

**Checklist de propagación S1/S2:**

```
[ ] ADR escrito, con qué evidencia lo motiva
[ ] modelo §7 actualizado (confirma/matiza/rompe)
[ ] ficha canónica REGENERADA (no editada a mano)
[ ] glosario: ¿el constructo cambia de tier?
[ ] RETROPROPAGACIÓN: nota fechada al report dueño (ADR-29.a)
[ ] RETROPROPAGACIÓN: forense archivado completo, con descartes (ADR-29.b)
[ ] milpa/rules recompilado + validador en verde
[ ] backtests revisados: ¿alguno dependía de la regla que cambió?
[ ] masterclass actualizada si el cambio es visible al lector
[ ] registro de artefactos: versión actualizada arriba
```

**Regla anti-captura:** un cambio de regla **sin cambio de fuente** es olor a captura. Si la evidencia no cambió, la regla no debería cambiar.

---

## 4. Registro de decisiones (ADR)

*Formato: qué se decidió · por qué · qué rompería revertirla · estado.*

### Fundación

**ADR-01 · El integrador se vacía en la estructura del Bloque B.** Los patrones del Bloque B *son* los que cruzan el corpus. → **Vigente.**

**ADR-02 · Regla de oro: los tiers se leen, no se reconstruyen de memoria.** Toda síntesis se construye leyendo los mapas de evidencia. Revertirla invalidaría la trazabilidad del corpus. → **Vigente, y en `instrucciones v2`.**

**ADR-03 · Firewall genético: la genómica queda fuera del corpus conductual.** → **Superada por ADR-19** (reformulada, no eliminada).

**ADR-04 · Marcar procedencia: población EN México ≠ diáspora ≠ marco importado.** Fue *la* falla recurrente (simpatía, machismo, marianismo son de diáspora). → **Vigente, y regla del Bloque A.**

### Corrección del eslabón débil

**ADR-05 · El choque honor vs. dignidad se resuelve a favor de emociones morales.** Smith 2017: autopercepción de dignidad, r=.96 dignidad-face. El dato de Castillo se conserva; se retira la etiqueta "honor". → **Vigente. Ejecutado en la fuente el 27/jul.**

**ADR-06 · Hofstede pasa de causa a correlato (crítica calibrada, no rechazo).** McSweeney es validez de constructo, no inexistencia. → **Vigente. Ejecutado en la fuente el 27/jul.**

### Instrucciones v2

**ADR-07 · El Bloque B aplica solo a reports temáticos.** → **Vigente.**

**ADR-08 · Se añade el Bloque C (validación forense) con sus tres blindajes.** → **Vigente, con el límite de ADR-29 sobre anti-confusión.**

**ADR-09 · Sección 11 opcional: reglas SI-ENTONCES para reports que alimenten el modelo.** → **Vigente.**

### Re-alcance

**ADR-10 · El sistema indígena-comunal vivo queda FUERA POR DISEÑO.** Es *otro orden institucional*, no un México sub-medido. → **Vigente.** *(Originada por el usuario.)*

**ADR-11 · La huella indígena difusa NO necesita perfil propio.** → **Vigente.**

**ADR-12 · El perfil rural mestizo se funde en el perfil 2.** De 8 perfiles a 6. → **Vigente. Errata residual corregida en modelo v2** (§3 decía "ocho perfiles").

**ADR-13 · La limitación real es clase *dentro* de la modernidad.** → **Vigente.**

### Ronda 4 · Validación

**ADR-14 · La ficha canónica se vuelve fuente de verdad ejecutable.** Motivada por un fallo real: V1 no pudo leer los documentos y reconstruyó de memoria. → **Vigente. La decisión de arquitectura más importante del programa.**

**ADR-15 · "Calidad y dignidad > precio en populares" se degrada a NO VALIDADA.** → **Vigente.** ⚠️ *Matiz de Hito 2: la regla del modelo que V1 rompió era **fantasma**. El constructo se degrada por su propia evidencia, no por haber roto una regla.*

**ADR-16 · G2 se refina: opera mediado por la estructura de crédito.** → **Vigente.**

**ADR-17 · El riesgo del crédito popular se relocaliza al prestamista.** → **Vigente.** *(n=2: Famsa y Crédito Real. Certificado como "la estructura decidió estos dos", no como ley general.)*

**ADR-18 · La percepción del secreto del voto es el canal causal del clientelismo.** 0.06 → 0.63 según observabilidad. → **Vigente.**

**ADR-19 · El firewall genético se reformula (reemplaza ADR-03).** Prohibida la inferencia ascendencia → conducta de grupo; admitido un canal individual estrecho (ADH1B, CYP2A6). → **Vigente.**

**ADR-20 · La confianza radial como canal de difusión se queda en HIPÓTESIS.** → **Vigente.** ⚠️ *Dos correcciones: (a) el report de tecnología **ya la tenía tierada así** — ADR-20 la redescubrió porque el modelo no lo leyó; (b) el desdoblamiento **nunca llegó al motor** hasta el modelo v2.*

### Simulador MILPA

**ADR-21 · El LLM queda FUERA del loop de decisión.** Motor de reglas compilado, determinista, auditable. → **Vigente.**

**ADR-22 · Los `params` se derivan cada tick; nunca son fijos por perfil.** Con params fijos el simulador sería esencialista por construcción. → **Vigente. Innegociable.**

**ADR-23 · Mapa doble obligatorio: conducta + espejo estructural.** → **Vigente.**

**ADR-24 · No se construye motor ABM ni síntesis poblacional propios.** → **Vigente.**

**ADR-25 · Gate de Fase 1: `bt.oxxo_vs_codi` debe pasar sin tunear a mano.** → **Vigente. ✅ CORRECCIÓN APLICADA el 28/jul/2026** *(ver ADR-37)*. ⚠️ *Histórico del defecto: la spec v0.1 describía la salida esperada en términos de **canal** —"el producto con canal de confianza se adopta; el institucional-frío no"—, lo que **conflacionaba §3.1 (canal personal, G1a) con §3.3 (utilidad vs. coerción)**. La explicación canónica del fracaso de CoDi es **riesgo fiscal percibido + fricción**, no desconfianza. Con esa spec, si MILPA implementaba bien el mecanismo de confianza y mal el de coerción, **el backtest pasaba igual, por la razón equivocada.* **El defecto estuvo abierto desde la Ronda 4: era el S2 más antiguo del programa.**

### Ronda 5 · Auditoría de la validación *(nuevo en v1.1)*

**ADR-26 · Los disparadores de contexto tienen DOS niveles, no uno.** Siete globales + **42 palancas de dominio**, evaluadas contra `(perfil, params, d_global, d_dominio)`. El modelo siempre tuvo dos niveles; la spec implementó uno, y las palancas quedaron invisibles para el bucle. **El gate de Fase 1 depende de `riesgo_fiscal_percibido`, que vive en §3.3 y no existe entre los siete globales.** *Adenda: entran como **booleanos de contexto**, no como parámetros calibrables — son estados, no magnitudes.* → **Vigente. S2.**

**ADR-27 · El choque alegría vs. malestar es un ARTEFACTO DE AGREGACIÓN, no una contradicción.** México sale #10 mundial en satisfacción vital y a la vez registra 18.1M de carga de salud mental, 39.8% de soledad en mayores y 135,445 desaparecidos. **Ambos lados son verdaderos en sus segmentos y solo chocan al promediarlos en un número nacional.** Se prohíbe todo parámetro de bienestar agregado nacional (modelo §5.4): es el objeto que fabrica la paradoja. Las dos resoluciones fáciles —*"feliz a pesar de todo"* y *"eso es negación"*— son igual de esencialistas. **Pendiente real y separado:** la escalera de Cantril mide **evaluación vital, no alegría**. **Crea la severidad S5 y el casillero de §5.1.** → **Vigente. S2.** *(Reformulada en mesa: el ADR original la declaraba "irresuelta"; el usuario objetó que ambas pueden ser verdad, y el veredicto correcto resultó ser que la pregunta estaba mal planteada.)*

**ADR-28 · Cuatro cambios de esquema.**
- **28.a · `procedencia` obligatoria en cada número** (`MEDIDO`/`DERIVADO`/`ORDINAL→CARDINAL`/`ASIGNADO`); el validador rechaza sin ella. *De **144** números, 4 están medidos; hoy un 0.93 de ENVIPE y un 0.74 asignado se ven idénticos.* **Corolario, duplicado como guardarraíl de lectura:** ninguna salida con precisión decimal. *(ADR-49, D2: se sella una quinta clase, `AJUSTADO`, ver `milpa/procedencia.yaml`; nace vacía.)* ⚠️ **`4 de 144 MEDIDO` deja de ser el titular único del programa (ADR-50): confunde tres trabajos con métodos y criterios de término distintos — medir un NIVEL (90 `params_base`), ajustar un RITMO (15 coeficientes) y decidir un ABIERTO (39 probabilidades de regla). Ningún número de esta línea cambia; ver ADR-50 para la partición completa y ADR-51 para los grados de libertad reales del ajuste. *(Corregido 3/ago/2026: decía "los 29 libres del ajuste" — son **22** (7+15), no 29 (14+15); ver ADR-51.)***
- **28.b · `confianza_institucional` de escalar a vector.** *Marina 89%, escuelas 77% vs. partidos 23.9%. Un escalar predice que quien desconfía de la policía desconfía de la Marina.*
- **28.c · Base por perfil solo con mecanismo estructural nombrado**, con **condición de dominancia** verificable: un agente del perfil 2 en celda de baja violencia debe quedar por debajo de uno del perfil 1 en celda de alta violencia. Si el orden no se invierte, la base domina al entorno y el parámetro es un rasgo.
- **28.d · `params_base` sale de una distribución, no de un punto.** *Con puntos, el modelo produce seis clases de mexicano — la forma estadística del esencialismo.*

→ **Vigente. S2.** *(28.b, 28.c y 28.d resultaron ser los parches de tres fallos que la batería de refutaciones confirmó después de redactarse el ADR.)*

**ADR-29 · La propagación es bidireccional.** Ver §3.1. Motivada por **seis casos consumados**, cinco reparados y uno no: **PD-01, 14 descartes irrecuperables.** → **Vigente. S2.**

**ADR-30 · `familismo` se desdobla en dos parámetros; no se curva uno.** `familismo_apoyo` (positivo) y `familismo_obligacion` (negativo o no monotónico). *Motivación: la refutación A.23 falló — `G5 → familismo: 0.50` es monotónico positivo, pero Cahill 2021 halla efecto protector, Zeiders 2013 halla que el familismo **obligatorio** no protege y puede ser riesgo, y Fuligni 1999 halla relación **curvilínea**. Un coeficiente monotónico no admite el hallazgo.* **Por qué desdoblar y no curvar:** apoyo y obligación son constructos distintos (Calzada 2012), no extremos de una escala; y un parámetro curvado **no puede estar en dos puntos de la curva a la vez** — que es la situación de la cuidadora, con apoyo alto y obligación alta simultáneos. **Check obligatorio:** una configuración donde `familismo_obligacion` alto mejore todos los desenlaces se rechaza en compilación. ⚠️ *Ambos parámetros son `ASIGNADO` y heredan marca **(b)**. Se pasa de un número inventado a dos.* → **Vigente. S2.** *(Corregida en mesa: la propuesta original retiraba `familismo` de G3; se conserva ahí como `familismo_apoyo`, porque en G3 el mecanismo es pooling —la tanda— y `§3.1` enruta el ahorro informal por ese canal.)*

---

### Ronda 6 · Saldo de deuda documental *(nuevo en v1.2 — 28/jul/2026)*

**ADR-31 · Se retira el "híbrido" de honor del report foundational.** El texto decía que México es un híbrido: honor en lo rural/tradicional, dignidad en lo urbano/educado. **Se retira**, por tres razones: (a) convierte una diferencia de **clase** en una diferencia de **cultura**, que es el movimiento que este corpus existe para evitar; (b) **dignidad y face correlacionan r=.96** — un "híbrido" presupone una oposición que los datos no muestran; (c) la clasificación honor/dignidad/face es un marco importado **(c)** que el sujeto mexicano, preguntado, no reconoce (Smith et al. 2017: 2.º en dignidad, 5.º en honor). **Lo que se conserva:** sensibilidad reputacional alta `[MEDIA]`, leída como **gestión de face bajo autopercepción de dignidad**. → **Vigente. S2.** *Cierra el tercero y último caso de propagación fallida reparable.*

**ADR-32 · Todo principio nuevo nace con su artefacto de salida.** Motivado por el hallazgo del 28/jul: **dos de los seis casos de propagación figuraban como ✅ sin estarlo**, y el glosario, el modelo y la gobernanza repetían el ✅ unos de otros. La causa raíz no fue negligencia: fue que **nada faltaba visiblemente** cuando el parche no se aplicaba. Se instituyen tres requisitos mecánicos:
- **a) Retropropagación (ADR-29.a):** un caso no se marca ✅ sin `grep` verificado contra el report dueño. El artefacto que debe existir es la **nota de corrección fechada en la fuente**.
- **b) Regeneración de la ficha:** toda regeneración adjunta **diff de cobertura** (reglas del motor por §, presentes en la ficha, lista nominal de ausentes). Sin diff, la regeneración no se da por hecha.
- **c) Constructo sin glosario:** el validador rechaza toda regla de `modelo §3` cuyo `PORQUE` nombre un constructo ausente del glosario. Comprobación mecánica por `grep`.
→ **Vigente. S2.** *Este ADR es la generalización del patrón que explica casi todos los fallos del programa: principio declarado sin requisito de salida.*

---

**ADR-33 · Se prohíbe la diagonal en el `ENTONCES` de una regla.** Motivado por `§3.7`: *"se vive como derecho **/** gratitud al líder"* pegaba dos hipótesis **rivales** —si el apoyo es un derecho no se debe gratitud a nadie y el titular es reemplazable; si es gratitud al líder es favor personalizado y no derecho—. **Una afirmación que contiene su propia negación no es falsable, y el defecto es de redacción, no de dato.** Al partirla aparecieron un tier mal leído (la mitad de atribución es `Media` correlacional según el forense, no `Fuerte`) y una rotura archivada que nunca bajó al motor (`conf.08`).
**Requisito de salida:** el validador rechaza toda regla cuyo `ENTONCES` contenga `/` u `o` entre dos predicados de conducta distintos. Comprobación mecánica. → **Vigente. S2.**

**ADR-34 · Todo forense cierra con TABLA DE PROPAGACIÓN, o no se archiva como canónico.** Motivado por el `barrido-propagacion-forense`: los cinco forenses produjeron **22 veredictos ROMPE/MATIZA** y **6 nunca bajaron al motor** (tasa de fuga ~41%), incluida una rotura que tocaba **G3, el único generador probado**. **El patrón no fue aleatorio: se fugó lo condicional.** Llegaron los veredictos binarios —"esto está roto"—, que caben en una línea de changelog; no llegaron los matices con condición —*"cede bajo proximidad y monitoreo"*, *"el techo está en 15–20%"*, *"parcialmente refutado como motor primario"*—, que obligan a reescribir la regla. **La retropropagación se ejecutó donde era barata.**
**Requisito de salida:** tabla con cuatro columnas — veredicto · regla del motor **citada textualmente** · edición concreta que exige · casilla de aplicado con fecha. Sin tabla, el forense no entra al canon. *ADR-29 ya ordenaba propagar; lo que faltaba era que el forense pudiera terminar sin dejar nada que faltara visiblemente.* → **Vigente. S2.**

**ADR-35 · Se amplía el alcance del motor al crédito, del lado del DECISOR — no del oferente.** Decidido el 28/jul para bajar P-04, P-05 y P-06 del barrido forense, que exigían reglas de crédito que el motor no tenía formuladas. Entran dos reglas en `§3.1` —**techo cuantificado de la disposición a pagar** (mora regulada 15–20%, viable solo con CAT de tres dígitos) y **advertencia condicional de la baja fricción** (`MEDIA`, tier asignado literalmente por V5)— y una prohibición dura en `§5.5` (**no afirmar burbuja**; el término admitido es *riesgo latente focalizado y vigilable*).
**Frontera explícita:** el motor **sigue sin entidad prestamista**. Modela al decisor. El hallazgo mejor sostenido del corpus sobre crédito —*el riesgo vive en el fondeo y el gobierno corporativo del prestamista, no en el deudor*— **no se puede representar aquí**, y su refutación sigue sin objeto. **Ampliar al lado de la oferta es una decisión distinta y mayor, que este ADR NO toma.** → **Vigente. S2.**

**ADR-36 · Nomenclatura versionada obligatoria, y se elimina la ficha canónica.** Dos partes de la misma decisión, motivadas por 22 versiones de archivo en una sola sesión y por la imposibilidad de distinguir dos archivos con el mismo nombre y la misma versión declarada.

**(a) Nomenclatura.** `<nombre-estable>-v<MAYOR>.<MENOR>.md`. MAYOR = estructura o alcance; MENOR = contenido. Cada archivo abre con bloque de **ARCHIVO · REEMPLAZA A (borrar) · VERIFICAS ASÍ · NOMBRE ESTABLE**. **Regla que lo sostiene: las referencias internas citan el NOMBRE ESTABLE, nunca el nombre de archivo** (*"ver `modelo §3.B`"*, no *"ver `modelo-decision-v3.0.md`"*), para que subir versión no deje referencias colgando. *Requisito de salida: un archivo sin bloque de cabecera no se sube.*

**(b) Se elimina `ficha-canonica-modelo.md`.** No contenía **ninguna afirmación propia**: era una compresión del modelo v1, que no era autocontenido. Desde el v2 el modelo lo es, así que la ficha solo aportaba **superficie de desincronización** — y esa desincronización **fue el mecanismo del Hito 2**: *de 13 reglas que los verticales dijeron estresar, 6 no existían en el motor y 4 divergían*. No inventaron: leyeron una ficha desfasada que omitía cuatro reglas `[FUERTE]` y degradaba una. Sus tres bloques con valor propio —leyenda de procedencia, alcance y frontera, e instrucción de cita textual— **se absorben en `modelo` §0.1, §0.2 y §9**. **Lo que se pega en los prompts pasa a ser el modelo íntegro.**
**Efecto medido:** un cambio típico pasa de tocar 6 archivos a tocar **1 o 2**. → **Vigente. S1** *(cambio de estructura del programa)*.

**(c) Adenda del 28/jul — las series numeradas también se versionan, con prefijo común y orden explícito.** Detectada al aplicar (a): los tres documentos de MILPA usaban `01/02/03` como nombre, y ese prefijo **era** su nomenclatura — codificaba **orden de lectura y dependencia**. Renombrar solo el `02` rompió la serie y dejó al documento diciendo *"Documento 2 de 3"* contra una numeración que ya no existía. **Es el defecto que ADR-36 existe para evitar, cometido al aplicarlo.**
**Regla:** una serie se renombra **completa**, con prefijo común (`milpa-whitepaper` · `milpa-spec` · `milpa-plan`), y **el orden de lectura pasa del nombre al cuerpo** — bloque explícito en la cabecera de los tres. *Un orden codificado en el nombre de archivo se pierde en cuanto el nombre cambia; escrito en el cuerpo, sobrevive a cualquier versión.*

**ADR-37 · El gate de Fase 1 exige TRES condiciones, no una: reproducción, prueba de mecanismo y anti-confusión.** Corrige la especificación que ADR-25 dejó pendiente desde la Ronda 4.

**(A) Reproducción** — con `coercitivo` y `riesgo_fiscal_percibido` encendidos, la adopción tipo CoDi queda **<10%** de la del canal retail-efectivo. *Necesaria, no suficiente: es lo único que pedía la spec v0.1.*
**(B) Prueba de mecanismo** — al apagar **`riesgo_fiscal_percibido`** con **el canal constante**, la brecha debe **colapsar ≥70%**. Si no colapsa, el modelo llegó al desenlace por otro camino.
**(C) Anti-confusión** — al apagar **el canal de confianza personal** con `riesgo_fiscal_percibido` encendido, la brecha debe **PERSISTIR** (se reduce <30%). **Ésta faltaba entera.** Sin ella el gate no distingue si pasó por §3.3 o por G1a — y **un gate que no distingue cuál de dos mecanismos lo hizo pasar no es un gate: es un espejo.**

**El gate pasa solo si A y B y C.**

**Dependencias verificadas:** `riesgo_fiscal_percibido` es disparador de **nivel 2** (ADR-26) y la spec v0.1 solo implementaba el nivel 1 —**el campo del que dependía el gate era invisible para el bucle**—; y la regla espejo `tramite.gobierno_digital.util_sin_coercion` ya está restituida en `tramite.yaml` v0.2.0, sin la cual apagar el disparador caería en `NO_COVERAGE` y **el gate pasaría por pérdida de cobertura, no por prueba de mecanismo**.

⚠️ **Los umbrales de B y C (≥70%, <30%) son ASIGNADOS**, no medidos: se eligen porque un colapso parcial no distingue mecanismos. **Calibrar contra series de adopción de SPEI antes de correr Fase 1.**
⚠️ **Límite declarado, no resuelto:** el gate **no separa coerción de fricción**. La explicación canónica del fracaso de CoDi tiene dos componentes y `riesgo_fiscal_percibido` solo captura el primero. → **Vigente. S2 → cerrado.** *Desbloquea `R3.4` del Hito D.*

**Hallazgo colateral, mismo día, misma familia:** la regla `civico.voto.clientelar` de la spec compilaba **`p: 0.63` con tier `FUERTE`**, y ese 0.63 es la cifra de **laboratorio** de Ascencio-Chang (2025), no de campo. **Se degrada a `MEDIA`** y se crea el campo obligatorio **`procedencia_p`** (`CAMPO` / `LABORATORIO` / `DERIVADA` / `ASIGNADA`): *es la disciplina de `procedencia.yaml` aplicada a las probabilidades de regla, a las que nunca se les había aplicado.* **Una `p` de laboratorio no puede llevar tier `FUERTE`.**

**Registro de decisión · Perímetro del Hito D = 27 reglas** *(28/jul/2026, antes de escribir el primer falsador)*. 20 `[FUERTE]` + 1 `[FUERTE como correlación]` + 1 compuesta `[FUERTE / MEDIA]` + **5** `[MEDIA-FUERTE]`. *(Eran 26; la partición de protesta/autodefensa por ADR-33 convirtió una en dos.)* Las `[MEDIA-FUERTE]` se falsan **contra la lectura fuerte**: si no sobreviven quedan **degradadas a `[MEDIA]`**, no refutadas. La compuesta lleva **un falsador por mitad**. La `[FUERTE como correlación]` se ataca **como correlación**, no como causa — atacar la causa fue el error de V1.

⚠️ **Corrección de RÓTULO, 29/jul/2026.** Este registro decía *"20 `[FUERTE]` + 5 `[MEDIA-FUERTE]` + 2 compuestas"*, tratando a `R1.4` (`[FUERTE como correlación]`) como si fuera una segunda regla partida. **Solo existe una compuesta:** `R4.3` (`[FUERTE / MEDIA]`), cuya ficha en `hitoD-preregistro` se declara "dos falsadores, uno por mitad". `R1.4` es un tier distinto — `[FUERTE como correlación]` —, no una mitad: lleva un solo falsador, contra la correlación. **El perímetro NO cambia: siguen siendo las mismas 27 reglas de esta misma decisión del 28/jul, antes del primer falsador.** Propagado a `modelo §7` (cambio 34) y `estado §4·S2` y `§7`.

---

### Sesión de correcciones · 29/jul/2026 (tarde) — censo de integridad documental

**ADR-38 · Ningún hallazgo sobre la ausencia o el contenido de un texto en otro archivo baja al canon sin el `grep`/lectura literal contra ese archivo, con línea citada.** Motivado por el caso "pelón": `a79227e` afirmó haber corrido `grep -rn "pelón"` y reportó que la frase "no aparece en ese archivo, ni en ningún otro del repo"; el comando real da 4 resultados, incluida la línea exacta que se dijo vacía. **17 segundos después**, en el mismo lote de trabajo, `7d6535e` bajó esa afirmación sin re-verificarla a `canon/estado-programa` — ÚNICA FUENTE DE ESTADO — y de ahí se propagó una tercera vez a `TRANSFER-maestra-8.md`, citada como *ejemplo de método riguroso*. **Cubre la dirección que ADR-29 no cubre:** ADR-29 (§3.1, retropropagación) protege contra hallazgos **correctos** que nunca bajan del forense al canon. Este ADR protege lo contrario: hallazgos **falsos** que sí bajan, y bajan rápido. Requisitos:
- **a)** Toda afirmación de la forma *"F dice/no dice X"*, *"la cita a F:L no checa"*, *"el archivo F no existe/no contiene Y"* que entre a un artefacto de `canon/` lleva, en el mismo commit, el comando de verificación y su salida real — no basta con citar la nota que lo afirma.
- **b)** Una afirmación sobre el contenido de un archivo ajeno **no hereda verificación** de la nota que la reporta. Bajarla a canon exige correrla de nuevo contra el archivo, no contra el reporte del hallazgo.
- **c)** Candidato a test mecánico, no implementado (requiere extraer la cita del texto libre — "T-grep-de-cita", `censo-integridad-v1_0.md §6`): hasta que exista, este ADR es la salvaguarda de proceso.
→ **Vigente. S2.** *(Aprobado 29/jul/2026, sesión de correcciones sobre `censo-integridad-v1_1.md §3`.)*

**ADR-39 · Verificación de premisas antes de ejecución.** Quien ejecuta un encargo verifica sus premisas contra el archivo **antes** de obedecerlo. Si una premisa no se sostiene, **se detiene y lo reporta** — no la ejecuta, y no ajusta el texto del encargo ni del corpus para que cuadre con una premisa falsa. **Motivado por:** la sesión de correcciones que produjo ADR-38 se comisionó desde un reporte de conversación no verificado contra el repo. Verificar antes de ejecutar encontró, entre otras cosas: la cita de `censo-integridad-v1_0.md` a `estado-programa-v1_9.md:208,234` ("idéntico") apunta a una línea 234 que no existe en un archivo de 212 líneas — la premisa de "dos citas idénticas" no se sostenía tal como estaba escrita; y tratar las tres ocurrencias de "107 WARN" del mismo archivo con una corrección uniforme habría fabricado una afirmación histórica falsa en una entrada de changelog que era exacta cuando se escribió. **Por qué vive aquí y no solo en `instrucciones-proyecto-v2.md`:** esa regla ya existe del lado de las instrucciones de sesión (v2.1), pero un artefacto de `canon/` no puede citar una instrucción de chat — este ADR le da ancla citable dentro del corpus. Requisito: antes de aplicar cualquier corrección que un encargo describa con un hecho verificable (cifra, cita, archivo, estado), correr la verificación **antes** del primer edit; si el resultado contradice la premisa, el hallazgo se reporta como entregable — no se fuerza el edit para que la premisa parezca correcta. No aplica a juicios de valor o preferencias del encargo, solo a hechos sobre el estado del repo.
→ **Vigente. S2.** *(Aprobado 29/jul/2026, misma sesión.)*

⚠️ **Nota de la sesión, sobre versión de archivo:** ADR-36 pide bump de versión MENOR ante todo cambio de contenido canónico, lo que llevaría este archivo a la siguiente versión punto-menor y a un rename de archivo, con su cascada de referencias (`estado §0`, cabeceras cruzadas). **No se ejecuta aquí** — el encargo que aprobó estos dos ADR pidió aplicarlos con los números correspondientes, no el bump de versión completo, y ese es un cambio de mayor alcance que merece su propio diff aprobado por separado.

---

**ADR-40 · Forma canónica de un veredicto archivado; vive en un bloque designado del pre-registro, no en `estado` ni en prosa libre.** Corrige el diseño original de `T18` (T-PASO2-EJECUCION, sesión de tests, 29/jul/2026), rechazado dos veces:

- **Primer rechazo:** derivaba su conteo "real" leyendo `canon/estado-programa` y lo comparaba contra el contador declarado en el mismo archivo — no cruzaba frontera de archivo, el mismo alcance autorreferencial que `T05` (constructos contra una lista fija en `check.py`, no contra un tercer origen).
- **Segundo rechazo:** el rediseño movió la forma canónica a `hitoD-preregistro`, pero permitía que apareciera en cualquier prosa del archivo — **no distinguía emitir de citar o hipotetizar**. El propio primer borrador de la Nota 5 que archivaba el veredicto de `R1.1` disparó su propio patrón al citar, entre backticks, la narración vieja de `estado`. Un archivo append-only que existe para discutir fichas necesita poder decir *"si `R3.2` → veredicto `B`…"* como hipótesis sin que eso fabrique un veredicto falso.

**Forma canónica**, para que un test pueda derivarla mecánicamente: `` `RX.Y` → veredicto `Z` `` — ID de regla entre backticks (el de `modelo §7`, Registro congelado), flecha `→` (no `->` ni `=>`), la palabra `veredicto` literal en minúsculas, la letra `A`/`B`/`C`/`D` entre backticks. Puede llevar paréntesis explicativo después; no antes.

**Dónde vive:** en un bloque designado, append-only, al final de `hitoD-preregistro` (`## Registro de veredictos archivados`) — **la única sección que un test puede leer** para derivar el conteo real. Fuera de ese bloque, la forma canónica en cualquier prosa de cualquier documento es cita o hipótesis, nunca emisión, y ningún test la cuenta. `estado §7` sigue narrando el resultado y contando cuántos van (`estado:192`), pero no es la fuente del archivo — es un consumidor, igual que cualquier otro documento que la cite.

**(a) El detector de forma sospechosa es heurístico, no un aserto.** `T18` también busca, dentro del bloque, líneas con forma de veredicto que no cumplan la forma exacta (ID de regla + la palabra "veredicto" a poca distancia, sin la flecha/backticks correctos) — para que una variante no se archive invisible. Sus constantes de proximidad (≤20 caracteres entre el ID y "veredicto", ≤15 entre "veredicto" y la letra) están **afinadas a mano contra un solo caso real conocido** (flecha ASCII `->` en vez de `→`), no derivadas de un principio. Falsos negativos conocidos: cualquier variante que separe el ID de regla y "veredicto", o "veredicto" y la letra, más allá de esas ventanas no se detecta. Falsos positivos conocidos: antes de esta versión, la clase de carácter de la letra era insensible a mayúsculas (heredaba `re.I` de "veredicto"), lo que habría marcado la preposición española "a" como si fuera la letra `A`; corregido, la letra ahora exige mayúscula exacta — pero el detector sigue sin garantía formal contra cualquier prosa futura que combine, por casualidad, un ID de regla entre backticks con la palabra "veredicto" y una letra mayúscula A-D dentro de esas ventanas.

**(b) Propagación legítima vs. editar para que un test calle.** Este mismo ADR subió el conteo de ADR de la sesión anterior en una unidad, y eso obligó a corregir tres citas que todavía citaban el conteo viejo (`estado:25`, `estado:95`, `gobernanza:2`) para que `T15` no quedara en rojo. **Eso es propagación legítima:** el número de ADR cambió de verdad al aprobar este ADR, y las citas dependientes deben reflejar el nuevo estado real — es exactamente lo que `T15` existe para exigir. Es una jugada **distinta y prohibida** editar un documento —o inventar una excepción, un bloque, una exención— **para que un test deje de fallar sin que el hecho subyacente haya cambiado**. La diferencia no es el gesto (ambas son ediciones), es si el número editado corresponde a un cambio real verificado (aquí sí: `grep -c` contra `gobernanza` da 40) o si el edit existe únicamente para silenciar la alarma.

**(c) Una propiedad afirmada de un test se verifica con un caso real del corpus, no con una aserción en el comentario.** Precedente, el mismo día: el punto (a) de arriba, en su primer borrador, afirmaba que quitar `re.I` de la letra corregía el falso positivo de la preposición "a" — sin correr la línea vieja contra la línea nueva para comprobarlo. Se corrió: `` `El caso de \`R5.1\` tiene veredicto a evaluar todavía.` `` — el patrón viejo (con `re.I` heredado) la marcaba como veredicto `A`; el nuevo, no. La corrección quedó verificada, no solo declarada. **Regla general:** un comentario o docstring que describe qué hace o qué evita un test es una afirmación como cualquier otra en este corpus — pesa lo mismo que una cifra en `estado`, y se sostiene igual: con un caso real corrido, no con la autoridad de quien lo escribió.

**(d) Riesgo conocido: reformular prosa para no disparar un test.** El remedio se aplicó dos veces hoy, en la misma sesión que escribió este ADR: la Nota 5 de `hitoD-preregistro` se reescribió para no disparar `T18` (movió el archivo del veredicto fuera de la prosa narrativa), y el punto (b) de este mismo ADR se reescribió para no disparar `T15` (evitó el par dígito+"ADR" literal al explicar la propagación 39→40). **Es válido mientras el texto reformulado siga afirmando lo mismo** — ninguna de las dos reformulaciones cambió un hecho, solo la forma de decirlo. Pero **T18 tiene una zona designada que hace esa distinción estructural** (dentro del bloque cuenta, fuera no, sin importar la forma); `T14` y `T15` no tienen zona equivalente — su única defensa contra este mismo choque futuro es que quien escribe prosa sobre "cuántos reports" o "cuántos ADR" recuerde reformular a mano. Es una disciplina, no una garantía, y no tiene test que la verifique.
→ **Vigente. S2.** *(Aprobado 29/jul/2026, misma sesión de tests, revisado tres veces antes de aprobarse.)*

**ADR-41 · Una regla aprobada en chat no rige hasta que existe en archivo.** Antes de eso es hipótesis, aunque todos la estén siguiendo. **Motivado por:** seis de los elementos de la subida de `instrucciones-proyecto-v2.md` a v2.2 (tres reglas de v2.1, cuatro de v2.2 más su pregunta de auditoría — la unión real, no las dos listas de "cinco" que traía el traspaso, que no coincidían entre sí) existieron solo en conversación, gobernaron el trabajo de una sesión completa, y se perdieron cuando la sesión se hizo en un entorno sin commitear. El único respaldo fue el chat. **Es la misma familia que ADR-29** —hallazgo correcto que no baja al motor— un piso más arriba: regla correcta que no baja al canon.

**Segundo hallazgo, derivado en sesión el 29/jul/2026.** La capa de autoría del repo no registraba firmante humano. De 25 commits, **21 tienen a Claude como autor, 4 a `corpus@local`, cero a un humano** (verificado: `git log --format='%an <%ae>'`). El trailer `Co-Authored-By` nombraba a Claude en **17 líneas** —es decir, duplicaba al autor en vez de complementarlo— y en **cuatro formas distintas** (`Claude`, `Claude Opus 4.6 (1M context)`, `Claude Opus 5`, `Claude Sonnet 5`), una con especificación de ventana de contexto dentro del campo de identidad. **La historia previa no se reescribe:** es el registro fiel de cómo se hizo el trabajo, y falsificarla sería falsificar procedencia. De aquí adelante: **Jonas autor, Claude co-autor, una forma normalizada de la cadena de modelo.**
→ **Vigente. S2.** *(Aprobado 29/jul/2026.)*

**ADR-42 · Qué significa el verde en este repo.** El único control automático sobre `main` es `.github/workflows/verify.yml` (`push`/`pull_request`/`workflow_dispatch`, un job, `python3 tests/check.py --baseline`). Tres registros retroactivos, ninguno existía antes en archivo:

**(1) El control cambió de semántica sin ADR.** El commit `f320550` ("CI: usa el modo linea base en vez de check.py sin banderas") movió el paso de CI de `check.py` sin banderas a `check.py --baseline` — un cambio de qué cuenta como éxito del único gate automático del repo, hecho sin pasar por gobernanza. Este ADR lo registra ahora, no lo autoriza retroactivamente ni lo revierte.

**(2) Verde no significa "0 fallas".** Significa **"0 fallas nuevas frente a una línea base congelada"** — hoy esa línea base (`tests/baseline.json`, `head afa7c7f`) contiene **18 FAIL y 81 WARN**, y el CI pasa con esos 99 presentes. Mecanismo exacto: `_baseline_compare()` (`tests/check.py:824-848`) calcula `nuevos = current - known` sobre la unión de FAIL y WARN, y `return 1 if nuevos else 0` — el proceso solo falla si aparece algo que no estaba en el archivo congelado. Un checkmark verde en la página de Actions no es "sin defectos": es "sin defectos que no supiéramos ya".

**(3) Límite de la clave, descubierto en sesión — no es un defecto, es una propiedad que hay que declarar.** `_baseline_key()` (`tests/check.py:736-737`) normaliza cada mensaje quitándole el número de línea (`re.sub(r":\d+ ", ": ", msg, count=1)`) antes de convertirlo en clave de comparación — necesario: sin esa normalización, cualquier edición que desplace líneas produciría falsos positivos masivos contra un defecto ya conocido. **Consecuencia declarada:** si un defecto ya congelado se reproduce N veces más en el mismo archivo, la suite no detecta ninguna de las nuevas apariciones — la clave `(test, mensaje-sin-línea)` ya estaba en `known`, y cuenta como la misma. **Caso probado en sesión, no hipotético:** dos WARN de T03 sobre `forense/censo-integridad-v1_1.md` (líneas 49 y 85), ambos citando el mismo nombre de archivo histórico (estado-programa, versión ya superada), colapsan en una sola clave — 82 WARN crudos, 81 tras la normalización. **Corolario operativo:** `--baseline` protege contra defectos *nuevos*, no contra la *multiplicación* de uno ya conocido dentro del mismo archivo; contar cuántas veces se repite un defecto congelado exige leer la salida sin `--baseline`, no confiar en el veredicto de línea base.
→ **Vigente. S2.** *(Aprobado 29/jul/2026, derivado de auditoría de sesión contra `tests/check.py` y `.github/workflows/verify.yml`.)*

**ADR-43 · Esquema de co-autoría (precisión a ADR-41).** ADR-41 pidió "una forma normalizada de la cadena de modelo". Su primera aplicación, cinco commits después, produjo **dos** formas: `Claude Sonnet 5` (cuatro commits) y `Claude Fable 5` (uno) — porque cada commit se escribió con el modelo de sesión vigente en ese momento, y ADR-41 no distinguía si "normalizada" quería decir una cadena fija o un formato consistente. **La historia no se reescribe** — los cinco commits quedan como están. **Precisión, no corrección:** la forma normalizada es un **esquema**, no una cadena única — `Claude <modelo> <noreply@anthropic.com>`, con el nombre del modelo como campo variable y el resto fijo. Es deliberado, no una relajación: registrar **qué modelo** co-escribió cada commit es procedencia más fina, no más débil — la misma disciplina que `procedencia.yaml` exige para los números del modelo, aplicada a la autoría. Verificado: los cinco commits de la sesión de ADR-41 ya cumplen este esquema tal como queda definido aquí — ninguno necesita enmienda.
→ **Vigente. S2.** *(Aprobado 29/jul/2026.)*

**ADR-44 · El repositorio es público; la función del programa pasa de privado-descriptivo a público-auditable.** El repo quedó público el 29/jul/2026 sin registro de decisión — mismo patrón que ADR-42(1): un cambio de semántica del programa entero, hecho sin pasar por gobernanza. Este ADR lo registra ahora, no lo autoriza retroactivamente ni lo revierte, y fija sus requisitos de salida:

**(a) Capa legal en el árbol — verificado en sesión.** `LICENSE`, `AUTHORSHIP.md`, `USO-ACEPTABLE.md`, `AVISO-DE-ALCANCE.md` y `CITATION.cff` existen en la raíz (`git ls-files`, corrido el 30/jul/2026). Ninguno de los cinco falta.

**(b) Re-examen de deuda "asumida a propósito" (regla v2.2).** Toda deuda declarada en `§5` de este documento como "asumida a propósito" queda re-examinada a esta fecha, no solo re-declarada. El dictamen ítem por ítem ya se corrió — este ADR no lo repite ni lo reabre — y vive en `revision-publicacion-2026-07-30.md §1` (tabla "EL EJE"), verificado ahí contra `gobernanza §5`, `estado §4` y `forense/hallazgos-congelados-2026-07-30.yaml`. Tres deudas caducaron por completo con la función pública (cero datos primarios propios, ya caducada dos veces; las ocho refutaciones sin objeto; el baseline con la autodeclaración falsa de `hitoD-preregistro:8`, congelada "por decisión de mesa" cuando la mesa todavía era privada); las demás siguen decisión con su forma o presentación ajustada, no con su fondo.

**(c) README sin cifra de estado no vigilada por test — PENDIENTE, es F6.** No se cumple hoy. `README.md` sigue teniendo cifras de estado (§"Estado del modelo", §"Falsos positivos conocidos") sin ningún test que las derive y las compare contra el árbol, más allá de la corrección puntual que esta misma sesión aplicó a `README:40` y a los conteos de T03/T10. El molde que cerraría esta condición (`T-README`, mismo patrón que `T16`) está en `forense/hallazgos-congelados-2026-07-30.yaml` y **fuera del perímetro de la sesión que sella este ADR** — regla de sesión vigente: T-README es F6 y no se toca en sesiones de canon. Este requisito de salida queda abierto a propósito, no por descuido: se cierra cuando exista el test, no antes, y bloquea la apertura de cualquier vía de patrocinio — no el registro de este ADR.

**(d) Ninguna decisión de mesa futura asume lector interno.** Regla hacia adelante, no retroactiva: toda decisión que de aquí en más se registre en `forense/hallazgos-congelados-2026-07-30.yaml`, en este documento o en `estado` se escribe asumiendo que quien la lee puede ser un desconocido, no el propio equipo. La deuda #12 de la tabla eje citada en (b) — "el repo es privado / esto solo lo leemos nosotros", nunca declarada ni medida — fue la última decisión de mesa que asumió lector interno sin decirlo; no se detectó otra de la misma clase en esta sesión.

**Dato de cierre, aportado por el encargo y no re-verificable desde esta sesión** (sin acceso autenticado a Settings/Insights de GitHub — mismo trato que un supuesto [S1] de `revision-publicacion-2026-07-30.md`, no una cifra derivada de este árbol): con "Preserve this repository" activo, ya existiría copia del árbol en el GitHub Archive Program. Si es correcto, el repliegue a privado **no revertiría la exposición** — cerraría, por el lado de los hechos y no de la decisión, la deuda #12 citada arriba: la restricción "el repo es privado" ya sería falsa incluso antes de este ADR. Verificarlo con acceso autenticado a GitHub queda en cola; este ADR no lo da por confirmado, lo registra como recibido.
→ **Vigente. S2.** *(Aprobado 30/jul/2026, sesión de canon F2/F4. (a) verificado en sesión; (b) referido a `revision-publicacion-2026-07-30.md`, no reabierto aquí; (c) declarado PENDIENTE, condición de salida no cumplida; (d) regla hacia adelante.)*

---

**ADR-45 · Vocabulario fijo de "prueba de falsación corrida"; tres poblaciones, nunca mezcladas sin marca.** Decisión de mesa del autor, 30/jul/2026, sobre la evidencia registrada en `forense/hallazgos-congelados-2026-07-30.yaml` D-05 (derivada al intentar propagar la corrección de README:40 al canon: cinco líneas de `estado`, `modelo` y `gobernanza` invocaban "una prueba de falsación corrida" sin decir de cuál de tres poblaciones distintas hablaban, más una autocontradicción de `modelo` sobre si G1a está falsado). Las tres respuestas se fijan **antes** de ver resultados y no se renegocian al escribir:

**(1) Denominador — se reportan AMBOS, siempre con etiqueta explícita de cuál es cuál.** Nunca uno solo, nunca los dos en la misma celda sin distinguir. `27` = perímetro elegible del Hito D (`gobernanza §4`, ADR-37); `49` = reglas totales del motor (`modelo §3.B`/§7, T12). `27` es subconjunto de `49`: las dos fracciones son verdaderas a la vez — el defecto era no decir cuál se estaba reportando, no que una de las dos fuera falsa.

**(2) Un veredicto `D` cuenta como prueba corrida.** Se corrió el procedimiento pre-registrado y produjo un veredicto archivado (`R1.1` → `D`, inejecutable por hueco de mercado, no de dato). Condición: la letra viaja junto al conteo, siempre — "2 de 27 corridas (`D`, `B`)", nunca solo "2 de 27". Razón registrable: lo contrario haría que el programa reporte menos trabajo del que hizo, y escondería el hallazgo de `hitoD-R1.1` — que el mercado del seguro agrícola voluntario no existe para la población de volatilidad máxima (Seguro Agrícola Catastrófico no contratable por el productor; Fondos de Aseguramiento concentrados en riego/gran extensión; adopción voluntaria atada al financiamiento).

**(3) Las tres poblaciones se reportan separadas — nunca sumadas, nunca una presentada como otra.**
- **Hito D** — falsación de REGLAS del motor. Vocabulario `RX.Y → veredicto A-E`. Fuente única: el bloque append-only `## Registro de veredictos archivados` de `hitoD-preregistro` (ADR-40). Hoy: 12 de 27 corridas (`R1.1`→`D`, `R3.2`→`B`, `R7.2`→`D`, `R4.2`→`D`, `R4.1`→`D`, `R9.1`→`D`, `R4.3`→`D` mitad A y mitad B, `R9.2`→`D`, `R5.1`→`A`, `R5.2`→`A`, `R1.2`→`E`, `R3.1`→`B`). *(Corregido 4/ago/2026 — propaga `R7.2`→`D`; propaga `R4.2`→`D`, ADR-55, Encargo Y; propaga `R4.1`/`R9.1`/`R4.3`/`R9.2`→`D`, ADR-56, Encargo Z; adjudica `R5.1`/`R5.2`→`A` y `R1.2`→`E`, ADR-58; adjudica `R3.1`→`B`, ADR-60 — cascada completa ejecutada en este acto, cerrando la deuda que ADR-58(e) dejó declarada en la nota de §4 abajo.)*
- **Hito C** — falsación de GENERADORES. Vocabulario ✅ probado / ⚠️ contradicho-contestado / ⬜ sin falsar. Fuente única: `forense/hitoC-prueba-generadores.md`. Hoy: G3 ✅, G1b ⚠️ contradicho, G2 ⚠️ contestado, G1a/G4/G5/G6 ⬜ sin falsar.
- **Ejercicio de glosario sobre G1a** (`glosario §6/§10`, 27/jul/2026, "utilidad + fricción baja > confianza", dominio seguro agrícola) — objeto propio. Origen `FORENSE` (`corpus/forense/Apuestas_Conductuales_sobre_el_Consumidor_Mexicano...md`, Etapa 3), veredicto informal B. **Anterior a que el Hito D existiera** (28/jul), **nunca pasó por el protocolo de ADR-40** ni entró al bloque archivado: no tiene ID de regla, no cuenta contra el 27 ni contra el 49, y no es "el Hito D".

**Requisito de salida.** Ninguna afirmación de conteo de veredictos o de corridas de falsación existe en `canon/` fuera de su bloque de fuente única sin decir cuál de las tres poblaciones cita y con qué denominador. `I-07` (candidato a test que mecanice esto) queda abierta — este ADR fija el vocabulario; no construye el instrumento que lo vigile.
→ **Vigente. S2.** *(Aprobado 30/jul/2026, sesión de canon. Cierra `forense/hallazgos-congelados-2026-07-30.yaml` D-05. Propagado a `modelo` (v3.4, cambio 36, §0.1/§7), `estado` (§0, L5, §4·S3) y `glosario` (§6, §10).)*

---

**ADR-46 · La unidad de contaminación es LA SESIÓN, no la máquina ni el modelo — dos niveles, condición verificable.** Decisión de mesa del autor, 30/jul/2026, sobre la evidencia registrada en `forense/hallazgos-congelados-2026-07-30.yaml` E-03 (declaraba "esta máquina queda INHABILITADA para pre-registrar contra las encuestas efectivamente descargadas" tras la tanda de Hito D Fase 1, ampliación del Paso 4). El texto original erraba en dos direcciones a la vez:

**(a) Demasiado amplio.** Un modelo no retiene contexto entre sesiones. Una sesión nueva arrancada en frío en la misma máquina no ha leído nada de lo que esa máquina procesó antes y está tan limpia como una sesión en la nube. Inhabilitar hardware desperdicia una vertiente entera del programa: bajo ese criterio, cada tanda de descargas quema la única máquina con salida real a INEGI.

**(b) Demasiado estrecho en lo que sí importa.** Lo que contamina no es dónde corrió el proceso de descarga: es qué puede LEER una sesión nueva en ese entorno — los payloads en `data/raw/`, y la bitácora que describe qué se exploró de la estructura de la fuente antes de bajarlos. Eso se controla con instrucción de encargo (qué archivos toca una sesión), no con qué hardware la ejecuta.

**Criterio fijado.**

**(1) Unidad de contaminación: LA SESIÓN — su contexto de lectura acumulado —, nunca la máquina ni el modelo que la corre.** Dos sesiones en la misma máquina, o la misma sesión reanudada en otra, no comparten contaminación por compartir hardware o proveedor de modelo; comparten contaminación solo si una leyó lo que la otra escribió.

**(2) Dos niveles de contacto con una fuente, que el texto original de `forense/hallazgos-congelados-2026-07-30.yaml` E-03 colapsaba en uno:**
- **Descarga ciega** — url, sha256, tamaño, formato del payload. No contamina: la sesión no aprende nada sobre el CONTENIDO de la fuente por el solo hecho de haber movido bytes con una URL ya conocida de antemano.
- **Exploración de estructura** — descubrir qué ediciones existen, probar patrones de nombre de archivo, leer cómo el portal organiza sus descargas (títulos de página, JSON-LD, navegación, acordeones). Contamina PARCIALMENTE: no es leer variables ni valores, pero sí es aprender algo sobre la fuente que una sesión sin ese contexto no sabría. Declarar hasta dónde llegó la exploración es obligatorio — no basta con declarar que hubo descarga.

**(3) Condición verificable, que reemplaza la prohibición de hardware.** Una sesión puede pre-registrar contra una fuente **si y solo si** no ha leído `data/raw/` de esa fuente, **ni** la bitácora de la tanda que la bajó, **ni** ningún registro de exploración de su estructura. Es una condición sobre lo que esa sesión ha leído, verificable contra su propio historial de lectura — no una propiedad del entorno donde corre.

**(4) El conservador va del lado de declarar más exploración, no menos.** Cuando el registro de una tanda no permite distinguir con certeza si un contacto con la fuente fue descarga ciega o exploración de estructura, se trata como exploración de estructura completa. Aplicado retroactivamente a `forense/hallazgos-congelados-2026-07-30.yaml` E-03: ninguna encuesta de la tanda del 30/jul calificó como descarga ciega — las cuatro efectivamente descargadas (ENCIG/`R3.1`, ENIF/`R1.2`, ENVIPE/`R7.2`, ENIGH-nc/`R5.1`) requirieron derivar la lista real del portal probando patrones de nombre por edición, y las tres que NO se descargaron (ENUT/`R5.2`, ENCUCI/`R8.3`, ENSANUT/`R4.2`) también tuvieron exploración de estructura real (JSON-LD, navegación, patrones de nombre) sin llegar a descarga — el E-03 original las declaraba libres de contaminación por no haberse descargado nada, sin ver que la exploración misma ya contamina parcialmente.

**(5) Aplicado a `forense/hallazgos-congelados-2026-07-30.yaml` E-02.** Verificado en esta sesión: el texto de E-02 ya trazaba la unidad de contaminación sobre "esa conversación", no sobre una máquina — no tenía el defecto (a)/(b) que este ADR corrige en E-03. No requirió reescritura, solo esta constancia de que se revisó.

**Requisito de salida.** Ninguna entrada futura de `forense/hallazgos-congelados-2026-07-30.yaml` que declare contaminación por descarga o exploración de una fuente usa "máquina" como unidad — usa "sesión" y, si hubo contacto con la fuente antes de la descarga (o en vez de ella), declara el nivel (descarga ciega / exploración de estructura) y hasta dónde llegó.
→ **Vigente. S2.** *(Aprobado 30/jul/2026, corrección ejecutada por una sesión distinta de la que bajó los datos de Hito D Fase 1 Paso 4 — ver `forense/hallazgos-congelados-2026-07-30.yaml` E-04 sobre por qué esa separación es deliberada. Corrige `forense/hallazgos-congelados-2026-07-30.yaml` E-03 con nota fechada, texto original conservado íntegro. Verificado contra E-02: sin el mismo defecto, no reescrita.)*

---

**ADR-47 · Falsar una regla no es calibrar un coeficiente — son dos tareas distintas, y el perímetro del Hito D las mezcla sin marca.** Decisión de mesa del autor, 30/jul/2026, sobre la evidencia registrada en `forense/hallazgos-congelados-2026-07-30.yaml` D-09 y D-10 (chequeo `CAL-X` de `CAL-G3`, `hitoD-preregistro-v2_0.md` Nota 8, y la aritmética de poder sobre el panel completo — `tests/calx_g3.py`, extensión narrada en `forense/notas/2026-07-30-calx-g3-salida.txt`).

**El distingo, nombrado.** **FALSAR UNA REGLA** pregunta *¿existe el patrón?* — veredicto binario (A/B/C/D, `hitoD-preregistro` §"Escala"), y exige **poder para resolver en ambas direcciones**: un criterio que solo puede confirmar y nunca refutar no es un falsador, es un embudo. **CALIBRAR UN COEFICIENTE** pregunta *¿cuánto vale?* — magnitud con banda de incertidumbre, y **no exige poder refutador**: una elasticidad estimada con un intervalo de confianza amplio sigue siendo información sobre la magnitud, aunque ese intervalo no alcance para declarar un nulo estricto. Tratar la segunda tarea con el aparato de la primera —exigirle a una calibración que además pase un gate binario de refutación— es aplicar una vara que la tarea no necesita y que, si no se puede alcanzar, bloquea un entregable que nunca dependía de alcanzarla.

**`CAL-G3` es el primer caso donde la confusión se hizo visible, no el único lugar donde puede existir.** `hitoD-preregistro-v2_0.md` Nota 8 (a) mostró que el criterio de falsación de `CAL-G3` (`CAL-C`/`9b`, RR≤1 con IC95% de precisión) es aritméticamente inalcanzable con las tasas base del codebook — confirmado, no solo con una transición del panel sino apilando las dos (`tests/calx_g3.py`, extensión del 30/jul): el mejor caso de toda la matriz (escenario A, ICC=0, cota imposible) da IC95%sup 1.281 contra el <1.25 exigido; los escenarios con poder real, B (olas 2-3, IC95%sup 1.461) y C (olas 1 y 3, IC95%sup 1.401), quedan más lejos todavía. **Ninguno de los dos alcanza.** El motivo de fondo, sin embargo, no es la falta de poder: `CAL-G3` nunca fue una tarea de falsación. `G3` no está en duda como dirección de generador (`hitoC-prueba-generadores.md`: ✅ probado y sobrevive); lo que está en duda es su **magnitud**, y el propio pre-registro de `CAL-G3` (`hitoD-preregistro-v2_0.md`, punto 2) ya lo intuía al declarar la elasticidad autónoma como entregable **independiente** del veredicto de falsación — la ficha traía las dos tareas encimadas desde que se escribió, sin nombrar el conflicto.

**Decisión de mesa, dos partes:**

**(1) `D-10` — la muestra analítica de `CAL-G3` se restringe a las olas 2-3.** El punto (4) de la ficha declaraba el módulo `TB` idéntico en las tres olas; no lo es — la ola 1 carece por completo de la opción *"9. Ninguna de las anteriores"*, y su categoría 3 (*"Contrato verbal o de palabra"*) opera como residual atrapa-todo que el instrumento parte en dos a partir de la ola 2 (`Nota 8`, punto b: cat3 ola1=6,044 sin cat9; cat3+cat9 ola2=5,570, reconstruye el 6,044 con 8% de holgura; cat3 sola de ola2=1,772 no lo reconstruye, −71%). Un diseño que identifica **con transiciones dentro-de-unidad** no puede correr sobre un instrumento que **fabrica** transiciones espurias — un jefe sin cambio real de situación se codifica informal en 2002 y sale del contraste en 2005, sin dirección de sesgo declarable a priori. La opción alternativa (imputar un equivalente de "ninguna" en 2002) exige un supuesto sobre datos que no existen, con un sesgo sin dirección declarable — se descarta. **El costo aparente de restringir a olas 2-3 es hipotético, no real**: la aritmética de poder (arriba) muestra que el escenario de tres olas apiladas (A) tampoco alcanza el gate de falsación en ningún nivel de ICC — el mejor caso posible (1.281) sigue del lado inalcanzable de 1.25. Restringir a olas 2-3 no sacrifica poder de falsación útil, porque ese poder no existía con tres olas tampoco.

**(2) `D-09` — el entregable de `CAL-G3` se parte.** La elasticidad se estima y se archiva como resultado **descriptivo**, con su intervalo de confianza, **sin** veredicto de falsación asociado. `CAL-G3` no emite `CAL-A`/`B`/`C`/`X` y no entra al conteo de corridas archivadas del bloque `## Registro de veredictos archivados` (ADR-40) — ese bloque es de veredictos `RX.Y`, y `CAL-G3` nunca tuvo namespace `R` (`hitoD-preregistro-v2_0.md` Nota 7, nota inicial: *"Esta ficha NO falsa una regla del motor"*). El criterio de falsación de `CAL-G3` (`CAL-C`/`9b`) queda **verificado sobre las dos transiciones del panel completo, no solo una**, y sigue fallando incluso bajo el supuesto más favorable posible (ICC=0, cota que ningún panel real alcanza) — no es un problema de correlación intra-hogar que un diseño distinto resolvería; es que el techo duro de hogares discordantes en el desenlace es estructuralmente insuficiente frente al `n` que la precisión exigida requiere. Aplicarle a una tarea de calibración un aparato de falsación que no puede pasar produciría, si se corriera la Fase C tal como está sellada, un `CAL-A` o `CAL-B` que no significaría lo que aparenta — exactamente lo que `Nota 8 (a)` ya advertía y este ADR ahora resuelve en mesa, antes de abrir microdatos.

**Lo que este ADR NO hace.** No reclasifica ninguna de las otras 27 fichas del perímetro del Hito D como falsación o calibración — eso es trabajo aparte, abierto sin resolver en `forense/hallazgos-congelados-2026-07-30.yaml` (ver la entrada nueva de esta misma sesión). No estima la elasticidad de `G3 → horizonte_temporal`: la Fase C sigue sin correrse en esta sesión. No toca `milpa/procedencia.yaml`: `-0.60` sigue `ASIGNADO` hasta que exista una estimación archivada.

**Requisito de salida.** `hitoD-preregistro-v2_0.md` recibe nota fechada (Nota 9) registrando ambas decisiones y este número de ADR, declarando la Fase C de `CAL-G3` **desbloqueada con alcance reducido** (olas 2-3, entregable descriptivo) y **no corrida en esta sesión**. `forense/hallazgos-congelados-2026-07-30.yaml` D-09 y D-10 cierran citando este ADR, sin editar su texto original — mismo molde que `D-06`. Ninguna ficha futura del Hito D que combine pregunta de existencia con pregunta de magnitud puede pre-registrarse sin declarar primero cuál de las dos es, siguiendo el distingo de este ADR.
→ **Vigente. S2.** *(Aprobado 30/jul/2026, decisiones de mesa del autor sobre `forense/hallazgos-congelados-2026-07-30.yaml` D-09/D-10 y la evidencia de `hitoD-preregistro-v2_0.md` Nota 8 y `forense/notas/2026-07-30-calx-g3-salida.txt`. No re-corre ningún cálculo: verifica los números citados contra archivo antes de escribir. Cierra `forense/hallazgos-congelados-2026-07-30.yaml` D-09 y D-10 con nota de cierre. Abre en `forense/hallazgos-congelados-2026-07-30.yaml`, sin resolver, la revisión de las 27 fichas del perímetro por tipo de tarea.)*

**ADR-48 · R0 congela el aparato de auditoría: cada sesión produce una medición o produce nada.** Decisión de mesa del autor, 30/jul/2026. El programa construyó, en tres días, un aparato de auditoría de proceso —cola con IDs y campo `casos`, derivador de bitácora, módulo de auditoría por escrito en el protocolo de cierre— y ese aparato empezó a consumir el trabajo que decía vigilar: el instrumento que mide el proceso acumuló sus propios defectos registrados (I-17, I-20 sobre `tests/bitacora.py`; I-18 sobre `tests/check.py`), documentar un falso positivo generó otro falso positivo dos veces el mismo día (I-01, casos 2 y 3), y dos ramas ciegas entre sí colisionaron un ID por derivarlo de contar el archivo (I-13). Ninguno de esos ciclos produjo una medición sobre México.

**La regla, entera.** *Cada sesión produce una medición o produce nada. Defecto que no impide medir: una línea en `forense/hallazgos.md` y sigue. Defecto que impide medir: para y reporta.* Sustituye al módulo de auditoría por escrito de `protocolo-sesion-v1_0.md` §2.

**Qué se corta.**
- **La cola sale del canon.** `canon/cola.yaml` → `forense/hallazgos-congelados-2026-07-30.yaml`. Registro, no cola: no rige, no bloquea, no ordena prioridades, **no se le añaden entradas** y **no se borra** (la citan por ID commits, cuerpos de PR y bloques de bitácora). Las 40 entradas se congelan en el estado en que estaban, salvo dos cerradas en el mismo acto: I-01 e I-13.
- **`tests/bitacora.py --cierra` deja de invocarse.** Sale de `protocolo-sesion-v1_0.md` §2; no estaba en CI (`.github/workflows/verify.yml` corre solo `check.py --baseline`), así que no había nada que quitar ahí. El script se queda en el árbol, con su marca de apagado al inicio. `--abre` sigue vigente: deriva estado, no audita proceso.
- **El módulo de auditoría de rigor extremo del Bloque B queda solo para artefactos que afirman algo sobre México** — reports, integrador, modelo. No para forenses de proceso ni para notas. Es un estrechamiento de ADR-07 (que ya lo limitaba a reports temáticos) al criterio que importa: se le exige auditoría a lo que hace una afirmación sobre el país, no a lo que narra cómo trabajamos.

**Qué se conserva, explícitamente.** Cuatro cosas, y son las que miden o protegen lo medido:
1. **`tests/check.py` en CI**, en modo `--baseline`. Es el único vigilante automático del corpus y no se toca.
2. **Pre-registro antes de abrir dato.** La protección append-only de `forense/hitoD-preregistro-v2_0.md` y la prohibición 4 del protocolo siguen enteras.
3. **Marcas de procedencia y de tier.** Todo el aparato de ADR-32/36/45/46 sobre de dónde sale una afirmación y con qué fuerza se declara.
4. **`forense/` append-only.** Nada de lo ya escrito ahí se reescribe — por eso las citas a `canon/cola.yaml` que quedan vivas en bitácora, pre-registros y documentos recogidos verbatim **no** se reapuntaron, y el nombre `cola.yaml` entra a la lista `HISTORICOS` de `tests/check.py` como borrado declarado.

**Convención de IDs (cierra I-13).** Ningún ID nuevo se deriva contando un archivo. Se usa **`D-AAAAMMDD-HHMM`** —fecha y hora UTC de apertura— **o un hash corto**; ambos son independientes del estado de cualquier otra rama, así que dos sesiones que no se ven no pueden colisionar salvo empate al minuto, y ese caso se rompe con el hash. Los correlativos ya asignados (`I-01`…`I-20`, `D-01`…`D-12`, `E-01`…`E-07`, `C-01`) **no se renumeran**: son historia citada por commits y PRs.

**Marca de cita ilustrativa (cierra I-01).** `{cita-ilustrativa}`, escrita pegada a la cita que exime. Exime **solo** la cita inmediatamente anterior — no la línea, no el documento. Implementada en `tests/check.py`, `T03`.

**Condición de reversión.** R0 se levanta cuando el programa haya archivado **una medición nueva sobre México** —un veredicto `RX.Y` del bloque append-only de `hitoD-preregistro`, o un coeficiente que deje de ser `ASIGNADO` en `milpa/procedencia.yaml`— **posterior a este ADR**. Hasta entonces, ninguna sesión abre trabajo de aparato. Levantar R0 es un ADR nuevo que cita cuál fue esa medición; no se levanta por acumulación de incomodidad ni por antigüedad.

**Requisito de salida.** Este es el **único** ADR de la sesión que lo aprueba: R0 corta precisamente la práctica de abrir un ADR por cada cambio. Verificable contra archivo: `T15` no reporta huecos ni duplicados y toda cita `N ADR` en `canon/` dice **48**; `canon/cola.yaml` no existe; `forense/hallazgos-congelados-2026-07-30.yaml` existe con cabecera de congelación; `forense/hallazgos.md` existe con la regla como cabecera; `protocolo-sesion-v1_0.md` §2 no menciona `--cierra`.
→ **Vigente. S1.** *(Aprobado 30/jul/2026, decisión de mesa del autor. Fuera de perímetro deliberadamente: quitar el versionado de los nombres de archivo — es un rename de todo el canon con retropropagación de citas y espera a R1.)*

---

**ADR-49 · Tres decisiones de mesa del autor, 31/jul/2026, en un solo acto.**

**(1) D1 — se retira `unico_calibrable_hoy`.** No se corrige: se retira. Su nombre codificaba el supuesto que `forense/hallazgos.md` (31/jul/2026) derogó — que la vía ENOE identifica la elasticidad `G3 → horizonte_temporal` vía conducta financiera. Esa premisa es falsa a nivel de reactivo: ningún cuestionario ENOE/ENOEN trae ahorro, crédito, deuda, planeación ni expectativas. `milpa/procedencia.yaml:301` pierde el campo. Lo que sobrevive es cierto y va **sin adjetivo**: el panel rotativo trimestral de la ENOE sigue al mismo hogar cinco trimestres — propiedad del instrumento ENOE, no una vía de calibración. **Cascada corregida en el mismo acto:** `canon/modelo-decision-v3_4.md` (§2.2, "Punto de calibración prioritario"), `canon/glosario-v5_6.md` §13, `canon/estado-programa-v1_9.md` §S3 y este mismo documento (línea de "Elasticidades / coeficientes", arriba). ⚠️ **No afecta** los seis valores de `horizonte_temporal` por perfil (`params_base`, ORDINAL→CARDINAL): nunca dependieron de ENOE.

**(2) D2 — se sella la clase `AJUSTADO`.** Las clases de procedencia pasan de cuatro a cinco: `MEDIDO` · `DERIVADO` · `ORDINAL→CARDINAL` · `ASIGNADO` · **`AJUSTADO`** — reproduce los momentos observados de un dato real (media, varianza, transición); **no está identificado causalmente**. Exige campo `ruta:` obligatorio: `pseudo_panel` | `momentos` | `composicion` | `transversal_con_seleccion`. Dos límites, en el cuerpo de la definición (`milpa/procedencia.yaml`): contesta **CUÁNTO**, nunca **SI EL PATRÓN EXISTE** (ADR-47 ya fijó ese distingo para falsar-vs-calibrar; `AJUSTADO` hereda el límite). `ruta: composicion` exige la regla de descomposición **declarada antes de ver los datos** — inventada después es post-hoc, y el Bloque C (ADR-08, validación forense) es el que lo atrapa. **Nace vacía: cero números `AJUSTADO` hoy. Ningún `ASIGNADO` se reclasifica** por este ADR.

**(3) D3 — G1a: homogeneidad de pendientes DECLARADA, con secuencia.** En `canon/modelo-decision-v3_4.md` §2.2, junto a la tabla de coeficientes: el `−0.60` de `confianza_institucional[dominio]` (G1a) se aplica al componente que el dominio seleccione, y eso **asume una elasticidad común a los seis componentes** del vector de ADR-28.b. Supuesto declarado, no medido. Afecta **solo G1a** — `G4` usa `confianza_institucional[justicia]`, componente nombrado, sin el problema. **No se desdobla en seis:** serían seis `ASIGNADO` donde hoy hay uno. **Pre-registro:** la salida de `CAL-CONF` Fase A (los seis niveles de confianza institucional por perfil) es lo que revisita el supuesto — si la dispersión entre componentes es la que ADR-28.b sostiene, la pendiente común queda implausible y el desdoblamiento pasa a justificado. Condición escrita **antes** de tener el dato. ⚠️ **No cierra `D-12`** (`forense/hallazgos-congelados-2026-07-30.yaml`): D-12 es otra cosa — la operacionalización única de Hito E derogaría ADR-28.b sin nombrarlo. Sigue abierta. *(Corregido 4/ago/2026, ADR-52 B: el pre-registro corrió — `PR #58` — y su resultado es que la dispersión no es estimable en este régimen; la pregunta de esta nota vuelve a mesa sin el dato que esperaba, y la mesa decide nombrar el componente en vez de desdoblar: `confianza_institucional[dominio]` pasa a `confianza_institucional[financiera]`, `−0.60` sigue `ASIGNADO`. Ver `modelo:396` para el texto completo de la enmienda.)*

**Versión y nombre de archivo.** El número de versión **no sube** y el archivo **no se renombra** — mismo criterio que ADR-48: subirlo obliga a retropropagar citas por `forense/` (append-only, cita `gobernanza-v1_15.md` como historia en varias entradas). Excepción declarada a ADR-36, acotada a esta enmienda.

→ **Vigente. S2.** *(Aprobado 31/jul/2026, decisión de mesa del autor. D4 — la corrección de `milpa/procedencia.yaml` `estado:` de "18/31" a "15/34", citando `forense/cobertura-motor.md` — no es decisión de mesa, es corrección mecánica de una cifra ya refutada en `forense/hallazgos.md`; no lleva ADR.)*

---

**ADR-50 · El motor es un ABM EJECUTABLE que se calibra por AJUSTE, no por identificación causal; MILPA Fase 1 deja de estar POSPUESTA y pasa a ser la arquitectura objetivo del programa.** Decisión de mesa del autor, 31/jul/2026, sobre `forense/metodologia-identificacion-vs-ajuste-v0_1.md` (propuesta sin sellar hasta este acto) — corrige un error del chat: MILPA entró al proyecto precisamente para NO depender de elasticidades causales identificadas. Sus parámetros no necesitan reproducir un contrafactual individual: necesitan reproducir la conducta observada. La ruta `momentos`, ya sellada por ADR-49 (D2 — `ruta:` obligatorio en la clase `AJUSTADO`: `pseudo_panel` | `momentos` | `composicion` | `transversal_con_seleccion`), presupone exactamente esto.

**PERÍMETRO de esta sesión (ADR-46, declarado): `canon/`, `milpa/`, `forense/`.** No leyó `data/raw/`, `data/manifiesto.yaml`, `forense/bitacora.md` ni ninguna nota de descarga (`forense/notas/*`) — sesión de escritura, sin dato.

**(1) La partición de los 144 números deja de ser una bolsa.** Verificado contra `milpa/procedencia.yaml` (`resumen`, `asignados_probabilidad` líneas 214-288, `asignados_coeficiente`) y `canon/modelo-decision-v3_4.md` §2.2:
- **90 `params_base` = NIVELES.** Se miden de transversal (15 escalares × 6 perfiles, `modelo` §1.1). No son incógnitas del ajuste — `CAL-CONF` ya opera sobre uno de ellos (G1a, confianza institucional).
- **39 probabilidades** (`resumen.desglose.probabilidades_de_regla`) = **10 ya fijas** (4 `MEDIDO` + 6 `DERIVADO`) **+ 15 con `calibrable_con` declarado + 14 sin él** — conteo por VALORES dentro de `valores:`, no por id de regla: `asignados_probabilidad` tiene 13 ids y 29 valores `ASIGNADO`; 6 ids (`dinero.ahorro.informal_sin_puente`, `dinero.ahorro.con_puente_y_respaldo`, `dinero.planeacion.formal_estable`, `dinero.credito.scoring_alternativo`, `salud.atencion.leve_sin_imss`, `tramite.mordida.discrecional`) suman 15 valores con `calibrable_con`; los 7 restantes (`dinero.consumo.estatus_mediado_por_credito`, `salud.atencion.grave`, `salud.prevencion.hombre_sin_permiso`, `tramite.mordida.con_registro`, `tramite.gobierno_digital.coercitivo`, `tramite.gobierno_digital.util_sin_coercion`, `civico.denuncia.con_seguro`) suman los 14 sin él. Las fuentes citadas en `calibrable_con` nombran ENIF (×2), ENSANUT y ENCIG; CNBV (×2) no tiene fuente MX en disco declarada en este campo. Si esas fuentes están efectivamente descargadas hoy no se verifica aquí — exige `data/manifiesto.yaml`, fuera del perímetro de esta sesión (ADR-46); queda sin contrastar, mismo tratamiento que la nota vigente de `forense/metodologia-identificacion-vs-ajuste-v0_1.md` sobre el vector de confianza.
- **15 coeficientes** (`modelo` §2.2: *"Los quince coeficientes son `ASIGNADO`"*) = elasticidades, sin ruta viva. `unico_calibrable_hoy` ya se retiró (ADR-49, D1) y `milpa/procedencia.yaml:326` es explícito: *"Ningún coeficiente de generador tiene ruta hoy."*
- **LIBRES DEL AJUSTE = 14 + 15 = 29.** *(Corregido 3/ago/2026, ADR-51: las 13 reglas de `asignados_probabilidad` tienen valores que suman exactamente 1.0 — son distribuciones sobre desenlaces, no parámetros independientes; las 14 probabilidades "sin él" cargan **7** grados de libertad reales, no 14. **LIBRES DEL AJUSTE REALES = 7 + 15 = 22.** El "29" queda como conteo de valores asignados, no de grados de libertad; no se borra, se re-etiqueta.)*

**(2) El orden de trabajo, ya escrito en `forense/metodologia-identificacion-vs-ajuste-v0_1.md` §7 y ahora vinculante:** estados primero (90, transversal, 30 con instrumento verificado en disco — ver misma salvedad de perímetro arriba) · abiertos después (39, "nadie los ha mirado") · ritmos al final, por ajuste (15, únicamente por las rutas 2/3/5 de esa propuesta §4 — nunca por la 1, panel intra-sujeto, que `CAL-ENOE` Fase A ya cerró como no disponible, `forense/hallazgos.md` 31/jul/2026).

**(3) Los momentos a reproducir SE DECLARAN ANTES DE AJUSTAR.** Un conjunto de momentos elegido después de ver el resultado es curve-fitting con otro nombre, y el Bloque C (ADR-08, validación forense) lo prohíbe — mismo principio que `ruta: composicion` ya exige (ADR-49, D2: "declarada antes de ver los datos"). Esto NO crea el pre-registro aquí: establece que hará falta antes de que exista una sola corrida `AJUSTADO`.

**(4) La lectura "motor como contexto" queda CONSIDERADA Y NO ADOPTADA como entregable primario — no refutada.** No existe hoy como propuesta escrita: verificado sin resultados para "motor como contexto", "modelo sin motor" ni frase equivalente en `canon/`, `milpa/`, `forense/` — se registra aquí por primera vez, con su razón. Bajo esa lectura el contador del programa sería un delta contra un modelo SIN motor, más blando que reproducir agregados observados directamente, y con contaminación creciente por tratarse de un repo público — cualquier corrección que el propio repo publique después de correr esa comparación deja de ser un contrafactual limpio. No hay archivo de `forense/` que marcar porque no hay archivo que la proponga; si aparece uno después, este párrafo es la referencia a citar.

**(5) Riesgo declarado: identificabilidad — condición pendiente, no resuelta.** 29 libres es manejable *(corregido 3/ago/2026, ADR-51: son 22 grados de libertad reales, no 29 — ver arriba; la conclusión de este párrafo no cambia con la cifra corregida)*, pero un momento solo es utilizable si se puede computar POR PERFIL. `modelo` §1.1 distingue los seis perfiles por horizonte, radio de confianza, aversión al riesgo, sensibilidad a estatus, deferencia, familismo (apoyo/obligación), exposición a violencia, acceso digital y confianza institucional — no hay hoy un chequeo formal ya corrido sobre ejes demográficos (edad, urbanización, ingreso, acceso digital, migración); son lecturas plausibles de esa tabla, no una verificación. Si la segmentación no llega a los seis perfiles, el conteo de momentos utilizables cae. `CAL-CONF` Fase B —no existe hoy como artefacto, sin resultados en `forense/` ni `canon/`— es, junto con ese chequeo de ejes, lo que decide esto. Se escribe como condición pendiente, no como resuelto.

**CONSECUENCIA MÍNIMA, y solo esta.** `4 de 144 MEDIDO` (ADR-28.a, arriba) deja de ser el titular único: confunde tres trabajos con métodos y criterios de término distintos. Reflejado ahí como cross-referencia a la partición de (1) — ningún número se toca, ni el 4 ni el 144 ni ninguno de sus derivados.

**Deuda visible, sin cascada en este acto (fuera de la CONSECUENCIA MÍNIMA declarada arriba).** `milpa/refutations.yaml:65` (`bloqueo: "MILPA Fase 1 está POSPUESTA por decisión (no por olvido)"`) y `canon/estado-programa-v1_9.md:91` (*"Fase 1 POSPUESTA por decisión, no por olvido"*) siguen sin actualizar tras esta decisión — texto desactualizado hasta que una sesión futura corra la cascada. Se declara aquí para que no quede como contradicción silenciosa; ver `forense/hallazgos.md`, 31/jul/2026.

**LO QUE NO SE HACE.** No reordena Hito E. No cierra `D-12` (sigue abierta, ver ADR-49 D3). No toca reglas, tiers ni ids. No crea números nuevos ni pone ninguno a cero.

**Versión y nombre de archivo.** El número de versión **no sube** y el archivo **no se renombra** — mismo criterio que ADR-48/ADR-49: subirlo obliga a retropropagar citas por `forense/` (append-only). Excepción declarada a ADR-36, acotada a esta enmienda.

→ **Vigente. S2.** *(Aprobado 31/jul/2026, decisión de mesa del autor. Sesión de escritura, Sonnet, sin dato — perímetro `canon/`/`milpa/`/`forense/`, ADR-46.)*

---

**ADR-51 · ADR-50 SE REESCRIBE: el ajuste sobrevive, se retira la calibrabilidad de los libres sin reactivo; corrección 22 (no 29) grados de libertad reales del ajuste.** Decisión de mesa del autor, 3/ago/2026, sobre el veredicto de P2 (`forense/notas/2026-08-01-p2-momentos-atributos.md`, PR #41 fusionado — responde al Encargo P2, precondición de este ADR) y el veredicto de C (`forense/notas/2026-07-31-encargo-c-familismo-deferencia-reactivo.md`, PR #42 fusionado).

**(a) Qué sobrevive de ADR-50 y qué se retira.** Sobrevive íntegro: **el método de ajuste** (ruta `momentos`, sellada por ADR-49 D2) y **MILPA como arquitectura objetivo del programa** — Fase 1 deja de estar POSPUESTA, esto no se revierte. **Se retira** la afirmación operativa con la que ADR-50 cierra su riesgo declarado en (5): que los libres del ajuste, aun con identificabilidad pendiente de verificar, son en principio calibrables una vez resuelta la segmentación por perfil. P2 verificó esa premisa bajo el reencuadre perfiles→atributos que la mesa adoptó después de ADR-50 (`revision-programa-2026-07-31.md`) y su veredicto es explícito: *"VEREDICTO GLOBAL — ADR-50 SE REESCRIBE. La subidentificación persiste sobre atributos [...] Lo que se reescribe es la afirmación operativa de ADR-50: que los libres del ajuste son calibrables por ajuste. No lo eran sobre perfiles y no lo son sobre atributos, por causas distintas."* (`forense/notas/2026-08-01-p2-momentos-atributos.md`, cierre de §4). La causa cambió de lugar: bajo perfiles el obstáculo era de **segmentación** (celdas insuficientes); bajo atributos el número de celdas deja de ser el cuello de botella (P2 §1: "el rango de la matriz de diseño no crece con el número de celdas") y el obstáculo pasa a ser de **medición** — 6 de 9 parámetros de generador no tienen reactivo co-observado con un desenlace en ningún instrumento en disco (P2 §2). Un ADR de corrección que solo cambiara "perfil" por "atributo" produciría, en palabras de P2, "un canon falso en su afirmación central" — por eso se reescribe, no se corrige.

**(b) Corrección 22, no 29 — tres derivaciones, no dos.** La premisa que llegó a esta sesión afirmaba tres derivaciones independientes (P2, "chat maestro", esta sesión); verificada antes de obedecerla (ADR-39), solo dos son derivaciones computadas con salida propia — la mesa ("chat maestro") **encargó** la re-derivación a P2 (`revision-programa-2026-07-31.md:142`, *"P2 · ¿Los momentos por atributo identifican los 29 libres?"*) pero no aporta una cifra propia verificable en el repo; se cita aquí como el origen de la pregunta, no como tercera derivación. Las dos derivaciones computadas coinciden exactamente:

1. **P2** (`forense/notas/2026-08-01-p2-momentos-atributos.md` §0.1-0.2, script `derivar29.py` sobre `milpa/procedencia.yaml`, probado primero contra un id conocido): `asignados_probabilidad` tiene 13 ids y 29 valores; **las 13 reglas suman exactamente 1.0 cada una, sin excepción** — son distribuciones sobre desenlaces, no parámetros independientes. Las 14 probabilidades "sin `calibrable_con`" (7 ids binarios) cargan **7** grados de libertad reales, no 14. **22 = 7 + 15**, no 29 = 14 + 15.
2. **Esta sesión**, re-derivación independiente contra el mismo archivo, sin heredar el script de P2:
   ```
   total ids: 13
   total valores: 29
   cada id suma 1.0: OK
   ids CON calibrable_con: 6 valores: 15
   ids SIN calibrable_con: 7 valores: 14
   g.l. reales (SIN calibrable_con, k-1 por id): 7
   g.l. reales totales del ajuste = dof_sin + 15 coeficientes = 22
   ```
   Las 7 reglas sin `calibrable_con` son las mismas que reporta P2: `dinero.consumo.estatus_mediado_por_credito`, `salud.atencion.grave`, `salud.prevencion.hombre_sin_permiso`, `tramite.mordida.con_registro`, `tramite.gobierno_digital.coercitivo`, `tramite.gobierno_digital.util_sin_coercion`, `civico.denuncia.con_seguro` — las siete son binarias (2 valores), de ahí 7 ids × (2−1) = 7.

**El "29" vive en seis lugares del canon, no en tres** (verificado por `grep -n "29" canon/gobernanza-v1_15.md canon/estado-programa-v1_9.md`, filtrado a mano contra los "29" que son fechas o citas a ADR-29, descartados): la tabla `VERIFICAS ASÍ` de la cabecera de `gobernanza`, la entrada ADR-28.a (§4), el cuerpo de ADR-50 dos veces (el total y el párrafo de riesgo (5)), la fila de bitácora §7 de ADR-50, y `estado-programa-v1_9.md` L0. Las seis quedan corregidas en este mismo acto, con el valor viejo conservado visible junto a la corrección — ninguna edición silenciosa.

**(c) M2/M3 — veredicto de C, no menú.** `familismo_obligacion` (G5): **PROXY CON SUPUESTO DECLARADO** — ENUT 6.11/6.11a mide carga de cuidado a nivel persona con tiempo declarado; pasa de "sin reactivo" a "medible vía proxy, pendiente de medición" (`encargo-c-familismo-deferencia-reactivo.md §2.3`, PR #42). `deferencia` (G6): **NO DETERMINABLE — no colapsable a límite permanente.** Dos candidatas en disco (ENCUCI, ENCIG) dan SIN REACTIVO; cuatro candidatas temáticamente más fuertes (LAPOP, WVS, Latinobarómetro, ENCUP) no están en disco y no se pudieron inspeccionar. Verificado en esta sesión que C-bis no ha corrido (`grep -rn "C-bis"` sobre el árbol completo, cero resultados): el veredicto que registra este ADR es "indeterminado, con cola de verificación declarada (C-bis)" — no límite permanente, porque el corpus de las 6 candidatas no se agotó.

**(d) Cierre de `D-12`.** `forense/hallazgos-congelados-2026-07-30.yaml` D-12 preguntaba si "una operacionalización única por constructo" (plan de Hito E) derogaría ADR-28.b (vector de 6 componentes de `confianza_institucional`) — su causa era que, bajo perfiles, `modelo §1.1` no daba valor por componente y la tabla de perfiles forzaba a tratar el constructo como escalar. **El reencuadre elimina esa tabla**: θ pasa a estimarse como condicional sobre atributos, no como valor fijo por perfil, y G1 (`confianza_institucional`) sale de la clase "inidentificable con cualquier número de momentos" — pasa a IDENTIFICADO·TRUNCADO, con reactivo co-observado (ENCIG batería XI × `tramite.mordida.discrecional`). Cita textual de P2: *"`D-12` se disuelve"* (§4, fila G1) y *"No cierra `D-12`: aporta la derivación de que su premisa se disuelve bajo el reencuadre, y cerrarla es acto de mesa"* (§8). Este ADR es ese acto de mesa: **`D-12` se cierra**, citando la derivación de P2. El archivo congelado no se edita — la entrada original queda íntegra; el cierre se registra aquí y en `forense/hallazgos.md`.

**(e) Costo declarado del reencuadre: G6 empeora.** Bajo perfiles, `deferencia` venía **dada** por `modelo §1.1` (90 `params_base` `ASIGNADO`) — INV-SEG parte 3 §Prueba 2 la marcaba "OK" en los tres escenarios porque θ ya tenía valor asignado y solo faltaban celdas de segmentación. Bajo atributos θ **ya no viene dada**: hace falta reactivo que mida deferencia jerárquica per se, y ninguna de las 8 fuentes candidatas inspeccionadas o pendientes lo tiene confirmado. Cita textual de P2: *"⚠️ INV-SEG p3 §Prueba 2 marcaba G6 'OK' en los tres escenarios — ese veredicto valía bajo perfiles, donde θ venía dado por §1.1. Bajo el reencuadre G6 empeora, y es el ejemplo más limpio del intercambio de §2.b: se cambió un supuesto por una medición que no existe"* (§4). Este costo se registra en el cuerpo del ADR, no en nota al pie, por instrucción expresa del encargo que lo comisiona.

**Requisito de salida — cascada ejecutada en este acto.** `milpa/refutations.yaml:65` y `canon/estado-programa-v1_9.md §L4` dejan de decir "POSPUESTA" (deuda declarada por ADR-50 sin cascada, saldada aquí); `canon/gobernanza-v1_15.md` (cabecera, "Estado del programa") tenía el mismo defecto sin declarar y se corrige en el mismo acto. Las seis ocurrencias de "29 libres del ajuste = 14+15" listadas en (b) quedan corregidas a "22 = 7+15", valor viejo visible. `D-12` cierra, ver (d).

**LO QUE NO SE HACE.** No toca `canon/modelo-decision-v3_4.md` — eso es el Encargo B, sesión aparte. No reclasifica ningún `ASIGNADO` de `milpa/procedencia.yaml` — el conteo de valores (29) no cambia, solo se distingue de los grados de libertad (22). ~~No corre C-bis ni agota las 4 candidatas pendientes de `deferencia` — la cola de verificación queda declarada, no ejecutada.~~ *(Corregido 3/ago/2026: C-bis SÍ corrió — ver (f) abajo. El resto de esta frase, sobre Hito E, reglas, tiers e ids, sigue vigente sin cambio.)* No reordena Hito E ni toca reglas, tiers o ids.

**(f) Enmienda M3, 3/ago/2026 — C-bis corre; `deferencia` pasa de NO DETERMINABLE a PROXY CON SUPUESTO DECLARADO (parcial).** La cola de verificación que (c) dejó declarada y sin correr se ejecutó en sesión aparte (`forense/notas/2026-08-03-cbis-deferencia-externas.md`, PR #49 fusionado), contra las cuatro candidatas que (c) nombraba sin disco: LAPOP, WVS, Latinobarómetro, ENCUP. Verificado contra esa nota antes de sellar:

- **LAPOP/AmericasBarometer** (cuestionario México 2023, 77 pp., leído completo): **SIN REACTIVO** — ningún ítem sobre deferencia ante jefe, patrón o cabeza de familia (§2 de la nota).
- **World Values Survey (Ola 7, México 2018):** documento localizado con precisión (`DOID 6635`, `SAID 3203`) pero **NO DETERMINABLE — documento localizado pero ilegible** dentro del alcance de esa sesión: el mecanismo de descarga exige JavaScript de cliente que ocho variantes de réplica sin navegador no reprodujeron (§3 de la nota). Candidata **abierta, no agotada**.
- **Latinobarómetro** (ola 2024, cuestionario regional aplicado en México, n=1200, leído completo): **hallazgo positivo.** Ítem `P4NOIJ` — "Obediencia" entre 15 cualidades a inculcar en los niños — es **PROXY CON SUPUESTO DECLARADO**: mide orientación de valor hacia la deferencia/sumisión a la autoridad vía crianza declarada, no la conducta de iniciativa suprimida ante una jerarquía laboral o familiar concreta que `R2.1` describe. Tres ambigüedades declaradas y no resueltas (§4.4 de la nota): si quien valora la obediencia en sus hijos la practica él mismo ante su jefe o patriarca; si el valor se sostiene por convicción o por adaptación a un entorno donde desobedecer tiene costo; si "obediencia" en crianza transfiere al dominio laboral.
- **ENCUP:** portal INEGI confirmado, con más profundidad que Encargo C, como SPA sin pestaña de Microdatos/Tabulados y sin entrada en el catálogo RNM; el espejo `fomentocivico.segob.gob.mx` no completa el *handshake* TLS a través del proxy de este entorno. **NO DETERMINABLE — espejo fuera del sandbox.** Candidata **abierta, no agotada** (rutas de recuperación: descarga manual del autor, o una sesión futura con ese host habilitado).

**Veredicto de `deferencia` (G6):** ~~NO DETERMINABLE — no colapsable a límite permanente~~ → **PROXY CON SUPUESTO DECLARADO (parcial).** No se colapsa a "sin reactivo en el corpus alcanzable" (hay un reactivo, con supuesto declarado, en Latinobarómetro, fuente ya leída completa) ni a "límite permanente" (WVS y ENCUP siguen sin agotarse, por razones declaradas y distintas: la primera es un documento nombrado que un navegador real resolvería, la segunda tiene ruta de recuperación fuera del sandbox). Cascada: `modelo §1.1.E`, fila `H-07`, se corrige en el mismo acto (`revision-programa` no aplica — ese documento es histórico, no se reedita).

**Lo que esta enmienda NO decide.** Si el proxy `P4NOIJ` sustituye a un reactivo directo si WVS o ENCUP eventualmente se abren (ambas quedan como candidatas activas). Si el nivel de generalidad (valor normativo de crianza vs. conducta ante jerarquía laboral concreta) es aceptable para el peso del generador G6 — decisión de mesa separada, misma disciplina que (c). No reclasifica ningún número de `milpa/procedencia.yaml` (`deferencia` no tiene valor numérico asignado hoy, solo estado de reactivo).

→ **Vigente. S2.** *(Aprobado 3/ago/2026, decisión de mesa del autor sobre la propuesta de enmienda sin sellar de `forense/notas/2026-08-03-cbis-deferencia-externas.md` §7.)*

**Versión y nombre de archivo.** El número de versión **no sube** y el archivo **no se renombra** — mismo criterio que ADR-48/ADR-49/ADR-50. Excepción declarada a ADR-36, acotada a esta enmienda. **La fecha de la cabecera tampoco se actualiza**, mismo criterio: una enmienda in situ no renombra, no sube de versión y no refecha el archivo — es la lectura consistente de por qué la cabecera de este documento siguió diciendo "30 de julio de 2026" a través de ADR-48, ADR-49, ADR-50 y esta misma enmienda, todas aprobadas en fechas posteriores. Se sella aquí como convención explícita, no como hallazgo nuevo: satisface el punto pendiente sobre la cabecera de `gobernanza` sin inventar una tercera vía frente a las dos que la bitácora de versiones (§7) y ADR-36 ya sostienen (renombrar-y-resubir vs. enmendar-in-situ-sin-tocar-metadatos) — esta enmienda, como las tres anteriores, toma la segunda.

→ **Vigente. S2.** *(Aprobado 3/ago/2026, decisión de mesa del autor sobre los veredictos de P2 y C. Sesión de escritura, Sonnet, sin dato — perímetro `canon/`/`milpa/`/`forense/`, ADR-46; no abrió `data/raw/` ni `data/manifiesto.yaml`.)*

---

**ADR-52 · Dos decisiones de mesa del autor, 4/ago/2026, en un acto — sellan las dos propuestas que `PR #60` dejó sin sellar.**

**(A) La clase la determina el estado del reactivo, no su historia.** La clase abierta por `PR #60` ("reactivo declarado retirado, pendiente de localizar", n=1, `modelo:269`) se sustituye por un par con criterio explícito: **sin reactivo verificado — búsqueda abierta** / **sin reactivo — búsqueda cerrada (límite de régimen)**. Criterio: la clase no la determina qué le pasó al reactivo, sino si queda algún acto nombrado que pueda producirlo. Reasignación: `exposicion_violencia` → búsqueda abierta (reactivo retirado por `PR #57`, dos actos en curso — posición 4 rehecha sobre `TPer_Vic1`, barrido de alcanzabilidad ENDIREH/ENSU). `sens_estatus` → búsqueda abierta, sale de "sin reactivo o no determinable": su veredicto NO DETERMINABLE descansa en una limitación del inventario ("no se puede distinguir 'no reportado' de 'no existe'"), nadie fue a la fuente (`hitoE §17`) — no es límite verificado. `aversion_riesgo` → búsqueda cerrada: único con candidato examinado y descartado con argumento (ENIF `P5_23`/`P5_24` mide conocimiento de protección IPAB, moderador, no medida de aversión; `hitoE §17`). El reparto sigue cerrando en 14 (8 `MEDIDO·PARCIAL(x)` + 2 búsqueda abierta + 1 búsqueda cerrada + 3 proxy), derivado independientemente contra `modelo:267-273`, no copiado. **Condición de caducidad:** la búsqueda se cierra si tres actos nombrados vuelven sin reactivo; para `exposicion_violencia` dos ya existen y están en curso, el tercero se nombra cuando cierren; para `sens_estatus` no hay ninguno todavía — declarado así, sin inventar actos. Cascada ejecutada en el mismo acto: `modelo:267-273` (tabla y control de suma), `modelo:276` (nota de reparto), `modelo:723` (condensado §12).

**(B) `G1a` nombra su componente, no se desdobla.** Vuelve de mesa la pregunta que ADR-49 D3 dejó abierta y que `modelo:396` registró como "pre-registro corrió, resultado: no puede llegar en este régimen" (`PR #58`, sin dato nuevo). La recomendación de desdoblar en seis `ASIGNADO` (`forense/notas/2026-08-04-barrido-escritorio-pendientes.md` §2.b) queda **superada**: derivado contra archivo (`grep -n "PORQUE G1" canon/modelo-decision-v4_0.md`), solo **tres** reglas invocan `G1` — dos de dominio financiero que usan `confianza_institucional` (`dinero.ahorro.informal_sin_puente`/`con_puente_y_respaldo`, `dinero.ahorro.seguro_deposito_atenua_aversion`) y una que corre por `radio_confianza`, no por `confianza_institucional` (`cooperacion.confianza.puente_personal`). Seis `ASIGNADO` habrían creado cinco números que ninguna regla invoca. **Decisión: `confianza_institucional[dominio] −0.60` pasa a `confianza_institucional[financiera] −0.60`** (`modelo:388`). Regla que va con la decisión: si una regla de otro dominio invoca `G1`, esa regla nombra su componente; el coeficiente no vuelve a ser genérico. Por qué no es "sin dato, todo sigue igual": `[dominio]` era infalsable —se aplicaba "al componente que el dominio seleccione", siempre había uno donde encajaba—; nombrarlo lo vuelve refutable, mismo criterio de falsabilidad del Bloque A. Asimetría corregida: `modelo:396` decía que `G4` "no comparte este problema" por usar `confianza_institucional[justicia]`, componente nombrado — la diferencia era de redacción, no de evidencia; ambos generadores nombran componente ahora. **Lo que NO cambia:** el `−0.60` sigue `ASIGNADO`; nombrar el componente no lo mide. **Nota derivada,** verificada contra `milpa/procedencia.yaml`: `confianza_institucional[financiera]` está `MEDIDO·PARCIAL(formalidad,edad,urbanización,migración,ingreso)` desde la ola 1 de Fase B (ENIF `P11_1_1`-`P11_1_5`) — el coeficiente apunta hoy a una condicional con número detrás. Cascada ejecutada en el mismo acto: `modelo:388` (tabla de coeficientes), `modelo:396` (enmienda in situ sobre el párrafo de ADR-49 D3), este mismo ADR (párrafo (3) D3, arriba, corrección in situ). `milpa/procedencia.yaml:473` (`{gen: G1, coefs: {confianza_institucional: -0.60, ...}}`) usa la misma clave genérica, sin corchete, que la fila de `G4` en el mismo archivo — simetría preexistente, no una cita colgando de `[dominio]`; se deja sin tocar y se declara aquí.

**LO QUE NO SE HACE.** No mide nada ni mueve ningún contador de `milpa/procedencia.yaml` (ambos `−0.60` y `−0.35` siguen `ASIGNADO`). No toca P2 ni las adendas de `ADR-51`/`PR #62`. No desdobla `G1a`.

**Versión y nombre de archivo.** El número de versión **no sube** y el archivo **no se renombra** — mismo criterio que ADR-48/ADR-49/ADR-50/ADR-51. Excepción declarada a ADR-36, acotada a esta enmienda.

→ **Vigente. S2.** *(Aprobado 4/ago/2026, decisión de mesa del autor sobre las dos propuestas que `PR #60` dejó sin sellar y que `forense/notas/2026-08-04-barrido-escritorio-pendientes.md` §2 documentó con recomendación. Sesión de escritura, Sonnet, sin dato — perímetro `canon/`/`milpa/`/`forense/`, ADR-46; no abrió `data/raw/` ni microdato.)*

---

**ADR-53 · Se sella el veredicto D5 (INESTABLE) de P3, se descarta D6 con razón escrita, y se habilita — sin correrlo — el LCA multinivel condicional de `p3-lca-preregistro-v1_0.md` §2.5.** Decisión de mesa del autor, 4/ago/2026, sobre la tensión que `forense/notas/2026-08-04-p3-lca-segmentacion.md` §7 dejó planteada y explícitamente sin adjudicar entre la mecánica de decisión de `p3-lca-preregistro-v1_0.md` §3.3/§6.1 (que aterriza en D5 por la letra del protocolo) y la fenomenología de la curva de BIC (monótona decreciente en 1–8, mínimo en el borde, congruente con la fenomenología que la fila D6 describe). No re-corre el LCA: verifica las cifras citadas contra `forense/notas/_p3_lca/resultados.json` antes de sellar.

**(A) Veredicto: D5 — INESTABLE**, con las dos precisiones que el propio pre-registro exige y que no se pueden omitir. **Primera:** D5 **no es victoria del statu quo** — `p3-lca-preregistro-v1_0.md` §1 y §6.1·D5 son explícitos: *"el desenlace INESTABLE no cuenta como victoria de ninguna [hipótesis]... el statu quo no gana por empate"*. Los seis descriptores de `modelo` §1.1.D siguen sin respaldo estructural y sin refutar. **Segunda:** `modelo` §1.1.D **no se toca** — el pre-registro lo prohíbe bajo los seis desenlaces (§6.2.8), y D5 no es excepción. Nada más cambia en `canon/` por el veredicto en sí.

**(B) D6 se descarta, con su razón escrita — no se omite.** La condición literal de D6 se cumple: BIC estrictamente decreciente en `k=1..8`, mínimo en el borde `k=8` (`resultados.json → principal.tabla_k_1_8`, verificado). Lo que la descarta es el propio criterio del pre-registro (§1, H-C): *"cualquier mínimo interior de BIC en el rango [con solución estable]"* la falsa. **S1 produce ese mínimo interior**: quitando los tres ejes de hogar (I4/I5/I6/I7), el BIC de `S1_solo_persona.tabla_k_1_8` toca fondo en `k=4` (`BIC=962 340.94`) y sube después — diferencias sucesivas entre `k` consecutivos, re-derivadas de `resultados.json`: `+6 866.45 · +1 027.54 · +35.86 · −50.21 · −85.63 · −85.76 · −85.93`. Un gradiente no cambia de familia de curva porque se le quiten tres indicadores. La explicación no es post-hoc: `p3-lca-preregistro-v1_0.md` §3.6.3 y §2.5.b pre-declararon, antes de ver el dato, que la independencia local está violada por diseño (cuatro de siete indicadores son constantes dentro del hogar) y que su efecto conocido es sobreestimar el número de clases — S1 es la prueba de control que el propio pre-registro montó para detectarlo (§2.5.c), y salió positiva. **El límite del argumento, declarado y no escondido:** el pre-registro exige mínimo interior "con solución estable" (§1, falsador de H-C), y el de S1 no lo es — la solución primaria de S1 tras la regla de no-separación (`k_primario=3`, `decision_Sec3_3.k_primario_tras_regla_no_separacion`) replica solo **1 de 50** arranques (`S1_solo_persona.tabla_k_1_8."3".n_replican_mejor_logL`), y el propio mínimo interior bruto en `k=4` dista solo **35.86** puntos de `k=3` y **50.21** de `k=5`, sobre un rango total de BIC de **7 929.85** (`S1_solo_persona.decision_Sec3_3.rango_BIC_observado_1_8`). S1 no demuestra que existan clases: demuestra que la forma de la curva depende de los ejes de hogar, y eso basta para que el descenso monótono del ajuste principal deje de ser evidencia limpia de gradiente. Ambas lecturas —H-A vía clases, H-C vía gradiente— quedan sin soporte estable: eso es, literalmente, D5, no D6. **Costo declarado, sin adorno:** D6 habría sido confirmación empírica del reencuadre perfiles→atributos (`modelo` §1.1.B). Bajo D5 el reencuadre sigue justificado por diseño, no por dato — se dice aquí para que nadie lo lea como validado por esta corrida.

**(C) LCA multinivel — HABILITADO, no corrido.** `p3-lca-preregistro-v1_0.md` §2.5 lo declara "la extensión correcta", pre-registrada como condicional: *"se corre si y sólo si S1 muestra que los ejes de hogar dominan la solución"* (§6·D5·nota). S1 lo mostró — ver (B). Queda habilitado con tres condiciones, parte de la activación y no letra pequeña: **(1)** es un análisis nuevo y necesita enmienda fechada al pre-registro (`p3-lca-preregistro-v1_0.md` §10), no una edición del cuerpo sellado; **(2)** necesita pre-registro propio, escrito por una sesión limpia que no haya abierto ENIGH ni leído los resultados de esta corrida (mismo criterio de contaminación por sesión, ADR-46); **(3)** no se corre ni se pre-registra en este acto.

**LO QUE NO SE HACE.** No toca `modelo` §1.1.D ni sus seis descriptores, bajo ningún concepto (§6.2.8). No enmienda el cuerpo sellado de `p3-lca-preregistro-v1_0.md` — la enmienda de la condición (C.1) queda pendiente de la sesión que la ejecute. No corre ni pre-registra el LCA multinivel. No mueve `8 de 14`, `4 de 144` ni ningún contador del Hito D.

**Por qué ADR y no solo enmienda.** El pre-registro exige ADR para D1/D2/D4/D6 (`p3-lca-preregistro-v1_0.md` §6.1) y no dice nada de D5 — D5 en sí es "nada cambia en canon". Lo que este acto registra no es el desenlace mecánico (D5, ya derivado en Fase B): es una decisión de mesa sobre una tensión que el propio protocolo dejó abierta sin regla de precedencia (D5 mecánico vs. fenomenología D6) más la activación de una extensión condicional nueva — mismo tipo de acto que ADR-47 (deslinde metodológico falsar/calibrar) y ADR-51(c)/(d) (veredictos de mesa sobre resultados de encargo, cierre de `D-12`). Se registra como ADR por esa razón, no porque D5 lo exija.

**Requisito de salida.** `canon/estado-programa-v1_9.md` §3·L5 registra el veredicto D5 de P3 y el descarte de D6, mismo molde que los veredictos de Hito D (`R1.1`→`D`, `R3.2`→`B`), citando este ADR. `forense/notas/2026-08-04-p3-lca-segmentacion.md` recibe adenda fechada (append-only, sin editar el cuerpo) registrando esta decisión y su número de ADR. `forense/hallazgos.md` registra la segunda ocurrencia del día de un pre-registro con desenlaces solapados y sin regla de precedencia entre filas (D5/D6 en P3, A/D en R7.2).

**Versión y nombre de archivo.** El número de versión **no sube** y el archivo **no se renombra** — mismo criterio que ADR-48/ADR-49/ADR-50/ADR-51/ADR-52. Excepción declarada a ADR-36, acotada a esta enmienda.

→ **Vigente. S2.** *(Aprobado 4/ago/2026, decisión de mesa del autor sobre el veredicto de Fase B de P3 (`forense/notas/2026-08-04-p3-lca-segmentacion.md`, PR #66/#67 fusionados) y sobre `p3-lca-preregistro-v1_0.md` §6.1/§2.5. Sesión de escritura, sin dato — perímetro `canon/`/`forense/`, ADR-46; no abrió `data/raw/` ni microdato.)*

---

**ADR-54 · `sens_estatus` pasa de "sin reactivo verificado — búsqueda abierta" a "sin reactivo — búsqueda cerrada (límite de régimen)".** Decisión de mesa, 4/ago/2026, sobre el examen de descriptor de `PR #64` (`forense/notas/2026-08-04-sens-estatus-examen-descriptor.md`).

**Fundamento.** El examen recorrió los cinco instrumentos permitidos del régimen y **ninguno sirve**, fundado en descriptor literal, no en supuesto — exactamente lo que a la clasificación de ADR-52 le faltaba para `sens_estatus` ("nadie fue a la fuente"; en este examen, alguien fue, a las cinco). **Vía de cierre: el precedente de `aversion_riesgo` en ADR-52 A** (candidato examinado y descartado con argumento), **no** la caducidad de tres actos (ADR-52 A), que queda intacta como criterio para las demás búsquedas abiertas. **La pregunta pendiente sobre esa caducidad** —¿un acto bloqueado por entorno gasta vida?— **no se decide aquí y sigue en mesa**.

**Reparto, re-derivado contra la tabla, no copiado.** Sigue cerrando en **14**, ahora **8+1+2+3** (8 `MEDIDO·PARCIAL(x)` + 1 búsqueda abierta [`exposicion_violencia`] + 2 búsqueda cerrada [`aversion_riesgo`, `sens_estatus`] + 3 proxy). Cascada ejecutada en el mismo acto: `modelo:269-270` (tabla y control de suma), `modelo:278` (nota de reparto), `modelo:725` (condensado §12).

**LO QUE NO SE HACE.** No adjudica la condición de caducidad ante bloqueo de entorno (queda en mesa, sin tocar). No mide nada ni mueve ningún contador de `milpa/procedencia.yaml`. No re-congela `tests/baseline.json` (protocolo §4.2 — no se congela en la misma corrida que cambió el medidor; encargo aparte).

**Versión y nombre de archivo.** El número de versión **no sube** y el archivo **no se renombra** — mismo criterio que ADR-48/ADR-49/ADR-50/ADR-51/ADR-52/ADR-53. Excepción declarada a ADR-36, acotada a esta enmienda.

→ **Vigente. S2.** *(Aprobado 4/ago/2026, decisión de mesa sobre el examen de descriptor de `PR #64` (`forense/notas/2026-08-04-sens-estatus-examen-descriptor.md`). Sesión de canon, sin dato — perímetro `canon/`/`forense/`, ADR-46; no abrió `data/raw/` ni microdato.)*

---

**ADR-55 · Propaga `R4.2` → veredicto `D` al bloque append-only de `hitoD-preregistro` (3 de 27 → 4 de 27). `R5.2` y `R1.2` corren completas en el mismo acto y no se archivan.** Encargo Y, mesa #19, 4/ago/2026, sobre las tres únicas fichas del perímetro con Umbral completo, escala propia completa y fuente nombrada con payload en disco (`R5.2`/ENUT, `R4.2`/ENSANUT, `R1.2`/ENIF — verificado por mesa contra `hitoD-preregistro-v2_0.md`).

**`R4.2` → `D`, archivado.** El chequeo barato que la propia ficha ordena primero (línea 120) determina que ENSANUT 2024 no tiene pregunta de "sin permiso laboral para atender su salud" ni pregunta dedicada de posposición de chequeo — verificado contra cuestionario y catálogos, no supuesto. Mismo tipo de veredicto D que `R1.1`/`R7.2`: hueco de instrumento, no de mercado ni de muestra. Detalle: `forense/notas/2026-08-04-y4-veredicto-r4-2.md`, narrado en Nota 17 de `hitoD-preregistro`.

**`R5.2` (ENUT 2024) corre completa, no se archiva.** Reducción de horas de cuidado al pasar de no-ocupada a ocupada-formal-tiempo-completo con varón adulto disponible: 23.98% (IC95% [14.39%, 33.57%]). El punto satisface la fila `A` de la escala propia; el IC95% no despeja el umbral de 20% — a diferencia de la corrida limpia de `R7.2` (ADR-45), aquí punto e intervalo dan lecturas distintas. **Propuesta `A`, con reserva estadística — mismo criterio que Nota 16/`R5.1`: se propone, mesa adjudica.** Detalle: `forense/notas/2026-08-04-y5-veredicto-r5-2.md`, Nota 18.

**`R1.2` (ENIF 2024) corre completa, no se archiva — hallazgo de diseño del pre-registro, no de dato.** CONSAR verificado innecesario (Umbral evaluable con ENIF sola). El falsador no se satisface: 42.98% de formales-estables-con-ingreso-suficiente aportan voluntariamente a Afore o tienen seguro privado (IC95% [39.88%, 46.08%]), muy por encima del `<15%` que el Umbral exige — no es un caso límite. **Ninguna fila de la escala propia (`A`-`D`) nombra "el falsador corrió limpio y no se satisfizo, la regla sobrevive"**: `A`/`B` exigen tasas bajas (no ocurrió), `C` describe un diseño más fino innecesario, `D` predecía un hueco de dato que no ocurrió. Mismo hueco de diseño que `R5.2` en este mismo acto — declarado como patrón, sin inventar fila nueva ni tocar la ficha. Detalle: `forense/notas/2026-08-04-y6-veredicto-r1-2.md`, Nota 19.

**Verificado en las tres, antes de adjudicar: sin solape de filas.** El programa lleva dos ocurrencias de pre-registros con desenlaces solapados sin regla de precedencia (`D5`/`D6` de P3, ADR-53; `A`/`D` de `R7.2`, Nota 12) — una tercera hubiera obligado a instrumentar precedencia al sellar. No ocurrió: en `R4.2` ninguna otra fila se satisface junto con `D`; en `R5.2` la fila `B` exige un control que este acto sí incluye, así que no se satisface junto con `A`; en `R1.2` `A`/`B` exigen tasas bajas que no aparecieron.

**LO QUE NO SE HACE.** No se adjudica `R5.2` ni `R1.2` — quedan propuestas, igual que `R5.1` (Nota 16). No se toca ninguna ficha, tier, ni generador. No se propone fila para `R1.3`, `R4.1`, `R4.3`, `R8.3`, `R9.1` ni `R9.2` (fuera de perímetro de este acto, decisión de mesa aparte).

**Cascada ejecutada en el mismo acto:** `estado-programa:93,97,120,194` (contadores `4 de 27`/`23 de 27`/`45 de 49` y ADR); `modelo-decision:64,636,637,821` (contador y referencia cruzada "de arriba"); `gobernanza:354,585` (este documento). Conteo de ADR verificado por `grep -oE "ADR-[0-9]+" canon/gobernanza-v1_15.md | sort -u | wc -l` = 55.

**Versión y nombre de archivo.** El número de versión **no sube** y el archivo **no se renombra** — mismo criterio que ADR-48 a ADR-54. Excepción declarada a ADR-36, acotada a esta enmienda.

→ **Vigente. S2.** *(Aprobado 4/ago/2026, Encargo Y, mesa #19. Sesión con dato — abrió `data/raw/enut2024_bd_csv.zip`, `enif2024_csv.zip`, y catálogos de ENSANUT en raíz `descargas_mx`; no tocó `milpa/procedencia.yaml` ni ningún generador.)*

---

**ADR-56 · Propaga `R4.1`, `R9.1`, `R4.3` (mitad A y mitad B) y `R9.2` → veredicto `D` al bloque append-only de `hitoD-preregistro` (4 de 27 → 8 de 27).** Encargo Z, mesa #19, 4/ago/2026, sobre las cuatro únicas fichas del perímetro con Umbral completo y escala propia completa que nunca habían declarado fuente (`forense/cruce-catalogo-fichas-v1_0.md`, candidatura ENSANUT verificada, no copiada). Fuente declarada para las cuatro: ENSANUT CONTINUA 2024, verificada contra los cuestionarios reales (Hogar, Utilizadores, Adultos), no contra el nombre de una variable — mismo estándar que `R4.2` (ADR-55).

**`R4.1` → `D` y `R9.1` → `D`, archivadas juntas por la Nota de simetría de la propia ficha de `R9.1` (`hitoD-preregistro:258`).** `R4.1`: ninguna fuente del catálogo construye la comparación antes/después que el Umbral exige — ENSANUT y ENIGH son ambas corte transversal, sin panel de acceso a salud anclado a un evento fechado. `R9.1`: sin variable de distancia en km (solo tiempo de traslado) y con exclusión estructural, por diseño del Cuestionario de Utilizadores, de quien no consultó a nadie. Fallan por mecanismos distintos, mismo desenlace — no se colapsan en un registro. Detalle: `forense/notas/2026-08-04-z1-declaracion-fuente-r4-1-r9-1.md`, `forense/notas/2026-08-04-z4-veredicto-r4-1-r9-1.md`, narrado en Nota 20/Nota 23 de `hitoD-preregistro`.

**`R4.3` mitad A → `D` y mitad B → `D`, archivadas por separado (regla del perímetro de la propia ficha: "dos falsadores, uno por mitad").** Ambas caen por la misma fila compartida de la ficha (línea 132: "si solo hay adherencia auto-reportada") — la única variable de adherencia en el catálogo entero (`A0313`, ENSANUT) es por entrevista, no por surtimiento de farmacia. Mitad B, además, sin variable de cuidadora: el único proxy disponible (corresidencia) es el confusor socioeconómico que la propia ficha ya advierte (línea 130) — no se corrió sin control, por diseño, no por omisión. Detalle: `forense/notas/2026-08-04-z2-declaracion-fuente-r4-3.md`, `forense/notas/2026-08-04-z5-veredicto-r4-3.md`, Nota 21/Nota 24.

**`R9.2` → `D`, meta-regla del corpus, tratada con el cuidado de su Bloque C.** El Umbral exige cobertura baja **y** abasto/campaña verificados por tercero; la segunda condición no tiene ninguna fuente en el catálogo completo (119 fuentes revisadas) — la única disponible, DGIS (`data/catalogo-fuentes-v1_0.md:123`), es el propio prestador, excluido por la ficha misma. Anti-superviviente (línea 270) cumplido a nivel de instrumento, no de caso: no existe ningún caso posible donde correr cobertura produjera `A` sin la pieza de abasto auditado, así que no se abrió microdato para buscarlo — el descarte se archiva aquí, con motivo (ADR-29.b). **Este `D` es ausencia determinable de instrumento auditor, no evidencia de que el hueco de cobertura sea actitudinal — la meta-regla del corpus no cae por este veredicto.** Detalle: `forense/notas/2026-08-04-z3-declaracion-fuente-r9-2.md`, `forense/notas/2026-08-04-z6-veredicto-r9-2.md`, Nota 22/Nota 25.

**Verificado en las cuatro, antes de adjudicar: sin solape de filas.** El programa lleva dos ocurrencias de pre-registros con desenlaces solapados sin regla de precedencia (`D5`/`D6` de P3, ADR-53; `A`/`D` de `R7.2`, Nota 12) — una tercera hubiera obligado a instrumentar precedencia al sellar. No ocurrió: en ninguna de las cuatro fichas otra fila se satisface junto con `D` (verificado ficha por ficha en cada nota de commit 2).

**LO QUE NO SE HACE.** No se toca el cuerpo de ninguna ficha, tier, ni generador. No se propone fila para ninguna otra ficha del perímetro sin fuente declarada — quedaba fuera del alcance de este acto (decisión de mesa aparte, mismo criterio que ADR-55).

**Cascada ejecutada en el mismo acto:** `estado-programa:93,97,120,194` (contadores `8 de 27`/`19 de 27`/`41 de 49` y ADR); `modelo-decision:64,636,637,821` (contador y referencia cruzada "de arriba"); `gobernanza:358,609` (este documento). Conteo de ADR verificado por `grep -oE "ADR-[0-9]+" canon/gobernanza-v1_15.md | sort -u | wc -l` = 56.

**Versión y nombre de archivo.** El número de versión **no sube** y el archivo **no se renombra** — mismo criterio que ADR-48 a ADR-55. Excepción declarada a ADR-36, acotada a esta enmienda.

→ **Vigente. S2.** *(Aprobado 4/ago/2026, Encargo Z, mesa #19. Sesión con dato parcial — abrió los cuestionarios PDF de ENSANUT CONTINUA 2024 (Hogar, Utilizadores, Adultos, Niños, Adolescentes) en raíz `descargas_mx`; no abrió ningún ZIP de microdato de fila-por-persona ni tocó `milpa/procedencia.yaml` ni ningún generador.)*

---

**ADR-57 · Asociar ≠ identificar: los tres β̂ marginales no representan coeficientes de generador; compuerta de identificación sobre las afirmaciones de intervención; el reactivo genérico de confianza en servidores públicos se registra como escala propia fuera del vector.** Decisión de mesa del autor, 4/ago/2026, sobre el veredicto del Encargo X (`forense/notas/2026-08-04-x-condicionamiento-y-forma.md` §4-§6). Completa la tríada de distinciones del programa: falsar ≠ calibrar (ADR-47), calibrar por ajuste ≠ identificar (ADR-50/51), y ahora asociar ≠ identificar.

**(a) Qué estableció X, y qué clase de objeto son los tres β̂.** Ninguno de los tres coeficientes estimados por el Encargo W sobrevive al condicionamiento sin matiz (X §6): `radio_confianza` — 28 de 39 celdas con signo opuesto al marginal, las 12 distinguibles de cero todas positivas; `confianza_institucional` (genérico) — reversión completa y significativa en las cuatro celdas del único eje disponible; `familismo_apoyo` — inversión con significancia en las celdas mayoritarias de tres de seis ejes, amplificación en las minoritarias. Los tres quedan rotulados, por decisión de este ADR: asociaciones marginales, correctamente calculadas, que no representan una propiedad estable de la relación θ→desenlace. Consecuencia nueva que este ADR sella: la concordancia direccional de un marginal con un valor `ASIGNADO` no cuenta como corroboración del asignado — los tres marginales de W concordaban en signo con sus asignados (−0.35→−0.01 · −0.60→−0.065 · +0.20→+0.028) y las tres concordancias resultaron compatibles con composición. Corolario simétrico, decidido aquí con la misma evidencia (X §4: el eje único de W2 no permite distinguir confusor de colisionador): el condicionado tampoco se promueve — "el verdadero β es el de las celdas" queda tan prohibido como "el marginal aguanta"; un condicionamiento discordante establece no-robustez del marginal y nada más. Las lecturas en tensión de X §5.2 (curva marginal de W2 vs. reversión por edad) quedan en tensión, sin adjudicar, hasta que exista un segundo eje estricto para ENCIG o un argumento de identificación.

**(b) Heterogeneidad como evidencia, no como ruido.** El patrón de `familismo_apoyo` (signo negativo en las celdas mayoritarias formal / 100k+ / no-migrante / con-smartphone; positivo y mayor en las minoritarias correspondientes) se registra como el primer soporte empírico propio de la segmentación al nivel de coeficiente — la tesis del Bloque A apareciendo en microdato. Un escalar único por generador es, a la luz de W3, menos segmentado que la filosofía del propio modelo; esto alimenta, sin sellarla aquí, la línea "motor como contexto".

**(c) COMPUERTA DE IDENTIFICACIÓN — sobre la clase de afirmación, no sobre el motor.** ADR-50/51 sobreviven íntegros: el motor se calibra por ajuste, MILPA sigue siendo la arquitectura objetivo, y sus parámetros necesitan reproducir conducta observada, no contrafactuales individuales. Lo que este inciso gobierna es qué puede afirmar cualquier artefacto o salida del programa: reproducir, describir y segmentar — siempre, con sus tiers y bandas; afirmaciones de intervención ("si se interviene X, pasa Y") — solo donde exista una llave de identificación declarada y sellada, de una de tres clases: (i) panel con el desenlace en el instrumento (mismos sujetos entre olas); (ii) experimento natural con grupo de comparación sobre encuestas repetidas; (iii) diseño experimental de terceros (evaluaciones aleatorizadas publicadas, clase Progresa/Oportunidades), usado como evidencia (a) con su cita. La lista corta inicial, con su estado verificado a la fecha de este ADR: ENNViH/MxFLS — panel de tres olas, dominio público; ruta viva vía `CAL-G3` (Fase C desbloqueada, olas 2-3, alcance descriptivo; la promoción de descriptivo a identificado exige su propio diseño intra-persona, no está concedida aquí). ENASEM + pensión Bienestar — panel 50+ con identificador de persona entre rondas más el experimento natural del programa; paso 1 ya corrido (Encargo S). ENOE — su panel rotativo queda refutado como ruta de conducta financiera (`CAL-ENOE` Fase A, 31/jul: el instrumento no trae reactivo de ahorro/crédito/deuda/planeación); permanece elegible únicamente como portador de desenlaces laborales para experimentos naturales (p. ej. salario mínimo de franja fronteriza). Ninguna llave de esta lista está ejercida: cada una exige su pre-registro propio. La compuerta abre por palanca, no en bloque — una llave sellada habilita afirmaciones de intervención solo sobre la relación que identifica.

**(d) Enmienda al `milpa-whitepaper`, in situ y declarada.** El §0 del whitepaper ("pasar de explicar conducta a ensayar intervenciones") y el §6.2 ("más confiable en dirección y orden de magnitud") quedan leídos bajo (c): el Encargo X mostró que la dirección misma no es estable al condicionar en 3 de 3 coeficientes probados, así que la salvaguarda de §6.2 no basta por sí sola. El whitepaper recibe una nota fechada al frente que remite a este ADR, sin subir versión — excepción a ADR-36 declarada aquí, mismo patrón que ADR-48 usó con protocolo (la renumeración obligaría a retropropagar citas; es exactamente la clase de trabajo que R0 corta). La reescritura completa queda para la próxima versión natural del whitepaper.

**(e) ESCALA PROPIA para el genérico de servidores públicos.** El reactivo ENCIG batería XI ítem 23 (`P11_1_23`, "servidores(as) públicos(as) o empleados(as) de gobierno") no mapea a ninguno de los seis componentes del vector (verificado por pos8: los 6 se resuelven con los ítems 1/2/3/14/16/17/19/20/21/22; financiera NO CUBIERTO en ENCIG). Se registra como `confianza_institucional[genérico-servidores-públicos]`, sección propia en `milpa/procedencia.yaml`, fuera del vector de ADR-28.b y fuera del denominador `D` — mismo molde que `condicionales_escalares` declara para lo que no es componente del vector. No se funde con justicia-policía ni con ningún otro componente (coherente con `modelo:309`, "no sobre un promedio", y con el patrón de ADR-52 B de nombrar componente por familia de reglas): las reglas de `modelo` §3.3 (trámite/mordida) que hoy invocan la confianza genérica nombran esta escala. `D` sigue derivándose de §1.1.F y este ADR no lo mueve; si una mesa futura quiere promover el genérico a componente, eso es cambio MAYOR con su propia derivación. Cierra la pregunta que el Encargo W §1.2 dejó declarada ("si ambas rutas deberían fundirse en un solo número"): no se funden.

**Reversión.** (c) se revierte por palanca: cada afirmación de intervención se habilita cuando su llave quede sellada con pre-registro propio. (a) se revierte para un coeficiente concreto si un diseño de identificación sellado produce un estimando con esa lectura. (e) se revierte solo por decisión MAYOR sobre el vector.

**Versión y nombre de archivo.** El número de versión **no sube** y el archivo **no se renombra** — mismo criterio que ADR-48 a ADR-56. Excepción declarada a ADR-36, acotada a esta enmienda.

→ **Vigente. S2.** *(Aprobado 4/ago/2026, decisión de mesa del autor sobre el veredicto del Encargo X. Sesión de escritura, sin dato — perímetro declarado en el propio encargo: `canon/gobernanza-v1_15.md`, `milpa/milpa-whitepaper-v0_1.md` (solo nota fechada al frente), `milpa/procedencia.yaml`, `forense/hallazgos.md` si aplica; no abrió `data/raw/` ni microdato.)*

---

**ADR-58 · La escala de falsación gana su fila de corroboración (fila `E`) y su regla de precedencia; se adjudican `R5.1` y `R5.2` → `A` con reserva estadística escrita, y `R1.2` → `E` como excepción acotada (MESA-R12=SÍ).** Decisión de mesa del autor, 4/ago/2026, sobre el patrón que dos de tres fichas del Encargo Y tropezaron el mismo día (Notas 18 y 19 de `hitoD-preregistro`): la escala `A/B/C/D` no tiene dónde anotar ni "el falsador corrió limpio y no refutó" ni "el punto satisface la fila pero el IC no despeja el umbral". Un registro que solo puede anotar refutaciones e inejecutables describe el estado de validación del modelo sesgado hacia abajo.

**(a) Fila `E`, prospectiva, en la plantilla de fichas.** Toda ficha futura declara, al sellar y antes de ver dato alguno: *"`E` · El falsador corrió limpio y no se satisfizo — la regla sobrevive a esta prueba"*, junto con qué significa ese desenlace para esa regla — corrobora (y qué corroboraría, dicho por adelantado), acota (a qué subpoblación o régimen), o falsador débil (la prueba no podía decir nada, y entonces la ficha era mejorable). El precedente negativo es `R1.2`/Nota 19: resultado a casi el triple del umbral, ejecutor negándose con razón a forzar una fila — ese desenlace ahora tiene casilla. Se materializa como adenda fechada a la plantilla en `hitoD-preregistro` (Nota 26, append-only: ni el cuerpo ni las fichas selladas se tocan).

**(b) Regla de precedencia, declarada al sellar.** Si dos filas de una escala pueden satisfacerse a la vez, la ficha declara cuál manda al sellar, no después; la fila más específica gana sobre la más general, y la escala de la ficha gobierna sobre cualquier leyenda genérica del corpus. El precedente es `R5.2`/Nota 18, que tuvo que verificar a mano la no-superposición con `B` porque ninguna regla lo decía.

**(c) Adjudicación de `R5.1` y `R5.2` → `A`, con la reserva en la línea.** Ambas corridas satisfacen la fila `A` de su ficha con IC95% que no despeja el umbral; ambos ejecutores se abstuvieron y dejaron la propuesta escrita — dos abstenciones razonadas, sin que nadie las pidiera, esperando mesa (transfer 4/ago §5.2). Mesa adjudica: el punto manda la fila, la reserva viaja con el veredicto, verbatim, en la línea del bloque append-only — no en una nota aparte que alguien pueda no leer. Un `A` con reserva escrita informa más y es más honesto que una abstención perpetua: el registro existe para describir el estado de validación, no para acumular limbo.

**(d) [MESA-R12=SÍ] Adjudicación de `R1.2` → `E`, con la excepción declarada.** La fila `E` es prospectiva por diseño; aplicarla a `R1.2` es una excepción acotada y con razón escrita: la corrida está completa, el desenlace es inequívoco y quedó archivado en Nota 19 antes de que esta fila existiera — no hay reinterpretación posible, solo una casilla que faltaba para un resultado ya escrito. La regla `dinero.planeacion.formal_estable` (id verificado contra `modelo-decision-v4_0.md:467`; el borrador de este ADR citaba `dinero.retiro.formal_insuficiente`, id que no existe en el motor — corregido aquí antes de sellar, no heredado del encargo) queda sobreviviente de su prueba, con el significado que la propia Nota 19 permite leer: el falsador era ejecutable, corrió limpio, y el cruce medido (42.98%, IC95% [39.88%, 46.08%] contra umbral `<15%`) no se acercó. Ninguna otra ficha corrida recibe fila `E` retroactiva por este ADR: la excepción es de una, nombrada, y no constituye precedente automático.

**(e) Cascada de contadores, una sola vez, perímetro acotado por el propio encargo.** El contador de corridas archivadas del bloque append-only de `hitoD-preregistro` pasa de **8 a 11** fichas (+`R5.1`, +`R5.2`, +`R1.2`). Se actualiza únicamente donde un test lo vigile o el canon lo declare vigente, derivado leyendo `tests/check.py` (T18, ~líneas 678-760: el parser del bloque compara contra el contador declarado en `estado-programa:194`, "Paso 2 — EN CURSO. N de 27 corrida"). El parser mismo se extiende en este acto: su regex canónico solo aceptaba `[A-D]` — verificado corriéndolo contra una línea existente del bloque antes de escribir nada — y sin extenderlo a `[A-E]` la línea `R1.2` → veredicto `E` quedaría invisible para T18, y 11 declarado no cuadraría contra 10 real. Extensión hecha en `tests/check.py` (líneas 678, 682, 704), dentro del perímetro de este acto ("el archivo del contador que la premisa 3 localice"). Fuera de perímetro de este acto, y por tanto NO tocadas — quedan en su valor anterior hasta el próximo acto que las toque, hallazgo registrado en `forense/hallazgos.md`: `estado-programa:93,120` (narrativas L5/S3 "8 de 27"/"19 de 27") y `gobernanza:358,649` (mismas cifras, este documento). Las menciones históricas ("4 de 27 → 8 de 27" en ADR-55/56/57 y cadenas) son historia correcta cuando se escribió y no se tocan. ⚠️ **Deuda cerrada por ADR-60 (ENCARGO M-1, 4/ago/2026).** Las dos citas nombradas arriba (`gobernanza:358` y la fila equivalente de §5, entonces `:649`) se actualizaron en el mismo acto que adjudicó `R3.1`→`B`, junto con `estado-programa:93,97,120,194` y `modelo-decision:64,636,637,821` — perímetro de cascada completa, no acotado esta vez. Esta nota no se borra: queda como registro de que la deuda existió tres adjudicaciones (tres commits de cascada acotada, ADR-55/56/58) antes de cerrarse.

**Reversión.** Una adjudicación de este ADR se revierte solo por corrida nueva pre-registrada de la misma regla, nunca por relectura. La fila `E` de una ficha futura se gobierna por lo que esa ficha declaró al sellar.

**Versión y nombre de archivo.** El número de versión **no sube** y el archivo **no se renombra** — mismo criterio que ADR-48 a ADR-57. Excepción declarada a ADR-36, acotada a esta enmienda.

→ **Vigente. S2.** *(Aprobado 4/ago/2026, decisión de mesa del autor. Sesión de escritura, sin dato — perímetro declarado en el propio encargo: `canon/gobernanza-v1_15.md`, `forense/hitoD-preregistro-v2_0.md` (adenda + bloque append-only, nada más), `canon/estado-programa-v1_9.md` (solo rider: contador Paso 2 y cadena de ADR/L0), `tests/check.py` (parser del bloque, extensión de rango de letra), `forense/hallazgos.md` si aplica; no abrió `data/raw/` ni microdato. MESA-R12=SÍ.)*

---

**ADR-59 · Se incorpora `instrucciones-proyecto-v2_4.md` verbatim; ADR-58(b) gobierna íntegro sobre la regla de precedencia de Bloque B-bis, y se corrige la firma de entorno universalizada por error en la plantilla de arranque de Bloque D.** Decisión de mesa del autor, 4/ago/2026: v2.4 sube sin enmendar — es la versión que gobernó los actos de hoy, y una copia que difiera de la que gobernó sería peor que un defecto documentado. Este ADR es el lugar de reconciliación para dos defectos que la lectura de v2.4 contra el repo real expuso; el texto de v2.4 no se toca.

**(a) La regla de precedencia de Bloque B-bis no desplaza a ADR-58(b) — lo asume sin citarlo.** v2.4 §Bloque B-bis cierra con: *"Y la regla de precedencia. Si dos filas de una escala pueden satisfacerse a la vez, se declara cuál manda, al sellar y no después. La escala de la ficha gobierna sobre cualquier legend genérico, y hay que decirlo en la ficha."* Eso obliga a declarar la precedencia al sellar — hacia adelante — pero no dice cuál gana cuando la ficha no lo declaró: no trae default. ADR-58(b) ya selló ese default: *"la fila más específica gana sobre la más general, y la escala de la ficha gobierna sobre cualquier leyenda genérica del corpus."* (La propia paráfrasis de B-bis — "legend genérico", sin concordancia de género con "leyenda" — es indicio de que restituye ADR-58(b) de memoria, no que lo reemplaza.) Se declara: ADR-58(b) sigue vigente e íntegro como default de precedencia; B-bis es el requisito procesal de declararlo explícito en fichas nuevas al sellar, no una regla nueva ni un reemplazo. Ninguna ficha, sellada antes o después de v2.4, necesita elegir entre los dos textos.

**(b) La plantilla de arranque (Bloque D) universaliza mal la firma de entorno; se corrige aquí, no en v2.4.** El punto 4 del bloque ARRANQUE escribe `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE → esperado: sin_variable` bajo un encabezado que ordena "si algo no cuadra, PARA y repórtalo", sin acotar de qué entorno es firma esa expectativa. Verificado en acto propio, 4/ago/2026, en un acto asignado a nube: `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=cloud_default` es el valor real del entorno, y el propio punto 4 ya exime la sonda de red cuando el acto no la necesita — el defecto no es la sonda, es tratar `sin_variable` como constante universal del bloque en vez de como una firma más, específica de un entorno. Se declara: `sin_variable` + sonda 200 contra INEGI es la firma del entorno Ubuntu-con-red; `cloud_default` sin sonda es la firma correcta de un acto de nube. Ninguna de las dos es, por sí sola, un desajuste — el desajuste sería una firma que no corresponde al entorno que el encargo asignó. No se enmienda v2.4 (llega y queda verbatim, como todo este ADR): la corrección vive aquí, igual que en (a). Queda en el backlog de v2.5, junto con la cuarta variante de alcanzabilidad, para cuando se reescriba Bloque D.

**Convención de fecha.** Las fechas de los ADR se registran en el huso horario local del repo (UTC-6), no en el del entorno de ejecución: un entorno que corre en UTC cruza de día a partir de las 18:00 hora local, y le pone a un acto la fecha de mañana. Esta misma entrada llevaba `5/ago/2026` por esa razón — el commit original quedó en `2026-08-05T00:58:13+00:00`, que en el huso del repo es 4/ago 18:58, media hora después de PR #100 (18:28 -0600) — corregido a `4/ago/2026` en sus tres apariciones antes de fusionar. Ningún test lo vigila.

**Versión y nombre de archivo.** El número de versión de `gobernanza` **no sube** y el archivo **no se renombra** — mismo criterio que ADR-48 a ADR-58. `instrucciones-proyecto-v2_4.md` es archivo nuevo: no reemplaza ni borra `instrucciones-proyecto-v2.md`, que conserva citas vivas.

→ **Vigente. S2.** *(Aprobado 4/ago/2026, decisión de mesa del autor. Sesión de escritura, sin dato — perímetro declarado en el propio encargo: `instrucciones-proyecto-v2_4.md` (nuevo, verbatim, sha256 verificado contra el valor que dio mesa), `canon/gobernanza-v1_15.md` (este ADR + cascada de cabecera), `canon/estado-programa-v1_10.md` (cascada del contador de ADR); no abrió `data/raw/` ni microdato.)*

---

**ADR-60 · Se adjudica `R3.1` → veredicto `B` con sus cuatro razones y la resolución de la contradicción textual 762/784; un veredicto `B` no retiquetea el tier del motor; se declaran tres límites sin resolver; se registra la convergencia `R3.1`/`W1`; `G1·radio_confianza` pasa a `ASIGNADO · SIGNO BAJO PRUEBA`; se prueba y descarta la hipótesis del desdoble de `radio_confianza`.** Decisión de mesa del autor, ENCARGO M-1, 4/ago/2026, sobre la corrida completa de `R3.1` contra ENCIG 2023 (`forense/hitoD-preregistro-v2_0.md` Nota 27; especificación congelada `forense/hitoD-R3_1-especificacion-v1_0.md`; propuesta `forense/hitoD-R3_1-veredicto-v1_0.md`) y sobre la deuda de cascada que ADR-58(e) dejó nombrada para "el próximo acto que abra `estado`/`gobernanza`/`modelo` con perímetro de cascada completa".

**(a) `R3.1` → `B`, con sus cuatro razones.** Dirección confirmada en 6 de 6 cómputos (dos interpretaciones de `P8_4` × sin ponderar/`FAC_TRA`/`FAC_P18`), brecha 9.28pp–32.73pp, sin traslape de IC95% en ninguno de los seis, resuelta por Respaldo 1 (`N_TRA` como proxy de discrecionalidad) sin pareo por tipo de trámite individual (`hitoD-R3.1` §2.2). `B` y no `A`, por cuatro razones que salen del archivo:

1. **La ficha se contradice a sí misma.** Línea 762: *"este respaldo degrada automáticamente el veredicto a B, nunca a A, por la misma razón que el Respaldo 2 de R3.2: no aísla discrecionalidad de tipo de trámite."* Línea 784 (fila `A` de la escala): *"brecha ≥20 puntos en la dirección predicha DENTRO de la rama presencial, pareada por tipo de trámite cuando sea posible (vía candidata primaria o Respaldo 1)"* — admite, leída sola, pareo vía Respaldo 1. Línea 785 (regla de precedencia de la propia ficha): *"no hay caso donde dos filas apliquen a la vez, porque 'pareado' y 'no pareado' son estados disjuntos de la misma corrida."* Lo hay: Respaldo 1 sin pareo es simultáneamente el caso que 762 fija a techo `B` y el caso que 784, leída sola y sin 762, dejaría competir por `A` — la propia regla de no-solape (785) no previno la superposición que ocurrió.
2. **Se resuelve hacia el techo `B` porque 762 es la regla más específica** — habla exclusivamente de Respaldo 1, no de la escala en general, y se declara primero en el cuerpo de la ficha (líneas 760-765), antes de llegar a la escala (línea 784) — ADR-58(b), reafirmado por ADR-59(a): *"la fila más específica gana sobre la más general, y la escala de la ficha gobierna sobre cualquier leyenda genérica del corpus."* La resolución se declaró en `forense/hitoD-R3_1-especificacion-v1_0.md §4`, **antes** de abrir `encig23_base_datos_csv.zip` — condición necesaria de validez: fijar el techo después de ver el resultado habría sido la racionalización post-hoc que el Bloque C prohíbe; al revés no valdría.
3. **762 da razón, 784 no.** 762 ancla su techo a un argumento verificable — "no aísla discrecionalidad de tipo de trámite" —, con precedente directo en el Respaldo 2 de `R3.2` (mismo defecto, mismo tratamiento, ya archivado). El paréntesis de 784 ("vía candidata primaria o Respaldo 1") no trae razón propia: leído en el contexto completo de la ficha, es una referencia incompleta a qué caminos pueden *alimentar* la comparación pareada, no una segunda regla que compita con 762 por el mismo caso.
4. **La fila `B` está escrita literalmente para este caso** — "brecha presente pero sin parear tipo de trámite/entidad, o resuelta solo por Respaldo 1 sin pareo" (línea 784). Cuatro de seis cómputos cruzan el umbral de 20pp, pero la magnitud no decide nada aquí: el techo de Respaldo 1 (razón 2) ya fija `B` sin importar la magnitud encontrada — `A` no se sostiene ni por ruta (Respaldo 1 solo, sin pareo, nunca produce `A`) ni por magnitud (cruzar 20pp no es la prueba que decide entre `B` y `A` cuando el camino de selección de pregunta ya está topado).

**(b) Un veredicto `B` NO retiquetea el tier del motor — precedente, no decisión nueva.** La fila `B` de la escala dice "degrada a `[MEDIA]`, no refuta", lo que podría leerse como que `R3.1` deja de ser `[FUERTE]` — y eso la sacaría del perímetro, porque `hitoD-preregistro:384` declara que `R3.3` "es `[MEDIA]` y queda fuera del perímetro". No es así, y el precedente lo prueba sin ambigüedad: `R3.2` lleva veredicto `B` archivado desde el 29/jul/2026 (Nota 6) y su tier en `modelo §7` (línea 675) sigue en `[FUERTE]`, con T12 en verde desde entonces. El tier del motor es una etiqueta de fuerza de literatura, asignada al escribir la regla (`modelo §3.B`/`§3.3`, línea 450); el veredicto de Hito D es sobre si una prueba pre-registrada refutó la regla. Son ejes distintos: "degrada a `[MEDIA]`" describe la fuerza que la prueba validó (corroboración débil, no fuerte — `hitoD-R3.1` §5), no una reetiquetación de la regla. Consecuencias, declaradas explícitamente porque nada en el repo lo decía en palabras y el próximo `B` volvería a chocar con lo mismo:

- El tier de `R3.1` en `modelo §3.B` (línea 450) y `§7` (línea 674) **no se toca** — sigue `[FUERTE]`.
- El desglose **20 `[FUERTE]` + 5 `[MEDIA-FUERTE]` + 1 `[FUERTE como correlación]` + 1 `[FUERTE / MEDIA]` = 27** (`modelo` línea 653) **no cambia**.
- `README:34` ("49 reglas · 20 `[FUERTE]`") **no cambia**. T12 sigue verde.
- El perímetro de 27 es **registro congelado** (ADR-37 §4, arriba; `modelo` línea 653: perímetro decidido el 28/jul/2026 por enumeración 20+1+1+5, no por consulta viva de tier). Un veredicto no encoge el denominador: moverlo porque se movió el numerador vuelve el contador ininterpretable, mismo criterio que congeló `4 de 144` (`README:39`).

**(c) Tres límites declarados, no resueltos.**

1. **La contradicción interna de la escala (762 vs. 784), como precedente registrable.** Una escala puede contradecirse aunque su propia regla de precedencia (línea 785) jure que no hay solape. Toda ficha futura con "regla de precedencia declarada" debe verificarse contra el cuerpo completo de la ficha, no solo contra su propia declaración de no-solape.
2. **Género no está entre los cuatro confundidores de la ficha** (`hitoD-preregistro` líneas 767-771: selección de trámite, composición del usuario, endogeneidad geográfica/institucional, sesgo de reporte). Fuente externa tipo (3), no verificada contra INEGI desde este repo: la prevalencia de corrupción en trámites fue ~18.8% en hombres contra ~9.6% en mujeres, y hombres y mujeres realizan trámites distintos por composición de uso — que es el confundidor 1 de la propia ficha ("selección de trámite") entrando por otra puerta, no aislado por este acto.
3. **Composición por contacto con autoridad de seguridad, en la partición de `N_TRA`.** También tipo (3): en México, el trámite con más experiencias de corrupción reportadas es el contacto con autoridades de seguridad pública (~59.4% frente a ~14.0% nacional). El Respaldo 1 usa tipo de trámite como proxy de discrecionalidad, y la especificación congelada (`hitoD-R3_1-especificacion-v1_0.md §2`) clasifica `N_TRA=20` ("contacto con policías — tránsito, infracciones, detenciones") como **ALTA**, nombrándolo explícitamente "el escenario de mordida más documentado en México". **Verificado dónde quedó en la corrida ejecutada, por aritmética sobre las propias tablas de `hitoD-R3.1` §2.4 (sin abrir microdato):** el desglose por `N_TRA` individual del grupo ALTA lista cinco códigos — 11 (n=100), 12 (n=76), 13 (n=125), 17 (n=372), 18 (n=288) —, cuya suma es exactamente 961: el mismo `n` total de ALTA que `hitoD-R3.1` §2.2 reporta para la interpretación (a) en los tres regímenes de ponderador. No queda aritméticamente ningún renglón para `N_TRA=20`: contribuyó **cero filas** a esta corrida, pese a estar declarado ALTA en la clasificación teórica — consistente con que un encuentro de calle con policía (tránsito/detención) no se codifique como `P7_3=1` ("Instalaciones de gobierno"), el filtro de universo que la ficha fija para "presencial estricto" (`hitoD-R3_1-especificacion-v1_0.md §3`), y quede fuera de esta corrida por ese filtro previo, no por la partición ALTA/BAJA en sí. De las tres ramas que este acto tenía que declarar: cae en **"excluida" (tercera rama)** — la sospecha de que la composición policial infla el promedio de ALTA **se descarta** para esta corrida específica: policía no aportó ninguna fila al grupo que produjo la brecha de 6/6 cómputos. Ninguna de las tres ramas tumba el `B` (declarado así de antemano en el propio encargo); esta, la que aplica, tampoco. Se hermana con `W1-P`, que prueba la misma sospecha sobre el instrumento de al lado (partición del universo de `W1` por contacto con autoridad de seguridad, ver (e)).

**(d) `R3.1` y `W1` comparten desenlace — se registra la convergencia, no se adjudica.** El id de la regla `R3.1` es `tramite.mordida.discrecional` (`hitoD-preregistro` línea 736; `modelo §3.3` línea 450) — verificado que es el mismo desenlace que `W1` usa en `milpa/procedencia.yaml:663` (*"reactivo AP5_1_1/2/3 ... x desenlace `tramite.mordida.discrecional`"*, entrada `coeficientes_generador_medidos.G1_radio_confianza`). Instrumentos distintos (ENCIG 2023 para `R3.1` / ENCUCI 2020 para `W1`), mismo desenlace modelado, y el `PORQUE` de `R3.1` es **G1** ("trampa social", `hitoD-preregistro` línea 736) — el mismo generador cuyo coeficiente estima `W1` (`procedencia.yaml:625`, `G1, coefs: {..., radio_confianza: -0.35}`). Tres piezas apuntan al mismo sitio: `R3.1` confirma gradiente de discrecionalidad dentro de lo presencial (dirección positiva, 6/6 cómputos); `W1` no sobrevive a condicionar (Encargo X, ADR-57); y la literatura externa (cláusula (e)) enmarca el conductor como discrecionalidad de ventanilla frente a confianza de pasajera. **Se registra como convergencia observada.** No se reescribe ningún generador aquí — eso exigiría acto propio y evidencia que `W1-P` todavía no ha devuelto.

**(e) `radio_confianza`: estado nombrado, con condición de resolución escrita.** `G1 · radio_confianza` está `ASIGNADO` en −0.35 (`procedencia.yaml:625`). Pasa a estado **`ASIGNADO · SIGNO BAJO PRUEBA`**, con condición de resolución escrita: el resultado del acto `W1-P` (partición del universo de `W1` por contacto con autoridad de seguridad, mismo mecanismo que (c).3 aplicó sobre `N_TRA` para `R3.1`). Por qué no se voltea el signo hoy (A-bis regla 2): lo que la discordancia entre el β̂ marginal (negativo, los tres ítems) y las celdas condicionadas (28 de 39 según la prosa de `procedencia.yaml:665`, recontadas en 33 de 39 por este acto — ver (f)) establece es que el marginal **no es robusto**, nada más. Condicionar puede acercar o alejar del estimando verdadero — `W1-P` decide entre dos lecturas incompatibles: si la composición policial explica la reversión, `W1` no midió lo que su rótulo dice y la reversión no es evidencia contra `−0.35` sino contra la especificación del universo; si la reversión sobrevive al corte por contacto con autoridad de seguridad, sí es evidencia contra el signo, en tres líneas (los tres ítems de ENCUCI). Por qué tampoco se deja como estaba: hoy figura como `ASIGNADO` sin más, y eso ya no describe su situación desde el Encargo X (ADR-57) — una deuda que deja de ser coherente y sigue operando por inercia es lo que v2.2 nombra.

Evidencia externa, **tipo (3), rotulada y acotada — no mueve ningún contador**: Sanabria-Pulido, P. y Langbein, L. (2025), "I'll Bribe You Because I Trust You: Street-Level Bureaucracy, Discretion, Social Capital, and Petty Corruption in Latin America", *Public Integrity*, doi 10.1080/10999922.2025.2520710. 18 países latinoamericanos, 2004-2014. Encuentran que la confianza generalizada se asocia con mayor probabilidad de solicitud de soborno, y la confianza comunitaria con menor — con excepción declarada por los propios autores: sin efecto cuando el soborno involucra a la policía. Reportada por búsqueda web de mesa, **no verificada contra el paper desde este repo**. ⚠️ Es la primera vez que evidencia externa e independiente toca el signo de un coeficiente de generador del motor — por eso entra estricta: como referencia con procedencia declarada, no como insumo de cálculo, no como ancla de validación, y sin mover contadores.

**Lo que expresamente NO cambia:** el `−0.35` que el motor usa · `asignados_coeficiente.detalle` · el contador `0 de 15` (`README:38`) · la clase `MEDIDO·β̂` de las tres entradas medidas (`procedencia.yaml:661`) · ADR-57(a), que sigue vigente e intacto.

**(f) La hipótesis del desdoble: probada y descartada, con su data.** Mesa propuso que `radio_confianza` mezclaba dos constructos de signo opuesto (confianza generalizada vs. comunitaria), lo que explicaría el ruido, la reversión al condicionar y la curva no monótona a la vez. Los tres ítems de ENCUCI (`AP5_1_1` mayoría de las personas, `AP5_1_2` personas que conoce, `AP5_1_3` vecinos) son exactamente esos referentes, y el Encargo X ya los corrió por separado, celda por celda (`forense/notas/2026-08-04-x-condicionamiento-y-forma.md §4.1`).

**La data la refuta.** Conteos por ítem, derivados por este acto directamente de las 39 celdas de §4.1 (13 por ítem: 2 de formalidad + 4 de edad + 7 de ingreso), no copiados de la prosa de la propia nota:

| Ítem | Positivas | Negativas | Significativas (IC95% excluye 0) | Marginal significativo |
|---|---|---|---|---|
| `AP5_1_1` (mayoría de las personas) | 11/13 | 2/13 | 2/13 (ambas positivas) | No (IC incluye 0) |
| `AP5_1_2` (personas que conoce) | 9/13 | 4/13 | 2/13 (ambas positivas) | No (IC incluye 0) |
| `AP5_1_3` (vecinos) | **13/13** | 0/13 | **5/13 (todas positivas)** | **Sí, negativo** — único de los tres |
| **Total** | **33/39** | **6/39** | **9/39, las nueve positivas** | — |

Los tres referentes muestran la misma dirección positiva al condicionar (28 celdas positivas sobre 3 ejes cada uno, en distintas proporciones), y ninguna celda con IC95% que excluye cero tiene signo negativo en ninguno de los tres ítems. `AP5_1_3` (vecinos, el referente que la literatura de confianza comunitaria predeciría en dirección contraria al de confianza generalizada) es el **más consistente** en la dirección positiva — 13 de 13 celdas — y es a la vez el **único de los tres con marginal significativamente negativo**: la reversión es total justo donde el marginal parecía más sólido. Si los tres ítems midieran constructos de signo opuesto, se esperaría que al menos uno mostrara un patrón de celdas distinto a los otros dos — no ocurre: los tres condicionan hacia el mismo signo. **Se registra como hipótesis de mesa probada y descartada**, para que ninguna sesión futura la vuelva a proponer sin evidencia nueva.

⚠️ **Discrepancia declarada, no corregida aquí.** El titular de §4.1 de `forense/notas/2026-08-04-x-condicionamiento-y-forma.md` ("28 de 39 celdas... 12 de ellas distinguibles de cero"), propagado también a `milpa/procedencia.yaml:665,685`, **no reproduce** contra sus propias 39 celdas — el recuento de este acto, hecho fila por fila sobre las tres tablas de §4.1, da **33 de 39 positivas y 9 de 39 significativas** (tabla arriba). La corrección de ese titular y su propagación a `procedencia.yaml` está asignada al commit 3 de `W1-P` (perímetro de ese acto, no de este); este ADR usa únicamente las cifras derivadas aquí (33/9), no las de la prosa original (28/12), y dos deja pendiente sin usar como si fueran ciertas.

⚠️ **Cerrada por ADR-61** (ENCARGO M-2, 4/ago/2026). La corrección asignada arriba al commit 3 de `W1-P` nunca se ejecutó ahí — la v2 de esa ficha reasignó ese commit fuera de sí misma por contaminación (`2026-08-04-w1-p-policial.md` §0) —, y llegó huérfana al acto que cierra la condición de (e). ADR-61 re-derivó el recuento por una vía independiente (33/9, mismo resultado) y propagó la corrección a `procedencia.yaml:665,685`. Esta nota no se edita más allá de esta adenda: queda como registro de que la discrepancia se declaró aquí y se corrigió dos actos después.

**Requisito de salida.** `forense/hitoD-preregistro-v2_0.md` recibe el veredicto `R3.1`→`B` en el bloque append-only (Nota 28, este acto) y la cascada de contadores 11→12 se ejecuta en `README.md`, `canon/estado-programa-v1_10.md` y `canon/modelo-decision-v4_0.md` en el mismo PR — perímetro acotado igual que ADR-55/56/58, pero esta vez con la deuda de cascada completa que ADR-58(e) dejó nombrada (`gobernanza:358,683`) cerrada en el mismo acto, no diferida de nuevo. Conteo de ADR (59→60) propagado a `estado-programa:27,99` y a esta cabecera.

**Reversión.** Una adjudicación de esta cláusula se revierte solo por corrida nueva pre-registrada de la misma regla, nunca por relectura — mismo criterio que ADR-58. El estado `ASIGNADO · SIGNO BAJO PRUEBA` de (e) se resuelve por el resultado de `W1-P`, no por este ADR ni por ningún otro acto de escritorio.

→ **Vigente. S2.** *(Aprobado 4/ago/2026, decisión de mesa del autor. Sesión de escritura, sin dato — perímetro declarado en el propio encargo: `forense/hitoD-preregistro-v2_0.md` (solo bloque append-only), `canon/gobernanza-v1_15.md` (este ADR + cascada de cabecera), `canon/estado-programa-v1_10.md`, `canon/modelo-decision-v4_0.md`, `README.md`, `forense/hallazgos.md`; no abrió `data/raw/`, microdato, ni `milpa/procedencia.yaml` — ese archivo es perímetro de `W1-P`.)*

---

**ADR-61 · Se resuelve la condición de ADR-60(e) con el veredicto de `W1-P` — `G1·radio_confianza` cambia de rótulo, no de valor; se corrige la lectura de escala de `W1-P` a razón de riesgos; se corrige el recuento de §4.1 (28/12 → 33/9); se declara que el censo de estimabilidad (`E-CE`) se venció por segunda vez antes de fusionar.** Decisión de mesa del autor, ENCARGO M-2, 4/ago/2026, sobre el resultado de `W1-P` (`forense/notas/2026-08-04-w1-p-policial.md`) y sobre tres cabos que quedaron abiertos el mismo día: la lectura de escala de la propia ficha de `W1-P` (declarada en su §8.1, no corregida ahí), el recuento huérfano de §4.1 del Encargo X (asignado y reasignado fuera de `W1-P` v2 sin ejecutarse nunca) y la segunda vencida del censo de estimabilidad (`E-CE`) contra `PR #109` (`B-3`). Detalle completo, recetas y salidas crudas de las cuatro cláusulas en `forense/notas/2026-08-04-m2-adjudicacion-adr-61.md`.

**(a) Se resuelve la condición de ADR-60(e): composición policial DESCARTADA, de forma permanente.** `W1-P` particionó el universo de contacto de `W1` (n=13 435) en estrato policial (`AP5_16_1='1'`, n=4 087) y no-policial (n=9 348), sobre los tres ítems (`AP5_1_1/2/3`), con falsador y umbral pre-registrados antes de abrir el DBF. Veredicto por ítem: `AP5_1_1` DISCREPANTE (signos opuestos, policial significativo −5.37pp [−9.92,−0.82] vs. no-policial +0.80pp [−0.58,2.18] n.s.); `AP5_1_2` ESTABLE (signos opuestos pero ningún IC95% excluye cero); `AP5_1_3` ESTABLE (mismo signo negativo en ambos estratos). Regla de precedencia de la propia ficha: 1 de 3 DISCREPANTE no alcanza la fila 2 (CONFIRMADA, exige ≥2 de 3); 2 de 3 ESTABLE sí alcanza la fila 3. **→ COMPOSICIÓN POLICIAL DESCARTADA** — se cierra de forma permanente, con dato, y no se vuelve a proponer esta hipótesis explicativa sin evidencia nueva. Consecuencia para el `−0.35`: la reversión de signo que el Encargo X encontró al condicionar por formalidad/edad/ingreso (ADR-57(a)) **no se reproduce** en el eje policial — donde hay señal significativa (`AP5_1_1`, `AP5_1_3`), el signo se mantiene negativo, igual que el marginal y que el ASIGNADO. La inestabilidad que ADR-57 documentó no es, por tanto, una propiedad universal de condicionar este estimando por cualquier eje — es específica de formalidad/edad/ingreso; esto acota el problema, no lo agranda ni lo resuelve. `G1 · radio_confianza` sale de `ASIGNADO · SIGNO BAJO PRUEBA` y pasa a **`ASIGNADO · asociación estable a la partición policial, inestable a formalidad/edad/ingreso`** — es descripción, no veredicto: no se escribe "validado" ni "refutado". El coeficiente sigue sin identificación (asociación, ADR-57), sigue en escala de diferencia de proporciones sin función de enlace declarada por el modelo, y el contador `0 de 15` (coeficientes en escala del modelo) **no se mueve**. El `−0.35` que el motor usa no cambia.

**(b) Corrección de escala: en razón de riesgos, los dos estratos no se distinguen en 2 de 3 ítems — leer la significancia exclusiva del estrato policial como "la señal vive en un solo estrato" es engañoso.** Las tasas base del desenlace difieren fuertemente entre estratos (27.30% [24.92,29.68] policial vs. 5.35% [4.65,6.04] no-policial, `2026-08-04-w1-p-policial.md` §8) — por A-bis regla 3, una misma diferencia de proporciones sobre bases tan distintas no es comparable en magnitud. Verificado aquí sobre la razón de riesgos (RR) que la propia ficha calculó por su restricción 2, con la receta ¿el punto RR de un estrato cae dentro del IC95% del otro?: `AP5_1_1` (RR pol. 0.821 vs. no-pol. 1.160 [0.899,1.498]) **sí distingue**; `AP5_1_2` (0.836 vs. [0.745,1.410]) y `AP5_1_3` (0.817 vs. [0.707,1.195]) **no distinguen** — el punto policial cae dentro del IC95% no-policial en ambos. Solo 1 de 3 ítems distingue los dos estratos en RR. Sobre la base chica (5.35%), un RR típico de ~0.82 equivale a ~0.96pp — cerca de 1 punto porcentual —, y los IC95% del estrato no-policial son más anchos que eso. Lo que este ADR declara: el estrato no-policial no dice "no hay efecto" — dice "no hay precisión, con este n, para distinguir un efecto del tamaño que muestra el policial". Confundir ausencia de significancia con ausencia de efecto es el error que esta cláusula previene, y se deja escrito porque el próximo corte estratificado de este mismo estimando va a topar con la misma trampa de escala.

**(c) Se corrige el recuento de §4.1 (Encargo X): 33 de 39 positivas, 9 de 39 significativas — no 28/12.** El titular de §4.1 de `forense/notas/2026-08-04-x-condicionamiento-y-forma.md` ("28 de 39 celdas... 12 de ellas distinguibles de cero") no reproduce contra sus propias tres tablas (39 celdas = 3 ítems × 13 celdas [2 formalidad + 4 edad + 7 ingreso]). Re-derivado aquí con receta propia (parser directo de las tres tablas, ajeno a la prosa del titular), validado contra un caso conocido antes de confiar en él (`AP5_1_1`/Edad/18-29: β̂=+0.0398, IC95%=[+0.0015,+0.0782], positivo y significativo — coincide con la fila literal del archivo): `AP5_1_1` 11 positivas/2 negativas/2 significativas; `AP5_1_2` 9/4/2; `AP5_1_3` 13/0/5. **Total: 33 de 39 positivas, 9 de 39 significativas (las nueve positivas)** — coincide, por una vía de derivación independiente, con la cifra que ADR-60(f) ya había recontado por su propio método; dos parsers distintos, mismo resultado. La conclusión cualitativa **no cambia y se refuerza**: cero celdas con IC95% que excluye cero tienen signo negativo, en los tres ítems y los tres ejes — lo que estaba mal era el conteo, no la lectura. Corregido en `milpa/procedencia.yaml:665` (`eje_condicionante`) y `:685` (`adr57_a`). `gobernanza-v1_15.md:714` (declaración de discrepancia, ADR-60(f)) se marca cerrada con adenda fechada, sin editar su texto original — mismo criterio que ADR-58(e)/ADR-60 usaron para cerrar deuda de cascada ajena sin reescribir la nota que la dejó abierta. `ADR-57` no se edita — append-only y sellado; su conclusión ("asociar ≠ identificar") sobrevive intacta, y no depende de si son 28 o 33 celdas: ambas cifras muestran reversión mayoritaria a signo positivo. La nota de Encargo X tampoco se edita (`forense/` append-only); la corrección vive aquí y en `procedencia.yaml`.

**(d) El censo de estimabilidad (`E-CE`) se venció por segunda vez antes de fusionar — patrón, no caso aislado.** Cronología verificada (`git log --date=local`, huso de Mesa): el censo se escribió (`0db6d1d`, 21:11:53) contra la foto `8cdabcb` (20:41:02); el corpus se movió 2m28s después (`65302f7`, PR #107, ENASEM) — primera vencida, que el propio remate de `E-CE` (`ca883a3`, 21:37:56, al fusionarse en PR #108/#111) atrapó y declaró, nombrando ENASEM/MHAS candidato de un censo v1.1. Ese mismo remate declaró explícitamente no haber podido cruzar `B-3` ("cero resultados al cierre de este acto"). `PR #109` (`B-3`, `dff4877`, 22:02:02) fusionó diez instrumentos nuevos a `data/manifiesto.yaml` 24m6s después de escrito el remate — entre ellos `enfih2019_bd_csv_zip` (Encuesta Nacional sobre las Finanzas de los Hogares, Banxico+INEGI) y `ensafi2023_bd_csv_zip` (Encuesta Nacional sobre Salud Financiera), ambos de dominio de dinero (mismo dominio que `G1`/`G3`/`G5`) — sin cruzarse contra el censo, verificado por `grep` (`enfih`/`ensafi`: cero resultados en `censo-estimabilidad-coeficientes-v1_0.md`). El reparto `SIN-RUTA=9` fue un conteo honesto de un corpus que duró veinte minutos. Lo que este ADR declara, más allá del caso: es la **segunda vez** que este censo se vence antes de ser leído por el programa. Cuando un artefacto que fotografía el estado de un corpus se vence dos veces seguidas, el defecto no es del censo — es que una foto escrita a mano no puede seguirle el paso a un corpus que se mueve mientras se escribe, y lo que corresponde es derivarla por comando, como `A3` concluyó para el catálogo ("un número impreso es un número que se puede quedar atrás de su comando") y como el propio `E-CE` ya hace parcialmente en su §7. Este ADR **no reescribe el censo** (`SIN-RUTA=9` sigue vigente al SHA `8cdabcb` que el propio censo declara, no como estado del programa) **ni implementa ningún mecanismo** — esa decisión, y su costo en sesiones que no midieron (regla de señal de v2.3), queda para mesa. Nombra a **ENFIH** y **ENSAFI** como los dos primeros candidatos a revisar en un censo v1.1, sin cruzarlos contra ningún coeficiente aquí.

**Cascada.** Conteo de ADR 60→61, tres sitios derivados con la receta de T15 (escanea todo `canon/*.md`, no solo `gobernanza`): `gobernanza-v1_15.md:2` (cabecera), `estado-programa-v1_10.md:27`, `estado-programa-v1_10.md:99` (cadena L0, entrada nueva). El rótulo de `radio_confianza` en `milpa/procedencia.yaml → rutas_estimabilidad_coeficiente.detalle` (fila `{gen: G1, coef: radio_confianza}`, que citaba "ASIGNADO · SIGNO BAJO PRUEBA (ADR-60 e), pendiente W1-P") se actualiza a la cita del rótulo nuevo de (a) — es del censo, pero la cita quedó vencida por este ADR. Contadores que **no se mueven**, declarado para que quede dicho: `0 de 15` coeficientes en escala del modelo, `12 de 27` Hito D, `9 de 14` condicionales, `4 de 144`. Ninguno. Este acto sella interpretación de resultados ya medidos, no mide nada nuevo.

**Reversión.** Una adjudicación de esta cláusula se revierte solo por corrida nueva pre-registrada, nunca por relectura — mismo criterio que ADR-58/59/60. El rótulo de (a) se revierte solo si una corrida nueva sobre el eje policial (o uno nuevo) lo justifica, no por este ni por ningún otro acto de escritorio.

**Versión y nombre de archivo.** El número de versión de `gobernanza` **no sube** y el archivo **no se renombra** — mismo criterio que ADR-48 a ADR-60.

→ **Vigente. S2.** *(Aprobado 4/ago/2026, decisión de mesa del autor, ENCARGO M-2. Sesión de escritura, sin dato — perímetro declarado en el propio encargo: `canon/gobernanza-v1_15.md` (este ADR + cascada de cabecera), `canon/estado-programa-v1_10.md`, `milpa/procedencia.yaml`, `forense/notas/2026-08-04-m2-adjudicacion-adr-61.md`, `forense/hallazgos.md`; no abrió `data/raw/` ni microdato; no tocó `hitoD-preregistro`, `tests/`, `data/`, `canon/modelo-decision-v4_0.md`, `ADR-57`, `ADR-60`, ni ninguna nota forense previa.)*

---

**ADR-62 · ENCARGO MT-mantenimiento: mesa + consultoría, lista cerrada de 8 ítems de mantenimiento sobre CI, dependencias, `tramite.yaml`, `manifiesto.py`, `svystat.py`, la serie MILPA y la deuda documental de AUTHORSHIP/AVISO — no mide nada nuevo sobre México, solo endurece el aparato.** Decisión de mesa del autor, 4/ago/2026. Ocho cláusulas, una por ítem del encargo:

**(a) Banner de incompatibilidad parcial en `milpa-spec` y `milpa-plan`.** Ambos documentos ganan una nota visible, arriba de su bloque de cabecera: describen una arquitectura (perfil como casilla de asignación; compilación desde la ficha canónica) que ADR-51 y ADR-36.b ya superaron. **Desvío de alcance declarado en el propio banner de cada archivo:** este acto NO renombra `milpa-spec-v0_2.md`→`v0_3` ni `milpa-plan-v0_1.md`→`v0_2` ni sube la cifra de versión de sus cabeceras, pese a que el encargo lo pedía. Verificado con `tests/check.py` (T03, referencias colgantes): renombrar dejaría colgantes las citas al nombre viejo en `canon/estado-programa-v1_10.md` (Registro de artefactos) y `forense/censo-integridad-v1_0.md` — ninguno de los dos en el perímetro cerrado del encargo — y tumbaría `--baseline` en rojo. Queda para un acto que también toque esos dos archivos.

**(b) Segundo carril de CI.** `.github/workflows/verify.yml` gana un step bloqueante, `python3 tests/test_svystat.py`, independiente de `check.py --baseline` y sin dependencia nueva (standalone). Antes, el test existía commiteado pero no corría en ningún lado — CI verde no decía nada sobre él.

**(c) `requirements.txt`.** Nuevo, con la única dependencia externa real del repo (`PyYAML`, que `tests/manifiesto.py` ya importaba sin declararla en ningún lado). Verificado en venv limpio: `pip install -r requirements.txt && python3 tests/check.py --baseline` → VERDE, exit 0. **Desvío de alcance declarado:** el encargo también pedía una línea de instalación en `README.md`; `README.md` no está en el perímetro cerrado del encargo (no aparece en la lista de 12 rutas), así que no se tocó — el `requirements.txt` solo, sin esa línea, ya satisface el criterio de aceptación verificado arriba.

**(d) `milpa/tramite.yaml` v0.2.0 → v0.3.0.** Las 10 probabilidades del archivo (5 reglas × 2 desenlaces) ganan `clase: ASIGNADO`; `nota_calibracion` se conserva verbatim porque dice en prosa el mismo hecho. Conteo derivado, no tecleado: `grep -c ', clase: ASIGNADO}' milpa/tramite.yaml` = 10 = `grep -c 'p: 0\.' milpa/tramite.yaml` — mismo comando en el mensaje del commit que introduce el cambio.

**(e) `tests/svystat.py` materializa su entrada.** `prop_ultimate_cluster()` recorre `rows` dos veces (agregados globales, luego bloque de UPM); con un generador, el segundo recorrido lo veía vacío sin lanzar excepción — defecto silencioso. Ahora `rows = list(rows)` al entrar. `tests/test_svystat.py` gana `test_generador_no_falla_en_silencio()`, que pasa el dataset conocido de `test_caso_sintetico_dos_estratos()` como generador y exige resultado idéntico al de la lista; corre en el step de CI de (b).

**(f) `tests/manifiesto.py`, dos arreglos.** (i) `entorno_actual()` deja de incluir `host` hacia adelante — SO y Python se conservan; las ~190 entradas ya escritas en `data/manifiesto.yaml` bajo la versión anterior de la función no se tocan, append-only. (ii) `--id` pasa a `action='append'`: `--verifica` con varios `--id` en la misma invocación ahora verifica **todos** (antes, argparse tomaba el último en silencio — el defecto de `forense/hallazgos.md` 4/ago que la TANDA-4 de la maestra esquivó a mano) y falla ruidoso, listando cada id ausente, si alguno no existe; `--registra`/`--compara` (que solo tienen uso para un id) rechazan `--id` repetido con error explícito en vez de tomar el último callados. Probado con dos ids reales del manifiesto: ambos se reportan.

**(g) `AUTHORSHIP.md` y `AVISO-DE-ALCANCE.md` retiran la cifra de suite tecleada.** Los dos decían "hoy… 19 FAIL y 84 WARN"; la cifra real al correr `tests/check.py` en este acto es **18 FAIL · 95 WARN**, y la del propio `README.md` §"Primera corrida de la suite" (28/jul, fechada) es una tercera cifra distinta (18 FAIL · 110 WARN) — tres números en circulación, la prueba viva de que una cifra tecleada se desactualiza sin que nadie lo note. Los dos archivos ahora remiten a correr `python3 tests/check.py` para la cifra vigente en vez de repetirla.

**(h) Tag de auditoría `audit-externa-2026-08` — BLOQUEADO, no completado.** Se creó localmente (`git tag -a audit-externa-2026-08 3f73c29…`, SHA re-derivado: main se movió de `bd2c975` a `3f73c29` a mitad de este acto — PR #114, ajeno, no toca `gobernanza`/`milpa`/`tests`/AUTHORSHIP-AVISO, se hizo merge fast-forward sin conflicto). `git push origin audit-externa-2026-08` devuelve **HTTP 403** de forma consistente (dos intentos, sin retry indefinido por instrucción del proxy) mientras que `git push` de la rama del encargo, al mismo remoto, en el mismo momento, funciona sin error — el bloqueo es específico a la creación de refs `tags/*`, no un problema de red ni de credencial general. No existe herramienta MCP de GitHub disponible en esta sesión para crear un tag por API como alternativa (`mcp__github__get_tag`/`list_tags` son de solo lectura). Se reporta como hallazgo, no se enmascara: `git ls-remote --tags origin | grep audit-externa` da vacío. La auditoría externa queda sin ancla citable — exactamente el defecto que este ítem intentaba cerrar.

**Cascada, forzada por T15/T16, no por elección.** Sellar este ADR sube el conteo de ADR únicos de `gobernanza` de sesenta y uno a sesenta y dos; `tests/check.py` (T15, T-ADR-COUNT) verifica mecánicamente que **todo** `canon/*.md` que cite el conteo de ADR lo tenga sincronizado — no solo este documento. Tres sitios, la misma receta que ADR-60/61 ya usaron: la cabecera de este mismo archivo (línea 2) y `canon/estado-programa-v1_10.md:27,99` (Registro de artefactos + cascada L0) — este segundo archivo **no está en el perímetro cerrado del encargo** (no aparece en la lista de 12 rutas), pero dejarlo sin actualizar deja el T15 real en rojo, que es un requisito de cierre más duro y más explícito que el perímetro ("Suite `--baseline` VERDE antes del push"). Se prefiere el requisito de cierre: cambio mínimo, mecánico, un dígito por sitio más una cláusula de cascada en la oración histórica de `estado §L0`, mismo patrón que cada ADR anterior desde el 48 documenta como "Cascada". Verificado corriendo la suite completa después del ajuste: T15 y T16 vuelven a `[ ok ]`, idéntico al estado antes de abrir este ADR salvo el conteo de ADR, que era justo lo que se estaba moviendo.

**Requisito de salida.** `forense/notas/2026-08-04-mt-mantenimiento.md` (fecha en huso de Mesa) con los comandos de aceptación de cada ítem corridos y su salida; línea en `forense/hallazgos.md` terminando "No impidió medir. Contadores movidos: 0."; `tests/check.py --baseline` VERDE antes del push (verificado arriba, en (c) y en la cascada de este párrafo, sobre el estado final del árbol).

**Reversión.** El desvío de (a) se revierte solo con un acto que también toque `canon/estado-programa-v1_10.md` y `forense/censo-integridad-v1_0.md` para renombrar sin dejar colgantes. El bloqueo de (h) se revierte solo si una sesión con credencial capaz de crear refs `tags/*` en este repo confirma el push — no es una condición que este acto pueda cerrar solo.

**Versión y nombre de archivo.** El número de versión de `gobernanza` **no sube** y el archivo **no se renombra** — mismo criterio que ADR-48 a ADR-61.

→ **Vigente.** *(Aprobado 4/ago/2026, decisión de mesa del autor, ENCARGO MT-mantenimiento. No abrió `data/raw/` ni microdato — mantenimiento puro, ningún contador de medición se mueve. Perímetro cerrado del encargo: `.github/workflows/verify.yml`, `requirements.txt` (nuevo), `tests/svystat.py`, `tests/test_svystat.py`, `tests/manifiesto.py`, `milpa/milpa-spec-v0_2.md`, `milpa/milpa-plan-v0_1.md`, `milpa/tramite.yaml`, `AUTHORSHIP.md`, `AVISO-DE-ALCANCE.md`, `canon/gobernanza-v1_15.md` (este ADR, solo append), `forense/notas/` (nota de cierre), `forense/hallazgos.md` (solo append). Dos desvíos declarados dentro de ese perímetro — (a) y (c), arriba —; una expansión forzada por T15/T16, no elegida — `canon/estado-programa-v1_10.md:27,99`, dos dígitos y una cláusula de cascada, ver párrafo "Cascada" arriba —; un bloqueo declarado y no resuelto — (h).)*

---

## 5. Deuda declarada (decisiones abiertas, conscientemente)

Esto **no** es una lista de pendientes: es deuda que se decidió asumir.

| Deuda | Estado | Decisión |
|---|---|---|
| **Sin datos primarios** (falla raíz) | Abierta | **Asumida a propósito.** Se trabaja con literatura tierizada y validación forense. |
| **Elasticidades / coeficientes** | Abierta, **sin ruta hoy** | Los 15 coeficientes son `ASIGNADO`; el corpus es transversal (da estados, no ritmos). `unico_calibrable_hoy` se retiró (ADR-49, D1): la vía ENOE no identifica conducta financiera (`forense/hallazgos.md`, 31/jul/2026). Ningún coeficiente tiene ruta de calibración con dato público hoy. |
| **Consumo popular D/E, rural** | Abierta | Vacío admitido; el mapa debe mostrarlo pálido, no interpolado. |
| **Confianza radial como canal medible** | Abierta | ADR-20. ⚠️ Ahora **contradicha** por los casos Nu y Kueski: G1b baja a HIPÓTESIS con coeficiente a revisión. |
| **Granularidad municipal** | Abierta | Techo de resolución real del simulador. |
| **Cuatro generadores sin falsar** ⭐ | Abierta | G1a, G4, G5, G6. G4 y G5 sin un solo caso disponible. |
| **Reglas del motor sin prueba de falsación pre-registrada** ⭐ | Abierta | **Es el Hito D** (bloque append-only de `hitoD-preregistro`): perímetro DECIDIDO de **27 reglas**, subconjunto de las **49** del motor. **12 de 27 corridas archivadas** — `R1.1` → `D`, `R3.2` → `B`, `R7.2` → `D`, `R4.2` → `D`, `R4.1` → `D`, `R9.1` → `D`, `R4.3` → `D` (mitad A y mitad B), `R9.2` → `D`, `R5.1` → `A`, `R5.2` → `A`, `R1.2` → `E`, `R3.1` → `B`; 15 de 27 sin corrida (37 de 49 sobre el motor completo). *(No confundir con el ejercicio informal de `glosario §6/§10` sobre G1a/seguro agrícola, 27/jul — ese NO es el Hito D: ver ADR-45.)* *(Corregido 4/ago/2026 — propaga `R7.2`→`D`; propaga `R4.2`→`D`, ADR-55, Encargo Y; propaga `R4.1`/`R9.1`/`R4.3`/`R9.2`→`D`, ADR-56, Encargo Z; adjudica `R5.1`/`R5.2`→`A` y `R1.2`→`E`, ADR-58; adjudica `R3.1`→`B`, ADR-60 — deuda de cascada de ADR-58(e) cerrada en este acto.)* |
| **Ocho refutaciones sin objeto** ⭐ | Abierta | El modelo no tiene variable de esfuerzo, colorismo, salud mental ni entidad prestamista. Incluye `ref.A.02` —la única MUY_FUERTE de las 49—. **Decisión pendiente: ampliar el modelo o declarar el alcance y retirarlas.** |
| **PD-01 · 14 descartes** | **Cerrada — pérdida consumada** | Nunca se escribieron. **No reconstruir:** un descarte fabricado es indistinguible de uno real. |
| **Sistema indígena-comunal** | **Cerrada por diseño** | ADR-10. No es deuda: es otro modelo. |

### 5.1 Casillero de pendientes irresueltos *(nuevo en v1.1 — severidad S5)*

Contradicciones que **no se resuelven** y que sin este casillero se caen del sistema.

| # | Pendiente | Estado |
|---|---|---|
| **conf.02** | **Policronía** — Trabajo y Tiempo refutan el mismo mito con **mecanismos opuestos** | Abierto, sin ADR |
| **conf.04** | Alegría vs. malestar | ✅ **Resuelto por ADR-27**: artefacto de agregación. *Queda vivo el desajuste de instrumento (Cantril mide evaluación vital, no alegría).* |
| **conf.05** | **Consumo compensatorio** — Fuerte (consumidor) vs. Hipótesis (salud) | Abierto — **no promediar** |
| **conf.06** | **Magnitud de la confianza interpersonal** — 12% · 21.8% · 22% · 32.1% · 18%; **dos dicen ser la misma ENCUCI 2020 y difieren 10.3 puntos** | Abierto. **Ninguna cifra de confianza se usa como establecida hasta cerrarlo** |
| **honor híbrido** | `foundational` sostiene un híbrido (honor rural + dignidad urbana) que ADR-05 superó | Abierto **por decisión**: es posición matizada, no error, y retirar la etiqueta para todo México podría ser sobre-corrección urbana |

| **conf.07** | `modelo §3.7` empaquetaba **Fuerte + Hipótesis** bajo un solo tier | ✅ **Resuelto 28/jul: regla partida en dos** (v2.1). El ascenso HIPÓTESIS→FUERTE del cambio 10 se sostiene sobre la mitad con identificación causal ("conserva autonomía de voto"); la mitad subjetiva ("se vive como derecho") vuelve a `[HIPÓTESIS]` |

⚠️ **Antes de meter algo a este casillero, aplicar la prueba de ADR-27:** *¿es contradicción real, o alguien promedió?* Dos de los cuatro pueden ser artefactos de agregación y no choques.

---

## 6. Cómo abrir una conversación nueva del proyecto

Para que ninguna corrida vuelva a reconstruir de memoria (el fallo de V1):

| Si vas a… | Pega esto |
|---|---|
| Correr un report temático | `instrucciones-proyecto-v2.md` |
| Correr una validación forense | instrucciones v2 + **ficha canónica** + `prompts-verticales-validacion` **(parchada 27/jul)** |
| Trabajar sobre el modelo | **`modelo`** + este documento |
| Tierizar un constructo | **`glosario`** — único punto legítimo de entrada de un tier |
| Construir el simulador | whitepaper + spec + `milpa/` + ficha canónica |
| Retomar el programa en frío | **`estado`** + este documento |

**Antes de cualquier corrida nueva:** verificar que la ficha canónica esté sincronizada con el modelo (§3). ⚠️ **Hoy no lo está: la ficha es foto del v1.**

**Regla nueva de plantilla forense** *(Hito 2)*: toda regla que un encargo mande estresar se **cita textualmente del motor §3**, con tier, dominio y perfiles. Si no trae cita, se marca como propuesta nueva y su veredicto **no cuenta como validación del modelo**.

---

## 7. Bitácora de versiones de este documento

| Versión | Qué cambió |
|---|---|
| 1.0 | Creación. ADR-01 a ADR-25, registro de artefactos y protocolo de propagación tras cerrar Ronda 4 y Fase 0. |
| **1.15 · enmienda in situ** | **4/ago/2026 — ADR-56: propaga `R4.1`, `R9.1`, `R4.3` (mitad A y mitad B) y `R9.2` → `D` al bloque append-only de `hitoD-preregistro` (4 de 27 → 8 de 27).** *Mismo criterio que ADR-48 a ADR-55: el número de versión NO sube y el archivo NO se renombra.* Encargo Z, mesa #19: las cuatro únicas fichas del perímetro con Umbral completo y escala propia completa sin fuente declarada, declaran ENSANUT CONTINUA 2024 (commit 1) y corren `D` en las cinco líneas de registro (commit 2) — `R4.1`/`R9.1` por ausencia de diseño antes/después y de variable de distancia/muestra de no-consulta; `R4.3` (ambas mitades) por adherencia solo disponible auto-reportada; `R9.2` por ausencia de auditor de abasto independiente del prestador. Verificado sin solape de filas en las cuatro. Cascada: `estado-programa:93,97,120,194`, `modelo-decision:64,636,637,821`, `gobernanza:358,609` (este documento). |
| **1.15 · enmienda in situ** | **4/ago/2026 — ADR-55: propaga `R4.2` → `D` al bloque append-only de `hitoD-preregistro` (3 de 27 → 4 de 27); `R5.2`/`R1.2` corren completas, no se archivan.** *Mismo criterio que ADR-48 a ADR-54: el número de versión NO sube y el archivo NO se renombra.* Encargo Y, mesa #19: `R4.2` archiva `D` (ENSANUT no cruza permiso laboral con conducta preventiva, chequeo barato de la propia ficha); `R5.2` (ENUT, reducción 23.98%, IC95% [14.39%,33.57%]) propone `A` con reserva estadística, no adjudicada; `R1.2` (ENIF, 42.98% vs. `<15%` exigido) no satisface el falsador y ninguna fila de la escala propia nombra ese desenlace, no adjudicada — mismo criterio que Nota 16/`R5.1`. Verificado sin solape de filas en las tres (tercera ocurrencia de PP-3/D5-D6/A-D-R7.2 no aplicó). Cascada: `estado-programa:93,97,120,194`, `modelo-decision:64,636,637,821`, `gobernanza:354,585` (este documento). |
| **1.15 · enmienda in situ** | **4/ago/2026 — ADR-54: `sens_estatus` pasa de "sin reactivo verificado — búsqueda abierta" a "sin reactivo — búsqueda cerrada (límite de régimen)".** *Mismo criterio que ADR-48/49/50/51/52/53: el número de versión NO sube y el archivo NO se renombra.* Fundamento: el examen de descriptor de `PR #64` (`forense/notas/2026-08-04-sens-estatus-examen-descriptor.md`) recorrió los cinco instrumentos permitidos del régimen y ninguno sirve, fundado en descriptor literal. Vía de cierre: el precedente de `aversion_riesgo` en ADR-52 A (candidato examinado y descartado con argumento), no la caducidad de tres actos, que queda intacta; la condición de caducidad ante bloqueo de entorno (CP-4) no se decide aquí y sigue en mesa. Reparto re-derivado, sigue cerrando en 14, ahora 8+1+2+3. Cascada: `modelo:269-270` (tabla y control de suma), `modelo:278` (nota de reparto), `modelo:725` (condensado §12). |
| **1.15 · enmienda in situ** | **4/ago/2026 — ADR-53: se sella el veredicto D5 (INESTABLE) de P3, se descarta D6 con razón escrita, se habilita el LCA multinivel sin correrlo.** *Mismo criterio que ADR-48/49/50/51/52: el número de versión NO sube y el archivo NO se renombra.* **(A)** Veredicto D5 con sus dos precisiones: INESTABLE no es victoria del statu quo (`p3-lca-preregistro-v1_0.md` §1/§6.1·D5), y `modelo` §1.1.D no se toca (§6.2.8). **(B)** D6 se descarta con razón: la condición literal (BIC monótono decreciente, mínimo en `k=8`) se cumple, pero S1 (solo indicadores de persona) produce un mínimo interior estable en `k=4` que la falsa por el propio criterio de H-C — con el límite declarado de que la solución de S1 tampoco es estable (1/50 arranques replican en su `k_primario=3`, márgenes de 35.86/50.21 puntos sobre un rango de 7 929.85). Costo declarado: D6 habría confirmado empíricamente el reencuadre perfiles→atributos; bajo D5 sigue justificado por diseño, no por dato. **(C)** LCA multinivel de `p3-lca-preregistro-v1_0.md` §2.5 queda HABILITADO (S1 mostró que los ejes de hogar dominan la solución), no corrido, con tres condiciones: enmienda fechada al pre-registro, pre-registro propio de sesión limpia, no se corre en este acto. Requisito de salida: `estado §3·L5`, adenda a `forense/notas/2026-08-04-p3-lca-segmentacion.md`, línea en `forense/hallazgos.md` sobre pre-registros con desenlaces solapados sin regla de precedencia (P3 D5/D6, R7.2 A/D). |
| **1.15 · enmienda in situ** | **4/ago/2026 — ADR-52: dos decisiones de mesa en un acto, sellando lo que `PR #60` dejó propuesto.** *Mismo criterio que ADR-48/49/50/51: el número de versión NO sube y el archivo NO se renombra.* **(A)** El reparto de las 14 condicionales sella un par de clases con criterio explícito —"sin reactivo verificado, búsqueda abierta" / "sin reactivo, búsqueda cerrada (límite de régimen)"—, sustituyendo la clase propuesta por `PR #60`; `exposicion_violencia` y `sens_estatus` caen en abierta, `aversion_riesgo` en cerrada; condición de caducidad (tres actos sin reactivo cierran la búsqueda) escrita junto a la tabla; reparto re-derivado, sigue cerrando en 14 (8+2+1+3). **(B)** `G1a` nombra su componente en vez de desdoblarse: derivado contra archivo, solo tres reglas invocan `G1`, todas de dominio financiero (dos vía `confianza_institucional`, una vía `radio_confianza`) — la recomendación de desdoblar en seis del barrido de escritorio queda superada; `confianza_institucional[dominio] −0.60` pasa a `confianza_institucional[financiera] −0.60`, sigue `ASIGNADO`. Cascada: `modelo:267-273`, `:276`, `:388`, `:396`, `:723`; corrección in situ sobre el párrafo de ADR-49 D3 (arriba). |
| **1.15 · enmienda in situ** | **3/ago/2026 — ADR-51 (f): C-bis corre, `deferencia` (M3) pasa de NO DETERMINABLE a PROXY CON SUPUESTO DECLARADO (parcial).** *Mismo criterio que ADR-48/49/50/51: el número de versión NO sube, el archivo NO se renombra y la fecha de cabecera NO se refecha.* La cola de verificación que ADR-51 (c) declaró sin correr se ejecutó (`forense/notas/2026-08-03-cbis-deferencia-externas.md`, PR #49): LAPOP SIN REACTIVO, Latinobarómetro da PROXY (`P4NOIJ`, "Obediencia" en crianza), WVS y ENCUP quedan NO DETERMINABLE — abiertas, no agotadas. Cascada: `modelo §1.1.E` fila `H-07` se corrige en el mismo acto; ningún número de `milpa/procedencia.yaml` cambia (`deferencia` no tiene valor asignado). |
| **1.15 · enmienda in situ** | **3/ago/2026 — ADR-51: ADR-50 SE REESCRIBE.** *Mismo criterio que ADR-48/49/50: el número de versión NO sube y el archivo NO se renombra.* Sobrevive el método de ajuste y MILPA como arquitectura objetivo; se retira la calibrabilidad de los libres sin reactivo (persiste la subidentificación bajo atributos, veredicto de P2). **Corrección 22 (no 29) grados de libertad reales: 7 + 15, no 14 + 15** — las 13 reglas de `asignados_probabilidad` suman 1.0 cada una, dos derivaciones independientes coinciden (P2, esta sesión). Registra M2 (`familismo_obligacion` → PROXY CON SUPUESTO DECLARADO) y M3 (`deferencia` → NO DETERMINABLE, cola de verificación C-bis declarada) con el veredicto de C. Cierra `D-12` (su causa, la tabla `§1.1` de perfiles, la elimina el reencuadre). Costo declarado: G6 empeora bajo atributos. Cascada: `milpa/refutations.yaml:65`, `estado §L4` y la cabecera de este documento dejan de decir "POSPUESTA"; las seis citas de "29 libres" se corrigen a "22", valor viejo visible. |
| **1.15 · enmienda in situ** | **31/jul/2026 — ADR-50: el motor es un ABM ejecutable calibrado por AJUSTE, no por identificación causal; MILPA Fase 1 deja de estar POSPUESTA.** *Mismo criterio que ADR-48/ADR-49: el número de versión NO sube y el archivo NO se renombra.* Partición de los 144 números verificada contra `milpa/procedencia.yaml` y `modelo §2.2`: 90 `params_base` = niveles (transversal) · 39 probabilidades = 10 fijas (4 MEDIDO + 6 DERIVADO) + 15 con `calibrable_con` + 14 sin él · 15 coeficientes = ritmos, sin ruta viva. **Libres del ajuste: 14 + 15 = 29.** Orden vinculante (`forense/metodologia-identificacion-vs-ajuste-v0_1.md` §7): estados → abiertos → ritmos. Momentos a reproducir se declaran antes de ajustar (Bloque C). "Motor como contexto" registrada CONSIDERADA Y NO ADOPTADA, no refutada — no existía como propuesta escrita antes de este ADR. Riesgo declarado, no resuelto: identificabilidad por perfil, condicionada al chequeo de ejes demográficos y a `CAL-CONF` Fase B (ninguno de los dos existe hoy como artefacto corrido). Consecuencia mínima: `4 de 144 MEDIDO` (ADR-28.a) deja de ser titular único, cross-referenciado sin tocar números. No cierra `D-12`, no reordena Hito E, no toca reglas/tiers/ids. Deuda visible sin cascada en este acto: `milpa/refutations.yaml:65` y `estado §L4` siguen diciendo "POSPUESTA" — ver `forense/hallazgos.md`. |
| **1.15 · enmienda in situ** | **31/jul/2026 — ADR-49: tres decisiones de mesa en un acto.** *Mismo criterio que ADR-48: el número de versión NO sube y el archivo NO se renombra.* (1) **D1** — se retira `unico_calibrable_hoy` de `milpa/procedencia.yaml`; el panel rotativo de la ENOE (mismo hogar, cinco trimestres) sobrevive sin adjetivo de unicidad, como propiedad del instrumento, no como vía de calibración — cascada corregida en `modelo §2.2`, `glosario §13`, `estado §S3` y `gobernanza` (tabla de deuda, arriba). (2) **D2** — se sella la quinta clase de procedencia, `AJUSTADO` (reproduce momentos observados, no identificado causalmente; `ruta:` obligatorio; nace vacía, cero números, ningún `ASIGNADO` reclasificado). (3) **D3** — G1a (`confianza_institucional[dominio] −0.60`) declara homogeneidad de pendientes entre los seis componentes como supuesto, no medición; no se desdobla en seis; pre-registro contra la salida de `CAL-CONF` Fase A; no cierra `D-12`. **D4** (aparte, sin ADR): `milpa/procedencia.yaml` `estado:` corregido de "18/31" a "15/34", citando `forense/cobertura-motor.md`. |
| **1.15 · enmienda in situ** | **30/jul/2026 — ADR-48: R0 congela el aparato de auditoría.** *El número de versión NO sube y el archivo NO se renombra: subirlo obliga a retropropagar citas por todo el canon, que es exactamente la clase de trabajo que este ADR corta. Es una excepción declarada a ADR-36, acotada a esta enmienda.* Regla: cada sesión produce una medición o produce nada; defecto que no impide medir, una línea en `forense/hallazgos.md`; defecto que impide medir, para y reporta. Se corta: `canon/cola.yaml` sale del canon y se congela como `forense/hallazgos-congelados-2026-07-30.yaml` (40 entradas, sin añadir ni borrar; I-01 e I-13 cerradas en el mismo acto), `tests/bitacora.py --cierra` deja de invocarse (el script se queda, marcado), y el módulo de auditoría del Bloque B queda solo para artefactos que afirman algo sobre México. Se conserva: `check.py --baseline` en CI, pre-registro antes de abrir dato, marcas de procedencia y tier, `forense/` append-only. Convención de IDs `D-AAAAMMDD-HHMM` o hash corto (cierra I-13); marca `{cita-ilustrativa}` implementada en `T03` (cierra I-01). Reversión: una medición nueva sobre México archivada después de este ADR. Único ADR de su sesión, por diseño. |
| **1.15** | **30/jul/2026 — ADR-47: falsar una regla ≠ calibrar un coeficiente, cierra `forense/hallazgos-congelados-2026-07-30.yaml` D-09/D-10.** Decisiones de mesa del autor sobre `CAL-G3`: (1) `D-10` — la muestra analítica se restringe a las olas 2-3 (la ola 1 carece de "ninguna de las anteriores" y su categoría 3 opera como residual atrapa-todo que fabrica transiciones en un diseño que identifica con transiciones; el costo de restringir es hipotético — ni el escenario de tres olas apiladas alcanza el gate de falsación en ningún ICC); (2) `D-09` — el entregable se parte: la elasticidad se archiva como resultado descriptivo con banda, sin veredicto de falsación; `CAL-G3` no emite `CAL-A/B/C/X` y no entra al conteo de `RX.Y` del bloque archivado (ADR-40), porque nunca fue tarea de falsación — la duda es de magnitud, no de dirección. Fija el distingo FALSAR (¿existe? binario, exige poder en ambas direcciones) vs. CALIBRAR (¿cuánto vale? magnitud con banda, sin exigir poder refutador). No reclasifica las otras 26 fichas del perímetro — abre esa revisión sin resolver en `forense/hallazgos-congelados-2026-07-30.yaml`. No toca `milpa/procedencia.yaml`: `-0.60` sigue `ASIGNADO`. Fase C de `CAL-G3` queda desbloqueada con alcance reducido y no se corre en esta sesión (`hitoD-preregistro-v2_0.md`, Nota 9). |
| **1.14** | **30/jul/2026 — ADR-46: la unidad de contaminación es LA SESIÓN, no la máquina ni el modelo, corrige `forense/hallazgos-congelados-2026-07-30.yaml` E-03.** El texto original de E-03 declaraba "esta máquina queda INHABILITADA" tras la tanda de descargas de Hito D Fase 1 — demasiado amplio (un modelo no retiene contexto entre sesiones; una sesión nueva en la misma máquina está tan limpia como una en la nube) y demasiado estrecho (lo que contamina es qué puede LEER una sesión nueva — `data/raw/` y la bitácora de la tanda —, no dónde corrió el proceso). Fija: (1) unidad = sesión; (2) dos niveles — descarga ciega (no contamina) vs. exploración de estructura (contamina parcialmente, declarar hasta dónde); (3) condición verificable — una sesión pre-registra contra una fuente si no leyó `data/raw/` de esa fuente, ni la bitácora de su tanda, ni ningún registro de exploración de su estructura; (4) el conservador declara más exploración, no menos — aplicado a E-03, NINGUNA de las siete encuestas de la tanda fue descarga ciega, incluidas las tres (ENUT/ENCUCI/ENSANUT) que el original declaraba libres solo por no haberse descargado. E-02 verificada: sin el mismo defecto, no reescrita. Corrección ejecutada por una sesión distinta de la que bajó los datos (`forense/hallazgos-congelados-2026-07-30.yaml` E-04). |
| **1.13** | **30/jul/2026 — ADR-45: vocabulario fijo de "prueba de falsación corrida", cierra `forense/hallazgos-congelados-2026-07-30.yaml` D-05.** Tres decisiones de mesa del autor: (1) denominador — 27 (perímetro del Hito D) y 49 (reglas del motor) se reportan siempre etiquetados, nunca uno sin decir cuál; (2) un veredicto `D` cuenta como corrida, la letra viaja con el conteo; (3) Hito D (reglas), Hito C (generadores) y el ejercicio suelto de `glosario §6/§10` sobre G1a (27/jul, sin bloque archivado) se reportan como tres poblaciones separadas, nunca sumadas ni una etiquetada como otra. Corrige `§5` (fila "48 de 49" mezclaba denominadores y etiquetaba el ejercicio de glosario como "Es el Hito D"). `I-07` queda abierta: sigue siendo patrón de proceso sin test. |
| **1.12** | **30/jul/2026 — ADR-44: registro retroactivo de la publicación del repositorio (29/jul), sin ADR previo.** Mismo patrón que ADR-42(1), un piso más arriba: la función del programa entero cambió de privado-descriptivo a público-auditable sin pasar por gobernanza. Fija cuatro requisitos de salida — capa legal verificada en el árbol, re-examen de deuda "asumida a propósito" (referido a `revision-publicacion-2026-07-30.md`), README sin cifra sin test (declarado **PENDIENTE**, es F6) y regla hacia adelante contra decisiones que asuman lector interno. *(Sesión de canon F2/F4; T-README y A1-A5 quedaron fuera de perímetro por regla de sesión. Nota: las filas 1.10 y 1.11 no existen en esta tabla — mismo hueco de autorreferencia que `censo-integridad-v1_0.md` C5-03 ya señaló para 1.9; no se reconstruyen aquí, es scope ajeno a este ADR.)* |
| **1.9** | **29/jul/2026 — Corrección de RÓTULO del perímetro del Hito D (ADR-37, §4).** El registro de decisión decía "20 [FUERTE] + 5 [MEDIA-FUERTE] + 2 compuestas", tratando a `R1.4` ([FUERTE como correlación]) como una segunda regla partida. Solo `R4.3` ([FUERTE / MEDIA]) es compuesta; `R1.4` es un tier distinto, con un solo falsador. El perímetro no cambia: siguen 27. Propagado a `modelo §7` (cambio 34) y `estado §4·S2`/`§7`. *(Fila añadida 29/jul/2026 en la sesión de correcciones — faltaba; `censo-integridad-v1_0.md` C5-03.)* |
| **1.8** | **28/jul/2026 — ADR-36 adenda (c):** las series numeradas se versionan completas, con prefijo común, y el **orden de lectura pasa del nombre al cuerpo**. Los tres docs de MILPA → `milpa-whitepaper` v0.1 · `milpa-spec` v0.2 · `milpa-plan` v0.1. |
| **1.7** | **28/jul/2026 — ADR-37: se cierra el S2 más antiguo del programa.** La spec del gate de Fase 1 pasa de una condición a **tres** (reproducción · prueba de mecanismo · anti-confusión). Desbloquea `R3.4`. Colateral: `civico.voto.clientelar` degradada de `FUERTE` a `MEDIA` — su `p: 0.63` era cifra de **laboratorio** compilada como campo; se crea el campo `procedencia_p`. |
| **1.6** | **28/jul/2026 — ADR-36: nomenclatura versionada y eliminación de la ficha.** Todo archivo canónico pasa a `<nombre-estable>-v<MAYOR>.<MENOR>.md` con bloque de cabecera obligatorio; las referencias internas citan **nombre estable**. La ficha canónica **se elimina** y sus tres bloques con valor propio se absorben en `modelo` §0.1, §0.2 y §9. Un cambio típico pasa de tocar 6 archivos a **1 o 2**. |
| **1.5** | ADR-35: ampliación de alcance a crédito del lado del decisor. Motor a **49 reglas**. |
| **1.4** | ADR-34: tabla de propagación obligatoria en forenses, tras el barrido (6 de 22 fugas). |
| **1.3** | ADR-33: prohibida la diagonal en el `ENTONCES`. |
| **1.2** | **28/jul/2026 — Ronda 6, saldo de deuda documental.** ADR-31 y ADR-32. Los tres casos de retropropagación **cerrados y verificados en la fuente** (dos figuraban como hechos sin estarlo). Glosario → **v5.1**; modelo → **v2.1**; ficha **regenerada con cobertura 43/43**; los tres YAML → **v0.2.0**. Conteo de números publicado: **107 → 144**. Perímetro del Hito D fijado: **20 reglas FUERTE, no 19**. `CHECKPOINT-v2` + `mapa-y-roadmap` + `inventario-corpus` **fusionados en `ESTADO-PROGRAMA.md`**; `ADR-30.md` borrado. Reports: **30 → 31**. Conflicto **conf.07** abierto y resuelto. |
| **1.1** | **ADR-26 a ADR-30 incorporados** (los archivos sueltos se borran). El **glosario entra a la cadena de dependencia** — su omisión permitió que el modelo creara reglas sin tier. **Retropropagación** añadida al protocolo (§3.1). **Severidad S5** y **casillero de pendientes irresueltos** (§5.1). Registro de artefactos actualizado: modelo v2, glosario v5, CHECKPOINT v2, forenses como canónicos, y los siete artefactos borrados el 27/jul. **Regla de borrado** (consolidar primero). ADR-25 marcado con corrección pendiente. Deuda ampliada con cuatro generadores sin falsar, 41 reglas sin prueba, ocho refutaciones sin objeto y PD-01. |
