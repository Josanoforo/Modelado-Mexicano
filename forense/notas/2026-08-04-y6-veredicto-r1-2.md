# `R1.2` · Estabilidad → planeación larga · Encargo Y, commit 2 · corrida completa, hallazgo fuera de la escala propia, no adjudicada

Operacionalización pre-registrada: `forense/notas/2026-08-04-y2-operacionalizacion-r1-2-enif.md`, corrida sin editarla después de ver el resultado. CONSAR verificado innecesario en el commit 1 — corrido con ENIF sola.

## Embudo del denominador (ENIF 2024, tabla `tmodulo`, n=13,502)

| Filtro | n acumulado |
|---|---|
| Formal (`p3_13∈{1,2,3,4}`) | 4,078 |
| + ingreso estable (`p3_12=1`) | 2,757 |
| + ingreso suficiente (≥1 salario mínimo mensualizado 2024, $7,567 MXN) | **2,308** |

## Resultado

**% (ponderado `fac_per`) que aporta voluntariamente a Afore (`p9_1=1 ∧ p9_3=1`) O tiene seguro privado de vida/GMM (`p8_5_1=1 ∨ p8_5_2=1`) = 42.98%.**
**SE (conglomerado último, `est_dis`/`upm_dis`) = 1.58pp. IC95% = [39.88%, 46.08%].**
Σpeso del denominador: 15,256,719 personas.

## Lectura contra la escala propia (línea 47)

**El Umbral pide `<15%`. El resultado es 42.98%, con un IC95% que ni se acerca a 15% (el límite inferior, 39.88%, está casi 25 puntos por encima del umbral).** No es un caso límite como `R5.2` — es una no-satisfacción decisiva y bien medida del falsador: **el falsador falla por casi 3x, no por poco.**

**Ninguna fila de la escala propia nombra este desenlace.** `A` describe "umbral cruzado" (tasas bajas, <15%) — no ocurrió. `B` describe "tasas bajas sin control de acceso efectivo" — las tasas **no son bajas**, así que `B` tampoco aplica bajo ninguna lectura (verificado: no hay solape, ninguna de las dos se satisface). `C` ("exigiría separar acceso de uso en el mismo panel") describe un diseño más fino que no hizo falta para llegar a este resultado — el Umbral, tal como está escrito, ya es evaluable y ya dio un resultado limpio, así que `C` no es el caso aplicable tampoco. `D` ("improbable, CONSAR publica aportación voluntaria") predecía que la fila `D` sería poco probable **porque el dato existiría** — y en efecto existió (via ENIF), pero el resultado no fue inejecutable, fue un no-cruce claro; `D` tampoco nombra ese desenlace.

**Consistente con `R5.2`: dos de las tres fichas de este acto producen un resultado que la propia escala no anticipó** — no un hueco de dato, un hueco de diseño del pre-registro: ninguna fila nombra "el falsador corrió limpio y no se satisfizo, la regla original sobrevive". Se declara aquí como patrón, no se inventa una fila nueva ni se retoca la ficha (fuera de perímetro de este encargo).

## Control de acceso efectivo — ejecutado igual, para no dejarlo a medias

Entre quienes no cumplen el numerador (Σpeso 8,699,480): motivos clasificados por la regla pre-registrada en el commit 1 (antes de ver las frecuencias) —
- **Acceso** (no le queda dinero / no sabe cómo o qué es / desconoce ventajas / no se lo ofrecieron / muy caros): 8,866,510 en peso combinado (`p9_4`+`p8_3`).
- **Decisión** (ahorra de otra forma / no confía / no lo necesita o no le interesa): 3,670,521 en peso combinado.
- Resto: vacío (afore ya inexistente, `p9_1=2`, o `p3_13` sin exigir la pregunta) u "otro"/jubilado.

El motivo dominante entre quienes no participan **es de acceso, no de decisión** — dato que hubiera importado si el Umbral se hubiera cruzado (para distinguir fila `A` de `B`); no cambia la conclusión principal porque el Umbral no se cruzó.

## Consecuencia sobre el motor, declarada, no ejecutada

Si mesa adjudica que este resultado equivale a "regla no refutada" (lectura más cercana al espíritu del Umbral, aunque sin fila que la nombre): **no habría partición** de "permite"/"produce" (línea 45) — a diferencia de lo que hubiera pasado si `A` se hubiera satisfecho. `dinero.planeacion.formal_estable` (`modelo-decision-v4_0.md:342`) no se toca en este acto de cualquier forma — fuera de perímetro.

**No se archiva.** Si mesa decide que este desenlace equivale a una fila (o amerita ampliar la escala propia con una fila nueva), se registra como entrada fechada en el bloque append-only, no editando esta nota.

Script y datos intermedios: `run_r1_2.py` (ejecutado sobre `enif2024_csv.zip`, tabla `conjunto_de_datos_tmodulo_enif2024.csv`).
