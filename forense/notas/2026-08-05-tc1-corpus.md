# ENCARGO TC-1 — arreglo de T02 y nacimiento del test de corpus (2026-08-05/06)

Worktree `/home/pc0/wt-tc1-010528-2`, rama `tc1-corpus-010528-2`, base
inicial `origin/main` = `58a307c` (verificado: `git log -1 origin/main`).
Nota: el encargo declaraba base `35314c9`; `origin/main` había avanzado
por el merge de PR #142 (desc1-descarga). No fue PARO — se refrescó y se
comprobó el delta: `git diff --stat 35314c9..58a307c` solo toca
`forense/hallazgos.md` (+2 líneas) dentro de este perímetro;
`tests/check.py`, `tests/corpus.py` (no existía) y `tests/baseline.json`
quedaron intactos en ese rango. Ejecutado en la madrugada del 6/ago
(timestamps de sistema en 01:xx) sobre un encargo fechado 5/ago — de ahí
el desfase de un día entre el nombre de este archivo (literal del
encargo) y la fecha real de ejecución.

**`origin/main` avanzó una SEGUNDA vez durante este mismo acto**, justo
antes del cierre (§ "Segundo avance de origin/main", más abajo) — PR #143,
la reconciliación de CONF-17. Se incorporó por merge antes de escribir
los conteos finales de esta nota; todo lo que sigue usa esa base ya
mergeada salvo donde se indique explícitamente "antes del segundo merge".

## ARRANQUE

- **Repo/worktree**: clon base en `/home/pc0/Modelado-Mexicano`, aparcado en
  `sesion/cal-conf-faseb-pos4-envipe-paso1` (NO se tocó). `git worktree add
  "$HOME/wt-tc1-$(date +%H%M%S)-$$" -b tc1-corpus-<suf> origin/main` con
  sufijo propio para no colisionar con otra sesión sobre el mismo home —
  precedente citado explícitamente por el encargo: la colisión de CONF-17
  (`/home/pc0/wt-conf17`, commit local `34005c2`, confirmado presente vía
  `git worktree list`, NO se entró — y que, según se supo después, ya era
  en ese momento el resultado de dos sesiones concurrentes sobre ese mismo
  worktree; ver más abajo). `git worktree add` lanzó dos veces `error:
  could not write config file .git/config: Device or resource busy` (exit
  code 0 igual, HEAD y branch quedaron correctos) — consistente con otra
  sesión escribiendo al mismo tiempo sobre el filesystem compartido;
  verificado después con `git status`/`git diff origin/main` que el
  worktree quedó limpio, sin corrupción.
- **SHA**: ver párrafo superior. `origin/desc1-descarga` ya no existe en
  origin (`git fetch` reportó `[deleted] -> origin/desc1-descarga`) — el
  encargo la daba por "empujada, PR abierto"; en realidad PR #142 ya había
  mergeado y GitHub borró la rama. No afecta el perímetro de este acto.
- **data/raw**: ausente en el worktree recién creado (gitignorado, un
  `git worktree add` no hereda symlinks no versionados). Se enlazó al
  mismo corpus compartido que usan `wt-desc1`/`wt-ver1`: `ln -s
  /home/pc0/mm-corpus/raw data/raw`. Confirmado con `ls -la data/raw`.
- **Entorno** (cuatro valores crudos):
  ```
  $ echo "${CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE:-sin_variable}"
  sin_variable
  $ ls data/raw/ | head -1
  BD_ENCUCI2020_dbf.zip
  $ du -sh data/raw/
  6.3G	data/raw/
  $ python3 --version
  Python 3.14.4
  ```
  Sin PARO.
- **Espejo**: ninguna cifra de esta nota viene de un espejo del proyecto —
  todas se derivaron en este worktree, con el comando a la vista.

## PASO 0 — reproduce el rojo antes de arreglarlo

```
$ python3 tests/check.py --baseline
════════════════════════════════════════════════════════════════════════
  VERIFICACIÓN DEL CORPUS
════════════════════════════════════════════════════════════════════════
  [ ok ]  T01 fuente única de verdad
Traceback (most recent call last):
  File "/home/pc0/wt-tc1-010528-2/tests/check.py", line 1293, in <module>
    sys.exit(main())
  File "/home/pc0/wt-tc1-010528-2/tests/check.py", line 1253, in main
    fn()
  File "/home/pc0/wt-tc1-010528-2/tests/check.py", line 108, in t02_duplicates
    by_hash[hashlib.md5(io.open(p, "rb").read()).hexdigest()].append(rel(p))
IsADirectoryError: [Errno 21] Is a directory: '/home/pc0/wt-tc1-010528-2/data/raw/R7.3_PUB_Bienestar'
```
Exit code 1. Reproduce exacto lo declarado por DESC-1 (mismo directorio,
mismo traceback, misma línea) — y, se supo después, lo mismo que
`corrida B` de CONF-17 reportó de forma independiente ese mismo día (ver
`forense/hallazgos.md`, entrada de reconciliación 2026-08-06). Tres actos
distintos, mismo defecto, encontrado sin coordinación. Valores crudos:
**0 FAIL · 0 WARN reportados** — el script truena dentro de T02 antes de
imprimir ningún resumen; T01 es el único chequeo que llegó a imprimir
`[ ok ]`.

Mecanismo, con el comando literal del encargo:
```
$ python3 -c "
import glob,os
ps=glob.glob(os.path.join('data/raw','**','.'),recursive=True)
dirs=[p for p in ps if not os.path.isfile(p)]
print('rutas devueltas por el glob:',len(ps))
print('de esas, DIRECTORIOS:',len(dirs))
for d in dirs[:10]: print(' ',d)
"
rutas devueltas por el glob: 88
de esas, DIRECTORIOS: 88
  data/raw/.
  data/raw/encig2017/.
  data/raw/encig2015/.
  data/raw/mociba2025/.
  data/raw/R7.3_PUB_Bienestar/.
  data/raw/eder2011/.
  data/raw/enasic2022/.
  data/raw/R1_1_AGROASEMEX/.
  data/raw/envipe2015/.
  data/raw/edr2024/.
```
Lectura del código (`tests/check.py:99-114`, `t02_duplicates`): el glob
real que usa el chequeo es `os.path.join(ROOT, "**", "*.*")` — cualquier
ruta cuyo *basename* contenga un punto matchea, sea archivo o carpeta.
`R7.3_PUB_Bienestar` matchea por el punto en `R7.3`. La línea 108 abre
cada match con `io.open(p, "rb")` sin comprobar `os.path.isfile` antes —
truena en el primer directorio que encuentra.

## PASO 1 — el arreglo mínimo (commit `fc38f80`)

Guarda `if not os.path.isfile(p): continue` antes de tratar el match como
archivo (`tests/check.py:107-108`).

```
$ python3 tests/check.py --baseline
...
  43 FAIL · 95 WARN
────────────────────────────────────────────────────────────────────────
  LÍNEA BASE: ROJO — 25 entradas nuevas frente a tests/baseline.json
  (HEAD congelado 837d5fe56d523615259efb77c17c84fee86ac684)
  · T02: 23 entradas (22 colisión por nombre + 1 por contenido, todas bajo data/raw)
  · T16: 2 entradas (self-check: canon declara "18 FAIL·95 WARN", real da "41 FAIL·95 WARN")
```
El script ya no truena. Corre completo por primera vez en esta sesión.

## PASO 2 — T02 deja de recorrer data/raw (commit `c9d37d8`)

Exclusión añadida junto a las existentes: `".git" in p or "/tests/" in p or
"/data/raw" in p`, con el razonamiento (INEGI empaqueta
`conjunto_de_datos.csv`/`diccionario_datos.csv` en casi todos sus zips de
microdato; `by_hash` duplica, peor, lo que ya hace `manifiesto.py
--verifica` por sha256 declarado) citado en el propio commit.

Verificación explícita del symlink, pedida por el encargo — tres rutas tal
como llegan al filtro:
```
$ python3 -c "... glob sobre ROOT, imprime 3 matches de data/raw ..."
'/home/pc0/wt-tc1-010528-2/data/raw/mujer_enadid23.pdf'
'/home/pc0/wt-tc1-010528-2/data/raw/cuest_modulo_envipe2018.pdf'
'/home/pc0/wt-tc1-010528-2/data/raw/Censo2020_cuest_ampliado.pdf'
```
`glob` no resuelve el symlink al target (`/home/pc0/mm-corpus/raw`) — las
rutas conservan el prefijo literal `.../data/raw/...`. La condición
`"/data/raw" in p` del encargo funciona sin ajuste.

```
$ python3 tests/check.py --baseline
...
  [ ok ]  T02 duplicados nombre/contenido
  [ ok ]  T16 T-SUITE-SELF-CHECK
  18 FAIL · 95 WARN
────────────────────────────────────────────────────────────────────────
  LÍNEA BASE: VERDE — nada nuevo frente a tests/baseline.json
  (HEAD congelado 837d5fe56d523615259efb77c17c84fee86ac684)
```
Delta contra paso 1: **-25 FAIL** (23 de T02 + 2 de T16, que dependía de
T02), **WARN sin cambio (95)**. T16 vuelve a `[ok]` como efecto colateral
correcto: los 18 FAIL que canon declara vigentes ahora sí coinciden con
los 18 FAIL reales. 18 FAIL · 95 WARN coincide con el valor de control
que cita el encargo (no autoritativo) — derivado aquí de forma
independiente, no copiado; y coincide también, de forma más fuerte, con
lo que `canon/estado-programa-v1_10.md:220` y `canon/gobernanza-v1_15.md:760`
ya declaraban como vigente antes de este acto.

## PASO 3 — nace `tests/corpus.py` (commit `0076d3c`)

Diseño: entrada→archivo ya lo cubre `manifiesto.py --verifica`. Nada
cubría archivo→entrada — el defecto de PR #77 (seis payloads huérfanos en
un `data/raw` local, dos actos sin notarlo), que DESC-1 (PR #142) declaró
literalmente como "no lo atrapa ningún test". `tests/corpus.py` es un
script independiente: no importa `check.py`, no toca `tests/baseline.json`,
no tiene `--freeze`. Reusa solo las funciones de resolución de
`manifiesto.py` (`repo_root`, `rutas`, `leer_manifiesto`, `resolver_raiz`,
`RAIZ_INTEGRADA`) — lectura pura.

C1 (huérfano), C2 (duplicado por contenido, leído del YAML) y C3 (entrada
sin archivo, solapa con `--verifica`/AUSENTE, declarado como solape) —
las tres en WARN, ninguna en FAIL: un hallazgo del corpus no debe poder
gatear el push de un acto ajeno, el mismo modo de falla que detuvo a
CONF-17 por carpetas que no creó.

### Addendum de mesa (2026-08-06) — límite declarado de C1

Recibido a media ejecución de este paso. Texto verbatim, ya incorporado a
la cabecera de `tests/corpus.py`:

> C1 no ve las raíces no integradas. Un resultado limpio de C1 no
> significa corpus sin huérfanos: significa data_raw sin huérfanos.

Confirmado empíricamente antes de escribirlo (no solo declarado): en este
worktree, el archivo _data/raices.local.yaml_ NO existe (gitignorado, por
máquina) — solo el clon base `/home/pc0/Modelado-Mexicano` lo tiene, de
los seis worktrees inspeccionados. Sin ese archivo, `descargas_mx` no
resuelve aquí y C1 no puede ver nada bajo esa raíz — que es precisamente
donde DESC-1 encontró la canasta nacional correcta (carpeta Windows
Downloads/Descargas MX).

**Distribución de raíces, derivada del manifiesto, sin resolver nada**
(`data/manifiesto.yaml`, cifras **post segundo merge**, ver más abajo —
471 entradas totales):

| corte | data_raw (explícito) | descargas_mx | sin campo `raiz` (⇒ data_raw) | total |
|---|---|---|---|---|
| todas las entradas | 28 | 29 | 414 | 471 |
| con payload (`sha256`) | 28 | 29 | 410 | 467 |
| sin payload (nota/doc) | 0 | 0 | 4 | 4 |

Universo efectivamente visible a C1 en este entorno: **438 de 467**
entradas con payload (data_raw explícito + sin campo). Las **29** bajo
`descargas_mx` quedan fuera del alcance de C1 tal como está escrito —
estructural, no un bug: aunque este worktree tuviera _raices.local.yaml_
configurado, C1 seguiría sin barrer esa raíz porque no está en su diseño
(solo entiende `data_raw`).

La misma raíz no configurada afecta a C3 de forma distinta (incidental,
no estructural): de los 467 con payload, C3 evaluó **438** y omitió
**29** por "raíz no configurada en este entorno" (mismo criterio de
`manifiesto.py --verifica`: "no configurada" no es el mismo hecho que
"archivo ausente"). En una máquina con _raices.local.yaml_ apuntando a
Descargas MX, C3 SÍ evaluaría esas 29 — a diferencia de C1, que nunca las
vería aunque la raíz estuviera configurada. C2 no tiene este límite: lee
puro YAML, cubre las 467 con payload sin excepción.

### Conteos crudos sobre el corpus real, POST segundo merge de origin/main

```
$ python3 tests/corpus.py
  manifiesto: 471 entradas totales, 467 con payload (sha256)
  por raíz, tal cual declarado: data_raw=28 · descargas_mx=29 · SIN CAMPO raiz (=data_raw por convención)=410
  universo visible a C1 (data_raw efectivo): 438 de 467

  [warn]  C1 huérfanos  (50 warn)  -- alcance: solo data_raw
  [ ok ]  C2 duplicado por contenido
  [ ok ]  C3 entrada sin archivo

  50 WARN  (C1=50 · C2=0 · C3=0)
```

**Antes del segundo merge, sobre 461 entradas: C1 daba 60.** Los 10 que
desaparecieron entre esa corrida y esta son EXACTAMENTE los que
`forense/hallazgos.md` atribuye a la reconciliación de CONF-17 ("10
entradas (6 de B + 4 de A) re-registradas... contra los archivos ya
presentes en el corpus compartido"): las 4 de `R1_1_AGROASEMEX/*`, y una
cada una de `R2.1_ECCO`, `R4_1_SESTAD_ESTAD`, `R7.3_PUB_Bienestar`,
`R7.4_R7.5_ACLED_HDX`, `R8.1_contraloria_social`,
`R9_1_ENSANUT_utilizadores` — siete carpetas, diez archivos. Ninguna
desaparición es efecto de este acto; las diez vinieron con el merge, no
con `tests/corpus.py`. Verificado por diff directo de las dos listas, no
solo por el número.

**C1 = 50. Lista completa, sin resumir** (ninguna interpretada ni
adjudicada — CONTADOR: cero):

```
data/raw/DescargaMasivaOD_582026_171540_NACIONAL_7930url.xml
data/raw/eder2025/889463930242.pdf
data/raw/enaproce2018/ejem_base_micro_ciega_csv.zip
data/raw/enaproce2018/ejem_base_micro_ciega_dta.zip
data/raw/enaproce2018/ejem_base_pyme_ciega_csv.zip
data/raw/enaproce2018/ejem_base_pyme_ciega_dta.zip
data/raw/engasto2012/gasto_dbf.zip
data/raw/engasto2012/gasto_de_consumo_ajustado_constante_dbf.zip
data/raw/engasto2012/gasto_de_consumo_ajustado_constante_dta.zip
data/raw/engasto2012/gasto_de_consumo_ajustado_constante_sav.zip
data/raw/engasto2012/gasto_de_consumo_ajustado_dbf.zip
data/raw/engasto2012/gasto_de_consumo_ajustado_dta.zip
data/raw/engasto2012/gasto_de_consumo_ajustado_sav.zip
data/raw/engasto2012/gasto_dta.zip
data/raw/engasto2012/gasto_mujeres_dbf.zip
data/raw/engasto2012/gasto_mujeres_dta.zip
data/raw/engasto2012/gasto_mujeres_sav.zip
data/raw/engasto2012/gasto_sav.zip
data/raw/engasto2012/hogar_dbf.zip
data/raw/engasto2012/hogar_dta.zip
data/raw/engasto2012/hogar_sav.zip
data/raw/engasto2012/persona_dbf.zip
data/raw/engasto2012/persona_dta.zip
data/raw/engasto2012/persona_sav.zip
data/raw/ennvih/doc/ehh02cb_b2.pdf
data/raw/ennvih/doc/ehh02cb_b3a.pdf
data/raw/ennvih/doc/ehh02cb_b3b.pdf
data/raw/ennvih/doc/ehh02cb_bc.pdf
data/raw/ennvih/doc/ehh02q_b2.pdf
data/raw/ennvih/doc/ehh02q_b3a.pdf
data/raw/ennvih/doc/ehh05cb_b2.pdf
data/raw/ennvih/doc/ehh05cb_b3a.pdf
data/raw/ennvih/doc/ehh05cb_b3b.pdf
data/raw/ennvih/doc/ehh05cb_bc.pdf
data/raw/ennvih/doc/ehh05q_b2.pdf
data/raw/ennvih/doc/ehh05q_b3a.pdf
data/raw/ennvih/doc/ehh09cb_b2.pdf
data/raw/ennvih/doc/ehh09cb_b3a.pdf
data/raw/ennvih/doc/ehh09cb_b3b.pdf
data/raw/ennvih/doc/ehh09cb_bc.pdf
data/raw/ennvih/doc/ehh09q_b2.pdf
data/raw/ennvih/doc/eloc02cb_bcc.pdf
data/raw/ennvih/doc/eloc02q_bcc.pdf
data/raw/ennvih/doc/eloc05cb_bcc.pdf
data/raw/ennvih/doc/eloc05q_bcc.pdf
data/raw/ennvih/doc/eloc09cb_bcc1.pdf
data/raw/ennvih/doc/eloc09cb_bcc2.pdf
data/raw/ennvih/doc/eloc09q_bcc1.pdf
data/raw/inegi_mmsi_2016/cuestionario_mmsi_2016.pdf
data/raw/inegi_mmsi_2016/manual_entrevistador_mmsi_2016.pdf
```

Desglose por categoría (conteo programático, no a mano): XML de canasta
= 1 · `eder2025/*` = 1 · `enaproce2018/*` = 4 · `engasto2012/*` = 18 ·
`ennvih/doc/*` = 24 · `inegi_mmsi_2016/*` = 2. Suma 50.

No adjudicado, pero con conexión factual verificable a hallazgos ya
declarados en otras notas (no una interpretación nueva, una cita):

- El `eder2025/889463930242.pdf` y los 4 `enaproce2018/ejem_base_*`
  coinciden exactamente con la tabla de "rechazados por deduplicación de
  sha256" de `forense/notas/2026-08-05-desc1-descarga.md` §TRAMO A — su
  ausencia del manifiesto es el comportamiento **por diseño** de
  `--registra` (no duplica por contenido), no un caso nuevo del defecto
  de PR #77. La huella del hecho (id existente que sí tiene el mismo
  sha256) queda en esa nota, no en el manifiesto.
- Los 18 `engasto2012/*` (gasto · gasto_de_consumo_ajustado[_constante] ·
  gasto_mujeres · hogar · persona, × dbf/dta/sav) — verificado
  directamente contra `data/manifiesto.yaml` (no solo citado): las 18
  entradas correspondientes tienen id con prefijo `engasto_2012_*` pero
  su campo `archivo` apunta a `engasto2013/...`, no a `engasto2012/...`
  (p.ej. `engasto_2012_gasto_dbf` → `archivo: engasto2013/gasto_dbf.zip`).
  Es la misma relación de contenido idéntico que documenta
  `2026-08-05-desc1-descarga.md` §TRAMO B ("ENGASTO 2013 reusa 18 de sus
  21 archivos... de ENGASTO 2012"), pero el id nombrado "2012" quedó
  registrado apuntando al path físico de 2013 — deja las 18 copias
  físicas bajo `engasto2012/` (mismo sha256, contenido idéntico) sin
  ningún `archivo` que las declare. Las otras 10 entradas relacionadas
  (`vivienda`×3, `lugar_compra`×3, `engasto12_fd`) sí apuntan a su propio
  path 2012 y no aparecen en esta lista. Por qué el id dice "2012" y el
  path dice "2013" no se investigó más allá de este hecho verificado —
  no se explica el origen, se reporta.
- `R7.3_PUB_Bienestar/padron_unico_bienestar.csv` — el mismo directorio
  que hizo tronar T02 en el Paso 0 — **era** huérfano en la primera
  corrida de C1 (antes del segundo merge); dejó de serlo cuando CONF-17
  lo registró. Ya no aparece en la lista de 50 de arriba. Se deja
  constancia aquí porque conecta directamente el bug de nombre-con-punto
  del Paso 0 con un hallazgo real de C1, aunque ese hallazgo específico
  ya no esté vigente al cierre de este acto.
- Los 27 restantes (XML de canasta = 1 · `ennvih/doc/*` = 24 ·
  `inegi_mmsi_2016/*` = 2) **no tienen conexión declarada encontrada** en
  esta sesión — quedan como huérfanos sin explicación, la forma más pura
  del defecto de PR #77 que C1 fue construido para atrapar. El XML
  (`DescargaMasivaOD_582026_171540_NACIONAL_7930url.xml`) fue copiado al
  corpus por DESC-1 como artefacto de índice, no como payload de una
  regla — no se adjudica si eso significa que no debería tener entrada,
  o si sí debería y nunca se le dio una.

C2 = 0, C3 = 0 (sobre las 438 entradas evaluables en este entorno).

## PASO 4 — el baseline, sin `--freeze`

Los cuatro cortes de `tests/check.py --baseline`, crudos — todos estables
antes y después del segundo merge (verificado, no asumido: se re-corrió
`check.py --baseline` tras incorporar PR #143 y dio exactamente los
mismos números, una vez corregida una autorreferencia — ver nota abajo):

| corte | FAIL | WARN | LÍNEA BASE vs `tests/baseline.json` (837d5fe) |
|---|---|---|---|
| Paso 0 (antes, sin arreglo) | truena antes de contar (0 reportado) | 0 reportado | no llega a compararse |
| Paso 1 (guarda isfile) | 43 | 95 | ROJO — 25 entradas nuevas |
| Paso 2 (+ exclusión data/raw) | 18 | 95 | VERDE — nada nuevo |
| Paso 3 (+ tests/corpus.py), pre segundo merge | 18 | 95 | VERDE — nada nuevo |
| Paso 3, post segundo merge (PR #143) | 18 | 95 | VERDE — nada nuevo |

`tests/corpus.py` aparte, no mezclado en la tabla anterior porque no
participa del mismo mecanismo: **50 WARN** (C1=50 · C2=0 · C3=0) post
segundo merge — **60 WARN** (C1=60) antes de incorporarlo, por las 10
entradas que registró CONF-17 (ver Paso 3). **0 FAIL** en ambos casos,
por diseño: el script no tiene concepto de FAIL.

**Nota de autocorrección, declarada**: la primera redacción de esta nota
citaba el nombre del archivo de raíces (§Addendum de mesa) entre
backticks, dos veces — T03 (referencias colgantes) escanea todo archivo
`.md` del repo buscando exactamente ese patrón (nombre-entre-backticks
terminado en `.md`/`.yaml`) y lo marca si el archivo citado no existe; el
de raíces, en este checkout, genuinamente no existe (gitignorado, por
diseño). Eso subió WARN de 95 a 97 y tumbó T16 a FAIL
(canon declara 95, la corrida real daba 97) — un efecto secundario real
de esta propia nota sobre la suite que este acto existe para devolver a
VERDE. Corregido antes de cerrar (backticks retirados de esas dos
menciones, itálica en su lugar) y reverificado: `check.py --baseline`
vuelve a 18 FAIL · 95 WARN · VERDE. Se declara en vez de omitirse.

**`--baseline` con el baseline vigente, sin tocarlo: VERDE**, ya desde el
Paso 2, y se mantiene VERDE después de incorporar el segundo avance de
`origin/main` — el arreglo mínimo (Paso 1) más la exclusión (Paso 2)
devuelven la suite exactamente al estado que `tests/baseline.json`
(congelado en `837d5fe`) ya declaraba, y ese estado no dependía de nada
que CONF-17 haya tocado. `tests/corpus.py` no participa de ese mecanismo
en absoluto.

**Qué haría falta congelar, y por qué — mesa decide, no este acto:**

1. `tests/baseline.json` (el de `check.py`): **no hace falta tocarlo
   ahora** — el estado post Paso 1+2 (estable pre y post segundo merge)
   ya coincide exactamente con lo congelado. Lo único desactualizado es
   el propio puntero "HEAD congelado `837d5fe`", varios commits atrás en
   la historia (antes de PR #140, PR #142, PR #143 y este acto) —
   refrescarlo a un HEAD más reciente sería housekeeping de procedencia,
   no una corrección de conteos, y sigue siendo decisión de mesa igual
   que cualquier `--freeze`.
2. `tests/corpus.py`: no tiene mecanismo de congelado por diseño de este
   acto, y esta nota no propone crearle uno. El propio conteo de C1 ya
   se movió una vez dentro de esta sesión (60→50) por una acción externa
   (CONF-17), sin que este acto hiciera nada — señal de que, si mesa
   quisiera vigilar C1 a través del tiempo, un snapshot fijo se
   desactualizaría rápido. No se diseña esa vigilancia aquí.

## Segundo avance de `origin/main` (PR #143, reconciliación CONF-17)

Detectado al hacer `git fetch origin main` justo antes de tocar
`forense/hallazgos.md`, siguiendo la instrucción del encargo de refrescar
antes de escribir en un archivo append-only propenso a colisión.
`origin/main` pasó de `58a307c` a `f31a453` (merge de PR #143). Diff
contra mi perímetro:

```
$ git diff --stat 58a307c..f31a453 -- tests/check.py tests/corpus.py \
    forense/hallazgos.md tests/baseline.json data/manifiesto.yaml
 data/manifiesto.yaml | 8937 +++++++++++++++++++++++++++++---------------------
 forense/hallazgos.md |    2 +
 2 files changed, 5218 insertions(+), 3721 deletions(-)
```

`tests/check.py`, `tests/corpus.py` y `tests/baseline.json`: intactos.
`data/manifiesto.yaml` cambió sustancialmente (10 entradas nuevas,
reescritura de formato en el resto) — fuera de mi perímetro de edición
(no lo toco), pero dentro de lo que `tests/corpus.py` LEE. Se decidió
mergear `origin/main` antes de cerrar en vez de reportar los conteos de
C1/C2/C3 contra un manifiesto ya superado: presentar 60 huérfanos como
cifra final cuando 10 de ellos ya estaban resueltos en `origin/main`
hubiera sido derivar una cifra de un estado obsoleto, el mismo defecto
que la regla del "espejo" prohíbe por otra vía. `git merge origin/main`
(commit `5096c84`) fue limpio, sin conflicto — `data/manifiesto.yaml` no
tenía cambios en mi rama, todo el diff vino de origin. Confirmado también
que `wt-conf17` no se tocó en ningún momento de este acto: la
reconciliación la hizo otro acto, sobre ese worktree, y llegó a mí solo
como commit ya mergeado en `origin/main`.

## Lo que este acto no hizo

No tocó `data/manifiesto.yaml` (lo leyó, no lo escribió — el único cambio
que trae es el merge de PR #143, hecho por otro acto). No entró a
`/home/pc0/wt-conf17`. No renombró ninguna carpeta del corpus. No
descargó nada. No corrió `--freeze`. No adjudicó ninguna de las 27
hipótesis ni promovió ninguno de los 15 (**CONTADOR: cero**, declarado).
La lista de 50 huérfanos de C1 (post segundo merge) se entrega completa
arriba, no resumida, no interpretada más allá de las conexiones
factuales citadas (con su propia nota de origen).
