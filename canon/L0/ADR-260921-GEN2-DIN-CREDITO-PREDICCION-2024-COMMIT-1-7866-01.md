**ADR-260921-GEN2-DIN-CREDITO-PREDICCION-2024-COMMIT-1-7866-01** (raíz de acto: `7866` = 4 hex del commit de 0-bis `78664b31`; no se renumera al fusionar), `ACTO GEN2-DIN-CREDITO-PREDICCION-2024-COMMIT-1 · CRÉDITO PREDICE ANTES DE ABRIR: PERSISTENCIA GANA EL BACKTEST EN 7 DE 9 CONDUCTAS`, 21/sep/2026, entorno **CAJA** (`ENTORNO-DERIVADO = CAJA`, corpus montado, `sin_variable`, red 200), Sonnet 5 por mandato de mesa (presupuesto), sin sub-agentes ejecutores (dos forks de reconocimiento, solo lectura), **MODO ABIERTO hasta congelar**. Encargo archivado verbatim (A.3) con sello de cuerpo: `forense/encargos/2026-09-21-GEN2-DIN-CREDITO-PREDICCION-2024-COMMIT-1.md` + `.cuerpo.sha256` (`b3c62b4a…`). Sin compuerta de merge declarada; el ARRANQUE (0.a-0.d) encontró la base 159 commits atrás — se fusionó `origin/main` antes de crear la rama (no PARO, D-10 §0.a) y de nuevo 15 commits antes de este cierre (PR #986, ajeno).

**P0 (FP-…-ff56-01/02, resueltas por dirección desde el propio encargo):** `CALC-DIN-CREDITO-PISOS-ENIF2021-RECORTE1870-0001` — recorte 18-70 de ENIF 2021, mismo medidor de `#943` importado por bytes (sha256), sólo envuelve el filtro de edad y el `PREFIJO`; oro (`EDAD_MIN=18,MAX=96` reproduce `#943` RESULT por RESULT, 0 diffs / 2 939) verificado antes de la corrida real; FILAS-PERSONAS 13 554 (18+, `#943`) → 12 406 (18-70). `CALC-DIN-CREDITO-K2-BANCARIA-HISTORIA-0001` — descriptivo congelado en `#975` corrido; ENIF 2024 queda `RESERVADA-NO-MEDIDA` (verificado en el propio RESULT). Las dos FP quedan cerradas.

**P1/P2 (emisiones)/P3 — `CALC-DIN-CREDITO-PREDICCION-2024-EMISIONES-0001`:** nunca abre ENIF 2024. Nueve conductas entran (`K1`, `K2-DEPARTAMENTAL/NOMINA/AUTOMOTRIZ`, `K3`, `K4A-AUTOEXCLUSION`, `K4B-OFERTA`, `K5`, `K6-P-TENEDORES` — unidad P consistente, decisión declarada en spec.md §1 sobre la ambigüedad de «K6» del encargo). Cuatro contendientes por `(conducta, celda)` importados por bytes de `tools/encig_origen_movil.py` (`#972`): `PERSISTENCIA` (siempre), `TENDENCIA-2/3/SERIE` sólo con `>= 3` olas comparables tras excluir celda rara (4 casos reales, IC de logit indefinido) — umbral propio, más estricto que el mínimo de 2 de `-0972` para T2/T-serie, para no ajustar una recta degenerada; un test sintético (caso `DOS-OLAS`) atrapó que el primer medidor no lo implementaba pese a declararlo, antes de sellar nada. P3 (ensayo y oro) integrado: backtest sobre las cuatro olas reales ya selladas (2012/2015/2018/2021), «el procedimiento corrido como si cada ola fuera la nueva» — **`PERSISTENCIA` tiene el MAE de backtest más bajo en 7 de 9 conductas**; sólo `K1` (2.02 vs 2.03 pp) y `K6-P-TENEDORES` favorecen `TENDENCIA-3`, por márgenes que esta spec deliberadamente no llama «materiales» (sin umbral inventado). Declarado==emitido verificado byte a byte (11 062 ids); sin NaN/inf.

**P2 (adjudicación) — `CALC-DIN-CREDITO-PREDICCION-2024-ADJUDICACION-0001`, CONGELADO SIN CORRER (F3, E.6):** único código autorizado a tocar crédito de ENIF 2024; guardia de una variable (`abre_conducta_2024`, `ReservaRota` fuera de la lista blanca de 9 conductas, antes de tocar el ZIP — probado con ruta inexistente, distingue `ReservaRota` de `FileNotFoundError`); mapa de columnas 2021→2024 desde el descriptor (A.15: `diccionario_datos_tmodulo_enif2024.csv` + catálogos, cero microdato) con el hallazgo de nemónicos desplazados documentado campo por campo (`P6_14→P6_13`, `P6_15→P6_14` —mismo nombre, otra pregunta—, `P6_17→P6_16`, `P6_4_k→P6_3_k`, `EDAD→edad_v`, `FAC_ELE→fac_per`, `P3_1_1→niv+gra`); `TMODULO` solo basta, sin cruzar `TSDEM` (verificado contra el encabezado real de 398 columnas). Probado exhaustivamente sobre ZIP sintético (882 ids, declarado==emitido, sin NaN/inf); nunca corrido contra `data/raw/enif2024_csv.zip` real; sin `ejecucion.json`. La regla de adjudicación (comparar contra las emisiones con veredicto `VENCE`/`PROPUESTA-CON-RESERVA`/`NADIE-VENCE`) se declara explícitamente diferida a `commit_3a`/COMMIT-3 (otra sesión): forzar el contrato de `tools/duelo/cruces_familia.py::adjudica()` —construido sobre réplicas bootstrap por celda— sobre esta serie —propagación analítica en logit— habría sido incoherente con el método ya sellado.

**CONTADOR:** `N_corridas_selladas` +3 (`K2-BANCARIA-HISTORIA-0001`, `PISOS-ENIF2021-RECORTE1870-0001`, `PREDICCION-2024-EMISIONES-0001`); `cuenta_gen2 = SI` en las tres (firma de contador de mesa, `#975`/heredada); **no adopta**; `-ADJUDICACION-0001` no cuenta aún (sin `ejecucion.json`, `cuenta_gen2 = NO` declarado en su spec). Registro (`registro --verifica --escribe`) verificado sin footprint sobre corridas ajenas en cada uno de los tres registros (diff 0 líneas contra `HEAD`/`origin/main` excluyendo las filas propias); las tres corridas replay `REPRODUCE`/`IDENTICO`.

**Hallazgo (una línea, `forense/hallazgos.md`):** el sidecar de `#943` (`DIN-CREDITO-PISOS-ENIF2021-spec-v1_0.md.sha256`) usa la convención de nombre vieja que `sella_sha256.py` ya no reconoce — no es un sello roto, sólo el nombre; fuera de perímetro re-sellarlo.

`tests/check.py --rapido`: VERDE, 0 FAIL, 289 WARN. **Pendientes de mesa:** `FP-260921-GEN2-DIN-CREDITO-PREDICCION-2024-COMMIT-1-7866-01` (¿ratifica `K6-P-TENEDORES`, no `K6-PR`, como la conducta K6 del lote?), `-02` (regla de adjudicación de COMMIT-3: bootstrap sintético desde el SE analítico, o regla puramente analítica sobre la diferencia — se decide con 2024 ya abierto). Nota: `forense/notas/nota-2026-09-21-gen2-din-credito-prediccion-2024-commit-1.md`. `## NO-CORRIDO / RESERVAS`: ver encargo archivado. Rótulo censado. **El PR no se fusiona en este acto: queda propuesto, mesa fusiona.**
