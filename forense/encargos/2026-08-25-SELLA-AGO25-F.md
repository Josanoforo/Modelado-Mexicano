# ENCARGO · ACTO SELLA-AGO25-F — las ocho letras (adjunta la hoja al lanzar)

> | | |
> |---|---|
> | **ARCHIVO** | `2026-08-25-SELLA-AGO25-F.md` |
> | **NOMBRE ESTABLE** | **`sella-ago25-f-encargo`** |
> | **ESTADO** | **CONSUMIDO** — 25/ago/2026, rama `claude/encargo-1-sella-ago25-f-75gu88` |
> | **SALIDA** | `forense/notas/2026-08-25-sella-f.md` |

Redactado por dirección, 25/ago/2026. ENTORNO: NUBE (`cloud_default`; sin red/microdato — declarado, sonda saltada) · Opus · CONTADOR: cero directo (v2.3) · ARRANQUE estándar: clon existente · SHA del momento · `data/raw` no aplica.

EXISTENCIA (dirección, 25/ago): las diez filas viven `ABIERTA`/`FIRMADA-sin-ejecutar` en el tablero (135 filas, lector de hoy); `SELLA-E` §5 dejó `L6` presentada verbatim; la propuesta mínima del hook vive en la nota de `eval-compartamos`; el mapeo candidato de θ, ídem. Ejecuta solo las letras que vengan verbatim en el lanzamiento; las ausentes se saltan y se dicen — el patrón que `SELLA-E` ya sentó.

TAREAS por letra (verbatim de mesa al ADR, siempre):

- **L1/`FP-127`**: si `b` — β al registro de la llave como `MEDIDO·ACOTADO` (sin tocar `milpa/`), nota de discrepancia visible junto al `−0.60`, fila `FIRMADA` para el acto `ESCALA-ASIGNADOS` (declarar la escala de los 15 asignados). `a`/`c`: propaga con su consecuencia.
- **L2/`FP-128`**: PROPUESTA→SELLADA (cabecera de una línea fechada, original intacto); fila cierra.
- **L3/`FP-129`** y **L4/`FP-130`**: enmienda fechada en `forense/ficha-r34-conda-v2-spec.md` con la opción firmada; si L4=`a`, grep de «activas» con conteo y enmienda de cada sitio vivo (historia intacta); `FP-104` se actualiza y sigue `ABIERTA` hasta re-corrida del veredicto — y si L3=`b`, fila `FIRMADA` para el acto que localice la serie homogénea en los Informes Banxico ya en corpus.
- **L5/`FP-63`**: registra la cadena verbatim; crea mini-fila UBUNTU «ejecutar destrucción»; NO destruyas desde NUBE.
- **L6/`FP-24`**: aplica la política firmada a las 20 filas con par de `relaciones.tsv` (la PROPUESTA de ENLACE-2 dice cómo — léela y ejecútala tal cual; si la firma difiere de la propuesta, propaga la de mesa).
- **L7/`FP-131`**: si sí — la propuesta mínima del hook pasa de nota a PROPUESTA-SELLADA en su sitio (sin implementar código en `milpa/`: implementarla es acto propio con fila `FIRMADA` nueva).
- **L8/`FP-132`**: escribe el mapeo firmado (necesidad/θ ← Compartamos) donde el registro de llaves y `necesidad-objeto-modelo.tsv` lo esperan, con cita. Además: cierra `FP-118` con la evidencia ya citada por dirección (PASO 0 agotado + corrida bajo plan B, ADR-157) — quedó fuera del palomeo de `SELLA-E` por estar `ABIERTA` y no `FIRMADA`; esta vez sí, citando la firma original de mesa («la descargamos y ya» → agotado → plan B pre-autorizado).

PERÍMETRO: tablero · `hitoD-R10_1-spec-v2_0-PROPUESTA.md` (cabecera, con L2) · `ficha-r34-conda-v2-spec.md` + sitios de «activas» (con L3/L4, lista declarada) · registro de llaves + sitio del `−0.60` (con L1/L8) · `relaciones.tsv` del curador (con L6) · sitio del hook (con L7) · gobernanza · estado · nota `2026-08-25-sella-f.md` · encargo. NO toca `milpa/`.

---

## Cierre

**CONSUMIDO, 25/ago/2026, ACTO SELLA-AGO25-F.** Resumen de ejecución — detalle completo, letra por letra, en `forense/notas/2026-08-25-sella-f.md`:

- **La "hoja" de las ocho letras no llegó adjunta al lanzamiento.** El encabezado la da por adjunta, pero el texto del encargo solo describe el procedimiento condicional por letra — ninguna opción (`a`/`b`/`c`, "sí"/"no", cadena verbatim, o adjudicación caso por caso) viene resuelta. Búsqueda dirigida en el árbol (`grep -rn` sobre `forense/notas/`, `forense/bitacora.md`, `canon/gobernanza-v1_15.md`) tampoco encuentra ningún ruling nuevo para `L1`–`L8`: las únicas menciones de `L1`–`L6` son el propio hallazgo de "cero verbatim" que `ACTO SELLA-AGO25-E` ya dejó escrito ese mismo día.
- **Cero letras ejecutadas.** `FP-127`, `FP-128`, `FP-129`, `FP-130`, `FP-63`, `FP-24`, `FP-131`, `FP-132` quedan exactamente como estaban. Ningún archivo del perímetro condicional (spec v2.0 de R10.1, ficha R3.4, registro de llaves, `relaciones.tsv`, sitio del hook, `necesidad-objeto-modelo.tsv`) se tocó.
- **`FP-118` no se cierra.** La cita de dirección ("PASO 0 agotado", `ADR-157`) describe el contenido que la fila tenía *antes* de que `ACTO RECENSO-DISENO-14` la reescribiera por completo (ahora trata de error estándar de diseño para ENNViH/CAL-G3). Cerrarla con esa cita fabricaría una ejecución sobre una premisa que el árbol ya no sostiene — misma disciplina conservadora que `ACTO SELLA-AGO25-E` ya aplicó para esta misma fila por otra razón (entonces era el `estado`; ahora es el contenido).
- **ADR.** `ADR-165` (candidateado contra el máximo verificado, `164`/`ACTO SELLA-AGO25-E`, ya fusionado), `canon/gobernanza-v1_15.md`.
- **Tests.** `19 FAIL · 141 WARN`, `LÍNEA BASE: VERDE`, sin movimiento neto (cero filas de tablero abiertas o cerradas por este acto).
- **Perímetro.** Respetado íntegro: `milpa/` no se tocó; ninguno de los archivos condicionales a una letra se tocó, porque ninguna letra verificó verbatim.
