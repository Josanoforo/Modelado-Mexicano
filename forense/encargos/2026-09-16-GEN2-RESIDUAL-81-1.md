ENCARGO · ACTO GEN2-RESIDUAL-81-1
Ejecuta el residual que `ACTO GEN2-REACTIVOS-RESIDUALES-2` dejó mapeado: recupera el enunciado del reactivo para los grupos ciegos cuya capa FD ya está en el repo, **por identidad exacta instrumento+tabla+variable** y no por nombre de variable, reparando primero el defecto que impedía ese cruce

CABECERA · redactado contra `10f8617` (merge de `PR #801` — base verificada en el ARRANQUE, 0 commits detrás de `origin/main`) · ENTORNO: **NUBE** (`tools/entorno.py`: `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=cloud_default · corpus=NO(examinados=0) · raices=data_raw:NO · numpy/pandas/scipy/pyreadstat AUSENTES · python 3.11.15`; red no sondeada porque este acto no la usa) · COMPUERTA: ninguna declarada por el lanzamiento · MODELO: Opus (integral) · Estado: VIVO · candidatos FP/ADR/NC: derivan al cierre (ADR real en `origin/main` al arrancar: 520; `NC` máxima: `NC-0242`).

LANZAMIENTO, verbatim (mesa, 16/sep/2026): llegó pegado en el mensaje que invocó el acto, no como archivo del repo; A.3 lo fija aquí.

PIEZAS (texto de mesa, verbatim)

> abre pr, enmienda, se mergeó por error, abrela y lanza el residual

LECTURA DEL LANZAMIENTO (del ejecutor, no de mesa). Son dos órdenes. La primera —abrir el PR de la enmienda de `GEN2-REACTIVOS-RESIDUALES-2`, porque `PR #796` se fusionó sobre un head anterior— se cumplió en `PR #802` y **no** es objeto de este encargo. La segunda, «lanza el residual», es este acto: el residual que el predecesor dejó **mapeado y no ejecutado**, asentado en `NC-0235`. De las tres piezas de ese residual, este acto toma la única ejecutable en NUBE y lo dice sin estirar el alcance:

- **ejecutable aquí** — los 18 grupos con capa FD limpia ya indexada en el repo (16 815 filas ciegas). No exige corpus: el texto ya está en el árbol.
- **NO ejecutable aquí** — los 55 grupos `REQUIERE-FD-EN-CORPUS` (92 941 filas) y los 8 `CANDIDATA-FD-EXT-POR-VERIFICAR` (19 892), que exigen el FD real montado; y el residual del lote (`NC-0236`, 1 153 filas con demanda viva), que exige releer descriptores de fuente. Los tres siguen `ABIERTOS` y se declaran en `## NO-CORRIDO / RESERVAS`.

VERIFICACIÓN DE EXISTENCIA (A.8, contestada por el ejecutor contra `10f8617` con comando; el lanzamiento llegó sin el bloque y A.8 manda contestarlo, no suponerlo):

(1) ¿Existe ya la estructura? SÍ: `data/reactivos-ciegos-81-v1_0.tsv` (censo derivado, `ADR-519`), las dos capas FD (`data/inventario-fd-v1_1.tsv` 17 094 filas, `-ext-v1_0.tsv` 10 635), el universo del buscador (241 591 filas) y el overlay acreditado del lote (`data/inventario-reactivos-contexto-v1_1.tsv`), cuyo vocabulario de salida este acto **reutiliza sin redefinir**.

(2) ¿Existe ya el contenido? NO, y la medición previa encontró **por qué no podía existir**: las dos capas usan **vocabularios distintos de `archivo_miembro`** y por eso el cruce por identidad exacta daba **CERO**.
```
$ (cruce por (archivo_miembro, variable_id) crudo, 16 815 filas ciegas de los 18 grupos)
  emparejan por (tabla,variable): 0
  emparejan solo por variable   : 10765
```
El índice de reactivos nombra el **miembro del payload** (`thogar.csv`, `mod_2017_ciberacoso.dbf`); la capa FD nombra la **hoja del descriptor** (`THOGAR`, `MOD_2017_CIBERACOSO`). Y en `ENASEM` la hoja viene **truncada a 31 caracteres** — el límite de nombre de hoja de Excel (`tr_enasem24_master_follow_up_file.csv` ↔ `TR_ENASEM24_MASTER_FOLLOW_UP_FI`). Es un defecto de vocabulario, no de contenido: el texto estaba, el puente no.

(3) ¿La estructura es posterior al trabajo? No: las dos capas son de agosto/2026 y el censo de ayer. Este acto no clasifica, pre-registra, carga ni sella ninguna regla del motor, y no cita ningún id de regla ni `R-n`: `tools/ya_medido.py` no aplica.

PIEZAS DERIVADAS (lo que este acto ejecuta)

P1 · **El puente, declarado y medido, no adivinado.** Regla de normalización de `archivo_miembro` en dos pasos, cada uno con su causa nombrada: (a) plegar a minúsculas y quitar la extensión conocida del miembro; (b) si eso no empareja, aceptar **prefijo de 31 caracteres** —el límite de Excel— **sólo cuando la tabla candidata es única** en ese instrumento. Ambigüedad de tabla nunca se resuelve por cercanía: se conserva como residual.

P2 · **El overlay acreditado, sólo donde la identidad es exacta.** Un tool (`tools/recupera_reactivos_fd.py`) y su salida (`data/inventario-reactivos-fd-recuperado-v1_0.tsv`), con el vocabulario de `contexto-v1_1` (`texto_tipo`, `fuente_texto`, `referencia_fuente`) y cita al `payload_id` y `sha256_12` del FD. **Sin propagación entre olas y sin fallback por nombre de variable** — la doctrina que `ACTO GEN2-REACTIVOS-RESIDUALES-BUSQUEDA-UTIL` fijó y que este acto no afloja: las 10 765 filas que emparejarían «sólo por variable» **no se publican**.

P3 · **El residual del residual, por motivo acreditado**, en el mismo vocabulario del predecesor: `TABLA-SIN-FD`, `VARIABLE-SIN-FD`, `TABLA-AMBIGUA`. Se publica como tabla, no como prosa, y no se retira del denominador.

P4 · **Consumo.** El buscador gana la clave explícita `--tablas fd_recuperado` (nunca en `vigente`, mismo convenio que `fd`/`fd_ext`/`contexto_v1_0`), y se demuestra la utilidad con consultas antes/después sobre instrumentos que hoy son ciegos por construcción.

PERÍMETRO: `forense/encargos/2026-09-16-GEN2-RESIDUAL-81-1.md` (este archivo) · `tools/recupera_reactivos_fd.py` (nuevo) · `data/inventario-reactivos-fd-recuperado-v1_0.tsv` y `data/reactivos-fd-recuperado-residual-v1_0.tsv` (derivados por ese tool) · `tools/busca_reactivos.py` (sólo `TABLAS` y su documentación) · `tests/test_recupera_reactivos_fd.py` (nuevo) · `data/INFRAESTRUCTURA-v1_0.md` (fila nueva, `T27`) · `forense/notas/2026-09-16-GEN2-RESIDUAL-81-1-cierre.md` · `forense/no-corrido.tsv` · `canon/gobernanza-v1_15.md`, `canon/estado-programa-v1_13.md`, `canon/registro-rotulos.tsv`, `tests/check.py` (cascada). NO toca: ningún índice histórico (`inventario-reactivos-*`, `inventario-fd-*`, `contexto-v1_1`, `residual-v1_1`, `reactivos-ciegos-81-v1_0`) · `tools/actualiza_reactivos_contexto.py` · `tools/censa_reactivos_ciegos.py` · `milpa/` · `data/corrida0/` · `data/manifiesto.yaml` · la cola de adquisición · el motor, las adopciones y los preregistros.

CONTADOR: **cero**. Ninguna corrida, ningún `RESULT`, ninguna adopción, ningún parámetro, cero llamadas a modelo, cero descargas, cero microdato abierto. Recuperar texto de un descriptor **no** acredita suficiencia científica de nada.

LO QUE NO HACE: no re-extrae de ninguna fuente (no hay corpus) · no modifica el overlay del lote ni su residual · no publica identidad que no sea exacta · no propaga texto entre olas ni por nombre de variable · no cierra `NC-0136` ni `NC-0235` (quedan los 55 sin FD, los 8 por verificar y el residual de este acto) · no abre `F6` · no adquiere · no firma por mesa · no fusiona ningún PR.

CIERRE: cascada completa (preflight de `tools/cierre_acto.py` → ADR → L0 → `--aplica` → rótulos → T25 → suite → push → UN PR) + `## NO-CORRIDO / RESERVAS` + `## CONSUMIDO` con el PR.

## NO-CORRIDO / RESERVAS

| qué | por qué | impacto | sucesor |
|---|---|---|---|
| «continuar por los 81 grupos» — las **7 620 filas** de los 18 grupos que el puente NO resuelve (`NC-0245`) | `NO-VERIFICABLE-AQUÍ`. Son diferencia **material**, no de ortografía: `endutih2025` escribe `ti25hog.dbf` donde el FD dice `tic_2025_hogares` (mapeo semántico, no normalizable), y `censo2020`/`cpv2020`/las dos `CNBV` tienen tablas de payload **sin hoja correspondiente** en el descriptor indexado. Adivinarlo publicaría identidad falsa — justo lo que este acto rechazó al no publicar las 10 765 filas que emparejarían por nombre de variable | La cobertura recuperada se queda en **9 195/16 815**; `NC-0136` y `NC-0235` siguen `ABIERTAS` | CAJA: acto que lea el FD real de `endutih2025`/`censo2020`/`CNBV` y publique el crosswalk de tablas; o MESA, si decide que un emparejamiento revisado a mano es acreditable |
| Los **55 grupos** `REQUIERE-FD-EN-CORPUS` (92 941 filas) y los **8** `CANDIDATA-FD-EXT-POR-VERIFICAR` (19 892) del censo de `ADR-519` | `PARO-ENTORNO`. Exigen el FD real montado, y NUBE no tiene corpus (`corpus=NO`, `archivos_examinados=0`). Este acto tomó **sólo** la pieza ejecutable aquí y lo declaró desde el encargo, sin estirar el alcance | Ninguna de esas 112 833 filas cambia de estado. `NC-0235` sigue `ABIERTA` con su sucesor intacto | ADQUISICIÓN dirigida + CAJA, por la columna `demanda_hoy` de `data/reactivos-ciegos-81-v1_0.tsv` |
| El residual del **lote prioritario** (`NC-0236`, 1 153 filas con demanda viva) | `FUERA-DE-PERÍMETRO`. Es otro objeto: el overlay del lote y su residual no se tocan aquí, y la cobertura prioritaria sigue en **43 020/55 895** | `NC-0236` no se mueve | CAJA, como ya declara esa fila |
| Consumir las 9 195 identidades donde ya hacen falta (`NC-0246`) | `FUERA-DE-PERÍMETRO`. Este acto publica y cablea; **no** edita `data/corrida0/mapa-demanda-19-corr-v1_0.tsv` —lo escriben a mano los actos de la serie `SPEC-DEMANDA`— ni congela spec alguna | Ninguna fila del mapa-19 cambia de estado; ninguna compuerta de `F6` se contesta por este acto, y `NC-0237` sigue `ABIERTA` | El próximo acto de `SPEC-DEMANDA`/`MEDICION-DEMANDA` para `CORR-0012`/`0013`/`0014`; MESA para la serie MOCIBA del panel |
| Endurecer `tools/verifica_head_remoto.py` tras el merge truncado de `PR #796` | `SUSTITUIDO-POR:PR #802`. Ese PR asienta el defecto y su precedente (`PR #572`) y deja la fila; **lo que queda huérfano** es el cambio al guard mismo, que sigue `SIN-ASIGNAR` y no lo absorbe ni `#802` ni este acto | El guard puede seguir reportando `PR_HEAD_SINCRONIZADO` sobre un PR que después fusione un head anterior | SIN-ASIGNAR (candidato a acto de mantenimiento) |

## CONSUMIDO

PR [#803](https://github.com/Josanoforo/Modelado-Mexicano/pull/803), abierto contra `main` el 16/sep/2026 y **no fusionado por el ejecutor** (el merge es de mesa). Cascada de cierre completa: **`ADR-522`** en `canon/gobernanza-v1_15.md`, anotación `L0` en `canon/estado-programa-v1_13.md`, los tres contadores mecánicos reconciliados por `tools/cierre_acto.py --aplica` (521→522), rótulo `GEN2-RESIDUAL-81-1` censado en `canon/registro-rotulos.tsv`, **`NC-0245`/`NC-0246`** nuevas en `forense/no-corrido.tsv` (`NC-0136` y `NC-0235` siguen `ABIERTAS`), fila nueva en `data/INFRAESTRUCTURA-v1_0.md` por `T27`, y nota de cierre en `forense/notas/2026-09-16-GEN2-RESIDUAL-81-1-cierre.md`.

**Renumerado (renumera quien fusiona segundo):** redactado como `ADR-521` con `NC-0243/0244`; `origin` tomó `ADR-521` (`ACTO GEN2-CAJA-SUCESORES-1`) y `NC-0243/0244` al fusionar 15 commits durante el acto → **`ADR-522`** y **`NC-0245/0246`**. El merge dejó el párrafo de `ADR-520` duplicado y se deduplicó conservando el de `origin`.

**Suite:** `3 FAIL · 4349 WARN`, **LÍNEA BASE VERDE**, re-medida tras el merge; pruebas propias **21/21**. `T16` se resolvió marcando `{cita-historica}` las cifras de `ADR-520`, sin tocar el test ni corregir el número ajeno.
