ENCARGO · MAESTRA33-A2 · CORRIGE-A1 — invoca /acto · ESTADO: LISTO-NUBE
SHA de redacción: a6d8504. ENTORNO: NUBE — NO CAJA. COMPUERTA: ninguna. MODELO SUGERIDO: Sonnet.
FIRMA DE MESA (verbatim): «[mesa: enterado FP-211 y "corrige A1 según el revisor"]».
A.8: veredicto en forense/notas/2026-08-31-revisa-calibracion-maestra33-a1.md §3 — 6 BLOQUEA sobre PR fusionado; ninguno corregido en main (verificado por dirección).
P1 · Por CADA fila de data/cola-adquisicion-v1_0.tsv, re-corre A.8 contra data/manifiesto.yaml por código: declara el campo consultado y cuántas entradas examinó (A.13); corrige estado y añade ids_manifiesto donde existan (WVS → OBTENIDO con sus seis). SIN sondear red, SIN descargar.
P2 · Absorbe data/cola-aperturas-externas-2026-08-06.tsv (15 filas) en la tabla viva, con puntero al histórico, y re-deriva el desglose de estados por comando.
P3 · Una línea cada uno: adquiere.md:8 → ADR-242; desglose corregido en el CONSUMIDO de A1; el toque a hallazgos.md declarado en el ADR de este acto.
P4 · Propaga el enterado de FP-211 verbatim; anota en el runbook del revisor que la calibración de A1 quedó saldada por este acto.
PERÍMETRO: data/cola-adquisicion-v1_0.tsv, .claude/commands/adquiere.md (línea 8), forense/encargos/ (CONSUMIDO de A1), tablero, forense/notas propia, archivo A.3, cascada. Frase exacta de perímetro vigente. FP/ADR: deriva. CONTADOR: cero, declarado.
LO QUE NO HACE: no sondea; no descarga; no re-ejecuta la caminata; no toca milpa/.

## CONSUMIDO

Ejecutado por `ACTO MAESTRA33-A2 · CORRIGE-A1`, 1/sep/2026, rama `claude/maestra33-a2-corrige-a1-4zy4qz`. Commits: `2181f86` (0-bis A.3, este archivo) · `4417ed5` (P1, `data/cola-adquisicion-v1_0.tsv`: WVS + 8 filas más a `OBTENIDO`) · `a85b1dd` (P2, absorbe `data/cola-aperturas-externas-2026-08-06.tsv`) · `7376a9c` (P3, `adquiere.md:8` + `## CONSUMIDO` de A1) · `8755650` (P4, `FP-211` + runbook del revisor) · `54252f2` (cascada, `ADR-247` candidato, `L0`, `registro-rotulos`) · cascada final (este `## CONSUMIDO` + `forense/notas/2026-09-01-corrige-a1-cierre.md`). Resultado: de los 6 `BLOQUEA` de `forense/notas/2026-08-31-revisa-calibracion-maestra33-a1.md` §3, los 5 arreglables con una línea quedan corregidos (`adquiere.md:8`, desglose `43/2` del `## CONSUMIDO` de A1, `hallazgos.md` declarado, la quinta cola absorbida) y el sexto (`WVS`) re-derivado por completo: `OBTENIDO` con sus seis `ids_manifiesto`. La misma re-corrida de `A.8`, extendida más allá de lo que la calibración había mirado, encuentra ocho correcciones adicionales (`ECEPIE`/Early Childhood Education Program, `ACLED`, OSF Interacting-As-Equals, `ENCOAP`, `LFEPIE`, Compartamos RCT ×2, `ENIF`) — desglose final: `73` filas (`30 OBTENIDO, 36 PENDIENTE, 6 NO-ACCESIBLE, 1 NO-OBTENIDO-POR-ESTE-AGENTE`). `FP-211` `FIRMADA` por mención literal. `python3 tests/check.py --baseline`: VERDE, `19 FAIL` — nada nuevo frente a `tests/baseline.json` (HEAD `c6a0d72`). SIN sondear red, SIN descargar, SIN re-ejecutar la caminata de `/adquiere`, SIN tocar `milpa/**` — las cuatro prohibiciones del encargo, respetadas. Detalle: `forense/notas/2026-09-01-corrige-a1-cierre.md`.

**Colisión al cerrar.** `origin/main` se movió de `a6d8504` a `4e8797e` (`PR #420`, `ACTO MAESTRA33-E7 · MAPEADOR-1`) mientras este acto estaba en vuelo, y ese acto fusionó primero tomando `ADR-247`. Commit `348681c`: fusiona `origin/main`, renumera el candidato de este acto `ADR-247`→`ADR-248` en `canon/gobernanza-v1_15.md` (título + cabecera + cascada), `canon/estado-programa-v1_10.md` (`L0` + tabla), `canon/registro-rotulos.tsv` y `forense/firmas-pendientes.tsv` (`FP-211`) — regla de la casa, renumera quien fusiona segundo. `python3 tests/check.py --baseline` re-corrido tras el merge: sigue VERDE, sin `FAIL` nuevo.
