> **Cabecera de procedencia (aparato de este acto, `SELLA-C`, gate `FP-91`, no del párrafo original):**
> - **Firma de mesa que autoriza este acto, verbatim:** «C: 2 mueren y ADV1-M6 se reescribe» — opción (C) sobre las tres candidatas que `FP-91` dejó abiertas (`ADR-128(b)(c)`, `ADV1-M6` del careo, `PLAN-CALCULO-TOTAL` OLA 4). Sin adjunto externo con `sha256` propio: la firma llega transmitida directamente por dirección al lanzar `SELLA-C`, mismo trato que `ADR-132` dio a `D-i`…`D-iv` (capturadas del propio texto de lanzamiento, sin paquete adjunto).
> - **Qué muere y qué se reescribe, dicho una vez y gobernando todo lo demás:** «2» son los siete umbrales de `ADR-68(c)` que `ADR-128(b)` ya había declarado `VENCIDO EN ALCANCE` (salvo el umbral (1), `ADR-128(c)`) — ese vencimiento **no se repite aquí**, ya está sellado. Lo que este documento hace es la otra mitad de la firma: **`ADV1-M6` se reescribe** como capa nueva de cómputo, mismo patrón que `ADV1-M5 v2` (`ADR-136`, `A.10` corolario 1) — el párrafo original **no se toca**.
> - **Fuente, intacta:** `forense/CAREO-ADV-DUELO-diseno-v2-2026-08-19.md:44` (§B, mecanismo `ADV1-M6`), verbatim salvo los rótulos que aquí se citan ya prefijados (patrón de `canon/registro-rotulos.tsv` fila `ADV1-M5`, HABITANTE 4): *"GO con dientes. De los 7 umbrales, **≥3 de resultado**: cuotas de `ADV1-M1` cumplidas por comando (P2, CV, sondas corridas) · cobertura de intervalos de M ≥60% de celdas puntuadas · M>B en ≥2/3 de celdas puntuadas. Al menos un umbral lo determina la naturaleza, no el equipo."* Esa línea sigue viviendo ahí, sin una palabra tocada.
> - **`diff` contra la fuente:** ninguno — este documento no edita el careo; cita por ruta y línea y traduce.

---

# `ADV1-M6 v2` · GO CON DIENTES — tres criterios de resultado, ahora como conjunto completo

> | | |
> |---|---|
> | **ESTADO** | **SELLADA**, `ADR-140` (ver §4 abajo). |
> | **QUÉ ES** | Una **capa nueva** sobre `ADV1-M6`, no un reemplazo. El párrafo original de `forense/CAREO-ADV-DUELO-diseno-v2-2026-08-19.md` §B **queda intacto y sigue siendo la fuente**; esta capa fija el `GO` con los tres criterios que `ADV1-M6` ya nombraba, presentados ahora como conjunto completo y no como subconjunto de siete. Patrón `A.10` corolario 1: lo superado no se edita, se re-emite con alcance nuevo. |
> | **POR QUÉ** | `FP-91` medía tres textos vigentes con tres lecturas distintas del mismo `GO`. La firma de mesa «C: 2 mueren y ADV1-M6 se reescribe» adjudica: los siete umbrales originales de `ADR-68(c)` mueren (ya `VENCIDO EN ALCANCE`, `ADR-128(b)`, salvo el umbral (1) por `ADR-128(c)`); `ADV1-M6` deja de leerse como «tres de siete» y pasa a ser, él mismo, el criterio completo de cierre. |
> | **PROCEDENCIA** | Firma de mesa de `SELLA-C` (gate `FP-91`), citada en la cabecera de este documento. Ninguna cifra se inventa: los tres criterios son cita textual de `ADV1-M6`, sin alterar una palabra. |

---

## 1 · Qué se conserva verbatim y qué se traduce

**Lo que se conserva sin tocar una palabra** — es el texto que `D-4` ya adoptó completo (`ADR-128(e)`) y que esta capa no reabre:

| Cláusula original (verbatim de `CAREO §B:44`) | Estado en v2 |
|---|---|
| *«De los 7 umbrales, ≥3 de resultado»* | **La cifra "7" queda como historia del linaje** (`ADV1-M6` nació como endurecimiento de tres de los siete umbrales de `ADR-68(c)`) — no como denominador vigente. Los siete de `ADR-68(c)` ya están `VENCIDO EN ALCANCE` por `ADR-128(b)`; con ese vencimiento, "de los 7" deja de tener referente vivo. `SELLA-C` adjudica: los tres criterios de resultado dejan de leerse como subconjunto de un universo muerto y pasan a ser, ellos mismos, **el conjunto completo** del `GO` de `ADV1-M6 v2` |
| *«cuotas de `ADV1-M1` cumplidas por comando (P2, CV, sondas corridas)»* | **Conservada literal — criterio (1) del conjunto completo** |
| *«cobertura de intervalos de M ≥60% de celdas puntuadas»* | **Conservada literal — criterio (2) del conjunto completo** |
| *«M>B en ≥2/3 de celdas puntuadas»* | **Conservada literal — criterio (3) del conjunto completo** |
| *«Al menos un umbral lo determina la naturaleza, no el equipo»* | **Conservada literal, y gobierna los tres** — ver §2 |

**Lo que se traduce, y por qué:**

| Del original | A v2 | Razón |
|---|---|---|
| Tres criterios leídos como «≥3 de 7» (subconjunto de un universo de siete) | Tres criterios leídos como **conjunto completo** del `GO` | El universo de siete (`ADR-68(c)`) está `VENCIDO EN ALCANCE` (`ADR-128(b)`); un subconjunto de un universo muerto no tiene denominador vivo. `SELLA-C` («2 mueren») cierra esa lectura y deja los tres como todo lo que hay |
| Umbral (1) de `ADR-68(c)` sobreviviendo aparte, por excepción de `ADR-128(c)` (`tests/test_motor_holdout.py`) | **Sin cambio, y declarado sin cambio aquí** | Es un mecanismo de ciego de proceso (commit de `L` anterior a ver la salida de `M`), no un criterio de veredicto — `ADR-128(c)` ya lo distinguió del resto de `(c)` que sí vence. `ADV1-M2` lo hereda y lo endurece. Este documento no lo toca ni lo cuenta entre los tres criterios de `ADV1-M6`: son universos distintos (proceso vs. resultado) |
| El `GO` de `ADV1-M6` competía sin desempate contra el criterio de `ADR-68(c)` y el de `PLAN-CALCULO-TOTAL` OLA 4 | El `GO` de `ADV1-M6 v2` es **la única compuerta formal de apertura de la fase de cálculo** | Ver §3 — esto es lo que cierra el hueco de compuerta que `APERTURA-FASE-CALCULO-v1_2.md:25` dejaba abierto |

---

## 2 · Los tres criterios, como conjunto completo — esto es el `GO` de `PILOTO-E1E3 T4`

Citados de `CAREO §B:44`, sin edición, ahora sin el marco «≥3 de 7»:

1. **Cuotas de `ADV1-M1` cumplidas por comando** — `P2` (celdas grado 0, ≥1/3 del set con ≥2 desenlaces documentados), `CV` (bajo el umbral de la semaforización INEGI, acuerdo `CAC-007/01/2018`), sondas corridas (perturbación, canario, placebo).
2. **Cobertura de intervalos de `M` ≥60% de celdas puntuadas** — la fracción de celdas puntuadas cuyo intervalo del corredor `M` contiene el valor real de `R`.
3. **`M > B` en ≥2/3 de celdas puntuadas** — el motor supera al baseline ingenuo en al menos dos tercios de las celdas puntuadas.

**Los tres se cumplen, no un subconjunto** — esa es la lectura que «2 mueren y ADV1-M6 se reescribe» adjudica: al morir el universo de siete, deja de existir un «resto» del que elegir tres; los tres que `ADV1-M6` ya nombraba **son** el criterio, completo.

**Cláusula que gobierna los tres, verbatim, sin tocar una palabra** (`CAREO §B:44`):

> *"Al menos un umbral lo determina la naturaleza, no el equipo."*

Es decir: al menos uno de los tres criterios puede fallar por una propiedad del fenómeno medido — no por ejecución deficiente del equipo — y ese fallo no se trata como defecto de proceso. Cuál de los tres lo determina la naturaleza no lo fija este documento (no estaba fijado en el original); se deriva cuando el piloto corra, con la evidencia a la vista, mismo criterio que `ADV1-M4` ya aplica a la lectura (5) del duelo (*"el fenómeno no es predecible con estas herramientas hoy"*).

**Lo que este documento NO hace:** no fija `Δ` ni banda TOST (`D-iv`, `DUELO-PREREG-V2`) · no adjudica `FP-83` ni ninguna otra fila de `FP-92`…`FP-102` · no decide qué emite el corredor `M` (sesión `EMISOR-M`) · no toca el marco de candidatas ni el sorteo · no reabre `ADR-68(c)` ni `ADR-128` — los cita, no los edita.

---

## 3 · La compuerta — declaración, no pregunta

`APERTURA-FASE-CALCULO-v1_2.md:25` dice: *"Ese GO es la apertura formal de la fase de cálculo."* Con los siete umbrales de `ADR-68(c)` `VENCIDO EN ALCANCE` (`ADR-128(b)`), esa frase quedaba sin compuerta que la satisficiera — la fase declarada `ABIERTA` en gobernanza (`ADR-132(b)`) no tenía un `GO` de resultado que pudiera, de hecho, dispararse.

**Se declara aquí, y se sella en `ADR-140` §(c):** el `GO` de `ADV1-M6 v2` — los tres criterios de §2 de este documento — ocupa ese lugar. Es la nueva compuerta formal de apertura de la fase de cálculo. `PILOTO-E1E3 T4` (`APERTURA-FASE-CALCULO-v1_2.md §4`) aplica estos tres criterios, y ningún otro, para producir su fila de mesa `GO`/`NO-GO`.

---

## 4 · Qué cambia aguas abajo, y qué no

**Desbloquea:** `PILOTO-E1E3 T4` deja de estar bloqueado por la contradicción de `FP-91` — tiene ahora un criterio único y sellado que aplicar cuando corra. `FP-91` cierra `FIRMADA` (ver tablero, `ADR-140` §(d)).

**No cambia:** el marco de candidatas (`ADV1-M1`) · el sorteo · la tubería `L` · los corredores `B` y `E` · el contrato celda-D v0.5 · `ADV1-M2`, `ADV1-M3`, `ADV1-M4`, `ADV1-M5 v2` · el umbral (1) de `ADR-68(c)` que `ADR-128(c)` mantiene vigente como mecanismo de proceso · ningún test de motor.

**Queda pendiente, declarado y no resuelto aquí:** cuál de los tres criterios «lo determina la naturaleza» — se deriva cuando el piloto corra, con la evidencia de esa corrida a la vista (ver §2).

---

## 5 · Lo que este documento deliberadamente NO hace

No edita el párrafo original del careo. No re-adjudica el vencimiento de `ADR-68(c)` — ya está sellado por `ADR-128(b)`, este documento lo cita. No fija `Δ`, banda TOST, ni el veredicto de `FP-83`/`FP-92`…`FP-102`. No corre ninguna celda-D ni produce el `GO`/`NO-GO` real — eso sigue siendo `PILOTO-E1E3 T4`, sin correr. No decide qué emite el corredor `M`.
