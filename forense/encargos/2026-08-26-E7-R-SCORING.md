# ENCARGO E7 · R-SCORING — árbitro R sobre microdato, corredores B/E (con M), scoring: el PRIMER MARCADOR del programa

**Archivado por A.3** (el encargo se commitea íntegro antes de ejecutarlo). Texto verbatim tal como dirección lo emitió, seguido de la verificación de la compuerta GATED que el propio texto delega al ejecutor, y del bloque `## CONSUMIDO`.

**Rótulo, D-6.** Dirección lanzó este encargo rotulado `E7` en forma pelada. Ese token colisiona con los habitantes ya censados del espacio `E` (`E0`/`E2`/`E3-TRIAGE`/`E4a`/`E4b`/`E4c`/`E5`, más `E-3`, `E1`, `E4`, `E5`, `E6` y `MAESTRA30-E1..E4`). Resuelto donde se puede resolver: **este acto se declara `ACTO E7 · R-SCORING` en todo archivo que escribe** y no reclama el token pelado; el rótulo queda censado en `canon/registro-rotulos.tsv`. El encargo archivado **no se edita** — A.3 pide el texto de dirección verbatim, y el texto de dirección no se edita para complacer a un test.

---

## Texto del encargo (verbatim)

ENCARGO E7 · R-SCORING — árbitro R sobre microdato, corredores B/E (con M), scoring: el PRIMER MARCADOR del programa, bajo PILOTO SIN VEREDICTO

Dirección (maestra-30), 26/ago/2026 · SHA de redacción 8b317d3 · Cifras: clase (1). GATED: no arranca hasta que el PR de E6 · L-RUN esté FUSIONADO en main (compuerta cero lo verifica; si corridas-L/ no está completo en main, PARA — no lo completes tú).

ENTORNO ASIGNADO: UBUNTU (corpus montado — R abre microdato). NO lanzar en NUBE ni en Codex. Acto de estimación: rige la regla de dos commits del Bloque D. Orden sagrado, verbatim del prereg: hashes → L → R → scoring; L ya corrió; aquí entra R y solo entonces scoring.

════════ ARRANQUE — hazlo antes de leer el resto del encargo ════════

Reporta las cinco líneas de abajo y NO empieces hasta tenerlas. Si algo no cuadra, PARA y repórtalo: encontrar que el terreno no es el que el encargo supone es entregable, no interrupción.

1 · REPO. Localiza el CLON EXISTENTE. No clones uno nuevo salvo que no haya ninguno, y si clonas, dilo. Reporta: ruta absoluta · git log -1 --format="%h %s" · git status ⚠️ No arranques desde el home. Si el cliente avisa "launched in your home directory", cámbiate al clon antes de nada.

2 · SHA. Confirma contra qué base trabajas y compáralo con el que el encargo declara. Si main se movió: NO es PARO — refresca, re-deriva lo que dependa del perímetro, y reporta la diferencia antes de editar.

3 · data/raw. AUSENTE NO ES PARO. Es raíz integrada, gitignorada, resuelta por código; un clon fresco siempre nace sin ella. Se crea o se enlaza. Reporta: existe / la enlacé a <ruta> / la creé. ⚠️ Si este acto DESCARGA algo: verifica al cerrar que los payloads quedaron en el CORPUS COMPARTIDO y no solo en tu worktree. Es el defecto de PR #77 y no lo atrapa ningún test.

4 · ENTORNO. "Ubuntu" no distingue nada; la política de red sí. CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE → esperado: sin_variable curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://www.inegi.org.mx/ Reporta los dos valores crudos. NUNCA curl -I. Si este acto no toca microdato ni red, dilo y salta este punto. ⚠️ [NUEVO v2.11] Un negativo producido por un comando que no examinó archivos no es un negativo (A.13). Todo veredicto negativo —incluida la sonda de este punto— declara cuántos archivos examinó el comando que lo produjo. A.2, tercera parte: ls data/raw/ 2>/dev/null | head -1 — el corpus DEBE estar montado; en UBUNTU grep envuelve ugrep -I: usa command grep y decláralo.

5 · ESPEJO. Prohibido derivar cifras del espejo del proyecto: está versiones atrás del repo y contiene archivos que el repo nunca tuvo. Toda cifra sale del clon de (1), con el comando a la vista.

════════════════════════════════════════════════════════════════════

═══ VERIFICACIÓN DE EXISTENCIA — la contesta quien ESCRIBE el encargo [NUEVO v2.8] ═══ CONTESTADA por dirección, 26/ago/2026, clon en 8b317d3 (con la parte gated que el EJECUTOR re-verifica al arrancar):

1 · ESTRUCTURA. Gobernantes: prereg-corrida-v1_0.md (F3: banda TOST = 0.5·EE(R), FP-163 FIRMADA ADR-199; regla de forma no constante; CV≥30%⇒SKIP, FP-79) · los tres scripts pineados F1 (corredor-B-tasa-base.py, corredor-E-combinacion-LM.py — E = mediana_por_cuantil({L-solo, L+corpus, M}), tres corredores, ADR-141 —, scoring-adv1-m3.py) · marco-congelado-piloto-v1_0.tsv (la SpecCelda de cada una de las 15: encuesta, ola, universo, variable, estimador, escala — eso ES el pre-registro de R) · data/diseno-muestral.yaml (ponderador/estrato/UPM por encuesta; ENSAFI ya MAPEADO, #373) · data/manifiesto.yaml (payloads). ADV1-M5 v2 (escala de cinco casillas) y ADV1-M6 v2 gobiernan cómo se reporta el marcador, no se recalculan aquí.

2 · CONTENIDO.

El entregable NO existe hoy: corridas-R/, marcador-piloto* → 0 hits sobre el árbol (find forense data milpa canon, 1,79x archivos — re-verifícalo tú al arrancar con tu conteo, A.13).
Los scripts existen, hash-pineados y son spec ejecutable: corredor-E y scoring abortan en __main__ fuera de su acto — este acto ES su acto; los importas/invocas conforme a sus propias firmas de función, cero re-implementación.
GATED (re-verificación del ejecutor, no de dirección): main contiene forense/prereg-duelo-v2/corridas-L/ con 240 JSON (ls | wc -l, y 15×2×8 por patrón, comandos pegados) y valor_extraido poblado por pipeline. Menos de 240, o valor_extraido: null masivo → PARA y repórtalo a mesa; no lo completes tú (eso es perímetro de E6).

3 · COBERTURA RETROACTIVA. El prereg (26/ago) es posterior a la adquisición de varios microdatos que R abrirá — normal y declarado: la spec de cada celda vive en el marco congelado, no en los payloads.

════════════════════════════════════════════════════════════════════

OBJETO

Producir, por primera vez: R (el valor del microdato) por celda con su EE(R) de diseño; los puntos M que corredor-E exige; los corredores B y E; el scoring completo con banda TOST y SKIP; y el documento del primer marcador — que puntúa y no adjudica (PILOTO SIN VEREDICTO, D-i).

PASOS

0-bis · A.3: commitea este encargo íntegro en forense/encargos/2026-08-26-E7-R-SCORING.md; al cerrar, ## CONSUMIDO con el PR. 1 · Compuerta cero: gate de E6 (arriba) + los 6 hashes F1 (salida cruda). Discordancia → PARO/A.7. 2 · Derivación de mecánica M — del árbol, no de memoria: los puntos M por celda los produce el emisor del motor (milpa/src/emisor.py + crosswalk pregunta↔regla sellado) sobre la SpecCelda de cada una de las 15. Localiza en el árbol el procedimiento sellado que mapea SpecCelda → punto M (crosswalk, adversariales de forense/adv-duelo/, docstrings del emisor). Si no existe procedimiento sellado para alguna celda, eso es PARO-reporte con la lista exacta de celdas huérfanas — es el tipo de hueco que el piloto existe para exhibir; no inventes un mapeo. 3 · COMMIT-1 — procedimiento R congelado, antes de abrir un solo valor (forense/prereg-duelo-v2/corridas-R/PROCEDIMIENTO-R-v1_0.md): por celda, verbatim del marco congelado: encuesta+ola (→ payload por manifiesto, hash verificado una invocación por --id, A.1), universo, variable, estimador, escala; diseño de varianza = ponderador/estrato/UPM de diseno-muestral.yaml si la encuesta está MAPEADO — si no lo está, la celda se corre con la reserva del censo escrita aquí (p. ej. MAS/HC1 declarado, nunca silencioso), jamás con diseño inferido de otra encuesta. Permitido leer nombres/etiquetas de variables para localizar la columna; prohibido mirar un valor antes de este commit. Cierra con la frase verbatim: «el primer resultado que produzca este procedimiento es el que se reporta». 4 · COMMIT-2 — corre, sin editar el COMMIT-1: R + EE(R) por celda → corridas-R/{id_celda}.json (valor, EE, N efectivo, diseño usado, payload+sha) · puntos M (paso 2) y B (corredor-B) · E = mediana_por_cuantil({L-solo, L+corpus, M}) (corredor-E) · scoring-adv1-m3.py con banda TOST = 0.5·EE(R) (forma no constante donde el prereg lo diga) y CV≥30%⇒SKIP aplicado aquí y solo aquí, con cada SKIP listado y su CV. Si el COMMIT-1 tenía un error, tercer commit lo dice; nunca se corrige hacia atrás. 5 · El marcador: forense/prereg-duelo-v2/marcador-piloto-v1_0.md — tabla por celda (L-solo, L+corpus, M, B, E, R±EE, banda, casilla de ADV1-M5 v2, SKIP si aplica) + agregados que scoring emita, comparación principal L-solo (FP-162; L+corpus reportado como auxiliar). Cabecera obligatoria, verbatim: «PILOTO SIN VEREDICTO (D-i) — este documento puntúa; no adjudica ADV1-M5, no mueve tier alguno, no abre fila de tablero.» Toda cantidad con su escala declarada (A-bis 3); IC/banda que no despejan → se reporta como cae, jamás se fuerza casilla (A-bis contraparte). 6 · Nota forense/notas/2026-08-26-r-scoring-cierre.md (firma A.2, gate E6 con conteos, hashes, celdas MAPEADO vs reserva, SKIPs, huérfanas M si hubo) · ADR (máximo re-derivado al arrancar; renumera al fusionar — E5/E6 concurren) + recifrado estado §L0 (línea del duelo: primer marcador producido, sin veredicto) · tests/check.py --baseline verde · PR.

PERÍMETRO Y CONCURRENCIA

Toca SOLO: forense/prereg-duelo-v2/corridas-R/** (nuevo) · forense/prereg-duelo-v2/marcador-piloto-v1_0.md (nuevo) · nota nueva · forense/encargos/…E7….md (A.3) · canon/gobernanza-v1_15.md · canon/estado-programa-v1_10.md. No toca corridas-L/ (solo lee), ni milpa/ (solo ejecuta el emisor), ni el tablero. Concurrentes: E5 (NUBE) y el residuo de E6 — colisión esperada solo en gobernanza/estado. "Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo."

PROHIBIDO

Adjudicar una casilla/letra o tocar un tier (D-i manda) · abrir un valor de microdato antes del COMMIT-1 · completar o editar corridas-L/ · re-implementar lo que los scripts pineados ya definen · diseño de varianza inferido de otra encuesta · cruzar escalas sin enlace · abrir fila de tablero por el marcador · red (todo insumo ya está en corpus; payload faltante o hash discordante = PARO, no descarga).

CONTADOR

El primer marcador del programa — la medición central de la fase, bajo PILOTO SIN VEREDICTO. Hito D y tiers: sin movimiento, por diseño.

---

## Verificación de la compuerta GATED (la re-hace el ejecutor, no dirección)

El texto de arriba declara `SHA de redacción 8b317d3` y exige `240` JSON en `corridas-L/`. **Ninguna de las dos cifras describe el árbol contra el que este acto corre.** Ambas se re-derivaron al arrancar, conforme al punto 2 del ARRANQUE («si main se movió: NO es PARO — refresca, re-deriva»):

**SHA.** `main` avanzó de `8b317d3` a `3bc28b1` (`Merge pull request #377 from Josanoforo/acto/e6-l-run`, 2026-08-26 15:27:26 -0600). Entre medias entraron `#375` (`ADR-204`, FP-164 octava clase), `#376` (`ADR-205`, R10.1 → fila C) y `#377` (`ADR-206`, este gate). Este acto corre contra `3bc28b1`.

**Gate de E6.** `corridas-L/` **está** en `main` y trae **`120`** JSON, no `240`:

```
$ git ls-tree -r --name-only origin/main -- forense/prereg-duelo-v2/corridas-L/ | wc -l
120
   .json: 120 · L-solo: 120 · L+corpus: 0 · celdas distintas: 15 · repeticiones por celda: 8 (valor único)
```

**Y `120` NO es un faltante: es una decisión de mesa, firmada, que nombra a este acto.** `forense/firmas-pendientes.tsv` `FP-165`, estado `FIRMADA`, campo `gatea`, verbatim: «AGUAS ABAJO, y por eso se declara aquí **para que E7 no pare en falso** […] En E7 la columna E se reporta INEJECUTABLE citando esta fila: eso es **REPORTE, no PARO**. NO gatea: L-solo (ejecutada y capturada por E6), la comparación principal FP-162, los hashes F1, el corredor B, ni el árbitro R — esos siguen su curso.» Firma de mesa en sesión: «FIRMO: el programa no hará más llamadas API; L+corpus NO se ejecutará […] ELEGIDO: NO-EJECUCIÓN.»

La cifra `240` del encargo se redactó contra `8b317d3`, **antes** de que esa firma existiera. La firma es posterior, más específica, nombra a este acto por su nombre y enumera qué no gatea. **La compuerta abre.**

**`valor_extraido`** (segunda mitad del gate, A.13 — universo examinado: los 120 archivos, uno por uno): **103 poblados · 17 `null` · 0 ausentes**. `17/120` = 14.2% no es «`null` masivo».

**Hashes F1**, re-calculados contra `3bc28b1` (no se cita el cálculo hecho contra el SHA viejo): **6 de 6 coinciden**, cero discordancias ⇒ A.7 no aplica.

**Consecuencia heredada, que este acto ejecuta y no discute.** El paso 4 del encargo manda correr `corredor-E-combinacion-LM.py`, que `ADR-141` selló como `E = mediana_por_cuantil({L-solo, L+corpus, M})` — **tres** corredores. Sin `L+corpus`, ese script queda **INEJECUTABLE tal como está sellado**. Por mandato de `FP-165`, la columna `E` del marcador **se reporta INEJECUTABLE citando esa fila**; no se calcula, no se re-implementa, no se re-sella el operador `⊕`. Las demás columnas siguen su curso.

---

## CONSUMIDO

**CONSUMIDO**, ejecutado el 26/ago/2026 por `ACTO MAESTRA30-E7 · R-SCORING` (`ADR-207`).

**Los pasos, uno por uno.** `0-bis` `A.3` ✓ (este archivo, commiteado íntegro antes de ejecutar) · `1` compuerta cero ✓ (hashes `F1` 6 de 6 contra `3bc28b1`; gate de `E6` abierto por `FP-165`, que nombra a este acto; `valor_extraido` 103/17/0 sobre los 120 examinados) · `2` derivación de `M` ✓ **con `PARO`-reporte**: cero de quince puntos, huérfanas exactas `DIN-03`, `DIN-11`, `TIC-06`, y los 3 `CANDIDATO-EMITE` desmontados como falsos positivos de subcadena · `3` `COMMIT-1` ✓ (procedimiento congelado antes de abrir un valor, cerrado con la frase verbatim) **más una `ENMIENDA 1` por adición** que no edita el `COMMIT-1` y declara un error propio · `4` `COMMIT-2` ✓ (`R` en 9 de 15, `B`, y el scoring **ejecutado**) · `5` marcador ✓ · `6` nota, `ADR-207`, recifrado `§L0`, suite `--baseline` **VERDE** y `PR` ✓.

**Lo que el encargo pedía y no se pudo entregar, con su razón.** El paso 4 manda correr `corredor-E`: **`INEJECUTABLE`** por `FP-165`, y el propio `FP-165` mandaba reportarlo así — reporte, no `PARO`. El mismo paso manda correr `scoring-adv1-m3.py`: **se ejecutó y falló cerrado** (`CONFIGURACION_INVALIDA: se requiere exactamente 1 corredor L/corpus; hay 0`), porque su contrato exige los cuatro corredores y el piloto tiene uno. **No se le pasó una configuración falsa para arrancarle números.** El paso 5 pide la casilla de `ADV1-M5 v2` por celda: **ninguna es evaluable**, porque las cinco se deciden sobre *skill* contra `B` y `B` es `SIN_BASELINE` en las 15.

**Seis de las quince celdas no tienen árbitro**, cada una con su razón escrita **antes** de correr: `DIN-07`, `EMP-02`, `EMP-04`, `EMP-05` sin microdato en el corpus; `DOC-06` sin payload y con ola de árbitro futura; `TIC-06` con la `SpecCelda` internamente inconsistente en el marco **sellado**, que este acto **no edita**.

**Desviaciones declaradas, todas en la nota:** el `SHA` de redacción (`8b317d3`) no era el de `main` (`3bc28b1`) y se re-derivó · la cifra `240` del gate era anterior a `FP-165` y se resolvió contra la firma, no contra el texto · `ENMIENDA 1` al `COMMIT-1` por adición · extensión mínima de perímetro (`firmas-pendientes.tsv`, `registro-rotulos.tsv`, lista blanca `T25`) con el precedente de `E4`/`E5`/`E6` · rótulo `MAESTRA30-E7` por `D-6`.

**`FP-166` queda `ABIERTA`** con cuatro caminos para mesa. Este acto **no elige**.

Detalle completo: `forense/notas/2026-08-26-r-scoring-cierre.md`. `ADR-207` en `canon/gobernanza-v1_15.md`. Rama: `acto/e7-r-scoring`.
