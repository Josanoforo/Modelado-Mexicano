# GEN2-ADOPCION-BLOQUE-Y-PINES-1 · ADENDA-1 · Lo que #1101 ya hizo de P-L, y la dependencia del canal que el «Hecho» no puede ignorar
**Dirección (Claude), 24/sep/2026 · archivo propio (A.3) · corrige P-L y el criterio de «Hecho»; las firmas L–P de §2 no cambian.**

1. **P-L, reducida.** PR #1101 (sesión CLI, sobre la rama huérfana de CONTADORES-2) ya trae: `marcador_segmento.py --escribe` en dos pasadas dentro del paso de derivados de `verify.yml`, el test de punto fijo (`test_c2_compuesto.py`, 30/30) y las cinco aserciones de T-C2-COMPUESTO actualizadas (22→19 RESERVADA, 16→13 emitibles, 206/20→174/52). **Si #1101 está en main al abrir, P-L no repite nada de eso**: queda (a) las dos filas `origen_numerico` en `decisiones.tsv` con la firma L verbatim, (b) `registro --verifica` sin NO_APTA, y (c) la clasificación de los **16 FAIL de T-REPRO(g)** de `CALC-ENCIG-DUELO-2025-ADJUDICACION-0001` (NC `…749c-01`): aserción de estado → se actualiza con cita al acto que la hizo verdad; protege una medición → FP y no se toca. Si #1101 no está en main, P-L espera y las demás piezas siguen.
2. **«Hecho» y el canal.** El canal de derivados está caído desde el 23/sep 19:15Z (`0edf93e2`): `resultados.tsv` re-derivado pesa 129–155 MB y GitHub rechaza el push (GH001, límite 100 MB); no hay `[deriva]` desde #1074 (NC `…749c-03`). Mientras no lo repare `GEN2-TUBERIA-VISTA-NORMALIZADA-1`, **ningún contador de vista se mueve en main aunque este acto haga todo bien**. Por eso el criterio de «Hecho» de las piezas L, M y N se lee así: `decisiones.tsv`, escritor y pines en main con test VERDE **y** `status` antes/después medido **en la rama con el marcador escrito en el árbol sin commitear** (como hizo #1101), pegado en la nota. El valor publicado en main llega con el primer `[deriva]` tras la reparación; la NC de este acto lo dice con `DIFERIDO-A: GEN2-TUBERIA-VISTA-NORMALIZADA-1`. No es PARO.
3. **Concurrencia.** Si TUBERIA-VISTA-NORMALIZADA-1 corre en paralelo, toca `verify.yml` (paso de derivados) y `tools/corrida0.py registro` (columnas de la vista): este acto no toca ninguno de los dos salvo lo que P-L(a)-(c) exige, y rebasa si choca.
