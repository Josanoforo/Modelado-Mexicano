ESTADO: CONSUMIDO — `PR #631`, `ADR-410`.
ENTORNO: CAJA
ENCOLADO: 2026-09-08 · ACTO GEN2-V213 reencola: reemplaza el cuerpo de este archivo, verbatim (texto técnico sin cambios respecto de la versión que `ACTO GEN2-T7-CIERRE` encoló), y recomputa la COMPUERTA contra `origin/main = df9336c5` bajo la regla nueva E.5 de `instrucciones-proyecto-v2_13.md`.
BITACORA:
- 2026-09-08 · GATEADO (recomputado por `ACTO GEN2-V213`). Verificado: `E5-0` sigue sin fusionar (nunca ejecutado — la pieza previa de esta misma cola sigue `GATEADO`, ver su archivo), así que la primera cláusula de la compuerta original ("E5-0 fusionado") sigue sin cumplirse por sí sola. Los seis checks de E3.1 sí están en verde en `origin/main` (`PR #608`, `ADR-394`). Añadida además, por E.5 de `instrucciones-proyecto-v2_13.md` — «ninguna corrida GEN2 real se lanza … hasta que … la readiness del marcador esté hecha» —, la misma condición que gatea a `E5-0`: `ACTO GEN2-E7` con Go/No-Go propio cerrado (`ADR-394`, D12, asigna esa readiness a E7, no a E5). **COMPUERTA (vigente): `E5-0` fusionado **y** `GEN2-E7` cerrado — ninguna de las dos hoy.** `GEN2-E5` es, de las cuatro piezas de esta reencola, la que estaba más lejos de cumplirse y sigue así.
- 2026-09-08 · **GATEADO → EJECUCIÓN** (promoción registrada por el propio ejecutor, patrón `/despacha` D3b · E7-D). Firma de mesa 8/sep/2026, verbatim: «629 mergeado. […] estamos listos para irnos como gordas en tobogán» (D-1 vigente: «sí corremos hoy»). Las **dos cláusulas de la COMPUERTA vigente**, re-derivadas por esta sesión contra `origin/main = fbce146d`, no heredadas del mensaje de mesa:
  1. **`E5-0` fusionado — CUMPLIDO.** `git ls-tree -r --name-only origin/main data/corrida0/CALC-000{1,2,3}/` → los tres directorios traen `spec.yaml` + `spec.md` + `medidor.py` (9 blobs). `origin/main = fbce146d` es el merge de `PR #629` (`Merge pull request #629 from Josanoforo/acto/gen2-e5-0-specs-ejecutables`).
  2. **`GEN2-E7` cerrado con `GO-MARCADOR` — CUMPLIDO.** `git cat-file -e origin/main:forense/notas/2026-09-08-GEN2-E7-readiness-marcador.md` → existe; `git show … | grep -c GO-MARCADOR` → **2** (≥1 exigido). **Uso, no mención** (D-6/T25): la §B es `### B · Go/No-Go → **GO-MARCADOR**` y la línea 215 lo emite `tests/gonogo_marcador.py -> GO-MARCADOR (6/6)`.
  Cláusulas adicionales del **GO por producto** del cuerpo, re-derivadas también: tests de E3.1 en verde (`python3 tests/test_corrida0.py` → **59 casos · 59 ok · 0 FALLOS**); `preflight` **VERDE en los tres CALC**, con la salvedad de `FP-352` — dentro del sandbox la raíz `descargas_mx` se lee vacía y `preflight` da `BLOQUEADO` falso; los seis payloads de `CALC-0001`/`CALC-0002` existen y su `sha256` **COINCIDE** al correr fuera del sandbox; `cmd_status` en `main` (`tools/corrida0.py:3135`, corre y emite los contadores); `tests/check.py --baseline` **LÍNEA BASE VERDE**; entorno CAJA (`CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=sin_variable`, `corpus=SI(examinados=398)`), **no NUBE**. La cláusula «caja limpia (`limpia_arbol --reporta` vacío)» **NO se cumple y no se puede cumplir desde el ejecutor** — mismo limbo que declaró `E5-0`: el reporte da 11 worktrees vivos y 1 rama remota `fuera_de_politica` (`claude/new-session-98j16e`), todas ajenas a esta sesión, y `/acto` §1.0.d prohíbe decidir su borrado desde aquí. Se declara, no se absorbe.
- 2026-09-08 · **CONSUMIDO** por `PR #631` (`ADR-410`), rama `acto/gen2-e5-calc-0001-0003`, entorno UBUNTU (caja) con microdato abierto. `CALC-0001` y `CALC-0002` SELLADAS (54 + 29 = 83 `RESULT` GEN2 en el árbol); `CALC-0003` PARÓ sin sellar (`MergeError`: 3 filas vacías en `c_portad.dta`). `T-REPRO` (T35) WARN→FAIL. **El contador NO dejó el cero y no es fallo de cableado**: falta una firma de mesa en `data/corrida0/decisiones.tsv` (`FP-356`, `NC-0045`), con control positivo simulado en memoria de que la mueve a 2 / 83 / 83 / 5. `NC-0042` CERRADA. Reservas `NC-0044`..`NC-0047`; firmas `FP-353`..`FP-357`. Detalle en `forense/notas/2026-09-08-GEN2-E5-corridas-selladas.md` y en `## NO-CORRIDO / RESERVAS` del encargo archivado.

──── CUERPO VERBATIM DEL ENCARGO (A.3) · el despachador NO lo edita ────

## E5 · ACTO GEN2-E5 · CALC-0001..0003 — correr, sellar, verificar (los cálculos, al final de todo)

Cabecera: **UBUNTU (caja, corpus montado)**, worktree nuevo desde `origin/main` con E5-0 fusionado · **Opus** · COMPUERTA por producto (**el GO**): los tres `spec.yaml` y sidecars en `main`; los tests de E3.1 en verde en `origin/main` (salida pegada); `preflight` VERDE en los tres CALC desde `origin/main`; `cmd_status` en `main` (E6) y `GO-MARCADOR` (E7); base congelada (si `main` avanza durante el acto, no se auto-mezcla); caja limpia (`limpia_arbol --reporta` vacío). NO se lanza en NUBE.
Por CALC, en orden: `preflight` → `run` (contrato y rutas resueltas) → `verify` (`REPRODUCE/IDENTICO` esperado; cualquier otra combinación se reporta como salga) → COMMIT-2 (`ejecucion.json`, `resultados.json`, `sello.sha256`; entradas `PENDIENTE-DE-MESA` en la propuesta citando `corrida0_resultado_id`) → `registro` y `status` (deben subir `N_corridas_selladas` y `N_resultados_sellados`; si no suben, algo del cableado falló y es hallazgo, no se fuerza). Spec mal contra el dato: **no se edita**; nace el siguiente id con `sucesor_de` y un tercer commit lo declara. Una pieza que PARA no tumba las otras; PARO de entorno A.2 detiene todo. Al cierre: **T-REPRO pasa de WARN a FAIL** (`tests/check.py`), declarado en E6 y ejecutado aquí, con línea base que congela GEN1. La sustancia metodológica es la de los `spec.yaml` congelados en E5-0: este acto no decide nada. Cierre A.14; rama borrada.
Perímetro: `data/corrida0/CALC-000{1,2,3}/{ejecucion.json,resultados.json,sello.*}` · `data/corrida0/{corridas,resultados,usos}.tsv` (re-derivados) · `forense/notas/<fecha>-GEN2-E5-*.md` · `milpa/tramite-ola5-propuesta-v0.yaml` (solo `append`) · `tests/check.py` (T35 WARN→FAIL) · `forense/firmas-pendientes.tsv` (recibo) · `forense/no-corrido.tsv` · cascada. **No toca specs, `spec.yaml`, `tramite.yaml` ni canon.** Si te encuentras escribiendo fuera de esta lista, PARA.
Contador: **medición: sí** — `N_corridas_selladas 0→3`, `N_resultados_sellados 0→k`, `dependencias_numericas_legacy_activas` 162 → 162−k. Sucesores: `SELLO-4` (mesa), C0-B lote 1 (specs para los 7 coeficientes y las 4 conductas de D10, sobre `demanda-corridas.tsv`), C0-C, C0-D.
