# S13 · Pre-registro de `R10.3` sobre LAPOP 2019/2021/2023 — segunda medición de la cláusula D2-h

### `prereg-caja-S13` · **v1.0** · 7 de septiembre de 2026

> | | |
> |---|---|
> | **ARCHIVO** | `forense/prereg-caja/S13-R10-3-spec-v1_0.md` |
> | **NOMBRE ESTABLE** | **`prereg-caja-S13`** — cítalo así, nunca por nombre de archivo |
> | **QUÉ ES** | Pre-registro, congelado antes de abrir ningún `.dta`, de la segunda medición de `comunicacion.inseguridad.ver_oir_callar` (`R10.3`) que la cláusula de movimiento D2-h exige — sobre las tres olas de LAPOP AmericasBarometer México ≥2019 ya en corpus (2019/2021/2023), una ola por vez, un veredicto por ola, nunca agrupadas. **Corrige una premisa del encargo antes de congelar el diseño (§0.2): la batería completa de 2004 no es replicable idéntica en ninguna de las tres olas — el desenlace (`aoj1`, denuncia) no existe fuera de 2004.** |
> | **QUÉ NO ES** | No abre ningún `.dta` — NUBE, sin corpus montado. No calcula ninguna proporción, ningún IC95, ninguna celda. No mueve el tier de `R10.3` (`[FUERTE]`, D2-h) — la cláusula de movimiento decide sola, condicionada al resultado que un acto `CAJA` produzca. No inventa un desenlace sustituto que D2-h no autorizó — declara que, con el corpus actual, ninguna ola ≥2019 satisface el criterio "misma batería" tal como D2-h lo escribió, y dos caminos declarados para que mesa decida (§0.2). |
> | **VERIFICAS ASÍ** | Caja abre cada ola por separado y confirma, antes de calcular, que `aoj1` (denuncia) sigue ausente — si el codebook completo de alguna ola trae un ítem de denuncia que el inventario no capturó, ese hallazgo se reporta antes de continuar con el falsador sustituto de §2. |

**Acto:** `ACTO MAESTRA38-N20-N21 · DOS-SPECS-CSES-R10-3`, 7/sep/2026, entorno **NUBE**, sobre `origin/main = 3d6dee3` (base refrescada, ver `S12-CSES-spec-v1_0.md`, cabecera, para la verificación de que el desplazamiento de SHA no toca este perímetro).

---

## 0 · Ficha bajo prueba y corrección de premisa (A.8/D-13)

### 0.1 · Definición vigente, cláusula D2-h y estado de la propuesta

`canon/modelo-decision-v4_0.md:778` (§7, tabla): `R10.3 | L299 | Inseguridad/autoridad no confiable → "ver, oír y callar" | [FUERTE] | Sí`. `python3 tools/ya_medido.py R10.3` (corrido al redactar esta spec): resuelve a `comunicacion.inseguridad.ver_oir_callar`; `milpa/tramite-ola5-propuesta-v0.yaml:3559` trae `civico...` — en realidad `comunicacion.inseguridad.ver_oir_callar_lapop2004`, `situacion: SELLADA-SIN-CARGA`, `tier: SELLADA` (hereda `[FUERTE]`, D2-h, sin movimiento).

**D2-h (`canon/modelo-decision-v4_0.md:785`), verbatim de la cláusula que esta spec ejecuta:** *"segunda medición con la misma batería (`AOJ1/AOJ11/AOJ12/vic1`) en una ola ≥ 2019 ya en corpus; si tampoco discrimina → FUERTE→MEDIA por D2; si discrimina → la de 2004 queda como acotada temporalmente."*

**Primera medición (2004), resultado ya archivado — reusado, no re-corrido aquí** (`ACTO MAESTRA38-L18`, `forense/notas/2026-09-06-MAESTRA38-LOTE-LAPOP-L18-resultados.md`, spec sellada `prereg-caja-S8-L18`): veredicto **`NO-DISCRIMINA`** — silencio 64.91% (contexto ALTO) vs. 67.42% (contexto BAJO), IC95 de `C_completo` = `[−13.90, +10.03]` pp, sobre 260 víctimas de 266 con `vic1=1`/`aoj1` válido (universo real medido, no el 5-10% pre-registrado — la tasa real fue 17.22%).

### 0.2 · Corrección de premisa — la batería `AOJ1/AOJ11/AOJ12/vic1` NO es replicable idéntica en ninguna ola ≥2019 (verificado, no asumido)

`S8-L18-spec-v1_0.md §0.2`/§7 ya había verificado, antes de que existiera D2-h, que el desenlace del módulo `AOJ` (`aoj1`/`aoj1a`/`aoj1b`, la variable de denuncia) **existe únicamente en la ola 2004** del corpus — 0 filas en 2006/2019/2021/2023, verificado variable por variable contra el inventario de esa fecha. **Re-verificado aquí contra `data/inventario-reactivos-descargas-mx-v1_2.tsv` (versión vigente hoy, sucesora de la `v1_1` que `S8` usó), búsqueda exacta por `variable_id`, ambas capitalizaciones, sobre los tres archivos declarados por el encargo:**

| variable | 2019 (`Mexico LAPOP AmericasBarometer 2019 v1.0_W.dta`) | 2021 (`MEX_2021_LAPOP_AmericasBarometer_v1.2_w.dta`) | 2023 (`MEX_2023_LAPOP_AmericasBarometer_v1.0_w.dta`) |
|---|---|---|---|
| `aoj1` (desenlace, denunció) | **0 filas** | **0 filas** | **0 filas** |
| `aoj11` (inseguridad barrio) | X — «Percepción de inseguridad en el barrio» | X — «Percepción de inseguridad en el barrio» | X — «Percepción de seguridad en el vecindario» |
| `aoj12` (confianza castigo) | X — «Confianza en que el sistema judicial castigue a los culpables» | **0 filas** | X — «Si es víctima de un crimen, la fe en el sistema de justicia» |
| `vic1` (víctima, filtro) | **0 filas — renombrada `vic1ext`** («Víctima de delincuencia en los últimos 12 meses») | **0 filas — renombrada `vic1ext`** («Víctima de delincuencia en los últimos 12 meses») | **0 filas — renombrada `vic1ext`** («Víctima del crimen en los últimos 12 meses») |

**Confirmado: `D2-h` cita una batería (`AOJ1/AOJ11/AOJ12/vic1`) de la que dos de los cuatro elementos no existen bajo ese nombre exacto en ninguna ola ≥2019, y uno de los cuatro (`aoj1`, el desenlace mismo — la variable de denuncia que hace posible medir el silencio) no existe bajo NINGÚN nombre localizable en el inventario.** Esto no es un hallazgo nuevo de esta pieza — es la misma limitación que `S8 §0.2`/§7 ya declararon el 5/sep, antes de que D2-h existiera (D2-h se escribe el 6/sep, citando el resultado de `S8`/`L18` pero sin re-verificar, en su propio texto, si el ítem que pide replicar sigue existiendo fuera de 2004). **D2-h, tal como está redactada, describe una condición (`se_mueve_si`) que el corpus actual no puede satisfacer para ninguna de las tres olas que el encargo nombra** — no porque el falsador no sea construible en absoluto, sino porque el desenlace específico (`aoj1`, denuncia) que la batería original medía es exclusivo de 2004.

**Esto no es un `NO-CONSTRUIBLE` de la regla completa — es una corrección del alcance de "misma batería".** El antecedente (`aoj11`, y `b18`/`b10a`/`aoj12` con huecos declarados en `S8 §0.2`, tabla) sí sobrevive en las tres olas nuevas (con el hueco de `aoj12` en 2021, ya conocido). Lo que no sobrevive es el desenlace de denuncia. Dos caminos, declarados aquí, no adjudicados — sucesor de mesa/`TRAMITE-6` decide, esta pieza los deja listos para que quien decida no tenga que re-investigar el corpus:

* **Camino A — desenlace sustituto declarado.** El corte `aoj11`/`b18`/`aoj12` (antecedente, "contexto de inseguridad/autoridad no confiable") se conserva idéntico a `S8 §3` (mismo `INDICE_CONTEXTO`, mismas dicotomizaciones de mitad de escala); el desenlace se sustituye por otra variable de conducta comunicativa/silencio que sí exista en 2019/2021/2023 — a determinar por caja/mesa (candidato más cercano en la misma familia de módulo: ninguno identificado en el inventario todavía; esta spec no lo inventa). Bajo este camino, el `se_mueve_si` de D2-h **no se satisface literalmente** ("la misma batería"), y cualquier corrida bajo el Camino A se reporta como **evidencia relacionada, no como la segunda medición que D2-h pactó** — declarado así, explícitamente, para que nadie reporte un veredicto de Camino A como si cerrara la cláusula.
* **Camino B — D2-h queda `NO-DISPARABLE` con el corpus actual.** Se declara que la condición de movimiento de D2-h no puede evaluarse tal como está escrita hasta que el corpus incorpore una ola ≥2019 con el desenlace de denuncia del módulo `AOJ` (si existe en el instrumento completo de LAPOP y solo no fue capturado por el inventario — a verificar por caja al abrir codebook completo, no solo el censo de variables) o hasta que mesa reescriba `se_mueve_si` con un desenlace distinto. Bajo este camino, esta spec pre-registra únicamente el falsador de **contexto/antecedente** (§2, abajo) como la pieza que sí es construible con el corpus de hoy, dejando expresamente fuera cualquier afirmación sobre "denuncia"/"silencio" en las olas nuevas.

Esta spec se redacta bajo el **Camino B** — no inventa un sustituto de desenlace sin mandato de mesa — y deja el Camino A declarado como sucesor posible si mesa/`TRAMITE-6` lo autoriza con una variable nombrada.

### 0.3 · Precedente de la misma corrección en este lote

`S12-CSES-spec-v1_0.md §0.2`/§2 (mismo acto, pieza hermana) aplica el mismo criterio: declarar `NO-CONSTRUIBLE-SIN-CODEBOOK` en vez de inventar un desenlace, cuando el inventario por censo de nombres no basta para confirmarlo. Esta pieza aplica el criterio análogo al desenlace de `R10.3` sobre las olas nuevas: declarar la ausencia verificada, no forzar un sustituto sin mandato.

---

## 1 · Antecedente — batería `AOJ11`/`B18`/`AOJ12`/`vic1ext`, idéntica a `S8 §1`/§3 salvo el nombre de `vic1`

**Variables (verificadas contra `data/inventario-reactivos-descargas-mx-v1_2.tsv`, §0.2):**

| ola | `aoj11` | `b18`/`B18` | `aoj12`/`AOJ12` | `b10a`/`B10A` (verificación cruzada, si existe) | `vic1ext` (filtro, ex-`vic1`) |
|---|---|---|---|---|---|
| 2019 | X | (a confirmar; `S8 §1.1` reportó X para esa fecha, re-confirmar al abrir) | X | ausente (`S8 §1.1`) | X |
| 2021 | X | X (`S8 §1.1`: "Confianza en la policía") | **ausente** | ausente | X |
| 2023 | X | X | X | X (`S8 §1.1`: "Confianza en el sistema de justicia") | X |

Los huecos de 2021 (`aoj12` ausente) y la ausencia de `b10a` en 2019/2021 son los mismos que `S8 §0.2`/tabla ya documentó — se heredan sin re-verificar variable por variable de nuevo aquí salvo `vic1`→`vic1ext`, que sí es un hallazgo re-confirmado por esta pieza (no estaba en la tabla de `S8`, que solo cubría el antecedente `AOJ11`/`B18`/`B10A`/`AOJ12`, no el filtro de victimización).

`INDICE_CONTEXTO` = mismo constructo que `S8 §3`: número de indicadores en dirección de inseguridad/desconfianza entre `aoj11`, `b18`, `aoj12` (los tres presentes; `b10a` como verificación cruzada donde exista) — dicotomizado `ALTO` (2 o 3 de 3) / `BAJO` (0 o 1 de 3). **Dirección de las escalas `b18`/`aoj12`/`b10a` no confirmada sin codebook para las olas nuevas** — `S8 §0.2` (línea 36-47) tuvo que resolverla contra el cuestionario 2004 (Tarjeta A, 1=Nada...7=Mucho) porque el `.dta`/`.sav` de esa ola no traía etiquetas; caja confirma si 2019/2021/2023 sí las traen (mismo criterio que evitó invertir la escala a ciegas en `L18`) antes de reusar el corte de `{1,2,3}`/mitad inferior.

**Filtro de universo:** `vic1ext=1` (víctima de delito en los últimos 12 meses) — mismo rol que `vic1` cumplió en 2004, bajo el nombre nuevo. Tamaño del subuniverso: no verificado, pendiente de que caja lo cuente por ola (mismo criterio de declaración que `S8 §0.3` fijó para 2004, donde la tasa real —17.22%— resultó el doble de lo pre-registrado).

---

## 2 · Falsador de esta spec — contexto/antecedente, sin desenlace de denuncia (Camino B, §0.2)

**Sin un desenlace de denuncia disponible, esta spec no pre-registra una celda `DENUNCIA` para las olas ≥2019.** Lo que sí pre-registra, como la pieza construible con el corpus de hoy: el **marginal de `INDICE_CONTEXTO`** (proporción `ALTO` vs. `BAJO`) sobre el subuniverso de víctimas (`vic1ext=1`) de cada ola, por separado — evidencia descriptiva de que el antecedente de `R10.3` (contexto de inseguridad/autoridad no confiable) sigue presente y es medible en 2019/2021/2023, **sin que esto constituya una prueba de la regla** (una regla `SI...ENTONCES` no se falsa con el marginal de su antecedente solo — se necesita el desenlace, que no está). Este marginal se reporta por ola, nunca agrupado (D2-h/`se_mueve_si`, "una ola, un veredicto", extendido al marginal por la misma razón de diseño muestral distinto entre olas).

**Este marginal NO dispara el `se_mueve_si` de D2-h** (§0.2) — se declara explícitamente como insuficiente para eso. Se reporta porque es lo único que el corpus actual permite construir sin inventar una variable, y porque confirma o corrige la expectativa de que el contexto de inseguridad de `S8 §1.1` ("estable en el tiempo") se sostiene también en las tres olas más recientes.

**Cota de n mínima:** numerador `<10` ⇒ `NO-ESTIMABLE`, misma guardia que el resto de la serie.

---

## 3 · Universo y ponderador — declarado por ola, verificado al abrir (D2-h/S6)

| ola | ponderador candidato | archivo |
|---|---|---|
| 2019 | `wt` («Peso del país») | `Mexico LAPOP AmericasBarometer 2019 v1.0_W.dta` |
| 2021 | `wt` («Peso de la muestra») — `weight1500` («Estratos de muestra») declarado como candidato de estrato, no de ponderador, pese al nombre | `MEX_2021_LAPOP_AmericasBarometer_v1.2_w.dta` |
| 2023 | `wt` («Peso de la muestra») | `MEX_2023_LAPOP_AmericasBarometer_v1.0_w.dta` |

**`weight1500` (2021) trae la etiqueta «Estratos de muestra» en el inventario — no es, por el nombre de columna, evidencia de que sea un ponderador pese a la raíz `weight`; caja lo confirma contra el codebook antes de usarlo para ponderar** — la trampa exacta que `S6 §1.3` (`fac_3b` vs. `fac_3b_px`) advirtió contra en otro payload: un nombre parecido a "peso" no garantiza que la columna pondere. UPM/estrato no confirmados en el inventario para ninguna de las tres olas — caja los busca al abrir, mismo criterio que `S2-L2 §1.0` fija para payloads sin diseño muestral visible en el censo.

**Ninguno de los cortes ni ponderadores está confirmado sin codebook — caja los declara al abrir, y si la variable no existe en el archivo, PARA** (cláusula S6, verbatim del encargo).

---

## 4 · Escala de falsación `B-bis`

**Bajo el Camino B (§0.2), esta spec no define una fila `CORROBORADA`/`CONTRARIA`/`NO-DISCRIMINA` de la regla completa para 2019/2021/2023** — no hay desenlace de denuncia con el que construir `C_completo` como en `S8 §4`. Se pre-registra únicamente la fila de marginal (§2):

| | Marginal `INDICE_CONTEXTO` (por ola) |
|---|---|
| **Qué se reporta** | proporción `ALTO` sobre el subuniverso `vic1ext=1`, con IC95, por ola, sin comparación entre olas (diseños muestrales y preguntas de antecedente no idénticos entre 2019/2021/2023, §1) |
| **`NO-ESTIMABLE`** | numerador `<10` en la ola correspondiente |
| **Lo que este marginal NO decide** | si `R10.3` discrimina o no en ola ≥2019 — eso exige el Camino A (desenlace sustituto, con mandato de mesa) o que caja encuentre, al abrir el codebook completo de cualquiera de las tres olas, una variable de denuncia que el inventario no capturó (§0.2, declarado como posibilidad a verificar, no descartada) |

**Si caja, al abrir cualquiera de las tres olas, encuentra el ítem de denuncia (`aoj1` bajo otro nombre, o un ítem equivalente del módulo `AOJ`/`VIC`) que el inventario no reveló:** ese hallazgo reabre esta spec — el falsador completo de §4/`S8` se vuelve construible con esa ola, y caja lo reporta como hallazgo de apertura antes de calcular, no como una corrida silenciosa bajo una spec que había declarado la variable ausente.

---

## 5 · `se_mueve_si` — heredada de D2-h sin cambio, con la salvedad de §0.2

Verbatim de D2-h: *"segunda medición con la misma batería (`AOJ1/AOJ11/AOJ12/vic1`) en una ola ≥ 2019 ya en corpus; si tampoco discrimina → FUERTE→MEDIA por D2; si discrimina → la de 2004 queda como acotada temporalmente."* **Esta spec no reescribe esta cláusula** — la hereda literal, per instrucción del encargo. Lo que agrega, declarado, no adjudicado: con el corpus verificado hoy (§0.2), esta condición **no tiene ola ≥2019 que la satisfaga literalmente** ("misma batería", con `aoj1` incluido) — el `se_mueve_si` de D2-h queda, hasta que exista el Camino A con mandato de mesa o un hallazgo de apertura (§4), sin ola que lo dispare. `R10.3` conserva `[FUERTE]` no porque una segunda medición lo haya confirmado, sino porque la segunda medición que D2-h pidió sigue sin poder correr con el corpus de hoy — distinción que esta spec deja explícita para que nadie reporte "D2-h no disparó" como si fuera equivalente a "D2-h se evaluó y no discriminó".

---

## 6 · Archivos que la caja necesita abrir

| id de manifiesto | archivo | sha256 |
|---|---|---|
| `mexico_lapop_americasbarometer_2019_v1_0_w` | `Descargas Manuales/Mexico LAPOP AmericasBarometer 2019 v1.0_W.dta` | `c88f79ebb8e73c473cd78d894eb093261f172e736a35bd7bc677b4e8b1454a57` |
| `mex_2021_lapop_americasbarometer_v1_2_w` | `Descargas Manuales/MEX_2021_LAPOP_AmericasBarometer_v1.2_w.dta` | `153fb0f81acfffb41bbe247b7fce81159350e1fdfcb342a14bb034bcb7d95566` |
| `mex_2023_lapop_americasbarometer_v1_0_w` | `Descargas Manuales/MEX_2023_LAPOP_AmericasBarometer_v1.0_w.dta` | `4a9410a53cde9d11edeb23465bdbadce8a6abcc18330b6eebe2a4493be6e765c` |
| `mex_2023_lapop_americasbarometer_v1_0_w_2` (gemelo `.sav`, verificación) | `Descargas Manuales/MEX_2023_LAPOP_AmericasBarometer_v1.0_w.sav` | `a355e4ca93476d66a4138ec7f565574214fbba517de219d71ff1c2851a61fa2a` |

No hay codebook registrado por separado en el manifiesto para 2021/2023 (verificado, `grep -in codebook data/manifiesto.yaml` solo devuelve el de 2019, mismo hallazgo que `S8 §6` ya reportó) — caja abre el `.dta`/`.sav` mismo para confirmar escalas y buscar el ítem de denuncia que §0.2 declara ausente por censo, no descartado hasta abrir codebook completo.

---

## 7 · Qué NO hace este acto

No abre ningún archivo de §6. No calcula ninguna celda, marginal ni IC95. No mueve el tier de `R10.3` (`[FUERTE]`, D2-h). No reescribe D2-h — la hereda verbatim (§5), y declara, sin adjudicar, que su condición de disparo no tiene ola que la satisfaga con el corpus de hoy (§0.2). No inventa un desenlace sustituto de denuncia sin mandato de mesa (Camino A, declarado, no ejecutado). No toca `S12-CSES-spec-v1_0.md` (pieza N20, mismo lote, archivo separado). No toca `canon/modelo-decision-v4_0.md`, `milpa/**`, `data/**` ni `forense/hallazgos.md`.

**El primer resultado que produzca este procedimiento es el que se reporta.**
