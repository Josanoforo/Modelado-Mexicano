ENCARGO · ACTO MAESTRA35-N2 · F-DD-RANGOS-Y-M-DIN — invoca /acto SHA de redacción: b6b923f (merge PR #469). Redacta dirección (Fable), 2/sep/2026, contra v2.12. Estado: LISTO PARA LANZAR. COMPUERTA: ninguna — declaración explícita. La condición que DF-a puso («sucesor tras la revisión del 8/sep (MAESTRA34-E1)») está cumplida por producto: MAESTRA34-E1 corrió el 2/sep (PR #463, ADR-285); las fechas son límites, no compuertas (PR #462). ENTORNO ASIGNADO: NUBE (`cloud_default`) — el emisor lee solo repo (`milpa/tramite.yaml`, `procedencia.yaml`, marcos). NO se lanza en UBUNTU: las cajas están ocupadas por MAESTRA35-L1 y MAESTRA35-L2 y este acto no abre microdato. MODELO SUGERIDO: Opus (extensión de una fórmula sellada con regresión total; no bajar).
⚠️ CIEGO A R (contrato del emisor, `CIEGO_A_R`): esta sesión NO abre `forense/prereg-duelo-v2/corridas-R/` ni ninguna columna de valor de R — y hoy menos que nunca, porque MAESTRA35-L2 está escribiendo ahí en paralelo. Sí abre el motor: M se emite desde el motor por diseño.
CARRILES: MAESTRA35-L1 (caja: `tools/medidor_*`, propuesta, `codificacion-R`, notas) · MAESTRA35-L2 (caja: `tools/arbitra.py`, `corridas-R/`, `notas-arbitra/`) · `/despacha` N3 → N5 (nube: `corridas-L/`, `L-extraido*`, scoreboard; N3 lee `corridas-M/` para puntuar — si este acto fusiona antes, N3 verá `M-DIN-M-01__v1_2.json` y aun así puntúa 13 de 14 porque su letra lo dice y porque DIN-M-01 sigue sin R; ninguno es compuerta del otro). Este acto no toca ningún archivo de esos tres. Renumera quien fusiona segundo.
FIRMAS DE MESA — verbatim. El ejecutor propaga, no decide (SELLA-3).

* Mesa, 2/sep/2026: «Maestra35-N2 estoy ok con lanzarla» (mesa escribió el rótulo con esa capitalización; serie normalizada por dirección: MAESTRA35-N2).
* DF-a (mesa, 1/sep/2026), verbatim en `forense/prereg-duelo-v2/exclusiones-v1_2.md`: «DIN-M-01 queda SIN M este ciclo, declarada como exclusión con razón para N3 (13 de 14); extender F-DD a rangos de ola es sucesor tras la revisión del 8/sep (MAESTRA34-E1), no de este acto.» → este es el sucesor.
* Lo que NO está firmado y este acto no decide: si DIN-M-01 entra al scoreboard de este ciclo (no puede: sin R), y si la enmienda ENIF 2024 de `tiene_ahorros` debe cambiar el M de alguna celda (no: M es lo que el motor emite para la conducta que el marco nombra, y esa conducta es `tiene_ahorros`, la línea ENNViH conservada — se verifica, no se decide).

═══ VERIFICACIÓN DE EXISTENCIA (A.8) — contestada por dirección contra b6b923f ═══ (1) ESTRUCTURA. Emisor M: `tools/emite_m.py` (skill `/emite-m`; F-DD = `calcula_grado_DD`, `_RE_OLA_CAL = ^([^\s(]+)\s+(\d{4})`, líneas 165-187; tabla `_OLA_CALIBRACION_FIJA` 68-77; regresión `regresion()` 294-336 contra `M-TRA-M-01/02`), salida `forense/prereg-duelo-v2/corridas-M/M-<id>__v1_2.json` (convención de MAESTRA34-N2: nunca se toca un M existente; `CONGELADO-M-v1_2.sha256` sella el marco TSV, no los M — añadir un M no lo rompe), exclusiones en `forense/prereg-duelo-v2/exclusiones-v1_2.md`, marco en `marco-M-sorteado-v1_2.tsv` / `marco-M-congelado-v1_2.tsv`. (2) CONTENIDO, comando y salida cruda:

* `ls forense/prereg-duelo-v2/corridas-M/ | grep -c DIN-M-01` → 0. NO-ENCONTRADO.
* `grep -c "olas\|rango\|ola N" tools/emite_m.py` → 0 en el parser; `git log --format=%h -- tools/emite_m.py` → 1 commit (f613f1c, MAESTRA33-E6): nadie ha extendido F-DD. EXISTE-NO-SATISFACE.
* El fallo está medido, no supuesto (`forense/notas/2026-09-02-marco-M-v1_2-emite-m.md` §Hallazgo): `ValueError: ola_calibracion sin forma '<INSTRUMENTO> <ANIO>...'` sobre `"ENNViH ola 2 (2005-06) …"`. Y el insumo cambió después: tras MAESTRA35-N1, `milpa/tramite.yaml:349` dice `ola_calibracion: "ENNViH ola 2 (2005-06) -- ponderador fac_3b vive en esta ola; ENIF 2024 -- ponderador FAC_PER (enmienda_enif2024)"` — dos instrumentos en un campo. El parche de una línea que aquella nota propuso (entrada fija «ENNViH 2005») ya no describe el campo: se descarta y se extiende el parser.
* Lo que el marco sellado ya adjudicó para esta celda (`marco-M-congelado-v1_2.tsv`, DIN-M-01, `razon_DD`): «(encuesta,ola)=(ENNViH/MxFLS,2002 (ola 1)) != ola_calibracion=ENNViH olas 2-3 (2005-06 -> 2009-12), panel retenido -> validacion externa». `grado_dependencia` sigue `PENDIENTE-D-D` en las cuatro DIN-M. Discrepancia de insumos, declarada: el motor dice «ola 2», su `universo` dice «ambas olas», el marco dice «olas 2-3». Para DIN-M-01 (ola 1) las tres lecturas dan lo mismo — transferencia de ola, P1 — así que este acto no la resuelve; la reporta (afectaría a DIN-M-03, ola 3, que no está sorteada).
* Conducta que el marco nombra: `tiene_ahorros` (columna `conducta`). `grep -n "conducta: tiene_ahorros\b" milpa/tramite.yaml` → una sola línea, la ENNViH conservada (`clase: "MEDIDO·p(tasa base ponderada)"`); la enmienda ENIF vive como `tiene_ahorros_enif2024`. El emisor toma la primera coincidencia exacta (`_primera_linea`, `\b`), así que M para DIN-M-01 será la línea ENNViH. EXISTE-SATISFACE.
* FP máximo por comando 240; ADR máximo 289. MAESTRA35-L1 tiene pre-asignado FP-241…, MAESTRA35-L2 FP-244…: este acto toma el primer libre al arrancar y lo dice. (3) COBERTURA RETROACTIVA. `emite_m.py` nace 1/sep; el marco v1_2 el 2/sep; `exclusiones-v1_2.md` el 2/sep. Todo posterior al emisor: la ausencia de soporte de rangos es real.

PIEZAS P0 · REGRESIÓN DE PARTIDA (un commit, antes de tocar el emisor). Con el emisor SIN editar, re-derivar hoy las 13 celdas del sorteado v1_2 que ya tienen M (`CIV-M-01/02/04/10/12/13`, `FAM-M-01/05/06/07`, `TRA-M-02/03/07`) más `M-TRA-M-01/02` de la regresión P2, y comparar campo por campo contra el archivo comiteado (misma prueba que MAESTRA34-N2 corrió con «cero drift»). Es la línea base: el motor cambió tres reglas después de esa prueba (MAESTRA35-N1) y hay que saber si algo se movió ANTES de tocar F-DD. Cualquier campo de valor que diverja se reporta como hallazgo de N1, no se corrige aquí, y la celda afectada queda fuera de la regresión de P1 con su razón. P1 · F-DD v1.1 en `tools/emite_m.py` — un commit propio, diff mínimo, declarado línea por línea. Gramática congelada aquí, no inventada al ejecutar: (a) `ola_calibracion` se parte en SEGMENTOS por `;` a profundidad 0 de paréntesis. De cada segmento se toma solo la CABECERA: el texto antes del primer `--` o `(`, que debe calzar `^<INSTRUMENTO> <OLA-SPEC>` donde OLA-SPEC ∈ { `\d{4}` · `\d{4}-\d{2,4}` (rango de años, p.ej. `2005-06`) · `ola \d+` · `olas \d+-\d+` }. Si tras `ola N` sigue un paréntesis `(\d{4}(-\d{2,4})?)`, ese es el rótulo de año de esa ola y se guarda junto con N. Nada fuera de la cabecera se parsea: «6 olas bienales disponibles» no es una ola. Un segmento que no calza → `ValueError` con el texto, como hoy (el emisor no adivina). (b) Instrumento: se compara el token antes de `/` en ambos lados, sin distinguir mayúsculas (`ENNViH/MxFLS` ≡ `ENNViH`). (c) Ola de la celda (`fila["ola"]`) se normaliza con la misma gramática: `2002 (ola 1)` → {año 2002, ola 1}; `2005-06 (ola 2)` → {rango 2005-06, ola 2}; `2013` → {año 2013}. (d) Regla: P0 VERIFICACION si algún segmento coincide en instrumento Y en al menos un identificador de ola (año, rango o número de ola); P1 PUNTUA en cualquier otro caso, con el mismo `detalle` que hoy (transferencia de instrumento / de ola). Para cadenas de la forma actual (`ENCIG 2023`, `ENVIPE 2025 (unica ola …)`, `ENIGH 2022 (mas reciente …; las …)`) el resultado debe ser IDÉNTICO al de `_RE_OLA_CAL` — es la condición (e). (e) Regresión OBLIGATORIA, salida cruda pegada: `regresion()` (M-TRA-M-01/02) PASA, y las 13 celdas de P0 re-derivadas con el emisor nuevo coinciden byte a byte con el comiteado salvo los campos exentos ya declarados (`fuente`, `correcciones_aplicadas_por_referencia`). Un solo campo de valor distinto = PARO de la pieza: la extensión cambió F-DD para celdas que no debía tocar, y no se ajusta el emisor para forzar el match (`--ajustar` no existe a propósito, y sigue sin existir). (f) `.claude/commands/emite-m.md` gana una sección «Actualización · F-DD v1.1 (MAESTRA35-N2)» con la gramática de (a)-(d) verbatim, mismo patrón que `/arbitra` ya usa para CODIFICA-R-1. No se edita nada por encima. P2 · EMITE `M-DIN-M-01__v1_2.json`, por la vía que MAESTRA34-N2 usó (importar `emite_celda` con `marco_nombre=marco-M-sorteado-v1_2.tsv`, nombre de salida `__v1_2`). Verificaciones congeladas antes de emitir: `grado_DD` debe ser `P1 PUNTUA` con detalle «transferencia de ola» — es lo que el marco sellado adjudicó en `razon_DD`; si F-DD v1.1 devuelve P0, PARO y reporte (la gramática está mal o el marco lo está, y eso lo decide mesa). `cita_p` debe apuntar a la línea ENNViH de `tiene_ahorros` y `clase` a `MEDIDO·p(tasa base ponderada)`; si apunta a `_enif2024`, PARO (el emisor cambió de contrato). Al JSON se le añade el campo `razon_DD_marco` con el `razon_DD` del marco verbatim, para que quien puntúe vea que F-DD y marco coinciden. Y la discrepancia ola 2 / olas 2-3 va en `aviso_F_DD` del propio JSON, no se resuelve. P3 · `exclusiones-v1_2.md`: append fechado bajo la sección de DIN-M-01 — «M emitido por ACTO MAESTRA35-N2 (F-DD v1.1); la celda sigue EXCLUIDA de este ciclo por falta de R (bloqueo ENNViH: lector `.dta` + join `fac_3b` sin estrato/UPM en tabla, MAESTRA34-L2 §4); sucesor de caja declarado.» Nada por encima se edita; la instrucción a N3 (13 de 14) queda como está.
PERÍMETRO Y CONCURRENCIA: `tools/emite_m.py` · `.claude/commands/emite-m.md` (append) · `forense/prereg-duelo-v2/corridas-M/M-DIN-M-01__v1_2.json` (nuevo; ningún M existente se reescribe — `git status` sobre `corridas-M/` debe mostrar solo el nuevo) · `forense/prereg-duelo-v2/exclusiones-v1_2.md` (append) · `forense/notas/` (P0 línea base, cierre) · `forense/hallazgos.md` · `forense/firmas-pendientes.tsv` (filas nuevas) · A.3 · cascada de `/acto`. NO toca `milpa/`, `corridas-R/`, `corridas-L/`, scoreboards, `marco-M-*`, `CONGELADO-M-*`, `tools/arbitra.py`, `tools/medidor_*`. Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.
FP/ADR CANDIDATOS: una fila de recibo (F-DD v1.1 + M de DIN-M-01), primer FP libre al arrancar (máximo derivado 240 en b6b923f; L1 y L2 tienen rangos pre-asignados — si ya fusionaron, el primer libre es mayor; dilo). ADR candidato por el comando de la casa (289 en b6b923f → primer libre); renumera quien fusiona segundo.
CONTADOR: celdas del sorteado v1_2 con M: 13 → 14 · celdas puntuables con L∩M∩R: sin cambio (DIN-M-01 sigue sin R) · reglas medidas: sin cambio · cero estimaciones, declarado. Lo que este acto mueve es un bloqueo de dos: dominio dinero pasa de «sin M y sin R» a «sin R».
Lo que este acto NO hace: no computa R (caja, sesión ciega, sucesor); no re-emite ningún M existente aunque la regresión de P0 muestre drift (lo reporta); no toca el sorteado ni el marco; no decide qué olas calibran `tiene_ahorros` (reporta la discrepancia); no edita instrucciones.
Sucesores declarados, no lanzados: (i) CAJA, sesión ciega — R de `DIN-M-01`: lector `.dta` (`pyreadstat`, disponible en la caja según MAESTRA34-L2 §4) + join `fac_3b` por `folio`+`ls` + diseño de varianza declarado desde `ennvih_diseno/*.pdf` o, si no hay estrato/UPM en tabla, `EE(R)` con la aproximación que el marco permita y la reserva escrita; (ii) si P0 encuentra drift en algún M por las enmiendas de MAESTRA35-N1, un acto de dirección decide si M de v1_2 se re-emite o se conserva «lo que M creía entonces» — no lo decide este acto.
