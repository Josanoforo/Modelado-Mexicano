**ADR-260923-GEN2-TRAMITE-FIRMAS-9-97dc-01**, `ACTO GEN2-TRAMITE-FIRMAS-9`, 23/sep/2026, NUBE, Sonnet 5, sin sub-agentes, MODO ABIERTO. Tres asientos que la máquina no hacía sola: (P2) `data/corrida0/marcador-segmento.tsv` seguía citando `CRUCE-GRUPO::…::edadxescolaridad` como `RESERVADA` pese a que `GOB.gobierno_digital.encig2025.edad_x_escolaridad` tiene `champion_actual: C2` desde F3 (21/sep) y el consumo ya estaba cableado en `tools/marcador_segmento.py` desde `#1025` (`cfc4b3e`) — el TSV nunca se regeneró tras ese cambio; `--escribe` lo hizo, sin editar nada a mano, y de paso incorporó el drift de F2/ENIF acumulado. (P1) `ACTO GEN2-DIN-LOTE-ENIF2024-SECUNDARIA-1` se fusionó (PR #1026) sin `## NO-CORRIDO / RESERVAS` ni `## CONSUMIDO`; se append al pie del encargo archivado, copiado verbatim de su nota de cierre y de la fila NC ya asentada, sin tocar nada arriba de la línea de append. (P3) `NC-…-8e53-04` (protección de rama en `main` / merge queue / quién fusiona, mesa la dejó en blanco dos veces) vivía solo como NC: fila `FP-260923-GEN2-TRAMITE-FIRMAS-9-97dc-01` nueva en `forense/firmas-pendientes.tsv`, `ABIERTA`, sin firmar nada. Ninguna pieza adopta ni mueve `cuenta_gen2`. Suite `--rapido` VERDE, 0 FAIL.
