# ACTO TRIAGE-63 · Triaje de las 63 filas `NO_PROBADO` de `acceso-puertas`

`ENCARGO TRIAGE-63`, 13/ago/2026, redactado contra `a97dc28`, corregido en vuelo por `ADENDA 1` (contra `e90a7a6`, PR #218/ADR-78). Este COMMIT cubre solo COMMIT 1 (triaje, sin red). COMMIT 2 (sondeo) queda **sin ejecutar** — ver §7, PARO DECLARADO.

## 0 · ARRANQUE

1. **REPO.** Clon existente `/home/pc0/Modelado-Mexicano` estaba en una rama vieja (`sesion/cal-conf-faseb-pos4-envipe-paso1`, `302ac5a`, 2026-08-03), no en `main`. Se creó un worktree nuevo dedicado a este acto: `/home/pc0/mm-triage-63`, rama `triage-63`, desde `origin/main`. `git worktree add` reportó `error: could not write config file .git/config: Device or resource busy` (contención conocida de `.git/config` compartido entre worktrees en esta máquina — ver memoria de sesión) — verificado que el worktree quedó funcional pese al error (`git status` limpio, `HEAD` correcto) y que el tracking remoto NO quedó registrado (`git config branch.triage-63.remote/merge` vacíos); el push de cierre usa `git push origin HEAD:triage-63` explícito, no depende de tracking.
2. **SHA.** El encargo se redactó contra `a97dc28`. `ADENDA 1` (recibida en vuelo) declaró que `main` avanzó a `e90a7a6` (PR #218, ACTO A8-LAND segunda vuelta, ADR-78). Verificado por comando: `git fetch origin` → `a97dc28..e90a7a6 main -> origin/main`; `git cat-file -t e90a7a6` → `commit`; ADR máximo en `canon/gobernanza-v1_15.md` → `ADR-78` (coincide con la receta T15 declarada: únicos 78, max 78); `tests/baseline.json.head` → `3d0d1e5fd05567e59f8df65b2e1495b43bbcfc3c` (coincide); `instrucciones-proyecto-v2_8.md` presente en `origin/main` (coincide). Worktree adelantado con `git merge --ff-only origin/main` — sin conflicto, sin commits propios previos.
3. **data/raw.** Presente (`20260813130000.export.CSV.zip`, de trabajo GDELT-UCDP-RECON) — irrelevante para este commit: no se abre ningún microdato.
4. **ENTORNO**, las tres sondas del encargo, crudas:
   ```
   $ echo "$CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE"
   (vacío)
   $ curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://www.inegi.org.mx/
   200
   $ ls data/raw/ 2>/dev/null | head -1
   20260813130000.export.CSV.zip
   ```
   Red disponible en este entorno (CAJA, no NUBE) — la sonda respondió, así que no hay PARO por entorno equivocado. Aun así, este commit (COMMIT 1) se ejecuta sin tocar red: toda la clasificación sale de tablas ya commiteadas (`acceso-puertas`, `manifiesto.yaml`, `cola-adquisicion-2026-08-12.tsv`, notas forenses previas). COMMIT 2 (con red) queda condicionado al gate de §7 — no se ejecutó.
5. **ESPEJO.** Todas las cifras de esta nota salen de `grep`/`awk`/`python3` corridos en `/home/pc0/mm-triage-63` en esta sesión, comando a la vista donde aplica.

---

## 1 · Lo que corrigió `ADENDA 1`, y por qué se sigue en vez de rehacer desde cero

`ADENDA 1` llegó después de que este acto ya había producido un primer intento de bucket A por coincidencia de subcadena (10 filas, tomadas literalmente del prosa del encargo). Esa receta es exactamente el defecto que `ADENDA 1 §2` documenta con evidencia propia: `SE` empareja con `IMSS_Datos_Abiertos_Asegurados` por contención de "se" en "aSEgurados"; un arreglo a nivel de token (≥4 caracteres) tampoco sirve (`CIDE_Panel_Mexico_2006` se lleva 8 filas por `PANEL`/`MEXICO`, `GLOBAL_PREFERENCES_SURVEY` empareja con `WVS_World_Values_Survey` por `SURVEY`). Instrucción explícita: **no emparejar por nombre, con ninguna receta.**

En vez de inventar una tercera receta, este commit usa una que ya existe y ya fue puesta a prueba por otro acto el mismo día: `forense/notas/2026-08-13-reconcilia-puertas.md` (ACTO RECONCILIA-PUERTAS, cierra D11, ya mergeado en `main`). Su §4.1 declara una jerarquía de evidencia de 3 niveles (URL idéntica vía `cola-adquisicion.url_conocida` · cita explícita en el texto de la fila real · declaración propia de SONDA-1) y la aplica fila por fila a 16 casos, rechazando explícitamente 5 más por "parecido de nombre sin URL/cita" (§5.2 de esa nota). Este commit adopta esa misma jerarquía, verifica sus 16 pares por comando (no de memoria) y la extiende con 1 caso adicional que `ADENDA 1` corrige por instrucción directa (`BIARE`).

---

## 2 · Bucket `DUPLICADA-DE-FILA-RESUELTA` — 17 filas

Gobierna ADR-76(e). El encargo original cita la formulación en prosa ("la más reciente cuyo `universo_declarado` cite un portal, no una tabla interna"), pero `ACTO SELLA` (`ADR-76`, ya sellado, `forense/hallazgos.md:251`) corrigió esa misma cita del encargo por la regla real, mecánica: `clase_origen ≠ gap_mapeo_map_b` gobierna sin importar fecha — la prosa producía falsos negativos en filas cuyo `universo_declarado` no repite la palabra "portal" en una frase reconocible por patrón aunque sí sondearon uno real (4 casos nombrados en `PROPUESTA-reconciliacion-universo-puertas.md:30`: `MassMobilization_Dataverse_MMdata`, `openICPSR_Microcredit_MexicoPlacement_proj116334`, `OECD_TrustSurveyData`, `Cenfri_MicroinsuranceMexico`). `acceso-puertas` no tiene columna `clase_origen`; el equivalente operable aquí es "existe fila hermana con `quien_puede` ya resuelto" — el mismo criterio, aplicado a esta tabla.

La cláusula de precedencia no es adorno — `ACTO SELLA` también re-derivó la cifra que `RECONCILIA-PUERTAS §4.3` citaba como evidencia ("14 de 16") y la corrigió a **15 de 16** (verificado aquí de nuevo, por comando, contra la columna "fecha vieja → nueva" de la tabla de §4.2 de esa nota: 15 filas muestran `08-13 → 08-12`, 1 fila — `ISSP` — muestra `08-13 → 08-13`, empatada, no invertida). En esas 15, la fila vieja (`NO_PROBADO`) trae `fecha_sondeo` **posterior** a la fila real que la contradice; una regla ingenua de "gana la más reciente" habría fallado en 15/16 casos.

| # | fila `NO_PROBADO` | fila real (hermana) | evidencia |
|---|---|---|---|
| 1 | `GDELT` | `GDELT_RawDataFiles` | URL exacta (RECONCILIA-PUERTAS §4.2) |
| 2 | `UCDP` | `UCDP_Downloads_GED` | URL exacta |
| 3 | `ENCOAP` | `INEGI_ENCOAP_2023` | URL exacta |
| 4 | `CNGMD` | `RNM_CNGMD_2023_catalogo977` | URL exacta |
| 5 | `WORLD_BANK_ENTERPRISE_SURVEY_MEXICO_2023` | `WorldBank_MEX_EnterpriseSurvey_2023_catalogo6453` | URL exacta |
| 6 | `LARGE_SCALE_FINANCIAL_EDUCATION_PROGRAM_IMPACT_EVALUATION_2011_2` | `WorldBank_MEX_LargeScaleFinancialEducation_2011_catalogo2049` | URL exacta |
| 7 | `IMPACT_EVALUATION_OF_PARENTAL_EMPOWERMENT_PROGRAM` | `WorldBank_MEX_ParentalEmpowerment_2010_catalogo1039` | URL exacta |
| 8 | `MICROCREDIT_IMPACTS_RANDOMIZED_MICROCREDIT_PROGRAM_PLACEMENT_EXP` | `openICPSR_Microcredit_MexicoPlacement_proj116334` | URL exacta |
| 9 | `DOES_CORRUPTION_INFORMATION_INSPIRE_THE_FIGHT_OR_QUASH_THE_HOPE` | `JPAL_CorruptionInformation_MexicoVoters_2009` | URL exacta |
| 10 | `ELECTORAL_PRECINCT_LEVEL_DATABASE_FOR_MEXICAN_MUNICIPAL_ELECTION` | `Zenodo_ElectoralPrecinctLevel_MexicoMunicipal` | cita propia (corrige DOI mal transcrito) |
| 11 | `ENCUESTA_ANUAL_DE_COMPETENCIAS_FINANCIERAS_DE_LA_POBLACION` | `Banxico_EncuestaCompetenciasFinancieras` | URL exacta |
| 12 | `OECD` | `OECD_TrustSurveyData` | URL exacta |
| 13 | `PRICE_AND_INFORMATION_TYPE_IN_LIFE_MICROINSURANCE_DEMAND` | `Cenfri_MicroinsuranceMexico` | URL exacta |
| 14 | `INTERACTING_AS_EQUALS_REDUCES_PARTISAN_POLARIZATION_IN_MEXICO` | `OSF_InteractingAsEquals_PartisanPolarizacion_Mexico` | URL exacta |
| 15 | `MASS_MOBILIZATION_PROTEST_DATA` | `MassMobilization_Dataverse_MMdata` | URL exacta (de 3 variantes `MASS_MOBILIZATION_*`, la única con URL en cola) |
| 16 | `ISSP` | `GESIS_ISSP` | cita propia (`GESIS_ISSP` se autocita contra el hallazgo N3 de `cola-adquisicion`) |
| 17 | `BIARE` | `RNM_ENBIARE_2021_ficha730` | **corrección directa de `ADENDA 1`**: BIARE es el módulo dentro de ENBIARE; no tiene entrada propia en `cola-adquisicion-2026-08-12.tsv` (verificado, vacío) y por eso el método de RECONCILIA-PUERTAS no la encontró — no es un desacuerdo con esa nota, es un caso que quedó fuera de su alcance (no era del lote SONDA-1 ni de sus 5 rechazos) |

**Verificado por comando** que las 16 primeras están efectivamente entre las 63 `NO_PROBADO` de esta sesión (no solo en el `universo-puertas-2026-08-12.tsv` de RECONCILIA-PUERTAS): `awk -F'\t' '$6=="NO_PROBADO"' data/acceso-puertas-2026-08-13.tsv` contiene los 17 nombres exactos.

**Caso descartado a propósito, mismo criterio:** `IMSS` *no* entra a esta tabla. Es tentador emparejarlo con `IMSS_Datos_Abiertos_Asegurados` (acrónimo inequívoco, sin la ambigüedad de `SE`/`GPS`/`CSES`), pero no tiene entrada en `cola-adquisicion-2026-08-12.tsv` (verificado: vacío), no aparece en `SONDA-1`, y `RECONCILIA-PUERTAS` no lo cita. Aplicar el mismo rigor que sacó a `SE` del bucket A exige declararlo `CANDIDATA-A-SONDEO`, no forzarlo — ver §5, y nota de riesgo de sondeo redundante ahí mismo.

---

## 3 · Bucket `ADQUIRIDO` / `ADQUIRIDO-PARCIAL` — 13 filas

El encargo advirtió que su propio conteo de 16 candidatas por `grep -c` de menciones "cuenta menciones, no cobertura." Verificado entrada por entrada contra `data/manifiesto.yaml` (entradas `id:`/`usado_para:` reales, no substring incidental) — **3 de las 16 candidatas originales no tienen payload real y se sacan del bucket:**

| fila | menciones que declaraba el encargo | lo que hay realmente en `manifiesto.yaml` | disposición |
|---|---|---|---|
| `SICS` | 3 | Las 2 únicas menciones documentan un **sondeo fallido**: *"El sistema SICS (sics.funcionpublica.gob.mx) fue INACCESIBLE... 502 Bad Gateway"* — no es un payload, es una nota de fracaso | sale de bucket B → `CANDIDATA-A-SONDEO` |
| `FINANZAS` | 27 | Las 2 menciones reales dicen textualmente *"estaba SIN PAYLOAD"* y *"Candidata a credito-ahorro-finanzas-hogar"* | sale de bucket B → `CANDIDATA-A-SONDEO` |
| `SERIES_SPEI_CODI_BANXICO` | 85 | `grep -ic "SERIES_SPEI_CODI_BANXICO"` → **0**. La cifra de 85 no corresponde a esta cadena literal en absoluto (probablemente se buscó "SPEI"/"CODI" sueltos, que sí aparecen pero como parte de la palabra "codi**ficación**", falso positivo) | sale de bucket B → `CANDIDATA-A-SONDEO` |

Las 13 restantes sí tienen payload real, verificado por entrada `id:`/`archivo:`:

| fila | evidencia real en manifiesto | disposición |
|---|---|---|
| `ENVIPE`, `ENCIG`, `ENNVIH`, `ENIF`, `ENOE`, `ENIGH`, `ENCUP` | encuestas INEGI ya extensamente procesadas en el corpus (cientos de referencias `id:`/`usado_para:` reales cada una) | `ADQUIRIDO` |
| `CCPV` | microdato real `Censo2020_CAAS_eum_csv.zip` | `ADQUIRIDO` |
| `PUB` | `id: r7_3_pub_beneficiarios_bienestar_csv` | `ADQUIRIDO` |
| `LATINOBARÓMETRO` | `id:` con `archivo: latinobarometro2024_bd_stata.zip` (microdato real, no solo cuestionario) | `ADQUIRIDO` |
| `LAPOP` | `id: lapop_abmex2023_cuestionario_mexico` — **solo cuestionario, no microdato** (ejemplo que el propio encargo ya verificó) | `ADQUIRIDO-PARCIAL` |
| `ACLED` | `id: r7_4_r7_5_acled_hdx_demonstration_events` — payload real descargado (sha256 verificado) pero el propio `usado_para` declara *"agregado MES×AÑO... NO evento individual georreferenciado"* — falta la granularidad material (evento individual) que `fuentes_que_sirve` (N17, N27) necesita | `ADQUIRIDO-PARCIAL` |
| `COMPARATIVE_STUDY_OF_ELECTORAL_SYSTEMS_MEXICO_2018` (= `CSES_2018` del encargo original) | payload real `cses5_modulo5_2016_2021_csv` (P-Lote-1), pero el propio `usado_para` declara *"cobertura de Mexico NO verificada en esta sesion"* | `ADQUIRIDO-PARCIAL` |

`COMPARATIVE_STUDY_OF_ELECTORAL_SYSTEMS_MEXICO_2018` también tiene una fila real resuelta (`CSES_Modulo5_2016_2021`, mismo dominio `cses.org`, misma ventana de módulo), pero **no** entra al bucket A: no tiene entrada propia en `cola-adquisicion` (vacío, verificado) y RECONCILIA-PUERTAS §5.2 la rechazó explícitamente por "parecido de nombre solo, sin URL propia." Con payload real ya confirmado en manifiesto, el punto es discutible en la práctica (ambas rutas terminan en "nada"), pero se declara `ADQUIRIDO-PARCIAL` por tener evidencia de payload más directa que evidencia de fila-hermana.

---

## 4 · Bucket `NO-RESOLUBLE-POR-NOMBRE` — 6 filas

| fila | por qué |
|---|---|
| `FABLE` | señalada por el encargo como probable artefacto de captura de datos, no una fuente citable |
| `RELLABORALES` | agrupada con su par de prueba; nombre abreviado sin institución identificable, sin entrada en `cola-adquisicion` |
| `RELLABORALESPRUEBA` | el nombre mismo declara que es una fila de prueba ("PRUEBA") |
| `SIN_CANDIDATO_IDENTIFICADO` | el nombre es literalmente un valor centinela ("sin candidato identificado"), no una fuente |
| `CANAL_DE_ADQUISICION_REFERIDOS_FINTECH` | describe un tipo de proceso interno (canal de referidos), no un sustantivo propio localizable |
| `SE` | **corrección directa de `ADENDA 1`**: colisión con el pronombre "se" (mismo defecto que documentó ALIAS-P el mismo día). Verificado en `cola-adquisicion-2026-08-12.tsv`: tiene entrada propia (`url_conocida = VACIO`, `clasificacion_a4_previa = NEGATIVA(CURADURIA_SEMANTICA_MULTI2)`, nota *"sin ninguna variable de frecuencia conductual"*) — no es solo "nombre no encontrado," es una fuente ya evaluada semánticamente por otro acto y declarada insuficiente. Sin URL propia, sin hermana. |

Ninguna de estas 6 filas se borra (perímetro del encargo lo prohíbe explícitamente).

---

## 5 · Bucket `CANDIDATA-A-SONDEO` — 27 filas

Nombre resoluble a un portal, sin payload confirmado, sin hermana confirmada por evidencia dura. Verificado contra `data/cola-adquisicion-2026-08-12.tsv` dónde había `url_conocida` ya conocida (columna 4 de la tabla) — reduce trabajo de búsqueda en un eventual COMMIT 2:

```
$ ITAM_panel_household_finance                                        (sin URL propia; búsqueda previa ya falló, declarado en la fila misma)
$ ACLED                                                                → ver §3, es ADQUIRIDO-PARCIAL, no aquí (nota: excluida de esta lista)
```

*(la tabla completa de 27, con URL conocida cuando existe)*

| # | fila | URL conocida en cola (si existe) | nota |
|---|---|---|---|
| 1 | `ITAM_panel_household_finance` | — | búsqueda previa ya registrada sin éxito en la fila misma |
| 2 | `AHORRO FINANCIERO Y FINANCIAMI` | — | **nombre truncado/malformado** (espacio en vez de guion bajo, "FINANCIAMI" incompleto) — probable "Ahorro Financiero y Financiamiento" (reporte periódico CNBV/Banxico); defecto de datos, ver §6.3 |
| 3 | `BASE_DEL_OBSERVATORIO_DE_CONFLICTOS_POR_EL_AGUA` | — | nombre de observatorio específico |
| 4 | `BASE_DE_EVENTOS_DE_PROTESTA_EN_MEXICO` | — | nombre de base de datos académica específica |
| 5 | `BDIF` | — | acrónimo institucional, sin payload confirmado |
| 6 | `DENUNCIA_VINCULADA_CON_TENENCIA_DE_SEGURO` | — | nombre de registro/reporte regulatorio |
| 7 | `ENAFIN` | — | acrónimo INEGI, sin payload confirmado en manifiesto |
| 8 | `ENCUESTA_ANUAL_DE_COMPETENCIAS_FINANCIERAS_2019_2024` | VACIO en cola | posible duplicado de fila 11 del bucket A (`...DE_LA_POBLACION`), rechazado por RECONCILIA-PUERTAS §5.2 por falta de URL propia — declarar, no fusionar |
| 9 | `ESTUDIOS_DE_RECHAZOS_Y_CORPUS_PRAGMATICO_DE_FELIX_BRASDEFER` | `pragmatics.indiana.edu/textbook/corpus/Encdeserv.html` | URL ya conocida, no sondeada aún |
| 10 | `EXPERIMENTO_DE_INFORMACION_ELECTORAL_2009` | sin entrada en cola | posible duplicado de fila 9 del bucket A (`DOES_CORRUPTION...`/JPAL) — RECONCILIA-PUERTAS lo dejó fuera por no tener entrada propia en cola; JPAL sí describe "2009 municipal elections... voters behavior," compatible con ambos títulos — declarar la ambigüedad, no fusionar |
| 11 | `FINANZAS` | — | ver §3: manifiesto declara "SIN PAYLOAD" explícitamente |
| 12 | `FINTECH_LENDING_TO_BORROWERS_WITH_NO_CREDIT_HISTORY` | — | título de paper específico |
| 13 | `GLOBAL_PREFERENCES_SURVEY` | sin entrada en cola | RECONCILIA-PUERTAS §5.2 rechazó fusión con `GPS_Global_Preferences_Survey` por falta de URL propia en cola pese al nombre inequívoco; un sondeo fresco en COMMIT 2 podría cerrar la ambigüedad con evidencia directa |
| 14 | `HOMESCAN_CONSUMER_PANEL_SERVICES` | — | panel propietario (producto Nielsen), nombre específico |
| 15 | `IMPACT_EVALUATION_OF_MOBILE_PEDAGOGICAL_TUTORS_2016` | `microdata.worldbank.org/catalog/6667` | URL ya conocida, no sondeada aún |
| 16 | `IMSS` | sin entrada en cola | ver §2: probablemente `IMSS_Datos_Abiertos_Asegurados`, pero sin evidencia de cola/cita/SONDA-1 — **si COMMIT 2 llega a esta fila, verificar primero `datos.imss.gob.mx` antes de un curl nuevo** para no duplicar el sondeo de hoy |
| 17 | `MASS_MOBILIZATION_DATA_PROJECT_EVENTOS_MEXICO` | sin URL propia | variante rechazada por RECONCILIA-PUERTAS §5.2; posible duplicado de fila 15 del bucket A |
| 18 | `MASS_MOBILIZATION_PROTEST_DATA_MEXICO` | sin URL propia | mismo caso que la anterior |
| 19 | `MEXICO_ENTERPRISE_SURVEYS_COMPONENTE_PANEL_CANDIDATO_2006_2010` | — | distinto de `WORLD_BANK_ENTERPRISE_SURVEY_MEXICO_2023` (bucket A); años distintos (2006-2010 vs 2023), sin evidencia de que sean el mismo catálogo |
| 20 | `MICROCREDIT_IMPACTS_COMPARTAMOS_RCT` | **VACIO propio** en cola (a diferencia de su fila hermana `..._RANDOMIZED_..._EXP`, que sí tiene URL) | `data/mapa-ext-academico-2026-08-06.tsv:4` (tabla más antigua, 06/ago) sí liga "COMPARTAMOS_RCT_2008_2012" al mismo proyecto openICPSR 116334 — evidencia de drift entre tablas (la entrada más nueva en `cola-adquisicion-2026-08-12` perdió esa URL al reingresar la fuente con identidad distinta). No se fusiona sin URL propia vigente — declarado, no forzado |
| 21 | `PANEL_DE_COMPRA_DE_HOGARES` | — | panel propietario, nombre específico |
| 22 | `REGISTRO_DE_TANDAS_Y_REPUTACION` | — | posible relación con "Tanda+" (ver fila 23), sin confirmar |
| 23 | `REGISTRO_OPERATIVO_DE_TANDAS_DIGITALES` | — | identificado en `mapa-fuentes-externas-consolidado-2026-08-06.tsv:28` como fuente "Tanda+" (privada, no el programa gubernamental "Tandas para el Bienestar"), condición de acceso ya declarada ahí: "requiere convenio," `NO-ACCESIBLE`. No se resuelve a `Tandas_para_el_Bienestar` (ya sondeada) sin evidencia — son entidades distintas por nombre |
| 24 | `REPORTE_SOBRE_USO_Y_CONFIANZA_DE_SERVICIOS_FINANCIEROS_DIGITALES` | — | título de reporte específico |
| 25 | `SERIES_SPEI_CODI_BANXICO` | — | ver §3: 0 menciones reales en manifiesto |
| 26 | `SICS` | — | ver §3: único hallazgo es un sondeo fallido (502) |
| 27 | `VOTAR_ENTRE_BALAS` | — | título de estudio/paper sobre violencia electoral |

---

## 6 · Hallazgos adyacentes — declarados, no resueltos en este commit

### 6.1 · La aritmética del encargo: por qué 64≠63 no era una fila doble

`ADENDA 1 §5` ya lo corrigió antes de que este commit tuviera que adivinarlo: el 64 salió de aplicar dos recetas distintas (emparejamiento por nombre para el bucket A informal + `grep -c` de menciones para el bucket B) en dos pasadas separadas, no de una fila real cayendo en dos categorías. Re-derivando bucket A desde una fuente de evidencia dura (RECONCILIA-PUERTAS + 1 corrección directa) y bucket B desde entradas reales de manifiesto (no menciones), la cuenta cierra por construcción:

```
17 (DUPLICADA-DE-FILA-RESUELTA) + 10 (ADQUIRIDO) + 3 (ADQUIRIDO-PARCIAL)
  + 6 (NO-RESOLUBLE-POR-NOMBRE) + 27 (CANDIDATA-A-SONDEO) = 63
```

Verificado por comando (`python3`, diccionario de 63 entradas cruzado 1:1 contra las 63 filas `NO_PROBADO` de la tabla — cero faltantes, cero sobrantes).

### 6.2 · Duplicados aparentes del lado de la demanda — mismo defecto que RECONCILIA-PUERTAS §5.2, más 2 casos nuevos

RECONCILIA-PUERTAS ya nombró 5 pares de filas que probablemente describen la misma fuente real bajo dos identidades de `fuente_canonica` distintas (sin fusionar, por falta de evidencia dura): `CSES_2018`/`CSES_Modulo5`, `GLOBAL_PREFERENCES_SURVEY`/`GPS`, `ENCUESTA_...2019_2024`/`...DE_LA_POBLACION`, y las 2 variantes `MASS_MOBILIZATION_*_MEXICO`. Este commit encuentra 2 casos adicionales del mismo patrón, con el mismo tratamiento (declarar, no fusionar):

- `MICROCREDIT_IMPACTS_COMPARTAMOS_RCT` / `MICROCREDIT_IMPACTS_RANDOMIZED_MICROCREDIT_PROGRAM_PLACEMENT_EXP` — probable mismo RCT (Angelucci-Karlan-Zinman, Compartamos Banco), evidencia de una tabla más antigua (`mapa-ext-academico-2026-08-06.tsv`) mezclada con evidencia ausente en la tabla vigente (`cola-adquisicion-2026-08-12.tsv`).
- `EXPERIMENTO_DE_INFORMACION_ELECTORAL_2009` / `DOES_CORRUPTION_INFORMATION_INSPIRE_THE_FIGHT_OR_QUASH_THE_HOPE` — la URL de JPAL confirmada para la segunda ("information dissemination campaign and voters' behavior, 2009 municipal elections Mexico") describe casi literalmente el título de la primera; sin entrada en cola para la primera, no se fusiona.

Es evidencia de un defecto real del lado de la demanda (alias sin curar en `relaciones.tsv`/`aliases-fuentes.tsv`), no de este bucket A/B — mismo alcance que RECONCILIA-PUERTAS ya declaró fuera de su acto.

### 6.3 · Defecto de datos: nombre truncado

`AHORRO FINANCIERO Y FINANCIAMI` (fila 55 de la tabla `NO_PROBADO`) tiene un espacio literal en vez de guion bajo y termina en una palabra incompleta ("FINANCIAMI"). Es una anomalía de captura de datos en `acceso-puertas-2026-08-13.tsv`, distinta de los 5 artefactos ya señalados por el encargo — no se corrige aquí (fuera del perímetro: solo se añade `estado_triaje`), se declara para que quien mantenga la tabla lo revise.

### 6.4 · `IMSS`: caso límite, judgment call declarado

Ver §2 y fila 16 de §5. Se aplicó el mismo criterio que sacó a `SE`/`GLOBAL_PREFERENCES_SURVEY`/`CSES` del bucket A (sin evidencia de cola/cita/SONDA-1, no se fuerza el emparejamiento) aunque el acrónimo "IMSS" no tiene la ambigüedad de esos otros casos. Se prioriza la consistencia del método sobre la intuición de un caso individual.

---

## 7 · PARO DECLARADO

El encargo fija el gate: *"Si CANDIDATA-A-SONDEO sale mayor a 20, para y reporta antes del commit 2."*

**CANDIDATA-A-SONDEO = 27.** Supera el umbral. **COMMIT 2 no se ejecuta en este acto.** Esta nota y el commit de la tabla/hallazgos cierran el trabajo de COMMIT 1; el sondeo de las 27 (o de las que gate el Hito D, si mesa decide acotar antes) queda para un acto sucesor, con orden derivado de `data/curacion-registro/necesidad-objeto-modelo.tsv` como especifica el encargo.

---

## 8 · Cierre de COMMIT 1

```
$ python3 tests/check.py --baseline   (ANTES de escribir estado_triaje, git stash)
20 FAIL · 107 WARN — LÍNEA BASE: VERDE

$ python3 tests/check.py --baseline   (DESPUÉS de escribir estado_triaje)
20 FAIL · 107 WARN — LÍNEA BASE: VERDE
```

Sin cambio, sin regresión. `data/acceso-puertas-2026-08-13.tsv` gana exactamente 1 columna (`estado_triaje`), vacía en las 51 filas ya resueltas, con uno de los 5 valores cerrados en las 63 `NO_PROBADO`. Ninguna otra columna, ninguna otra fila, ninguna otra tabla tocada. `data/manifiesto.yaml` y `data/universo-puertas-2026-08-12.tsv` no se editan.

**Contador que instituye este commit:** filas de `acceso-puertas` con `estado_triaje` declarado — hoy **63 de 63** (era 0 de 63 al abrir).

**Qué NO hace este commit:** no descarga ningún payload; no da de alta puertas nuevas; no edita `manifiesto.yaml`; no sella ningún ADR; no resuelve las 17 de `DUPLICADA-DE-FILA-RESUELTA` editando `universo-puertas` (la precedencia ya está sellada en ADR-76(e), esta tabla solo rotula); no borra ninguna fila, incluidas las 6 de `NO-RESOLUBLE-POR-NOMBRE`; no ejecuta COMMIT 2 (§7).
