# Nota de cierre · ACTO MAESTRA31-E6 · DICCIONARIOS-FD

27/ago/2026 · Ejecuta `forense/encargos/2026-08-27-MAESTRA31-E6-DICCIONARIOS-FD.md` (dirección, maestra-31, archivado verbatim por A.3 antes de ejecutar). Entorno **UBUNTU**, worktree propio `/home/pc0/mm-maestra31-e6-diccionarios-fd` sobre `origin/main`=`07b1452` (el clon base `/home/pc0/Modelado-Mexicano` estaba parado en `acto/cal-g3-puntual`, no en `main` — mismo patrón ya declarado por `ADR-209`/`ADR-210`/`ADR-212`); `data/raw` gitignorado, enlazado a `/home/pc0/mm-corpus/raw` (321 entradas de primer nivel, 720 recursivas) antes de continuar. `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE` = `sin_variable` (esperado); `curl` a INEGI = `200` (alcanzable, pero este acto no usa red — cero descarga, cero API). SHA de arranque = SHA de este cierre: `main` no se movió durante la ejecución.

## §1 · Paso 1 — perímetro real, no heredado (tres cifras, sin colapsar)

La cifra "30" de `ACTO MAESTRA31-E4` venía de un SÍNTOMA (`variable_id` > 60 caracteres vía `INSPECT_XLSX`), no de un censo por nombre. Recalculado aquí por comando, el síntoma da **31**, no 30 — E4 estaba subcontado en 1 (`forense/notas/2026-08-26-orden-superior-cierre.md` no re-derivó esta cifra por comando propio, la tomó de la exploración previa).

**Cifra 1 — payloads que casan el patrón de nombre**, restringido a `.xlsx` (variantes: `diccionario`/`glosario`/`descriptor` en cualquier posición, o `fd` como token delimitado por `_`/`.`/`-`/límite de nombre — cubre `*_fd.xlsx`, `fd_*.xlsx`, `FD_*.xlsx`, `Diccionario_*`, `Glosario_*`, `*_descriptor_bd.xlsx`): **33**. Verificado por `perimetro_xlsx()` en `tools/inventario_fd.py` (recalcula sobre el árbol real, no es una lista congelada a mano) y por comando independiente (`find -L data/raw ... | wc -l`).

**Cifra 2 — cruce nombre × síntoma, el converso que E4 no derivó**:
- 23 payloads casan AMBOS (nombre y síntoma).
- **10 casan el nombre pero NO el síntoma**: `Diccionario_DGPASF.xlsx`, `Glosario_Datos_Abiertos_DGASF.xlsx`, `Censo2020_CAAS_descriptor_bd.xlsx`, `Censo2020_CEU_descriptor_bd.xlsx`, `diccionario_cuestionario_ampliado_cpv2020.xlsx`, `enasic_2022_fd.xlsx`, `enfih_2019_fd.xlsx`, `enif_2024_fd.xlsx`, `enut2019_fd.xlsx`, `enut2024_fd.xlsx`. Se abrieron 6 de los 10 (Diccionario_DGPASF, Censo2020_CAAS, diccionario_cuestionario_ampliado_cpv2020, enasic_2022, enfih_2019, enut2024): los 6 SÍ tienen el mismo bloque de título; su primera fila mide ≤60 caracteres (a veces en blanco, con el título real en la fila 2) — el síntoma de E4 **subestimaba** el hueco en este eje, no lo sobreestimaba.
- **8 presentan el síntoma pero NO casan el nombre**: 6 archivos `Base_Ahorro_Financiero_y_Financiamiento_*.xlsx` de CNBV, `cuestionario_supervision_en_campo.xlsx` (WB6667) y `encup_2012_base_datos_xlsx.xlsx`. Se abrieron los 3 tipos (uno de cada familia): `Base_Ahorro_Financiero...` es una serie de tiempo con un rótulo de eje largo en la fila 1 (dato, no diccionario); `cuestionario_supervision_en_campo.xlsx` es un formulario XLSForm de Banco Mundial cuya fila 1 ya es el encabezado real (`type/name/label/...`), sin bloque de título; `encup_2012_base_datos_xlsx.xlsx` trae microdatos crudos en su hoja principal y preguntas sueltas sin columna de mnemónico en una hoja secundaria. Ninguno de los 3 tiene el patrón de este acto — el síntoma de E4 **sobreestimaba** en este otro eje. Los 8 quedan fuera del perímetro de este acto.

**Cifra 3 — fichas descriptivas/diccionarios fuera de `.xlsx`**: **46** payloads con el mismo patrón de nombre en otros formatos — **34 PDF, 6 XLS (binario legado), 4 HTML, 2 ZIP** que envuelven un PDF o un XLSX de ficha descriptiva (`enif_2021_fd_pdf.zip`, `ensafi2023/ensafi_2023_fd_xlsx.zip` — mismo mecanismo `_xlsx.zip` que `ADR-198`/`ENSAFI-DESCRIPTOR` ya había medido para el cuestionario, aquí aparece también en la ficha descriptiva). **Cero en `.txt`/`.doc`/`.docx`** (0 archivos con esas extensiones en todo el corpus). Verificado por `payload_id` sobre 3 muestras (una por formato no-zip: `FD_ENCUCI2020.pdf`, `elcos2012/elcos_fd.xls`, `enut2009_diccionario_variables.html`) que los tres contribuyen **0 filas** a `data/inventario-reactivos-v1_0.tsv` — confirmado también por la distribución completa de `metodo` en las 178,246 filas: solo aparecen `INSPECT_CSV`/`INSPECT_XLSX`/`INSPECT_XML`/`INSPECT_ZIP`, nunca `INSPECT_PDF`/`INSPECT_XLS`/`INSPECT_HTML` — esos tres formatos están en `FORMATOS_SIN_CAMPOS` de `tools/inventario_reactivos.py` y `inspect_one()` nunca intenta leer columnas ahí, sin importar el contenido. **El bloque de título SÍ es convención de INEGI, no del formato `xlsx`** (la nota de cierre de `ACTO MAESTRA31-E4 · ORDEN-SUPERIOR` §F1 ya lo había confirmado en al menos un PDF, `eder2017_fd.pdf`) — el hueco real es más ancho que este acto, y queda **medido, no resuelto**: extraer de PDF/XLS/HTML exige una librería distinta por formato, lo que deja de ser "un extractor, para un patrón" (regla de tope núm. 1). Candidato natural a acto sucesor.

### Reconciliación con `FP-171` enmienda F1

`FP-171` (26/ago, enmendada 27/ago por `ACTO MAESTRA31-E4` mismo día que este acto arrancó) ya trae una cifra relacionada pero distinta: **69** payloads (31 `.xlsx` + 6 `.xls` + 32 `.pdf`), derivada con un patrón que — verificado aquí reproduciendo esa cifra por separado — es **solo el token `fd` delimitado**, sin las variantes `diccionario`/`glosario`/`descriptor` que no contienen la subcadena `fd` (p. ej. `Diccionario_DGEE.xlsx`, que no tiene ningún "fd" en su nombre, y los 4 `enut*_diccionario_variables.html`). El patrón fd-token-solo reproducido aquí da 68 (34 pdf + 6 xls + 26 xlsx + 2 zip), no exactamente 69/31/32 — la diferencia residual no se investigó dígito a dígito (es historia de otro acto, `FP-171` no está en el perímetro de escritura de éste); lo que sí se verificó es que **ninguna de las dos cifras (69 de F1, 79 de este acto) contradice a la otra**: son el mismo fenómeno medido con un patrón de nombre cada vez más ancho — 34 (patrón original *_fd.xlsx, citado por F1) → 68/69 (fd-token con prefijo, F1) → **79** (fd-token + diccionario/glosario/descriptor, este acto, el más completo derivado hasta hoy). El perímetro de EXTRACCIÓN de este acto (33 `.xlsx`) es el que importa para el CONTADOR abajo; los otros 46 quedan medidos en la Cifra 3.

## §2 · COMMIT-1 — especificación congelada

Íntegra en el docstring de `tools/inventario_fd.py` (commit `1bd790e`, antes de correr el extractor sobre el perímetro). Resumen:

- **Regla de detección**: la tabla real de variables empieza en la primera fila que declara, en dos celdas distintas de la MISMA fila, un rótulo id (`Mnemónico`/`Nemónico`/`Nombre`/`Nombre de la columna`/`Clave`) y un rótulo texto (`Pregunta`/`Pregunta y categoría`/`Descripción`/`Observaciones`) — comparación EXACTA de rótulos normalizados (minúsculas, sin acento, espacio colapsado), no búsqueda de contenido. Re-evaluada por fila (varias instituciones repiten encabezado a media hoja). Observada en **14 archivos de 10+ instituciones/familias distintas** ANTES de escribirse: CNBV (2 plantillas: `Diccionario_DGEE`/meta-clave-valor, `Diccionario_DGPASF`/tabular plano), ENASEM 2018, MOCIBA 2015, ENDUTIH 2024, ENIF 2015/2024, ENVIPE 2013, ENASIC 2022, ENFIH 2019, Censo de Población y Vivienda 2020 (2 plantillas: `descriptor_bd`, `diccionario_cuestionario_ampliado`), ENADID 2023, ENUT 2024.
- **B-bis, declarado antes de correr**: la regla **NO resultó uniforme** en forma entre instituciones — el bloque de título va de 6 a 21 filas, con tres formatos de leyenda distintos (`(1)`/`[1]`/pares clave-valor numerados "1. Título"), con y sin hoja "Índice"/"ÍNDICE" separada, con y sin fila de sección repetida a media hoja (`SECCIÓN 1. CARACTERÍSTICAS...`). Lo uniforme, en las 14 muestras, es el punto de enganche (rótulo de columna dual), no la forma del bloque que lo antecede. Este es el resultado que B-bis pedía declarar ANTES de ver el dato de cobertura — y es el que se reporta, sin ajustarlo después de correr.
- **Esquema**: hermano de `data/inventario-reactivos-v1_0.tsv` (`payload_id · sha256_12 · instrumento · ola · archivo_miembro · variable_id · texto_reactivo · metodo · universo_declarado`), `metodo="INSPECT_FD_XLSX"` (deliberadamente distinto de `INSPECT_XLSX`, mecanismo distinto).
- **NO-EXTRAIDO**: hojas donde nunca se detecta un encabezado con ambos rótulos van a `SIN_ENCABEZADO_RECONOCIDO`; hojas con encabezado pero sin filas de datos subsecuentes van a `ENCABEZADO_SIN_FILAS_DE_DATOS`. Lista completa abajo (§3).
- **Denominador de cobertura**: 33 (el perímetro xlsx). Cubierto = payload con ≥1 fila en la tabla nueva (mismo criterio que `payloads_cubiertos` de `tools/inventario_reactivos.py`).
- **Falsador (regla de tope núm. 5)**, declarado antes de correr: si cobertura < 50% del perímetro, se conserva lo producido, se anota como vía abandonada en `hallazgos.md`, y no se itera la regla.
- **Frase de sello verbatim**: «El primer resultado que produzca este procedimiento es el que se reporta.»

## §3 · COMMIT-2 — la tabla, resultado de la única corrida

`data/inventario-fd-v1_0.tsv` (commit `24f718a`, sin editar `tools/inventario_fd.py`).

**Verificación por comando (obligatoria, no se afirma en prosa sin el conteo):**
```
awk -F'\t' 'NR>1 && length($7)>0' data/inventario-fd-v1_0.tsv | wc -l
→ 17094
```
17,094 filas totales en la tabla == 17,094 filas con `texto_reactivo` poblado (100%, por construcción: el extractor solo emite cuando ambas celdas — id y texto — son no vacías). `awk -F'\t' 'NR>1 && length($7)==0' ... | wc -l` → `0`. Columnas por fila: `9` en el 100% de las 17,094 (`awk -F'\t' 'NR>1{print NF}' | sort -u` → `9`, sin excepción). `metodo` distinto: solo `INSPECT_FD_XLSX`.

**Cobertura**: **32 de 33 payloads (96.97%)**. Muy por encima del falsador (50%) — **NO se dispara**. No se itera la regla para subir el número.

**El único payload sin filas**: `mociba2020/mociba2020_fd.xlsx`. Su hoja de datos (`TR_ENDUTIH20_MOCIBA`) declara el rótulo id como `"Nombre \n(mnemónico)"` — con un salto de línea EMBEBIDO dentro de la propia celda, que normaliza a `"nombre (mnemonico)"`, una forma compuesta que no calza ninguna de las cinco etiquetas `ID_LABELS` exactas derivadas de las 14 muestras de COMMIT-1. Es exactamente el tipo de variación que B-bis anticipó (la regla no resultó uniforme en forma) — se documenta aquí, **no se parchea**: es el primer resultado, y es el que se reporta.

**15 hojas de 6 payloads adicionales sin filas** (los otros 31 payloads sí cubren ≥1 hoja): hojas `Índice`/`ÍNDICE`/`Indice` (listan tablas, no son tablas de variables), `Modelo de datos`, `Diagrama Entidad - Relación_img`, `DIAGRAMA`, `Cifras de control`, `tc_cve_catalogo_entidad`, `Catálogo de entidades`/`Catálogo de municipios`/`Catalogo_P10_12`/`Catalogo`, `NOTAS ACLARATORIAS`, y 4 hojas (`TViviend`/`Thogar`/`TSDem`/`TMod_Vic` de `envipe2013`, `TVIVIENDA`/`TSDEM` de `fd_enif2012`) con encabezado reconocido pero sin filas de datos subsecuentes (`ENCABEZADO_SIN_FILAS_DE_DATOS` — hojas de estructura declarada sin contenido, o cuyo contenido real vive en una hoja hermana). Ninguna de estas 15 es una tabla de variables perdida: son metadatos/catálogos/diagramas del propio archivo, correctamente excluidos por la regla. Lista completa (payload → hoja → razón) en la salida JSON de la corrida, reproducible con `python3 tools/inventario_fd.py`.

## §4 · Conteos A.13 (todo veredicto negativo declara cuántos archivos examinó)

- Universo de `data/raw` examinado para el censo de nombre: **720** payloads (recursivo, excluyendo el bucle de symlink `data/raw/raw`), mismo universo que usa `tools/inventario_reactivos.py`.
- Perímetro de este acto (nombre × `.xlsx`): **33**, los 33 se abrieron y procesaron (0 errores, `payloads_error: []`).
- Muestra abierta manualmente para derivar la regla de COMMIT-1, antes de escribirla: **14** archivos.
- Muestra abierta para resolver la cifra 2 del §1 (converso nombre↔síntoma): **9** archivos adicionales (6 del lado "nombre sin síntoma", 3 del lado "síntoma sin nombre").
- Muestra abierta para confirmar que PDF/XLS/HTML contribuyen 0 filas a la tabla hermana: **3** archivos (uno por formato), más la distribución completa de `metodo` sobre las 178,246 filas existentes (universo completo de esa tabla, no una muestra).
- Búsqueda de `.txt`/`.doc`/`.docx` con patrón de diccionario: **0 encontrados**, sobre el mismo universo de 720.

## §5 · Qué NO hizo este acto (verificado, no solo declarado)

`git diff --stat 07b1452 -- tools/inventario_reactivos.py tools/curador_registro/ data/inventario-reactivos-v1_0.tsv data/inventario-reactivos-v1_0.tsv.meta` → vacío (ninguno de los cuatro se tocó). No se clasificó ni normalizó el `texto_reactivo` extraído. No se llamó a ningún modelo ni servicio de red (0 llamadas API, 0 descargas — el único uso de red de todo el acto fue el `curl` de una línea del ARRANQUE). No se adjudicó casilla, letra ni tier.

## §6 · CONTADOR

**17,094 variables con texto recuperado**, sobre el perímetro de 33 payloads derivado en el paso 1 — la primera capa semántica del corpus con `texto_reactivo` poblado, donde antes había 0 en las 178,246 filas de la tabla hermana. Cobertura de payloads: 32 de 33 (96.97%). El falsador de la regla de tope (cobertura < 50%) **no se disparó**; la vía no se abandona.

Esta tabla, por sí sola, no resuelve los 13 parámetros `ASIGNADO_PROBABILIDAD` sin ruta de `forense/perimetro-alcanzable-v1_0.md` (ADR-212) — eso exige emparejar el texto contra el vocabulario del motor, y ese emparejamiento es explícitamente `E5`, fuera de este acto. Lo que este acto entrega es la materia prima que antes no existía en ninguna tabla del repo: 17,094 pares (`variable_id`, `texto_reactivo`) de instituciones (ENASEM, MOCIBA, ENDUTIH, ENIF, ENVIPE, ENASIC, ENFIH, ENADID, CNBV, Censo de Población y Vivienda) que nunca antes tuvieron una fila con texto en `data/inventario-reactivos-v1_0.tsv`.
