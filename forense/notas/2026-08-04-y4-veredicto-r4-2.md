# Veredicto `R4.2` · Hombre sin permiso laboral → pospone el chequeo · Encargo Y, commit 2

**Veredicto: `D`.** Determinado por el chequeo barato pre-registrado en `forense/notas/2026-08-04-y3-operacionalizacion-r4-2-ensanut.md`, ejecutado antes de abrir ningún microdato de fila-por-persona: la fila `D` de la ficha se satisface letra por letra ("si la encuesta no cruza permiso laboral con conducta preventiva").

**No hay pregunta de "sin permiso laboral" en ENSANUT 2024.** Verificado por `grep -i "permiso"` sobre los dos cuestionarios en etiquetas (Hogar y Adultos) — únicas coincidencias en el módulo de tamizaje §10.7, sin relación con trabajo. La pieza más cercana, `H0405A/B/C` ("¿Por qué motivo no buscó atención?"), trae la categoría genérica **"06 No tuvo tiempo"**, que no distingue motivo laboral de otros (cuidado de hijos, etc.).

**Tampoco hay pregunta dedicada de posposición de chequeo.** `H0402=30` ("Chequeo o consulta médica") es una categoría dentro de 40 motivos de última necesidad de salud en 3 meses — capta solo la última necesidad reportada, no un evento repetible de posposición.

**Reconstrucción intentada y rechazada:** encadenar `H0402=30 ∧ H0403=Sí ∧ H0404=No ∧ H0405∋06` produce una variable que mide "no tuvo tiempo" genérico, no "sin permiso laboral" — el defecto exacto de descriptor-vs-cuestionario que este encargo pide evitar (mismo tipo que mató a `BP1_20`). No se fabrica el cruce que la fuente no da.

**No hay variable de formalidad/afiliación laboral** en los catálogos de `integrantes`/`hogar` revisados (`grep -i "afiliad|derechohab|imss|issste"` sin resultados relevantes) — un segundo motivo, independiente del primero, por el que el Umbral (que exige controlar tipo de empleo) tampoco sería evaluable aunque existiera la pregunta de permiso.

**Verificado, no hay solape con ninguna otra fila:** `A`/`B`/`C` exigen todas que el cruce permiso-laboral × conducta exista en alguna forma medible; ninguna lo hace, así que ninguna se satisface junto con `D`.

**Nota de infraestructura, no cambia el veredicto:** el zip `adultos_ensanut2024_w.csv.csv.zip` (raíz `descargas_mx`) no contiene microdato de fila-por-persona, solo su propio catálogo duplicado. El microdato relevante vive en `integrantes_ensanut2024_w_ICB.csv.csv.zip`. Irrelevante para el veredicto: la variable que el Umbral pide no existe en ningún archivo del instrumento.

Fuentes inspeccionadas: `1 VFINAL Cuestionario Hogar ENSANUT 2024_ETIQUETAS.Cuestionarios.pdf`, `4 VFINAL Cuestionario adultos ENSANUT 2024_ETIQUETAS.Cuestionarios.pdf`, catálogos de `integrantes_ensanut2024_w_ICB` y `adultos_ensanut2024_w` (raíz `descargas_mx`, registrados en `data/manifiesto.yaml`).
