# ENCARGO · ACTO GEN2-CELDA-D-CAREO-1 · TRES DISEÑOS CIEGOS, UN CAREO, UNA CELDA REGISTRADA CON SU RESERVA INTACTA

**CABECERA** · redactado contra `9dffd64` (merge de #823; re-deriva al abrir) · **ENTORNO: NUBE** — cero microdato; **prohibido derivar el cruce `localidad × edad` de ENIF 2024** en este acto y en cualquier otro antes del COMMIT-3 del piloto: es la reserva de evaluación y derivarla la destruye · COMPUERTA: ninguna (FP-376 gatea al piloto, no a este acto) · MODELO SUGERIDO: Opus · FP/ADR/NC: candidatos FP-378 · NC-0275+ · ADR-533 (máximos derivados hoy: FP-377, NC-0274, ADR-532) — **deriva al cierre, no heredes** · vehículo: `/acto`.

**FIRMA DE MESA, verbatim (mesa la pega en el lanzamiento; A.12 — sin ella el acto no arranca; texto propuesto por dirección tras el "aceptado" del 17/sep, mesa lo corrige si no es lo que firmó):**

> FIRMA DE MESA, mesa, 17 de septiembre de 2026 — OBJETO (primer piloto celda-D): se acepta que hoy no existe celda con evaluación ciega en el universo examinado (Opus #823, Astra). Se autoriza el piloto de factibilidad con **reserva por interacción**: `ahorra_solo_informal` × (`localidad` × `edad`), 8 celdas de ENIF 2024 que **no se derivan** hasta que las emisiones estén selladas; sale `formalidad`; candidatos b(2021), marginales+independencia, L en dos dietas, matriz INEJECUTABLE declarada con sus faltantes, emisor fuera de competencia. El diseño vigente es el v1.1 de dirección.

**ADJUNTOS (A.3: cada uno viaja con su sha256; si uno no llega, esa pieza PARA y no tumba el lote):**
- `ASTRA-CELDA-D-DISENO-Y-ADVERSARIAL-b881ee6-v1_0.md` · `2b813f430fd3ad4f…` — retorno externo, insumo tipo 3. **Lo carga mesa.**
- `D-THETA-DOCUMENTO-v1_1-post-adversarial.md` · `8a6472a72631dfdc…` — insumo de dirección que #822 P5 dejó NO-CORRIDO (el adjunto no viajó). **Lo carga mesa.**
- `ENCARGO-EXTERNO-ASTRA-celda-d-piloto-2026-09-17.md` · `43f63e1fdf51f270…` — el brief que produjo el retorno de Astra.
- `CELDA-D-PILOTO-diseno-direccion-v1_0-CIEGO-2026-09-17.md` · `c7d55e61bdf929c0…` — diseño ciego de dirección; historia.
- `CELDA-D-PILOTO-diseno-direccion-v1_1-post-careo-2026-09-17.md` · `68a794936ef2ca47…` — **el vigente**.

**VERIFICACIÓN DE EXISTENCIA (A.8, dirección, contra `9dffd64`):**
- (1) Estructura: Dominio 5 (`data/INFRAESTRUCTURA-v1_0.md:157`, registrar una celda-D), Dominio 7 (ADR/cascada), Dominio 8 (notas, `hallazgos.md` append-only con trampa `merge=union`), Dominio 9 (`firmas-pendientes.tsv`), `no-corrido.tsv`, `decisiones.tsv`. Cubren; sin hueco. **Nota:** `INFRAESTRUCTURA-v1_0.md:159` dice que `celdas-d/` tiene "hoy 2 archivos"; hay 3 (Opus lo midió, fuera de su perímetro) — este acto lo enmienda con fecha.
- (2) Contenido: `ls forense/notas/insumos-externos/` → **no existe**; `ls forense/notas/insumos-direccion/` → **no existe** (#822 P5 NO-CORRIDO). `grep -c "copiada verbatim|identidad num" forense/no-corrido.tsv` → **0**; patrón `circular` → solo NC-0263, otro objeto (1 archivo examinado). **NO-ENCONTRADO**: no hay NC sobre "el emisor copió al árbitro". `ls data/curacion-registro/celdas-d/` → 3 archivos, ninguno DIN. `grep -rln "localidad × edad|reserva por interacci"` sobre 2 262 archivos → 2 coincidencias del 12/ago sobre clustering de R5.1, no sobre reserva de evaluación. Veredicto: todo lo que este acto produce es **NO-ENCONTRADO**; nada duplica.
- (3) Cobertura retroactiva: el registro de celdas-D nació el 11/ago (ADR-68) y el crosswalk el 16/sep (#820); ningún trabajo anterior sobre esta celda pudo pasar por ellas. FP-376 (`localidad` MAPEO-N-A-1) sigue **ABIERTA**: el piloto puede correr sin ella; el marcador no puede consumirlo sin ella.

### PIEZAS

**P0 · Archivo verbatim (A.3).** Los cinco adjuntos a `forense/notas/insumos-externos/celda-d-piloto/` (Astra, brief) y `forense/notas/insumos-direccion/` (D-θ v1.1, diseño v1.0, diseño v1.1), cada uno con cabecera de procedencia (externo tipo 3 / dirección tipo 3, con sha256 verificado por comando y pegado). Sin editar una coma. Si un sha no coincide, esa pieza PARA y se reporta cuál.

**P1 · Verificación del retorno de Astra (A.6).** Astra citó `archivo:línea @ b881ee6`: cada cita se resuelve con `git show b881ee6:<ruta> | sed -n <línea>p` y se marca VERIFICADA / NO-RESUELVE / DICE-OTRA-COSA (dos pasadas, precedente ADR-530/532). Sus tres fuentes externas (Cawley-Talbot 2010 JMLR; Benjamini-Hochberg 1995 DOI 10.1111/j.2517-6161.1995.tb02031.x; Hyndman-Athanasopoulos FPP3 §5.8) se intentan abrir desde nube: VERIFICADA con la afirmación que sostienen comprobada, o SIN-FETCH con receta manual de un minuto. **Nada SIN-FETCH sostiene una conclusión del careo.** Los dos errores del brief de dirección que Astra atrapó (22 momentos, no 23; `consumidor`, no `reglas_impacto`) van a `hallazgos.md` como línea A.13 de dirección.

**P2 · El careo, formal.** Una nota `forense/notas/2026-09-17-GEN2-CELDA-D-CAREO-1.md` con: (a) tabla de los tres diseños × siete criterios, con la celda elegida o "ninguna" y la cita de cada uno; (b) los **tres hechos que decidieron el careo**, cada uno con su comando re-ejecutado en este acto: `P3_13` ausente en ENIF 2021 (`data/inventario-reactivos-v1_2.tsv`: 0 filas 2021 / 4 filas 2024); `P5_6_6/7` ausentes en 2021; emisor = árbitro en 5/7 entradas `_ejes_` (`milpa/tramite.yaml:712`, `:1026`; `RES-0041 = 0.672` vs `tramite-ola5-propuesta-v0.yaml:2012` = `0.672014`); (c) la reconciliación (diseño v1.1 §0) con **atribución por hallazgo** — Opus 1-3, Astra 4-5, dirección la reserva por interacción; (d) los modos de falla de Astra (§3.1-3.16) y de Opus (F1-F10) fundidos en **una** tabla con procedencia por fila, cada uno cruzado contra `forense/hallazgos.md`: `YA-OCURRIÓ-AQUÍ (cita)` / `CONCEBIBLE` / `NO-APLICA` — y para cada uno, si el diseño v1.1 lo atrapa, con qué cláusula; (e) el módulo de auditoría de rigor extremo completo (el careo afirma sobre el modelo), incluida la pregunta v2.1.

**P3 · Filas, registro y enmiendas.**
- **NC nueva (aparato):** "el emisor copió al árbitro verbatim en 5 de 7 entradas `_ejes_`; el marcador por segmento no puede comparar M contra R en ellas" — pieza citada, impacto: el sucesor de NC-0239 y el diseño del marcador por segmento; sucesor: SIN-ASIGNAR hasta que dirección redacte (no lo asignes tú).
- **NC nueva (registro):** "15 filas `conducta_p_medido` con `escala_legacy = NO-DECLARADO-EN-EL-REGISTRO`, entre ellas RES-0057/0058 (consumidor del piloto)" — con el comando y el conteo; sucesor: acto de registro, no de medición.
- **FP nueva, nace FIRMADA** con la firma verbatim de arriba y el ADR de este acto; `decisiones.tsv` recibe la fila (objeto `primer-piloto-celda-D`).
- **Enmienda fechada in situ a NC-0239:** el bloqueador se cuenta celda por celda (ADR-531) y su sucesor es `GEN2-CELDA-D-PILOTO-1`.
- **Registrar la celda-D:** `data/curacion-registro/celdas-d/DIN.ahorro_solo_informal.enif2024.localidad_x_edad.yaml` con el YAML del diseño v1.1 §9 **verbatim**, `momentos_holdout_refs` con la marca `RESERVADA … NO DERIVADO`, `requiere_decision_mesa: true` (FP-376; siete códigos; admisión de C2). `tests/test_celdas_d.py` en verde; si el YAML no valida contra v0.5, se corrige la sintaxis, nunca el contenido, y se reporta.
- **Enmienda fechada** a `data/INFRAESTRUCTURA-v1_0.md:159` (3 archivos, no 2; hoy 4 con este).
- **`hallazgos.md`:** línea A.13 de dirección (P1); línea sobre emisor = árbitro; línea PARA-v2.14: "la reserva de evaluación se declara antes de derivarla; un R derivado antes de las emisiones degrada el piloto a factibilidad".

**P4 · Hoja del sucesor.** Al pie de la nota: el esqueleto de `GEN2-CELDA-D-PILOTO-1` — **ENTORNO: CAJA** (abre ENIF 2021 y 2024), Opus, **tres commits mínimo y en este orden**: COMMIT-1 spec congelada sin abrir microdato (códigos leídos del FD de cada ola; siete códigos comunes; `tloc`/`edad` por archivo; universo; `FAC_PER`; seed; tolerancias; la frase de sello); COMMIT-2 emisiones de C1, C2 y C3 selladas como RESULT GEN2 con cadena completa (E.2); COMMIT-3 derivación de R en el cruce y adjudicación por celda con las condiciones `INDECIDIBLE` verbatim; C4 asentado como INEJECUTABLE; C5 diagnóstico. Gated a: este merge, la firma de los tres puntos de v1.1 §7 (FP-376 solo para que el marcador consuma). Dirección lo redacta; este acto solo deja el esqueleto.

**PERÍMETRO Y CONCURRENCIA:** `forense/notas/insumos-externos/celda-d-piloto/` (nuevo) · `forense/notas/insumos-direccion/` (nuevo) · `forense/notas/2026-09-17-GEN2-CELDA-D-CAREO-1.md` (nuevo) · `data/curacion-registro/celdas-d/DIN.ahorro_solo_informal.enif2024.localidad_x_edad.yaml` (nuevo) · `forense/no-corrido.tsv` · `forense/firmas-pendientes.tsv` · `data/corrida0/decisiones.tsv` · `forense/hallazgos.md` · `data/INFRAESTRUCTURA-v1_0.md` (solo :159) · cascada de `/acto` (`gobernanza`, cabeceras, `registro-rotulos`, `tests/check.py` solo T25). **No toca** `milpa/` (ni `tramite.yaml`, ni `tramite-ola5-propuesta-v0.yaml`), specs, `resultados.tsv`/`usos.tsv`, el crosswalk, el marcador, ni las otras tres celdas-D. En paralelo: `/deriva` y el servicio de adquisición — sin archivo común; **ningún otro acto abre ENIF 2024 hasta el COMMIT-3 del piloto** (mesa lo vigila). «Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.»

**CONTADOR:** cero mediciones, dicho sin disfraz. Mueve `no_corrido_abiertas` (+2, se reporta); una FP nace FIRMADA; una celda-D nueva registrada (3 → 4). **LO QUE NO HACE:** no abre microdato · **no deriva el cruce** · no elicita L · no escribe `spec.yaml` · no firma FP-376 · no adjudica · no toca el emisor ni el marcador · no redacta el piloto (deja su esqueleto). **SUCESOR:** `GEN2-CELDA-D-PILOTO-1` (CAJA, Opus, tres commits), redactado por dirección tras este merge. **CIERRE:** cascada + `## NO-CORRIDO / RESERVAS` (A.14; "Ninguno." es respuesta obligatoria) + `## CONSUMIDO`.

## NO-CORRIDO / RESERVAS

| qué | por qué | impacto | sucesor |
|---|---|---|---|
| **`P0`** — «`D-THETA-DOCUMENTO-v1_1-post-adversarial.md` · `8a6472a72631dfdc…` — insumo de dirección que #822 P5 dejó NO-CORRIDO (el adjunto no viajó). **Lo carga mesa.**» | `PARO-PREMISA`. **El adjunto tampoco viajó esta vez.** El lanzamiento trajo cuatro archivos, no cinco, y ninguno tiene ese sha256. Por A.3 («*si uno no llega, esa pieza PARA y no tumba el lote*») esa pieza para y las otras cuatro se archivan. Ninguna afirmación del careo se apoya en él: las dos referencias `[ADJ]` del diseño v1.0 §2.5 se leen como del diseño, no como del D-θ. | `forense/notas/insumos-direccion/` queda con **2 de 3** piezas de dirección. **Es la segunda vez seguida** que este mismo adjunto no llega (`NC-0271` es la fila hermana de #822): un patrón de dos no es azar — el canal por el que mesa lo manda no lo está entregando. | `NC-0277` · **CERRADA en este mismo acto.** Mesa cargó el adjunto al leer el PARO-reporte, en la misma sesión, y este acto lo archivó verbatim con su sha verificado (`8a6472a72631dfdc…`) en `forense/notas/insumos-direccion/`; llegó también `ADVERSARIAL-D-THETA-v1_0.md`. **Al tercer intento.** `NC-0271` sigue `ABIERTA` con su premisa resuelta: se cierra la premisa, no su segunda pieza, que es sustancia de `#822` |
| **`P1`** — «Sus tres fuentes externas … se intentan abrir desde nube: VERIFICADA con la afirmación que sostienen comprobada, o SIN-FETCH con receta manual de un minuto» | `NO-VERIFICABLE-AQUÍ`. **Ejecutado con reserva, no omitido.** Las tres quedan `SIN-FETCH`: 5 URLs probadas (`www.inegi.org.mx`, `doi.org`, `www.jmlr.org`, `otexts.com`, `rss.onlinelibrary.wiley.com`), `http_code 000` en las cinco, el proxy de egreso responde `403` al `CONNECT`. Receta manual de un minuto escrita en la nota §1.2. A.6: encontrado por buscador y no abierto byte a byte es evidencia de segunda mano. | **Ninguno.** El propio encargo lo blinda: «*nada SIN-FETCH sostiene una conclusión del careo*». Los 16 estados del contrato de la tabla fundida se derivan de `v0.3`/`v0.5`/`RONDA1`/`CAREO-ADV`, todos en el árbol y todos verificados por comando. Lo que queda sin verificar es la **atribución bibliográfica** de tres argumentos, no los argumentos. | `NC-0278` · `DIFERIDO-A:` cualquier acto con red (CAJA) que abra las tres y pegue la afirmación que cada una sostiene |
| **`P3`** — «**Enmienda fechada in situ a `NC-0239`:** el bloqueador se cuenta celda por celda (`ADR-531`) y su sucesor es `GEN2-CELDA-D-PILOTO-1`» | `SUSTITUIDO-POR:ADR-531` (`ACTO GEN2-M1-ALCANCE-1`, 16/sep/2026). **DISTINTO DE LO PEDIDO, y por buena razón: esa enmienda ya existía.** Verificado por comando antes de escribir — la columna `sucesor` de `NC-0239` ya traía las dos frases exactas y ese sucesor. **No se duplicó.** Absorbe: el conteo celda por celda y el sucesor `GEN2-CELDA-D-PILOTO-1`. **Queda huérfano: nada.** La verificación de existencia de este encargo (A.8(2)) no buscó esta fila, y por eso la dio por NO-ENCONTRADA. | Cero: `NC-0239` sigue `ABIERTA`, con el mismo texto y el mismo sucesor. Lo único nuevo que este acto le añade es el estado del contador que ella misma declara como medida del avance: **1 celda registrada, 0 adjudicadas** — registrar no es adjudicar. | `NC-0279` · `GEN2-CELDA-D-PILOTO-1`, sin cambio respecto de lo que `ADR-531` ya había escrito |
| **`CONTADOR`** — «Mueve `no_corrido_abiertas` (**+2**, se reporta)» | **DISTINTO DE LO PREVISTO: son +7, no +2.** Las dos que el encargo nombra (`NC-0275` aparato, `NC-0276` registro) más las **cinco filas A.14 propias** de este acto, que el pronóstico no contaba: `NC-0277` (adjunto que no llegó), `NC-0278` (fuentes `SIN-FETCH`), `NC-0279` (enmienda que ya existía), `NC-0280` (perímetro mal calculado) y `NC-0281` (línea base roja sin congelar). Se reporta el número real y no se ajusta el conteo al pronóstico. | `no_corrido_abiertas` sube **+7**. Ninguna de las cinco extra es deuda nueva del programa: tres son reservas de este acto, una es una deuda que se descubrió ya pagada, y la quinta es una decisión que sólo mesa puede tomar. | `SIN-ASIGNAR` — no requiere sucesor; es una corrección del contador del propio encargo |
| **Los tres puntos de `v1.1 §7`** que van a mesa antes de congelar: `FP-376` (`localidad` como `MAPEO-N-A-1`), la definición por **siete códigos comunes**, y la admisión de **`C2`** como segundo piso | `DECISIÓN-DE-MESA-PENDIENTE`. Este acto **no los firma** — el encargo lo dice en `LO QUE NO HACE` («no firma FP-376») y la firma de mesa que lo autoriza tampoco los cubre. `FP-376` sigue `ABIERTA`. | El piloto **puede correr** sin `FP-376`; lo que no puede es que **el marcador consuma** su resultado. Sin la definición por siete códigos comunes el piloto **pierde la persistencia**, que es su piso. Sin `C2`, «`L` vence a la persistencia» no distingue información de interacción de información de marginales. | `GEN2-CELDA-D-PILOTO-1`, que los lleva como compuerta declarada en su hoja (nota §7) |
| **`A.8`** de la cabecera — «`grep -rln "localidad × edad\|reserva por interacci"` sobre **2 262 archivos** → 2 coincidencias» | **EL VEREDICTO REPRODUCE; EL DENOMINADOR NO.** Re-derivado en este acto sobre `9dffd64`: **2 coincidencias**, las mismas dos, ambas del 12/ago sobre clustering de `R5.1` y ninguna sobre reserva de evaluación — pero el árbol tiene **5 256** archivos rastreados, no 2 262. La cabecera **no se reescribe**: se enmienda aquí, con fecha. | Ninguno sobre el hallazgo: la reserva por interacción sigue siendo `NO-ENCONTRADO` en el árbol, que es lo que el criterio (g) necesitaba. Lo que queda corregido es el A.13 del encargo — **un negativo declara cuántos archivos examinó el comando que lo produjo, y ese número tiene que ser el real**. | `SIN-ASIGNAR` — corrección de cabecera, no deuda; queda dicha para quien audite el encargo |
| **`PERÍMETRO`** — «cascada de `/acto` (`gobernanza`, cabeceras, `registro-rotulos`, **`tests/check.py` sólo T25**)» | `FUERA-DE-PERÍMETRO`, **y se para a decirlo en vez de tomar el atajo.** El acto tuvo que tocar además `_T_YAMEDIDO_ARCHIVOS_CONOCIDOS` (**T30**), porque el encargo archivado verbatim cita `R5.1` una vez —dentro de su propio bloque A.8, nombrando lo que un grep **encontró**— y `T30` exige la salida de `tools/ya_medido.py` para toda cita id/R-n. Las dos salidas eran editar un encargo verbatim (prohibido por A.3 y por el paso 5 de `/acto`) o usar la exención que el propio test documenta. Se usó la exención, con la razón escrita y la salida de `python3 tools/ya_medido.py R5.1` pegada en el comentario. **El perímetro estaba mal calculado:** predijo un test de cascada y el acto disparó cuatro (`T02`, `T22`, `T25`, `T30`), que es la cascada real de archivar cuatro adjuntos verbatim y abrir una fila de mesa. | Ningún contador del programa. `tests/check.py` crece 47 líneas —comentarios y cinco rutas de exención—, **cero lógica de test cambiada**: mismos umbrales, mismos patrones, mismas reglas. `T25` y `T30` vuelven a verde por exención documentada, no por relajación. | `NC-0280` · `SIN-ASIGNAR` — es una observación sobre cómo se calcula el perímetro de un acto que archiva texto de terceros; `GEN2-CELDA-D-PILOTO-1` hereda la lección |
| **`CIERRE` paso 6** — «`tests/check.py --baseline` en VERDE …, o PARO-reporta con la salida cruda» | **RESUELTO, y por la vía preferida.** El cierre salió primero **ROJO con 9 entradas nuevas** —todas de `T03`, todas la misma cita: los dos adjuntos que mesa no había cargado— y el acto **PARÓ y reportó en vez de congelar**, porque recongelar es autorización de mesa (los **siete** recongelados de `tests/baseline.json` llevan esa fórmula; precedente: `ACTO GEN2-E0 · ENCOLA`). **Mesa cargó los dos adjuntos en la misma sesión** —la vía (a)—, se archivaron verbatim con su sha verificado por comando y **8 de las 9 se cerraron solas**. La novena era otra cosa: el adversarial cita la **v1.0 que él mismo revisa**, derogada por su propia sucesora; se declara en `_T03_DEPENDENCIAS_PENDIENTES` con la razón escrita (mismo patrón que `NC-0219`) en vez de pedir historia derogada o fabricar procedencia. | **Cierra en `3 FAIL · 4360 WARN`, LÍNEA BASE VERDE, y `tests/baseline.json` SIN TOCAR** — el mejor de los dos desenlaces que se pusieron a mesa. **Cero `FAIL` nuevos**: los 3 son los heredados del corpus documental (`T06`×2, `T08`). El hallazgo reflexivo sobrevive al cierre en verde: **reportar una ausencia crea la referencia colgante que se reporta**, medido dos veces en el punto fijo (`4364` → `4373` → `4370`). | `NC-0281` · **CERRADA en este mismo acto**, sin recongelar nada |

## CONSUMIDO

Ejecutado por [`PR #827`](https://github.com/Josanoforo/Modelado-Mexicano/pull/827)
(`ACTO GEN2-CELDA-D-CAREO-1`, rama `claude/nice-cerf-jpywyo`, 17/sep/2026 nominal —
ejecución del entorno 2026-09-16 —, NUBE `cloud_default`, Opus, sin corpus montado).
`ADR-533`.

**Entregables.** `forense/notas/2026-09-17-GEN2-CELDA-D-CAREO-1.md` (P1, P2, P4 y el
módulo de auditoría de rigor extremo) · los cuatro adjuntos que llegaron, archivados
verbatim en `forense/notas/insumos-externos/celda-d-piloto/` y
`forense/notas/insumos-direccion/` (P0; el quinto PARA, `NC-0277`) ·
`data/curacion-registro/celdas-d/DIN.ahorro_solo_informal.enif2024.localidad_x_edad.yaml`
(P3, la primera celda-D del piloto: `celdas-d/` pasa de 3 a 4 archivos) · `FP-378`
nacida FIRMADA · fila en `data/corrida0/decisiones.tsv` · `NC-0275`…`NC-0281` ·
tres líneas en `forense/hallazgos.md` · enmiendas fechadas a
`data/INFRAESTRUCTURA-v1_0.md:159` y a `NC-0239`.

**Cascada.** `canon/gobernanza-v1_15.md` `ADR-533` · `canon/estado-programa-v1_13.md`
L0 · `canon/registro-rotulos.tsv` (`GEN2 · GEN2-CELDA-D-CAREO-1`) · `tests/check.py`
por `T25` y —fuera del perímetro declarado, y por eso con fila propia (`NC-0280`)— por
`T30`.

**Suite:** cierra en `3 FAIL · 4360 WARN`, **LÍNEA BASE VERDE**, y `tests/baseline.json`
**sin tocar**. Salió primero `ROJO` con 9 entradas nuevas de `T03` y el acto **PARÓ y reportó
en vez de congelar**; mesa cargó los dos adjuntos en la misma sesión —la vía (a) de
`NC-0281`— y ocho se cerraron solas. `NC-0277` y `NC-0281` quedan **CERRADAS**;
`NC-0271` sigue abierta con su premisa resuelta y el resto nombrado.

**Sucesor:** `GEN2-CELDA-D-PILOTO-1` (CAJA, Opus, tres commits en orden), redactado por
dirección tras este merge; su esqueleto está al pie de la nota.
