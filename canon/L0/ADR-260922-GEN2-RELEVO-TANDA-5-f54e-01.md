**ADR-260922-GEN2-RELEVO-TANDA-5-f54e-01**, `ACTO GEN2-RELEVO-TANDA-5`, 22/sep/2026, NUBE (`cloud_default`), Sonnet 5 (mandato de mesa), sin sub-agentes, MODO ABIERTO, COMPUERTA: ninguna de merge. Cierra las dos firmas que `GEN2-TRAMITE-FIRMAS-5` dejó fuera a propósito: F-L (18 pines de L a `CALC-L-DESDE-CAPTURAS-v1_0`) y F-R (el eje RESULTADO del replay también gobierna la vía (i), sobre las ocho corridas de la nota de TANDA-4 §3). **P1 EJECUTADO**: `tools/pines_mesa.py::valida_pin` unifica la guarda (a) de las tres vías bajo `veredictos_afirmativos_en_resultado()`; ya no hay excepción de "REPRODUCE estricto" para la vía (i). **P2 (F-L) NO EJECUTADO**: la premisa de que `CALC-L-DESDE-CAPTURAS-v1_0` acredita vía (i) falla mecánicamente — el registro deriva `resultado_replay = NO-VERIFICADO` por un defecto de comparación sensible al orden en `tools/corrida0.py::_evidencia_vigente` (compara `input_sha256_efectivos` como cadena; el mismo conjunto de pares `id=hash` en otro orden se lee como "cambió"), ajeno al perímetro de este acto. FP `F-L` queda FIRMADA con la nota del bloqueo; 18 lecturas siguen legacy. **P3 (F-R) sin pines nuevos**: los 8 usos `::R` de las corridas que F-R nombra ya son GEN2 desde TANDA-3, vía sucesores `-v2/-v3/-v4`; nada cita hoy el CALC base. FP `F-R` FIRMADA sin mover el contador. **P4**: dos FP nuevas (`F-260922-…-f54e-01/02`) FIRMADAS; `NC-0425`, `NC-…-7bf5-03` y `NC-…-958c-01` CERRADAS citando `PR #973` (fusionado). `dependencias_numericas_legacy_activas` se queda en 146.
