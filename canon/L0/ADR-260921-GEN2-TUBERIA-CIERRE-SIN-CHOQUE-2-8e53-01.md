**ADR-260921-GEN2-TUBERIA-CIERRE-SIN-CHOQUE-2-8e53-01**, `ACTO GEN2-TUBERIA-CIERRE-SIN-CHOQUE-2`, 21/sep/2026, NUBE (`cloud_default`, corpus NO montado, `archivos_examinados=0`, A.13), Opus 5, sin sub-agentes, MODO ABIERTO, COMPUERTA: ninguna, cero microdato. **Re-corre la prueba de no-pérdida de la `L0` de `#962` sobre el universo MÁXIMO que domina al que el encargo pedía** —`git rev-list --all`: 161 commits, **92** versiones distintas de la línea (las «49 de primer padre» no son alcanzables: el archivo tiene 44 commits de primer padre en toda su historia, y así se declara), **142 fragmentos distintos**, los 142 presentes en la vista vigente, **426/426 trozos del MEDIO**, cero ausencias— con la unidad de fragmento (`*( … )*`) derivada del texto, no heredada: **`#962` no perdió nada**. **Cierra las cuatro `NC` con evidencia:** tres sin objeto (`…-3d08-03`, `…-6e60-05`, `…-6e60-08`) y **`…-6e60-04` por arreglo — el defecto ya había ocurrido**: `EC.fp_max` devolvía el FP fantasma **260921** porque `#962` puso el guardia `(?![\d-])` en `adr_max` y no en `fp_max`, en el mismo archivo; corregido (una línea), `fp_max` = **409**, y `tools/tablero_programa.py` deja de publicar el espacio numérico cerrado como si fuera el universo (`adr_max_espacio_cerrado`, `fp_max_espacio_cerrado`, `ids_raiz_de_acto`). **`canon/registro-rotulos.tsv` deja de chocar** (41 de 62 PR en 7 días, 66 %, el tercero del árbol): cabecera devuelta a la línea 1 sin perder ni cambiar ninguna fila (436 → 436, mismo multiconjunto, y ningún lector dependía del orden), guarda nueva **`T51`** de unicidad del par `(espacio, valor)` con los 7 pares de hoy congelados como exención declarada —ninguno borrado—, mutación exacta de `#962` ejercida en `tests/test_tuberia_ids_union.py` (`G3-bis`, `G3-ter`) y, por eso, **ENTRA a `merge=union`**. **`estado-programa` deja de tocarse en cada cierre:** medido que un cierre aún escribía a mano la fila `gobernanza` de la tabla §0 (11 de 11 PR) y una anotación `*Anotación L0 (…)` nueva (6 de 11) — 96 acumuladas, el mismo canal de apéndice que infló la `L0`; las 96 y la fila quedan HISTÓRICAS con hash fijado bajo la guarda nueva **`T52`**, con marca en el archivo y el paso 3 de la cascada de `acto.md` diciéndolo. **P3, medido y no tocado:** vistas derivadas de `corrida0` (12–13 %, tocarlas es E.7, decisión de mesa), `tests/check.py` (19 %, es código) y el hallazgo que el mapa del encargo no nombra, `forense/analisis/ci-guardias/censo-tests.tsv` (**22.6 %**, séptima fuente de choque, vista derivada). `cuenta_gen2 = NO`. Vista completa (histórico + fragmentos): `python3 tools/l0_vista.py`.
