# Cierre — ACTO R21-FALSADOR-V2-RESPEC, 25/ago/2026

**Encargo:** re-especificación del falsador de `R2.1` a nivel empleado/segmento (opción A de mesa); el compass se aterriza como censo. Entorno **NUBE**. `SELLA-R21` verificado retirado (no presente en el árbol) — sin `PARO`.

## Arranque

1. Repo verificado, `git status` limpio.
2. `SHA` de redacción del encargo (`9e9132d`) verificado **desactualizado** contra `origin/main` al arrancar: `HEAD` real = `c6a5ab3`, 5 commits después (`c6a5ab3aab914506f23d64b9858fbfef3f404392`). Reportado antes de escribir; sin discrepancia de contenido en los archivos tocados por este acto.
3. `data/raw` ausente — OK, declarado, no se requiere microdato.
4. Entorno `cloud_default`, sin microdato: sonda saltada, negativos con conteo (`A.13`, ver censo).
5. Cero cifras del espejo citadas en este acto; el canon es lo que este acto commitea.

## Existencia (dirección, contra `9e9132d` / re-verificado contra `HEAD` real)

- `R2.1` sin veredicto en el Registro (`README.md:36`: 20 de 27; `R2.1` no está entre las 20 archivadas).
- El compass **NO estaba** en el árbol al arrancar (`0` archivos con patrón `R21-censo-fuentes` o el sha256 exigido, sobre el árbol de la sesión). El adjunto requerido se recibió después, en el mismo hilo, como un archivo subido por el usuario (no parte del árbol del repo, sin cita como tal). Verificado byte a byte: `sha256sum` → `27f78baa1537d418306eacf1d8a359ec7f5452d80ff1550b22c47d03b57cd762`, coincide exactamente con el exigido por el encargo. Adjuntado sin ediciones en `forense/R21-censo-fuentes-v1_0.md`.
- La ficha `R2.1` vive en `hitoD-preregistro-v2_0.md:73` con el falsador `v1` y su escala (`D` pre-registrado como "probable").
- Re-corrido el barrido interno de dirección declarado por el encargo (grep sobre `data/inventarios/`, `data/manifiesto.yaml`, `modelo` por 9 términos de voz organizacional): sin hallazgos de voz organizacional en el corpus interno de inventarios/manifiesto — consistente con lo declarado por el encargo (los aciertos de "denunci" que sí existen en el corpus son de denuncia ciudadana, dominio distinto). No se re-tecleó el conteo exacto 9×13 del encargo; se confía en el censo externo adjunto como la pieza que satisface `A.4`/`A.13` para esta ronda.

## Qué se hizo (perímetro cerrado)

1. **`forense/R21-censo-fuentes-v1_0.md`** — nuevo, el compass, byte-idéntico (verificado por sha256, cero ediciones).
2. **`forense/hitoD-preregistro-v2_0.md`** — SOLO la ficha `R2.1`, por append de enmienda fechada 25/ago/2026: `v1` intacto arriba, falsador `v2` a nivel individuo, escala B-bis completa, candado de contaminación nominal, precedencia `v1`/`v2` declarada, sucesor MEDIDOR declarado sin lanzar.
3. **`canon/gobernanza-v1_15.md`** — `ADR-190`, verbatim de la firma de mesa + línea de procedencia del censo (corrido por mesa con web search, 25/ago/2026 — no por un asistente externo; corrección de atribución dejada en actas).
4. **`forense/firmas-pendientes.tsv`** — fila nueva `FP-158`, nacida `FIRMADA` (`A.12`), `gatea` = el acto medidor `v2`.
5. **`canon/estado-programa-v1_10.md`** — nota añadida a la línea Hito D: "`R2.1` re-especificada, medidor pendiente"; el contador **no se mueve** (sigue 20 de 27).
6. **Esta nota de cierre.**
7. **`forense/encargos/2026-08-25-R21-FALSADOR-V2-RESPEC.md`** — encargo archivado.

**El Registro de veredictos archivados NO se tocó** — no hay veredicto que archivar; `R2.1` sigue sin letra, `v2` la producirá el acto medidor.

## No hizo (declarado)

No midió. No archivó letra alguna. No borró ni editó `v1`. No lanzó el medidor ni el pase de falsadores. No tocó duelo, pool, milpa ni el resto del pre-registro.

## Contador

**Cero, declarado.** Este acto no mide ni archiva; fabrica el falsador que el acto medidor sucesor probará.
