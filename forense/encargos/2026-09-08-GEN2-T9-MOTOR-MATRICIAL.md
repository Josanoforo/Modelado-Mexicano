ENCARGO · ACTO GEN2-T9 · v1.1 · EL MOTOR ES LA MATRIZ — CORRIGE D11, RE-DERIVA LA DEMANDA, MARCADOR COMO PUNTO DE LA MATRIZ · NO-GEN2 PARA LOS CORREDORES ENVUELTOS

Cabecera: **NUBE** · **Opus** · redactado contra `origin/main = 7a958b06` (PR #613) · ADR/FP: deriva en el 0-bis · COMPUERTA: ninguna. NO se lanza en UBUNTU. Cierra con `## NO-CORRIDO / RESERVAS` (A.14) y rama borrada. **Sustituye a T9 v1.0 (no lanzada).**

FIRMAS DE MESA — verbatim, 8/sep/2026: **D-1** «decisión 1 no cuentan como Gen2, no cometamos un error sobre los 600 PR's que ya cagamos». **D-2** «revisa el whitepaper, esto es una matriz, no colapsamos a menos que la literatura y benchmark de lo que queremos lograr lo demanden, podemos modular por ola si es lo más práctico». **D-2 bis** «el motor no es una matriz?, solo el modelo de decisión? […] tenía entendido que sí es una matriz». Y la de fondo, 6/sep: «mi merge manual es la firma de las decisiones».

═══ VERIFICACIÓN DE EXISTENCIA (A.8) — lo que D-2 bis obligó a verificar, con archivo ═══
- **El motor es matricial por sello de mesa.** `canon/gobernanza-v1_15.md:1821–1823`, ADR-91 (17/ago/2026, PR #246), firma verbatim: *«M1 cómputo matricial como definición del ejecutable»*, adoptado antes del gate de Fase 1. El ejecutable es `milpa/src/motor.py` (`evaluar(celda_d, catalogo, matriz_B)`, `correr(...)`), `matriz.py` (`cargar_B`, `g(matriz, theta, celda)`), `theta.py`, `pi.py` (`construir_pi(cortes)`), `celdas.py` (`celda(cortes, **coords)`), `momentos.py`; consume `milpa/procedencia.yaml` (Θ: 18 `MEDIDO·PARCIAL` + 5 `MEDIDO·NACIONAL`; β de generador; `asignados_probabilidad`), `data/curacion-registro/celdas-d/` y el catálogo de momentos `AJUSTE`/`HOLDOUT`. El **emisor** (`milpa/src/emisor.py` sobre `tramite.yaml`) es el emisor binario de reglas que alimenta al marcador; no es el motor: es una de sus salidas.
- **D11 del ADR de E3.1 contradice ADR-91.** Decía: «`motor.py`, `theta.py`, `pi.py`, `celdas.py`, `momentos.py` son scaffold histórico; el sistema numérico activo es `tramite.yaml + emisor + matriz B`». Vino de la auditoría de readiness externa (7/sep) y dirección la propagó sin cotejarla contra ADR-91 — error de dirección, de la misma clase que A.8 nombra (dictar contra el árbol sin comando). **Queda revocada por este acto**; el ADR-91 rige.
- **La demanda de C0-A no incluyó al motor matricial.** `data/corrida0/demanda-resultados.tsv` por tipo: `conducta_p_medido 50 · conducta_p_asignado 14 · coeficiente_ejecutable 7 · coeficiente_asignado 8 · asignado_probabilidad 13 · celda_R/M/L/AGREGADO 14/14/28/14` = 162. **Cero filas** para las 23 condicionales Θ de `procedencia.yaml`, para los cortes π, para las celdas-D ni para el catálogo de momentos (`grep -cE "condicional|theta|MEDIDO·PARCIAL"` → 0 entre los tipos). El cierre transitivo se derivó desde el emisor y el marcador, no desde el ejecutable sellado. `N = 162` es una cota inferior.
- **La constancia de `M` dentro de CIV es la matriz evaluada en el origen.** Las 14 celdas del marcador son agregados nacionales sin vector de atributos (`x = ∅`); con `x = ∅` la evaluación matricial se reduce a la `p` base de la regla, y por eso seis celdas de una regla repiten un número. No es un defecto del emisor ni pide colapsar olas: pide que la celda porte su punto de la matriz.

PIEZAS
**P1 · D-1 propagada.** `data/corrida0/decisiones.tsv`: `CALC-M-marco-M-sorteado-v1_3` y `CALC-AGG-…` → `cuenta_gen2: NO · insumo LEGACY (tramite.yaml GEN1)`; los `spec.yaml` sellados no se editan (E.3); `cmd_registro`/`cmd_status` leen la decisión y los sacan a `corredores_envueltos_legacy`, nunca a `N_corridas_selladas`; test. Regla escrita (nota + hallazgo): **ningún CALC cuyo input resuelva a `milpa/tramite.yaml`, `milpa/procedencia.yaml` o `corridas-R/M/L` cuenta como GEN2, por completa que sea su cadena** — E.1 con nombre.
**P2 · D11 revocada y la demanda re-derivada (C0-A bis).** (a) ADR con la revocación, citando ADR-91 verbatim, y fila `NC-` propia: «D11 de E3.1 dictada contra el árbol; corregida por T9». (b) `cmd_demanda` gana los consumidores del motor matricial, leídos de `motor.py`/`matriz.py`/`pi.py`/`celdas.py`/`momentos.py` y no de memoria: tipos nuevos `condicional_theta` (una fila por Θ con clase y eje), `corte_pi` (una por corte de eje que `construir_pi` consume), `celda_D` (una por celda de `data/curacion-registro/celdas-d/`), `momento` (una por momento del catálogo, con rol `AJUSTE`/`HOLDOUT`). Re-derivar `demanda-*.tsv`; reportar `N_resultados_activos` nuevo y `N_corridas_requeridas` nuevo con el diff por tipo; `dependencias_numericas_legacy_activas` sube en consecuencia y **eso es lo correcto**. Nada se recalcula: solo se cuenta lo que faltaba.
**P3 · D-2 · unidad de celda = punto de la matriz; no se colapsa; se modula por ola donde hay serie.** (a) `NC-0020 → CERRADA` con la decisión: la celda del marcador es `regla × segmento (x, sobre los seis ejes del modelo) × ola × instrumento`; las 14 celdas actuales son el caso `x = ∅` y se conservan; **no se colapsan olas**; la dimensión que falta es el segmento, y existe del lado del árbitro: la propuesta trae **77 entradas `_ejes_` con IC por eje** que ninguna celda del marcador consume (transfer 46→47 §5, verificado entonces). (b) Diseño del marcador GEN2 (para C0-D, después de C0-C): por celda con `x ≠ ∅`, `M` se emite con el motor matricial — `p` base GEN2 de la regla más `g(B, θ(x))` — y `R` es el IC por eje de la entrada `_ejes_` correspondiente; por celda con `x = ∅`, `M` es la `p` base y la celda lleva `modela_segmento: NO`. (c) Modulación por ola donde es práctico: si la conducta trae `serie_olas`, el punto usa la **ola más cercana distinta a la del árbitro** (leave-one-out; empate → anterior), `modela_ola: SERIE-LOO · ola_usada`, F-DD contra la ola usada; sin serie, `modela_ola: NO`. Hoy tienen serie 2 reglas. Nada de promediar ni ajustar tendencias: es modelo nuevo y va a C0-B con spec. (d) El agregado conserva su métrica sellada y añade dos vistas informativas, por regla y por segmento, sin colapsar nada. (e) Demostración del aparato, no medición: `CALC-M-…-ola` y `CALC-AGG-…-ola` (serie legacy), más **`CALC-MOTOR-celdas-semilla`** — el motor matricial corrido por `corrida0.py` sobre las tres celdas-D semilla y el catálogo `AJUSTE` (insumos de repo), `cuenta_gen2: NO` por P1, `verify` `REPRODUCE/IDENTICO`. Prueba que la ruta matricial también nace con cadena. (f) Enmiendas fechadas al plan v2.0 (§4 C0-D, §D11) y a la nota de E7 (`paso-3-unidad-de-celda`: las cuatro opciones estaban incompletas — faltaba la dimensión de la matriz); no se reescribe nada.
**P4 · Los cinco ajustes de la revisión de #613.** (i) cola de E7 → `CONSUMIDO (PR #612, #613)`; `cierre_acto.py` cierra la cola solo con todas las piezas marcadas, mientras `EN-CURSO (parcial: X de Y)`; T37 lo verifica. (ii) `FP-346 → FIRMADA`: R de GEN2 sale de `tools/arbitra_gen2.py`; `arbitra.py` LEGACY sin editar. (iii) `diagnostico-14-celdas-v1_0.tsv`: enmienda fechada al pie (0.62 es v1_2; v1_3 emite 0.085118). (iv) `L-spec-v1_2.json` con aviso obsoleto sobre DIN-M-01: fila FP con sucesor «L-spec v1.3 cuando corra el corredor L GEN2 (NC-0019)»; no se edita el JSON. (v) `limpia_arbol.py --reporta`: `NO-VERIFICABLE-SIN-GH` como estado propio del tablero.
**P5 · Cierre.** `## NO-CORRIDO / RESERVAS` (esperables: modulación por ola demostrada sobre 2 reglas; marcador por segmento diseñado, no corrido — depende de C0-C), ADR, recibo, `## CONSUMIDO`, rama borrada.

PERÍMETRO: `data/corrida0/decisiones.tsv` · `tools/corrida0.py` (`cmd_demanda`, `cmd_registro`, `cmd_status`) · `data/corrida0/demanda-*.tsv` (re-derivados) · `tools/emite_m.py` (envoltura por celda; `emisor.py` intacto) · `tests/gonogo_marcador.py`, `tests/test_corrida0.py`, `tests/test_corredores_gen2.py` · `data/corrida0/CALC-M-…-ola/`, `CALC-AGG-…-ola/`, `CALC-MOTOR-celdas-semilla/` (nuevos) · `forense/no-corrido.tsv` (NC-0020, NC nueva por D11) · `forense/firmas-pendientes.tsv` (FP-346, FP L-spec, recibo) · `forense/encargos/cola/…E7…md` · `tools/cierre_acto.py`, `tools/limpia_arbol.py`, `tools/tablero_programa.py` · `forense/prereg-duelo-v2/diagnostico-14-celdas-v1_0.tsv` (pie) · `forense/notas/` (enmiendas fechadas + nota) · `forense/hallazgos.md` · cascada. **No toca** `milpa/src/**` (el motor se corre, no se edita), `milpa/tramite.yaml`, `procedencia.yaml`, `procedimiento-scoring-v1_2.md`, CALC sellados ni `L-spec-v1_2.json`. Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.

CONTADOR: cero GEN2 (D-1). `N_resultados_activos` y `N_corridas_requeridas` **suben** por P2 (se reporta el número nuevo, no se estima aquí). `no_corrido_abiertas` −1 (NC-0020) +1 (D11). `corredores_envueltos_legacy` → 5.

Lo que NO hace: no colapsa celdas; no cambia la métrica sellada; no promedia series; no edita el motor; no cuenta nada como GEN2; no corre el marcador por segmento (C0-D, tras C0-C). Sucesores: **E5-0** (caja; compuerta `GO-MARCADOR` en `main`) — la re-derivación de P2 no la bloquea, porque E5-0/E5 miden reglas del emisor cuyas filas ya estaban en la demanda; **C0-B lote 1** se redacta sobre la demanda re-derivada y ahora incluye Θ y π; **C0-D** corre el marcador por segmento cuando C0-C entregue motor y emisor limpios.

---

## NO-CORRIDO / RESERVAS

| qué | por qué | impacto | sucesor |
|---|---|---|---|
| **P2(a) · re-verificar los actos que se apoyaron en `D11`.** «ADR con la revocación» — la revocación está hecha; lo que no se corrió es el barrido de los actos posteriores a `ADR-396` que hayan excluido `milpa/src/**` de su perímetro *citando* `D11`. | `DECISIÓN-DE-MESA-PENDIENTE` — la revocación la firma mesa en el merge. Barrer `GEN2-E5`/`E5-0`/`E6` buscando decisiones apoyadas en una premisa revocada es trabajo propio, no de aquí. | Ninguna cifra se mueve. Lo que queda abierto es si algún perímetro posterior se calculó sobre una premisa que ya no rige. | Acto sucesor con el barrido; `C0-C`. `NC-0022`. |
| **P3(e) · el motor matricial corrido de punta a punta** sobre las tres celdas-D semilla y el catálogo `AJUSTE`. Corrieron el catálogo, las celdas-D, los cortes y el muro `AJUSTE`/`HOLDOUT`; **no** corrieron `matriz.g(B, θ(x))` ni `motor.evaluar()`. | `PARO-PREMISA` — `procedencia.cargar()` **lanza** `ClaseDesconocida` sobre `milpa/procedencia.yaml`: dos valores de `clase:` que `milpa/src/clases.py` no conoce (`REFUTADO-POR-COTA`, `EVIDENCIA_EXPERIMENTAL_TERCEROS`). Sin `Procedencia` no hay `B`. **No se parchea aquí:** `milpa/src/**` está fuera del perímetro por declaración del propio encargo. | `CALC-MOTOR-celdas-semilla` sella `RESULT-MOTOR-ESTADO-B = NO-EJECUTABLE` en vez de veredictos por celda. El ejecutable que `ADR-91` selló no arranca hoy. | Acto sucesor con `milpa/src/clases.py` en perímetro; después, re-correr el CALC hasta `ESTADO-B = CARGA`. `NC-0023`. |
| **P3(b) · el marcador por segmento** (celda con `x ≠ ∅`, `M` por motor matricial y `R` por el IC de la entrada `_ejes_`). **Diseñado, no corrido.** | `DIFERIDO-A:C0-D` — el encargo lo declara explícitamente («no corre el marcador por segmento (C0-D, tras C0-C)»). Depende de `C0-C` y, antes, de que el motor arranque. | Las 14 celdas siguen con `x = ∅`: `RESULT-AGGOLA-CELDAS-CON-SEGMENTO = 0` contra **74** puntos por eje con IC95 que existen y ninguna celda consume. El eje `M`-vs-`R` por segmento sigue `NO-ESTIMABLE`. | `ACTO C0-D`, después de `C0-C`. `NC-0024`. |
| **P3(c) · modulación por ola:** demostrada sobre **2 reglas** (6 de 14 celdas), no sobre el universo. | `NO-VERIFICABLE-AQUÍ` — hoy sólo 2 reglas del marco vigente traen `serie_olas`. Las otras 8 celdas salen `modela_ola: NO` porque no hay serie que leer, no porque el aparato falle. | `RESULT-MOLA-N-CON-SERIE = 6`, `N-SIN-SERIE = 8`. Ninguna cifra de la modulación entra a un veredicto. | `C0-B` lote 1 (spec de la modulación); actos de medición que añadan `serie_olas`. `NC-0025`. |

**Reservas sobre el propio encargo (no son piezas no corridas; son cifras suyas que el árbol no sostiene, corregidas aquí y no en él —A.3):**

- «Θ: 18 `MEDIDO·PARCIAL` + 5 `MEDIDO·NACIONAL`» → el árbol da **10 + 2 = 12**, por la fórmula oficial (`procedencia.contador_condicionales_medidas()`).
- «77 entradas `_ejes_` con IC por eje» → **7 entradas**, **24 ejes**, **74 puntos por eje** con `ic95`.
- Contador declarado `no_corrido_abiertas` −1 +1 = 10 → el real es **13**: `NC-0020` cierra (−1) y entran **cuatro** filas (+4), no una. Las tres de más son piezas realmente no corridas que este acto encontró; dejarlas fuera para que cuadrara el contador habría sido el error que A.14 existe para impedir.
- «`CALC-M-…-ola` y `CALC-AGG-…-ola` (serie legacy)» y `CALC-MOTOR-celdas-semilla` sí se corrieron y sellaron, los tres con `verify REPRODUCE`. `CONTEXTO=DISTINTO` en los tres: cada uno se selló en su propio commit y el árbol siguió avanzando dentro del mismo acto.

Las conclusiones del encargo se sostienen las dos —el motor faltaba en la demanda; existen puntos por eje con IC que ninguna celda del marcador consume—. Las cifras, no.
