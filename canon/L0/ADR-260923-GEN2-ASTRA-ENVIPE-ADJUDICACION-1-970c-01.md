**ADR-260923-GEN2-ASTRA-ENVIPE-ADJUDICACION-1-970c-01**, `ACTO GEN2-ASTRA-ENVIPE-ADJUDICACION-1`, 23/sep/2026, NUBE, Sonnet 5, MODO RÍGIDO en el procedimiento heredado. Compuerta (`#1031`+piloto 4/`#1036` en `origin/main`) y premisas de §3 verificadas por comando (input_ids sin ENVIPE2025, historial `-S` del sello de R → 0 apariciones en la rama de Astra, empate de celdas ya documentado por la propia rama de Astra en `admisibilidad.tsv`). PARO-ENTORNO al ejecutar: el método heredado del piloto 4 propaga la incertidumbre de ΔMAE con réplicas bootstrap de `R` sobre `envipe2025_csv`; el CALC sellado del árbitro no guarda esas réplicas crudas (685 entradas, 0 tipo lista, verificado por comando) — solo escalares ya cerrados para los cuatro candidatos originales. C-ASTRA es un quinto candidato sin IC de ΔMAE sellado en ningún lado: reproducir el método exige reabrir microdato ENVIPE 2025, que es el PARO (a) de este mismo encargo; sustituirlo violaría el PARO (d) y la LATITUD ("NO DECIDES: nada del procedimiento heredado"). Cero corridas nuevas, cero adopción, `celdas_validadas` sin cambio. Sucesor: acto en CAJA que materialice las réplicas de R por celda, o decisión de mesa sobre un método de IC alternativo. Suite `--rapido`: ver comando y salida en el commit de cascada.
