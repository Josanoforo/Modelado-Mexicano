ENCARGO · ACTO GEN2-REGISTRO-REPLAY · LA EVIDENCIA DE REPLAY NO SE PISA — reparación de NC-0094: el registro deja de confundir «no verificado ahora» con «nunca reprodujo», y deja de borrar veredictos ajenos al regenerar

CABECERA · NUBE, Opus (cirugía en el registrador + tests dirigidos: juicio, no receta) · NO se lanza en CAJA — no abre microdato y la caja está ocupada por el acto DBF · COMPUERTA: ninguna (re-deriva contra origin/main al abrir; redactado contra 4497029a, PR #657) · candidatos FP/NC/ADR: deriva al cierre, no heredes.

FIRMA DE MESA, 9/sep/2026, verbatim (adentro; su merge sella): «Dame los encargos de la propuesta de Astra.» — despacho del punto 2 de PROPUESTA-MEJORAS-NOTAS-PR631-659 (Astra, 9/sep), adaptada por dirección tras verificación: el hallazgo principal fue REPRODUCIDO por dirección en clon propio — 23 corridas cuyos campos resultado_replay/contexto_replay cambiarían a NO-VERIFICADO en una regeneración sin verificar, incluyendo veredictos ADVERSOS (NO-REPRODUCE del CALC-MOTOR-celdas-semilla, varios NO-EJECUTABLE).

VERIFICACIÓN DE EXISTENCIA (A.8, contestada por dirección, 9/sep/2026, contra 4497029a): (1) ESTRUCTURA — gobiernan: tools/corrida0.py (_lee_oferta inicializa ambos campos en NO_VERIFICADO; _filas_registro los proyecta; registro --escribe escribe las tres vistas; verify() no escribe artefactos canónicos — leído del código) · los directorios CALC-*/ (contienen sello.json, ejecucion.json, resultados.json; ningún archivo-comprobante de verify con nombre propio: verificado en CALC-R-CIV-M-10, siete archivos listados) · forense/no-corrido.tsv (NC-0094 EXISTE, del acto R-SERIE-CSV; NC-0097 EXISTE, asiento de firmas de los tres R). (2) CONTENIDO — la reproducción del defecto: 23 corridas afectadas, comando y salida en poder de dirección (anexo de la propuesta, ejecutado verbatim). NO existe hoy protección alguna de diff-antes-de-escribir en el camino de registro --escribe: NO-ENCONTRADO en el código leído. (3) COBERTURA RETROACTIVA — el acto DBF en vuelo cierra con la contención MANUAL (adenda NC-0094 ya entregada: diff pegado, paro si toca ajenas). Este acto convierte esa contención en mecánica.

PIEZAS (D-11, cuatro afines de NUBE): P0 · REGISTRO DEL INSUMO (A.3). Archiva verbatim forense/notas/2026-09-09-PROPUESTA-MEJORAS-NOTAS-PR631-659-astra.md con cabecera de procedencia ARRIBA del texto intacto («Insumo externo (ChatGPT/Astra) · entregado a dirección 9/sep/2026 · registrado por ACTO GEN2-REGISTRO-REPLAY conforme a A.3 y regla de mesa 4 · hallazgo principal reproducido por dirección en clon propio, 23 corridas · el texto de abajo no se edita»). El original viaja adjunto a este lanzamiento; si falta, PARA y pídelo. P1 · CONTENCIÓN MECÁNICA. registro --escribe calcula el diff de resultado_replay/contexto_replay contra las vistas publicadas ANTES de escribir. Si alguna corrida AJENA al lote autorizado cambiaría de veredicto, se niega, lista los ids afectados con sus transiciones, y termina sin tocar las tres vistas. Sin opción de borrado silencioso, sin --force. P2 · SEPARACIÓN DE EVIDENCIA HISTÓRICA Y OBSERVACIÓN DE SESIÓN (el cierre real de NC-0094). Los dos ejes conservan su semántica sellada (E.3) y ganan fuente: una sesión sin corpus NO degrada un REPRODUCE anterior ni esconde un NO-REPRODUCE anterior — reporta su propia limitación aparte. Comprobante: reutiliza la identidad que YA existe en sello.json/ejecucion.json si contiene lo suficiente (corrida, hash, spec, código, inputs verificados, fecha, entorno, dos ejes con razones); si no alcanza, registra la salida estructurada del verify en la nota de cierre del lote correspondiente — fuera de los bytes sellados, jamás editando sellos — y haz que el registro la cite. Los TSV derivados no se vuelven su propia fuente. Un veredicto heredado sin comprobante suficiente se proyecta como evidencia histórica con su fecha y alcance, nunca como verificación recién acreditada. Prohibido: borrar, arrastrar evidencia a otra identidad (cambio de spec/inputs la invalida como vigente), fingir verificación actual, esconder resultados contradictorios. P3 · VALIDACIONES DIRIGIDAS + ASIENTO NC-0097. Las seis de la propuesta, sobre fixture o copia temporal — NO 23 replays reales: (a) registrar un lote nuevo no modifica evidencia ajena sin razón explícita; (b) segundo registro sobre el mismo corte = idempotente; (c) sesión sin corpus no borra evidencia de CAJA; (d) cambio de input/spec invalida el comprobante como vigente; (e) un NO-REPRODUCE posterior queda visible junto a su contexto — conservar historia no privilegia el último éxito; (f) el caso de las 23 filas reproducido en fixture demuestra primero el defecto y después la protección. Además: asienta las firmas de contador de los tres CALC-R del CSV donde NC-0097 lo pide (asiento con cita, no re-firma ni re-medición) y ciérrala; cierra NC-0094 solo si P1+P2 completos — si solo P1 aterriza, NC-0094 queda ABIERTA con el residuo escrito.

PERÍMETRO Y CONCURRENCIA. Toca: tools/corrida0.py · tests/ (fixture y casos dirigidos) · forense/notas/ (la propuesta archivada + nota del acto) · forense/no-corrido.tsv y forense/firmas-pendientes.tsv (tokens/append) · 0-bis · cascada. EN PARALELO: el acto DBF en CAJA (CALC nuevos, prereg-caja, TSV re-derivados con contención manual) — si su merge llega antes que el tuyo, re-deriva y reporta; NO toques ningún directorio CALC-*/ ni sellos. E2 (PREP-LOTE) espera tu merge: mismo archivo corrida0.py. «Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.»

CONTADOR: no — este acto protege mediciones, no las produce; lo dice en una línea al cierre.

LO QUE NO HACE: no corre replays reales de las 23 corridas · no edita sellos ni resultados para alojar metadatos (E.3) · no inventa otra máquina de estados ni un registro paralelo · no toca la identidad de la demanda (E2) ni el derivador del digesto (E3) · no reabre NC-0082.

SUCESORES: GEN2-PREP-LOTE (gateado a este merge) · la primera regeneración global posterior, que estrena la protección en producción.

CIERRE · Cascada completa + ## NO-CORRIDO / RESERVAS + ## CONSUMIDO con el PR.

---

## NO-CORRIDO / RESERVAS

| qué (verbatim del encargo) | por qué | impacto | sucesor |
|---|---|---|---|
| «asienta las firmas de contador de los tres CALC-R del CSV donde NC-0097 lo pide (asiento con cita, no re-firma ni re-medición) y ciérrala» | `FUERA-DE-PERÍMETRO` | `NC-0097` sigue **ABIERTA**. El asiento vive en `data/corrida0/decisiones.tsv`, que **no aparece** en la lista «Toca» de este encargo; y escribirlo tiene consecuencia mecánica medida: `_cuenta_gen2_resuelto` da precedencia a `decisiones.tsv` sobre la etiqueta de la spec, así que las tres filas cambiarían `motivo_cuenta_gen2` de los tres `CALC-R` en `corridas.tsv` y obligarían a **re-derivar las tres vistas** — tampoco en el perímetro, y en manos del acto DBF que corre en CAJA ahora mismo. Es el caso exacto que la regla del encargo anticipa («si te encuentras escribiendo fuera de esta lista, PARA»). Ningún contador se mueve por esto: el registro **ya cuenta** los tres por `etiquetas.cuenta_gen2_firma`; lo que falta es la fila del artefacto de mesa. | `FIRMAS-ADOPCION-1` (o acto con `decisiones.tsv` en su perímetro) — el sucesor que la propia fila `NC-0097` ya nombraba |
| P2 · «registra la salida estructurada del verify … y haz que el registro la cite» — la parte de la salida **estructurada** | `NO-VERIFICABLE-AQUÍ` | Los 23 asientos son `HEREDADO`, con `fecha_verificacion = DESCONOCIDA` y `razones = NO-ARCHIVADA-EN-SU-MOMENTO`: evidencia histórica de alcance limitado, proyectada y **impresa** como tal, nunca como verificación recién acreditada. Ninguno es todavía `VERIFY-ESTRUCTURADO`. El mecanismo existe, está cableado y probado por mutación; falta que un lote corra `verify` **con corpus**, que esta nube no tiene (el acto se lanzó en NUBE a propósito). No bloquea: la evidencia ya no se pisa, que era el defecto material. | `NC-0104` → `GEN2-PREP-LOTE`, o la primera regeneración global con corpus |
| P2 · columna de fuente en las tres vistas | `FUERA-DE-PERÍMETRO` | La cita de procedencia vive en `forense/replay-evidencia.tsv` y en `registro --fuentes`, no en una columna del TSV. Añadirla exige una **re-derivación global** de `data/corrida0/*.tsv`, que no está en la lista «Toca» y que chocaría de frente con el acto DBF. Como contrapartida, las tres vistas salen **byte a byte idénticas** a las publicadas — que es también la prueba en producción de que la reparación funciona. | `NC-0104` → la primera regeneración global posterior |
| Reserva · alcance de lo despachado | `DECISIÓN-DE-MESA-PENDIENTE` | Este acto despacha **solo el punto 2** de la propuesta de Astra (`§3`, integridad del registro). Los puntos 3 (identidad ENCIG/ENCUCI por consumidor), 4 (tests de conteo y huso, `NC-0080`), 5 (calibración del revisor) y 6 (localización en el derivador, alineación documental) **no se tocaron** y siguen siendo propuesta, no decisión. Se dice aquí para que su ausencia no se lea como cobertura. | `GEN2-PREP-LOTE` (puntos 3 y 4) y el encargo de operación (puntos 5 y 6), ambos por despachar |

**CONTADOR: no** — este acto protege mediciones, no las produce. Ningún `CALC` nace, ningún sello se toca, `status` da lo mismo antes y después, y las tres vistas derivadas salen sin diferencia con el disco.

---

## CONSUMIDO

Ejecutado por [PR #660](https://github.com/Josanoforo/Modelado-Mexicano/pull/660) — `ACTO GEN2-REGISTRO-REPLAY · LA EVIDENCIA DE REPLAY NO SE PISA`, rama `claude/funny-ramanujan-puyxic`, 9/sep/2026, NUBE, Opus. Base `origin/main = 66eed1b4f4a3` (`PR #658`); re-derivado contra `435e60a` tras el merge de `PR #661`, que fusionó primero — **RENUMERADO** por la regla de la casa: `ADR-434` → `ADR-435` y `NC-0098` → `NC-0104`. Cascada cerrada: `ADR-435`, `L0` recifrado, tres contadores a `435`, rótulo `GEN2 · GEN2-REGISTRO-REPLAY` censado en `canon/registro-rotulos.tsv`, `T22`/`T25` censados con comentario. `tests/check.py --baseline` VERDE. **El merge de mesa es la firma.**
