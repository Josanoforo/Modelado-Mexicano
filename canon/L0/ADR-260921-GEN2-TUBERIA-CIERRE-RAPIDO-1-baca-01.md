**ADR-260921-GEN2-TUBERIA-CIERRE-RAPIDO-1-baca-01 · `ACTO GEN2-TUBERIA-CIERRE-RAPIDO-1` · LA SESIÓN VERIFICA EN SEGUNDOS Y EL CI JUZGA · T16 SE RETIRA**, 21/sep/2026, entorno **NUBE (`cloud_default`), Opus 5, sin microdato** (`forense/encargos/2026-09-21-GEN2-TUBERIA-CIERRE-RAPIDO-1.md`, redactado contra `b67d09f5` y archivado verbatim por 0-bis A.3 en `baca5e9`, sello de cuerpo `610a4394…`). **MODO ABIERTO · COMPUERTA: ninguna** · **`cuenta_gen2 = NO`**. Ejecuta P0 y P-A del lote de cuatro piezas (A→B→C→D) de la firma de mesa del §1 (verbatim, v1.1, corrección del punto 5), asentada por este acto en `forense/firmas-pendientes.tsv` (`FP-260921-GEN2-TUBERIA-CIERRE-RAPIDO-1-baca-01`, A.12). **P0**: firma asentada; sondas de `gh` (protección de `main`, `GITHUB_TOKEN` empujando a `main`, lectura de CI desde la sesión) **no ejecutadas** — `gh` no está disponible en este entorno — quedan como pregunta a mesa (no PARO, previsto por el propio encargo §4/P0). **P-A**: `tests/check.py --rapido` corre el subconjunto rápido (T02, T22, T25, T15, T27, T30, T34 + `verifica_sidecars.py`) en un solo comando, con la lista y su razón en un solo sitio (`SUBCONJUNTO_RAPIDO`); medido **7.70s** contra el presupuesto de 15s. `.claude/commands/acto.md` (líneas 349/401/473) y `tools/cierre_acto.py` (`corre_baseline`) usan `--rapido` en vez de `--baseline --parallel`: la sesión verifica en segundos, el CI juzga con la suite completa en el push. **T16** (T-SUITE-SELF-CHECK) se retira de `tests/check.py` — premisa `EJECUTADO` del encargo: cero afirmaciones vigentes que comparar quedaban en `canon/`; sus 2 FAIL salen de `tests/baseline.json` (13 FAIL aceptados, de 15). **P-B (una sola derivación del árbol compartida), P-C (suite estricta, retiro del canal WARN) y P-D (derivados fuera de los PR)** del mismo lote **no corrieron en este acto** — su alcance (retirar `warn()`/`senal()` de 22 sitios con la tabla de destinos del encargo, deduplicar la derivación de T32/T32-quater/T45/marcador con prueba byte-idéntica, y un job de CI con permiso de escritura que re-deriva y commitea) excede lo que este acto pudo verificar con el rigor que sus propios PAROS exigen (ningún byte de P-B cambia, ningún FAIL sin acotar en familia con deuda vieja, el job de derivados no commitea nada que no sea derivado); quedan **NO-CORRIDO** con sucesor explícito (ver `## NO-CORRIDO / RESERVAS` del encargo archivado). Una pieza que PARA no tumba el lote (D-11): P0/P-A se entregan completos y verificados; P-B/P-C/P-D pasan a un acto sucesor. **El PR queda propuesto; mesa central fusiona.** Nota de cierre: `forense/notas/nota-2026-09-21-gen2-tuberia-cierre-rapido-1.md`.
