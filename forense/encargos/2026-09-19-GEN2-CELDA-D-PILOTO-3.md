ENCARGO · ACTO GEN2-CELDA-D-PILOTO-3 · COMMIT-1 · ENCIG 2025, `edad × escolaridad`: ¿SE TRANSPORTA UNA INTERACCIÓN HISTÓRICA ESTABLE CUANDO EL NIVEL SE MOVIÓ ONCE PUNTOS? SPEC Y CÓDIGO CONGELADOS, SIN MICRODATO

CABECERA (D-12) · SHA de redacción `6f365928` (merge de #887); re-deriva al abrir · ENTORNO: NUBE — este acto es solo el COMMIT-1: spec, código y guardias; cero microdato; NO caja · una sola sesión, rama propia; `/acto` PARA si ya está archivado en otra rama viva · COMPUERTAS (cumplidas): `CALC-ENCIG2023-CRUCES-HISTORICOS-0002` y `CALC-ENCIG2021-CRUCES-HISTORICOS-0003` (#887) y `CALC-PISO-PERSISTENCIA-ERROR-0001` (#883) en main · MODELO: Opus · FP/ADR/NC: deriva al cierre · CONTADOR: `cuenta_gen2` NO-APLICA — no sella corrida; registra una celda-D y congela una spec. Adjuntos (A.3): careo `796689c4dce6f43d`, Diseño A, Diseño B, brief 03 `f48171b735534bb3`; si falta uno, PARA.

FIRMAS DE MESA (propuestas por dirección; tu lanzamiento con este archivo es el sello; sin texto, PARA)

F1 · El cruce (enmienda declarada a la regla del brief 03). "El piloto 3 corre en ENCIG 2025, `edad × escolaridad`, con 15 celdas puntuables. La celda `18–29 × hasta primaria` queda `FUERA-DE-SOPORTE` declarada ex ante. Es una enmienda a la regla de elección, hecha después de ver ENCIG 2021 y 2023 y antes de que nadie vea ENCIG 2025; se asienta como tal en la spec y en hallazgos." F2 · Quién ejecuta. "COMMIT-1 lo congela Claude en nube sin microdato. COMMIT-2 y COMMIT-3 los ejecuta Codex en caja, mecánicamente, con el código congelado aquí. Quien diseñó no toca el dato."

Por qué F1, con los hechos leídos de los dos CALC sellados (comando en la nota de dirección): la regla congelada exigía `n ≥ 200` en todas las celdas; `edad × escolaridad` falla en una de 16 (`18–29 × hasta primaria`: n = 70 en 2023, 110 en 2021). Aplicada a la letra, la regla deja dos candidatos empatados en todos sus desempates — `sexo × escolaridad` (puntaje 0.87; 1 de 8 celdas con IC de δ que excluye 0) y `sexo × edad` (0.79; 0 de 8 → `SIN-PODER-DE-FALSACION`). Mientras tanto `edad × escolaridad` tiene puntaje 2.15 (1.60 en 2021), 7 de 16 celdas con δ distinguible de 0 y 13 de 16 con el mismo signo en las dos olas: ahí está la interacción, y es estable — justo lo que les faltó a los retadores del piloto 2. La regla de soporte de la casa (spec del piloto 2 §3.4) nunca exigió todas las celdas: tolera hasta ⅓ sin soporte. La regla de dirección fue más estricta que la casa, por error de redacción de dirección. Lo que la enmienda no contamina: la adjudicación es fuera de muestra sobre 2025, que nadie ha visto. Alternativa a la letra, si mesa la prefiere: `sexo × escolaridad`, 8 celdas, poder bajo declarado.

VERIFICACIÓN DE EXISTENCIA (A.8; contra `6f365928`)

(1) Gobiernan: celdas-D (`data/curacion-registro/celdas-d/`, 5 registradas), `tools/celda_d/marginales_reproduccion.py` (guardia de una sola variable de agrupación), specs de los pilotos 1 y 2, marcador. (2) `ls data/curacion-registro/celdas-d | grep -i encig` → 0; `git grep -l "PILOTO-3"` fuera de `forense/encargos` → 0: NO-ENCONTRADO. `marcador-segmento.tsv`: `edad × escolaridad` ENCIG 2025 = `RESERVADA`. `git grep -iE "edad.{0,4}(x|×).{0,4}escolaridad" `sobre payloads/derivados de `encig25*` → verifica 0 y declara el conteo de archivos (A.13). (3) Ningún piloto previo tocó ENCIG.

PIEZAS

P0 · COMPUERTA DE CONTENIDO — por texto, antes que nada (A.15). `CALC-PISO-PERSISTENCIA-ERROR-0001` midió en ENCIG `R − piso ≈ +10 a +12 pp` en todas las celdas y todos los ejes (MAE 10.8 pp; ENIF 2.9; ENVIPE 2.6). Un salto uniforme es la firma de dos cosas distintas: adopción digital real 2023→2025, o cambio de instrumento. Dirección buscó verificación por texto de `P7_3` 2023↔2025 en las notas de rejilla, enlace y cruces: NO-ENCONTRADO con esos términos. Lee los cuestionarios y FD de 2021, 2023 y 2025 (ids en `data/manifiesto.yaml`: `encig2021_cuestionario_pdf`, `encig23_cuestionario_pdf`, `encig25_cuestionario_pdf` y sus estructuras): redacción del reactivo, opciones y códigos de `P7_3`, filtro `N_TRA`, flujo que lleva a la pregunta, catálogo de trámites. Si 2025 cambió el reactivo, las opciones o el universo: PARO-PREMISA — no hay piloto, y el entregable es ese hallazgo más la re-rotulación del error de persistencia de ENCIG como `NO-COMPARABLE`. Si es el mismo instrumento, sigue, y el +11 pp se reporta como cambio de nivel en un universo de trámites, sin leerlo como disposición.

P1 · Spec (humana + `spec.yaml`, D-15). Estimando, primera línea: proporción de trámites con `P7_3 ∈ {4,5}` entre válidas `{1,2,4,5,6}`, `N_TRA == 1`, por `edad × escolaridad`, ENCIG 2025; unidad trámite; no comparable en nivel con ENIF/ENVIPE. Candidatos, todos en logit, todos con fórmula cerrada y `RESULT` de origen citado por id:

* C2 (el piso a vencer): `expit(L p₂₅(a) + L p₂₅(b) − L p₂₅)`, marginales 2025 por el módulo guardado, réplica por réplica.
* C1a / C1b (referencia, no piso a vencer): persistencia compuesta con marginales 2023, y `p₂₃(a,b)` directo. Las reglas ciegas de A y de B coinciden en esto cuando el CALC dice `CAMBIA`: la persistencia no puede acotar lo que se movió once puntos. Se reportan para medir cuánto.
* S½ (Astra): `C2 + ½·δ₂₃(a,b)`.
* Sλ (Opus, corregido): `C2 + λ·δ̄(a,b)`, `δ̄ = (δ₂₁+δ₂₃)/2`; `λ = τ̂²/(τ̂²+σ̄²)` con `τ̂² = max(0, Var_entre(δ̄) − σ̄²)` (el original contaba ruido como señal). λ se deriva en esta sesión de los `DELTA` y `DELTA-EE` sellados y se congela como constante; no se teclea.

Soporte y adjudicación de la casa, escalados: celda `PUNTUADA` si `n ≥ 200` en 2021, 2023 y 2025; `FUERA-DE-SOPORTE` global si fallan ≥ 5 de 15; por celda, `INDECIDIBLE` con las dos condiciones del programa verbatim (`CAREO-ADV-DUELO-diseno-v2:38`); un retador gana solo si vence a C2 en ≥ ¾ de las `PUNTUADA`. Lectura secundaria pre-registrada (de B): `ΔMAE = MAE(C2) − MAE(j)` con IC réplica por réplica, respetando estrato, UPM, ponderador y agrupación de trámites por persona. B-bis, sellado aquí: nadie vence y el límite superior de ΔMAE ≤ 0.5 pp → corroborada, acotada a cruces de dos ejes estructurales en tres dominios. Nadie vence pero el IC aún admite > 0.5 pp → falsador débil, aunque sea la tercera vez; si ambas lecturas caben, manda falsador débil. Un retador vence → limita a C2 solo en este desenlace, rejilla y ola; C2 sigue adoptado en DIN y TRA (A.10). Sλ vence y S½ no, o al revés → el hallazgo es sobre cuánto encoger, y se reporta así.

P2 · Código congelado, dentro del directorio del CALC. Emisiones (COMMIT-2) y adjudicación (COMMIT-3) en archivos separados. El de emisiones solo llama `marginal(ola, grupo: str)`; el de adjudicación es el único autorizado a agrupar por dos variables sobre `encig25*`, y se niega a correr si no existe el sello de emisiones. Tests con fixture sintético (aquí no hay dato): guardia de una variable; negativa sin sello; `18–29 × hasta primaria` jamás entra al puntaje.

P3 · Registro. Celda-D `GOB.gobierno_digital.encig2025.edad_x_escolaridad.yaml`, `champion_actual: NINGUNO`, estado `SPEC-CONGELADA`. Marcador: el par sigue `RESERVADA`. `decisiones.tsv`: F1 y F2.

PERÍMETRO

`forense/prereg-caja/GOB-gobierno-digital-exe15-spec-v1_0.md` + sidecar · `data/corrida0/CALC-GOB-DIGITAL-EXE-EMISIONES-0001/` (spec y código; sin resultados) · `tests/test_piloto3_guardias.py` · la celda-D nueva · cascada. No toca: ningún payload · `marginales_reproduccion.py` (se importa) · marcador salvo re-derivar · CALC existentes · yaml del árbitro. «Si te encuentras escribiendo fuera de esta lista, PARA.»

LO QUE NO HACE

No abre ENCIG 2025 · no emite · no adjudica · no recalcula δ. SUCESOR: brief a Astra para COMMIT-2/3 en caja, que dirección escribe al fusionar esto.

MÓDULO DE AUDITORÍA (afirma sobre México: aplica completo)

`edad × escolaridad` en gobierno digital es, antes que actitud hacia el Estado, brecha de acceso, conectividad y alfabetización digital por cohorte; la celda con interacción en ambas olas es `60+ × hasta primaria`: es donde C2, que supone no-interacción, más se equivocaría, y donde un lector aplicado más daño haría con una cifra centrada. El universo son trámites realizados: excluye a quien no tuvo contacto con el Estado — más rural, más informal; límite declarado. La celda sin soporte (jóvenes con primaria o menos) es escasa porque la cobertura escolar subió: su ausencia es estructura, no dato faltante. Sin región ni condición indígena en la rejilla: fuera por diseño. Clase (a). Escala logit para δ, proporción para p; nada cruza unidad. Peligroso leído simplista: "+11 pp" como "los mexicanos ya confían en el gobierno digital".
