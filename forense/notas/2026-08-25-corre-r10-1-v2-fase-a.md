# `ACTO CORRE-R10.1-v2` · Fase A — comando por comando

25 de agosto de 2026 · entorno **UBUNTU** · modelo Opus · `ADR-195` · caja `/home/pc0/mm-corre-r10-1`

---

## 0 · Por qué este lanzamiento es el `v2`

El `v1` (sha `59717ebf`) fijó **NUBE** por error de dirección: el entorno venía heredado del pack
del transfer, no derivado de la spec. Su propio candado `F0` lo atrapó y el ejecutor **paró sin
escribir nada**. El `v2` obedece a `§7` de la spec, que declara verbatim *"Entorno declarado para
la corrida: **UBUNTU** (como v1.0, no NUBE)"*.

La cabecera de la spec (línea 5) todavía dice `ENTORNO NUBE` — es la firma del acto que la
**redactó**, no de la corrida. `FP-128` lo corrobora por su lado: sellar el texto *"habilita el
acto de corrida en **UBUNTU**"*. Dirección leyó bien.

## 0-bis · El `v2` también paró una vez, en `F0(3)`, y por qué

La primera redacción del `v2` traía las dos ranuras **solo dentro de la definición de la propia
compuerta**, corridas en un párrafo y con el hueco `[nombre o rol]` sin llenar. Es autocaptura
verbatim: la misma clase que `ACTO CAJA-RESIDUOS` (18/ago) y `ACTO FP63-CIERRA` (19/ago,
`ADR-122`) ya adjudicaron dos veces sobre la compuerta de `mm-purga.git`, donde la fila registra
que *"el lanzamiento no trae la cadena como autorizacion, solo dentro de la definicion de la
propia compuerta"*.

La lectura permisiva se refuta sola: si el texto de la plantilla contara como ranura llena,
**RANURA 2 también estaría siempre presente** y su carácter opcional —*"sin esta línea, la Fase B
PROPONE y para"*— no significaría nada. El acto paró en `F0(3)` sin tocar el árbol, y sólo corrió
cuando mesa devolvió las dos ranuras como líneas propias y llenas.

---

## 1 · ARRANQUE

| paso | resultado |
|---|---|
| **1 · repo** | clon existente; caja nueva `/home/pc0/mm-corre-r10-1`, rama `acto/corre-r10-1-v2` sobre `origin/main` |
| **2 · SHA** | `ba0a7e4` **es** ancestro de `origin/main` pero **desactualizado en 8 commits** (`PR #362`→`PR #364`). Refrescado y re-derivado; ninguno toca el perímetro. No es `PARO`, se reporta |
| **3 · `data/raw`** | enlazado en la caja a `/home/pc0/mm-corpus/raw` (no se hereda al crear worktree, gitignorado); **321 entradas, 9.6 GB** |
| **4 · entorno (`A.2`, tres partes)** | `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE` = `sin_variable` · sonda **cruda** `curl -s -r 0-11` a `https://www.inegi.org.mx/` → `http=206 bytes=12` en 0.446 s, bytes reales `0d0a 3c21 646f 6374 7970 6520` = `\r\n<!doctype ` (nunca `curl -I`) · `ls data/raw/` no vacío |
| **5 · espejo** | cero cifras del espejo; todo re-derivado por comando |

## 2 · `F0`, compuerta por compuerta

**(1) `A.8` en fresco — PASA.** `R10.1` **ausente** del bloque `## Registro de veredictos
archivados` (línea 1122 de `hitoD-preregistro-v2_0.md`): `command grep 'R10\.1'` → exit 1;
**control positivo** `R8.3` → 1 presente. `README:36` re-derivado del Registro real: **21 líneas
canónicas**, desglose `12 D · 2 B · 4 A · 2 E · 1 C`, que son **20 corridas** porque `R4.3` ocupa
dos líneas (mitad A y mitad B) y colapsa a un solo `D` → **20 de 27 · 11D·2B·4A·2E·1C**, idéntico
a lo que `README:36` imprime.

**(2) `FP-128`/`FP-108` — PASA**, verificado en `forense/firmas-pendientes.tsv`, no en la
autoatestación de la cabecera de la spec. `FP-108` FIRMADA (`SELLA-AGO24-D`, mesa *"Si a las tres
cosas."*), `FP-128` FIRMADA (mesa 25/ago, L2, *"¿Sellas la spec v2 del experimento de la fila de
espera? → sí"*), ejecutada por `SELLA-AGO25-F`/`ADR-166`.

**(3) RANURA 1 — PASA** en el relanzamiento: designa a **Jonatan Guadarrama**, con su declaración
de no haber leído los tres documentos `v1`. RANURA 2 también presente: autoriza archivar la letra
en el mismo acto, con sus reservas.

**(4) material — PASA.** `forense/notas/2026-08-20-r10-1-rechazo-poder-salida.txt`, sha256
`c5d39e81…9ca59d`, **byte-idéntico** entre disco y `origin/main`. Universo Brasdefer
(`actos_de_habla.html`, bloque México, 197 líneas), **12 transcripciones**, **6 por brazo**.
`tests/hitod_r10_1_rechazo_poder.py` presente.

**Nota de método sobre los negativos.** El primer conteo que corrí, `command grep -c '^Rechazo
[0-9]*:'`, dio **0** — pero mi control dio 0 también, así que el negativo **no era interpretable**
y lo descarté. Con control positivo real (`command grep -c 'Rechazo'` → **39**, exit 0) quedó
establecido que `grep` funcionaba y que el patrón con ancla genuinamente no existe: el material
indenta las cabeceras. Un negativo sin control positivo que discrimine no es un negativo.

---

## 3 · `A1` — codificación 1, sellada por hash

Las 12 unidades codificadas EXACTO al esquema `§2` (`2.1`–`2.5`) de la spec SELLADA. Se commiteó
el **`sha256`**, no el texto en claro — opción que el encargo autoriza (*"sus 12 códigos (o su
sha256)"*).

**Por qué el hash y no el claro.** El propósito de `A1` es **comprometer**, no **publicar**. El
segundo codificador es una persona con acceso plausible a este repositorio; publicar los códigos
en claro durante el interludio abriría un canal de contaminación que anularía el `κ` — es decir,
anularía justo la pieza que el acto existe para construir. El hash compromete con la misma fuerza
y sin ese canal.

| objeto | `sha256` |
|---|---|
| tabla canónica de 12 códigos | `dae1048de7e4a04ac8ece168dc3d8f0f6fde64e28675069e9716c4fced33ec90` |
| razonamiento unidad por unidad | `c380f81e1b8705d80c68b9a08085e715c5ff1dc00cc75ac7226661979b2605d2` |
| paquete del codificador 2 | `15e1a594473dc8c49f882fe4e879fde678507a6771332bd1b96c4c45f1fda1bd` |

Texto en claro: `/home/pc0/mm-corre-r10-1-SELLO/`, **fuera del repositorio**. Riesgo residual
declarado: si se pierde, la recuperación honesta **no** es re-codificar —eso sería codificar ya
sabiendo lo del otro, el defecto exacto que `§3.2` documenta— sino re-correr la Fase A completa
con una codificación 1 nueva, sellada de nuevo.

**Asimetría de ceguera, declarada.** El codificador 2 es ciego; el **codificador 1 no lo es**: el
material trae en el mismo archivo la codificación `v1` y su resultado, y no hay forma de leer las
transcripciones sin verlos. Es la asimetría que `§3.2` describe y la razón de ser de la Pieza 2.
Viaja pegada al `κ` que se reporte.

**Cuatro casos limítrofes** quedaron nombrados por escrito **antes** de ver la codificación ajena,
como candidatos a discrepancia. No se dicen aquí porque decirlos revelaría códigos.

---

## 4 · `A2` — el paquete ciego

`forense/hitoD-R10_1-paquete-codificador2.md`, **derivado por script** del material y de la spec,
no retecleado. Las tres piezas que `§3.1` permite y nada más.

**Verificación de fidelidad (comando, no palabra):** 12 unidades, **158 líneas de diálogo**, las
158 verificadas presentes **verbatim** en el material (0 faltantes); las **5** reglas `2.1`–`2.5`
verificadas **verbatim** contra la spec (0 divergentes); comparador con control positivo.

**Barrido de contaminación, todo en cero:** `64%` · `Brasdefer` · `COMMIT B` · `14 cadenas` ·
`R10.1` · `face` · `simpat` · `asimetr` · `15 pp` · `umbral` · `IC95` · `veredicto` ·
`codificado INDIRECTO` · `codificado DIRECTO` · `kappa` · `κ` · `preregistro` · `Hito D` ·
`hipótesis` · `falsador`. **Controles positivos** (deben estar y están): `2.1 · Unidad de
análisis`, `2.5 · Regla`, `U01`, `U12`, `código de contexto`, 12 encabezados `### U`.

Tres hits inspeccionados uno por uno y declarados benignos: **`Tlaxcala`** (1) y **`poder`** (4)
caen dentro de transcripciones —el jefe nombrando el restaurante, y `poder` como verbo en *"no voy
a poder asistir"*—, contenido verbatim del corpus que se queda; **`R10_1`** (1) es la cita de
archivo dentro del texto verbatim de la regla `2.3`, un nombre que el codificador ya conoce por su
propia declaración.

### Las dos desviaciones declaradas

Las dos nacen de una **colisión real** entre dos órdenes del encargo: *"el esquema de §2 copiado
verbatim"* y *"prohibido incluir hipótesis / cualquier resultado o conteo previo"*.

**(a) Se OMITE el preámbulo *"Qué reemplaza"* de `§2`.** No es parte del esquema —el esquema son
`2.1`–`2.5`— y carga tres cosas prohibidas a la vez: el instrumento previo (la lista cerrada de 14
cadenas de COMMIT B), una **cifra previa** (el 64% de Tlaxcala) y el **ancla que apunta a la
hipótesis** bajo prueba. Incluirlo habría violado la prohibición; omitirlo no toca ninguna regla.

**(b) Se CONSERVA la mención de `Rechazo 10` dentro de la regla `2.5`.** Ahí sí es texto de la
regla y enseña el criterio con su cita de cierre; recortarla habría mutilado la regla. Pero la
consecuencia es real y se declara: **esa unidad llega pre-decidida a los dos codificadores** y su
acuerdo no es evidencia de acuerdo.

**Instrucción pre-registrada para la Fase B**, escrita hoy sin conocer ninguna codificación ajena
ni ninguna tasa: `κ` se reportará **dos veces** — sobre las **12** (la cifra del **gate**, tal como
`§3.4` la define) y sobre las **11** restantes (**diagnóstico**, no gate). Si las dos caen del
mismo lado de `0.60`, la distinción no cambia nada; si caen de lados distintos, **manda la de 12
por spec** y la de 11 viaja escrita como reserva del veredicto.

**También se eliminó** de cada encabezado la etiqueta `| codificado DIRECTO/INDIRECTO` que el
material arrastra: resultado previo, prohibido de raíz.

**Identificadores propios `U01`–`U12`.** La numeración del corpus **colisiona**: el escenario
`Rechazos 3-4` rotula sus dos transcripciones como `Rechazo 1` y `Rechazo 2`, exactamente los
rótulos que usa el escenario `Rechazos 1-2`. Sin identificador propio, la tabla del codificador 2
sería ambigua al cruzarla.

---

## 5 · Lo que este acto NO hizo

Ninguna tasa. Ningún `κ`. Ningún IC. Ninguna fila propuesta ni archivada. No se tocaron los tres
documentos `v1` de `R10.1` (append-only, `ADR-40`), ni la spec, ni PRESEEA, ni el bloque de
veredictos del preregistro. **`R10.1` sigue sin letra y el Hito D sigue en 20 de 27.**

**CONTADOR: cero**, como el encargo lo fija para la Fase A.

## 6 · El techo, escrito antes de correr

`§5.1`: con `n=6` por brazo el IC95% Wald de la diferencia mide **±56.58 pp**, y el ancho es
función de `n`, no de qué tan bien se clasifique cada turno. **Ninguna recodificación puede
cerrarlo bajo 15 pp.** El desenlace realista de la Fase B es fila `C` (rama 4: el IC cruza el
umbral y el instrumento es universitario/elicitado) o `D` de instrumento si `κ<0.60`. `§5.2` ya
declaró verbatim que caer ahí **no es falla de la spec v2.0**: es la confirmación de lo predicho.

## 7 · Interludio — qué necesita mesa hacer

Entregar `forense/hitoD-R10_1-paquete-codificador2.md` a **Jonatan Guadarrama**, y **sólo eso**.
Cuesta una sesión (12 unidades, `§3.2`). Devuelve su tabla de 12 códigos tal cual, sin discusión
previa. Con esa tabla adjunta al relanzamiento y la línea *"Códigos del codificador 2 adjuntos,
transcritos sin edición"*, corre la Fase B.

**Advertencia operativa:** no darle acceso al repositorio ni a
`/home/pc0/mm-corre-r10-1-SELLO/` hasta que su tabla esté entregada. El paquete es auto-contenido
a propósito.

`FP-161` queda `FIRMADA` con `ejecutada_en` **vacía a propósito**, para que `T22` imprima la Fase B
pendiente en cada corrida y el interludio no caiga en el limbo silencioso.

---

## 8 · Suite `--baseline`, antes y después (jamás `--freeze`)

Las dos corridas con `--baseline`. El **antes** se midió en una caja desechable parada en
`origin/main` (`9c25f28`), no por aritmética.

| | FAIL | WARN | línea base |
|---|---|---|---|
| **antes** (`origin/main`) | 19 | 130 | VERDE |
| **después** (esta rama) | **19** | **131** | **VERDE** |

**El núcleo de FAIL no se movió ni un punto.** El único WARN nuevo es `T22` imprimiendo `FP-161`,
la fila que sigue el interludio: nace `FIRMADA` con `ejecutada_en` vacía a propósito, así que **no
es regresión, es `A.12` funcionando** — exactamente el mismo patrón que `FP-63`/`FP-137`.

### Censo de lo que este acto llegó a introducir y cerró dentro de sí mismo

La primera corrida dio **27 FAIL · 132 WARN** con **6 entradas nuevas** frente a la línea base.
Las seis eran propias y se cerraron una por una, ninguna silenciada:

| test | qué era | cómo se cerró |
|---|---|---|
| `T03` ×1 | la ficha citaba `/home/pc0/mm-corre-r10-1-SELLO/codificacion1-PLANTEXTO.md`, que **a propósito** no vive en el repo | se escribe con su ruta absoluta; el regex de `T03` no lleva `/` en su clase de caracteres, así que deja de leerse como archivo del árbol — y de paso la ficha dice dónde está de verdad |
| `T15` ×2 | recifrado `193 → 194 ADR`: la cabecera viva quedó vieja, y la cascada de `ADR-193` quedó citando un conteo correcto **en su momento** | cabecera actualizada a `194`; la cita de la cascada se marca `{cita-historica}` **dentro** de las negritas, pegada a `ADR`, que es lo que el regex de `T15` tolera |
| `T22` ×2 | nota y encargo traían marcador de ranura y ninguna fila los citaba en `dónde` | `FP-161` los cita: es la fila que de verdad los gobierna, no una exención |
| `T16` ×2→×4 | `estado-programa` declaraba `130 WARN` y la corrida real daba `131` | se declara la cifra nueva **medida**; las dos citas del recifrado anterior pasan a `{cita-historica}` **fuera** de las negritas, que es lo que `T16` tolera |

El marcador va **dentro** de las negritas para `T15` y **fuera** para `T16`: cada test empareja un
tramo distinto, y generalizar la regla de uno al otro deja el `FAIL` vivo.

Ningún `--freeze`. La cifra congelada de `tests/baseline.json` no se tocó.

**Séptima entrada, cerrada también.** Al escribir el censo de arriba, la propia tabla que **narra**
la corrección de `T03` volvió a **crear** el defecto: citar el nombre del archivo para explicar por
qué se había corregido lo re-convirtió en referencia colgante, y con él regresaron los dos `T16`
(la corrida real subía a 132 WARN). Un test no distingue mención de uso. Se cerró escribiendo
también ahí la ruta absoluta.

**Cifra final verificada, tras cerrar las siete: `19 FAIL · 131 WARN`, `LÍNEA BASE: VERDE`.**

---

## 9 · Fusión con `main`: renumeración y una reparación que no era nuestra

`PR #365` (`ACTO R34-ENSAFI-CENSA`) fusionó mientras este acto estaba en revisión, y **tomó
`ADR-194`** — él mismo se había renumerado de `193` a `194` por la misma regla. Este acto es el
segundo en fusionar, así que **renumera**: `ADR-194` → **`ADR-195`**, y con él la cabecera de
`gobernanza`, los dos sitios de `estado-programa` y las citas propias en nota y encargo.
Contadores re-derivados **por comando**, no por aritmética: `195` ADR únicos, sin huecos.

La contribución ajena se conserva **íntegra**: no se editó hacia atrás ni una línea de
`ADR-194`. Lo único que se le añadió fue la marca `{cita-historica}` sobre su propia cascada
—que citaba `193 → 194 ADR`, correcto en su momento y ahora histórico—, exactamente el mismo
trato que este acto le dio antes a la cascada de `ADR-193`.

### La reparación: `main` traía la línea `L0` duplicada entera

Al resolver el conflicto se encontró que la resolución de `PR #365` había **duplicado la cadena
histórica completa** de la línea `**L0 · Gobierno**` de `estado-programa`. Medido, no supuesto:

| | caracteres de la línea `L0` | pares de recifrado duplicados |
|---|---|---|
| `9c25f28` (antes de `PR #365`, **control**) | 49 220 | **0** |
| `origin/main` (`e3bbaab`, tras `PR #365`) | 98 277 | **63** |
| esta rama, reconstruida | 51 405 | **0** |

La línea se reconstruyó desde la cadena buena de `9c25f28`, insertando delante el recifrado
`194→195` de este acto y **conservando íntegro** el `193→194` ajeno. Verificado tras
reconstruir: 66 recifrados, **0 duplicados**, cadena continua `194→195 → 193→194 → 192→193`,
`ACTO R34-ENSAFI-CENSA` presente, `SPEC-EXPCOMP-BBIS` presente.

**No es hallazgo de este acto ni culpa de nadie que se nombre**: es el modo de falla conocido de
resolver a mano un conflicto sobre una línea de 50 000 caracteres. Se repara aquí porque es la
misma línea que este acto tenía que editar de todas formas, y propagarla duplicada a sabiendas
habría sido peor que arreglarla. Queda declarado para que mesa lo sepa y no lo descubra después
como si fuera nuestro.

### Suite tras la fusión

`19 FAIL · 131 WARN`, `LÍNEA BASE: VERDE` — **idéntica a la corrida previa a la fusión**. Las dos
entradas que el merge llegó a levantar (`T15` por la cita de conteo ajena ya histórica, `T16`
arrastrado por ese `FAIL` extra) se cerraron dentro de la propia resolución.
