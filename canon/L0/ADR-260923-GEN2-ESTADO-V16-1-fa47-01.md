**ADR-260923-GEN2-ESTADO-V16-1-fa47-01**, `ACTO GEN2-ESTADO-V16-1`, 23/sep/2026, NUBE (`cloud_default`, red `DENEGADA-POR-POLITICA`, corpus no montado, cero microdato), Sonnet 5, un agente de sólo lectura ayudó a localizar comandos y citas, verificados de nuevo por el ejecutor antes de citarlos, MODO ABIERTO, COMPUERTA: ninguna. Emite `canon/estado-programa-v1_16.md`: cabecera nueva (era 18–23/sep, régimen operativo nuevo: canal por PR, sello externo, auto-merge) + §0–§14 heredadas verbatim de `v1_15` (`diff` vacío sobre las líneas 17–789) + §15 nueva (status íntegro de `corrida0.py`, cierre de la etapa de retadores — 18 celdas-D con dictamen, cero `VENCE` puro —, régimen operativo, misiones de Astra, cobertura de dominios, frente público, 11 FP abiertas, decisiones de mesa del 23/sep, 32 comentarios técnicos reproducibles) + §16 nueva ("Lo que este estado no puede afirmar": `CONTADORES-2`, `FIRMAS-14`, `AUDITORIA-POST-HOC-ASTRA-1` y tres unidades de ASTRA-5 en vuelo). `canon/estado-programa-v1_15.md` **NO se retira** esta vez — el encargo la exige intacta y así queda, sin `git diff`; no aplica T01. Puntero técnico actualizado por grep en 7 sitios (`tools/cierre_acto.py`, `tests/check.py` — `RUTA_ESTADO_PROGRAMA` + comentarios + entradas nuevas en `_T22`/`_T25` **junto a** las de `v1_15`, no en su lugar —, `tools/l0_vista.py`, `.claude/commands/acto.md`, `.claude/commands/revisa.md`, `tests/test_cierre_acto.py`, `tests/test_tuberia_ids_union.py`). Nace `tests/test_estado_derivado.py` (P3): re-ejecuta cada comentario `<!-- comando: ... -->` del `estado-programa-v*.md` más nuevo y falla si no reproduce; 32/32 VERDE. Hallazgo declarado: el `[SUPUESTO]` del encargo de que `adoptados` reflejaría "el valor que `CONTADORES-2` deje" no se sostuvo — esa rama sólo tiene su 0-bis, sin PR; `adoptados=72` es el heredado de `CONTADORES-1`, declarado así en vez de forzar la atribución. `celdas_validadas` sin cambio (219, ya reflejaba `CONTADORES-1`); `cuenta_gen2 = NO-APLICA`; cero microdato, cero contadores del programa movidos por este acto. Suite `--rapido` VERDE, 0 FAIL (tras escribir esta entrada antes que `T15` la reclamara, mismo defecto que documentó `ADR-260922-GEN2-ESTADO-V15-1-7e23-01`).
