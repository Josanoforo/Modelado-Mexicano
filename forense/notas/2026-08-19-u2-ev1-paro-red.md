# Nota · ACTO U2-EV1 — intento de lanzamiento, PARO por entorno

**Fecha:** 2026-08-19 · **Rama:** `claude/u2-ev1-external-validation-szb9wl` · **PR:** ninguno abierto por este acto.
**Base:** `origin/main = 62e60e9` (fusión de `PR #287`, `ACTO COEF-UNIVERSO`). **Encargo:** `forense/encargos/2026-08-19-U2-EV1.md`. **Ley:** `PROPUESTA-remediacion-brecha-documental.md:16` (§2), citada por ruta y línea, no re-derivada.

---

## 1 · Firma de entorno · A.2

- `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE` → `cloud_default` (NUBE, según lo asignado por el lanzador).
- `grep` verificado ANTES de cualquier NO-ENCONTRADO, por el hallazgo de `ACTO COEF-UNIVERSO` (`grep` como envoltura de `ugrep -I` en la caja Ubuntu, que descarta en silencio bytes no-UTF8): `type grep` → `/usr/bin/grep` (binario real, no función de shell); `grep --version` → GNU grep 3.11. **No aplica el defecto de COEF-UNIVERSO en esta caja.** Todo NO-ENCONTRADO de esta nota se declara con este mecanismo verificado.
- `data/raw` → **ausente, sin symlink** (`ls: cannot access 'data/raw': No such file or directory`). Corpus no montado en esta sesión.
- Red: `curl -s -o /dev/null -w "%{http_code}" --max-time 10 https://www.inegi.org.mx/` → **`000`, exit 56** (fallo de conexión). Confirmado por segunda vía independiente, `WebFetch` (que pasa por el proxy de egreso del entorno, ruta distinta de `curl`): `{"error_type":"EGRESS_BLOCKED","domain":"www.inegi.org.mx","message":"Access to www.inegi.org.mx is blocked by the network egress proxy."}` — bloqueo explícito de política de red, no timeout ambiguo.

## 2 · Verificación del gate — `B2-SEMANTICO` fusionado

Premisa del lanzador: gate cumplido, `PR #268`. Re-derivada, no heredada:

```text
git log --oneline main | grep -i b2-semantico   -> 12 commits ACTO B2-SEMANTICO
git log --oneline --all --grep="268"            -> e563e5d Merge pull request #268 from Josanoforo/b2-semantico
grep -n "ADR-108\|ADR-109" canon/gobernanza-v1_15.md  -> presentes, texto sellado
ls data/cableado-universo-v1_0.tsv               -> existe (producto de C6)
```

**Gate SATISFECHO**, confirmado por comando: `PR #268` fusionado (`e563e5d`), `ADR-108`/`ADR-109` en canon, `data/cableado-universo-v1_0.tsv` producido.

## 3 · Deltas del lanzador, re-derivados contra el árbol (ninguno heredado del texto)

| delta declarado | re-derivación | veredicto |
|---|---|---|
| `main` hoy `62e60e9` | `git log -1 --format=%H` → `62e60e92181001050b98147d75bf08afa011567e` | **coincide** |
| ADR máx 121 | `grep -o "ADR-1[0-9][0-9]" canon/gobernanza-v1_15.md \| sort -u \| tail -1` → `ADR-121` | **coincide** |
| tablero FP máx 66 | `tail -1 forense/firmas-pendientes.tsv` → fila `FP-66` | **coincide** — este acto abre `FP-67` |
| suite recifrada 21 FAIL · 121 WARN, LÍNEA BASE VERDE | `python3 tests/check.py --baseline` → `21 FAIL · 121 WARN`, `LÍNEA BASE: VERDE` contra `tests/baseline.json` | **coincide exacto** |

Ningún delta se hereda del texto del encargo: los cuatro se re-corrieron por comando en esta sesión, contra el árbol de arranque de este acto (`62e60e9`), antes de escribir cualquier producto.

## 4 · Las tres preguntas abiertas de `FP-32`, resueltas al ejecutar

**(1) Si se lanza.** Resuelto por la firma de mesa (`ADR-111(e)`, verbatim en `FP-32`): sí, gateado. El gate está satisfecho (§2). Se lanza.

**(3) Si la fila ENASIC sigue siendo obligatoria tras `BARRIDO-2`.** Verificado por comando, no asumido: `ENASIC` no aparece en `data/cableado-universo-v1_0.tsv` ni en `data/coef-universo-v1_0.tsv` (0 coincidencias en los dos; `BARRIDO-2` cableó relaciones semánticas distintas). Pero `canon/gobernanza-v1_15.md` (`ADR-67(b)`, `:874`) fija `ENASIC P7_12_7` como la **θ actitudinal real** de `familismo_obligacion` — el primer y único resultado sellado del programa hasta hoy (`U1/E4b′`, θ=0.6933, IC95%[0.6725,0.7140], n=5,579, vía `produce.py`/Taylor). `BARRIDO-2` no tocó esa producción (dominio distinto: BARRIDO-2 es inventario de activos INEGI internos vía cableado semántico; la producción de `familismo_obligacion` corre por el motor formal, `especificaciones-produccion.json`/`produce.py`). **Veredicto: la fila ENASIC sigue obligatoria** — es la única producción con un EE propio calculado hoy contra la que el cruce oficial-vs-propio tiene algo que cruzar. Sin ella, `U2/EV-1` no tendría objeto.

**(2) Con qué perímetro.** Se pre-registra en §5 — no se ejecuta en esta sesión por el PARO de §1.

## 5 · Pre-registro del cruce (antes de mirar el resultado, per §4 del encargo madre)

Con el gate satisfecho y la fila ENASIC confirmada obligatoria, el diseño del cruce se fija AHORA, con `sha256` de cada insumo declarado en cuanto se adquiera, para que la ejecución material (en el entorno que sí tenga acceso a INEGI) no pueda mover el diseño después de ver el número:

- **Objeto único del cruce (perímetro derivado, §4 pregunta 2):** `familismo_obligacion` vía `ENASIC 2021`, reactivo `P7_12_7`, θ=0.6933, EE derivable de IC95%[0.6725,0.7140] con n=5,579 (`produce.py`/Taylor, citado en `forense/encargos/2026-08-12-veredicto-pr185-mapeo-universo-map-a.md:33`). Es el ÚNICO objeto: ningún otro coeficiente del programa tiene hoy un EE propio calculado (`radio_confianza` sigue `NO_LISTA_DECISION_HUMANA_PENDIENTE`, `gobernanza:964`; `coeficientes medidos: 0/15` fuera de éste, `COEF-UNIVERSO §F`).
- **Insumo oficial a adquirir:** indicador de precisión (CV/EE/IC) de INEGI para `ENASIC 2021`, reactivo `P7_12_7`, y el "validador oficial" citado por `veredicto-pr185` (`922/download/29534`, ruta dentro de `inegi.org.mx`, sin resolver aquí — bloqueado, §1). `sha256` del archivo se declara al momento de la descarga, no antes.
- **Método del cruce, fijado ahora:** comparar el EE propio (Taylor, `produce.py`) contra el EE oficial publicado para la misma celda/reactivo del mismo diseño complejo (mismo `FAC_ELE`/`EST_DIS`/`UPM_DIS` que declara el manifiesto de ENASIC); si el oficial reporta CV en vez de EE, convertir por `EE = CV × estimador` y declarar la conversión con la fórmula a la vista. Criterio de discrepancia material: razón `|EE_propio − EE_oficial| / EE_oficial > 0.15` (mismo umbral que `ADR-80`/`benchmark-enlace-invarianza` usa para invarianza — no se inventa uno nuevo).
- **Qué mueve el resultado:** si el EE propio no cae dentro del rango de discrepancia, es la primera evidencia externa de un defecto en el cálculo de precisión del motor (`produce.py`), y bloquea el uso de `familismo_obligacion` como `LISTA_PARA_USO_MODELO` hasta remediarse. Si cae dentro, es la primera validación externa positiva del programa, tal como nombra §4 del encargo madre.

Este diseño queda pre-registrado con `sha256` de esta nota como ancla temporal (se declara al commitear); ningún resultado se ha mirado.

## 6 · PARO — por qué no se ejecuta la descarga aquí

`§1` establece, por dos vías independientes (`curl` directo y `WebFetch` vía proxy), que esta sesión NUBE **no tiene salida de red hacia `inegi.org.mx`** — bloqueo explícito de política de egreso, no error transitorio. Y `data/raw` no está montado: tampoco hay corpus local con el indicador de precisión ya descargado (verificado: `data/manifiesto.yaml` — 0 coincidencias de `precision`; `find` en el árbol — 0 coincidencias de `precision` en nombres de archivo).

Esto es un bloqueo material de entorno, no de diseño ni de dato: el objeto del cruce está identificado (§5), el método está fijado (§5), y lo único que falta es el archivo oficial mismo. Regla del bloqueo (`AGENTS.md` §Bloqueos): verificado con dos vías independientes, no hay alternativa directa dentro de esta caja, se registra aquí con receta concreta (§7), y se continúa por lo que sí es alcanzable — el pre-registro de §5, que es precisamente lo que impide que un acto sucesor con red mire el resultado antes de fijar el diseño.

**No se fabrica ni se aproxima ningún número de EE/CV/IC oficial.** Sustituir el archivo INEGI por una estimación o un valor de un reporte de terceros incumpliría literalmente §4 del encargo madre ("el pre-registro del cruce va antes de mirar el resultado, con sha256 de cada insumo oficial") y `A-bis` regla 3 de `COEF-UNIVERSO` (no comparar entre escalas ni fuentes sin enlace declarado).

## 7 · Receta concreta para el acto sucesor

1. Ejecutar en un entorno con salida de red confirmada hacia `inegi.org.mx` (la caja Ubuntu, según el patrón ya usado por `COEF-UNIVERSO` y `ACTO B2-SEMANTICO`, tiene salida — `curl` no aislado por `unshare` fuera de la fase de curación; o una sesión NUBE cuya política de egreso incluya el dominio).
2. Descargar el indicador de precisión de `ENASIC 2021` para `P7_12_7` (buscar en el catálogo de indicadores de precisión de encuestas de INEGI, o el enlace `922/download/29534` si sigue vigente); registrar `sha256` inmediatamente al bajarlo.
3. Aplicar el método de §5 tal cual — no rediseñarlo al ver el archivo.
4. Si el archivo no trae la celda `P7_12_7` exacta, declarar `NO-ENCONTRADO` con el universo de búsqueda a la vista (catálogo completo revisado, término buscado, fecha) — no sustituir con la celda más cercana sin decirlo.

## 8 · Cascada y cierre de este acto

**No se abre ADR** — este acto no toma ninguna decisión de datos, método o alcance que exija sello de gobernanza; fija un diseño pre-registrado (reversible, sin efecto hasta que corra) y documenta un bloqueo de entorno. `FP-32` permanece con su primera pregunta resuelta (SÍ, lanzado); la pregunta 3 queda resuelta en §4; la pregunta 2 queda pre-registrada en §5, pendiente de ejecución material. Se abre `FP-67` (tablero) para que mesa decida el entorno de ejecución de la descarga — ver `forense/firmas-pendientes.tsv`.

`python3 tests/check.py --baseline` al cierre: `21 FAIL · 122 WARN`, LÍNEA BASE VERDE, sin `--freeze`. El único movimiento frente al arranque de este acto (§3, `21 FAIL · 121 WARN`) es **+1 WARN** de `T22` por la fila nueva `FP-67` (`ABIERTA`) — mecanismo ya documentado en `canon/estado-programa-v1_10.md`. La nota de este acto se nombró `2026-08-19-u2-ev1-paro-red.md`, con un sufijo temático deliberado, para no colisionar por nombre normalizado con `forense/encargos/2026-08-19-U2-EV1.md` bajo `T02` — el patrón que `convencion.md` documenta como esperado en cinco actos previos; este lo evita en vez de heredarlo, así que `FAIL` no se mueve.

**Contadores de medición sobre México: cero.** Este acto no corre ninguna estimación nueva; produce el diseño pre-registrado del primer cruce externo y documenta el bloqueo que le impide ejecutarse hoy.

## 9 · Concurrencia declarada (§ colisión esperada)

Lanzador declaró en paralelo: `DOC-BACKFILL` (nube) y `FICHA-R51-D3` (caja). Colisión de ADR/fila esperada y no tratada como PARO — este acto no abrió ningún ADR (§8) y re-derivará `FP-67` contra el tablero real al fusionar, no lo hereda de este texto.
