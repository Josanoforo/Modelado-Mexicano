ESTADO: GATEADO
ENTORNO: CAJA (UBUNTU)
ENCOLADO: 2026-09-08 · ACTO GEN2-T7-CIERRE encola "E5-0 · ACTO GEN2-E5-0 · SPECS EJECUTABLES", texto de `ENCARGOS-GEN2-v1_3-readiness-primero-2026-09-07.md` (adjunto por el operador). Nuevo en la cola — no reemplaza ningún archivo previo.
BITACORA:
- 2026-09-08 · GATEADO. Nace de `ENCARGOS-GEN2-v1_3-readiness-primero-2026-09-07.md`, que reordena la automatización antes de los cálculos: la readiness (E3.1) debe fusionarse antes de que E5-0 pueda resolver códigos/ponderadores contra el schema endurecido de spec.yaml. COMPUERTA: "E3.1 fusionado" — hoy NO lo está (E3.1 sigue en cola, `LISTO-NUBE`, sin PR ni merge todavía) — así que este encargo queda `GATEADO`, no `LISTO`.

──── CUERPO VERBATIM DEL ENCARGO (A.3) · el despachador NO lo edita ────

## E5-0 · ACTO GEN2-E5-0 · SPECS EJECUTABLES — resolver códigos y ponderadores abriendo solo metadato, y congelar `spec.yaml`

Cabecera: **UBUNTU (caja)**, worktree nuevo desde `origin/main` · **Opus** · COMPUERTA por producto: `git show origin/main:tests/test_corrida0.py | grep -c "T-VERIFY-CONTEXTO\|T-OUTPUTS-EXACTOS\|T-MANIFIESTO-RUTA-AL-MEDIDOR"` → 3 (E3.1 fusionado); `python3 tools/limpia_arbol.py --reporta` sin árboles fuera de política (E4 fusionado); gates de spec en `main`: S12 `870522a3…`, S13 `c41235b8…`, S6 v1.2 `c1cd3b63…` (íntegros del sidecar). NO se lanza en NUBE: abre codebooks y metadatos del corpus.
Principio (readiness §26): «spec conceptual pre-registrada → abrir codebook solamente → resolver códigos/ponderador → congelar `spec.yaml` ejecutable → COMMIT-1 → abrir microdato → calcular». **Este acto termina en el COMMIT-1. No abre ningún microdato ni calcula nada.** Adivinar códigos para lograr un `preflight` VERDE está prohibido.
**CALC-0001 / S12 (CIDE-CSES 2015).** Abrir solo el codebook y los metadatos de variables del `.sav` (etiquetas y códigos; sin cargar valores) para resolver códigos exactos de `pcyc13`, `pcyc14`, `pvoto1/2/3`, el desenlace de §2 y el ponderador; si el desenlace no existe con esos códigos, `NO-CONSTRUIBLE` declarado en el yaml (outputs `null` permitidos solo para ese brazo). `spec.yaml` completo (P2), `spec-check` VERDE, `preflight` VERDE, COMMIT-1.
**CALC-0002 / S13 (LAPOP 2019/2021/2023, R10.3).** La spec declara que no existe desenlace comparable para emitir el veredicto D2-h. Regla: **no se convierte ausencia de desenlace en veredicto**. Este acto (a) confirma en los codebooks de las tres olas si existe un desenlace comparable al de la corrida 2004 (leyendo la nota de LOTE-LAPOP sobre qué desenlace usó 2004); (b) si existe, escribe **S13 v1.1** (md + sidecar, pre-registrado, antes de cualquier microdato) con ese desenlace, y `spec.yaml` contra v1.1; (c) si no existe, `spec.yaml` contra S13 v1.0 con `veredicto_D2h: NO-CONSTRUIBLE` y solo los outputs descriptivos pre-registrados. Decisión de mesa embebida: las dos rutas están autorizadas; el acto reporta cuál aplicó y por qué, con el codebook citado.
**CALC-0003 / S6 v1.2 (ENNViH 2002).** La spec ya fija ramas, tablas, ponderadores en pareja, bootstrap y sensibilidad; aquí el riesgo es técnico: `seed {aplica: true, valor, rng}`, `dependencias_materiales: [zipfile-deflate64, …]`, `ponderador` por brazo (`fac_3b` / `fac_3b_px` y `fac_3a_px` en pareja), rutas de los ocho `.dta` resueltas por P1, ventanas de `es09`/`ce01` leídas del cuestionario `ennvih1_2002_hogar_q` y citadas con página **en el yaml**, outputs con tipo/unidad (proporciones, enteros de numerador, `NO-ESTIMABLE` permitido por celda). `spec-check` y `preflight` VERDE, COMMIT-1.
Común: un COMMIT-1 por CALC, con la frase «el primer resultado que produzca este procedimiento es el que se reporta»; los tres `spec.yaml` con sidecar; nota `forense/notas/<fecha>-GEN2-E5-0-specs-ejecutables.md` con los codebooks abiertos (id de manifiesto, página) y ningún número.
Perímetro: `data/corrida0/CALC-000{1,2,3}/spec.yaml` (+ `spec.md` local que cite la spec `.md` sellada) · `forense/prereg-caja/S13-R10-3-spec-v1_1.md` + sidecar (solo en la ruta b) · `forense/notas/` (1) · `forense/firmas-pendientes.tsv` (recibo) · cascada. **No toca microdato, `tramite.yaml`, canon ni las specs S12/S6.** Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.
Contador: cero; tres CALC en estado `PRE-FLIGHT-VERDE`.
