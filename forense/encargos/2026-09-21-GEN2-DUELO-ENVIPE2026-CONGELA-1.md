# ENCARGO · ACTOS GEN2-DUELO-ENVIPE2026-CONGELA-1 y GEN2-DUELO-ENVIPE2026-COMMIT-2-3-1 · EL DUELO PROSPECTIVO NACIONAL SE CONGELA Y SE CORRE: DOS ACTOS, DOS SESIONES, UN SOLO ARCHIVO

> Este archivo lleva **dos encargos** (F3: quien congela no ejecuta). La sesión A hace solo la PARTE A. La sesión B hace solo la PARTE B, y solo cuando la PARTE A esté en `origin/main`. Las dos en **CAJA** (corpus montado; el hook debe decir CAJA; `data/raw` ausente se enlaza). NO es NUBE: allí el payload es «no visible» y el verde no prueba nada.

CABECERA COMÚN · SHA de redacción `6901853c`; re-deriva al abrir · MODO: **RÍGIDO** (spec congelada, reserva de evaluación): latitud solo logística · FP/ADR/NC: raíz de acto. **MODELO: Sonnet por mandato de mesa (presupuesto del 21/sep).** D-13 pide Opus para actos que miden; mesa lo baja a Sonnet a sabiendas. Mitigación: el procedimiento ya está congelado, tu latitud es solo logística, y toda bifurcación que no esté en LATITUD se pregunta a mesa con opciones en vez de resolverse. Si en algún paso dudas entre dos interpretaciones del procedimiento, PARA y pregunta: ése es el riesgo que mesa acepta.
**Si al fusionar `main` choca la línea L0 de `canon/estado-programa-v1_14.md`: NO conserves los dos lados; toma la de `main` y re-inserta solo tu anotación.** `canon/L0/` existe: tu anotación va ahí como fragmento.

## FIRMAS DE MESA — verbatim, no se piden otra vez
Reserva (21/sep, `decisiones.tsv` clave `reserva:envipe2026`): «ENVIPE 2026 queda RESERVADA desde hoy, entera: nacional, marginales por eje y cruces. Bajar el payload está permitido; abrirlo, derivar de él o leer sus tabulados, no, fuera del código que se congele para ello. Rige E.6.» Diseño con enmiendas A/B/C (`FP-260921-GEN2-TRAMITE-FIRMAS-4-8a1f-07`): en el repo, cítalo de ahí. D-22 ampliada (`-8a1f-05`): «Congelado exige: `preflight` VERDE sobre el commit final con main fusionado; que `_valida_outputs` acepte la salida de cada rama terminal del procedimiento, incluida la de celda rara, sobre sintético y sobre oro; que todo id que el código pueda emitir nulo por lectura estática esté declarado; y ningún input con hash sobre un archivo vivo.» Regla del nivel cruce y `cuenta_gen2 = SI` sea cual sea el veredicto (encargo del COMMIT-1, `#968`, sellado por lanzamiento). Fecha límite del COMMIT-2: **31/oct/2026**.

## LO QUE DIRECCIÓN SABE (contra `6901853c`, sin corpus)
- `[EJECUTADO]` `envipe2026_csv` ya está en el manifiesto con sha (`#977`, bajado y hasheado sin abrir). `corrida0 preflight CALC-DUELO-ENVIPE2026-EMISIONES-0001` → **VERDE** aquí (el payload «no visible en este contexto» no bloquea); en caja debe decir COINCIDE.
- `[LEÍDO]` `CALC-DUELO-ENVIPE2026-EMISIONES-0001/spec.yaml:16`: `congelado: NO -- envipe2026_csv sin sha en el manifiesto; congelar = preflight VERDE cuando entre, sin otra edición`. `…-ADJUDICACION-0001/spec.yaml:364` trae `commit_3a` escrito: sha de `emisiones_selladas` antes de correr. 593 y 370 ids `permite_no_estimable` declarados.
- `[LEÍDO: nota de #968]` D-22 (2)–(4) ya demostrados: ensayo sobre ENVIPE 2025 sellado (`CALC-DUELO-ENSAYO-ENVIPE2025-*`), origen móvil sellado (`CALC-DUELO-ORIGEN-MOVIL-0001`), cero no finitos. Faltaba solo (1).
- `[EXISTE]` spec humana `forense/prereg-caja/DUELO-PROSPECTIVO-ENVIPE2026-spec-v1_0.md` con sidecar; B-bis §6 del diseño.

---
## PARTE A · GEN2-DUELO-ENVIPE2026-CONGELA-1 · rama `acto/gen2-duelo-envipe2026-congela-1` · una sesión
**Objetivo.** Declarar el COMMIT-1 congelado. «Hecho»: `preflight` VERDE en EMISIONES con el zip **COINCIDE** en caja, sobre el commit final con `origin/main` fusionado; ADJUDICACION solo con los bloqueos de las emisiones aún inexistentes; el campo `congelado` de los dos `spec.yaml` pasa de `NO` a `SI` con la salida cruda del preflight citada; NC «congelar cuando el payload entre» cerrada.
**Única edición autorizada:** ese campo, en los dos `spec.yaml`. Nada más — ni parámetros, ni fórmulas, ni ids, ni `.py`, ni la spec humana.
**PAROS:** a) preflight no VERDE o payload no COINCIDE → no toques nada; NC con el diagnóstico exacto · b) cualquier otra edición a los CALC · c) abrir, listar o descomprimir `envipe2026*` · d) entorno equivocado.
Cierre: nota de diez líneas; cascada.

## PARTE B · GEN2-DUELO-ENVIPE2026-COMMIT-2-3-1 · rama `acto/gen2-duelo-envipe2026-commit-2-3-1` · **otra** sesión (no la A, no la de #968)
**Objetivo.** Correr el duelo: emisiones selladas → 3a → realidad y adjudicación. Es la primera prueba prospectiva nacional del programa y la segunda de las tres pruebas de la regla de salida de θ. «Hecho»: las dos corridas selladas por `corrida0 run`, cada una en `origin` antes de la siguiente; veredicto con las palabras del B-bis (corroborada · acotada · falsador débil; si dos aplican, manda la de arriba); vista y replay (E.7); marcador re-derivado; nota de una página.
**P0.** Verifica que PARTE A está en `origin/main` (`congelado: SI`); preflight VERDE con COINCIDE; censo: nadie abrió `envipe2026*` (rastro por dos variables → PARO).
**P1 · COMMIT-2.** `python3 tools/corrida0.py run CALC-DUELO-ENVIPE2026-EMISIONES-0001`, sin argumentos que la spec no declare. Sella. Lista los nulos emitidos por familia. Commit, push, `git ls-remote`: el orden del diff es el sello.
**P2 · Compuerta sobre lo sellado.** En `resultados.json`: los puntos de todos los contendientes nacionales (K, T3; T5 y TC solo si construibles) y los marginales base son no nulos. Si alguno es nulo, PARO antes de derivar R.
**P3 · COMMIT-3a.** Exactamente lo que `spec.yaml:364` dice: el sha de `emisiones_selladas` en ADJUDICACION, commit propio, `preflight` → VERDE, push.
**P4 · COMMIT-3.** `corrida0 run CALC-DUELO-ENVIPE2026-ADJUDICACION-0001`. Reporta: por contendiente MAE en pp, cobertura del IC95 (R dentro del IC del candidato, con intervalo binomial) y punto dentro del IC de R, **sin colapsarlas**; desglose por eje; el B-bis palabra por palabra; a nivel cruce, la regla de diferencia de error medio con IC. Lo no pre-registrado va después, en `EXPLORATORIO — NO ADJUDICA`.
**P5 · Cierre.** Marcador y tablero por comando (las filas de ENVIPE 2026 dejan de estar RESERVADA); nota: qué se probó, qué salió, con qué certeza, qué NO significa (unidad delito, no persona; el promedio nacional esconde el eje; si todos yerran en el mismo sentido es cambio de nivel, no veredicto) y **una línea para la regla de θ**: ¿algún retador con interacción venció al piso en los cruces, sí o no?
**LATITUD (solo logística):** enlazar `data/raw`, instalar numpy/pandas, reintentar un paso que falló por entorno **sin** haber producido salida. Nada más.
**PAROS — lista cerrada:** a) leer `envipe2026*` fuera de `corrida0 run` de estos dos CALC, o por dos variables antes de que las emisiones estén en `origin` · b) editar spec humana, `spec.yaml` fuera de P3, o un `.py` · c) `corrida0 run` no sella → no se parcha; diagnóstico exacto y el id · d) P2 encuentra un nulo · e) repetir una corrida que ya selló · f) sesión o entorno equivocados · g) adoptar.
**Compuertas:** «P0 en verde» y «emisiones en `origin`» protegen: abrir dato.
**Perímetro:** `ejecucion.json`, `resultados.json`, `sello.*` de los dos CALC · la línea de P3 · filas propias de vista y replay · marcador/tablero por comando · nota · cascada. Ajeno: todo lo demás. Si te encuentras escribiendo fuera de esta lista, PARA.
No hace: adjudicar celdas-D de otros dominios · tocar ENVIPE 2025 reservada · correr LLM (la fila L queda `NO-ACREDITABLE` salvo captura sellada previa). Sucesor: lectura de dirección; regla de θ. `## NO-CORRIDO / RESERVAS` · `## CONSUMIDO` en cada parte.
