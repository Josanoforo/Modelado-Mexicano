# REGION-HIST · ENIF 2018, 2021, 2024 · spec humana v1.0

El primer resultado que produzca este procedimiento es el que se reporta. R1/R2 de mesa del 23/sep/2026: solo seis regiones oficiales de diseño (`REGION=1..6`) para ENIF, sin forzar estados; n≥200, varianza estimable y requisito oficial más estricto. Las fichas `enif_2018_fd.xlsx` (TModulo), `enif_2021_fd_pdf.zip` y `enif_2024_fd.xlsx` (TMODULO) documentan la misma partición oficial en seis regiones; se cotejan códigos, no etiquetas sueltas.

Conducta `informal_cualquiera`: alguna de `P5_1_1..P5_1_6=1` en la persona elegida; denominador al menos una respuesta válida 1/2 en esa batería. La codificación se hereda de `ENIF-PERSISTENCIA-IC-CALIBRADO-spec-v1_0.md` y `tools/medidor_ahorro_enif24.py`. Unidad persona, 18–70 en 2018 y 18+ en 2021/24: para historia comparable se usa **dominio 18–70** en las tres olas. La variable de edad es `EDAD` en 2018/21 y `EDAD_V` en 2024. El diseño se forma sobre el marco completo antes de aplicar el dominio de edad. Factor `FAC_PER` en 2018/24 y `FAC_ELE` en 2021; estrato `EST_DIS`, UPM `UPM_DIS`. No se confunde `P3_15_EPC` (residencia hace cinco años) con región actual.

Punto razón ponderada en cada región y ola. 1 000 réplicas compartidas del marco completo por ola, UPM dentro de estrato, PCG64(20260923); módulo compartido SHA256 `4df2c630179c194345594d959d012b7dd18d94ac93fab3f48b8f6683753dafd6`. IC95 percentil; supresión R2 como REGION-HIST-ENVIPE v1.0. El resultado `-JSON` conserva réplicas conjuntas sin identificadores ni pesos. No hay IC calibrado en estas corridas. Tres olas son elegibilidad inicial, no validación fiable automática. Todo RETROSPECTIVA; adopta NO.

## Auditoría de rigor extremo

El ahorro informal puede responder a oferta, ingreso, liquidez y acceso; una tasa regional no demuestra preferencias culturales. Las seis regiones no equivalen a entidades ni ciudades. El recorte 18–70 protege comparabilidad pero no describe a personas mayores de 70 en 2021/24. Estado no identifica clase ni condición indígena. La serie histórica no promete detectar cambios futuros.
