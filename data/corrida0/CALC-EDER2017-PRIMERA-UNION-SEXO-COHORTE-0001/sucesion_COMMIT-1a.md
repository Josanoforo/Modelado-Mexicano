# Sucesión COMMIT-1a

El primer intento quedó archivado en `primer_intento_fallido.log`. No alcanzó
ninguna fila de respuesta: la cabecera de `historiavida.csv` decodificada con
latin-1 conservó el BOM como `ï»¿folioviv`, por lo que la guarda estricta no
resolvió la llave. Se corrige antes del primer resultado con la misma regla
del padre: nombre exacto o sufijo único; dos candidatos siguen siendo error.
No cambia fuente, variable, códigos, universo, denominador, estimandos,
semilla, bootstrap ni salidas comprometidas.
