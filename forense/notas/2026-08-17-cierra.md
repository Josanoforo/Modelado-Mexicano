# ACTO CIERRA-17AGO — las dos cascadas, la auditoría acotada del integrador, la ejecución del vocabulario, y los tres arreglos que hacen que las cosas cierren

**Acto:** ACTO CIERRA-17AGO · **Encargo:** `forense/encargos/2026-08-17-CIERRA-17AGO.md` (archivado verbatim por A.3, en este mismo PR) · **Entorno:** NUBE, repo-only, sin `data/raw` · **SHA de redacción del encargo:** `d197465` · **SHA real de arranque:** `d0019a2` (main posterior al merge de `#249`) · **PR:** `#250` · **Depende de:** `ADR-92(d)/(e)` (las dos adjudicaciones selladas), `ADR-93(c)/(d)` (FP-30 sellada, defecto de `tests/check.py:286` señalado sin arreglar), `ADR-94` (este acto).

---

## §0 · ARRANQUE — las cinco líneas, crudas

**1 · REPO.** Ruta absoluta: `/home/user/Modelado-Mexicano` (clon existente, no se clonó nada nuevo). Rama de trabajo: `claude/new-session-w9nxfz` (asignada, existente).

```
$ pwd
/home/user/Modelado-Mexicano
$ git log -1 --format="%h %s" && git status
d0019a2 Merge pull request #249 from Josanoforo/claude/new-session-7avlfg
On branch claude/new-session-w9nxfz
nothing to commit, working tree clean
$ git rev-parse --is-shallow-repository
true
```

Clon superficial detectado. Conforme al Bloque ARRANQUE 1, se corrió `git fetch --unshallow` **antes de cualquier veredicto**:

```
$ git fetch --unshallow
From https://github.com/Josanoforo/Modelado-Mexicano
 * [new branch]      codex/barrido-2 -> origin/codex/barrido-2
 + f8eb2e3...d0019a2 main            -> origin/main  (forced update)
$ git rev-parse --is-shallow-repository
false
```

**2 · SHA.** Base declarada por el encargo: el `main` posterior al merge de `#249`. Verificado:

```
$ git log --oneline -1 origin/main
d0019a2 Merge pull request #249 from Josanoforo/claude/new-session-7avlfg
```

Coincide con HEAD local. `origin/main` no avanzó más — no hay deriva que clasificar.

**3 · data/raw.** No se toca microdato. `data/raw` no se leyó ni se creó en este acto — confirmado por el perímetro real escrito (ver §5, cierre).

**4 · ENTORNO.** Variable cruda de entorno gestionado (proxy del arnés, `CCR_AGENT_PROXY_ENABLED=1`, `HTTPS_PROXY=http://127.0.0.1:34749`) — confirma sesión repo-only mediada por proxy, no acceso directo. Sonda:

```
$ curl -sS -o /dev/null -w "%{http_code}\n" --max-time 10 https://github.com/
400
```

400 del proxy sobre una ruta raíz sin payload — no es una respuesta de datos, es la firma esperada de "hay red genérica mediada, no hay acceso a red de datos" en este tipo de entorno. No se tocó red de datos.

**5 · ESPEJO.** Prohibido para cifras — este acto no citó ninguna cifra de México desde el espejo del proyecto; todas las cifras citadas (Gini, informalidad, ENIF, etc.) ya vivían en el corpus tocado, no se introdujo ninguna nueva.

---

## §1 · GATE — PR #249 fusionado, FP-30 firmada

```
$ git fetch origin && git log --oneline -1 origin/main
d0019a2 Merge pull request #249 from Josanoforo/claude/new-session-7avlfg
$ grep -c "FP-30" forense/firmas-pendientes.tsv
2
$ python3 -c "
import csv
r=[x for x in csv.DictReader(open('forense/firmas-pendientes.tsv',encoding='utf-8'),delimiter='\t') if x['id']=='FP-30']
print(r[0]['estado'], '|', r[0]['firmada_en'][:60])"
FIRMADA | ADR-93, PR #249, 17/ago/2026 -- firma de mesa verbatim: "FP3
```

Gate pasa. `FP-30` es `FIRMADA` (`ADR-93`, `PR #249`). Se procede.

---

## §2 · VERIFICACIÓN DE EXISTENCIA (re-corrida, no heredada)

**1 · ESTRUCTURA.** Confirmado contra `data/INFRAESTRUCTURA-v1_0.md` Dominio 7 (ADR + cascada) y Dominio 1 (corpus): este acto escribe `canon/` y `corpus/reports/`, no `data/`, no `tools/`.

**2 · CONTENIDO.**

```
$ grep -n "^\*\*ADR-\(92\|93\)" canon/gobernanza-v1_15.md | head -4
1576:**ADR-92 · Se registran, verbatim, las cinco firmas de mesa ...
1606:**ADR-93 · Se registran, verbatim, las tres firmas de mesa ...
```

Leídas completas: `ADR-92(d)` sella `FP-27`/`conf.02` (adopta mecanismo de `Tiempo`, conserva desenlace de `Trabajo`); `ADR-92(e)` sella `FP-28`/`conf.05` (partición en `.estatus`/`.recompensa`). `ADR-92` declara explícitamente, verbatim: *"este acto no ejecuta la cascada al corpus de `conf.02` ni de `conf.05`... eso es acto sucesor"* — `EXISTE-SATISFACE` para las dos adjudicaciones.

```
$ grep -i "cascada\|propagaci" forense/firmas-pendientes.tsv | grep -i "conf.02\|conf.05"
(vacío)
```

Ninguna fila del tablero registra la propagación al corpus como hecha. `NO-ENCONTRADO` — confirma el hueco que este acto llena.

**3 · COBERTURA RETROACTIVA.** `canon/glosario-v5_6.md` y `canon/integrador-psicologia-mexicano.md` existen desde antes de las adjudicaciones del 17/ago — su ausencia de la cascada, verificada arriba, sí prueba que no se propagó (no es solo que nadie lo haya buscado).

**Estampa de universo (A.10), re-corrida:**

```
$ git rev-list --count origin/main..origin/codex/barrido-2
12
```

Coincide exacto con lo que declara el encargo (12 commits). Sin deriva.

---

## §3 · MÉTODO OBLIGATORIO — control positivo, con su falla real

**Control 1 — el patrón ingenuo pierde grafías acentuadas y el inglés.**

```
$ grep -inE "policr[oó]n|monocr[oó]n|polychron|monochron" \
    "corpus/reports/Psicología_del_Trabajo_en_México__Un_Mapa_Basado_en_Evidencia.md"
129:**Myth 5: "'Mañana' culture means unreliability."** Polychronic time orientation reflects...
$ grep -inE "policr[oó]n|monocr[oó]n|polychron|monochron" \
    "corpus/reports/La_arquitectura_invisible_de_la_interacción_social_en_México.md"
(vacío)
$ grep -inc "policron" "corpus/reports/Psicología_del_Trabajo..." "corpus/reports/La_arquitectura_invisible..."
0
0
```

El patrón ASCII-only con `[oó]` sí encontró `:129` (inglés, "Polychronic") pero **perdió `La_arquitectura_invisible`:155** — "policrónica" ahí resultó invisible al grep. Investigado: sospecha de normalización Unicode inconsistente en el archivo (acento compuesto vs. precompuesto). Se rehizo el barrido con `unicodedata.NFKD` + `combining()` en Python, tal como exige el encargo:

```python
def strip_accents(s):
    return ''.join(c for c in unicodedata.normalize('NFKD', s) if not unicodedata.combining(c))
pat = re.compile(r'policron|monocron|polychron|monochron', re.IGNORECASE)
```

```
CONTROL Psicología_del_Trabajo... -> [129]
CONTROL La_arquitectura_invisible... -> [155]
```

Con NFKD, ambos controles positivos dan. Barrido real de `El_Mexicano_y_el_Tiempo` con el patrón NFKD:

```
lineas: [21, 27, 45, 46, 48, 49, 50, 79, 100, 148, 156, 167, 188, 233, 243, 244, 252]   total: 17
```

**17 líneas — coincide exacto con lo que el encargo citaba.** El grep ASCII-only (sin NFKD) había encontrado solo 10 de las 17 en este mismo archivo: perdía `:46, :48, :50, :100, :156, :167, :233` — siete líneas con "monocrónic-" en distintas formas, todas con acento compuesto de dos codepoints en este archivo particular. **Hallazgo del acto, no anticipado por el encargo:** el patrón `[oó]` de un solo carácter que el propio encargo sugiere como "ya probado" no es suficiente por sí solo contra este corpus — hace falta NFKD real. Se reporta como hallazgo en `hallazgos.md`.

**Control 2 — cobertura de `consumo compensatorio` en el corpus, más allá de lo citado.**

```
$ grep -rlinE "consumo compensatorio|compensatorio" corpus/reports/
corpus/reports/Behavioral_Finance_Mexicano__...md
corpus/reports/Health__Body__Food_and_Substance_Use_in_Mexico__...md
corpus/reports/Psicología_del_Consumidor_Mexicano__...md
corpus/reports/Report_26__...md
```

`Behavioral_Finance` y `Report_26` no estaban en el perímetro del encargo — inspeccionados, sus menciones son de "aversión al riesgo"/"consumo" genérico, no del constructo `consumo_compensatorio` de `conf.05` (verificado por lectura, no se tocan). Dentro de `Health, Body, Food` se encontró una **tercera mención no nombrada por el encargo ni por el benchmark en su tabla de COMMIT**: línea `:281` (*"¿Dónde hay evidencia débil pero intuición social fuerte? Consumo compensatorio (comida/alcohol como recompensa)..."*) — el benchmark sí la nombra en su propia §3 (`Health, Body, Food:35, :75, :281`), pero la tabla del encargo solo cita `:35, :75`. Se incluye `:281` en COMMIT 3, declarado aquí.

**Control 3 — citas de línea del integrador para `conf.05`, tres fuentes en desacuerdo.**

`FP-39` cita `integrador:36,:204`. El benchmark `conf05` §3 cita `integrador:36,:113,:204`. El encargo (tabla de COMMIT 3) cita `integrador:36,:113,:122`. Ninguna de las tres coincide con las otras dos. Re-derivado contra el árbol real, no heredado de ninguna:

```
$ grep -n "consumo compensatorio" canon/integrador-psicologia-mexicano.md
36:8. **[Fuerte · la ironía maestra] ... Consumo compensatorio es **Fuerte** (Velandia-Morales 2022)...
113:| **Consumo compensatorio / aspiracional** | ... | Compensatorio **Fuerte** (base LatAm)...
122:- Distancia de poder alta (convergente); ... consumo compensatorio (Velandia-Morales 2022); ...
204:**Evidencia a favor.** **Fuerte**: consumo compensatorio (Velandia-Morales 2022); ...
255:**Evidencia en contra / límites.** ... el consumo compensatorio como driver del refresco/botana es **Hipótesis**; ...
```

Las **cuatro** líneas `:36,:113,:122,:204` tratan el constructo como una sola rama `Fuerte`, sin distinguir. Se acotan las cuatro en COMMIT 3 — más amplio que cualquiera de las tres fuentes por separado, declarado como el barrido de este acto, no heredado.

---

## §4 · COMMIT 1 — cascada de `conf.02`, las 16 líneas de `El_Mexicano_y_el_Tiempo` declaradas una por una

Reescritos: `Psicología_del_Trabajo`:129 (mecanismo sustituido, desenlace conservado, en inglés); `La_arquitectura_invisible`:155 (acotado a preferencia individual); `integrador`:245,:351 (cita de ADR-94 añadida, ya adjudicaban de facto); `glosario`:316 (resuelto).

`integrador`:248,:308,:384 — revisados, **sin cambio**: los tres critican el uso de policronía/marcos importados como vara universal o los listan entre marcos a usar con crítica; ninguno afirma policronía como rasgo nacional establecido, ninguno contradice el mecanismo adoptado.

**Las 16 líneas de `El_Mexicano_y_el_Tiempo` (todas menos `:49`, que cambia por FP-40), declaradas una por una — su mecanismo es el adoptado, ninguna requiere cambio:**

| Línea | Por qué no cambia |
|---|---|
| `:21` | Hallazgo `[MEDIA]`: marcos importados "úsense como hipótesis, no como verdad nacional" — ya es la postura adoptada. |
| `:27` | "Gran parte... es cualitativa... VACÍO grande" — vocabulario A.4 correcto (no dice "no existe"), ya hedgeado. |
| `:45` | Encabezado de sección ("Crítica a la policronía de Hall") — expositivo, no afirma nada por sí solo. |
| `:46` | Define el marco de Hall (monocrónico/policrónico) para poder criticarlo después — descriptivo. |
| `:48` | Crítica al marco como binario/esencialista, cita a Hall admitiendo que es "culturally determined" — parte del mecanismo adoptado. |
| `:50` | Es la línea fuente del mecanismo mismo: "confunde norma social contextual... con incapacidad de operar monocrónicamente". |
| `:79` | Inventario de evidencia cualitativa/antigua — consistente. |
| `:100` | Riesgo de mala interpretación: exportar la "vara monocrónica" como universal — parte de la crítica adoptada. |
| `:148` | "VACÍO en medición... por región" — vocabulario A.4 correcto. |
| `:156` | Ejemplo del mecanismo: entorno de exportación → opera monocrónicamente sin fricción. |
| `:167` | Mismo error nombrado: puntualidad monocrónica como vara universal mal aplicada. |
| `:188` | Marco "sobreusado" como verdad nacional — consistente. |
| `:233` | Lista de "errores a evitar", incluye exportar la puntualidad monocrónica como universal. |
| `:243` | Auditoría de sesgo anglosajón — describe el marco, no lo adopta como rasgo nacional. |
| `:244` | "Carece de medición" en segmentos rural/indígena — vocabulario A.4 correcto. |
| `:252` | Nota metodológica, repite el hedge de `:27`. |

Ninguna de las 16 usa vocabulario prohibido por A.4 ni afirma policronía como rasgo mexicano establecido — todas están ya alineadas con el mecanismo que se adopta, que es justamente el de este mismo report.

---

## §5 · COMMIT 2 — `FP-40`

`El_Mexicano_y_el_Tiempo`:49 decía *"No hay medición nacional reciente de policronía mexicana. Esto es un VACÍO, no un dato"*. Reescrito a `NO-ENCONTRADO` con universo declarado (literatura de medición organizacional 2020-2026 y microdato del programa), citando Carraher et al. (2004) — dueños de pequeños negocios en siete países incluido México. `FP-40` → `FIRMADA`, cita de mesa verbatim *"FP40- De acuerdo!!!!"*.

---

## §6 · COMMIT 3 — cascada de `conf.05`

`glosario:136` desdoblada en dos filas (`.estatus` Fuerte, `.recompensa` Hipótesis razonable) — **la marca de procedencia `(a)+(c)` se conserva sin cambio en ambas**, no se adjudica `FP-38` (sigue `ABIERTA`; su cita de línea a `glosario:136` derivará hacia la primera de las dos filas nuevas — declarado aquí para quien la resuelva). `glosario:319` resuelta. `Psicología_del_Consumidor`:64,:384 y `modelo-decision`:384,:446,:678 etiquetados a estatus, sin cambio de contenido. `Health, Body, Food`:35,:75,:281 (la tercera del Control 2) etiquetados a recompensa, tier sin cambio. `integrador`:36,:113,:122,:204 (las cuatro del Control 3) acotadas a estatus. `integrador`:255 validado sin tocar — ya tenía la partición correcta.

**`hitoD-preregistro-v2_0.md`:59-60 (R1.4) queda fuera de perímetro — registro maestro append-only.** Línea que le tocaría, redactada aquí para quien sí tenga perímetro:

```
## R1.4 · Movilidad bloqueada → consumo_compensatorio.estatus `[FUERTE como correlación]`
> **SI** hay movilidad real bloqueada + presión de estatus (perfiles 2, 3, 5) **ENTONCES** consumo compensatorio/aspiracional (rama estatus), aun apalancado — PORQUE G2
```

(El resto de la ficha R1.4 — falsador, umbral, A/B/C/D — no cambia; solo el título y la línea SI-ENTONCES ganan la etiqueta de rama.) Fila abierta: `FP-43`.

---

## §7 · COMMIT 4 — `FP-39`, auditoría acotada

Alcance derivado de `estado-programa` §S5 (no de "el integrador entero"): `conf.02`, `conf.05`, instrumento de `conf.04`. `conf.06` cerrada por `ADR-64` salvo su residual (`FP-29`), fuera de este acto.

- **`conf.02`:** reproducido — `integrador:245,:351` adjudicaban de facto sin ADR. Regularizado en COMMIT 1.
- **`conf.05`:** reproducido y ampliado — la fila citaba `:36,:204`; el barrido de este acto (§3, Control 3) encuentra que `:113,:122` tienen el mismo defecto. Regularizado en COMMIT 3.
- **Instrumento de `conf.04`:** `grep -n "Cantril" canon/integrador-psicologia-mexicano.md` → 0 resultados. El integrador no toma posición sobre si la escalera de Cantril mide evaluación vital o alegría — silencio, no contradicción. No se le añade una posición que no tenía.

`FP-39` → `FIRMADA`, cita de mesa verbatim *"F39, revisar el integrador solamente contra esas"*.

---

## §8 · COMMIT 5 — `FP-30` ejecutada, `FP-41` cerrada

```
$ python3 tests/check.py 2>&1 | grep -A1 T07     # ANTES
  [FAIL]  T07 vocabulario de tiers  (1 fail)
  · T07: 1
      7 vocabularios de tier ajenos al Bloque A: `SÓLIDO` ×44 · `MEDIO` ×29 · `HIPÓTESIS RAZONABLE` ×22 ·
      `Moderada` ×3 · `MODERADA` ×2 · `MODERADA-FUERTE` ×1 · `Narrativa exagerada` ×1
```

`CANONICO` alineado al Bloque A más las equivalencias que `FP-30` ya selló, con `MEDIA-FUERTE` retenida (única decisión nueva del acto, declarada en `ADR-94(e)`). Conteo normalizado sobre `tok.strip().upper()`.

```
$ python3 tests/check.py 2>&1 | grep -A1 T07     # DESPUÉS
  [ ok ]  T07 vocabulario de tiers
```

`FP-41` → `FIRMADA`: *"No requirió firma nueva de mesa: es la ejecución de `FP-30`. Se cierra en el mismo acto que la ejecuta."*

---

## §9 · COMMIT 6 — el tablero: `ejecutada_en`, `encargo`, WARN de T22, filas nuevas

**Metodología para `ejecutada_en`/`encargo` de las 42 filas existentes.** Se derivó de lo que cada fila ya declaraba en su propio `firmada_en` (PR citado, si el texto describe una ejecución concreta ya hecha) y se cruzó cada PR contra los 64 archivos de `forense/encargos/*.md` por coincidencia de número y de nombre de ACTO:

```
$ grep -l "#242" forense/encargos/*.md    # FP-09, ACTO A10-ESTAMPA
forense/encargos/2026-08-17-EA10-a10-estampa.md
$ grep -l "#245" forense/encargos/*.md    # FP-13, ACTO RUTA-SELLO
(sin match directo por número -- confirmado por nombre: RUTA-SELLO-taxonomia.md)
$ grep -l "#238" forense/encargos/*.md    # FP-23, ACTO RECONCILIA-SPEC
forense/encargos/2026-08-14-B2-mantenimiento-via-capa3.md
forense/encargos/2026-08-14-RECONCILIA-SPEC-encargo.md
forense/encargos/2026-08-17-EHIG-higiene-vivos.md
```

Donde el cruce dio un match de **nombre** además de número (A10-ESTAMPA, RUTA-SELLO, RECONCILIA-SPEC, REGISTRA-17AGO(-II), y este mismo acto CIERRA-17AGO), se citó la ruta. Donde solo hubo coincidencia de número sin coincidencia de nombre de ACTO (varios archivos comparten rango de PR por close-en-fecha), se declaró `SIN ENCARGO` — no una ruta adivinada. Tres reglas de fondo, no una por fila:

1. Fila `FIRMADA` cuya sustancia se agota en el propio texto de la firma (una determinación, no una tarea pendiente en otro archivo) → `ejecutada_en = N/A`.
2. Fila `FIRMADA` cuyo propio texto declara la ejecución como explícitamente futura/condicional ("acto sucesor", "post-BARRIDO-2", "no se edita en este acto") → `ejecutada_en` vacío.
3. Fila `FIRMADA` cuyo propio texto describe una escritura real ya hecha en el repo, con PR identificable → `ejecutada_en = PR #NNN`.

**Casos que este mismo acto ejecuta** (`FP-27`, `FP-28`, `FP-30`, `FP-39`, `FP-40`, `FP-41`) quedan con `encargo` apuntando a este mismo encargo — `FP-27`/`FP-28` estaban `FIRMADA` desde `PR #248` pero su ejecución es la de este acto, no la de su firma. `ejecutada_en` se deriva por commit, no en bloque: `FP-27` (`conf.02`, `COMMIT 1`) queda en `PR #250`, porque ese commit es el único de los siete que sí fusionó ahí antes de que mesa lo cerrara a medio acto. Los otros cinco (`FP-28`, `FP-30`, `FP-39`, `FP-40`, `FP-41`) quedan en `PR #251` — la corrección post-cierre que llevó el resto del acto a un PR nuevo, declarada en `forense/notas/2026-08-17-consolida.md` §1.

**Resultado, verificado por comando, no contado a mano:**

```
$ python3 -c "
import csv
rows=list(csv.DictReader(open('forense/firmas-pendientes.tsv',encoding='utf-8'),delimiter='\t'))
print('total:', len(rows))
print('FIRMADA sin ejecutar:', [r['id'] for r in rows if r['estado']=='FIRMADA' and not r['ejecutada_en'].strip()])
"
total: 46
FIRMADA sin ejecutar: ['FP-17', 'FP-18', 'FP-24', 'FP-43', 'FP-44', 'FP-45', 'FP-46']
```

`FP-17`/`FP-18`/`FP-24` son deuda real que ya existía y nadie veía — no la crea este acto, la hace visible. `FP-43..46` son las cuatro filas nuevas.

**Las dos líneas de §S5 en `estado-programa` que BARRIDO-2 bloquea — redactadas aquí, `CASCADA NO ESCRITA — colisión de perímetro con BARRIDO-2`:**

Línea `:136` (`conf.02`), tal como quedaría:
```
- **conf.02** ✅ **Resuelto por ADR-94, 18/ago/2026:** se adopta el mecanismo de `Tiempo` (norma contextual + estructura) y se conserva el desenlace de `Trabajo` (el mito es falso). La policronía sobrevive como preferencia individual medible, sin adscripción nacional.
```

Línea `:137` (`conf.05`), tal como quedaría:
```
- **conf.05** ✅ **Resuelto por ADR-94, 18/ago/2026:** partido en `consumo_compensatorio.estatus` (Fuerte) y `consumo_compensatorio.recompensa` (Hipótesis razonable). No había una sola cosa que promediar.
```

Es el **tercer acto consecutivo** que hereda esta misma colisión sobre `canon/estado-programa-v1_10.md` — `ADR-92` y `ADR-93` ya la declararon, cada uno sobre su propia cascada de conteo de ADR. Se declara en `ADR-94(g)` como patrón, no como incidente aislado. Filas abiertas: `FP-44`, `FP-45`.

**`FP-46`** abre la fila de ejecución material de `FP-24` (las 20 filas CON PAR de `relaciones.tsv`, `data/**`, fuera de este acto) — primera clienta real del arreglo (a).

---

## §10 · Cierre — suite antes/después, por test, no agregado

**ANTES (commit inicial `d0019a2`, antes de cualquier edición de este acto):**

```
  [warn]  T03 referencias colgantes  (47 warn)
  [FAIL]  T05 ADR-32.c constructos en glosario  (5 fail)
  [FAIL]  T06 consistencia numérica  (2 fail)
  [FAIL]  T07 vocabulario de tiers  (1 fail)
  [FAIL]  T08 mapa de evidencia por report  (1 fail)
  [FAIL]  T09 marco (c) usado como causa  (8 fail)
  [warn]  T10 diáspora (b) sin marcar  (65 warn)
  [FAIL]  T11 afirmaciones de estado absolutas  (1 fail)
  [warn]  T13 cabecera de versión ADR-36  (1 warn)
  [FAIL]  T15 T-ADR-COUNT  (2 fail)
  [warn]  T22 T-FIRMAS  (13 warn)
  [FAIL]  T16 T-SUITE-SELF-CHECK  (4 fail)
  24 FAIL · 126 WARN — LÍNEA BASE: VERDE (HEAD congelado 45d7d2f5)
```

**DESPUÉS (los siete commits de contenido de este acto, incluido ADR-94):**

```
  [warn]  T03 referencias colgantes  (47 warn)          -- sin cambio
  [FAIL]  T05 ADR-32.c constructos en glosario  (5 fail) -- sin cambio, ajeno a este acto
  [FAIL]  T06 consistencia numérica  (2 fail)            -- sin cambio, ajeno a este acto
  [ ok ]  T07 vocabulario de tiers                       -- FAIL -> ok (COMMIT 5)
  [FAIL]  T08 mapa de evidencia por report  (1 fail)     -- sin cambio, ajeno
  [FAIL]  T09 marco (c) usado como causa  (8 fail)       -- sin cambio, ajeno
  [warn]  T10 diáspora (b) sin marcar  (65 warn)         -- sin cambio, ajeno
  [FAIL]  T11 afirmaciones de estado absolutas  (1 fail) -- sin cambio, ajeno
  [warn]  T13 cabecera de versión ADR-36  (1 warn)       -- sin cambio, ajeno (integrador ya
                                                             carecía del bloque antes de este acto)
  [FAIL]  T15 T-ADR-COUNT  (2 fail)                      -- sube de "91 vs 93" a "91 vs 94"
                                                             (CASCADA NO ESCRITA, declarada)
  [warn]  T22 T-FIRMAS  (17 warn)                        -- sube de 13 a 17 (arreglo (f):
                                                             7 FIRMADA-sin-ejecutar reales,
                                                             10 ABIERTA sin cambio neto)
  [FAIL]  T16 T-SUITE-SELF-CHECK  (10 fail)              -- sube de 4 a 10, consecuencia
                                                             aritmética de que WARN real subió
                                                             (17 citas de "N FAIL·M WARN" en
                                                             canon/ que antes coincidían por
                                                             casualidad con el WARN real ya no
                                                             coinciden -- deuda preexistente,
                                                             no clase nueva)
  29 FAIL · 130 WARN
```

```
$ python3 tests/check.py --baseline
LÍNEA BASE: ROJO — 10 entradas nuevas frente a tests/baseline.json (HEAD congelado 45d7d2f5)
  · T15 (estado-programa, cascada no escrita) · T16 ×2 (gobernanza, misma causa) ·
    T22 ×7 (FP-17, FP-18, FP-24, FP-43, FP-44, FP-45, FP-46 — FIRMADA sin ejecutar)
(5 entradas de la línea base ya no aparecen — mejora: las 5 de T07/T16 que dependían
 del FAIL de T07, resuelto)
```

**No se recongela `tests/baseline.json`.** Exige ADR de mesa (`ADR-76(f)`) y este acto no lo trae — queda ROJO, declarado, para que mesa decida en su siguiente acto o en la revisión de este PR.

**Contadores de medición sobre México: 0.** Este acto cierra dos contradicciones del casillero que llevaban meses, ejecuta una firma que había vuelto a mesa dos veces, y le pone al tablero la columna que le faltaba para que una decisión firmada no se confunda con una hecha.

---

## ADENDA — 18/ago/2026, `PR #251` — dos citas mutables resincronizadas, y un límite conocido de `T16` expuesto, no creado

Mesa pidió arreglar las citas de `T16` que quedaban rojas. Investigado a fondo antes de tocar nada, porque cambiar el número equivocado sería falsificar historia, no corregir un defecto.

**Lo que sí era una cita mutable, corregido:** `gobernanza-v1_15.md:764` y `:856` son, por declaración explícita de `ADR-84` (*"la cifra vigilada por T16 vive en las citas mutables designadas — `gobernanza:764,856`, `estado-programa:129,221`"*), las únicas dos citas de este archivo que `T16` espera ver resincronizadas cada vez que la cifra real cambia. Este acto cambió la cifra real (`T07` cierra, `−1 FAIL`; el arreglo del tablero hace visible el `WARN` real de `T22`, `+4` neto) y no las había resincronizado — omisión propia, no de un acto anterior. Corregido: `**20 FAIL · 126 WARN**` → `**19 FAIL · 130 WARN**` en ambas, verificado por comando que son exactamente esas dos líneas y ninguna cifra más (`sed -n '764p;856p'` antes y después).

**Lo que no era una cita mutable, y no se toca:** `gobernanza-v1_15.md:1274`, `:1387`, `:1393` (declaran `**20 FAIL · 126 WARN**`) y `:1656` (declara `**24 FAIL · 126 WARN**`, la propia `Cascada` de `ADR-94` de este acto, describiendo el estado *antes* de empezar) no están en la lista de citas designadas por `ADR-84` — son prosa histórica de actos anteriores (`ADR-84` sellándose a sí mismo, la resincronización de `ACTO TABLERO-FIRMAS COMMIT 3`) que **coincidía** con la cifra real de `origin/main` por casualidad, no por diseño: verificado directamente contra `origin/main` (`4415e04`, worktree aislado) que ninguna de las cuatro falla ahí — la cifra real de `main` seguía siendo `20 FAIL · 126 WARN` desde que `ADR-84` la fijó, así que estas cuatro líneas llevaban semanas "correctas" sin que nadie las tocara. En cuanto este acto mueve la cifra real, dejan de coincidir — no porque cambiaran, sino porque describían un momento pasado con el mismo formato que `T16` usa para "vigente", y el único marcador que `T16` reconoce como histórico (`_CAMBIO_FECHADO`, un blockquote `> **vX.Y — fecha**`) no cubre esta forma de prosa. Es exactamente el límite que el propio código de `T16` declara en su docstring: *"si un canónico narra un cambio pasado con cualquier otra forma... este test NO lo reconocerá como histórico y marcará FAIL un registro que en realidad es correcto."* Mismo mecanismo, mismo diagnóstico que ya resolvió `gobernanza:1106`/`:1136` (`ADR-90`) — pero esa corrección exige editar `tests/check.py`, fuera del perímetro declarado de este acto (`NO escribe: ... tests/**`).

**Resultado:** `LÍNEA BASE` sigue `ROJO`, `10` entradas — sin cambio en la cifra, con reatribución correcta: dos citas genuinamente arregladas, cuatro citas históricas genuinas expuestas (no creadas) por el propio movimiento de este acto, declaradas aquí en vez de falsificadas. `python3 tests/check.py --baseline` corrido antes y después de esta adenda: mismas `10` entradas, mismo desglose (`T15`×1, `T16`×2 claves — ahora atribuidas a `1274`/`:1387`/`:1393` y a `:1656`, no a `764`/`:856` —, `T22`×7). No se toca `tests/baseline.json` sin `ADR-76(f)`.
