# ADR-260924-GEN2-TUBERIA-VISTA-NORMALIZADA-2-f1b2-01

24/sep/2026 · `ACTO GEN2-TUBERIA-VISTA-NORMALIZADA-2`. COMMIT-A: `tolerancia`, `funciones_dependencia` y `fuente_replay` salen de `resultados.tsv` (26→23 columnas) hacia `corridas.tsv` (constantes por `corrida_id`, 0/313 no-constantes). `tools/vista.py` nuevo (`join_resultado`) los reconstruye, con fallback al valor propio de la fila y sin `KeyError` si falta la corrida. Equivalencia verificada: multiconjunto `{resultado_id, corrida_id, valor, estado, cuenta_gen2}` idéntico antes/después (70609/70609). Consumidores reales adaptados: `tools/delta_pin_superado.py`, `tools/adq_investigacion.py`, y un hallazgo no anticipado por el encargo — `milpa/src/emisor.py::cargar_indice_linaje_emision` (fuera del `git grep -- tools tests` que el encargo usaba para definir "los 18 consumidores"; sin el fix, el gate de emisión se habría roto en silencio). Guarda de tamaño en `verify.yml`: falla a 100 MB, avisa a 50 MB (autorizado a ~70 MB). `data/corrida0/*.tsv` no se tocan (derivados protegidos); el re-derivado real lo trae el `derivados/auto-*` posterior a la fusión. COMMIT-B (camino_linaje bajo demanda) queda `NO-CORRIDO`: cardinalidad medida `65287/65287 = 1.0` (regla ≥0.5 → deriva bajo demanda, no dedup), pero implementarlo fielmente con su test de reproducción de 500 filas es pieza propia — sucesor `GEN2-TUBERIA-VISTA-NORMALIZADA-3`. Suite `--rapido` VERDE, 0 FAIL; test_corrida0.py 94/94; test_motor_gen2_explicito.py con los mismos 2 fallos pre-existentes en main.
