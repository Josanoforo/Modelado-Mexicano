# ENCARGO · NOTAS-P3 — la PARTE 3 que mesa pidió el 17/ago, por fin ejecutada

**SHA de redacción:** `290f9a0` (`#259`, `origin/main`) · **Entorno:** NUBE, repo-only · **Estado:** VIVO — **Gate: ninguno duro, lanzable ya.** Recomendado después de la tanda en vuelo (`CONF-07-CIERRE` / `LANE-A` / `E3-TRIAGE`): este acto añade filas al tablero, y correrlo junto a otros que también lo tocan multiplica los conflictos de TSV. Colisión de número de ADR: esperada, protocolo conocido. 🚫 Sin `--freeze`.

## Por qué existe, con la voz de mesa

`CONSOLIDA-17AGO §PARTE 3` quedó viva tras dos actos que ejecutaron sus Partes 1-2 (#250-ish y #259, que lo marcó parcialmente consumido nombrando exactamente esto como lo pendiente). La orden de mesa, verbatim en ese encargo: *"ya no quiero más items abiertos o decisiones pendientes regados por todo el proyecto."* Este acto la cumple: un solo lugar (el tablero) para todo lo abierto que hoy vive disperso en notas, en el modelo y en los yaml del motor.

════ ARRANQUE ════ (los 5 puntos estándar: clon no-superficial con `main` local actualizada · SHA contra `290f9a0`, re-derivar si se movió · data/raw no se usa · firma A.2 de tres partes · toda cifra del clon.)

## VERIFICACIÓN DE EXISTENCIA — contestada por quien escribe, contra `290f9a0`

```
la ley de este acto:        forense/encargos/2026-08-17-CONSOLIDA-17AGO.md §PARTE 3            EXISTE-SATISFACE
  — universo CERRADO Y DECLARADO ahí: forense/notas/*.md · forense/hallazgos.md ·
    canon/modelo-decision-v4_0.md · milpa/*.yaml. "Todo lo demás ya fue barrido por
    ADR-91 y por #248; NO LO REPITAS."
  — patrón de dirección con DOS CONTROLES POSITIVOS declarados ("Pendiente de mesa,
    no ejecutado aquí:" · "RANURA (c): verificada SIN FIRMA") — re-valídalos TÚ antes
    de usarlos (receta probada contra caso conocido, A.4)
su estado:                  #259 lo marcó "parcialmente consumido — PARTE 3 sigue viva"        EXISTE-SATISFACE
el conteo "212 notas":      del 17/ago — RE-DERÍVALO (ls forense/notas/*.md | wc -l);
                            hoy son más, y tu número es el denominador                          A DERIVAR
lo ya barrido (no repetir): ADR-91 (lote de firmas) · #248 · A.12/T22 vigilan canon/ y
                            forense/ por marcadores — tu valor añadido es lo que ESOS
                            mecanismos no ven: prosa de notas, modelo, yamls                    EXISTE-SATISFACE
```

## PERÍMETRO

`forense/firmas-pendientes.tsv` (filas nuevas + punteros) · `canon/gobernanza-v1_15.md` (ADR) · `estado-programa` cascada `:27`/`:101` (⚠️ FP-48) · `forense/notas/` (la nota del acto; las notas barridas NO se editan — son registro) · `hallazgos.md` (append) · `forense/encargos/2026-08-17-CONSOLIDA-17AGO.md` (→ `CONSUMIDO` completo al cierre) · `forense/encargos/` (el propio). NO edita `modelo-decision` ni `milpa/*.yaml` — los lee; lo que encuentre ahí se registra como fila, no se "arregla" al paso. Fuera de lista: PARA.

## C1 · El barrido, con la disciplina que PARTE 3 ya escribió

Corre el patrón (re-validado) sobre el universo (re-derivado). Por cada hallazgo, clasifica antes de abrir fila:

* **VIVO** — pendiente real sin fila → fila nueva con procedencia (archivo:línea), qué gatea, y de quién es la decisión.
* **RESUELTO-SIN-TACHAR** — la palabra de mesa o el ADR ya existen y la nota no se enteró (la clase que MESA-18AGO cazó 3 veces y FP-29 sufrió) → no abre fila: línea en `hallazgos.md` con el puntero, y si el desalineado vive en el tablero, se corrige la columna.
* **HISTÓRICO** — narración de un pendiente que otro acto cerró → nada; se anota solo si el conteo final lo necesita.
* **HIPOTÉTICO** — brainstorm/escenario que nunca fue compromiso → nada. No promuevas hipótesis a pendientes (la disciplina de lectura de transcripciones aplica igual a notas).

PRISMA del barrido al cierre: archivos examinados · hallazgos por clase · filas abiertas · desalineados corregidos — cada cifra con su comando.

## C2 · Cierre

Filas nuevas al tablero (cada una con su procedencia) · `CONSOLIDA-17AGO` → CONSUMIDO completo (Partes 1-2 con sus PRs, Parte 3 con éste) · ADR re-derivado dos veces · cascada · nota con el PRISMA · `hallazgos.md` · encargo `CONSUMIDO`. Auditoría: contadores sobre México: cero, en una línea. Lo que mueve: el número de pendientes fuera del tablero pasa a cero medido — que es exactamente lo que mesa pidió. ¿Qué afirmación describe el estado del corpus escrita a mano? — ninguna: universo re-derivado, patrón re-validado, PRISMA con comandos.

⚠️ Y la trampa de este acto en particular: abrir filas es barato y cerrarlas cuesta — cada fila nueva le habla a mesa en cada corrida (T22). Si dudas entre VIVO e HIPOTÉTICO, la vara es: ¿alguien se comprometió, o alguien especuló? Especulación no gana fila.

**NO hace:** no resuelve ningún pendiente que encuentre · no edita modelo ni yamls · no re-barre canon/ ni lo que ADR-91/#248 ya cubrieron · no congela.

---

> **CIERRE — 18/ago/2026.** Ejecutado contra `290f9a0`, sin deriva de `main`. Universo re-derivado: **225** notas (`ls forense/notas/*.md | wc -l`), no 212. Patrón re-validado (A.4) contra los dos controles positivos declarados — ambos capturados (`forense/notas/2026-08-13-proc-11.md:47`, `forense/notas/2026-08-14-enlace2-clase-limbo.md:90`). Corrida: **148 líneas en 49 archivos** (`grep -rniE` del patrón sobre `forense/notas/*.md` + `hallazgos.md` + `canon/modelo-decision-v4_0.md` + `milpa/*.yaml`). Triaje: **3** `FILA` (`FP-54`, `FP-55`, `FP-56`) · **0** duplicadas de las cinco candidatas de dirección (corte de edad ya tenía `FP-53`; GDELT/UCDP seguía `YA RESUELTO` vía `ADR-76(g)`) · **10** `RESUELTO-SIN-TACHAR` registrados en `hallazgos.md`, **0** columnas de tablero que corregir · **1** `SOLO ANOTADO` (ENDIREH CP-1/VFAM) · falsos positivos del patrón descartados y declarados (1, `cal-enoe-fasea.md:147`). `ADR-104` sella la estampa de universo del cierre (registro puro; renumerado de 103 a 104 al fusionar `origin/main` -- colisión con el `ADR-103` de `ACTO GATE-DURABLE-V7`/`PR #260`, que fusionó primero). Cascada `estado-programa:27`/`:101` actualizada quirúrgicamente (FP-48: solo apéndice, nunca reescritura). `CONSOLIDA-17AGO` → PARCIALMENTE CONSUMIDO, únicamente PARTE 4 sigue viva. `python3 tests/check.py --baseline`: verificado antes (stash, estado previo a este acto) — **19 FAIL · 126 WARN, VERDE**; tras filas+hallazgos+ADR+cascada — **21 FAIL · 129 WARN, ROJO** (+3 WARN de T22 por las tres filas nuevas, señal esperada de A.12; +2 FAIL de T22 por autocaptura del patrón en este encargo y su nota, mismo defecto que `2026-08-14-tablero-firmas*.md`/`2026-08-18-ci-categoria.md` ya resuelven vía `_T22_ARCHIVOS_CONOCIDOS`). CI (`verify.yml`) corre `--baseline` como gate bloqueante; a pedido explícito del usuario tras el primer push ("resuelve CI"), ambos archivos se sumaron a `_T22_ARCHIVOS_CONOCIDOS` en un segundo commit -- no es `--freeze` (no toca `baseline.json`), es el remedio que el propio test documenta para esta clase de hallazgo. Final: **19 FAIL · 129 WARN, VERDE**, `git diff --check` limpio, `tests/test_svystat.py` (segundo gate de CI) sin cambio. Detalle completo, cada comando con su control positivo y su salida cruda: `forense/notas/2026-08-18-p3-barrido-final.md`. Contadores de medición sobre México: **0** — este acto no mide. **Este encargo queda `CONSUMIDO`.**
