# Nota de cierre — `ACTO SELLA-AGO24-C-v2`

**Acto:** `SELLA-AGO24-C-v2` · 24/ago/2026 · entorno **NUBE** (`cloud_default`) · modelo **Opus** · sin `--freeze`.
**Encargo:** `forense/encargos/2026-08-24-SELLA-AGO24-C-v2.md` (reexpedición; la v1 nunca se lanzó — 0 ramas, 0 fusiones).
**Sello:** `canon/gobernanza-v1_15.md` **`ADR-153`**.
**Base:** `origin/main = 754eb862a105a0474ef715d80ac122923a1174ee`, sin cambio durante el acto.

---

## 1 · Arranque, comando a comando

| # | Qué exigía el encargo | Comando | Resultado |
|---|---|---|---|
| 1 | Clon existente, ruta, `git log -1`, `git status`; no desde el home | `pwd`; `git log -1 --format='%H %ad %s'`; `git status --short`; `git branch --show-current` | `/home/user/Modelado-Mexicano` (no es el home); `754eb862…` *Merge pull request #320 from Josanoforo/adq-diseno-1*, 24/ago/2026; árbol limpio; rama `claude/sella-ago24-ocho-firmas-toosya` |
| 2 | Base `754eb86` o posterior, con `D`/`#318`, `COMMIT-DOC`/`#319` y `ADQ-1`/`#320` adentro — **verifica los tres ADR** | `git log --oneline -8`; `grep -n "ADR-15[012]" canon/gobernanza-v1_15.md` | Los tres verificados **por ADR, no por número de PR**: `ADR-150`/`SELLA-AGO24-D` (`0505b01`, fusionado en `8791bcf`/`#318`), `ADR-151`/`COMMIT-DOC-COERCION` (`b91285e`, `5bef289`/`#319`), `ADR-152`/`ADQ-DISENO-1` (`b42355c`, `754eb86`/`#320`). `RECENSO-DISEÑO-2` **no** fusionó mientras el acto corría: `main` sin cambio, no hubo que re-derivar |
| 3 | `data/raw` | — | **No aplica** a este acto, declarado. Este acto no lee microdato |
| 4 | Entorno esperado `cloud_default`; sin red ni microdato | — | **NUBE**, declarado; **sonda saltada**, declarada. `A.13`: todo negativo de esta nota declara los archivos examinados (§2) |
| 5 | Espejo | — | **Nada del espejo** |

---

## 2 · Verificación de existencia (`A.13`) — contra el repo real, no contra el encargo

**Las siete filas existen y estaban `ABIERTA`.** `forense/firmas-pendientes.tsv` leído con un lector `csv` de 9 columnas (`id`, `qué_se_firma`, `dónde`, `creado`, `gatea`, `estado`, `firmada_en`, `ejecutada_en`, `encargo`), no con `grep` de línea:

- **122 filas de datos** (el encargo declaraba 122) — coincide.
- **10 `ABIERTA`** (el encargo declaraba 10) — coincide, y son exactamente `FP-70`, `FP-93`, `FP-94`, `FP-97`, `FP-98`, `FP-101`, `FP-104`, `FP-109`, `FP-112`, `FP-118`. Las siete del encargo están entre ellas.
- **`F5` no existía** como `id`. Se crea (ver §4, decisión de `id` declarada).
- Máximo `FP` previo: **122**, derivado por comando, no tecleado.

**La definición que `FP-101` sella EXISTE.** `instrucciones-proyecto-v2_11.md`, Bloque B-bis, título en `:109` y cuerpo en `:111`, `:113`, `:115`. La línea que da la definición operativa es la **`:113`**, verbatim: *«La ficha declara, antes de correr, qué significa que el falsador no refute: si la regla queda corroborada, si queda acotada, o si el falsador era demasiado débil para decir nada.»* El encargo citaba `111-113`: correcto como rango del bloque.

**La procedencia de `FP-98` EXISTE.** `forense/adv-duelo/ADV1-M5-v2-propuesta-2026-08-20.md:3` — *«Benchmark web: 20/ago/2026, cuatro campos — ver §9 del cuerpo abajo para las fuentes citadas»* — y `:16` — *«PROCEDENCIA DEL DISEÑO | Benchmark web, 20/ago/2026, cuatro campos. Fuentes en §9»*. `§9` presente en `:149` (*«Procedencia del benchmark — 20/ago/2026, cuatro campos»*). `forense/BENCHMARK-INTERVALO-CORREDOR-M-2026-08-20.md` presente, 9,306 bytes. Los dos punteros del encargo son exactos.

**Las cinco fuentes de eventos de `F5` EXISTEN en las líneas citadas.** `data/cola-adquisicion-2026-08-12.tsv`, líneas `12` y `14-17`, leídas por `awk` con el número de línea físico: `GDELT`, `MASS_MOBILIZATION_DATA_PROJECT_EVENTOS_MEXICO`, `MASS_MOBILIZATION_PROTEST_DATA`, `MASS_MOBILIZATION_PROTEST_DATA_MEXICO`, `UCDP` — las cinco con `n_necesidades_servidas = 2:N17,N27`. El encargo acertó las cinco líneas. El alcance `54 fuentes` está corroborado en `forense/notas/2026-08-12-acto-o-cola-adquisicion.md` §3.1 (*«agrupado por `fuente_canonica_normalizada` → 54 fuentes distintas»*). El rótulo `COTA_SUPERIOR_NO_RECONCILIADA` vive en `canon/gobernanza-v1_15.md:870` (`ADR-67`, sustituto adoptado en `ADR-74(a)`) — el número de línea del encargo es exacto.

**El cierre previo de `FP-109` es `NO-ENCONTRADO`.** Buscado en `canon/gobernanza-v1_15.md` y en `forense/notas/` del 19-20/ago. El único sitio de gobernanza que trata `FP-64`/`ENOE` en esas fechas es `ADR-127`/`T3`, y su título dice, verbatim: *«`FP-64` — seis candidatos derivados y descartados, ninguno cruza el criterio; **no se adjudica**»*. Ningún acto cerró `FP-109` antes. **Esta firma es la que sella**, y así queda escrito en la fila, no como si estuviera citando un cierre anterior.

**El veredicto que `FP-112` archiva EXISTE y se negó a archivarse solo.** `forense/hitoD-R7_3-veredicto-v1_0.md`, cabecera: *«No adjudica: propone fila `C`»*; §4: *«Una fila `C` nunca se ha archivado en este programa … Este acto **propone `C` y no la archiva**, y abre fila de firma para que mesa decida la clase, en vez de decidirla el ejecutor (`ADR-76`/`ADR-79`)»*.

**Archivos examinados en esta verificación:** `forense/firmas-pendientes.tsv` (122 filas; 8 leídas íntegras), `instrucciones-proyecto-v2_11.md` (105-118), `forense/adv-duelo/ADV1-M5-v2-propuesta-2026-08-20.md`, `forense/BENCHMARK-INTERVALO-CORREDOR-M-2026-08-20.md`, `data/cola-adquisicion-2026-08-12.tsv`, `forense/notas/2026-08-12-acto-o-cola-adquisicion.md`, `canon/gobernanza-v1_15.md` (`ADR-57(c)`, `ADR-127`, `:870`), `forense/hitoD-R7_3-veredicto-v1_0.md`, `forense/hitoD-preregistro-v2_0.md` (Nota 31 + bloque append-only, 15 líneas contadas), `forense/prereg-duelo-v2/` (6 archivos), `forense/notas/2026-08-24-sella-d.md`.

---

## 3 · Las ocho firmas — qué se escribió por cada una

| Firma | Verbatim de mesa (24/ago/2026) | Qué se escribió |
|---|---|---|
| `FP-112` | «FP-112: Si, esto ya lo habíamos declarado.» | `ABIERTA` → `FIRMADA`, `ejecutada_en` = `2026-08-24`. `hitoD-preregistro-v2_0.md`: Nota 32 + línea `R7.3 → veredicto C` al final del bloque append-only. Contador `15 → 16 de 27`, seis citas vivas |
| `F5` (`FP-123`, nueva) | «F5: GO, para eso creamos una infraestructura robusta…» | Fila nueva `FIRMADA`: GO sobre las cinco fuentes de eventos + alcance `37 × 54` con `COTA_SUPERIOR_NO_RECONCILIADA`; gate de `ADQ-CORRE-R74R75` redirigido a `ADR-153` |
| `FP-109` | «FP-109: A, esto ya lo había cerrado en otra sesión.» | `FIRMADA`, opción (a) + **cláusula**: el cierre previo se buscó y no está; esta firma sella. `ADR-57(c)` gobierna, ENOE candidato (ii), falta el pre-registro de diseño sobre el decreto |
| `FP-70` | «FP-70: a, y si fuera necesario exploramos el universo desconocido.» | `FIRMADA`, opción (a) + fila sucesora **`FP-124`** `FIRMADA` (cruce U2/EV-1, UBUNTU) con las dos reservas arrastradas literalmente |
| `FP-97` | «FP97: Acto propio, podemos pedírselo a chatgpt…» | `FIRMADA` (acto propio): el ejecutor del código lo designa mesa, **externo incluido**; aceptación objetiva = suite + tests del scoring |
| `FP-101` | *(el encargo no lo transcribe — ver §6.2)* | `FIRMADA`. Resolución verificada: Bloque B-bis, `instrucciones:111-113`. Enmienda fechada en `mesa-pendientes.md` §1, original intacto. **PARO parcial** en la otra mitad, §6.1 |
| `FP-98` | «FP98: según yo el intervalo integrado llegó desde una web search…» | `FIRMADA` (adoptar) + procedencia verificada (`ADV1-M5 v2` `:3`/`:16`/`§9` + benchmark 20/ago). El scoring implementa `§4` tal cual |
| `FP-94` | «FP94: GO.» | `FIRMADA` + fila sucesora **`FP-125`** `FIRMADA` (bandas de `DOC-01..06` pre-registradas antes del scoring) |

---

## 4 · Decisión de `id` para `F5`, declarada y no silenciada

Mesa nombró el mensaje **`F5`**; el tablero no contiene ningún `id` fuera del esquema `FP-NN` (verificado sobre las 122 filas). Este acto **no inventa un esquema nuevo ni renombra la firma**: la fila se abre como **`FP-123`** —máximo previo `122`, derivado por `grep`, sin huecos— con `«F5»` citado verbatim en `firmada_en` y la derivación del `id` escrita dentro de la propia fila, para que quien la lea dentro de un mes no tenga que reconstruirla. Mismo criterio con que `SELLA-AGO24-D` mapeó las respuestas `D1`…`D4` a filas `FP`.

---

## 5 · Cascada del contador y recifrado

**Contador `Hito D` `15 → 16 de 27`, derivado del oráculo.** El valor no se tecleó: sale del bloque append-only de `hitoD-preregistro-v2_0.md`, única fuente que `ADR-40` reconoce y que `T18` parsea. Composición `8D·2B·3A·2E` → **`8D·2B·3A·2E·1C`**.

**Set de sincronía re-derivado por `grep` sobre el marcador de cascada `T20` del Hito D, no heredado del encargo** (`SELLA-AGO24-D` pudo haber movido líneas): **seis** citas vivas —`README.md:36`, `canon/estado-programa-v1_10.md:95` y `:275`, `canon/modelo-decision-v4_0.md:65`, `:700` y `:885`— más **dos** en `canon/gobernanza-v1_15.md` que el encargo no listaba pero que el mismo marcador vigila y que están dentro del perímetro (`:360`, la definición del Hito D; `:2959`, la fila del casillero de pendientes). Las ocho actualizadas; `T20` en `[ ok ]`.

**Recifrado estándar.** `gobernanza` cabecera `152 → 153 ADR`; `estado-programa` `:27` y `:103` igual, con la cita de `ADR-152` marcada `{cita-historica}` (mecanismo que `T15` ya usa, mismo precedente que `ADR-149`/`ADR-151`).

**Punto fijo declarado = medido en `:207` y `:299`.** Las dos líneas que declaran el `FAIL`/`WARN` de la propia suite son auto-referenciales: editarlas cambia `T15`/`T16` y por tanto el conteo que declaran. Se iteró hasta punto fijo en vez de dejar una cifra aproximada, siguiendo la desviación §3 de `forense/notas/2026-08-24-sella-d.md` y `ADR-149(f)`.

**Suite, antes y después:**

```
ANTES  (754eb86, sin tocar nada):  19 FAIL · 146 WARN — LÍNEA BASE: VERDE
DESPUÉS (punto fijo alcanzado):    19 FAIL · 148 WARN — LÍNEA BASE: VERDE
```

Tail literal de la corrida final (`python3 tests/check.py --baseline`):

```
════════════════════════════════════════════════════════════════════════
  19 FAIL · 148 WARN
════════════════════════════════════════════════════════════════════════

────────────────────────────────────────────────────────────────────────
  LÍNEA BASE: VERDE — nada nuevo frente a tests/baseline.json (HEAD congelado e24d033ed3c095f1e81c2fbb8248f108e9d3ef65)
  (5 entradas de la línea base ya no aparecen — mejora, no bloquea, no baja la cifra congelada sin --freeze explícito)
────────────────────────────────────────────────────────────────────────
```

**El alza de `WARN` de 2 está explicada por `T22` haciendo su trabajo, no por deuda nueva** — derivada de la lectura del propio `t22_firmas()` (`tests/check.py:1421`), no supuesta: la rama `(a)` deja de emitir **7** señales `ABIERTA` (las siete filas firmadas hoy) y la rama `(c)` —«`FIRMADA` con `ejecutada_en` vacío», el arreglo de `ADR-94`— empieza a emitir **9** (las seis firmadas hoy que quedan pendientes de ejecución, más `FP-123`/`FP-124`/`FP-125`). `−7 + 9 = +2`. `FP-112` es la única de las ocho con `ejecutada_en` puesto, porque es la única que este acto ejecuta además de firmar. `FAIL` sin cambio: **19**.

**Un defecto conocido, pisado y corregido en el acto.** La primera redacción del `ADR-153` escribía el marcador de cascada `T20` **entero, con sus delimitadores de comentario**, dentro de la prosa del ADR — exactamente el defecto que `forense/hallazgos.md` registró el 20/ago (*«Escribir el marcador `T20:HITO-D` con sus delimitadores de comentario dentro de la prosa de un ADR convierte esa prosa en una cita vigilada»*). `T20` lo detectó (`canon/gobernanza-v1_15.md:3150 declara 15 de 27`); reescrito sin los delimitadores y con la razón anotada en el propio ADR. El vigía funcionó.

---

## 6 · Discrepancias entre el encargo y el repo — declaradas, no forzadas

### 6.1 · Perímetro corto: `PARO` parcial en la mitad de `FP-101`

El encargo ordena escribir la cita de la definición B-bis «en el sitio exacto de la compuerta B-bis de `DUELO-PREREG-V2` (derívalo)» y sitúa ese sitio, en el `PERÍMETRO`, **dentro de `forense/prereg-duelo-v2/`**. Derivado por comando en vez de supuesto:

```
grep -rn "COMPUERTA B-bis\|compuerta B-bis" --include=*.md --include=*.py .
```

El sitio real es **`forense/escala-cinco-casillas-piloto-v1_0.md`** (`:13` *«Estado de este documento: NO SELLADO — COMPUERTA B-bis ACTIVADA»*, y `:25`), con `forense/escala-cinco-casillas-piloto-v2_0.md:82` citando ya el mismo Bloque B-bis (contra `instrucciones-proyecto-v2_10.md:113`) sin conectarlo con esta pregunta. **Los dos archivos están fuera del perímetro declarado**, y dentro de `forense/prereg-duelo-v2/` la única mención de la compuerta es la del propio `mesa-pendientes.md`.

**`PARO` en esa mitad**, conforme a la cláusula del propio encargo (*«Fuera de esta lista: PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo»*). Lo que sí se hizo, todo dentro del perímetro: la resolución de `FP-101` queda sellada en el tablero y escrita **entera**, con la cita verbatim de la definición, en `forense/prereg-duelo-v2/mesa-pendientes.md` §1 — que es, además, el archivo que el propio `DUELO-PREREG-V2` designó para cerrar §1 (*«Cómo cerrar este archivo»*). La compuerta de `escala-cinco-casillas-piloto-v1_0.md` **sigue activada** y así queda anotado en la enmienda, para que ningún lector futuro la crea cerrada.

**Es el tercer perímetro corto de la misma clase en tres días** — `ADR-147(c)`, `ADR-149(f)`, `ADR-151` y ahora éste. En los cuatro, el encargo mandó tocar algo que su propia lista no cubría.

### 6.2 · `FP-101` sin verbatim en el encargo

El encargo ordena `FP-101 → FIRMADA (verbatim de mesa)` pero, **a diferencia de las otras siete, no transcribe el texto de la firma**. Este acto **no lo inventa**. La fila lo dice así en `firmada_en`: el verbatim queda por transcribir de la cola de mesa del 24/ago, y lo que esta firma sella es la **resolución** —que sí está escrita en el encargo y se verificó contra el repo—. Sellar la resolución sin fabricar la cita es preferible a las dos alternativas: inventar un verbatim, o dejar `FP-101` abierta cuando mesa ya la firmó.

### 6.3 · `ADQ-CORRE-R74R75` y `SELLA-AGO24-B` no existen en el repo

El encargo declara, dentro de `F5`, que «el gate de `ADQ-CORRE-R74R75` se lee contra ESTE ADR (su texto original decía "SELLA-AGO24-B", acto que no existió)». Verificado por `grep` sobre `.md`/`.tsv` del árbol: **cero coincidencias** de `SELLA-AGO24-B` **y cero de `ADQ-CORRE-R74R75`**. Es decir: el acto `B` en efecto no existió, y el encargo que llevará ese gate **tampoco está todavía en el repo**. La declaración se escribe en `ADR-153` y en la fila `FP-123` como lo que es —**prospectiva**, dirigida a un encargo futuro— y no como una verificación hecha.

### 6.4 · Dos citas vigiladas del contador que el encargo no listaba

El encargo nombra como set de sincronía «estado línea Hito D · `README:36` · `modelo-decision` citas vivas». La re-derivación por marcador encontró **dos más**, en `canon/gobernanza-v1_15.md` (`:360`, `:2959`), que el mismo `T20` vigila. `gobernanza` **sí** está en el perímetro, así que se actualizaron sin salirse de él; se anota porque el encargo no las nombraba y sin `T20` habrían quedado desincronizadas.

**Ninguna discrepancia requirió escribir fuera del perímetro declarado.**

---

## 7 · Perímetro — archivos tocados, uno por uno

- `forense/firmas-pendientes.tsv` — 7 filas `ABIERTA` → `FIRMADA`; 3 filas nuevas (`FP-123`, `FP-124`, `FP-125`). 122 → 125 filas
- `forense/hitoD-preregistro-v2_0.md` — Nota 32 (append) + línea `R7.3 → C` al final del bloque append-only
- `canon/estado-programa-v1_10.md` — `:27`, `:95`, `:103`, `:207`, `:275`, `:299`
- `README.md` — `:36`
- `canon/modelo-decision-v4_0.md` — `:65`, `:700`, `:885`
- `canon/gobernanza-v1_15.md` — cabecera (`153 ADR`), `:360`, `:2959`, `ADR-153` nuevo, bitácora de versiones
- `forense/prereg-duelo-v2/mesa-pendientes.md` — enmienda fechada en §1, original intacto
- `forense/hallazgos.md` — una línea
- `forense/notas/2026-08-24-sella-c-v2.md` — esta, nueva
- `forense/encargos/2026-08-24-SELLA-AGO24-C-v2.md` — nueva, verbatim, `CONSUMIDO`

- `tests/check.py` — **extensión de perímetro por desviación mecánica, declarada** (precedente `ADR-147(c)`/`ADR-149(f)`/`ADR-151`): una entrada en `_T25_ARCHIVOS_CONOCIDOS` para el encargo archivado. El encargo se archiva **verbatim** y su texto de dirección escribe `ADV1-M5` sin su prefijo, que `T25`/`D-6` (`ADR-128`) marca; las dos salidas eran editar el texto de dirección —inaceptable— o censar el archivo, que es lo que `ACT-PIL-2` ya hizo por la misma razón. ese rótulo ya estaba censado en `canon/registro-rotulos.tsv`: no se añade rótulo nuevo. Esta nota, en cambio, **no** necesitó exención — su prosa es propia y se reescribió como `ADV1-M5`.

**No se tocó** `forense/escala-cinco-casillas-piloto-v1_0.md` y `forense/escala-cinco-casillas-piloto-v2_0.md` (§6.1), `instrucciones-proyecto-v2_11.md` (sólo lectura; no cifra `ADR` ni `Hito D`), `tests/`, `tests/baseline.json` (sin `--freeze`), `milpa/`, `corpus/`, `data/`, ni ningún `resultado.tsv`. Ningún archivo fuera de la lista de arriba.

---

## 8 · Lo que este acto deliberadamente NO hace

No reabre `FP-83` (la propia `FP-98` lo declara: se abre fila propia, no se reabre). No implementa la composición de `scoring-adv1-m3.py` — `FP-97` la manda al sucesor, y el ejecutor lo designa mesa. No corre el cruce `U2`/`EV-1` (es UBUNTU, `FP-124`). No pre-registra las bandas de `DOC-01..06` (`FP-125`). No retira ENOE de `ADR-57(c)` ni escribe el pre-registro de diseño de la llave (ii). No cierra la `COMPUERTA B-bis` de `escala-cinco-casillas-piloto-v1_0.md` (§6.1). No mide nada sobre México: el único contador que se mueve, `Hito D` `15 → 16`, archiva un veredicto que ya estaba corrido desde el 20/ago y que sólo esperaba adjudicación de mesa.
