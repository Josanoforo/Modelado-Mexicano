**ADR-260924-GEN2-ADOPCION-BLOQUE-Y-PINES-1-ec71-01**, `ACTO GEN2-ADOPCION-BLOQUE-Y-PINES-1`, 24/sep/2026, NUBE, Sonnet 5, MODO ABIERTO, COMPUERTA: ninguna. Ejecuta las firmas L–P de `GEN2-TRAMITE-FIRMAS-14-ADENDA-3`, corregidas por `ADENDA-1` propia del encargo. **P-L:** verificación sin cambio — `PR #1101` ya trae el marcador/`test_c2_compuesto` en punto fijo y las 16 filas `origen_numerico` de `CALC-GOB-DIGITAL-EXE-EMISIONES-0002`; con el marcador re-derivado en el árbol (sin commitear) los 16 T-REPRO(g) de `CALC-ENCIG-DUELO-2025-ADJUDICACION-0001` protegen una medición real y quedan gateados por `FP-…-749c-01` (ABIERTA), no una aserción vieja. **P-M:** 10 filas `cuenta_gen2=SI` (ENIGH duelo + 3×3 remesas/perfil/contexto 2016-2018-2020, todas `REPRODUCE`/`IDENTICO`); `pendientes_adopcion`/`adoptados_activos` NO se mueven por esto — son un eje distinto (Banxico/CTX/Motral, `NC-…-369b-01`, exige editar `milpa/`), hallazgo de premisa declarado (`NC-…-ec71-01`); 6 corridas (no 4) siguen `NO-VERIFICADO` (`NC-…-ec71-02`, recuento corregido tras `/revisa` sobre `PR #1102`). **P-N:** momento 08 acotado a unidad DELITO, `NO-REPRODUCE-GEN1` rotulado, dictamen en `decisiones.tsv:catalogo:M08:...` — **sin pin**: un pin `catalogo-momentos::M08` → `RESULT-ENVIPE-SEG-CON-P-DENUNCIA` se probó (`i-CRUDO`, `pines_mesa.valida_pin` → `ACEPTADO`) pero rompió `T-REPRO(c)` en `check.py --baseline` (el catálogo sellado no materializa un literal comparable para `AJUSTE`) y se revirtió en el mismo acto; momentos 01/02 dictaminados `NO-CONSTRUIBLE`; escritor NO extendido con código nuevo — `milpa/src/momentos.py:119-134` no consume nada en E0 para `AJUSTE` por diseño, confirmado por dos vías independientes, D-14 no se abre (`NC-…-ec71-03`). **P-O:** el pin de vía (iii) no estaba huérfano — su llave sigue vigente, sólo redundante con el escritor de RES-0028 (`PIN-SOBRE-CONSUMIDOR-YA-MARCADO`); nota aclaratoria, sin re-apunte ni retiro. **P-P:** `celdas_validadas_definicion_desde=38dd709` (PR #1086, no #1078 como citaba el encargo — misma confusión que `ADR-…-FIRMAS-14-9556-01` ya corrigió para otra fila); spec §9 declara el ancla antes del código (D-15).

`status` (tras revertir el pin de M08): `N_corridas_selladas` 234→244 · `N_resultados_gen2_sellados` 65599→65890 · `celdas_validadas` 219→219 con `celdas_validadas_definicion_desde=38dd709` nuevo. Todo lo demás sin cambio: `N_resultados_gen2_adoptados_activos` (72), `dependencias_numericas_legacy_activas` (146), `legacy_activas_por_consumidor__catalogo_de_momentos` (23), `pendientes_adopcion` y `__motor` — declarado por comando, no forzado. Nota completa: `forense/notas/2026-09-24-GEN2-ADOPCION-BLOQUE-Y-PINES-1-cierre.md`. Suite `--rapido` VERDE, 0 FAIL; suite de `celdas_validadas` (22 tests) VERDE; `check.py --baseline --parallel` VERDE tras revertir el pin (rojo antes, por `T-REPRO(c)`, ver enmienda del ADR).

Segunda enmienda (`/revisa` sobre `PR #1102`, PR abierto): `NO-FUSIONAR`, 3 hallazgos — `registro-rotulos.tsv` desactualizado (pin/conteos), recuento de P-M 5→6, `README.md` sin declarar en Perímetro. Los tres corregidos en el mismo PR; ninguno tocó `status`. Ver segunda enmienda en `canon/gobernanza-v1_15.md`.
