# ENCARGO MAP-2 — El cruce de los cuatro universos (2026-08-06)

Redactado contra `origin/main` = `168af29`.

## Arranque

### 1 · Repo y worktree

Clon localizado por búsqueda (no supuesto): `find "$HOME" -maxdepth 4 -type d
-iname "*modelado*mexicano*"` → `/home/pc0/Modelado-Mexicano`.

```
$ git fetch --all --prune
 - [deleted]  origin/int1-integridad-1786003491
 - [deleted]  origin/map1-lector-1786000558
   168af29..f9eb6d8  main -> origin/main
```

Las dos ramas borradas del remoto son exactamente las de INT-1 (PR #148) y
MAP-1 (PR #147) — ya fusionadas y limpiadas, consistente con que este acto
depende de INT-1 ya cerrado.

Worktree nuevo, sufijo por timestamp: `/home/pc0/wt-map2-1786030513`, rama
`map2-cruce-1786030513`, creado desde `origin/main`. `git worktree add`
emitió el mismo aviso que documenta la nota de INT-1 —
`error: could not write config file .git/config: Device or resource busy`,
exit 0, worktree registrado igual — evidencia de otra sesión escribiendo
en el mismo `.git` compartido en este momento, tal como advertía el encargo.

```
git status         -> "On branch map2-cruce-1786030513, nothing to commit, working tree clean"
git log -1         -> f9eb6d8 Merge pull request #148 from Josanoforo/int1-integridad-1786003491
git worktree list  -> incluye /home/pc0/wt-map2-1786030513  f9eb6d8 [map2-cruce-1786030513]
```

### 2 · SHA

Base declarada por el encargo: `168af29`. `origin/main` había avanzado a
`f9eb6d8` — un solo merge de diferencia:

```
$ git log --oneline 168af29..f9eb6d8
f9eb6d8 Merge pull request #148 from Josanoforo/int1-integridad-1786003491
9a66c45 INT-1: segunda pasada de correccion T03
bb01865 INT-1: corrige dos citas sueltas de raices.local.yaml (riesgo T03)
c5811d6 INT-1: addendum post-merge — origin/main avanzó a 168af29, sin tocar manifiesto, suite verde x3
aedb4b6 Merge origin/main into int1-integridad-1786003491 antes de PR
87446af ENCARGO INT-1: barrido de integridad del corpus
```

Solo INT-1 (PR #148). No es PARO — el encargo mismo anticipaba este avance
("INT-1 y probablemente ID-X aterrizaron después"). Corrección: ID-X
(PR #130, `CORRIDA-IDG3`) ya estaba fusionado *antes* de `168af29`
(2026-08-05T20:22:47Z, contra `168af29` commiteado 2026-08-06T08:12:47Z)
— no aterrizó después, ya estaba dentro de la base declarada. El único
delta real de esta base es INT-1.

También confirmado por `gh pr list`: PR #141 (`instrucciones-proyecto
v2.6`, Bloque D-ter) fusionó el 2026-08-06T00:12:57Z, y
`git merge-base --is-ancestor ea1481a 168af29` → sí — v2.6 ya era canon
vigente en la base declarada del encargo, no una sorpresa posterior. El
encargo cita "A.2 de v2.5"; A.2 sigue vigente en v2.6 sin cambios (rótulo
`[NUEVO v2.5]` conservado dentro del documento v2.6), así que la cita es
correcta. Lo que el encargo no menciona — y sí es relevante para el
vocabulario prohibido del PASO 2 — es Bloque D-ter (A.4–A.7, v2.6), ya
vigente: A.4 exige declarar el universo de búsqueda antes de sellar
cualquier "no existe", que es exactamente lo que el PASO 0 de este encargo
pide para U1. Se aplica sin que el encargo lo cite por nombre.

### 3 · Las tres raíces

`data/raw` y `data/raices.local.yaml` ausentes en el worktree fresco
(gitignorados, esperado):

```
$ ls -ld data/raw
ls: cannot access 'data/raw': No such file or directory
```

Todos los worktrees existentes enlazan `data/raw` al mismo destino
compartido; enlacé igual:

```
$ ln -s /home/pc0/mm-corpus/raw data/raw
$ readlink -f data/raw
/home/pc0/mm-corpus/raw
```

`data/raices.local.yaml` copiado de `/home/pc0/wt-int1-1786003491/data/raices.local.yaml`
(la dependencia directa de este acto; también existe en el worktree
principal y en `wt-map1b-censo-1786000741`, mismo contenido):

```
$ cat data/raices.local.yaml
data_raw: /home/pc0/mm-corpus/raw
descargas_mx: /mnt/c/Users/PC0/Descargas MX
downloads: /mnt/c/Users/PC0/Downloads

$ ls -ld "/mnt/c/Users/PC0/Descargas MX"
drwxrwxrwx 1 pc0 pc0 4096 Aug  5 18:09 /mnt/c/Users/PC0/Descargas MX
```

Tres de tres resuelven.

### 4 · Entorno — firma de cuatro valores crudos

```
$ echo "${CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE:-sin_variable}"
sin_variable

$ curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://www.inegi.org.mx/
200

$ ls data/raw/ 2>/dev/null | head -1
BD_ENCUCI2020_dbf.zip

$ ls data/raw/ 2>/dev/null | wc -l
239
```

`sin_variable` + red alcanzable + corpus montado con 239 entradas visibles
= entorno local (Ubuntu/WSL) con el corpus compartido presente. Es el
entorno que el encargo asigna. No se lanzó en la nube.

### 5 · Espejo

Ninguna cifra de esta nota sale de un espejo del proyecto. Todo lo
reportado arriba y en lo que sigue proviene de comandos corridos dentro de
`/home/pc0/wt-map2-1786030513`, con el comando pegado junto al resultado.

### 6 · Verificación de la dependencia (INT-1)

```
$ git log --oneline -1 -- forense/notas/2026-08-06-int1-integridad-corpus.md
9a66c45 INT-1: segunda pasada de correccion T03

$ git log --oneline -1 -- data/integridad-corpus-2026-08-06.tsv
87446af ENCARGO INT-1: barrido de integridad del corpus
```

Ambos en `main`. Clasificación re-derivada de forma independiente contra
el TSV crudo (471 − 4 sin sha = 467 filas evaluadas), no copiada de la
cabecera del documento:

```
$ tail -n +2 data/integridad-corpus-2026-08-06.tsv | awk -F'\t' '{print $8"\t"$9}' | sort | uniq -c | sort -rn
    441 COINCIDE   INTEGRO
     18 COINCIDE   NO_EVALUADO
      5 COINCIDE   ERROR:NotImplementedError
      2 NO_COINCIDE  CORRUPTO
      1 NO_COINCIDE  INTEGRO

$ tail -n +2 data/integridad-corpus-2026-08-06.tsv | awk -F'\t' '{print $7}' | sort | uniq -c
    467 EXISTE
```

Reconcilia exactamente con la tabla `PASO 2` de la nota de INT-1 (441 / 18
/ 5 / 3 NO_COINCIDE / 0 AUSENTE / 0 RAÍZ-NO-RESUELTA / **COINCIDE+CORRUPTO
= 0** — la clase peligrosa) y con su cabecera de control (`C-HASH`:
COINCIDE 464 = 441+18+5, NO_COINCIDE 3 = 2+1; `C-ESTRUCTURA`: INTEGRO
442 = 441+1, CORRUPTO 2, NO_EVALUADO 18, ERROR 5). Dos mecanismos
distintos, mismo número, en dos sesiones distintas. La dependencia queda
verificada, no solo citada. Este acto hereda esta clasificación como
columna, sin recalcularla más allá de esta confirmación puntual.

De los 3 `NO_COINCIDE`, uno estructuralmente `INTEGRO` pese al hash
discordante — nota para el lector, no reabre nada: un archivo puede
truncarse o sustituirse y aun así traer una estructura ZIP/PDF legible.
`C-HASH` es la señal que importa aquí, no `C-ESTRUCTURA`.

## PASO 0 — Los cuatro universos, declarados antes de cruzarlos

### U1 · Lo que el portal ofrece

```
$ wc -l data/indice-descarga-masiva-2026-08-05.tsv
7931 (7930 filas + cabecera)

$ tail -n +2 data/indice-descarga-masiva-2026-08-05.tsv | cut -f1 | sort -u | wc -l
7930   # URLs únicas — ninguna duplicada

$ tail -n +2 data/indice-descarga-masiva-2026-08-05.tsv | cut -f1 | grep -c '/programas/'
7510

$ tail -n +2 data/indice-descarga-masiva-2026-08-05.tsv | cut -f1 | grep -vc '/programas/'
420   # las 420 son, sin excepción, contenidos/investigacion/... (BIARE/bienestar)

$ tail -n +2 data/indice-descarga-masiva-2026-08-05.tsv | cut -f1 | grep '/programas/' \
    | sed -E 's#.*/programas/([^/]+)/.*#\1#' | sort -u | wc -l
136
```

7,930 / 136 / 420 — los tres reconcilian exactamente con el control de mesa.

⚠️ **Declaración de universo de U1, en una línea:** las 7,930 URLs son el
apartado **Microdatos** (más un apéndice de 420 URLs de `contenidos/
investigacion/bienestar/ampliado`, fuera de `/programas/`), de la puerta
**Descarga Masiva**, a nivel **nacional**. Cinco canastas del mismo
apartado no están generadas (Tabulados 15,318 · Indicadores 392 · DENUE
486 · Sala de prensa 71 · INV 3), y seis puertas enteras del portal no se
tocaron: Datos Abiertos, BIE, Banco de Indicadores, APIs, Servicios Web,
Acervo Digital. Todo "no está en el portal" en este documento significa
"no está en el apartado Microdatos de Descarga Masiva" — nunca "no existe
en INEGI" y mucho menos "no existe". Precedente ya pagado por este repo:
CPV le faltaba el Cuestionario Ampliado hasta que alguien lo notó a mano.
Esto satisface A.4 (Bloque D-ter, v2.6): universo declarado antes de
sellar cualquier clasificación negativa.

### U2 · Lo que los inventarios declaran que existe

```
$ python3 tests/catalogo.py 2>&1 | tail -25
ARCHIVOS: 11   ENTRADAS (con repetición): 201   RESUELTAS: 197   SIN_RESOLVER: 4
FUENTES ÚNICAS (dedup por identidad canónica, tabla de alias): 128
...
MICRODATOS **Y** ACCESO LIBRE (el conjunto operable): 43
```

11 / 128 / 43 / 4 — los cuatro reconcilian exactamente con el control de
mesa.

### U3 · Lo que está en disco Y es lo que dice ser

```
$ grep -c '^- id:' data/manifiesto.yaml
471
$ grep -c '^  sha256:' data/manifiesto.yaml
467
$ tail -n +2 data/integridad-corpus-2026-08-06.tsv | awk -F'\t' '$8=="COINCIDE" && $9=="INTEGRO"' | wc -l
441
```

471 / 467 / 441 — los tres reconcilian exactamente con el control de mesa
(y con la verificación de dependencia del Arranque §6, ya derivada arriba
por dos mecanismos).

### U4 · Para qué sirve — las 14 reglas abiertas del Hito D

`forense/hitoD-preregistro-v2_0.md` tiene **27 fichas** (confirmado no
solo por el control de mesa sino por el propio encabezado de
`forense/cruce-catalogo-fichas-v2_0.md:7`: *"27 fichas de
hitoD-preregistro-v2_0.md"*, y por conteo estructural independiente de
sus siete bloques `§3.x`: 4+2+3+3+2+8+2+3 = 27). El único bloque que un
test puede leer para veredictos reales es `## Registro de veredictos
archivados` (línea 1054):

```
$ sed -n '1054,1071p' forense/hitoD-preregistro-v2_0.md | grep -c '^`R'
14   # 14 líneas de emisión…

$ sed -n '1054,1071p' forense/hitoD-preregistro-v2_0.md | grep -oE '^`R[0-9]+\.[0-9]+`' | sort -u | wc -l
13   # …pero R4.3 aparece dos veces (mitad A, mitad B) = 13 fichas únicas selladas
```

Selladas (13): `R1.1 R1.2 R1.3 R3.1 R3.2 R4.1 R4.2 R4.3 R5.1 R5.2 R7.2 R9.1
R9.2`. 27 − 13 = **14 abiertas**: `R1.4 R2.1 R2.2 R3.4 R7.1 R7.3 R7.4 R7.5
R8.1 R8.2 R8.3 R10.1 R10.2 R10.3`.

Esta lista de 14 se re-derivó tres veces, por tres mecanismos
independientes, con el mismo resultado: (1) el conteo estructural de
`cruce-catalogo-fichas-v2_0.md` de arriba, (2) el parser de este acto
sobre el bloque de veredictos archivados, y (3) el propio
`forense/notas/2026-08-05-conf17-fetch-corrida-A.md:58-59`, que trae la
lista en Python literal: `selladas 13 [...]` / `abiertas 14 [...]` —
carácter por carácter idéntica a la derivada aquí. El control de mesa
(13 de 27) también reconcilia.

⚠️ **Nota sobre las "reservas ya establecidas" que el encargo cita para el
PASO 3.** El encargo menciona `R1.1 AGROASEMEX nivel fondo, no productor`
en el mismo párrafo que `R7.3`, `R8.1` y `R2.1`. Verificado: **`R1.1` NO
es una de las 14 abiertas — está sellada** (veredicto `D`, archivada
29/jul/2026). `forense/notas/2026-08-05-conf17-fetch-corrida-A.md:69,467`
lo confirma explícitamente: *"R1.1 — sellada (D) — No, [no] se reabre"* /
*"Hueco de mercado, no de dato — el barrido ya lo confirma sin reabrir el
veredicto"*. Leo la instrucción del encargo como defensiva, no como un
error de conteo: `R1.1`/AGROASEMEX es un tema adyacente y fácil de
confundir con los verdaderamente abiertos (comparte bloque `§3.1`,
comparte sesión `CONF-17`), y el encargo previene contra tratarlo como
tal. Se hereda el hallazgo (padrón a nivel Fondo, 4 recursos CKAN, sin
folio/CURP/RFC de productor — `corrida-B.md:402`) como contexto citable
en el mapa del PASO 2 donde `AGROASEMEX` aparezca como fuente candidata,
pero **no** se cuenta como una de las 14 en el PASO 3. `R2.1` (ECCO),
`R7.3` (PUB Bienestar) y `R8.1` (Contraloría Social/SICS) sí están,
correctamente, entre las 14 — confirmado por tema en
`corrida-A.md:116,185,297`.

## PASO 1 — La clave de unión

### `tests/dedup.py` — cruce contra `data/manifiesto.yaml`

```
$ python3 tests/dedup.py 2>&1 | grep -A6 "CRUCE CONTRA"
CRUCE CONTRA data/manifiesto.yaml (via tabla de alias)
  entradas de manifiesto: 471
  ids emparejados a alguna fuente canonica: 409
  MANIFEST_SIN_MATCH (id sin fuente canonica que lo explique): 62
```

409 + 62 = 471. El script solo imprime los primeros 20 de los 62; para
clasificar hacia falta la lista completa. Repliqué la lógica exacta de
`prefijos_id()` y del cruce de tokens en un script de solo lectura fuera
del repo (no se editó `tests/dedup.py` — fuera de perímetro) y obtuve los
62 completos.

**Nota lateral, no perimetral:** dos de los 62 (`hito`, `encargo`)
resultaron ser el propio id truncado — el regex `^- id: ([a-z0-9_]+)` de
`dedup.py` (y el mismo patrón dentro de `prefijos_id()`) para en la
primera mayúscula. Los ids reales son `hitoD_fase1_ediciones_requieren_navegador`
y `encargoEG_ruptura_enoe_y_descriptores_pendientes_navegador`. Verificado
que no cambia su clasificación (ninguno de los dos es una fuente de todos
modos, categoría (c) abajo) ni el conteo total (471 sigue siendo 471 via
`grep -c` y via `yaml.safe_load`, dos mecanismos). Se reporta porque
podría afectar a otro id en otro contexto — no se auditó el universo
completo de 471 en busca de más mayúsculas, fuera del perímetro de este
acto.

### Clasificación de los 62

| clase | n | qué son |
|---|---|---|
| **(a) emparejable a mano** — la fuente SI está en el catálogo, el id no la nombra | **19** | 16x módulos ENSANUT 2024 (`nse_*`, `adultos_*`, `hogar_*`, `integrantes_*`, `menores_*`, `utilizadores_*` — el id nombra el módulo del cuestionario, no la encuesta) · 2x `enbiare2021_*` (canónico `ENCUESTA NACIONAL DE BIENESTAR`, confirmado en `alias-fuentes.yaml`) · 1x `r7_3_pub_beneficiarios_bienestar_csv` (canónico `PUB`, confirmado — la falla de match es mecánica: `prefijos_id()` sobre un id que empieza `r7_3_...` extrae solo `{"R"}` porque el regex `^[a-z]+` para en el primer dígito, y `"R"` nunca compara contra el alias `"PUB"`; la colisión entre la convención de nombrar payloads `RX_Y_...` y la de extraer prefijo-antes-del-primer-digito es la causa) |
| **(b) hueco real del catálogo** — la fuente NO está en ningún inventario resuelto | **34** | 28x `engasto_2012_*`/`engasto_2013_*` (ENGASTO) · 3x `enasic2022_*`/`enasic_2022_*` (ENASIC) · 1x `r2_1_ecco_reporte_se_2023` (ECCO) · 1x `r8_1_contraloria_social_2019_2025_csv` (Contraloría Social/SICS) · 1x `r7_4_r7_5_acled_hdx_demonstration_events` (ACLED/HDX) |
| **(c) no es fuente** | **9** | 3x notas declarativas que reusan el esquema de `manifiesto.yaml` sin campo `archivo`/`sha256` (`nota_metodologica_rotulo_pareada`, `hitoD_fase1_ediciones_requieren_navegador`, `encargoEG_ruptura_enoe_y_descriptores_pendientes_navegador` — las tres traen un campo `hecho:` en vez de `usado_para:`/`archivo:`) · 1x `descargamasiva_382026_131650_xml` (el manifiesto de 576 URLs de la canasta CPV 2020, un mecanismo, no una encuesta) · 5x `descargamasiva_3072026_*` (el paquete lanzador `DescargaMasivaApp.exe` + `.xml` + `leeme.txt` en si — el propio `usado_para` de cada uno dice literalmente "paquete lanzador... NO microdato") |
| **total** | **62** | ok |

⚠️ **Corrección de memoria, no de este acto.** Antes de clasificar los 5
`descargamasiva_3072026_*`, mi contexto traía una atribución previa
(identificados como ENUT) de un acto anterior (M-2). El propio
`usado_para` de las cinco entradas la contradice, textual: "paquete
lanzador 'Descarga Masiva' de INEGI (`DescargaMasivaApp.exe` +
`DescargaMasivaOD.xml` + `leeme.txt`), NO microdato". Se reporta la
clasificación derivada del manifiesto, no la heredada.

Para (b): **ENGASTO y ENASIC no son huecos ciegos** — ambos aparecen,
por nombre, en inventarios reales, pero bajo una sección explícitamente
titulada "Fuentes que se sospecha existen pero no pudieron confirmarse"
(`inventario_fuentes_capital_social_mexico.md:11`,
`inventario_fuentes_cultura_valores_opinion_mexico.md:306`) — un anexo
deliberadamente fuera de la extracción numerada que usa
`tests/catalogo.py`. El corpus ya tiene 28 (ENGASTO) + 3 (ENASIC)
payloads reales con hash — más confirmación de la que el propio anexo
reclama tener. La brecha no es "nadie sabe si existe": es "alguien ya
lo bajó y el catálogo canónico nunca se actualizó para reflejarlo".
ECCO, Contraloría Social/SICS y ACLED/HDX si son huecos ciegos —
ausentes de los 11 inventarios, de `alias-fuentes.yaml`, de
`catalogo_unico.json` y de `catalogo_derivado.json` — consistentes con
su origen fuera de las 11 inventarios (surgieron del barrido público de
17 condiciones / CONF-17, un proceso de búsqueda distinto).

### Cruce U1 x catálogo, por slug

```
slugs únicos bajo /programas/ (U1):            136
fuentes canónicas con slug_portal declarado:    34
SLUG_SIN_FUENTE (slug del portal sin fuente):  102
fuentes canónicas SIN slug_portal:              94   (128 - 34)
```

**Los dos huecos, distintos:**

- **102 de 136 slugs del portal no corresponden a ninguna fuente del
  catálogo** (`Cnije`, `accidentes`, `cngspspe` [361 URLs], `justiciapenal`
  [558 URLs], `mcs` [177 URLs], `intercensal` [131 URLs]… lista completa
  en el TSV del PASO 2). Mayoría reconocible como programas de Censos de
  Gobierno (`cn*`), Estadísticas de Salud/Vitales (`natalidad`,
  `mortalidad`, `nupcialidad`), Encuestas Económicas (`em*`, `e*` de
  establecimientos) — dominios que los 11 inventarios de este programa
  (enfocados en persona/hogar, ciencias sociales) nunca se propusieron
  cubrir. No es "el catálogo está roto": es "el catálogo cubre menos
  dominios que el portal", ya declarado en U1.
- **94 de 128 fuentes canónicas no tienen slug_portal verificado** — sin
  evidencia de que vivan en este apartado (pueden vivir en otra puerta,
  no ser de INEGI, o simplemente no haber sido cruzadas todavía por
  MAP-1, que verificó por contenido, no solo por nombre, y dejó 94 sin
  intentar).

⚠️ **Prohibido adivinar un slug** (instrucción del encargo) — cumplido:
no se agregó ningún `slug_portal` nuevo, ni siquiera para los tres casos
donde el cruce por nombre es obvio a simple vista: `enbiare` (5 URLs),
`engasto` (46 URLs) y `enasic` (3 URLs) aparecen como slugs reales del
portal y caen en los 102 SLUG_SIN_FUENTE de todos modos, porque
`alias-fuentes.yaml` no trae su `slug_portal` verificado por contenido
— pese a que `ENCUESTA NACIONAL DE BIENESTAR` (ENBIARE) ya es una fuente
canónica resuelta (categoría (a) arriba). Es un hallazgo distinto del de
(b): no es que el catálogo no sepa que ENBIARE/ENGASTO/ENASIC existen —
es que nadie corroboró todavía que su slug de portal sea ese, por
contenido, como pide la convención de `alias-fuentes.yaml`. Se deja así,
sin resolver, para que MAP-1 (o quien reabra esa tabla) lo verifique con
la disciplina que esa tabla ya declara.

## PASO 2 — El mapa (`data/mapa-fuentes-2026-08-06.tsv`)

134 filas: las 128 canónicas de `data/catalogo_unico.json` + 6 fuentes
identificadas en el PASO 1 que están en disco pero fuera del catálogo de
128 (`ENGASTO`, `ENASIC`, `ECCO`, `CONTRALORIA_SOCIAL_SICS`, `ACLED_HDX`,
y `BANXICO` — esta última sin payload propio en este corpus, candidata
de `R3.4`, agregada solo para no dejarla invisible en el mapa). Construido
con un script de solo lectura (no toca `tests/`) que junta cinco fuentes
ya derivadas en este mismo acto: `catalogo_unico.json` (identidad,
dominios, operabilidad) + `alias-fuentes.yaml` (nombre/institución/
`slug_portal`, y los tokens para repetir el emparejamiento id→canónico) +
`data/manifiesto.yaml` (409 automáticos + 19 manuales del PASO 1 = 462
ids con fuente) + `data/integridad-corpus-2026-08-06.tsv` (heredado de
INT-1, **no recalculado** — `C-HASH`/`C-ESTRUCTURA` por id) +
`data/indice-descarga-masiva-2026-08-05.tsv` (URLs por `slug_portal`).

**Verificación de cierre de cuentas, no pedida por el encargo pero barata
de correr:** Σ `n_payloads_disco` de las 134 filas = **462** (= 409+19,
exacto). Σ `n_payloads_integros` = **436** — los 441 `COINCIDE+INTEGRO`
totales de INT-1 menos 5, y los 5 reconcilian: son los cinco paquetes
lanzador `descargamasiva_3072026_*` (categoría (c) del PASO 1, ZIP real,
`testzip()` pasa, pero no son una fuente y quedan fuera del mapa a
propósito). Ningún payload del corpus queda sin explicar.

### Distribución de `estado_cobertura`

| estado | n |
|---|---|
| NO-EN-ESTE-APARTADO | 87 |
| EN-DISCO-NO-CUBRE | 33 |
| EN-PORTAL-SIN-BAJAR | 10 |
| EN-DISCO-DAÑADA | 2 |
| EN-DISCO-SIN-VERIFICAR | 2 |
| **CUBIERTA** | **0** |
| **total** | **134** |

**`EN-DISCO-DAÑADA` (2):** `ENVIPE` (76 payloads, 72 íntegros — los 2
`NO_COINCIDE` de INT-1 son `envipe_2023_*_dta`/`_sav`) y `ENDIREH` (40
payloads, 37 íntegros — el `NO_COINCIDE` de INT-1 es el `_dbf` de 2016;
la diferencia adicional de 2 son entradas `COINCIDE+NO_EVALUADO/ERROR`
de la misma fuente, no relacionadas con el hallazgo de INT-1).

**`EN-DISCO-SIN-VERIFICAR` (2):** `PUB` y `CONTRALORIA_SOCIAL_SICS` — los
dos `.csv` que CONF-17 bajó, familia `otros` en la taxonomía de INT-1
(*"ninguno — SOLO hash"*), `COINCIDE` pero `NO_EVALUADO` estructuralmente.
No es un hallazgo nuevo — es exactamente la clase que INT-1 ya nombró,
ahora atribuida a una fuente en vez de a un id suelto.

**`CUBIERTA` = 0, y es la cifra más importante de este paso.** Solo 10 de
las 134 filas tienen alguna de las 14 reglas abiertas citándolas como
candidata (`ENIF`, `ENCUP`, `INE`, `LAPOP`, `LATINOBARÓMETRO`, `PUB`,
`ECCO`, `CONTRALORIA_SOCIAL_SICS`, `ACLED_HDX`, `BANXICO`), y de esas 10,
**ninguna** trae un veredicto `VIABLE` pleno en
`forense/cruce-catalogo-fichas-v2_0.md` — el mejor caso alcanzado
(`ENIF`/`R3.4`, `ENCUP`+`INE`/`R7.1`) es `VIABLE ECOLÓGICO`: agregado
(entidad×año, casilla/sección), no a la unidad que el Umbral de la ficha
pide. Se clasificaron deliberadamente como `EN-DISCO-NO-CUBRE`, no
`CUBIERTA` — la distinción entre "viable pleno" y "viable ecológico" no
se colapsa, siguiendo la misma disciplina de no-colapso que rige
`estado_cobertura` en general. El resto de las candidatas (`LAPOP`/
`LATINOBARÓMETRO` para `R8.3`, `PUB` para `R7.3`, `ECCO`/`CONTRALORIA_
SOCIAL_SICS`/`ACLED_HDX` para `R2.1`/`R8.1`/`R7.4`/`R7.5`) traen
veredicto `NO ENLAZA` o `NO EXISTE el diseño` en el documento base, o son
hallazgos posteriores de `CONF-17` (byte a byte, pero sin adjudicar
enlace) que `cruce-catalogo-fichas-v2_0.md` (4/ago) no alcanzó a
incorporar por fecha. **Detalle regla-por-regla en el PASO 3.**

### `EN-PORTAL-SIN-BAJAR` (10) — primer vistazo a la cola del PASO 5

`ENADIS` (10 URLs) · `ENVE` (12) · `CNGF` (28) · `CNGMD` (219) ·
`ENAFIN` (9) · `ENAID` (4) · `ENCOAP` (12) · `ENCRIGE` (3) · `ENE` (13) ·
`ENVI` (4). Ninguna de las 10 gatea alguna de las 14 reglas abiertas —
la cola priorizada del PASO 5 vendrá de otro lado (payloads que sí
gatean pero están dañados o sin verificar, no de este grupo).

## PASO 3 — Las 14 reglas abiertas, de qué depende cada una

Cruzado contra el mapa del PASO 2 (`estado_cobertura` reutiliza el mismo
vocabulario de seis valores, no uno nuevo) y contra
`forense/cruce-catalogo-fichas-v2_0.md` (v2.0 vigente) + la actualización
posterior de `CONF-17` para `R2.1`/`R7.3`/`R8.1`/`R7.4`/`R7.5`/`R10.2`,
que abrió cuatro candidatas byte a byte que v2.0 (4/ago) no tenía. Este
acto **no adjudica** — no decide si una fuente satisface una ficha en el
sentido del veredicto `RX.Y`; solo hereda el veredicto estructural
(`VIABLE`/`VIABLE ECOLÓGICO`/`NO ENLAZA`/`NO EXISTE`) que
`cruce-catalogo-fichas-v2_0.md` ya registró, sin recalcularlo.

| regla | condición del Umbral | fuentes candidatas | estado_cobertura (PASO 2) | ¿CUBIERTA? | qué falta exactamente |
|---|---|---|---|---|---|
| `R1.4` | Prima pagada por marca sobre sustituto funcional equivalente, D/E ≤ A/B | ninguna | — | No | Panel de consumo D/E popular — dato propietario, ninguna de las 6 clases lo trae. Hueco declarado desde el pre-registro mismo. |
| `R2.1` | Diferencia <20pp tasa de reporte voluntario de errores, jerarquía tradicional vs. plana, canal pareado | `ECCO` (CONF-17, byte a byte) | EN-DISCO-NO-CUBRE | No | `ECCO` trae percepción 0-100 (45 factores), no tasa conductual de reporte de errores. Dato organizacional propietario para lo que sí pide la ficha. |
| `R2.2` | Rotación y productividad ±10pp, liderazgo autoritario no-benévolo vs. benévolo | ninguna | — | No | Mismo hueco que `R2.1` — dato propietario, STPS/registros laborales no lo tienen a esta granularidad. |
| `R3.4` | Condiciones A∧B∧C (ADR-37): reproducción + mecanismo + anti-confusión, series Banxico SPEI/CoDi | `ENIF` (respaldo individual) + `BANXICO` (agregado, no catalogado) | EN-DISCO-NO-CUBRE (`ENIF`) / NO-EN-ESTE-APARTADO (`BANXICO`, sin payload) | No | Condición A sola es `VIABLE ECOLÓGICO` (agregado nacional). B y C exigen que una fuente separe riesgo fiscal de fricción de uso **dentro del mismo producto** — ninguna fuente pública lo hace, por diseño, no por falta de búsqueda. |
| `R7.1` | Diferencia participación <15pp, concurrente vs. no concurrente, mismo electorado | `ENCUP` + `INE` | EN-DISCO-NO-CUBRE (`ENCUP`) / NO-EN-ESTE-APARTADO (`INE`, sin payload propio, sin `slug_portal`) | No | `INE` libera casilla/sección (mejora vs. v1.0, que solo tenía municipio) pero sin abrir el instrumento no se confirma si el cruce concurrente/no-concurrente viene pre-armado o se construye sección por sección. |
| `R7.3` | RDD Pensión del Bienestar con efecto electoral independiente de aprobación presidencial, ≥5-10pp nacional | `PUB` (CONF-17) + `INE` | EN-DISCO-SIN-VERIFICAR (`PUB` — CSV, familia otros, sin chequeo estructural) / NO-EN-ESTE-APARTADO (`INE`) | No | El diseño RDD en sí — nadie lo construyó. Falta verificar si `PUB` es nominal y geolocalizable (granularidad "ambigua, declarada" desde `B-4b-alpha`, no resuelta por `CONF-17`, que solo confirmó el hash). |
| `R7.4`/`R7.5` | ≥25% de casos documentados de respuesta colectiva ante agravio cruzan la predicción ambiental | `ACLED_HDX` (CONF-17) | EN-DISCO-NO-CUBRE | No | Verificar si `ACLED_HDX` cubre México con la granularidad/ventana temporal que la ficha pide — `CONF-17` abrió el archivo (`mexico_demonstration_events_by_month-year.xlsx`) pero no confirmó la variable. |
| `R8.1` | Contribución ≥60% sostenida ≥2 años sin sanción/monitoreo, fuera de usos y costumbres | `CONTRALORIA_SOCIAL_SICS` (CONF-17) | EN-DISCO-SIN-VERIFICAR (CSV, familia otros) | No | Verificar si `SICS` trae variable de sanción/monitoreo a nivel comité — `CONF-17` abrió el archivo (520 bytes, sospechosamente pequeño) sin confirmar contenido más allá del hash. |
| `R8.2` | Participación sostenida ≥2 ciclos, incumplimiento <10%, tandas digitales entre desconocidos | ninguna | — | No | Dato de plataforma propietario — mismo hueco declarado en el pre-registro. |
| `R8.3` | Diferencia <10pp disposición a transar con desconocidos, enforcement alto vs. bajo | `LAPOP` + `LATINOBARÓMETRO` | EN-DISCO-NO-CUBRE (ambas) | No | **No es un problema de fuente.** `conf.06` sigue abierto ([[project_modelado_mexicano_conf06_status]] — cerrado por `ADR-64` pero la condición A/C3 de `R8.3` sigue abierta) — añadir `LAPOP`/`Latinobarómetro` sería una sexta cifra sin reconciliar. Bloqueado, no vacío. |
| `R10.1` | Diferencia <15pp rechazo indirecto, interlocutor superior vs. inferior, muestra mexicana no universitaria | ninguna | — | No | No hay fuente de datos en el catálogo — el ancla es un estudio académico puntual (Félix-Brasdefer), no una serie ni un registro. |
| `R10.2` | Diferencia <10% rotación/desempeño, retro pública vs. privada, controlando sector | `ECCO` (reutilizada de `R2.1`, sin candidata propia) | EN-DISCO-NO-CUBRE | No | Mismo hueco que `R2.1`/`R2.2` — nada propio que buscar aquí distinto de lo ya evaluado en `R2.1`. |
| `R10.3` | Aumento <15pp disposición a testificar tras protección efectiva a testigos, zona insegura | ninguna — bloqueo ético declarado | — | No | Nada — bloqueo ético (solo dato secundario ya publicado y agregado es admisible; nunca recolección primaria en zona de violencia activa). Correcto no buscar más. |

**Cuenta final, la cifra que pide el encargo:** de las **14 reglas
abiertas, 0 tienen hoy una fuente en estado `CUBIERTA`**. 6 tienen al
menos una fuente con payload en disco que no cierra la condición
(`R2.1`, `R3.4`, `R7.1`, `R7.4`/`R7.5`, `R8.3`, `R10.2`) — todas
`EN-DISCO-NO-CUBRE`. 2 tienen una fuente con payload sin verificar
estructuralmente (`R7.3`, `R8.1`). Las 6 restantes (`R1.4`, `R2.2`,
`R7.3`-diseño, `R8.2`, `R10.1`, `R10.3`) no tienen ninguna candidata con
payload en disco — de esas, 3 no tienen candidata alguna en ningún
catálogo (`R1.4`, `R2.2`, `R8.2` — dato propietario declarado
inalcanzable desde el pre-registro mismo), 1 no tiene fuente en el
catálogo de datos por ser estudio académico (`R10.1`), 1 está bloqueada
por diseño ético y no debe buscarse (`R10.3`), y 1 (`R7.3`) tiene el
diseño mismo — no la fuente — como lo que falta.

## PASO 4 — Los doce lugares, tabla de autoridad

⚠️ **La cifra "doce" no reconcilia con el propio listado del encargo.**
Contando el bloque de los 11 inventarios como **un solo lugar** (así los
trata `tests/catalogo.py`, con un parser): U1=1 + U2=5 + U3=4 + U4=6 =
**16**. Contando los 11 inventarios por separado: **26**. Ninguna de las
dos formas de contar da doce. No se fuerza la cifra — se reporta el
listado íntegro que el encargo mismo dio, con la discrepancia declarada.

### Los 16 lugares y qué pregunta responden de verdad

| lugar | qué pregunta responde (de las 5) | ¿manda o es derivado? | quién lo cita / de qué depende |
|---|---|---|---|
| `data/indice-descarga-masiva-2026-08-05.tsv` | ¿qué ofrece el portal? | **Manda** — sola en su pregunta, sin competencia | Este acto (PASO 0/1). Nadie más lo usa todavía. |
| 11× `data/inventarios/inventario_fuentes_*.md` | ¿qué existe? | **Manda** — fuente primaria, autoría humana, dominio por dominio | `tests/catalogo.py` los parsea; todo lo demás de U2 deriva de aquí |
| `data/catalogo_unico.json` | ¿qué existe? (vista deduplicada) | Derivado — regenerable por script | `tests/dedup.py`, `tests/cruce_operables.py` (fuera de perímetro) |
| `data/catalogo_derivado.json` | ¿qué existe? (vista con repetición, 201 filas) | Derivado — regenerable por script | Insumo de `catalogo_unico.json` |
| `data/inventarios/alias-fuentes.yaml` | identidad canónica + `slug_portal` (pregunta propia, no una de las 5) | Derivado de los 11, pero **manda** para identidad canónica — nadie más la resuelve | `tests/dedup.py`, `tests/catalogo.py:acron()`, este acto (PASO 1/2) |
| `data/catalogo-fuentes-v2_0.md` | ¿qué existe? (síntesis por **clase**, no por dominio) | Ni deriva por script ni es primario — **re-presentación curada a mano** de `inventario_fuentes_clase-fuente-mexico.md` (uno de los 11) | Citado por `forense/cruce-catalogo-fichas-v2_0.md:22` como insumo |
| `data/manifiesto.yaml` | ¿qué tenemos? (qué **declaramos** tener, con metadata rica: `usado_para`, `url_origen`) | **Manda** para intención de uso — pero no es la única verdad sobre presencia física | `tests/manifiesto.py --verifica`, este acto y todos los anteriores |
| `data/censo-raices-2026-08-06.tsv` | ¿qué tenemos? (qué hay **físicamente**, listado directo del filesystem) | **Manda** para presencia física — trae columnas `en_manifiesto`/`id_manifiesto` que existen exactamente para auditar al manifiesto, no al revés | Ninguna otra tabla lo cita todavía — subutilizado |
| `data/manifiesto-staging.yaml` | ¿qué tenemos? (candidatos, pre-promoción) | Subordinado, declarado — *"Este archivo NO es `data/manifiesto.yaml`. Ninguna entrada... se promueve automáticamente"* | Alimenta a `manifiesto.yaml` vía `--promueve` |
| `forense/notas/2026-08-06-int1-integridad-corpus.md` + `data/integridad-corpus-2026-08-06.tsv` | ¿está íntegro? | **Manda** — primer barrido completo, sin competencia | Este acto (Arranque §6, heredado sin recalcular) |
| `forense/cruce-catalogo-fichas-v2_0.md` | ¿para qué sirve? (candidata por **condición del Umbral**, las 27 fichas) | **Manda** para esa pregunta específica — pero es una entre seis artefactos que tocan "para qué sirve" | Este acto (PASO 3), `CONF-17` lo cita como insumo |
| `forense/cruce-catalogo-fichas-v1_0.md` | ¿para qué sirve? (versión anterior, pregunta más angosta: solo las 18 sin fuente) | **Superseded, declarado por su propio sucesor** — *"no se edita, queda como está"* | Ya no se cita para nada activo; PR #137/#138 (cerrados sin fusionar) intentaron borrarlo |
| `forense/hitoD-preregistro-v2_0.md` (bloque `## Registro de veredictos archivados`) | ¿para qué sirve? (el veredicto `RX.Y` real — `A`-`E` — no solo la candidatura) | **Manda para el veredicto final** — el encargo no lo listó entre los 12/16, y es la autoridad más fuerte de las seis para esta pregunta | `CONF-17`, este acto (PASO 0/U4), todo acto de Hito D |
| `forense/censo-estimabilidad-coeficientes-v1_0.md` | ¿para qué sirve? (pregunta más angosta: identificabilidad de los 15 coeficientes de generador) | **Manda** para esa sub-pregunta específica — no compite con `cruce-catalogo-fichas`, la complementa un nivel más abajo (coeficiente, no ficha) | `gobernanza` ADR-57(c) |
| `data/diseno-muestral.yaml` | ¿para qué sirve? (pregunta distinta: ¿se puede inferir correctamente con esta fuente? — ponderador/estrato/UPM) | **Manda** para usabilidad estadística — prerequisito de uso, no asignación a regla | Nace del `PARO` de PR #96 (ENCUP paso 2, ADR-46) |
| `milpa/procedencia.yaml` | ¿para qué sirve? (la más angosta: de dónde salió cada uno de los 144 **números** ya en el modelo) | **Manda** para procedencia numérica final — el eslabón más cercano al modelo, no al catálogo | Bloque A del canon lo exige |
| `corpus/indice.yaml` | **Ninguna de las 5** — responde una sexta pregunta no listada: ¿dónde vive el documento/reporte X? | Ni manda ni deriva — *"generado a mano contra `ce0658c`; no hay script de regeneración"*, **stale por construcción** | Claves `report:X`/`validacion:Y` de `milpa/*.yaml` |

### Propuesta por artefacto sin autoridad clara (propuesta, no ejecutada)

- **`data/catalogo-fuentes-v2_0.md` vs. `inventario_fuentes_clase-fuente-mexico.md`** — el primero re-presenta al segundo sin declarar que lo hace. Propuesta: **el propio `catalogo-fuentes-v2_0.md` debería decir, en su cabecera, "vista curada de `inventario_fuentes_clase-fuente-mexico.md`, no fuente independiente"** — no retirar, solo declarar la relación.
- **`forense/cruce-catalogo-fichas-v1_0.md`** — ya declarado superseded por su propio sucesor (v2.0) y por dos PR de borrado cerrados sin fusionar (#137, #138 — la mesa lo intentó y no se completó). Propuesta: **retirar**, si la mesa confirma que ninguna pregunta activa depende de la pregunta más angosta que v1.0 respondía (las 18 sin fuente) y que v2.0 no repite.
- **`corpus/indice.yaml`** — stale por construcción, sin script de regeneración, contra un commit (`ce0658c`) que ya no es HEAD. Propuesta: **regenerar con script** (si `milpa/*.yaml` sigue citando claves `report:X`/`validacion:Y` activamente) **o retirar** (si nadie más lo resuelve a mano). No se puede saber cuál desde este acto — perímetro no permite tocar `milpa/`.
- **`forense/hitoD-preregistro-v2_0.md` (el bloque de veredictos)** — no tiene un defecto, tiene un problema de **visibilidad**: es la autoridad real para "¿para qué sirve?" y el encargo, al enumerar los 12/16 lugares, no lo incluyó. Propuesta: **ningún cambio al archivo — solo nombrarlo explícitamente la próxima vez que se enumeren "los lugares que contestan para qué sirve"**, para que no vuelva a quedar fuera por accidente de enumeración.
- **`data/manifiesto.yaml` vs. `data/censo-raices-2026-08-06.tsv`** — ninguno de los dos declara mandar sobre el otro, y sus columnas de cruce (`en_manifiesto`/`id_manifiesto` en el censo) existen precisamente para que uno audite al otro. Propuesta: **ninguna edición — solo usarlas juntas la próxima vez que alguien pregunte "¿qué tenemos?"**, tal como este mismo acto lo hizo en el PASO 2 (`n_payloads_disco` salió del manifiesto; pudo haber salido del censo también, y no se cruzaron entre sí en este acto — trabajo para uno futuro, no de este).

## PASO 5 — La cola de descarga, derivada y no opinada

Priorizada por cuántas de las 14 reglas abiertas gatea cada fuente
(PASO 2/3), luego por bytes declarados. Bytes vía sonda `-r 0-0 -D -`
contra el `Content-Range` (no `-I`, no descarga — mismo método que
`CONF-17` ya usó en este corpus). **No se bajó nada.**

### Prioridad 1 y 2 — las que más reglas gatean, y ninguna es de INEGI

| fuente | reglas que gatea | bytes | por qué no está en esta cola de INEGI |
|---|---|---|---|
| `INE` | `R7.1`, `R7.3` (**2** — el máximo de todo el mapa) | no aplica | Instituto Nacional Electoral — otra institución, otro portal. No tiene `slug_portal` ni URL en este índice porque no es de INEGI, no porque falte buscarlo aquí. |
| `ECCO` | `R2.1`, `R10.2` (**2**) | ya en disco, íntegro | Ya bajado por `CONF-17` (Secretaría de Economía, no INEGI) — no hay nada que bajar; lo que falta es de contenido (percepción, no tasa conductual), no de bytes. |
| `BANXICO` | `R3.4` (**1**) | no aplica | Banco de México — otra institución. Misma situación que `INE`. |
| `PUB`, `CONTRALORIA_SOCIAL_SICS` | `R7.3`, `R8.1` (**1** c/u) | ya en disco | Ya bajados por `CONF-17` (portales de Bienestar / Función Pública, no INEGI). Pendientes de **verificación manual**, no de descarga — ver abajo. |
| `LAPOP`, `LATINOBARÓMETRO` | `R8.3` (**1** c/u) | ya en disco, íntegro | Bloqueadas por `conf.06` sin reconciliar — bajar más no ayuda. |
| `ENIF`, `ENCUP` | `R3.4`, `R7.1` (**1** c/u) | ya en disco, íntegro | El hueco es de diseño/instrumento (§PASO 3), no de bytes. |

**Hallazgo del paso, y es el más citable:** de las fuentes que sí gatean
alguna de las 14 reglas, **ninguna tiene hoy un byte pendiente de bajar
del portal de INEGI** — o ya están en disco (7 de 10), o viven en un
portal que este mecanismo (Descarga Masiva de INEGI) no cubre y nunca
cubrirá (`INE`, `BANXICO`, 3 de 10). La cola "para las 14 reglas" no es
una cola de descarga — es una cola de **verificación manual** y de
**sourcing fuera de INEGI**.

### Verificación manual pendiente (no es descarga, es apertura de lo ya bajado)

- **`PUB`** (`r7_3_pub_beneficiarios_bienestar_csv`, 114,046 bytes,
  `COINCIDE`+`NO_EVALUADO`) — abrir el CSV y confirmar si trae
  identificador nominal/geolocalizable, la pregunta que `R7.3` necesita
  y que ni `B-4b-alpha` ni `CONF-17` resolvieron (solo confirmaron hash).
- **`CONTRALORIA_SOCIAL_SICS`** (`r8_1_contraloria_social_2019_2025_csv`,
  **520 bytes** — declarado así en el propio manifiesto, confirmado aquí
  contra el TSV de integridad) — 520 bytes para "2019-2025" a nivel
  nacional es sospechosamente pequeño para lo que `R8.1` pide (inventario
  de comités). Candidato fuerte a **re-verificar si el payload real se
  bajó completo** antes de gastar tiempo abriéndolo.

### Re-bajar (dañadas, heredado de INT-1 — distinto de "nunca se bajó")

| id | bytes declarados (manifiesto) | bytes en el servidor HOY (sonda `-r 0-0`) | nota |
|---|---|---|---|
| `envipe_2023_bd_envipe_2023_dta` | 16,221,003 | **16,221,003** — coincide exacto | El servidor no cambió; lo que está mal es el archivo local (truncado), no el tamaño declarado. |
| `envipe_2023_bd_envipe_2023_sav` | 26,786,689 | **26,786,689** — coincide exacto | Igual que arriba. |
| `endireh_2016_bd_mujeres_endireh2016_sitioinegi_dbf` | 2,895,872 | **102,349,631** — **35.34×** lo declarado | Confirma en vivo, hoy, la cifra que el propio encargo ya traía ("35× lo declarado") — dos mediciones independientes, mismo número. El archivo local no es solo "diferente", es una fracción (2.8%) del real. |

Ninguna de las tres gatea directamente alguna de las 14 reglas abiertas
(`ENVIPE` gatea `R7.2`, `ENDIREH` no aparece como candidata de ninguna
de las 27 fichas) — van a la cola por integridad del corpus, no por
presión de regla.

### Pendiente de herramienta (no de descarga)

5 zips de `ENNViH` ola 1 (2002) en `DEFLATE64` — el archivo está bien
(`COINCIDE`), el directorio central se lee, pero Python 3.14 no trae
decodificador `DEFLATE64` y no hay `unzip`/`7z` en este entorno (INT-1,
heredado sin re-verificar). No gatean ninguna de las 14. Acción: instalar
`7z` o `unzip` en el entorno, no volver a bajar nada.

### Las cinco canastas sin generar — ningún agente puede hacer esto

Tabulados (15,318) · Indicadores (392) · DENUE (486) · Sala de prensa
(71) · INV (3) — el zip de cada canasta no tiene URL persistente; se
genera al momento, por sesión, del lado del portal (mismo mecanismo que
ya produjo `DescargaMasivaOD_582026_171540_NACIONAL_7930url.xml` para
Microdatos). Receta de un clic, para el usuario, no para un agente:
entrar al apartado correspondiente en Descarga Masiva → generar la
canasta → guardar el `.zip`+`.xml` resultante en `Descargas MX` →
`tests/manifiesto.py --escanea` lo recoge solo. Ninguna de las cinco
gatea directamente alguna de las 14 reglas abiertas (son canastas de
tabulados/indicadores agregados, no microdato persona/hogar) — van al
final de la cola.

### Fondo de la cola — `EN-PORTAL-SIN-BAJAR`, cero reglas, 10 fuentes

`ENADIS` (219,466 B) · `CNGMD` (6,967,040 B) · `ENAID` (4,683,150 B) ·
`ENVI` (327,168 B) · `CNGF` (171,360 B) · `ENCOAP` (63,166 B) · `ENE`
(13,840 B) · `ENAFIN` (6,452 B) · `ENCRIGE` (6,116 B) · `ENVE`
(2,513 B). ⚠️ Las últimas tres (`ENAFIN`, `ENCRIGE`, `ENVE`) probaron
contra una URL con `/ejemplo/` o `Ejemplo_` literal en la ruta — mismo
patrón ya nombrado para `ENAPROCE`/`ENESTYC` (bases "ejemplo"/"ciega" de
pocos KB). No se afirma que sean dummy sin abrir el archivo — se marca
la sospecha, no se adjudica.
