ENCARGO · ACTO GEN2-C0-D-CORRECTIVO · LA ADJUDICACIÓN SE CORRIGE POR SUCESIÓN — las cifras se quedan; el destino deja de decir más de lo que la evidencia sostiene

Cabecera: CAJA (UBUNTU), Opus · COMPUERTA: ninguna · redactado contra origin/main = be6b852e+ (re-deriva; #650 pudo fusionar) · candidatos: deriva. Verificación ancla de dirección: medidor.py:378-385 reproduce la escalera exacta que la revisión describe (veto de universo · NO-DISCRIMINA→EXPLICADO · else→EXPLICADO).

FIRMA DE MESA, 9/sep/2026: «chutate este» → revisión adversarial de Astra VALIDADA por dirección; mandarlo es ordenar la corrección. P0 la commitea verbatim (forense/notas/…REVISION-ADVERSARIAL-PR649-astra.md, procedencia declarada) — su reproducción independiente (260 sha verificados, 152/152 exactos ejecutando medir con los bytes) es la más fuerte que el programa ha tenido y entra al acta.

PIEZAS (los 5 pasos de la revisión, como especificación; ajustes de circuito abajo) P1 · Spec v1.2 + medidor sucesores. Ramas EXHAUSTIVAS y explícitas, sin else: CORPUS-AYUDA→REFUTADO-CON-ALCANCE · CORPUS-ESTORBA→CONFIRMADO-CON-ALCANCE · NO-DISCRIMINA→INCONCLUSO (tokens finales: dirección los fija contra registro-rotulos al redactar la spec). La primaria se deriva SOLO de L↔L en U_LL — el ranking de M no puede vetarla; el efecto de igualar universos sale como diagnóstico separado, jamás como adjudicación. Sucesor de alcance separado del signo: la condición vuelve a la del encargo original (capturas pre-GEN2 → sucesor nombrado = NC-0077 reutilizada, cero deuda nueva). Guardia de identidad: CORRESPONDE contrasta también id_celda e indice, no solo variante. P2 · CALC-C0D-v3 (sucesor_de: v2) sobre los mismos 260 insumos: las cifras DEBEN permanecer (+4.6978 pp, IC95 [−0.8022, +11.2747], n=13; secundarias intactas) — solo cambia el destino, esperado INCONCLUSO. Falsadores: los dos contraejemplos de Astra, ejecutados (M cambia de ranking con primaria concluyente → la primaria manda; corpus-ayuda inequívoco → su rama propia), el caso real, y los controles previos de inválidos/cobertura conservados. P3 · Corrección por sucesión, nunca hacia atrás. Enmienda fechada sobre nota y ADR-426 (original intacto), n=14→13 celdas comunes en el enunciado de las secundarias, y el texto sustantivo de la revisión se adopta verbatim como lectura canónica («…el IC95 va de −0.80 a +11.27: la comparación no discrimina la dirección… Igualar el universo reduce la diferencia marginal; no demuestra que el resto esté explicado por la métrica.»). Prohibido introducir prueba de equivalencia o umbral post-hoc.

PERÍMETRO: forense/prereg-caja/C0D-MARCADOR-spec-v1_2* · data/corrida0/CALC-C0D-MARCADOR-v3/** + TSV re-derivados · enmiendas fechadas en nota de #649 y gobernanza (ADR-426) · forense/notas/ (revisión verbatim + nota propia) · forense/no-corrido.tsv (NC-0077 cita) · 0-bis · cascada. No toca sellos v1/v2, capturas, ni recaptura alguna. Si escribes fuera, PARA. CONTADOR: sí — CALC sucesor sellado; cuenta_gen2 viaja como firma en el lanzamiento (estándar de siempre) o no cuenta y se dice. ## NO-CORRIDO obligatoria.

---

## NO-CORRIDO / RESERVAS

| qué (verbatim del encargo) | por qué | impacto | sucesor |
|---|---|---|---|
| **P0** — *«P0 la commitea verbatim (`forense/notas/…REVISION-ADVERSARIAL-PR649-astra.md`, procedencia declarada) — su reproducción independiente (260 sha verificados, 152/152 exactos ejecutando medir con los bytes) es la más fuerte que el programa ha tenido y entra al acta.»* | **`PARO-PREMISA`** — el texto verbatim **no llegó**. No venía en el mensaje que lanzó el acto; no está en el árbol (`ls forense/notas/ \| grep -i astra` → dos notas **ajenas**: `2026-09-08-cierre-escalamiento-lateral-astra.md` y `2026-09-09-PROPUESTA-GOBIERNO-DECISIONES-PENDIENTES-astra.md`); `gh pr view 649 --json comments` → **cero comentarios**. Un texto verbatim no se reconstruye de memoria ni de paráfrasis: escribirlo sería fabricar la procedencia que el propio paso existe para asentar. | El acta **no** recibe el documento fuente ni su reproducción independiente (260 sha, 152/152). **Ninguna otra pieza se detuvo:** P1, P2 y P3 se ejecutaron enteras. Lo que sí queda verbatim y con procedencia son los fragmentos que el propio encargo trae —incluida la lectura canónica adoptada en P3 y la descripción de la escalera—, archivados por 0-bis A.3; y **esa descripción se verificó contra el código** (`medidor.py:378-385` la reproduce exacta), que es la comprobación que el encargo mismo pedía. | **`NC-0082`** — mesa o dirección pega el texto verbatim y un acto de trámite lo commitea con procedencia declarada. Hasta entonces, la procedencia asentada es el encargo archivado. |
| **CONTADOR** — *«CALC sucesor sellado; `cuenta_gen2` viaja como firma en el lanzamiento (estándar de siempre) o no cuenta y se dice.»* | **`DECISIÓN-DE-MESA-PENDIENTE`** — **no viajaba ninguna firma** en el lanzamiento, y **esto es el decirlo**. Además, y esto no lo levanta ninguna firma sola: la regla `E.1` clasifica `CALC-C0D-MARCADOR-v3` **`envuelto_legacy = SI`** (derivado por el comando de la casa) porque sus insumos **son** los artefactos GEN1 del duelo. `decisiones.tsv` **no se toca**: escribir ahí sería falsificar una firma de mesa. | El CALC sucesor **sí** quedó sellado (162 `RESULT`, `verify` REPRODUCE), pero `cuenta_gen2 = NO`. `N_resultados_gen2_sellados` no se mueve. Único contador que se mueve: **`corredores_envueltos_legacy` 10 → 11**. | **`FP-368`** — fila propia y **no** glosa de `FP-367`, porque `FP-367` nombra `CALC-C0D-MARCADOR-v2` **y nada más** y `v2` quedó `SUPERADO→v3`: por el estándar `FIRMA-CONTADOR` (autoridad, fecha, **objeto**), una firma sobre `v2` no alcanza a `v3`. **Absorbe las dos preguntas de `FP-367` sin cambiarles una coma; nada queda huérfano** — si mesa firma, `FP-367` cierra con la misma firma por objeto superado; si declina, cierran juntas. |
| **Re-captura de `L_CORPUS` con corpus GEN2** — implicada por `RESULT-C0D-ADJUDICACION-SUCESOR`, que este acto hace **no** depender del signo | **`DIFERIDO-A:acto de re-captura L_CORPUS con corpus GEN2`** — fuera del perímetro, que dice literalmente *«ni recaptura alguna»*. | El brazo `L+corpus` se midió contra el corpus del **1–2/sep/2026**; entraron **435 payloads** después. Mientras no se re-capture, todo lo que el marcador diga sobre «el corpus» es sobre ese corpus fechado — cualquiera que sea el destino del veredicto. | **`NC-0077`, REUTILIZADA y no duplicada** (cero deuda nueva, como el encargo exige). La fila lleva enmienda fechada: cambia su **estatuto**, de «deuda que el veredicto no exige» a «el sucesor que el alcance nombra». Sigue `ABIERTA`. |
| **Suite — el cuarto `FAIL` de `T-YAMEDIDO`** | **`FUERA-DE-PERÍMETRO`** — reincidencia exacta de `NC-0080`, declarada el día anterior. Depende del huso horario: sin `TZ=UTC` da 4 `FAIL` y `LÍNEA BASE ROJO`; con `TZ=UTC` da **3 `FAIL` y VERDE**. El cuarto es sobre `forense/encargos/2026-09-08-GEN2-TRAMITE-BANDEJA.md`, un encargo **ajeno** a este acto. | Ninguno. Ningún archivo de este acto ni la regla `T-YAMEDIDO` están en su perímetro, y no se tocan. Se declara para que nadie lo lea como daño de este PR. | **`NC-0080`** (`ACTO GEN2-C0-D`), ya `ABIERTA` — no se duplica. |
| **Filas de `forense/no-corrido.tsv` con 10 columnas en vez de 12** (líneas 8, 33-34, 38-42, 63) | **`FUERA-DE-PERÍMETRO`** — **preexistentes**, verificado contra `HEAD` antes de tocar el archivo: `git show HEAD:forense/no-corrido.tsv \| awk -F'\t' 'NF!=12'` las reporta idénticas. No las introdujo este acto. | Ninguno para este acto: sus tres escrituras (`NC-0077` enmendada, `NC-0081`, `NC-0082`) tienen las 12 columnas. Se declara para que el defecto no se atribuya a este PR ni se pierda. | **`SIN-ASIGNAR`** — se deja anotado aquí, sin abrir fila propia: es una observación de higiene de un archivo ajeno al objeto de este acto, no deuda que este acto contraiga. |
