# Nota forense · ENCARGO EXPLORA-2 · 2026-08-06/07

Encargo archivado en `forense/encargos/2026-08-07-explora-2.md` (commit `5ce24f4`, Estado `VIVO` al arrancar). Ejecutado en worktree `/home/pc0/wt-explora2-1786042858`, rama `explora2-1786042858`.

## ARRANQUE (crudo)

```
$ git -C /home/pc0/Modelado-Mexicano rev-parse --show-toplevel
/home/pc0/Modelado-Mexicano
$ git -C /home/pc0/Modelado-Mexicano log -1 --format="%h %s"
302ac5a Merge origin/main into sesion/cal-conf-faseb-pos4-envipe-paso1
$ git -C /home/pc0/Modelado-Mexicano status
On branch sesion/cal-conf-faseb-pos4-envipe-paso1
Untracked files: data/raw
```
Clon existente confirmado, no se clonó uno nuevo.

```
$ git fetch --all --prune && git log -1 --format="%h %s" origin/main
aa87828 Merge pull request #149 from Josanoforo/claude/explora-puertas-banxico-qnm3cn
```
Base declarada por el encargo: `215af44` (confirmado ancestro de origin/main). Real: `aa87828`. **#149 (EXPLORA-1) y #150 (MAP-2) YA estaban fusionados** al arrancar este acto — el encargo los daba como "probablemente pendientes"; no lo estaban. No es PARO (§ARRANQUE.2 lo anticipa explícitamente). Worktree creado sobre `origin/main` real: `git worktree add -b explora2-1786042858 ../wt-explora2-1786042858 origin/main` (con un `error: could not write config file .git/config: Device or resource busy` transitorio x2 — causa confirmada: `wt-verif3-1786042795` se creó ~63s antes, VERIF-3 corriendo en paralelo exactamente como anticipaba el encargo; el worktree quedó íntegro, verificado con `git worktree list` + `git status` limpio en el nuevo worktree).

```
data/raw: ausente en el worktree nuevo -> enlazada: ln -s /home/pc0/mm-corpus/raw data/raw
data/raices.local.yaml: copiada del clon base (552 bytes, gitignorada, no se hereda entre worktrees)
```

Firma de entorno (3 partes, A.2):
```
$ echo "${CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE:-sin_variable}"
sin_variable
$ curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://www.inegi.org.mx/
200
$ curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://www.banxico.org.mx/
200
$ ls data/raw/ | head -1
BD_ENCUCI2020_dbf.zip
```
No PARO REAL. Cleared to proceed.

PASO 0 (archivar el encargo verbatim) hecho **antes** de esto — commit `5ce24f4`.

## Tarea A — Banxico (resultado completo en `data/exploracion-puertas-2026-08-08.tsv`, filas A.1–A.4)

**A.1 — token.** Se buscó un token SIE ya obtenido en data/raices.local.yaml, `data/manifiesto.yaml`, `.env*`, `forense/notas/*.md`: no existe ninguno (grep sin resultados salvo menciones narrativas en notas previas, ninguna con token real). Este acto inicialmente no llenó el formulario de registro de Banxico por decisión de autorización (usar el correo del usuario sin pedir permiso primero) — **esa razón resultó premisa incorrecta, corregida en el cierre**: `WebFetch` directo a `https://www.banxico.org.mx/SieAPIRest/service/v1/token` (ejecutado después, con autorización explícita del usuario para el registro) muestra que el mecanismo real **no pide correo ni ningún dato personal** — es una página con CAPTCHA (imagen de seguridad + campo de código) y botón "Generar token", que un humano resuelve en menos de un minuto y este agente no puede resolver (no hay herramienta de reconocimiento visual de captcha en este entorno). Fila TSV corregida: `NO-ACCESIBLE` por límite estructural del agente, no por autorización de correo; receta manual corregida para reflejar el mecanismo real (captcha, no formulario de correo).

En su lugar se encontró una vía alterna que **sí resuelve el objetivo real de A.1** (localizar los ids de serie): la página `codi.org.mx/secundarias/estadisticas.html` expone 5 enlaces directos a `SieInternet/consultarDirectorioInternetAction.do?...&idCuadro=CFxxx` — un sistema de consulta de cuadros SIE que **no exige token** (distinto de `SieAPIRest`, que sí lo exige). Los 5 `idCuadro` (CF881 cuentas validadas, CF882 cuentas con pago/cobro, CF884 operaciones, CF885 monto, CF883 dispositivos enrolados) se bajaron, se leyeron y se registraron en el manifiesto — ver TSV y `data/manifiesto.yaml`.

Limitación honesta: la vista sin JS de `SieInternet` solo expone los **últimos 3 días** por defecto (25/26/27-jul-2026 al momento de la consulta, 6-ago-2026). Se intentó ampliar el rango con parámetros `fechaInicio`/`fechaFin` en la URL — el servidor respondió `HTTP 400 "Ha tecleado un caracter no válido..."`, confirmando que esos no son los nombres de parámetro correctos. **No se repitió la conjetura** (misma disciplina que el encargo exige para no repetir los intentos fallidos de idSerie de PR#149). La serie histórica completa queda como receta manual (navegador con el selector de fecha JS, o SieAPIRest con token real).

**A.2 — las tres candidatas + Informe Anual.**
- `codi.org.mx/secundarias/estadisticas.html`: **EXISTE-SATISFACE**. Es la mejor puerta — desde ahí salen los 5 `idCuadro`.
- `codi-avances-banco-mexico.html`: fecha real confirmada byte a byte — "Última Actualización: 10 de julio de 2026", coincide exacto con el snippet que citaba EXPLORA-1. Contenido: avance institucional (35 participantes SPEI con CoDi, tiempos de procesamiento), no cifras de cuenta/usuario final.
- Informe Anual sobre las Infraestructuras de los Mercados Financieros: la landing (`infraestructuras-mercados-fin.html`) no trae los PDF directos — enlaza a una subpágina de listado (`.../informes-infraestructuras-mer.html`) que si los trae, con año identificado por el `<td>` contenedor de cada enlace "Texto completo". **Confirmado: NO existe edición 2025** al 6-ago-2026 — la más reciente es **2024**. Se bajaron las 6 ediciones 2019–2024 (2494KB–4305KB c/u, PDFs reales verificados con `file`).

La edición 2024, sección 2.3.3 "CoDi" (pp. ~17–19), trae las cifras primarias más ricas de todo el acto: dic-2024, 9,903 operaciones promedio diarias (−18.2% vs dic-2023), \$10.6M pesos promedio diario operado; cierre de 2024: 35 participantes SPEI con CoDi, 51 terceros desarrolladores, 1,020 comercios enrolados (Cuadro 2.1: 998 vía institución/desarrollador + 22 desarrolladores), 86 entidades gubernamentales (Cuadro 2.2: 52 municipal + 28 estatal + 6 federal); cuentas que usaron CoDi en el último trimestre de 2024: 25.0 mil cobradores + 216.4 mil pagadores + 16.4 mil categoría combinada ≈ **257.8 mil cuentas activas trimestrales**. Los órdenes de magnitud de operaciones/monto **corroboran de forma cruzada e independiente** los valores diarios leídos en vivo de CF884/CF885 (jul-2026): ~7-12K operaciones/día y ~\$3.5-13M/día respectivamente — dos fuentes primarias distintas, mismo fenómeno, compatibles.

**A.3 — la discrepancia (3.09M oct-2024 vs 21.8M/17.8M sept-2025).** **ACOTADA, NO RECONCILIADA DEL TODO** — dicho con esas palabras, per ADR-37 (no se fuerza una reconciliación falsa).

Hechos primarios en mano (todos con fecha, fuente y hash en el TSV/manifiesto):
- Ninguna cifra oficial de Banxico, ni actual (jul-ago 2026) ni del Informe Anual 2024, llega a millones de "cuentas CoDi" en sentido estricto. El máximo oficial encontrado es ~257.8 mil cuentas activas trimestrales (cierre 2024, fuente: Informe Anual IdMF 2024, no una fuente de segunda mano).
- La serie más cercana en orden de magnitud a **21.8M** es "Total de dispositivos Android enrolados" (CF883): 24,150,606–24,161,851 (25-27 jul 2026), un STOCK acumulado creciendo ~5K/día. Retrocediendo ~11 meses (sept-2025 → ago-2026) a ese ritmo, un valor cercano a 21.8M en sept-2025 es plausible por trayectoria, aunque no se pudo verificar el dato histórico exacto (sin acceso a rango de fechas, ver limitación de A.1). **17.8M** (Fintechexpert, misma fecha) podría ser un corte distinto (p.ej. un solo sistema operativo, o fecha de corte ligeramente distinta) — no verificado.
- La serie más cercana en orden de magnitud a **3.09M** es "Cuentas que han realizado al menos un pago" (CF882): 2,797,082–2,799,154 (25-27 jul 2026), también un STOCK (definido por Banxico como cálculo por día operativo SPEI). **Contradicción no resuelta**: si es un stock monótono creciente, el valor de oct-2024 (21 meses antes) debería ser MENOR que el de jul-2026, no mayor — pero 3.09M > 2.80M. Posibles explicaciones no verificadas: cambio de metodología/rebase de la serie, 3.09M mide otra cosa (p.ej. "cuentas validadas" en un momento de mayor definición de universo, o incluye pago+cobro), o la cifra de prensa no reproduce. No se adjudica cuál.

Conclusión honesta: **son probablemente dos magnitudes distintas** (dispositivos enrolados vs. cuentas-con-operación), no dos fechas de la misma magnitud — pero la pieza que cerraría esto del todo (el valor histórico exacto en las fechas de prensa) no se pudo obtener sin token+idSerie o sin un navegador con JS.

**A.4 — obligación de reporte.** Series/encuestas que miden CoDi/SPEI vistas en este acto: los 5 cuadros SIE CF881/882/884/885/883; el Informe Anual IdMF (anual, sección 2.3.3 dedicada a CoDi); **ENIF 2024** (ya en el corpus, 5 olas, 22/22 COINCIDE+ÍNTEGRO, no re-descargada — respaldo previo de PR#149); **ENDUTIH** (ya en el corpus desde sesión previa, id `endutih2024_bd_dbf_zip` — encuesta de TIC en hogares; **no se verificó en este acto** si su cuestionario pregunta específicamente por CoDi/pagos digitales, queda para quien adjudique la ficha R3.4). No se identificaron series adicionales de SPEI puro (fuera de CoDi) en las páginas consultadas.

## Tarea B — RNM y microdato económico (resultado completo en TSV, filas B.1–B.5)

**B.1 — los dos enlaces sin resolver.**
- `catalog/518/download/13137` → **HTTP 500**, cuerpo completo (1109 bytes): `Error An Error Was Encountered RESOURCE_NOT_AVAILABLE!` — mensaje específico del propio sistema RNM (no un 500 genérico), catalogado pero no disponible server-side. No hay archivo, no hay payload que registrar.
- `catalog/518/download/19219` → **HTTP 200**, `Content-Disposition: attachment; filename="INFORME_FINAL_LEVANTAMIENTO.pdf"`, `Content-Type: application/octet-stream`, 3,942,875 bytes, PDF real de 26 páginas verificado con `file` y `pdftotext`. Título real (primera página): *"Encuesta Nacional sobre Productividad y Competitividad de las Micro, Pequeñas y Medianas Empresas 2018 (ENAPROCE 2018) — Informe Final del Levantamiento de la Información"*. Es documentación metodológica/de campo, **NO microdato**. No fue "el hallazgo más caro de la ventana" — se cierra la pregunta, pero en negativo.

**B.2 — política de acceso literal, catálogos 330 y 518.** Extraída del nodo `id="metadata-data_access"` de cada página (no de un snippet, byte a byte), texto **idéntico en sustancia** entre las dos ediciones:

> "Condición de acceso: La información de la ENAPROCE a nivel microdato es confidencial; la descarga de la base de datos de este proyecto es restringida al público en general. Sin embargo, es posible acceder a la información de microdatos de manera indirecta a través del siguiente mecanismo: Laboratorio de análisis de datos para servidores públicos del Estado Mexicano, funcionarios de organismos internacionales e investigadores y estudiantes calificados..."

Esto **confirma byte a byte** la hipótesis que EXPLORA-1 dejó sin verificar (SIN-FETCH): acceso INDIRECTO exclusivamente vía Laboratorio, nunca descarga directa. Clasificación A.4: `NO-ACCESIBLE` para ambas ediciones.

**B.3 — ENESTYC en el buscador interno RNM.** `catalog/search?search=ENESTYC` y la variante `?ps=ENESTYC` devuelven ambas el catálogo **completo sin filtrar** (825 de 825 operaciones estadísticas — mismo total en los dos intentos, confirmando que el parámetro se ignora silenciosamente en vez de dar error). No hay `<form>` ni `<input>` server-renderizados en la vista sin JS — el buscador real es JS/AJAX, mecanismo no determinado. **No se probó un tercer nombre de parámetro** (misma disciplina anti-conjetura que la Tarea A aplica a idSerie). Clasificación: `NO-ENCONTRADO` (del mecanismo, no de la encuesta — no se afirma que ENESTYC no esté catalogada). Receta manual: abrir `rnm/index.php/home` en navegador y usar el buscador visual.

**B.4 — CKAN datos.gob.mx.** `package_search?q=enaproce` y `?q=enestyc`: ambas devuelven `success:true, count:0` — **cero datasets**, confirmado, adjudicable. Hallazgo de entorno no anticipado por el encargo: `datos.gob.mx` / `www.datos.gob.mx` fallan TLS en esta caja por defecto (`curl: (60)`, diagnóstico verboso: `SSL certificate OpenSSL verify result: unable to get local issuer certificate` — falta la CA intermedia "Let's Encrypt E8" en `/etc/ssl/certs/ca-certificates.crt` de este entorno). **Se verificó explícitamente que NO es un bloqueo de sandbox**: el mismo fallo ocurre con `dangerouslyDisableSandbox: true` activo. Se verificó el certificado real con `curl -v -k`: `subject: CN=datos.gob.mx`, vigente 20-may-2026 a 18-ago-2026, `subjectAltName` coincide exacto, emisor Let's Encrypt E8 — un certificado legítimo, solo con una cadena de confianza incompleta en este entorno local. Con ese diagnóstico en mano (no a ciegas) se usó `curl -k` puntualmente para esta consulta de solo-lectura a un API JSON público. Declarado aquí explícitamente, no oculto — es una gap de trust store local, no evidencia de MITM ni de bloqueo de red.

**B.5 — Laboratorio de Microdatos, `Solicitud_Uso.pdf`.** Es un formulario PDF **interactivo** de 4 páginas (no un documento de solo lectura): datos personales, datos de la institución, datos del supervisor/director de tesis (si aplica), información completa del proyecto de investigación (objetivo, beneficio social, programas de información requeridos, metodología, justificación, resultados esperados, plan de difusión), preferencia Laboratorio físico vs. Procesamiento Remoto, software requerido, autorización de publicación del proyecto.

**Corrección a EXPLORA-1 (PR#149):** el TSV de #149 afirmaba como receta manual "enviar a microdatos@inegi.org.mx". Se buscó esa cadena (y cualquier "@") en el texto completo extraído del PDF real: **no aparece en ningún lugar del documento**. El único correo presente es `unidad.transparencia@inegi.org.mx`, y es específicamente para ejercer derechos ARCO de protección de datos personales — no para enviar la solicitud de microdatos. El mecanismo real de entrega del formulario (portal en línea, correo distinto, entrega presencial) **no quedó determinado en este acto**. Esto es exactamente el tipo de brecha SIN-FETCH que EXPLORA-2 existe para cerrar: EXPLORA-1 nunca abrió este PDF, y la cifra de correo parece derivada de un snippet de búsqueda no verificado. Clasificación: `NO-ACCESIBLE` (condición institucional se mantiene; el detalle del correo de envío queda como pendiente, no como hecho verificado).

## Qué cierres de `forense/cruce-catalogo-fichas-v2_0.md` cambian con esto

*(No se edita ese archivo — solo se nombra qué cambiaría; la reclasificación es de otro acto.)*

- **`forense/cruce-catalogo-fichas-v2_0.md:67` (R3.4).** El veredicto actual — "VIABLE ECOLÓGICO para A; NO ENLAZA para B/C" — **no cambia de categoría**, pero deja de ser una proyección y pasa a estar **evidenciado**: la condición A (reproducción con series agregadas de Banxico SPEI/CoDi) tiene ahora id de cuadro real, valores reales, fecha real y PDF primario en disco (criterio de éxito del encargo, cumplido). La fila podría actualizarse para citar los 5 `idCuadro` y el Informe Anual 2024 como evidencia de A, en vez de remitir solo al pre-registro. La discrepancia 3.09M/21.8M/17.8M que motivaba parte de la incertidumbre queda acotada (probablemente dos magnitudes, no dos fechas) pero no cerrada del todo — no ameritaría bajar de "Parcial" a "Completo" en la columna de cobertura.
- **`forense/cruce-catalogo-fichas-v2_0.md:58-59,109` (R2.1/R2.2/R10.2).** Sin cambio directo — estas tres son sobre datos organizacionales propietarios (clima laboral, liderazgo, rotación), no sobre ENAPROCE/ENESTYC en sí, y este acto no tocó esa clase de dato. Sí es relevante para la **contradicción documental sin resolver** que EXPLORA-1 ya había señalado (`forense/hallazgos.md`, entrada DESC-1 2026-08-05, dice que estas tres "dependen PARCIALMENTE de las bases ejemplo de ENAPROCE/ENESTYC", mientras este archivo las marca NO EXISTE sin relación declarada): si esa dependencia parcial fuera real, el hallazgo B.2 de este acto (ENAPROCE/ENESTYC microdato es NO-ACCESIBLE, no simplemente ausente) se aplicaría también a la porción dependiente — pero la contradicción en sí sigue sin que este acto la adjudique.
- No se identificó ninguna fila de `forense/cruce-catalogo-fichas-v2_0.md` que este acto mueva de NO EXISTE / EXISTE-NO-SATISFACE a EXISTE-SATISFACE de forma completa — el hallazgo más caro posible (microdato real en `download/13137` o `/19219`) no se materializó.

## Perímetro

Escrito: `data/exploracion-puertas-2026-08-08.tsv`, `forense/notas/2026-08-08-explora2.md` (este archivo), `forense/hallazgos.md` (append, ver commit siguiente), `data/manifiesto.yaml` (22 entradas nuevas), `data/raw/R3.4_Banxico_CoDi_SPEI/**` y `data/raw/R2.1_R2.2_R10.2_RNM_microdato/**` (payloads, corpus compartido vía symlink). No se escribió fuera de esa lista — verificado con `git status` antes de cada commit.

## Addendum — cierre: merge, push, PR, y un defecto de entorno nuevo

Post-fusión de PR #153 (VERIF-3): `git fetch` mostró `origin/main` avanzado a `1da4f0e`. `git merge-tree` confirmó cero conflictos reales (única sección "changed in both" fue `forense/hallazgos.md`, apéndice en ambos lados sin solaparse); `git merge origin/main` — auto-merge limpio (`2f9093b`), confirmado con `tail forense/hallazgos.md` que las dos entradas (EXPLORA-2 y VERIF-3) quedaron intactas. Suite re-corrida post-merge: `tests/check.py --baseline` VERDE (18 FAIL·95 WARN, idéntico), `tests/test_svystat.py` 4/4.

`git push -u origin explora2-1786042858`: el push de objetos/refs funcionó (`git ls-remote` confirmó `HEAD` remoto = local, exacto, dos veces). PR abierto: **#154**. Pero **el config de tracking de la rama no persistió** — tres intentos distintos (`push -u`, `push --set-upstream`, `git config` directo ×2) fallaron todos con el mismo error: `error: could not write config file /home/pc0/Modelado-Mexicano/.git/config: Device or resource busy`, y `git config --get branch.*.remote/.merge` quedó vacío después de cada intento. Mismo síntoma que el `error` transitorio de `git worktree add` en el ARRANQUE (§ARRANQUE), pero esta vez persistente a través de 5 intentos, no transitorio — evidencia de contención sostenida en `.git/config` del clon base compartido por escritura concurrente de otras sesiones (VERIF-3 y posiblemente otras, activas al mismo tiempo). **No es pérdida de datos**: cada `push` transfirió el commit real y se verificó byte a byte contra el remoto; lo único que no persiste es la conveniencia de `branch.<rama>.remote/.merge` (afecta solo a un futuro `git push`/`git pull` sin refspec explícito en *este* worktree, que fallaría con "no upstream branch" hasta corregirse a mano). Per §0.5 (no pelear con infraestructura ajena en bucle): no se reintentó una sexta vez; se documenta como hecho de entorno, no de este acto. Fila nueva candidata para el catálogo de firmas de entorno del proyecto: contención de `.git/config` bajo worktrees concurrentes silencia escrituras de config sin fallar el comando que las contiene (`git worktree add`/`git push` reportan éxito en su función principal pese al error de config intercalado) — quien audite después no debe asumir que la ausencia del error implica que el config se escribió, ni que su presencia implica que el push falló.

Encargo marcado `CONSUMIDO` con PR #154 (commit `4594765`), per PASO 0.

## Addendum 2 — token Banxico obtenido por el usuario: reconciliación A.3 mucho más fuerte

Tras el cierre, el usuario resolvió a mano el captcha en `https://www.banxico.org.mx/SieAPIRest/service/v1/token` y compartió el token. Guardado en `data/secretos.local.yaml` (gitignorado, patrón igual a raices.local.yaml; copiado también al clon base `/home/pc0/Modelado-Mexicano` para que worktrees futuros lo hereden). Validado contra una serie conocida: `GET .../series/SF60653/datos/oportuno` con header `Bmx-Token` → `200`, dato real de tipo de cambio devuelto.

**Segunda corrección de premisa del encargo original.** Las dos rutas de "catálogo completo" que el encargo citaba (`/SieAPIRest/service/v1/series/catalogoSeries`, heredada del propio encargo, y `/doc/catalogoSeries`, heredada del texto original de EXPLORA-1) **no son endpoints reales**: la primera responde `HTTP 400 {"mensaje":"Formato de series incorrecto.","detalle":"Formato de series debe ser SX####[-SX####][,SX####[-SX####]]"}`; la segunda es solo la página de documentación HTML. Confirmado contra la documentación oficial vía `WebFetch` (no supuesto): el API REST de SIE **no tiene endpoint de catálogo ni de búsqueda** — el descubrimiento de `idSerie` es exclusivamente por navegador, recorriendo el árbol de Sectores. Misma limitación estructural que el buscador de RNM en B.3 (JS/AJAX, no reproducible por `curl`). No se siguió adivinando `idSerie` (disciplina anti-conjetura que el propio encargo exige para no repetir los intentos de PR#149).

**El hallazgo real no vino del token.** Antes de seguir buscando el `idSerie` a ciegas, se revisó el contenido de un payload que este mismo acto ya había descargado y registrado en A.2 sin abrirlo a fondo: `banxico_codi_cuentas_validadas_x_mil_hab_trimestral.xlsx` (id `banxico_codi_cuentas_validadas_x_mil_hab`). Su hoja `Metadatos` declara el concepto exacto: *"Acumulado histórico del número de cuentas validadas en CoDi activas hasta el último día del trimestre"* (fuente: Banco de México, CNBV y CONAPO). Trae dos hojas de detalle — `Cifras_Estatales` (545 filas, cuentas por entidad y trimestre, 2022-T1 a 2026-T1) y `Fuente_LADA` (6,726 filas, mismo rango por LADA) — sumadas por trimestre con `openpyxl` de dos formas independientes:

| Trimestre | Nacional (suma 32 entidades) | Nacional (suma LADAs) | Diferencia |
|---|---|---|---|
| 2024-T4 (oct-dic 2024) | 20,745,799 | 20,745,799 | 0 |
| 2025-T2 | 21,500,497 | 21,500,497 | 0 |
| **2025-T3 (jul-sep 2025)** | **21,884,617** | **21,884,617** | **0** |

Las dos hojas coinciden exacto en las 17 filas trimestrales (2022-T1 a 2026-T1) — cross-check real, no un solo cálculo. **2025-T3 = 21,884,617** frente a los **21.8M** que citaba El Economista (sept-2025): diferencia de +0.39%. Esto es una reconciliación **contra fuente primaria (Banxico+CNBV+CONAPO), no contra prensa citando prensa** — el nivel de evidencia que ADR-37 pide para un veredicto A.

Lo que esto cambia para 3.09M y 17.8M:
- **17.8M** (Fintechexpert, misma fecha) sigue sin confirmar con un punto primario exacto. Hipótesis más fuerte, no verificada: subconjunto "cuentas activas" del total "validadas alguna vez" (21.8M) — mismo artículo/fecha, corte distinto.
- **3.09M** (Cobertura360, oct-2024): la propia serie "cuentas validadas" ya vale 20,745,799 en 2024-T4 — descarta que 3.09M mida esto. Más importante: **la contradicción de dirección señalada en el cierre anterior deja de serlo**. Se había marcado como contradicción que 3.09M (oct-2024) > 2.80M (cuentas-con-pago, jul-2026) "si es un stock monótono creciente". Ahora está confirmado que "cuentas validadas" **sí** es monótona (creciente en las 17/17 filas) — pero eso es una serie distinta de "cuentas con pago" (CF882), que no tiene por qué serlo. Son dos magnitudes con dinámicas distintas, consistente con la caída de -18.2% interanual en operaciones que ya traía el Informe Anual 2024 (fila A.2) — no es una contradicción sin resolver, es evidencia adicional de que son series diferentes.

TSV (fila A.3) y `data/manifiesto.yaml` (campo `usado_para` del payload) actualizados para reflejar esto. El token, aunque obtenido y validado, **no fue necesario** para este resultado — se queda documentado y disponible (`data/secretos.local.yaml`) para quien quiera perseguir el `idSerie` exacto de CF882 con acceso a navegador.

### Qué cierre de `forense/cruce-catalogo-fichas-v2_0.md` cambia adicionalmente

- **`:67` (R3.4)** — el addendum del Addendum original queda superado: la condición A ya no es "acotada, no reconciliada del todo" sin más — el lado 21.8M queda **confirmado contra primaria**, con tabla y fuente. Solo 17.8M y 3.09M quedan como hipótesis fuertes sin cierre exacto. No se recomienda subir de "Parcial" a "Completo" todavía (dos de tres cifras siguen sin punto primario exacto), pero el peso de evidencia es sustancialmente mayor que al momento del PR original.

### Nota de perímetro sobre este Addendum 2

Se tocó `.gitignore` (una línea, `data/secretos.local.yaml`) — no está en la lista de perímetro original del encargo. Declarado explícitamente en vez de hacerlo en silencio: es la forma de que el token de Banxico que compartió el usuario no quede committeado en texto plano en el historial de git. Ningún otro archivo fuera del perímetro original fue tocado.
