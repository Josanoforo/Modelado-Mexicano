═══════════ MENSAJE DE LANZAMIENTO (pegar como primer mensaje en la sesión de Claude cloud/NUBE, con los TRES adjuntos) ═══════════
/acto
FIRMA DE MESA, 9/sep/2026, verbatim: «revisa PR652 y dame el input para claude cloud. Revisa la propuestra de Astra para automatizaciones post calculo y dame el encargo final.» — el encargo de abajo ES esa integración, preparada por dirección; tu merge sella.
ADJUNTOS QUE VIAJAN CON ESTE MENSAJE (A.3: si falta alguno, PARA y pídelo — un encargo que cita un archivo fuera del repo sin traerlo está mal escrito):

1. Este encargo (0-bis lo archiva verbatim).
2. PROPUESTA-FINAL-AUTOMATIZACIONES-POSTCALCULOS-GEN2-2026-09-09.md (insumo externo, Astra).
3. REVISION-ADVERSARIAL-PR649.md (bytes canónicos, sha256 esperado ccd2219a5522a00db60feebb81eda6353bb9b1153dd4931c2c8df2c2212139fd — re-derívalo al recibir y pega el comando).

CONTADOR: no — este acto no mide y lo dice en una línea al cierre (regla de señal v2.3: es aparato que protege mediciones, no una sesión de medición). No viaja firma de contador porque no hay contador que firmar.
═════════════════════════════════════════════════════════════════════════════════════
ENCARGO · ACTO GEN2-REVISA-CALC · EL REVISOR APRENDE A LEER UN CÁLCULO — un bloque dentro de /revisa, calibrado contra los casos que ya dolieron; los otros cuatro contratos quedan archivados como backlog, no implementados
CABECERA · NUBE, Opus (diseño de runbook + calibración documental: juicio, no receta congelada; D-13 permite subir, nunca bajar en actos que midan — éste no mide) · NO se lanza en CAJA — la caja queda libre para el lote ENVIPE en vuelo y los siguientes lotes de datos · COMPUERTA: GATED a PR #652 fusionado (el plan de obra v1.1 y el baseline/check.py que este acto va a pisar deben estar en `origin/main`; la skill /acto lo verifica y se niega con A.13 si no está — no arranques a mano para saltártelo) · redactado contra `origin/main = 6e0381bb` (PR #651 fusionado) + `origin/pr/652` revisado por dirección · candidatos FP/ADR/NC: deriva al cierre con el comando de la casa, no heredes (al redactar: ADR máx 429 en pr/652 · NC máx 0083 — derivados, no tecleados; si el ENVIPE fusiona antes, re-deriva).
VERIFICACIÓN DE EXISTENCIA (A.8, contestada por dirección, 9/sep/2026, comandos corridos en esta sesión contra el clon): (1) ESTRUCTURA — gobiernan: `.claude/commands/revisa.md` (el revisor vigente), `tools/corrida0.py` (validadores a reutilizar: `estado`, `verify`, `registro` en lectura, `status`), `forense/no-corrido.tsv` (NC-0082), `forense/notas/` (archivo de insumos). Este encargo escribe: el bloque REVISA-CALC dentro del runbook existente, dos archivos nuevos en notas, el token de estado de NC-0082 por la rutina autorizada, 0-bis y cascada. Ningún script nuevo si instrucciones + validadores existentes bastan (perímetro de la propuesta §2). (2) CONTENIDO — `grep -n "REVISA-CALC" .claude/commands/revisa.md` → 0 coincidencias en el archivo completo (45 encabezados/puntos examinados, A.13): NO-ENCONTRADO — la especialización no existe; el revisor tiene once puntos, opción `--post-hoc` (línea 100) y la regla «NUNCA empuja commits» (línea 51): EXISTE-NO-SATISFACE para revisar un CALC. `python3 tools/corrida0.py delta` y `vigencia` → «NO-IMPLEMENTADO: … lo llena ACTO GEN2-E3» (salida cruda citada): declarados, no implementados. `lote` y `siguiente` → error de usage del parser: NO-ENCONTRADO como subcomandos. `grep "^NC-0082" forense/no-corrido.tsv` → EXISTE, ABIERTA: «P0 — commitear verbatim la revision adversarial … NO S[E TRANSPORTÓ]» — el hueco exacto que P0 cierra. Hash del adjunto 3 re-derivado por dirección: MATCH con el declarado por Astra (receta: bloque entre los marcadores INICIO/FIN, sin los marcadores, `strip` + salto final). (3) COBERTURA RETROACTIVA — `revisa.md` es anterior a los casos de calibración (#634→#651): que el runbook no los mencione no significa que no apliquen; por eso P2 los cita uno a uno.
PIEZAS (D-11: un acto = un PR = una firma; afines del mismo entorno): P0 · REGISTRO Y TRANSPORTE. (a) Archiva verbatim `forense/notas/2026-09-09-PROPUESTA-FINAL-AUTOMATIZACIONES-POSTCALCULOS-astra.md` con cabecera de procedencia ARRIBA del texto intacto («Insumo externo (ChatGPT/Astra) · entregado a dirección 9/sep/2026 · registrado por ACTO GEN2-REVISA-CALC conforme a A.3 y regla de mesa 4 · el texto de abajo no se edita»). (b) Archiva `forense/notas/2026-09-09-REVISION-ADVERSARIAL-PR649-astra.md`: cabecera de procedencia + la línea «Documento histórico, anterior a PR #651; la corrección posterior vive en #651/ADR-428 — NO se usa para reabrir defectos ya corregidos» + los bytes canónicos del adjunto 3, con el sha256 re-derivado y pegado (comando + salida, A.13). (c) Con esa evidencia, cierra `NC-0082` por la rutina autorizada (`tools/cierre_acto.py` / conciliación vigente), token en el campo `estado` — nunca en la prosa (A.16) — citando archivo + hash + este PR, con estampa de universo (A.10). P1 · EL BLOQUE REVISA-CALC DENTRO DE `/revisa` — mismo informe, mismo comentario único, cero rutina nueva, cero segundo revisor. Activación: cambios en `data/corrida0/CALC-*/`, `forense/prereg-caja/`, decisiones/vistas de corrida, afirmaciones de ejecución/sucesión/adopción en el cuerpo del PR, o citas `corrida0_*` añadidas a consumidores; un medidor compartido amplía solo a consumidores afectados demostrables — nunca replay de todo el programa; una fila cambiada del TSV global se resuelve por identidad, no obliga a revisar todos los CALC; «CONTADOR: cero» no excluye un CALC de revisión. Comprobaciones (mecánica reutilizada + lectura humana, la tabla de la propuesta §2 adaptada al runbook): identidad/sucesión (`estado`, cadena, sellos de antecesoras intactos) · congelación (orden por ascendencia de commits, no solo timestamps) · sello (validador existente) · replay (`verify` solo en entorno permitido, conservando SUS TRES componentes — contexto, resultado, veredicto — y razones: `REPLICA-RESULTADO · CONTEXTO-DISTINTO` no se colapsa) · registro/status en lectura · adopción (cita, generación, grano, consumidor real) · inferencia (¿la conclusión excede al intervalo, al universo o al diseño? ¿una secundaria modifica indebidamente la primaria?). Entorno: en NUBE se verifica estáticamente; ver un payload no levanta la prohibición de abrir microdato; lo que exige CAJA sale como `NO-VERIFICADO` con causa y receta para caja — y esa observación NO se escribe como estado nuevo de ningún CALC. Seis estados de fallo que no se confunden: sello inválido · contexto no verificable · biblioteca ausente · input no visible · fallo de ejecución · divergencia numérica. Pesos: contradicción material de identidad/sello/adopción/conclusión → BLOQUEA · frontera de entorno → NO-VERIFICADO/RESERVA (jamás degradado a aprobado) · falta legítima de firma o adopción fuera del encargo → explicación, no defecto. Contradicción post-hoc del runbook: se documenta el canal vigente (la evidencia de calibración la entrega EL EJECUTOR DE ESTE ACTO en su propio PR; el revisor sigue sin commitear ni empujar) — cualquier cambio de permisos del revisor queda explícito en el diff del runbook, nada tácito. P2 · CALIBRACIÓN DOCUMENTAL, CUATRO CASOS HISTÓRICOS, en la nota del acto: #634/#644 (semántica de verificación) · #647 (adopción con delta al grano) · #649 (el negativo: pasó la reproducción 152/152 y aun así necesitó corrección — la reproducción no valida la inferencia; el bloque humano lo hace visible y lo cita, no se exige que la máquina lo descubra) · #651 (el positivo: sucesión correcta, sellos intactos, dos RESULT de destino cambiados). Pasa cuando: detecta una afirmación material falsa · reconoce la sucesora correcta sin pedir reescritura histórica · conserva una reserva de entorno sin inventar divergencia. La calibración documental NO se rotula como replay de CAJA.
PERÍMETRO Y CONCURRENCIA. Toca: `.claude/commands/revisa.md` · `forense/notas/` (2 archivos nuevos + nota del acto) · `forense/no-corrido.tsv` (token de NC-0082 + filas nuevas si las hay) · `forense/encargos/` (0-bis) · cascada (ADR derivado, registro-rotulos, censo T22 si los archivos nuevos disparan marcadores — cada exención argumentada como en #652, no silenciada — y `tests/baseline.json` re-congelado al final). EN PARALELO: `acto/gen2-lote-envipe-1` (data/corrida0/CALC-ENVIPE-0001, prereg-caja ENVIPE, no-corrido append) — compartidos SOLO `no-corrido.tsv` (append/estado al final, sin reordenar) y `baseline.json` (re-congela DESPUÉS de merge de main y repórtalo, patrón del #652). «Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.»
LO QUE NO HACE (y es la mitad del valor): NO implementa `lote`, `delta`, `vigencia` ni `siguiente` — quedan como contratos archivados en la propuesta (backlog citable), cada uno esperando su uso próximo y su gate D-14; `delta` sigue opcional POR DECISIÓN DE MESA (regla 8) hasta autorización concreta con par y pregunta · NO añade ejecución de CAJA a NUBE · NO crea segundo comentario, revisor paralelo, script nuevo (si el runbook + validadores bastan), rutina programada, framework, caché de estados ni motor de workflows · NO reabre el #649 ni ningún defecto corregido — el documento histórico entra como fuente, no como reapertura · NO auto-adopta, auto-firma ni repara specs · NO toca los sellos de ningún CALC.
SUCESORES (se despachan cuando su uso llegue, no antes; cada uno con su D-14 respondido en el encargo que lo lance): ACTO-lote (unido al siguiente lote real de caja — ENCIG o ENIF — con la triple semántica previsualiza/`--ejecuta`/`--escribe` y reanudación por sellos) · ACTO-vigencia (cuando dirección localice las reglas temporales del uso) · ACTO-delta (cuando mesa active B-7 para un par concreto, empezando por el precedente #647) · ACTO-siguiente (solo tras piloto de utilidad contra una selección humana; si no ahorra, no se construye).
CIERRE · Cascada de /acto completa + `## NO-CORRIDO / RESERVAS` (obligatoria; «Ninguno.» si aplica) + `## CONSUMIDO` con el PR + la línea de contadores movidos (cero, y se dice).

## NO-CORRIDO / RESERVAS

Ninguno. Las cinco piezas de este acto (P0(a), P0(b), P0(c), P1, P2) se ejecutaron enteras:

- P0(a) — propuesta final de Astra archivada verbatim con cabecera de procedencia: `forense/notas/2026-09-09-PROPUESTA-FINAL-AUTOMATIZACIONES-POSTCALCULOS-astra.md`.
- P0(b) — revisión adversarial del PR #649 archivada verbatim, sha256 re-derivado dos veces (adjunto y archivo commiteado) con MATCH exacto: `forense/notas/2026-09-09-REVISION-ADVERSARIAL-PR649-astra.md`.
- P0(c) — `NC-0082` cerrada citando ambos archivos, el hash y este acto.
- P1 — bloque `## 2-bis · REVISA-CALC` añadido a `.claude/commands/revisa.md`.
- P2 — calibración documental contra #634/#644, #647 y #649→#651 en `forense/notas/2026-09-09-GEN2-REVISA-CALC-cierre.md`.

Lo que el encargo explícitamente **no pide** ejecutar aquí — `lote`, `delta`, `vigencia`, `siguiente` — está clasificado bajo `DIFERIDO-A:<sucesor>` en la sección "SUCESORES" del propio encargo (arriba), no como pieza pedida y omitida: el encargo mismo los excluye del perímetro de este acto ("LO QUE NO HACE"). No hay pieza pedida, no tocada y sin fila.

**Contadores movidos: cero, y se dice.** Este acto no mide nada sobre México (`CONTADOR: no`, declarado en la cabecera del encargo).
