ENCARGO · ACTO GEN2-T11 · RUTINAS-FIX — DIGESTO VIVO + ESCÁNER SIN AUTO-LOCK · PROPUESTA, mesa lo lanza

Cabecera: **NUBE** (repo-only) · **MODELO SUGERIDO: Sonnet** (parche pre-validado + exclusión con test; subir permitido, D-13) · redactado contra `origin/main = fbd847de` (PR #615) · ADR/FP: deriva en cierre (`tools/cierre_acto.py`) · **COMPUERTA: ninguna** — no gatea. **NO se lanza en UBUNTU/caja.** Lote D-11 de 2 piezas afines. CONCURRENCIA declarada: la adenda de T9 vuela en `claude/gen2-t9-adenda-ola-previa-y-origen-arbitro` (toca `tools/emite_m.py`, CALC demo, nota, tests propios) y T10-benchmark puede volar (toca `tools/baseline_temporal.py` + su test + `verify.yml` + propuesta) — **cero archivos comunes** con este acto. El parche `fix-digesto-fecha-y-neutralizacion.patch` va adjunto al lanzamiento; el 0-bis lo archiva junto con este encargo (A.3).

FIRMA DE MESA que este acto propaga: el lanzamiento mismo (D-A del documento AJUSTES-RUTINAS-2026-09-08) — repara el aparato de las tres rutinas que la firma del 8/sep ordenó ajustar («revisemos actualizar de una vez estas rutinas…», cabecera de `forense/rutinas.tsv`).

═══ VERIFICACIÓN DE EXISTENCIA (A.8) — contestada por quien escribe, 8/sep, contra `fbd847de` ═══
- Defecto 1 (fecha): reproducido con `python3 tools/digesto_tramite.py --sin-suite --stdout` → `TypeError: fromisoformat: argument must be str` en `seccion_i` (`tools/digesto_tramite.py:1305`; segundo sitio `:1361`). La huella de PARO del agente de rutina vive en `claude/tramite-2026-09-08` (commit `3a0fa0c4`), coincide.
- Defecto 2 (neutralización): con el defecto 1 arreglado, el auto-check `verifica()` PARA con «rótulo pelado `E7`», luego «`E3`» — las secciones H e I (nuevas de ADR-399 pieza D) copian campos de `no-corrido.tsv`/`rutinas.tsv` sin `neutraliza()` (las firmas sí lo usan: `:334`, `:347`, `:385`). **EXISTE-NO-SATISFACE.**
- Defecto 3 (lock): `tests/manifiesto.py` resuelve el lock a la raíz compartida (`:493`, ADR-399 D6) y `cmd_escanea` no lo excluye — el censo de hoy (`censo/2026-09-08`, `forense/censo-raiz/2026-09-08.txt`) lo stageó como «nuevo» con mtime = hora del propio escaneo (09:16:06). Primer censo post-D6; defecto medido, no concebido (D-14: real · material — candidata falsa perpetua en staging · barato).
- Test de regresión del digesto: `ls tests/ | grep -i digesto` → **NO-ENCONTRADO** (0 archivos). No existe nada que este acto duplique.

PIEZAS
**P1 · Digesto.** `git apply` del parche verbatim (2 sitios de fecha + neutralización en `seccion_h`/`seccion_i` con `cuenta`). Test de regresión nuevo `tests/test_digesto_fecha.py`, mínimo: (a) `seccion_i(raiz_fixture, datetime.date.today())` no lanza; (b) una fila de tsv fixture con sucesor `E7 (x)` sale neutralizada `_E7` y suma en `cuenta`. Verificación real: `python3 tools/digesto_tramite.py --sin-suite --stdout` → exit 0, pegar el pie de «Sustituciones». No se escribe `forense/digesto/DIGESTO-*.md` aquí — eso es del tick de `/tramite`, que este acto revive.
**P2 · Escáner.** `cmd_escanea` salta el basename `.manifiesto.lock` en toda raíz recorrida: no entra a «nuevos», ni a staging, ni a «páginas guardadas»; si aparece, una línea informativa `LOCK-PROPIO (excluido)`. Test en `tests/test_manifiesto_alcance.py` o archivo propio: fixture con `.manifiesto.lock` presente → 0 nuevos por él, 0 entradas de staging. No toca `data/manifiesto-staging.yaml` real ni el censo de hoy (histórico, queda como está).
**P3 · Cierre.** `python3 tools/tablero_programa.py --actualiza` (esperado por derivación de hoy: ADR `401` · FP máx `347` · abiertas FP-342/343/347 — reporta el valor real, no esta constante) · una línea en `.claude/commands/revisa.md`: `pip install -r requirements.txt` antes de derivar cifras de suite, o declarar NO-VERIFICABLE con conteo A.13 · `## NO-CORRIDO / RESERVAS` (mínimo: «huellas del día viven en 3 ramas sin fusionar — DECISIÓN-DE-MESA-PENDIENTE (D-B) — sucesor: mesa») · cascada completa de `/acto`, `## CONSUMIDO`, rama borrada.

PERÍMETRO: `tools/digesto_tramite.py` · `tests/manifiesto.py` · `tests/test_digesto_fecha.py` (nuevo) · test de P2 · `.claude/commands/revisa.md` · `forense/tablero/TABLERO-PROGRAMA.md` (derivado) · `forense/no-corrido.tsv` (solo filas nuevas) · `forense/encargos/` (0-bis) · cascada. **`forense/rutinas.tsv` NO** (vive en la rama de trámite del día). Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.

CONTADOR: **cero GEN2**. Lo que se mueve: `/tramite` deja de PARO en su siguiente tick; el censo de mañana deja de auto-stagearse.

Lo que NO hace: no edita `forense/no-corrido.tsv` histórico (los rótulos pelados de ADR-393 quedan como historia; la neutralización del digesto es el mecanismo) · no fusiona las ramas de rutina (D-B, mesa) · no toca `milpa/**`, CALC, `adquiere_cron.sh`, ni nada de T9/T10/adenda. Sucesores: el tick normal de `/tramite` (produce el primer digesto vivo post-E7-D) · trámite de una línea para asentar `GO-E5-0=SI` (D-D).
