**ADR-260923-GEN2-ESTADO-V16-1-fa47-01**, `ACTO GEN2-ESTADO-V16-1`, 23/sep/2026, NUBE (`cloud_default`, red `DENEGADA-POR-POLITICA`, corpus no montado, cero microdato), Sonnet 5, un agente de sólo lectura ayudó a localizar comandos y citas, verificados de nuevo por el ejecutor antes de citarlos, MODO ABIERTO, COMPUERTA: ninguna. Emite `canon/estado-programa-v1_16.md`: cabecera nueva (era 18–23/sep, régimen operativo nuevo: canal por PR, sello externo, auto-merge) + §0–§14 heredadas verbatim de `v1_15` (`diff` vacío sobre las líneas 17–789) + §15 nueva (status íntegro de `corrida0.py`, cierre de la etapa de retadores — 18 celdas-D con dictamen, cero `VENCE` puro —, régimen operativo, misiones de Astra, cobertura de dominios, frente público, 11 FP abiertas, decisiones de mesa del 23/sep, 32 comentarios técnicos reproducibles) + §16 nueva ("Lo que este estado no puede afirmar": `CONTADORES-2`, `FIRMAS-14`, `AUDITORIA-POST-HOC-ASTRA-1` y tres unidades de ASTRA-5 en vuelo). `canon/estado-programa-v1_15.md` **retirada del árbol por T01** (`git rm`, historia recuperable por SHA). El encargo pedía que quedara intacta y coexistiendo con `v1_16`; eso rompe `T01` (fuente única de verdad), `FAIL` nuevo no baselineado. `estado-programa` ya registró este mismo choque dos veces antes (`v1_10→v1_11`, `v1_11→v1_12`, ambos retirando la versión vieja pese al mismo pedido del encargo). Se preguntó a mesa con tres opciones (retirar como siempre, aceptar el `FAIL` en `tests/baseline.json`, o relajar `t01_single_source` con firma) y mesa decidió retirar `v1_15`, igual que los dos precedentes; `v1_15` nunca se editó, sólo se retiró (recuperable por SHA de `a43ce35`). Puntero técnico actualizado por grep en 7 sitios (`tools/cierre_acto.py`, `tests/check.py` — `RUTA_ESTADO_PROGRAMA` + comentarios + reemplazo de la entrada `v1_15` por `v1_16` en `_T22`/`_T25` —, `tools/l0_vista.py`, `.claude/commands/acto.md`, `.claude/commands/revisa.md`, `tests/test_cierre_acto.py`, `tests/test_tuberia_ids_union.py`). Nace `tests/test_estado_derivado.py` (P3): re-ejecuta cada comentario `<!-- comando: ... -->` del `estado-programa-v*.md` más nuevo y falla si no reproduce; 32/32 VERDE. Hallazgo declarado: el `[SUPUESTO]` del encargo de que `adoptados` reflejaría "el valor que `CONTADORES-2` deje" no se sostuvo — esa rama sólo tiene su 0-bis, sin PR; `adoptados=72` es el heredado de `CONTADORES-1`, declarado así en vez de forzar la atribución. `celdas_validadas` sin cambio (219, ya reflejaba `CONTADORES-1`); `cuenta_gen2 = NO-APLICA`; cero microdato, cero contadores del programa movidos por este acto. Suite `--rapido` VERDE, 0 FAIL; `--baseline --parallel` completo VERDE tras el retiro y la corrección de `T19a` (`tests/test_cierre_acto.py` y `tests/test_tuberia_ids_union.py` sin regresión).
