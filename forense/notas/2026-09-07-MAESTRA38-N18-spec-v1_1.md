# `ACTO MAESTRA38-N18` — `S7-L17-spec` v1.0 → v1.1 (corrige mapeo del bloque `a0927`)

Entorno **NUBE**, sin corpus ni red. `COMPUERTA: ninguna` (declaración
explícita). `origin/main = b1be143` (`git log -1`).

## Rótulo — colisión declarada

El encargo cita `ACTO MAESTRA38-N17` en su propia cabecera. Verificado
contra `canon/registro-rotulos.tsv` y `origin/main`: `MAESTRA38-N17` ya
está ocupado por `ACTO MAESTRA38-CARGA-LAPOP-2 · PROPAGA-FP316-FP315-CORREGIDO`
(fusionado, PR #566). Máximo real de la serie N = 17. Este acto se
ejecuta y se registra como **`MAESTRA38-N18`** — regla de la casa: cita
el rótulo equivocado, se renumera al máximo real + 1.

## Qué hace

Escribe `forense/prereg-caja/S7-L17-spec-v1_1.md` (copia de `v1.0`) con
dos cambios:

1. **§0** gana la línea: «v1.1 registra el mapeo real; la medición de
   `L17` (`ADR-357`) ya lo usó; no se re-corre.»
2. **§2** corrige el mapeo del bloque `a0927`: `v1.0` asumía
   letra=vacuna/dígito=razón; el codebook, confirmado contra
   `adultos_ensanut2024_w.dta` en `MAESTRA38-LOTE-ENSANUT`/`L17`
   (`ADR-357`), lo invierte: **letra = razón** (`a`=no había vacunas,
   `b`=no derechohabiente, `c`=no estaba quien aplica, `d`=enfermo,
   `e`=otra), **dígito = vacuna** (`1`=Influenza, `2`=Neumococo,
   `3`=Tétanos, `4`=Otra). Se propaga la corrección a §3 (dicotomización
   `RAZON_LOGISTICA`/`RAZON_NO_LOGISTICA`, que en `v1.0` usaba la
   convención invertida).

`FP-326` (ya `FIRMADA-RECIBO`, `forense/firmas-pendientes.tsv:314`) y
`ADR-357` (`canon/gobernanza-v1_15.md:6157`) son la fuente de la
corrección — citados, no re-derivados.

## Verificación A.8 (`ya_medido.py`)

```
$ python3 tools/ya_medido.py salud.vacunacion.disponible
...
MEDIDA-EN: 2026-09-06-MAESTRA38-LOTE-ENSANUT-resultados.md, tramite.yaml
```

`MEDIDA-EN`, no `NUNCA-MEDIDA` — confirma que la medición de `L17` ya
corrió con el mapeo correcto (`ADR-357`); esta pieza solo actualiza el
documento de pre-registro para que deje de contradecir lo que la caja
realmente midió. **Medición: cero** — este acto no abre ningún `.dta`,
no calcula ninguna cifra nueva.

## Integridad de `v1.0`

```
$ sha256sum forense/prereg-caja/S7-L17-spec-v1_0.md
f3aea086cee60e59fa3858334eaa6e00218e30a0b19c67fb37ae806fd586830f
$ cat forense/prereg-caja/S7-L17-spec-v1_0.sha256
f3aea086cee60e59fa3858334eaa6e00218e30a0b19c67fb37ae806fd586830f  S7-L17-spec-v1_0.md
```

Idéntico antes y después de este acto — `v1.0` y su `.sha256` quedan
intactos, sin editar.

## Perímetro

Solo: `forense/prereg-caja/S7-L17-spec-v1_1.md` + `.sha256`, esta nota,
`canon/registro-rotulos.tsv` (fila `N18`), `forense/tablero/TABLERO-PROGRAMA.md`
(recibo), y la cascada de cierre (`ADR`, `L0`, `T25` si aplica). No toca
`milpa/**`, `canon/modelo-decision-v4_0.md` ni ningún `.dta`.

## Contador

Specs de caja `20 → 21` (declarado por el encargo). Medición: cero.
