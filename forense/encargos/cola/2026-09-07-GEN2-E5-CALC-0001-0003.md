ESTADO: GATEADO
ENTORNO: CAJA
ENCOLADO: 2026-09-08 · ACTO GEN2-V213 reencola: reemplaza el cuerpo de este archivo, verbatim (texto técnico sin cambios respecto de la versión que `ACTO GEN2-T7-CIERRE` encoló), y recomputa la COMPUERTA contra `origin/main = df9336c5` bajo la regla nueva E.5 de `instrucciones-proyecto-v2_13.md`.
BITACORA:
- 2026-09-08 · GATEADO (recomputado por `ACTO GEN2-V213`). Verificado: `E5-0` sigue sin fusionar (nunca ejecutado — la pieza previa de esta misma cola sigue `GATEADO`, ver su archivo), así que la primera cláusula de la compuerta original ("E5-0 fusionado") sigue sin cumplirse por sí sola. Los seis checks de E3.1 sí están en verde en `origin/main` (`PR #608`, `ADR-394`). Añadida además, por E.5 de `instrucciones-proyecto-v2_13.md` — «ninguna corrida GEN2 real se lanza … hasta que … la readiness del marcador esté hecha» —, la misma condición que gatea a `E5-0`: `ACTO GEN2-E7` con Go/No-Go propio cerrado (`ADR-394`, D12, asigna esa readiness a E7, no a E5). **COMPUERTA (vigente): `E5-0` fusionado **y** `GEN2-E7` cerrado — ninguna de las dos hoy.** `GEN2-E5` es, de las cuatro piezas de esta reencola, la que estaba más lejos de cumplirse y sigue así.

──── CUERPO VERBATIM DEL ENCARGO (A.3) · el despachador NO lo edita ────

## E5 · ACTO GEN2-E5 · CALC-0001..0003 — solo correr, sellar y verificar (v1.3)

Cabecera: **UBUNTU (caja, corpus montado)**, mismo worktree o nuevo desde `origin/main` con E5-0 fusionado · **Opus** · COMPUERTA por producto (**el GO**): E5-0 fusionado con los tres `spec.yaml` y sidecars en `main`; los seis checks de E3.1 en verde en `origin/main` (`python3 -m pytest -q tests/test_corrida0.py` o el equivalente de `tests/check.py`, salida pegada); `preflight` VERDE en los tres CALC **desde `origin/main`** (base congelada: si `main` avanza durante el acto, no se auto-mezcla); caja limpia (E4). NO se lanza en NUBE.
Qué hace, por CALC y en orden: `preflight` → `run` (con el contrato de P2 y las rutas de P1) → `verify` (`REPRODUCE/IDENTICO` esperado; cualquier otra combinación se reporta como está) → COMMIT-2 (`ejecucion.json`, `resultados.json`, `sello.sha256`; entradas `PENDIENTE-DE-MESA` en la propuesta citando `corrida0_resultado_id`). Si una spec estaba mal contra el dato: **no se edita**; nace el siguiente id con `sucesor_de` y un tercer commit lo declara. Una pieza que PARA no tumba las otras; PARO de entorno A.2 detiene todo. Sustancia metodológica: la de los `spec.yaml` congelados en E5-0 — este acto no decide nada.
Perímetro: `data/corrida0/CALC-000{1,2,3}/{ejecucion.json,resultados.json,sello.*}` · `forense/notas/<fecha>-GEN2-E5-*.md` · `milpa/tramite-ola5-propuesta-v0.yaml` (solo `append`) · `forense/firmas-pendientes.tsv` (recibo) · cascada. **No toca specs, `spec.yaml`, `tramite.yaml` ni canon.** Si te encuentras escribiendo fuera de esta lista, PARA.
Contador: **medición: sí** — `N_corridas_selladas 0→3`, `N_resultados_sellados 0→k`. Sucesores: `SELLO-4`, E6.
