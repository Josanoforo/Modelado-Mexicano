ENCARGO · MAESTRA33-E2 · AGENTE-DESPACHO-1 — invoca /acto
SHA de redacción: af41796 (merge PR #410 / ADR-238). ENTORNO: NUBE (Claude Code) — NO UBUNTU, NO doble. COMPUERTA: el PR de MAESTRA33-E1 (rama claude/agente-tramite-d13-k01d40) fusionado en origin/main; si no está, la skill se niega con cero commits. MODELO SUGERIDO: Opus, ultrathink.
FIRMA DE MESA (verbatim, 31/ago/2026): "Siguiente automatización" — continúa la instauración del modelo D-13/ADR-237 ordenada por mesa.

VERIFICACIÓN DE EXISTENCIA (A.8, contestada por dirección contra af41796):
· forense/encargos/cola/: NO-ENCONTRADO (ls -d → no existe). Convención LISTO-NUBE/EN-CURSO: NO-ENCONTRADO — grep -rln "LISTO-NUBE|despacha|DESPACHO" .claude/ forense/encargos/ canon/gobernanza-v1_15.md → 2 archivos, ambos usan la palabra como doctrina, ninguno la implementa. Universo: árbol completo salvo .git y data/raw, 31/ago/2026.
· .claude/commands/acto.md: EXISTE-NO-SATISFACE — ejecuta UN encargo que le entregan; no elige de una cola ni corre recurrente.
· Agente de trámite (E1, en vuelo): EXISTE-NO-SATISFACE — papeleo y digesto, no ejecución de encargos.

PIEZAS (COMMIT-1 congela las specs; "el primer resultado que produzca este procedimiento es el que se reporta"):
P1 · Convención de cola: forense/encargos/cola/ con una línea ESTADO en cabecera: LISTO-NUBE → EN-CURSO (fecha+sesión) → CONSUMIDO (A.3, con su PR) o PARO-REPORTADO (con la razón verbatim). Regla dura, escrita en el runbook: a la cola SOLO se entra por PR fusionado a main — el merge de mesa es la autorización; el agente jamás ejecuta nada que no esté en main.
P2 · .claude/commands/despacha.md — skill del agente: tick = arranque ligero (clon, SHA); CANDADO: si existe un EN-CURSO en la cola o una rama remota de acto abierta, reporta y termina sin tocar nada (una sesión nube a la vez, mecánico); si no, toma el LISTO-NUBE más antiguo con ENTORNO: NUBE, lo marca EN-CURSO en un commit propio, y lo ejecuta con /acto verbatim. Nunca edita el contenido de un encargo; premisa que no se sostiene = PARO-REPORTADO, que es entregable, y no reintenta solo. Encargos ENTORNO: CAJA los lista como "esperando caja" y no los toca. CONTADOR del tick: el que muevan los encargos que ejecuta; el despacho en sí, cero.
P3 · forense/agente-despacho-v1_0.md — runbook: prompt exacto (≤5 líneas) para la segunda tarea recurrente de mesa (sugerida: 2 veces por día hábil, desfasada de la de trámite), qué esperar, y falsador a 1 mes: si ejecuta algo fuera de cola/main, o dos sesiones nube llegan a coincidir por su causa, se apaga la tarea y se anota — mismo criterio que D-10..D-13.
P4 · Commitea el PRIMER elemento de la cola con ESTADO: LISTO-NUBE: el ANEXO B″ de abajo, verbatim, como forense/encargos/cola/2026-08-31-MAESTRA33-B2-MARCO-M-SORTEA-v1_1.md.

PERÍMETRO Y CONCURRENCIA: este acto toca .claude/commands/despacha.md, forense/encargos/cola/ (nueva), forense/agente-despacho-v1_0.md, la fila de recibo en firmas-pendientes.tsv, el archivo A.3 propio y la cascada de cierre. E1 (trámite) toca tablero y digesto: renumera quien fusiona segundo. Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.
FP/ADR candidatos: siguientes libres tras lo que E1 consuma — deriva del árbol al cerrar, no heredes. CONTADOR: cero mediciones, declarado (infraestructura).
LO QUE NO HACE: no crea ni redacta encargos (eso es dirección); no ejecuta CAJA; no firma; no corre dos actos a la vez; no reintenta PAROs; no toca canon/ salvo cascada.
SUCESORES: LOTE-CAJA (FP-204 corresidencia + piezas R — lo redacta dirección y corre en TU máquina) · AGENTE-ADQUISICION-1 (caja, tercera automatización).

──── ANEXO · B″ (primer elemento de la cola; ejecutará el despachador, no este acto) ────
ENCARGO · MAESTRA33-B2 · MARCO-M-SORTEA-v1_1 — invoca /acto · ESTADO: LISTO-NUBE
SHA de redacción: af41796. ENTORNO: NUBE — NO UBUNTU. MODELO SUGERIDO: Sonnet (receta congelada, cero juicio). COMPUERTA: marco-M-congelado-v1_1.tsv y CONGELADO-M-v1_1.sha256 en main (verificados por dirección; re-verifica hash antes de sortear).
A.8: sorteo v1_0 EXISTE (sorteo-marco-M-resultados-v1_0.md); resultados v1_1 NO-ENCONTRADO (ls forense/prereg-duelo-v2/, 31/ago/2026).
COMMIT-1 (congela antes de correr): semilla = semilla_desde_sha_merge("af41796f50baad1737987b7e9a1e737c38ab85f2", "MARCO-M-v1_1") — función existente en sorteo_v2.py:191, que NO se edita (cargadores propios si hace falta, precedente del reglamento ADR-178); universo = filas elegibles de marco-M-congelado-v1_1.tsv (N_elegibles esperado 22 — deriva, no heredes); regla de tamaño = ADR-231 §e leída del árbol; celdas P0-calibración entran VERIFICACION-NO-PUNTUA (F-DD). Frase de sello incluida.
COMMIT-2: forense/prereg-duelo-v2/sorteo-marco-M-resultados-v1_1.md con semilla, comando, lista sorteada y clasificación P0/P1 por celda. CONTADOR: celdas sorteadas v1_1 de 0 → N. LO QUE NO HACE: no emite puntos M ni abre corridas-R/ (ciego). Si lanzas agentes que sean en sonnet. Necesito que todo quede cableado no quiero espacios donde después me digan que faltó unir tal o cual parte del proceso.

## CONSUMIDO

Ejecutado por [`PR #413`](https://github.com/Josanoforo/Modelado-Mexicano/pull/413) (`claude/agente-despacho-d13-0l8pdg` → `main`; commits `f07d9ae` 0-bis A.3 · `3c241ee` COMMIT-1 · `8bf40b0` COMMIT-2 · `f8cce67` cascada de cierre · `8f6e46e` fusión de `main` y renumeración · `fde590d` revisión adversarial), sellado como `ADR-241` (candidato re-derivado por el comando de la casa; se candidateó 240 contra el máximo 239, y **se RENUMERÓ a 241** al fusionar `origin/main` y encontrar que `PR #412`/`ACTO MAESTRA33-C1 · RE-SPEC-CORRESIDENCIA` había tomado el 240 — renumera quien fusiona segundo, que es la regla que el propio encargo declaró y que este acto tuvo que aplicarse a sí mismo). Compuerta "el PR de `MAESTRA33-E1` (rama `claude/agente-tramite-d13-k01d40`) fusionado en `origin/main`" **verificada mecánicamente antes de tocar nada**: `git log origin/main --merges` da el merge de `PR #411` desde esa rama exacta como tip literal de `origin/main` (`6a12244`), y `git merge-base --is-ancestor origin/main HEAD` lo confirma. El SHA de redacción `af41796` **ya no era** `origin/main`: `main` se movió a `6a12244` entre la redacción y el arranque, y la diferencia se declara aquí en vez de heredarse. **Las cuatro piezas se entregaron; ninguna PARÓ.** `P1` la convención de cola `forense/encargos/cola/`, partida en una cabecera que gestiona el despachador (`ESTADO:` anclada a principio de línea, `ENTORNO:`, `ENCOLADO:`, `BITACORA:` a la que solo se añade) y un cuerpo verbatim intocable, con la máquina de estados `LISTO-NUBE` → `EN-CURSO` → `CONSUMIDO`/`PARO-REPORTADO`; la regla dura —a la cola solo se entra por PR fusionado a `main`— es el primero de los siete guardrails de la skill, no una nota al pie. `P2` `.claude/commands/despacha.md`: candado mecánico de dos comprobaciones, selección determinista, marca `EN-CURSO` empujada ANTES de trabajar, ejecución con `/acto` verbatim, cierre en `CONSUMIDO` o `PARO-REPORTADO`. `P3` `forense/agente-despacho-v1_0.md`: spec congelada de las cuatro piezas (COMMIT-1), prompt de cinco líneas, qué esperar de cada tick y falsador a un mes. `P4` `forense/encargos/cola/2026-08-31-MAESTRA33-B2-MARCO-M-SORTEA-v1_1.md` con `ESTADO: LISTO-NUBE` — **encolado, no ejecutado**, como el encargo manda.

**Las cuatro juntas se midieron contra el árbol en vez de suponerse, y las cuatro cambiaron el diseño.** (i) `^ESTADO:` es unívoco porque el cuerpo verbatim trae su propio `ESTADO: LISTO-NUBE` a media línea: 1 coincidencia anclada contra 2 sin anclar en el mismo archivo, así que el despachador no puede reescribir la del cuerpo — sin el anclaje, marcar `EN-CURSO` habría editado el encargo, que es justo lo que `A.3` prohíbe. (ii) El archivo de la cola **es** su propio archivo `A.3` y no se duplica al ejecutarlo: `T02` agrupa por basename normalizado, descartando el directorio (`tests/check.py:183`, y el `FAIL` en la `:187`), así que la copia habría sido un `FAIL` de la suite y no una redundancia inofensiva. (iii) La cola **no** colisiona con el agente de trámite porque su lector globea `encargos/*.md` plano, no recursivo (`tools/digesto_tramite.py:366`): un encargo encolado y todavía no ejecutado no aparece como "encargo sin marca" ni tienta a la otra skill a marcarlo. (iv) El candado es de `git` puro porque **`gh` no existe en la nube** (`which gh` no imprime nada y sale con 1; `gh --version` → `command not found`): `git ls-remote --heads origin` es el estado vivo y única fuente primaria (1 rama viva al arrancar el acto, `main`, antes de que este mismo acto empujara la suya), `git for-each-ref` queda degradado a RESPALDO declarado, y "rama abierta" se define como **no contenida en `main`** para que una rama fusionada y no borrada no deje al despachador apagado para siempre.

**El modo de falla que la pieza asume a propósito.** Si una sesión muere a media ejecución su `EN-CURSO` se queda y bloquea los ticks siguientes. Es el comportamiento correcto —parados es mejor que duplicados— y el agente **no** tiene permiso para limpiarlo: decidir que una sesión murió es de mesa. Lo que la skill sí obliga es a reportar su antigüedad en cada tick bloqueado. Un candado que se bloquea en silencio es un candado roto.

**Premisas del encargo encolado, re-verificadas antes de encolarlo** — encolar un encargo muerto sería el defecto que esta pieza existe para evitar. Las seis se sostienen: los dos artefactos v1_1 están en `main` (mismo commit `77939ce`), el `sha256` del congelado re-computa idéntico al declarado por dos vías independientes, `semilla_desde_sha_merge` está en `forense/prereg-duelo-v2/sorteo_v2.py:191` con la firma citada, `ADR-231` §e está en `canon/gobernanza-v1_15.md:4143`, el resultado v1_1 no existe todavía y el v1_0 da la forma. **Una junta queda anotada en el runbook sin tocar el cuerpo del encargo**: el marco congelado trae dos columnas legibles como "elegible" que dan números distintos (`elegible` 23, `elegible_v1_1` 22); la buena es la segunda y coincide con `grado_DD` (22 `P1 PUNTUA` / 5 `P0 VERIFICACION-NO-PUNTUA`); el propio sidecar `CONGELADO-M-v1_1.sha256` lo advierte en su `nota_lectura` y el cargador existente apunta a rutas v1_0 sin conocer la columna nueva. Por eso el encargo manda "deriva, no heredes" y autoriza cargadores propios (`ADR-178`). Con 22, `ADR-231` §e cae en `15≤N<30` → `n_sorteo = ceil(22/2) = 11`, lo que `ADR-238` ya precalculó.

**Un defecto atrapado por la suite y corregido.** Los agentes de verificación de esta sesión dejaron dos archivos vacíos (`fetch_err.log`, `fetch_out.log`) en la raíz del clon al redirigir la salida de `git fetch`; `T02` los marcó como contenido idéntico bajo nombres distintos y la línea base salió ROJA con 1 entrada nueva. Eran residuo de herramienta, fuera del perímetro de este acto y no rastreados: se borraron. Queda anotado porque es exactamente la clase de ruido que un agente de fondo puede meter sin darse cuenta, y la suite fue lo único que lo vio.

**Revisión adversarial del propio acto, y el defecto de fondo que encontró.** Cinco ángulos independientes contra las cuatro piezas, cada hallazgo re-verificado por un segundo agente que re-corrió los comandos; nueve sobrevivieron y los nueve están corregidos. **El de fondo era una carrera real en el candado**: el chequeo (bloque 2) y la marca del cerrojo (bloque 4) no son atómicos, y entre los dos cabía una ventana de segundos en la que dos sesiones pasaban las dos. Se cierra con tres cosas que ahora están escritas: nombre de rama **invariante por encargo** (`claude/despacha-<CÓDIGO>`, sin la fecha de hoy — con la fecha dentro, dos sesiones que cruzaran la medianoche derivarían nombres distintos y el freno desaparecería), de modo que dos sesiones que elijan el mismo encargo chocan en el `push` y el remoto rechaza al segundo; **un `push` rechazado significa ceder**, jamás forzar ni renombrar; y la skill **re-verifica** el candado con su cerrojo ya puesto, antes de trabajar. El rechazo del `push` es el único punto atómico del tick, y por eso se le deja decidir. **El segundo**: el paso 2 de `/acto` busca `GATED a …` y estos encargos escriben `COMPUERTA:` (`grep -n GATED` sobre el primer elemento de la cola no da nada), así que la equivalencia queda escrita en la skill — sin tocar `acto.md`, que está fuera del perímetro. Los otros siete: el candado lee la cola de `origin/main` y no del árbol de trabajo, el bloque de la marca trae sus comandos `git`, `2.b` declara la fecha del bloqueo igual que `2.a`, `MODELO SUGERIDO` se lee y se reporta, los `ENTORNO: CAJA` tienen comando de listado, y **dos citas propias se corrigieron por imprecisas**: `T02` agrupa por basename normalizado en `tests/check.py:183` (la `:187` solo emite el `FAIL`), y `which gh` no imprime `command not found` —no imprime nada y sale con 1—, lo hace `gh --version`. Corregirlas importa: una cita `archivo:línea` falsa es de los defectos que este programa persigue por su cuenta, y estas se habían repetido en cinco archivos.

**La regla de concurrencia se le aplicó a este propio acto.** Mientras estaba en vuelo, `PR #412` (`ACTO MAESTRA33-C1 · RE-SPEC-CORRESIDENCIA`) fusionó primero y tomó `ADR-240` y `FP-207`. Se fusionó `origin/main` en la rama y se **renumeró a `ADR-241` y `FP-208`** — "renumera quien fusiona segundo", que es literalmente lo que el encargo declaró en su bloque de concurrencia. Los conflictos se resolvieron conservando el trabajo de los dos actos: ambas entradas en `_T25_ARCHIVOS_CONOCIDOS`, ambas filas en `canon/registro-rotulos.tsv`, la entrada de `MAESTRA33-C1` intacta como `ADR-240` con la de este acto encima, y la anotación de `L0` de `MAESTRA33-C1` conservada con la de `ADR-241` insertada delante, sin reescribir ninguna. ADR re-derivado tras la fusión: 241 únicos, máximo 241, sin huecos y sin duplicados. **No se detectó por memoria**: se detectó porque la revisión re-corrió `git ls-remote` y `git fetch`, que es la misma comprobación que esta pieza instala como candado.

`python3 tests/check.py --baseline`: **LÍNEA BASE VERDE** (19 FAIL · 154 WARN, nada nuevo frente a `tests/baseline.json`, salida 0). Cero microdato, cero descargas, cero mediciones (`CONTADOR: cero mediciones, declarado (infraestructura)`). Sin `schedule` en GitHub Actions: la recurrencia vive en la tarea de mesa. **Este acto no ejecutó el encargo que encoló**: eso es del despachador, en su primer tick.
