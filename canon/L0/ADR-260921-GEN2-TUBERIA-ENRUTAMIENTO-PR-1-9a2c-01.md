**ADR-260921-GEN2-TUBERIA-ENRUTAMIENTO-PR-1-9a2c-01 · `ACTO GEN2-TUBERIA-ENRUTAMIENTO-PR-1` · CADA PR DICE, DERIVADO DE SU DIFF Y EN CI, QUÉ TIPO DE MERGE ES**, 21/sep/2026, entorno **NUBE (`cloud_default`), Opus 5, sin sub-agentes; cero microdato, cero descargas, cero llamadas a modelos** (`forense/encargos/2026-09-21-GEN2-TUBERIA-ENRUTAMIENTO-PR-1.md`, redactado contra `fc13cdcc` y archivado verbatim por 0-bis A.3 en `9a2ca8b3`, sello de cuerpo `1082c48b…`). **MODO ABIERTO · COMPUERTA: ninguna** (declaración explícita, no dispara verificación) · **`cuenta_gen2 = NO`**. Ejecuta el **Entregable 2 del brief de TUBERÍA**, firmado el 20/sep/2026 y nunca escrito: este acto asienta la firma verbatim en `forense/firmas-pendientes.tsv` (`FP-260921-GEN2-TUBERIA-ENRUTAMIENTO-PR-1-9a2c-01`, A.12), con su línea del **auto-merge NO instrumentado** y su razón (9 % de los merges, ninguna de las renumeraciones, reevaluable si esa fracción crece). **El defecto que atrapa:** por E.2 el merge del PR que trae un bloque ES la adopción, y en `PR #943` —2 939 resultados sellados— eso no estaba escrito en ninguna parte del PR. `tools/clasifica_pr.py` deriva del diff **todas** las señales que dispara (no sólo una), la clase principal por precedencia **ADOPTA > FIRMA > APARATO > REVISIÓN**, y la instrucción de enrutamiento de esa clase con N y los CALC **contados del diff**; las rutas viven en su tabla `SEÑALES`, en un solo sitio y cada una con la línea que dice por qué dispara (D-15). **La clase informa, no adjudica (D-16):** la herramienta sale 0 clasifique lo que clasifique y sólo falla si no puede leer el diff; el job nuevo `enrutamiento-pr` de `.github/workflows/verify.yml` **queda fuera de `needs` de `check`**, hereda `permissions: contents: read` y trae su propio clonado con la rama base (D-23, mismo patrón que `guardas-res`) — ningún job existente se tocó y no se instrumentó ninguna forma de auto-merge. `tests/test_clasifica_pr.py`, cableado como paso bloqueante del job (D-21), cubre un caso por clase, dos señales con precedencia listando ambas, diff vacío, varios CALC contados, renombre y borrado, y el punto de entrada corrido de verdad (D-22). **Re-corrida retrospectiva** sobre el universo declarado de los **198** `Merge pull request` de `origin/main` entre el 14 y el 21/sep/2026 (`forense/analisis/enrutamiento-pr-1/recorrida-198-pr.tsv`): **44 ADOPTA / 33 FIRMA / 38 APARATO / 83 REVISIÓN**, reproduciendo exactamente la clasificación de dirección con **cero discrepancias**, así que ninguna ruta de la tabla se corrigió. Tres medidas que el encargo no anticipaba: de los 115 PR que disparan alguna señal, **60 disparan dos o más** —devolver sólo la clase principal habría escondido más de la mitad—; **los 44 PR ADOPTA disparan también FIRMA, los 44**, porque un `sello.json` nunca llega sin el `spec.yaml` de su CALC, de modo que la precedencia decide la clase en el 100 % de las adopciones; y los 198 PR adoptaron **80 CALC** en la semana, hasta 7 en un solo PR (`PR #789`). Premisa logística caída y resuelta (D-19): el clon era superficial y veía 59 merges en vez de 483 — `git fetch --unshallow` antes de derivar. No fusiona, no etiqueta, no bloquea por clase y no decide nada por mesa; **el PR queda propuesto y mesa central fusiona**. Sucesor posible, no lanzado: que `/revisa` lea la clase del resumen en vez de deducirla, «con evidencia de uso, no antes» (`NC-260921-GEN2-TUBERIA-ENRUTAMIENTO-PR-1-9a2c-01`). Nota de cierre: `forense/notas/nota-2026-09-21-gen2-tuberia-enrutamiento-pr-1.md`.
