# ACTO PROC-10 · `milpa/procedencia.yaml` no gana entrada para `P7_12_7` — `condicionales 9 de 14` no sube, hueco de taxonomía encontrado y declarado

**Acto:** ACTO PROC-10 · **Entorno:** repo-only, NUBE, `cloud_default`, sin `data/raw`, sin sonda HTTP (no aplica) · **SHA de redacción del encargo:** `e90a7a6` (`origin/main`, merge #218 · ADR-78) · **Depende de:** ADR-75(a) (`RESUELTA-ACOTA`, enmienda in situ sobre `gobernanza-v1_15.md:866`) y la celda-D `data/curacion-registro/celdas-d/G5.familismo_obligacion.actitud.yaml`, ambos ya sellados por `ACTO RES` (PR #215). Este acto no estima nada, no abre payload, no corre diseño — verifica si la medición ya sellada puede entrar a `milpa/procedencia.yaml` bajo la clase `MEDIDO·PARCIAL(x)`, el paso que `ACTO RES` nombró "pendiente, para acto futuro" (`forense/notas/2026-08-13-res-reserva.md` §5).

## §0 · ARRANQUE

1. **REPO.** Clon existente en uso: `/home/user/Modelado-Mexicano`. Ningún clon nuevo. `git log -1`: `e90a7a6 Merge pull request #218 from Josanoforo/claude/new-session-qwc8wn`. `git status`: rama `claude/proc-10-procedencia-condicionales-4mfo2t` (la designada, ya existía local y en `origin`), árbol limpio.
2. **SHA.** `origin/main` = `e90a7a6`, idéntico al declarado por el encargo — sin divergencia.
3. **data/raw.** Ausente (`ls data/raw` → no existe). Declarado por el encargo como no necesario para este acto; confirmado y saltado.
4. **ENTORNO.** `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=cloud_default`. Sonda HTTP saltada — este acto no toca microdato ni red.
5. **ESPEJO.** Toda cifra de esta nota sale del clon de (1), comando a la vista en cada sección.
6. **Colisión.** `git fetch origin` + `git branch -r` → solo existen `origin/main` y `origin/claude/proc-10-procedencia-condicionales-4mfo2t` (esta rama). Ninguna otra rama viva toca `canon/` ni `milpa/`. Sin PARA por concurrencia.
7. **Duplicado.** `grep -in "proc-10" forense/hallazgos.md forense/notas/*.md` → sin resultados (antes de esta nota). Este acto no se había ejecutado.
8. **Baseline recongelado.** `tests/baseline.json` → `head`: `3d0d1e5fd05567e59f8df65b2e1495b43bbcfc3c` — coincide con el `3d0d1e5` que el encargo cita (ADR-76(f)). `python3 tests/check.py --baseline` corrido antes de tocar nada: **20 FAIL · 107 WARN — LÍNEA BASE: VERDE**, `T15`/`T19b` ambos `[ ok ]` (ver §7; la cifra vieja `18 FAIL · 104 WARN` de `ADR-75`/`ADR-76` no aplica, es la del congelado anterior, tal como el encargo advirtió).

## §1 · La clase `MEDIDO·PARCIAL(x)` exige eje no vacío — verificado en el archivo, no heredado del encargo

`milpa/procedencia.yaml:26-37`, releído verbatim: *"Condicional medida empíricamente (distribución sobre desenlaces, no media puntual) sobre un subconjunto DECLARADO x de los ejes de atributos de `modelo §1.1.A` — nunca los seis a la vez. (x) lista los ejes efectivos entre paréntesis."* Es, por nombre propio, la "sexta clase" del archivo — MEDIDO, DERIVADO, ORDINAL→CARDINAL, ASIGNADO, AJUSTADO y esta.

`modelo-decision-v4_0.md:110-123` (§1.1.A) nombra los seis ejes de x: formalidad laboral, edad, urbanización, ingreso, acceso digital, condición migratoria.

```
$ grep -c 'clase: "MEDIDO·PARCIAL' milpa/procedencia.yaml
9
$ grep -n 'clase: "MEDIDO·PARCIAL' milpa/procedencia.yaml
135:    clase: "MEDIDO·PARCIAL(edad)"
146:    clase: "MEDIDO·PARCIAL(formalidad,edad,urbanización,ingreso,acceso_digital)"
156:    clase: "MEDIDO·PARCIAL(formalidad,edad,urbanización,migración,ingreso)"
180:    clase: "MEDIDO·PARCIAL(edad,dominio), por institución"
192:    clase: "MEDIDO·PARCIAL(edad,dominio), por institución"
203:    clase: "MEDIDO·PARCIAL(formalidad,edad,dominio), por ítem"
232:    clase: "MEDIDO·PARCIAL(formalidad,edad,dominio), por ítem"
252:    clase: "MEDIDO·PARCIAL(formalidad,edad,urbanización), por ítem"
317:    clase: "MEDIDO·PARCIAL(edad,dominio,formalidad,ESTRATO)"
$ grep -n "familismo_obligacion" milpa/procedencia.yaml
83, 104, 105, 117, 629, 899, 936
```

Las nueve entradas vigentes traen `x` no vacío, sin excepción. Las siete apariciones de `familismo_obligacion` son todas del **coeficiente** (`rutas_estimabilidad_coeficiente`, `asignados_coeficiente`, vocabulario histórico) — objeto distinto de la **condicional**, ya distinguido por este mismo corpus (`vocabulario_version: 0.4` en la celda-D). `NO-ENCONTRADO`: confirma, por lectura directa y no por herencia del encargo, que no existe hoy ninguna línea `clase: "MEDIDO·PARCIAL"` para `familismo_obligacion`/`norma_de_género`.

## §2 · La medición existe y las cifras cuadran contra el expediente

`data/curacion-registro/expedientes-produccion/t0-89f4c3a49c00c0e1/ESP-OPACA-B-d13ec4fe/{resultado.tsv,resumen.json,especificacion-recibida.json}` y `data/curacion-registro/produccion-modelo.tsv` fila `PROD-cca3ea0bccd54d70083728b2`, leídos directo (no confiados de la prosa del encargo):

| Campo | Encargo declara | Expediente trae | ¿Cuadra? |
|---|---|---|---|
| `n` | 5,579 | `5579` (TPER_ELE) | Sí |
| De acuerdo (Sí) | 0.6933 | `0.6932672048` | Sí (redondeo) |
| IC95 | [0.6725, 0.7140] | `[0.6724933968, 0.7140410128]` | Sí (redondeo) |
| Fuente | ENASIC 2022 `P7_12_7` | ENASIC 2022 `P7_12_7`, tabla `TPER_ELE` | Sí |

`estado`=`CALCULO_REPRODUCIBLE`, `verificacion_supervisor`=`REPRODUCIDA_EN_TEMP_Y_COMPARADA_BYTE_A_BYTE`, `estado_uso_modelo`=`LISTA_PARA_USO_MODELO`, `requiere_decision`=`NO` (los dos últimos, fijados por `ACTO RES`; sin cambio en este acto). `especificacion-recibida.json.criterio_parada`, verbatim: *"Ya no aplica un criterio de parada material; el primer resultado que produzca produce.py sobre esta especificación es el que se reporta"* — coincide con la frase de cierre que el encargo pide declarar. Ningún número no cuadra; este eje no produce PARA.

## §3 · La razón real del PARO — el eje condicionante no existe

`G5.familismo_obligacion.actitud.yaml:90-98`, campo `procedencia_condicional`, verbatim:

> `MEDICION_DIRECTA_MICRODATO` — P7_12_7 es una distribución marginal directa sobre TPER_ELE (un reactivo, un ponderador, sin condicionante analítico cruzando variables) — contraste explícito contra el precedente del propio corpus, `G5.radio_confianza.encuci_vs_enbiare.yaml:149`, cuyo BASELINE/ENCUCI es `MEDICION_CONDICIONAL_MICRODATO` precisamente PORQUE condiciona por formalidad×edad; esta celda no condiciona nada — no un proxy (eso sería un ítem sustituto, no es el caso), no ausencia de estimación (sí existe, vigente).

Confirmado, independiente del texto de la celda-D, en el propio expediente: `especificacion-recibida.json.estimando` = `"distribucion_descriptiva_completa_del_reactivo"` — la distribución nacional del reactivo, sin desagregar; `resultado.tsv.poblacion` cubre a toda `TPER_ELE` sin partición por ningún eje. Ningún artefacto del expediente trae columna, fila o desglose por formalidad, edad, urbanización, ingreso, acceso digital o migración.

**x = ∅.** La clase exige x ≠ ∅ ("nunca los seis a la vez" presupone al menos uno declarado). Escribir `clase: "MEDIDO·PARCIAL()"`, o declarar un eje que el expediente no trae, falsearía qué mide el número — la regla 3 del Bloque A-bis, aplicada aquí a una **clase** en vez de a una escala, tal como el encargo lo planteaba.

## §4 · Segunda confirmación independiente — el cubo actual de `modelo-decision` tampoco lo acomoda

`modelo-decision-v4_0.md:271` (§1.1.F Paso 5, fila "Proxy declarado, pendiente de medición", 3 de 14) clasifica hoy a `familismo_obligacion` ahí, citando `"ENUT 6.11/6.11a, M2"`. Dos hallazgos, ninguno heredado sin verificar:

- **Cita obsoleta.** ADR-67(b) (10/ago) ya fijó `P7_12_7`/ENASIC como la θ real de este generador, no ENUT. `forense/notas/2026-08-13-res-reserva.md` §6 ya lo declaró "hallazgo menor, no perseguido" — fuera del perímetro de `ACTO RES`, que no alcanzaba `modelo-decision-v4_0.md`.
- **El rótulo ya no aplica, más allá de la cita.** La celda-D niega explícitamente que esto sea un proxy, y la medición no está "pendiente" — está calculada, reproducida byte a byte y adjudicada (ADR-75(a)). De las cuatro filas de la tabla (`MEDIDO·PARCIAL(x)`, búsqueda abierta, búsqueda cerrada — límite de régimen, proxy pendiente), **ninguna describe hoy correctamente este objeto**: no es proxy, no está pendiente, no tiene búsqueda cerrada sin candidato (el candidato existe y se midió), y no tiene eje (§3).

Esta tabla no se edita en este acto — el titular no se mueve (§6) y el perímetro de este acto no incluye reescribir la fila de Paso 5 sin que el desenlace sea (A). Queda declarado, mismo criterio que `ACTO RES` ya practicó para el mismo tipo de hallazgo.

## §5 · Decisión — desenlace (B)

Nota de secuencia, declarada y no oculta: el propio §0 del encargo instruye abrir el expediente para saber si trae ejes efectivos antes de poder decidir entre (A) y (B) — así que la apertura del expediente (§2-§3 arriba) necesariamente precedió a esta sección. Un "COMMIT 1" ciego, redactado antes de ver el expediente, no era procesalmente posible sin contradecir esa misma instrucción de arranque. Lo que sigue cubre los cinco puntos que el encargo pide congelar, en retrospectiva declarada, no como pre-registro ciego:

1. **Qué se habría escrito, si hubiera eje:** generador `familismo_obligacion` (G5) · clase candidata `MEDIDO·PARCIAL(x)` · fuente ENASIC 2022 `P7_12_7` · `n_util` 5579 · `eje_condicionante` — **campo sin valor válido, ver §3** · `antes`: "Proxy declarado, pendiente de medición (ENUT 6.11/6.11a, M2)" — cita obsoleta, ver §4 · `nota`: el límite de constructo de ADR-75(a) (norma de deber de cuidado diferenciada por sexo, no obligación familiar genérica) más el límite de que la medición no condiciona ningún eje de x. La plantilla (`condicionales_confianza_institucional.salud`, `procedencia.yaml:133-144`) exige `eje_condicionante` no vacío; ese es precisamente el campo que no se puede llenar sin inventar.
2. **Nombre:** no aplica — no se escribe ninguna entrada. Esta nota usa el nombre vigente hoy en `procedencia.yaml`/celda-D/`modelo-decision`: `familismo_obligacion`. El renombre a `norma_de_género` está redactado en la enmienda de ADR-75(b) pero declarado explícitamente "no ejecutado" ni en canon ni en `procedencia.yaml` — este acto tampoco lo ejecuta. No se resuelve en silencio: se declara que no hace falta resolverlo porque nada se escribe.
3. **Denominador:** no se mueve. `D`=14 hoy; `obligación_medida` (`P6_38`) no se registra en este acto — política de ADR-75(b): entraría como condicional nueva (`D`:14→15) cuando se registre, no aquí. Si el desenlace hubiera sido (A), el titular habría sido "10 de 14", nunca "10 de 15".
4. **Cifras:** verificadas en §2 contra el expediente y `produccion-modelo.tsv`. Cuadran.
5. **Frase de cierre:** verificada en §2 contra `especificacion-recibida.json.criterio_parada`. Cuadra verbatim.

**Desenlace: (B).** No hay eje condicionante declarado ni derivable del expediente sin inventarlo. No se escribe `milpa/procedencia.yaml`. No cascada ningún sitio de "N de 14". El contador queda en **9 de 14**, ahora con la razón taxonómica escrita, no solo la de perímetro que dejó `ADR-75(a)`.

## §6 · Cascada verificada, no ejecutada — nada se mueve

```
$ grep -rn "de 14" README.md canon/*.md | grep -vE "\{cita-historica\}|\{cita-ilustrativa\}"
```

Mismo comando que el encargo cita. Los sitios sustantivos de "9 de 14" coinciden con los que el encargo declara — `README.md:37` · `estado-programa-v1_10.md:97` · `modelo-decision-v4_0.md:11,277,621,725` — y con los bloques "Cascada"/"Lo que NO hace" de ADR-61 a ADR-78 en `gobernanza-v1_15.md`. **Hallazgo declarado, no corregido aquí:** los números de línea de `gobernanza-v1_15.md` que el encargo cita (`736,872,904,938,952,1026,1028`) ya no coinciden exactamente con la salida cruda de hoy (equivalentes actuales: `740,896,902,934,982≈952,1056,1058`…) — el archivo creció ~15-20 líneas por sección desde que el encargo derivó esa lista, porque `ADR-77` y `ADR-78` se sellaron después (mismo fenómeno de arrastre que `ACTO RES` ya documentó para `gobernanza:938`, `forense/notas/2026-08-13-res-reserva.md` §2). No cambia nada de lo que sigue: ninguno de esos sitios se edita en este acto porque el número no cambió, así que el desplazamiento de línea es cosmético, no material (`AGENTS.md`, "Defectos · No material").

Ningún sitio de la cascada fue tocado. `README.md`, `canon/modelo-decision-v4_0.md`, `canon/estado-programa-v1_10.md`, `canon/gobernanza-v1_15.md` — sin editar.

## §7 · ¿Hace falta ADR?

No. ADR-75(a) ya adjudicó el límite de constructo de esta θ; este acto no repite esa decisión, solo verificó si podía propagarse a `milpa/procedencia.yaml` y encontró que no puede, con la taxonomía vigente. Este acto no sella ADR bajo ninguna lectura — ni ejecutar (A) lo habría requerido (habría sido propagación de una decisión ya sellada, mismo criterio que las Entradas 0 y 4 de `registro-recalculo-v1_0.md`, que cerraron `SIN CAMBIO` sin ADR propio), ni declarar (B) lo requiere. Si mesa decide abrir una séptima clase de procedencia, correr la medición condicionada, o aceptar que esta θ no cuenta para "de 14", eso es acto de mesa aparte — nombrado aquí, no ejecutado.

## §8 · Lo que NO se tocó

`milpa/procedencia.yaml`, `canon/modelo-decision-v4_0.md`, `canon/estado-programa-v1_10.md`, `canon/gobernanza-v1_15.md`, `README.md` — ninguno editado. El hallazgo de §4 (tabla de Paso 5 con rótulo y cita obsoletos) queda declarado, no corregido, mismo criterio que `ACTO RES` ya fijó para el mismo tipo de hallazgo fuera de perímetro. No se renombra nada. No se crea la celda-D de `obligación_medida`. No se mueve el denominador. No se reabre ADR-67(a) (`radio_confianza` sigue bloqueada). Contadores no tocados, verificados uno por uno, no heredados: `13 de 27` (Hito D) · `0 de 15` (coeficientes) · `1 de 2` (llaves) · `4 de 144`. Ninguno.

## §9 · Cierre — verificación de línea base

`python3 tests/check.py --baseline`, corrido antes de escribir esta nota y `forense/hallazgos.md`, y de nuevo después:

- **Antes** (§0.8): `20 FAIL · 107 WARN` — LÍNEA BASE: VERDE (HEAD congelado `3d0d1e5fd05567e59f8df65b2e1495b43bbcfc3c`). `T15 T-ADR-COUNT`: `[ ok ]`. `T19b contador 14 cruzado (modelo)`: `[ ok ]`.
- **Después:** ver corrida final de este acto, misma cifra esperada — este acto no toca ningún archivo que T15/T19b vigilen (`milpa/procedencia.yaml`, `modelo-decision-v4_0.md`, `gobernanza-v1_15.md`), solo añade `forense/notas/2026-08-13-proc-10.md` (este archivo, nuevo) y una línea a `forense/hallazgos.md`.

Ningún archivo fuera del perímetro declarado (`milpa/procedencia.yaml`, `canon/modelo-decision-v4_0.md`, `canon/estado-programa-v1_10.md`, `README.md`, `forense/notas/2026-08-13-proc-10.md`, `forense/hallazgos.md`) fue escrito; de esa lista, solo los dos últimos se tocaron, porque el desenlace fue (B).
