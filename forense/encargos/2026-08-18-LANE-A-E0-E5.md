# ENCARGO · LANE-A-E0-E5 — re-correr el gate de MOTOR-3/E0 ahora que MOTOR-2 está sellado, y cerrar E5

**SHA de redacción:** `93a4dd9` (`origin/main`, tras `ADR-100`/`ADR-101`, ACTO MESA-18AGO, 18/ago/2026)
**Entorno asignado:** **NUBE** (`cloud_default`, repo-only). No toca microdato.
**Estado:** CONSUMIDO — ejecutado por `ACTO LANE-A-E0-E5`, 18/ago/2026, este PR. Fase CON SELLO de `MOTOR-3/E0` completa (C1 catálogo · C2 rebanada · C3 cierre), encargo de E5 archivado por A.3 antes de correr, Entrada 5 cerrada `RECALCULADO — SIN CAMBIO` con universo declarado, `FP-15` `CERRADA`. Detalle: `forense/notas/2026-08-18-motor3-con-sello-y-entrada-5.md`.
**Origen:** DISPARADOR-A de `FP-26` (`ADR-101(h)`), primer eslabón: *"verificación de E0 → E5/FP-15 → …"*. `ADR-101(i)` deriva que `ACTO MOTOR-3/E0` (`PR #237`) solo ejecutó FASE-PLAN — `milpa/src/` no existe — porque su propio gate midió `MOTOR-2` sin sellar. `ADR-100` (este mismo acto) sella `MOTOR-2`.

## Verificación de existencia (A.8), contestada por quien escribe

```
milpa/src/ existe:                    NO (verificado, ACTO MESA-18AGO, 18/ago)
gate propio de MOTOR-3/E0:            grep -cE "^\*\*ADR-(8[4-9]|9[0-9]|10[0-9]) .*(motor|matriz|M1)"
                                       canon/gobernanza-v1_15.md   -- re-correr, no asumir 0
                                       (ADR-100 cita "motor"/"M1" -- debería dar >=1 ahora)
plan ya escrito:                      forense/notas/2026-08-14-motor3-plan.md   EXISTE
encargo de E0, cuerpo autocontenido:  forense/encargos/2026-08-14-MOTOR-3-E0-autocontenido.md   EXISTE
                                       (RANURAS DEL SELLO §, llenar con las firmas de ADR-100)
contenido de E5 ya derivado:          forense/encargos/2026-08-14-MOTOR-1-consolidado.md §4   EXISTE-SATISFACE
                                       (universo a citar, veredicto 57(c)=SIN CAMBIO, llaves=1 de 2 no 0)
encargo de E5 archivado (A.3):        grep -rln "Entrada 5" forense/encargos/   -- re-correr;
                                       si sigue sin el encargo propio, archívalo tú mismo antes de correr
                                       (instrucción del inciso (7) de ADR-100)
```

## Perímetro

ESCRIBE: `milpa/src/**` (nuevo, fase CON SELLO del encargo de `MOTOR-3/E0`) · `milpa/catalogo-momentos-v0_1.md` + su tabla · `tests/test_motor_*.py` (solo tests nuevos del motor) · `forense/registro-recalculo-v1_0.md` (Entrada 5, una fila) · `forense/encargos/` (el encargo de E5, archivado por A.3 antes de correr) · nota del acto. NO ESCRIBE: `tools/curador_registro/**` · `canon/**` (salvo citar `ADR-100`/`ADR-57(c)` por número, no editar) · `data/curacion-registro/**` · `relaciones.tsv` · manifiesto.

## Tarea

1. Re-correr el gate de `forense/encargos/2026-08-14-MOTOR-3-E0-autocontenido.md`. Con `ADR-100` en el árbol, debe medir `≥1` — ejecutar la fase **CON SELLO** completa: `C1` (catálogo de momentos, roles `AJUSTE`/`HOLDOUT` sellados en el commit), `C2` (rebanada mínima del motor, holdout intocado con test), `C3` (cierre con los 15 fixes de `RONDA-M` listados). Llenar las **RANURAS DEL SELLO** del encargo con las firmas verbatim que `ADR-100` ya trae para M1-M6.
2. Archivar por A.3 el encargo propio de la Entrada 5 (instrucción del inciso (7) de `ADR-100`: *"archívese E5 por A.3 antes de correr"*) — cuerpo derivado de `forense/encargos/2026-08-14-MOTOR-1-consolidado.md §4`.
3. Ejecutar la Entrada 5 de `registro-recalculo`: formato de entrada derivado del propio archivo (no `^## Entrada`, ver su tabla §1), citando `ADR-100` en el universo, veredicto sobre `ADR-57(c)` = SIN CAMBIO (ya derivado), y la cifra que **no** debe copiarse (llaves = 1 de 2, no 0 — cítala, no la muevas).

## Cierre

`milpa/src/` con la rebanada mínima corriendo, holdout probado intocado · Entrada 5 cerrada con universo declarado · `firmas-pendientes.tsv`: `FP-15` cerrada, `ejecutada_en` este PR · `tests/check.py --baseline` VERDE · línea en `hallazgos.md` · encargo `CONSUMIDO`.
