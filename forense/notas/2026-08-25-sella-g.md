# Nota · `ACTO SELLA-G` — dos firmas de mesa verbatim, dos micro-enmiendas

25/ago/2026. Entorno **NUBE** (`cloud_default`). Rama `claude/pack-nube-3-actos-w4v5j8`, `ACTO 1` del pack
`PACK NUBE-3`.

## 1 · Arranque

Repo `/home/user/Modelado-Mexicano`, clon existente. `git log --oneline -20` confirma `origin/main` con
`f6f8e00` (merge de `PR #345`, `ACTO INDICE-NO-INEGI`) como tip más reciente antes de este acto. Entorno NUBE
declarado, sin sonda de red — este acto no toca `data/raw/` ni microdato.

## 2 · `FP-149` — firma de mesa, opción (d)

`FP-149` (`ABIERTA`, nacida `ACTO ESCALA-ASIGNADOS`, 25/ago/2026 — ver
`forense/notas/nota-2026-08-25-escala-asignados.md` §3) preguntaba qué hacer con los 15 coeficientes ASIGNADOS
declarados `ESCALA_NO_DERIVABLE`. Firma de mesa, verbatim, capturada en el mismo mensaje que autoriza este pack:

> "firmado" sobre la opción (d) presentada por dirección — completar las escalas de los 15 ASIGNADOS en tres
> pasos: (1) derivación dimensional forzada por los dos extremos que el modelo ya declara, citando solo fuentes
> pre-medición; (2) elección ciega pre-registrada solo para el residuo subdeterminado, con higiene de contexto
> tipo R10.1; (3) relectura de CAL-G3 bajo la escala resultante, diga lo que diga. "Solo-signo" es estatus
> TRANSITORIO del tablero, no doctrina; toda asignación futura entra con escala.

`FP-149` pasa a `FIRMADA` (`forense/firmas-pendientes.tsv`, columna `firmada_en` cita el verbatim completo,
`ADR-173`). Fila mini nueva `FP-152`, `FIRMADA`: *"ESCALAS-COMPLETAS — Paso 1 en el acto 2 de este pack; Paso 2
se redacta con la lista de subdeterminadas"* — hace visible, en el tablero, que la ejecución del Paso 1 vive en
el `ACTO 2` de este mismo pack (`ACTO ESCALAS-COMPLETAS-P1`), no en este acto.

**Este acto no ejecuta el Paso 1.** La ejecución (derivación dimensional real, veredicto por coeficiente) es el
`ACTO 2` del pack, en su propio commit.

## 3 · `FP-151` — firma de mesa, encargo UBUNTU

`FP-151` (`ABIERTA` desde `ACTO PURGA-EJECUTA`, 25/ago/2026) describe un segundo espejo,
`~/BACKUP-mm-mirror-2026-08-10.git`, con la misma forma de redundancia que `~/mm-purga.git` ya medido (`FP-143`,
ejecutada). Firma de mesa, verbatim, en el mensaje de lanzamiento de este pack:

> "ahora dame los encargos incluyendo: autorizo destruir backup etc etc"

**El referente inequívoco.** El único backup nombrado en todo el registro del tablero bajo esa forma es el que
`FP-151` describe — verificado por `grep -rn "BACKUP-mm-mirror-2026-08-10" forense/firmas-pendientes.tsv
canon/gobernanza-v1_15.md` antes de escribir esta nota, sin resultado de un segundo candidato. La cadena que
`FP-151` exige, misma forma que `FP-143`/`FP-63` ya fijaron para `mm-purga.git`, es:

> AUTORIZO DESTRUIR BACKUP-mm-mirror-2026-08-10.git

Las dos formas (el verbatim libre de dirección y la cadena canónica que la fila exige) se registran juntas en
`forense/firmas-pendientes.tsv:firmada_en` de `FP-151`, con su equivalencia declarada explícitamente — no se
sustituye una por otra en silencio.

`FP-151` pasa a `FIRMADA` (`ADR-173`). Fila mini nueva `FP-153`, `FIRMADA`: *"UBUNTU: ejecutar destrucción de
~/BACKUP-mm-mirror-2026-08-10.git con la cadena citada, verificación previa idéntica a FP-143"* — el encargo que
ejecuta esto es `forense/encargos/2026-08-25-SELLA-G-encargo.md`.

**Verificación de existencia, antes de escribir nada de ejecución.** `~/BACKUP-mm-mirror-2026-08-10.git` en
**este** entorno (NUBE, `/home/user/Modelado-Mexicano`) se comprobó con `test -e ~/BACKUP-mm-mirror-2026-08-10.git`
→ **NO-ENCONTRADO** (`~` resuelve a `/home/user`, sin ese directorio). Esto **no** es sorpresa: el propio texto
de `FP-151` describe el espejo como medido contra `/home/pc0/Modelado-Mexicano` (host UBUNTU), no contra este
entorno de nube. **La firma queda dada y registrada; la destrucción física NO se ejecuta en este acto** — es un
encargo autorizado-pero-no-ejecutado, exactamente lo que la fila `FP-153` declara, para correr en el host UBUNTU
real donde el espejo vive. Este acto no fabrica una destrucción que no hizo.

## 4 · Micro-enmiendas de dirección, 25/ago

**(a)** `forense/notas/2026-08-25-serie-homogenea.md` recibe una línea fechada junto a la cita «13 de 27»,
aclarando que el contador vigente al fusionarse ese acto era **18**, y que este acto (`SELLA-G`) no lo mueve.
El resto del archivo queda intacto — verificado por `git diff` antes de commitear: una sola inserción, cero
líneas borradas.

**(b)** Este mismo archivo. `L14` y `L16` (de la hoja de letras que otros actos `SELLA-AGO25-*` ya vinieron
resolviendo letra por letra) siguen sin carta de decisión de mesa: `L14` queda pendiente para después del acto 3
de este pack (sello sobre cifras en vivo — el acto 3 toca precisamente el cuadro del sorteo, `L14` se decide con
esa base ya puesta); `L16`, tal como está presentado hoy, sin más detalle disponible en el árbol ni en el
lanzamiento de este pack. Ninguna de las dos se adjudica aquí — quedan **abiertas y declaradas**, no ejecutadas
por conjetura.

## 5 · Tablero

`FP-149` → `FIRMADA`. `FP-151` → `FIRMADA`. `FP-152` (nueva) → `FIRMADA`. `FP-153` (nueva) → `FIRMADA`. Ninguna
otra fila se toca.

## 6 · Cascada

`ADR-173` (`canon/gobernanza-v1_15.md`), candidateado contra el máximo verificado
(`grep -oE '^\*\*ADR-[0-9]+' canon/gobernanza-v1_15.md | sort -t- -k2 -n -u | tail -1` → `ADR-172`, sin huecos) →
`173`, a re-verificar por quien fusione. `canon/estado-programa-v1_10.md`: conteo de ADR recifrado `172→173`.

## 7 · Tests

`python3 tests/check.py --baseline` corrido antes de editar y después: línea base sigue **VERDE**, sin FAIL
nuevo — este acto no toca ningún archivo que la suite lea como dato de México (solo el tablero de firmas,
gobernanza, estado y dos notas).

## 8 · Lo que este acto NO hace

No ejecuta el Paso 1 de `FP-149`/`ESCALAS-COMPLETAS` (es el `ACTO 2` de este pack, commit separado). No destruye
ningún backup — el path no existe en este entorno; la destrucción queda como encargo `FP-153`, autorizado y sin
ejecutar, para el host UBUNTU real. No toca `milpa/`. No toca `tools/curador_registro/**` ni
`data/curacion-universo/**` (perímetro ajeno, workstream concurrente). No adjudica `L14` ni `L16`.
