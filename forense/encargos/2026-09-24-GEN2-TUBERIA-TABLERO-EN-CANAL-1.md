# ENCARGO · ACTO GEN2-TUBERIA-TABLERO-EN-CANAL-1 · Primero el canal vivo (corre en cada push a `main`, con disparo manual, y publica `usos.tsv`), después el tablero como derivado más del canal: un solo productor sobre `origin/main`, publicado en Pages, y `/tramite` T0 se niega a regenerarlo desde otro árbol

> ENTORNO: **NUBE** — CI, derivador del tablero, `docs/`. Hook imprime ENTORNO-DERIVADO; si dice CAJA, PARA.

CABECERA · SHA de redacción `8358b891` (re-deriva al abrir; reescrito 24/sep 17:00 con el diagnóstico del canal) · una sola sesión, rama propia `acto/gen2-tuberia-tablero-en-canal-1` (o la que la plataforma fije; se declara en el 0-bis) · MODELO: Sonnet (sube a Opus si toca `tablero_programa.py` más allá de la guarda) · MODO: **AUTÓNOMO** (cláusula v1.0 `3fbc487684b77b7f`; PARO solo por D-19 estricta) · ids con raíz de acto (D-24) · D-21 aplica · «Si te encuentras escribiendo fuera de la lista de §9, PARA.» · cierre por /acto: `## NO-CORRIDO / RESERVAS` («Ninguno.» obligatorio si aplica) y `## CONSUMIDO` al pie.
CONTADOR: cero mediciones; no adopta; no cambia ningún número del tablero (lo deriva; si un número cambia respecto al v6 es porque `main` cambió, y la nota lo muestra con `git log`).

## 1 · OBJETIVO
**P0 · El canal vive.** Diagnóstico de dirección (24/sep, verificado): `corrida0.py status` deriva `adoptados`, `pendientes` y `legacy` de `data/corrida0/usos.tsv`, cuya última derivación en `main` es del **21/sep** (`c73f4836`); el resto de vistas, del 23/sep 23:30. Siete cortes del tablero «sin avance» son eso. Dos causas ya corregidas (GH001 por tamaño: #1113; `KeyError('tolerancia')`: #1121) y una viva: **el job de derivados solo se dispara cuando el push trae un asiento nuevo en `replay-evidencia.tsv`**; los PR de adopción (#1119) y adquisición (#1120) no sellan corridas, así que el job ni corrió. P0: el paso de derivados corre **en cada push a `main`** (ya no es caro), gana `workflow_dispatch` para que mesa lo dispare a mano, incluye `usos.tsv` en lo que re-deriva y commitea (verificar que `registro --escribe` lo produzca; si no, añadirlo), y abre su PR `derivados/auto-*`. Prueba real: tras el merge de este acto, el push dispara el job, el PR `[deriva]` se abre, `check` VERDE, y `status` en `main` muestra por fin lo que ADOPCION-4 y ENCIG-PISOS ya hicieron (`pendientes` 0, `adoptados` > 72: los valores los da el derivador, citados).
**P1 · Tablero en el canal.** En ese mismo paso, después de las vistas y el marcador: `python3 tools/tablero_programa.py --actualiza`, de modo que el bloque `<!-- TABLERO-DERIVADO:BEGIN … END -->` de `canon/TABLERO-PROGRAMA.md` se regenere con el árbol del push y viaje en el mismo PR. **P2 · Guarda** en el derivador: si `HEAD` ≠ `origin/main` o el árbol está sucio, `--actualiza` se niega salvo `--permitir-rama`, que rotula el bloque `NO-ES-ORIGIN-MAIN`; `/tramite` T0 no pasa la bandera. **P3 · Pages**: `docs/tablero.md` sirve el bloque de `main` con fecha y SHA. **P4 · Protocolo**: `docs/PROTOCOLO-TABLERO.md`: la conversación del tablero lee (raw de `main` o Pages), interpreta y cita; nunca regenera ni sube versiones; un bloque con `¿árbol == origin/main? False` es inválido. **P5 · Tercera columna**: el bloque derivado gana, junto a cada contador, el valor «en árbol» cuando el job corre (es el mismo número en CI, por construcción; la columna existe para que una sesión que re-derive localmente sin publicar pueda compararse).
«Hecho» sobre el commit final con origin/main fusionado: un run de Actions citado por id donde el job de derivados corre en un push **sin** asiento nuevo · PR `[deriva]` abierto por `github-actions` con `check` VERDE, que trae `usos.tsv`, `corridas.tsv`, `resultados.tsv` (< 50 MB), `marcador-segmento.tsv` y `canon/TABLERO-PROGRAMA.md` con `SHA` = commit del push y `¿árbol == origin/main? True` · `workflow_dispatch` disponible (la nota deja la receta de un clic para mesa) · `python3 tools/tablero_programa.py --actualiza` desde una rama devuelve ≠ 0 sin `--permitir-rama` (salida pegada) · `docs/tablero.md` y `docs/PROTOCOLO-TABLERO.md` en `main` · test de determinismo del bloque (dos corridas sobre el mismo árbol = mismo bloque) VERDE · `check.py --baseline` VERDE sin `--force`.

## 2 · FIRMAS DE MESA
Decisión de dirección del 24/sep con mesa («ya resolvamos tablero»): «El canal corre en cada push a main con disparo manual y publica usos.tsv; el tablero se deriva en CI sobre origin/main y entra al PR [deriva]; la conversación del tablero pasa a lectora; T0 se niega fuera de origin/main.» Es proceso, no medición: no requiere fila FP; el ADR la cita. Vigentes: D4-A y R(a) (auto-merge cubre `[deriva]`), D-23 (un comando que deriva no escribe estado fuera de su derivado).

## 3 · LO QUE DIRECCIÓN SABE
- `[EJECUTADO]` (`3252aaec`) `canon/TABLERO-PROGRAMA.md` en main: bloque derivado con `SHA 8a867a04 · ¿árbol == origin/main? False` (regenerado por `/tramite` desde una rama, commit `78f79c80`). El v6 que mesa recibió por chat: `SHA 3423b498 · True`, de un clon limpio. Dos productores, dos resultados.
- `[LEÍDO]` `.claude/commands/tramite.md` l.135-137: T0 corre `tools/tablero_programa.py --actualiza` «con la línea base verde… derivación mecánica». `[EXISTE]` `tools/tablero_programa.py`, `tools/tablero_vista.py`, `data/curacion-universo/tablero-cobertura.json`.
- `[EJECUTADO]` (`8358b891`) `git log -1 -- data/corrida0/usos.tsv` → `c73f4836` 21/sep; `grep -c 'RESULT-BANXICO-2024\\|RESULT-MOTRAL15\\|RESULT-CTX-20' data/corrida0/usos.tsv` → 0 aunque #1119 creó sus reglas en `tramite.yaml`; `status` → 72 / 10 / 146. Tras #1120 no existe rama `derivados/auto-*` ni commit del bot: el job no se disparó (sin asiento nuevo). `[LEÍDO]` `corrida0.py:5085-5115`: `pendientes` y `adoptados` se computan de `usos_activos`.
- `[LEÍDO]` `verify.yml`: el paso de derivados corre `lote_desde_asientos.py`, `registro --verifica --escribe --lote`, `resuelve_citas.py tabla --escribe`, `marcador_segmento.py --escribe` ×2 (#1101), y abre el PR `derivados/auto-*` con `gh pr create` (casilla de Actions activa desde el 23/sep). VISTA-2 (#1113) y VISTA-3 (#1121) ya fusionaron: la guarda de tamaño y la corrección del `KeyError` están en `main`; este acto edita ese mismo paso (condición de disparo, `workflow_dispatch`, `usos.tsv`, tablero).
- `[SUPUESTO]` que `tablero_programa.py --actualiza` es determinista sobre un mismo árbol (dos corridas = mismo bloque); el test de P4 lo demuestra. Si no lo es (fechas, orden de dict), se arregla en ≤ 10 líneas y se declara.
- ADJUNTOS: ninguno (el v6 está en el chat de mesa; el acto lo cita como referencia, no lo archiva: el archivo canónico es el de `main`).

## 4 · YA HECHO / YA DECIDIDO
`grep -c 'tablero_programa' .github/workflows/verify.yml` → reporta (esperado 0). `ls docs | grep -c tablero` → 0. `git ls-remote --heads origin | grep -i tablero` → 0.

## 5 · PIEZAS
P0 · Canal: quitar la condición «solo con asiento nuevo», añadir `workflow_dispatch`, incluir `usos.tsv` en la derivación y en el `git add` del PR; probar con un push sin asiento (el merge de este PR sirve). P1 · Tablero en el paso (después de vistas y marcador; antes de `gh pr create`). P2 · Guarda `origin/main` + `--permitir-rama`; `/tramite` T0 sin bandera. P3 · `docs/tablero.md`. P4 · `docs/PROTOCOLO-TABLERO.md` + test de determinismo. P5 · Columna «en árbol». P6 · Prueba real: primer `[deriva]` con todo adentro; cita del run y del PR; `status` antes/después en la nota.

## 6 · LATITUD — CLÁUSULA DE AUTONOMÍA v1.0 (`3fbc487684b77b7f`)
1. Discrepancias encargo↔repo las resuelve el ejecutor y las declara. 2. Firma con letra en choque e intención clara: INTERPRETACIÓN-DECLARADA, se sigue. 3. Lo redactable se redacta rotulado PROPUESTO-POR-EJECUTOR con fuente. 4. Bifurcación con opción recomendada: se ejecuta la recomendada. 5. PARO solo por D-19 estricta. 6. Nunca: cifra tecleada, sello reescrito, reserva abierta, fuera de §9 sin declarar. 7. El «Hecho» no se rebaja. Si el job falla por una causa nueva (permisos de PR, otro archivo > 50 MB, un test de línea base), **se arregla en el mismo acto** y se declara; es exactamente lo que este encargo pide. Cómo servir el bloque en Pages: tuyo. Pregunta a mesa: ninguna.

## 7 · PAROS — lista cerrada
a) no aplica · b) editar el bloque derivado a mano, tocar `registro`, `marcador_segmento.py`, la guarda de tamaño de VISTA-2, o un sello · c) mover contadores a mano · d) no aplica · e) CAJA · f) el tablero ya llega por `[deriva]` con `True`.

## 8 · COMPUERTAS
«El bloque solo lo escribe el job sobre origin/main; T0 se niega fuera» protege: **congelar / borrar** (un tablero de rama es un estado falso publicado). «Test de determinismo antes de meterlo al canal» protege: **congelar** (un derivado no determinista genera PR infinitos).

## 9 · PERÍMETRO Y CONCURRENCIA
Propio: `.github/workflows/verify.yml` (el paso de derivados: condición de disparo, `workflow_dispatch`, `usos.tsv`, tablero), `tools/tablero_programa.py` (guarda y `--permitir-rama`), `.claude/commands/tramite.md` (una línea en T0), `docs/tablero.md`, `docs/PROTOCOLO-TABLERO.md`, `canon/TABLERO-PROGRAMA.md` **solo por el job**, test propio, nota, L0, cascada. Ajeno: vistas (solo por el job), marcador, guarda de tamaño (se conserva), `check.py`, `registro` salvo que `usos.tsv` exija una línea. En vuelo: PISOS-GEN2-2, CORPUS-COMPLETO, SALUD, DONDE-CAMBIO, CLASE-AMAI (caja; no tocan CI).

## 10 · LO QUE NO HACE · SUCESORES
No cambia qué mide el tablero ni sus definiciones (`celdas_validadas_definicion_desde` sigue siendo de CONTADORES). Sucesor: ninguno; si Pages exige un include que Jekyll no soporte, `-2` con la copia por job.
