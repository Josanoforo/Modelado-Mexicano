# 2026-08-13 · ACTO A8-LAND — nota de cierre

Entorno: **NUBE**, `cloud_default` (confirmado, `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=cloud_default`), sonda HTTP saltada per el propio encargo (no toca microdato ni red). Repo-only: no abrió `data/raw`, no tocó `milpa/**`, `tests/**` ni `tools/**`.

---

## 1 · ARRANQUE — un hallazgo de terreno antes de escribir nada

El clon existente (`/home/user/Modelado-Mexicano`) estaba **shallow** (`git rev-parse --is-shallow-repository` → `true`, límite en `ed46c26`, 8/ago/2026). Contra un clon shallow, `git log --diff-filter=A -- instrucciones-proyecto-v2_6.md` da **2026-08-08** como fecha de nacimiento — no coincide con el `2026-08-05` que la VERIFICACIÓN DE EXISTENCIA del encargo declaraba. `git fetch --unshallow origin` (860 commits reales contra 264 visibles) resuelve la discrepancia: la fecha real es **2026-08-05** (`661ee90`), exactamente la que el encargo citaba — el desajuste era del clon, no del encargo. Se reporta porque encontrar que el terreno no era el que parecía es entregable (regla del propio Bloque D), aunque en este caso confirmó al encargo en vez de contradecirlo.

SHA de redacción `8b30306` confirmado idéntico contra `origin/main` tras `git fetch origin --prune` (el remoto había limpiado `origin/claude/new-session-qwc8wn`, rama vacía sin commits propios); cero ramas vivas además de `main`, ninguna toca `canon/gobernanza`.

---

## 2 · Sitios de cascada tocados, con línea

- `instrucciones-proyecto-v2_7.md` — archivo nuevo, copia exacta de `instrucciones-proyecto-v2_6.md` (diff de 3 hunks: línea 158, línea 195, línea 271→fin — exactamente las 5 ediciones que el encargo especificaba, ni una más).
- `canon/gobernanza-v1_15.md:2` — cabecera, `76 ADR` → `77 ADR`.
- `canon/gobernanza-v1_15.md` (después del cierre de ADR-76, antes del `---`) — entrada nueva `**ADR-77 · ...**`, con incisos (a)-(d) del encargo más "Lo que este ADR NO hace" y "Cascada".
- `canon/gobernanza-v1_15.md:764,856` — dentro de ADR-62(g) y la Cascada de ADR-66 (ya sellados): `104 WARN` → `107 WARN`, con nota de la causa — mismo patrón que `ADR-72`/`ADR-73` ya usaron ahí, no es la primera vez que estas dos líneas se actualizan.
- `canon/estado-programa-v1_10.md:27,101,129,221` — `76 ADR` → `77 ADR` (dos citas) + cláusula nueva "; a 77 después, con ADR-77..." al final de la narrativa histórica de L0 + `104 WARN` → `107 WARN` en dos sitios más (líneas 129 y 221).
- `forense/encargos/convencion.md` — una línea: todo encargo archivado lleva también el bloque VERIFICACIÓN DE EXISTENCIA de Bloque D.
- `forense/encargos/2026-08-13-A8LAND-instrucciones-v2_7.md` — archivo nuevo, **fuera de la lista de perímetro que el propio encargo declaró** (§5, abajo).
- `forense/hallazgos.md` — una entrada (append).
- Este archivo.

**NO tocado**, per perímetro declarado y confirmado contra `data/INFRAESTRUCTURA-v1_0.md` Dominio 7: `data/`, `milpa/`, `tests/`, `README.md` (verificado: no cita conteo de ADR), `canon/modelo-decision-v4_0.md`, `instrucciones-proyecto-v2_6.md`. **NO tocado**, deliberadamente, dentro de `canon/gobernanza-v1_15.md`: líneas 1104 y 1116, dentro del cuerpo ya sellado de ADR-76 — ver §4.

---

## 3 · La cifra de suite — antes y después, ninguna copiada

**Antes** (COMMIT 1 sin empezar, `HEAD = 8b30306`):
```
18 FAIL · 104 WARN
LÍNEA BASE: VERDE — nada nuevo frente a tests/baseline.json
```
Idéntico a lo que `ADR-76(f)` ya había medido al sellarse — sin drift en el terreno entre entonces y el arranque de este acto.

**Después** (los dos commits de canon completos):
```
19 FAIL · 107 WARN
LÍNEA BASE: ROJO — 2 entradas nuevas
```

El camino hasta ahí tuvo una vuelta de más, declarada porque el propio A.8/A.9 que este acto sella exige exactamente este tipo de honestidad: mi primer intento de anotar el cambio de cifra en gobernanza-v1_15.md/estado-programa-v1_10.md usó backticks dobles con espacio alrededor de "v2_6.md" esperando escapar la cita — no funciona, `t03_dangling_refs()` escanea la línea cruda carácter a carácter y encuentra igual el backtick simple interior, así que ese intento **subió** el WARN real a 109 en vez de dejarlo en 107 (verificado, corrida intermedia). Corregido quitando los backticks del todo en mi propia prosa (files que sí controlo, a diferencia del encargo archivado) — texto plano entre comillas rectas, sin comillas de código. `tests/check.py --baseline` corrido después de cada uno de los cuatro sitios de T16 corregidos, no solo al final.

---

## 4 · Dos hallazgos declarados al cierre, ninguno absorbido por freeze unilateral

`ADR-76(f)` ya fijó la regla: *"recongelar la línea base exige ADR de mesa; un ejecutor que encuentre drift lo reporta y no lo recongela."* Este acto la sigue al pie de la letra, incluso siendo él mismo el que produce el drift.

**(i) T03 — tres citas verbatim de "v2_6.md" sin prefijo.** `forense/encargos/2026-08-13-A8LAND-instrucciones-v2_7.md` (líneas 53, 61, 74) cita ese nombre corto, entre backticks simples en el propio archivo, tres veces, sin el prefijo "instrucciones-proyecto-" — el archivo real existe (`instrucciones-proyecto-v2_6.md`), pero T03 compara por nombre exacto y no lo reconoce. Las tres citas son del **propio texto del encargo**, y `forense/encargos/convencion.md` exige archivarlo "tal como se lanzó — no un resumen" — editarlas para silenciar T03 violaría esa regla. **No se editan.** Precedente exacto ya en el baseline: `forense/encargos/2026-08-12-M6-sello.md` tiene una entrada T03 aceptada de la misma clase (cita a un archivo externo que no existe en el repo). Esta es nueva, no congelada — queda para que mesa decida si la acepta con `--freeze` o si adopta un marcador (`{cita-ilustrativa}` ya existe para T03) sobre el propio archivado.

**(ii) T16 — `gobernanza-v1_15.md:1104`, dentro de `ADR-76(f)`.** Esa línea dice *"`python3 tests/check.py --baseline` → **18 FAIL · 104 WARN**, **LÍNEA BASE: VERDE**"*, explícitamente sobre lo que `ADR-76` midió **al sellarse** ("Estado derivado en este acto, no copiado"). Es correcto hoy como registro histórico — mi propia corrida "antes" (mismo commit `8b30306` contra el que ADR-76 se selló) confirma exactamente esa misma cifra. Cambiarla a 107 falsearía lo que ADR-76 realmente midió. `T16` (`tests/check.py:574-611`) es puramente mecánico — solo reconoce como histórico el formato `> **vX.Y — fecha.**` (`_CAMBIO_FECHADO`), y su propio docstring documenta este exacto punto ciego como límite conocido, no como bug a corregir aquí (`tests/check.py:578-592`). Nota: `gobernanza-v1_15.md:1116` usa el mismo dato con backticks simples en vez de negritas — no matchea el regex de T16 en absoluto, nunca apareció como FAIL en ninguna corrida.

Fix real de cualquiera de los dos, si mesa lo quiere: (i) congelar con `--freeze` bajo un ADR que lo autorice, o extender el marcador `{cita-ilustrativa}` al archivo de encargo; (ii) extender `_CAMBIO_FECHADO` de T16 para reconocer prosa histórica fuera del formato de blockquote — cambio a `tests/**`, fuera del perímetro que este encargo concede.

---

## 5 · Brecha de perímetro encontrada y corregida: el propio archivo de encargo

El encargo A8-LAND no incluyó su propio archivo de encargo en la lista de perímetro (§ "Perímetro y concurrencia"), a diferencia de sus precedentes recientes — `forense/encargos/2026-08-13-adr-provisionalidad.md:14` sí lo declara explícitamente ("`forense/encargos/2026-08-13-adr-provisionalidad.md` (este archivo, A.3)"). La regla A.3, vigente desde v2.5, exige que todo encargo lanzado se commitee en `forense/encargos/` antes o junto con su lanzamiento. Encontrado durante COMMIT 2 (no en el ARRANQUE, donde debería haberse notado), corregido en el mismo acto que lo encontró, en su propio commit — `forense/encargos/2026-08-13-A8LAND-instrucciones-v2_7.md`, con nota de archivado explicando la brecha al inicio del archivo. Mismo criterio que ADR-76(b) ya sentó precedente: tocar algo fuera de la lista corta se declara, no se oculta ni se convierte automáticamente en PARO cuando la razón es sólida y el archivo es aditivo.

---

## 6 · La instrucción "Tres reglas"/"Cuatro reglas" — seguida al pie de la letra, no completada más allá

La INSERCIÓN 2 del encargo especifica dos sustituciones exactas en la línea de cierre de Bloque D-ter: `Tres reglas.` → `Cuatro reglas.` y `manda sobre las tres` → `manda sobre las cuatro`. Esa misma línea contiene una tercera ocurrencia del mismo conteo — `Las tres salen del mismo defecto...` — que el encargo no menciona. Dejarla sin cambiar produce, en `instrucciones-proyecto-v2_7.md`, la secuencia "Cuatro reglas. [...] Las tres salen del mismo defecto [...] manda sobre las cuatro" — internamente inconsistente en número. Se siguió el encargo exactamente como está escrito (dos sustituciones, no tres) en vez de tomar la libertad editorial de "corregir" una tercera no instruida — mismo criterio que A.3/A.8 exigen: el texto va tal como se dio, y una instrucción incompleta se reporta, no se completa por inferencia. Reportado aquí para que mesa decida si `Las tres` era una omisión o intencional.

---

## 7 · Contadores que NO se mueven

`13 de 27` (Hito D) · `9 de 14` (condicionales) · `0 de 15` (coeficientes) · `1 de 2` (llaves) · `4 de 144`. **Ninguno.** Este acto no tocó `milpa/`, `canon/modelo-decision-v4_0.md`, ninguna celda-D, ni ninguna tabla de `data/` — es documental y de gobierno puro, mismo perímetro que `ADR-72`/`ADR-76`.

A.9 inciso (b) queda **`PENDIENTE`**: `instrucciones-proyecto-v2_7.md` no se ha pegado todavía en el proyecto de Claude — no puede haberlo hecho este acto, el pegado es a mano, fuera del repositorio, y es la propia regla A.9 aplicada a sí misma en su primer uso. El texto íntegro, listo para pegar, va en el reporte de cierre de este acto (fuera de este repositorio) — sin eso, A.9 diría que esta misma versión no está sellada.

---

## 8 · La línea honesta

Este acto no midió nada sobre México — es exactamente lo que su propio texto (A.8/A.9) dice que debe ser: documental, verificado antes de escribirse, y franco sobre lo que no pudo cerrar. Los dos hallazgos de §4 no son defectos que este acto causó por descuido — son el costo mecánico, ya anticipado por el propio T16/T03, de archivar un encargo verbatim y de preservar la historia de un ADR ya sellado en vez de retocarla para que un test pase. Forzarlos a VERDE habría exigido exactamente lo que `ADR-76(f)` ya prohibió: recongelar sin mesa. La brecha de §5 (el propio encargo, fuera de su propia lista de perímetro) es la clase de cosa que A.8 — la regla que este mismo acto sella — existe para atrapar la próxima vez, en otro acto.

---

**Requisito de salida.** Este archivo; línea en `forense/hallazgos.md` (append); `forense/encargos/2026-08-13-A8LAND-instrucciones-v2_7.md` actualizado de `VIVO` a `CONSUMIDO` al cerrar; `tests/check.py --baseline` corrido antes (`18 FAIL · 104 WARN`, VERDE) y después (`19 FAIL · 107 WARN`, ROJO por los dos hallazgos declarados en §4, ninguno congelado unilateralmente); texto íntegro de `instrucciones-proyecto-v2_7.md` entregado en el reporte de cierre, listo para pegar en el proyecto de Claude (A.9).
