# Nota de cierre · GEN2-ASTRA5-U2-ENDIREH-CIERRE-1 · 24/sep/2026

Contadores movidos: **cero** (no mide, no adopta).

- ARRANQUE: hook `ENTORNO-DERIVADO = NUBE` = encargo; base `c12a0d87` = SHA de redacción (0 detrás); sin rama, worktree ni PR abierto con el rótulo; 0-bis `b0df5e63`.
- Premisa `[SUPUESTO]` (§3) verificada: los 16 CALC sellados tienen `sello.json` y `cuenta_gen2: SI`; ninguno requiere NC por etiqueta.
- §4: `ls canon/L0 | grep -ci endireh` → 0 (tampoco hay uno de #1099); `ls forense/encargos | grep -c ENDIREH-CIERRE` → 0 antes del 0-bis. La cascada no existía: PARO (f) no aplica. #1099 dejó un «ADR raíz» como propuesta en `forense/analisis/dominios/genero/endireh-adr-raiz.md`, no en `canon/`; se cita, no se duplica.
- «Hecho» (§1): `grep -c ENDIREH forense/replay-evidencia.tsv` → 16 = CALC ENDIREH sellados (16). Ningún asiento faltante; no se añadieron filas.
- Hallazgo: `CALC-ENDIREH-PISOS-2006-MODULOS-0001` sin sello y sin declaración → `NC-…-b0df-01`.
- Recibo: `forense/analisis/astra5-genero-endireh/recibo-para-claude.md`. Ruta del encargo seguida verbatim; el resto de la unidad vive en `forense/analisis/dominios/genero/`.
