# Nota de cierre — `ACTO CIERRA-4-FIRMAS`, 26/ago/2026

Ejecuta el mensaje de lanzamiento del 26/ago/2026 (cuatro firmas de mesa, ninguna ranura ausente), propagado en `canon/gobernanza-v1_15.md` `ADR-198`. Entorno **NUBE** (`cloud_default`).

## §1 · Las cuatro firmas, verbatim

- **L1:** «CONFIRMO R8.2: fila B con la reserva escrita del anexo de #368.»
- **L2:** «FIRMO R2.2: archívese D con el universo combinado; la vía llave-clima y la spec IMSS×NOM-035×Cheng/Farh quedan sembradas como reapertura.»
- **L3:** «FIRMO FP-163: banda TOST y margen material del piloto = regla derivada Δ_material = 0.5·EE(R), banda [−0.5·EE(R), +0.5·EE(R)], tal como el pre-registro la justifica.»
- **L4:** «SELLO FP-160: la spec B-bis de EXP-COMPARTAMOS-1 queda sellada tal como está propuesta.»

Las cuatro llegaron — ninguna ranura vacía.

## §2 · `R8.2` (L1) → archiva `B`

Cierra la ranura de confirmación de 5 segundos que `ACTO PROPAGA-LETRAS` (`ADR-194`/`ADR-196`, Nota 34 de `hitoD-preregistro`) dejó abierta. Confirma la fila `B` de la escala de la ficha (apps con usuarios pero sin tasa de incumplimiento publicada), con la reserva escrita del anexo de `#368`: las apps de tanda mexicanas localizadas son organizadoras entre conocidos — no custodian dinero, no ejercen enforcement de plataforma — y ninguna publica tasa de incumplimiento; la variante plataforma-entre-desconocidos que el falsador imagina no se localizó operando en México, así que el falsador, tal como está redactado hoy, no tiene objeto medible con métrica pública. Universo: 2 queries de dirección + censo `R2.1` + cruce `#363`. Enmienda fechada aplicada en la ficha (`forense/hitoD-preregistro-v2_0.md`); línea nueva en el Registro de veredictos.

## §3 · `R2.2` (L2) → archiva `D`

El censo que la enmienda del 25/ago/2026 dejó pendiente de mesa corrió: `forense/R22-censo-liderazgo-v1_0.md`, copiado byte-idéntico del compass entregado por dirección — `sha256` `3a865d27e71e3e1763d46cff9354095925d47b2fd698edb0563aee1f2f9130bd`, verificado igual tras la copia, antes de anexarle la línea de procedencia ("corrido por mesa en sesión del proyecto con web, 26/ago según su propia estampa"). Veredicto propio del censo: **SIN-DATO** — no se localizó, en el universo declarado (bases/términos de su §ESTAMPA, A.10), ninguna fuente mexicana que cruce tipología de liderazgo (paternalista benévolo/autoritario, escala Cheng/Farh) con desenlaces duros del empleado (rotación/desempeño/lealtad), controlando sector, salario y prestaciones.

Estampa de universo combinada `A.10`: pase `#361` (RE-ESPEC→censo, ejecutado) + gemelo `b06ad2f` (SIN-DATO-mundo) + cruce `#363` (falsador NO-ENCONTRADO) + este censo (universo verbatim de su propia §ESTAMPA, incluida la reserva de tesis de posgrado no indexadas centralmente).

**`D` no es juicio sobre la regla del constructo — el ataque nunca corrió.** Reservas que viajan con el veredicto:
1. La literatura mexicana empírica mide liderazgo **transformacional**, no la escisión benévolo/autoritario (Cheng/Farh) que este falsador exige — el constructo de la ficha nunca se ha medido contra desenlaces duros en México.
2. La candidata "estrella" (Martínez 2003) es **cualitativa** y **clase (b)** — mezcla organizaciones en México con firmas de la diáspora en EE.UU., diáspora, no evidencia limpia sobre México.
3. Sembradas como reapertura, **VENCIBLE EN ALCANCE**, no ejercidas por este acto: la spec-candidata IMSS×NOM-035×Cheng/Farh que el propio censo propone, la vía llave-clima (sinergia con el `D` de `R10.2` ya anotada en `Nota 34`), y los dos umbrales que el censo nombra (agregado STPS/INEGI publicado; panel empresa-empleado con liderazgo).

La regla queda `[MEDIA-FUERTE]`, con su ataque sin correr. Enmienda fechada aplicada en la ficha; línea nueva en el Registro de veredictos.

**Hito D: 21 de 27 → 23 de 27**, cifra derivada de `python3 tests/check.py` T18/T20, no tecleada. Composición: `12D·2B·4A·2E·1C` → `13D·3B·4A·2E·1C`. Recifrado: `README.md:36`, `canon/estado-programa-v1_10.md`, `canon/gobernanza-v1_15.md`, y las citas vivas de `canon/modelo-decision-v4_0.md` (`:65`, `:700`, `:885`).

## §4 · `FP-163` (L3) → FIRMADA

Mesa sella la banda TOST y el margen material del piloto (`Δ_material = 0.5·EE(R)`, banda `[−0.5·EE(R), +0.5·EE(R)]`) tal como `forense/prereg-duelo-v2/banda-tost-margen-v1_0.md` la derivó y `forense/prereg-duelo-v2/prereg-corrida-v1_0.md §F3` la propagó sin re-derivar. El pre-registro **no se modifica en su regla** — este acto solo propaga el sello. `ejecutada_en` = este acto, en `forense/firmas-pendientes.tsv`. Desbloquea las corridas `L` (no las ejecuta): cuando corran, adjudicarán la casilla (3) de `ADV1-M5` (empate-TOST) con banda firmada.

## §5 · `FP-160` (L4) → FIRMADA (spec B-bis sellada)

Mesa sella la spec B-bis de `EXP-COMPARTAMOS-1` (`forense/spec-bbis-exp-compartamos-v1_0-PROPUESTA.md`, COMMIT 1) tal como está propuesta — opción (a) de la fila: pasa a v1.0 congelada, habilita el acto `EJERCE-LLAVE` posterior en `UBUNTU`. **Enmienda de ESTADO, no de archivo**: el archivo conserva su nombre —las citas vivas de `modelo-decision`/`registro-llaves-identificacion` siguen apuntando a él sin cambio— pero queda declarada **sellada, vigente** en `ADR-198` y en `forense/firmas-pendientes.tsv`. Gatea, sin ejecutarlo, el acto futuro `EJERCE-LLAVE` (abriría el microdato de `116334-V1.zip` bajo esta spec ahora congelada) — este acto no abre ningún microdato.

**Estado de llaves de identificación** (`canon/estado-programa-v1_10.md`, población de conteo propia, `ADR-67(c)`): el conteo sigue **`4` de `5`**, sin movimiento — lo que cambia es la descripción de la spec de la 5ª llave (`EXP-COMPARTAMOS-1`), de "en mesa" a **sellada, ejercicio pendiente**. El numerador solo sube a `5` de `5` cuando el acto `EJERCE-LLAVE` corra el diseño sobre el microdato, congelado por esta spec.

## §6 · Suite y cierre

`python3 tests/check.py --baseline` (nunca `--freeze`): **19 FAIL · 129 WARN, LÍNEA BASE VERDE** contra `tests/baseline.json`. Neto de WARN −2 sobre la corrida anterior: `FP-163` y `FP-160` salen de `ABIERTA` (pasan a `FIRMADA`) y dejan el vigía `T22`(a).

`ADR-198` con las cuatro firmas verbatim. Set de sincronía: `README.md:36` (23 de 27, `13D·3B·4A·2E·1C`); `canon/estado-programa-v1_10.md` (Hito D, cabecera de ADR 198, suite); `canon/gobernanza-v1_15.md` (cabecera 198 ADR, línea de Hito D); citas vivas de `canon/modelo-decision-v4_0.md` (`:65`, `:700`, `:885`); `forense/firmas-pendientes.tsv` (`FP-160`/`FP-163` → `FIRMADA`); esta nota-cierre; encargo `CONSUMIDO`.

## Perímetro y NO-hace

Lista cerrada: Registro del pre-registro + fichas `R8.2`/`R2.2` (enmiendas fechadas) + `forense/R22-censo-liderazgo-v1_0.md` [nuevo, copia byte-idéntica] + `README.md:36` + citas `modelo-decision` + `gobernanza` (`ADR-198`) + `estado` + tablero de firmas (`forense/firmas-pendientes.tsv`) + notas + encargo. Fuera de esta lista, PARA.

No ejerce la llave de `FP-160` — no se abrió ningún byte de `116334-V1.zip`. No corre ninguna `L` ni calcula `R`/CV de `FP-163`. No convierte el `D` de `R2.2` en juicio sobre la regla del constructo — **el ataque nunca corrió**, y se dice con esas palabras en la entrada del Registro. No toca `R2.1`/`R3.4`/`R10.1`/duelo-corredores/pool/milpa. No renombra `forense/spec-bbis-exp-compartamos-v1_0-PROPUESTA.md` — las citas viven con ese nombre.
