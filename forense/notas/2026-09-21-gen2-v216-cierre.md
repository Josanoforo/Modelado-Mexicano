# Nota de cierre · ACTO GEN2-V216

**Entorno.** NUBE (`cloud_default`). Sonda a INEGI `DENEGADA-POR-POLITICA` (http_code=000, connect 403). Corpus no montado (`archivos_examinados=0`). Cero microdato, cero descargas — este acto no lo necesitaba.

**Base.** El encargo se redactó contra `b219aeef`; `main` se movió a `c839364` antes de arrancar. No es PARO (§2 de v2.15/v2.16): se refrescó y se re-derivó sobre el `HEAD` nuevo sin que ninguna premisa de contenido dependiera del SHA viejo.

**Lo que se hizo.** P1: `instrucciones-proyecto-v2_16.md` entró byte a byte igual al adjunto (sha256 confirmado antes de commitear). P2: la HISTORIA se extendió con una entrada por cada renglón del delta del cuerpo, citando el mismo origen (PR/hallazgo/firma) que el propio cuerpo trae; las 13 semillas `PARA-v2.16` de `hallazgos.md` se revisaron una por una — 11 promovidas, 2 declaradas explícitamente NO promovidas con texto propuesto para `v2.17` (no se metieron al cuerpo verbatim). P3: `PLANTILLA-ENCARGO-v2_1.md` nueva, con las cinco piezas que el encargo pedía (ids con raíz de acto, sin campos a rellenar, adendas como archivo propio, adjuntos con sha256/embebido, «hecho» por comando, test huérfano en CI); `PLANTILLA-LOTE-v1_0.md` actualiza su puntero. P4: `CLAUDE.md` importa v2.16; `instrucciones_vigentes` sube a v2.16 por derivación de `tools/tablero_programa.py`, no a mano; `test_arnes_sesion.py` sigue en verde. P5: este ADR con raíz de acto, fragmento L0 propio, registro de rótulo `GEN2-V216`.

**Lo que no se tocó.** `acto.md` (el encargo no cita `v2.15` por nombre en ningún punto que exigiera editarlo — el bloque de ejecución de la skill ya describe el aparato v2.16 sin nombrar versión). `data/raw` (no aplica). Ninguna versión anterior de instrucciones se borró (PARO b del encargo).

**Suite.** `tests/check.py --baseline --parallel` VERDE (código 0); 12 FAIL + 1 WARN de la línea base ya no aparecen (mejora, no se baja la cifra congelada). `tests/test_arnes_sesion.py`: 13/13.

**Contador.** `cuenta_gen2 = NO-APLICA`. Cero contadores del programa movidos: este acto instala norma, no mide.

**Sucesor.** `v2.17` revisa las dos semillas no promovidas (`MOTOR-LINAJE-1` sobre el estado `superado`; `ARBITRO-MARGINALES-1` sobre «vencido» sin retador) y la caducidad de D-24 a los tres meses (21/dic/2026), como el propio cuerpo v2.16 declara en su sección 9.
