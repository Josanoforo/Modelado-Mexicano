# ENCARGO · ACTO GEN2-ADOPCION-BLOQUE-Y-PINES-4 · Sucesor de #1114 con la premisa verificada: las tres reglas de la propuesta ola 5 que citan los diez RESULT no existen en el motor vivo; se crean en `milpa/tramite.yaml` por el escritor, con los campos pendientes redactados por el ejecutor y rotulados, y mesa adopta al fusionar

> ENTORNO: **NUBE** — RESULT sellados, la propuesta ola 5, el escritor, reports GEN1 como fuente de tier/porqué; cero microdato. Hook imprime ENTORNO-DERIVADO; si dice CAJA, PARA.

CABECERA · SHA de redacción `3252aaec` (re-deriva al abrir) · una sola sesión, rama propia `acto/gen2-adopcion-bloque-y-pines-4` (o la que fije la plataforma; se declara) · MODELO: Opus · MODO: **AUTÓNOMO** (cláusula v1.0 en §6) · ids con raíz de acto (D-24) · D-21 aplica · «Si te encuentras escribiendo fuera de esta lista de §9, PARA.» · cierre por /acto: `## NO-CORRIDO / RESERVAS` («Ninguno.» obligatorio si aplica) y `## CONSUMIDO` al pie.
CONTADOR: cero mediciones. Mueve por derivación: `N_resultados_gen2_pendientes_adopcion` 10 → 0; `adoptados_activos` +10; `legacy_activas_por_consumidor__motor` no sube (las reglas nuevas nacen GEN2). Escribir en `milpa/tramite.yaml` es la adopción: **merge de mesa**, nunca auto-merge.

## 1 · OBJETIVO
Verificado por dirección sobre `3252aaec` (esta vez sí): `milpa/tramite.yaml` tiene 22 reglas y **ninguna** de las tres conductas (`grep -c` de `contexto_institucional_alto`, `atraso_credito`, `atraso_tarjeta`, `valora_afirmativamente` → 0). Las tres viven en `milpa/tramite-ola5-propuesta-v0.yaml` con `p` y `corrida0_resultado_id` correctos: `contexto_institucional_alto_{2019,2023}` (l.4004-4010; 2021 NO-ESTIMABLE), `atraso_{credito_hipotecario,tarjeta_credito,credito_nomina,credito_personal,credito_automotriz}_2024` (l.4046-4058), `valora_afirmativamente_seguridad_social_{total,con_acceso,sin_acceso}` (l.4074-4080) — con `rol_uso: proxy_descriptivo` y `situacion/tier/porque/falsable_si: PENDIENTE-DE-MESA`. El motor no carga la propuesta. Por tanto la adopción es **crear las tres reglas en el consumidor vivo**, copiando verbatim `p`, IC, fuente, universo, ponderador, `ola_calibracion`, `corrida0_resultado_id`, `corrida0_generacion` y `rol_uso` de la propuesta (patrón ya usado: enmienda ENCUCI FP-200=b, `tramite.yaml` l.63-70, «copiados verbatim de milpa/tramite-ola5-propuesta-v0.yaml»), y **redactando** los cuatro campos pendientes (cláusula §3): `situacion` (segmento y disparador), `tier` (según §3 de instrucciones: FUERTE solo con RESULT GEN2 detrás — aquí sí lo hay, pero es proxy descriptivo: **MEDIA** salvo que el report lo suba con razón), `porque` (driver, citando el report GEN1 correspondiente: *Confianza y desconfianza* / *Autoridad* para contexto institucional; *Finanzas conductuales* / *Crédito fácil* para atraso; *Psicología del Trabajo* para seguridad social) y `falsable_si` (qué ola o dato lo cambiaría), todos rotulados `PROPUESTO-POR-EJECUTOR`. `uso_motor: PROXY-DESCRIPTIVO` explícito: no se sortea como confirmación independiente (mismo rótulo que la propuesta usa en l.4006-4009).
«Hecho» sobre el commit final con origin/main fusionado: `milpa/tramite.yaml` con tres reglas nuevas (o las que la propuesta separe; el conteo lo da el diff) que citan los diez RESULT por id, `p` idéntico al RESULT (test por conducta), `rol_uso: proxy_descriptivo`, cuatro campos redactados y rotulados · diff seco en la nota antes de `--apply` · escritor extendido (`tools/escribe_relevo_consumo.py`: modo «crear regla desde propuesta», con test) · `status` en rama: `pendientes_adopcion` 0, `adoptados_activos` 82 (o el valor derivado, citado) · el motor carga y emite con las reglas nuevas (`tests/test_motor_gen2_explicito.py` o el que aplique, VERDE) · `T-REPRO` VERDE · `check.py --baseline` VERDE sin `--force`.

## 2 · FIRMAS DE MESA — dadas, verbatim
- **Bloque de diez (FIRMAS-7, 21/sep, `…369b-01`)**: los diez RESULT están ADOPTADOS por firma; faltaba el consumidor.
- **Decisión 2 de ADOPCION-2 (W, 24/sep)**: «Los diez RESULT pendientes se adoptan escribiendo en milpa/tramite.yaml, solo por el escritor de consumo, un RESULT por regla existente, con diff seco en la nota; el PR lo fusiona mesa.» **INTERPRETACIÓN-DECLARADA (cláusula §2):** «regla existente» presuponía un hecho falso; la intención (que los diez entren al motor vivo por el escritor, con diff seco y merge de mesa) se preserva creando la regla desde la propuesta ola 5, que es la fuente que la firma tenía a la vista.
- **Mesa, 24/sep (chat):** «le damos cierta autonomía a la sesión para que corra completo y resuelva» → cláusula v1.0 (§6), que autoriza redactar los campos pendientes rotulados.

## 3 · LO QUE DIRECCIÓN SABE
- `[EJECUTADO]` (`3252aaec`) `grep -c '^  - id:' milpa/tramite.yaml` → 22; `grep -c` de las tres conductas → 0. Propuesta ola 5: líneas citadas en §1; 80 `PENDIENTE-DE-MESA` en todo el archivo (solo los de estas tres reglas son de este acto). Patrón de regla viva con cita: `tramite.yaml:62` (`corrida0_resultado_id: RESULT-ENCIG-MOR-A-P-SOL1`, `corrida0_generacion: GEN2`) y l.82.
- `[LEÍDO]` Nota de #1114 (`claude/new-session-9bylha`): tabla RESULT → conducta → `p` de la propuesta, NO-ENCONTRADO en `tramite.yaml` los diez; FP `…ADOPCION-3-d164-01` con opciones. Este acto la cierra con `DECISIÓN-DADA: crear reglas (cláusula §3)`.
- `[LEÍDO]` `tools/escribe_relevo_consumo.py` V1: escribe etiqueta en regla existente; no crea reglas. Extenderlo es parte del acto (modo «crear desde propuesta», que copia el bloque verbatim de la propuesta y añade los cuatro campos).
- `[SUPUESTO]` que `emisor.py` (l.476-479, «devuelve la PRIMERA coincidencia») no colisiona con conductas ya existentes: las tres son nuevas; si una colisiona por prefijo, se desambigua por sufijo de ola como hizo ENCUCI (`_encig2025`), y se declara.
- ADJUNTOS: `CLAUSULA-AUTONOMIA-v1_0.md` (sha256 al lado; se archiva en `forense/encargos/` y se cita desde el ADR).

## 4 · YA HECHO / YA DECIDIDO
`git ls-remote --heads origin | grep -i 'adopcion'` → `claude/new-session-9bylha` (#1114, PARO; mesa lo fusiona como diagnóstico o lo cierra: este acto no depende de él). `grep -c 'PROPUESTO-POR-EJECUTOR' milpa/tramite.yaml` → 0.

## 5 · PIEZAS
- **P1 · Escritor: modo «crear desde propuesta».** Lee el bloque de la propuesta por `conducta`, copia verbatim los campos medidos, añade `situacion/tier/porque/falsable_si` desde un archivo de redacción del ejecutor (`forense/analisis/adopcion-4/redaccion-reglas-v1_0.yaml`, con fuente por campo), escribe la regla en `tramite.yaml` bajo un bloque comentado que cite este acto, la propuesta (líneas) y la firma. Test: `p` y `corrida0_resultado_id` idénticos a la propuesta y al RESULT; los cuatro campos presentes y rotulados.
- **P2 · Redacción.** Un YAML por conducta con `situacion`, `tier` (regla: MEDIA por defecto para proxy descriptivo; FUERTE solo si el report y el RESULT lo sostienen, con cita), `porque` (report GEN1 citado por archivo y sección), `falsable_si` (ola o dato). Todo `PROPUESTO-POR-EJECUTOR`. §3 de instrucciones aplica: sin confundir estructura con cultura; unidad declarada (contexto institucional: víctimas de extorsión, unidad persona-víctima; atraso: tenedores del producto; seguridad social: ocupados).
- **P3 · Diff seco → nota → `--apply` → motor carga → tests → `status`.**
- **P4 · Cierre.** FP `d164-01` y NC `369b-01`, `ec71-01` cerradas; hoja `PENDIENTE-DE-MESA` de la propuesta: las tres reglas cambian a `ADOPTADA-EN-MOTOR: <este acto>` en el archivo de propuesta (comentario, no edición del `p`).

## 6 · LATITUD — CLÁUSULA DE AUTONOMÍA v1.0 (verbatim; adjunta)
1. Discrepancias encargo↔repo las resuelve el ejecutor y las declara. 2. Firma con letra en choque y intención clara: INTERPRETACIÓN-DECLARADA, se sigue. 3. Lo redactable se redacta, rotulado PROPUESTO-POR-EJECUTOR, con fuente; mesa adopta al fusionar. 4. Bifurcación con opción recomendada: se ejecuta la recomendada. 5. PARO solo por D-19 estricta (dato reservado · sello · contador a mano/adoptar sin firma de contenido · procedimiento congelado · entorno). 6. Nunca: cifra tecleada, sello reescrito, reserva abierta, fuera de §9 sin declarar; merge de mesa cuando se escribe el motor. 7. El «Hecho» no se rebaja; lo no alcanzado va a NO-CORRIDO.
Pregunta a mesa prevista: **ninguna**. Si el ejecutor cree que hay una, primero aplica §1–§4.

## 7 · PAROS — lista cerrada (D-19 estricta)
a) abrir dato reservado · b) reescribir un sello, `--force`, editar `tramite.yaml` fuera del escritor · c) mover contadores a mano; cambiar un `p` respecto al RESULT · d) cambiar un procedimiento congelado · e) CAJA.

## 8 · COMPUERTAS
«`p` y cita idénticos al RESULT, por test» protege: **congelar** (§2 regla de oro). «Campos redactados rotulados y con fuente; merge de mesa» protege: **adoptar** (E.2: mesa adopta con los ojos abiertos).

## 9 · PERÍMETRO Y CONCURRENCIA
Propio: `milpa/tramite.yaml` (solo por escritor), `milpa/tramite-ola5-propuesta-v0.yaml` (solo comentario de estado), `tools/escribe_relevo_consumo.py` + `tests/test_escribe_relevo_consumo.py`, `forense/analisis/adopcion-4/`, `forense/encargos/CLAUSULA-AUTONOMIA-v1_0.md` (archivo), `no-corrido.tsv`/`firmas-pendientes.tsv`, nota, L0, cascada. Ajeno: `decisiones.tsv` (no hace falta: la adopción es el merge), catálogo, procedencia (CATALOGO-CONTRATO-Y-TEST-1), CALC, `emisor.py` (si exige cambio, ≤ 10 líneas declaradas o NC). En vuelo: CATALOGO-CONTRATO-Y-TEST-1, ENCIG-PISOS-GEN2-1 (caja), U5 (caja), TABLERO-EN-CANAL-1.

## 10 · LO QUE NO HACE · SUCESORES
No cambia valores, no releva las 34 lecturas legacy del motor (son otras reglas), no sortea proxies como confirmación. Sucesor: FIRMAS-16 ratifica los cuatro campos redactados (o los corrige) por fila; el informe v1.3 cita las reglas como proxies descriptivos.
