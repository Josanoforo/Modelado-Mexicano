**ADR-260923-GEN2-TRAMITE-FIRMAS-12-c3fa-01**, `ACTO GEN2-TRAMITE-FIRMAS-12`, 23/sep/2026, NUBE, Sonnet 5, MODO ABIERTO, COMPUERTA: ninguna de merge. Asienta las cuatro decisiones de mesa del 23/sep (tarde) — D1 (tercero de tiempo sobre sellos y COMMIT-1), D2 (posicionamiento benchmark auditable), D3 (segunda pasada del deep search), D4 (AMAI NSE como corte de clase) — como filas `FIRMADA` con texto verbatim en `firmas-pendientes.tsv`, verbatim dentro del propio encargo que las ejecuta (§0). Deja la alianza académica (validador neutral CIDE/ITAM) como quinta fila `ABIERTA`, dueño mesa, tal como el encargo la declara. Los tres adjuntos de fuentes (informe de competencia, brief, adenda ASTRA-4) y el cuarto de P3 (hoja de decisiones) no llegaron con esta invocación — solo el .md del encargo —, así que P2/P3 quedan `NO-CORRIDO` (PARO f del propio encargo, aplicado textual: se declara el resto), sin archivar nada sin sha verificable. Dos filas nuevas en `no-corrido.tsv` (`c3fa-01`, `c3fa-02`), sucesor `GEN2-TRAMITE-FIRMAS-13` nombrado por el propio encargo §10. `celdas_validadas` sin cambio (92). CONTADOR del acto: cero mediciones, no adopta (declarado desde la cabecera). Suite `--rapido` VERDE, 0 FAIL.
