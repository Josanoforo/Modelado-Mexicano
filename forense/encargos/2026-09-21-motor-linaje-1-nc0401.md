# ENCARGO · ACTO MOTOR-LINAJE-1 · el cargador de linaje de emisión deja de reventar ante un id íntegramente superado, y lo hace nombrando al sucesor en vez de seguirlo en silencio

> ENTORNO: **NUBE** — el hook de arranque imprime ENTORNO-DERIVADO; si no coincide, PARA en una línea.

CABECERA · SHA de redacción: `d44949529e32a84942995214bec0eec19630458a` · una sola sesión
(rama `motor/linaje-1`) · MODELO SUGERIDO: **Opus** (toca un cargador que decide qué número
emite el motor; no es receta) · MODO: **ABIERTO** · CALC-id reservado: ninguno
CONTADOR · mueve: NC-0401 cerrada, y de paso NC-0402 y NC-0397 cerradas con evidencia.
**NO debe moverse**: ningún contador de adopción, ninguna cifra de `milpa/tramite.yaml`,
ninguna fila de `data/corrida0/resultados.tsv`.

## 1 · OBJETIVO

Que `tests/test_motor_gen2_explicito.py` vuelva a correr en CI, y que la razón por la que hoy
no corre quede cerrada por contrato y no por parche. Al cerrar debe existir: (a) el cargador
con el comportamiento de §5-P1; (b) el pin del consumidor actualizado con delta calculado por
script, o la pregunta a mesa de §5-P2; (c) los tres tests cableados en CI; (d) las tres filas
NC cerradas.

«Hecho» significa: `PYTHONPATH=. python3 tests/test_motor_gen2_explicito.py` sale 0, los otros
dos siguen en 6/6 y 9/9, `tests/check.py --baseline` VERDE, y los tres corren en `verify.yml`.

## 2 · FIRMAS DE MESA

Verbatim, 21/sep/2026: «test_motor_gen2_explicito: el encargo es TUYO (emisor.py es tu
perímetro). Una condición: el cargador NO sigue la sucesión en silencio. Que un id íntegramente
superado resuelva solo a su sucesor cambia qué número emite el motor sin que nadie lo firme —
eso es adopción por la puerta de atrás. El contrato correcto: error explícito que nombre al
sucesor; y el pin del consumidor se actualiza en el mismo acto CON delta calculado por script.
Si el valor del sucesor difiere del anterior más allá de la tolerancia del tipo, no se escribe:
va a mesa por los bins de la ADENDA del 15/sep. La vista resultados.tsv no se toca (es
derivada). Cierra de paso NC-0402 y NC-0397 con tu evidencia: 6/6 y 9/9 en clon completo; la
causa era el clon superficial que tests/check.py provocaba (arreglado en #923).»

## 3 · LO QUE DIRECCIÓN SABE — cada línea con su rótulo

- [EJECUTADO] En clon completo de `d4494952`: `test_motor_holdout` 6/6 ok,
  `test_celda_d_piloto_consumidor` 9/9 ok, `test_motor_gen2_explicito` falla en `setUpClass`
  con `ValueError: RESULT-C1-POSEL-AMENAZA-DELTA: se esperaba una fila vigente; hay 0`.
- [LEÍDO] `milpa/src/emisor.py:594-610`: `cargar_indice_linaje_emision` agrupa por
  `resultado_id`, filtra las filas cuyo `estado` empieza con `SUPERADO`, y exige
  `len(vigentes) == 1`; con 0 lanza el mismo `ValueError` que con 2.
- [LEÍDO] `data/corrida0/resultados.tsv:213`: ese id tiene **una sola** fila, con
  `estado = SUPERADO→CALC-0001-v2`. El sucesor vive bajo otro `resultado_id`.
- [EJECUTADO] Inventario de estados en la vista: ningún otro `resultado_id` queda hoy con cero
  filas vigentes; este es el único caso vivo. **Eso no autoriza tratarlo como caso único**: el
  contrato tiene que quedar bien para el siguiente.
- [REPORTADO] mesa: la causa de NC-0402 y NC-0397 era el clon superficial que provocaba
  `tests/check.py`, corregido en #923. Dirección no abrió #923; la evidencia que sí tiene es la
  primera línea de esta lista.
- [SUPUESTO] Que el valor de `CALC-0001-v2` está dentro de la tolerancia del tipo respecto del
  valor superado. **No verificado.** Si resulta falso, la rama prevista es §5-P2.

## 4 · YA HECHO / YA DECIDIDO — búsqueda por objeto

Dirección buscó por objeto «cargador de linaje / fila vigente / resultado superado» en
`forense/no-corrido.tsv`, `data/corrida0/decisiones.tsv` y los ADR del repo; encontró las tres
NC citadas y ninguna decisión previa sobre qué debe hacer el cargador con un id íntegramente
superado. Universo: repo completo en `d4494952`.
**Al ejecutor: repítela con tu acceso, que es mejor que el mío.** Si existe un ADR que ya fija
este contrato, manda ese ADR y este encargo se reduce a aplicarlo.

## 5 · PIEZAS

**P1 · El contrato del cargador.** Produce: `cargar_indice_linaje_emision` distingue tres
situaciones que hoy colapsa en una sola excepción — (i) exactamente una vigente: como hoy;
(ii) más de una vigente: error, como hoy; (iii) **cero vigentes**: error propio, distinto, que
nombra el id, dice que está íntegramente superado y **nombra al sucesor** tal como lo declara
el campo `estado`. No resuelve al sucesor. No devuelve `None`. No omite la entrada.
Se sabe que quedó bien: hay un test que ejercita los tres caminos con datos sintéticos, y el
mensaje de (iii) contiene el id del sucesor.

**P2 · El pin del consumidor.** Produce: el consumidor que hoy pide
`RESULT-C1-POSEL-AMENAZA-DELTA` pasa a pedir el sucesor, **y el delta entre el valor viejo y el
nuevo lo calcula un script, no una lectura a ojo**, y queda en el diff.
Rama prevista, que es parte del encargo y no una desviación: si el delta excede la tolerancia
del tipo declarada para ese resultado, **no se escribe el pin**; se entrega el delta, se abre
fila en `forense/firmas-pendientes.tsv` y va a mesa por los bins de la ADENDA del 15/sep. El
acto cierra igual, con P1 hecho y P2 en `## NO-CORRIDO` con razón
`DECISIÓN-DE-MESA-PENDIENTE`.

**P3 · Las tres NC.** Produce: NC-0401 cerrada con su PR; NC-0402 y NC-0397 cerradas citando la
corrida en clon completo, con la salida cruda pegada. Si al correrlas tú alguna no da 6/6 o
9/9, **esa discrepancia es el entregable** y NC-0401 sigue siendo el objetivo.

**P4 · CI.** Los tres tests corren en `verify.yml`, con clon completo. Si el fetch de la suite
es superficial, se arregla ahí (D-23: una herramienta de verificación no muta el clon que
verifica).

## 6 · LATITUD

Decides tú: el cómo, el orden, los nombres, el mensaje exacto de la excepción, cómo se llama el
script del delta, y remover obstáculos reversibles y baratos. Un defecto adyacente de ≤ 10
líneas que te impida terminar, lo arreglas y lo declaras.
Preguntas a mesa con opciones y recomendación, siguiendo con lo demás: cualquier bifurcación
que cambie qué número emite el motor.

## 7 · PAROS — lista cerrada

a) abrir o derivar dato de una ola reservada — **ENVIPE 2026 está RESERVADA desde el 21/sep**
b) borrar, forzar o reescribir algo sellado — incluida `data/corrida0/resultados.tsv`, que es
   derivada y **no se toca**
c) adoptar, o mover un contador que este encargo dice que no debe moverse
d) cambiar estimando, universo, umbral o código de un procedimiento congelado
e) entorno equivocado (este acto es NUBE; no abre microdato)
f) el objetivo dejó de ser alcanzable

Fuera de esa lista no se para. Que el sucesor no exista en la vista, que el test revele un
segundo defecto, que el CI esté mal cableado: se resuelve y se declara.

## 8 · COMPUERTAS

- «El delta del pin está dentro de la tolerancia del tipo» **protege: adoptar.** Sin esa
  condición no se escribe el pin.
- «`check.py --baseline` VERDE» **protege: adoptar** el cierre del acto.
- El orden de las piezas es orden sugerido, no compuerta.

## 9 · PERÍMETRO

Propio: `milpa/src/emisor.py`, `tests/test_motor_gen2_explicito.py` y el test sintético nuevo,
el script del delta, `.github/workflows/verify.yml` en lo que toca a estos tres tests, y las
filas NC propias.
Ajeno que no se toca: `data/corrida0/resultados.tsv` y `tools/corrida0.py` (DIRECCIÓN; la vista
es derivada) · `milpa/tramite.yaml` (ninguna cifra del motor se mueve en este acto) · ids y CI
fuera de estos tres tests (TUBERÍA).
Si te encuentras escribiendo fuera de esta lista, PARA.
PERÍMETRO DE CIERRE, permanente y sin que se pida: CI, filas en la vista, registro de tablas,
cascada de `/acto`, hallazgos, NC y FP propios.

## 10 · LO QUE NO HACE · SUCESORES · CIERRE

No hace: no cambia ninguna emisión del motor, no adopta nada, no toca la capa θ ni la matriz,
no adjudica ninguna celda.
Sucesores: si P2 va a mesa, el sucesor es el acto que aplique la decisión de los bins.
Auditoría de rigor extremo: **no aplica** — este acto no afirma nada sobre México.
Cierre: `## NO-CORRIDO / RESERVAS` («Ninguno.» es obligatorio si no hubo) antes de
`## CONSUMIDO`, con este encargo archivado verbatim y marcado con su PR.

---

## NO-CORRIDO / RESERVAS

*(escrito por el ejecutor al cerrar, A.14 · `ACTO MOTOR-LINAJE-1`, 21/sep/2026)*

| qué | por qué | impacto | sucesor |
|---|---|---|---|
| **P2 · el pin del consumidor** — actualizar el pin de `RESULT-C1-POSEL-AMENAZA-DELTA` al sucesor con delta por script. | `SUSTITUIDO-POR:` la medición de la propia pieza. `python3 tools/delta_pin_superado.py` (universo 10 542 ids de `data/corrida0/resultados.tsv`, 222 íntegramente superados, 2 archivos examinados, A.13) reporta `SIN-PIN` en **222 de 222**: ningún uso ACTIVO de `data/corrida0/usos.tsv` pin-ea un id íntegramente superado. La premisa `[SUPUESTO]` del encargo —«el consumidor que hoy pide `RESULT-C1-POSEL-AMENAZA-DELTA`»— **no se sostiene: no existe tal consumidor**. El sustituto absorbe la pieza entera y **no deja nada huérfano**: no hay pin, luego no hay delta, luego no hay nada que firmar, y por eso tampoco se abrió fila en `forense/firmas-pendientes.tsv`. | Ningún contador se mueve y ninguno deja de moverse. La rama §5-P2 de mesa (delta fuera de tolerancia → bins de la ADENDA del 15/sep) no se dispara. | `SIN-ASIGNAR` — la pieza no tenía objeto. `NC-0433`. |
| **P1 · el universo del caso** — el encargo declara `[EJECUTADO]` que este id es «el único caso vivo» con cero filas vigentes. | `PARO-PREMISA` — premisa falsa, de **estado del repo**, no de qué se mide; el objetivo seguía alcanzable, así que se replanteó, se siguió y se declara (v2.15 §2). Medido aquí: **222 ids**, bajo **8 corridas sucesoras distintas**. No cambia el contrato entregado: lo confirma. El encargo pedía que quedara bien «para el siguiente»; el siguiente ya estaba en el árbol 221 veces. | Ninguno. Se asienta para que nadie vuelva a tratarlo como caso único. | `SIN-ASIGNAR`. `NC-0434`. |
| **P3 · cerrar NC-0401** (`tests/test_motor_gen2_explicito.py` corre en CI). | `DECISIÓN-DE-MESA-PENDIENTE` — la causa que NC-0401 citaba está **cerrada**: el test pasa de **0 pruebas ejecutadas a 13 corridas, 9 en verde**. Las 4 restantes son **dos causas nuevas**, las dos sobre objetos que este encargo **veda**: (1) el `p` de `milpa/tramite.yaml` para `share_horas_mujeres_40mas` es `0.2215` y su RESULT sellado `RESULT-ENUT-A-R` es `0.22148146779116093` (tolerancia `5e-07`) → `NO_COVERAGE`, la guardia funcionando bien; arreglarlo mueve una cifra de `milpa/tramite.yaml`, vedada por el CONTADOR. (2) `construir_snapshot()` levanta `LookupError`: `tramite.evasion_norma` no tiene `ola_calibracion:` propia ni entrada en `_OLA_CALIBRACION_FIJA`, y el guardia dice «no se inventa, extiende la constante con la cita real»; ni el yaml ni `tools/emite_m.py` están en el PERÍMETRO §9, y la cita real no es derivable en NUBE sin abrir dato. | El test sigue `FALLA-DE-VERDAD` en el censo y `SKIP` en el job `guardias`: **no gatea CI todavía**. `NC-0401` sigue ABIERTA con causa nueva. | `SIN-ASIGNAR` — acto que resuelva, con firma, el `p` vs su RESULT sellado y la `ola_calibracion` con cita real. `NC-0435`. |
| **Perímetro de cierre · NC-0400** (`tests/test_consulta_gen2.py`, misma causa raíz). | `DECISIÓN-DE-MESA-PENDIENTE` — la causa citada está cerrada (de **0 a 18 pruebas ejecutadas, 14 en verde**). Las 4 restantes comparan respuestas de ejemplo congeladas que se sellaron cuando el índice era incargable; re-sellarlas cambia artefactos de otro acto. | Sigue `FALLA-DE-VERDAD` en el censo. `NC-0400` sigue ABIERTA con causa nueva. | `SIN-ASIGNAR` — acto dueño de `forense/ejemplos/GEN2-*`. `NC-0436`. |

**Reservas:** ninguna abierta. **ENVIPE 2026 intacta**: este acto no abrió, no derivó y no imprimió ningún dato de esa ola, ni de ninguna otra — cero microdato, `data/raw` ausente, 0 archivos examinados (A.13).

**PAROS de la lista cerrada (§7) disparados:** ninguno. Los dos obstáculos encontrados (clon superficial; premisas falsas de estado del repo) son reversibles y baratos: se resolvieron y se declaran, como manda §7 *in fine*.

## CONSUMIDO

Ejecutado por **`PR #937`** — `ACTO MOTOR-LINAJE-1`, rama
`claude/ecstatic-edison-y1t005`, 21/sep/2026, entorno NUBE, cero microdato.
`ADR-572` · `NC-0433`–`NC-0436` · `NC-0402` y `NC-0397` CERRADAS ·
`NC-0401` y `NC-0400` siguen ABIERTAS con causa nueva.
Nota de cierre: `forense/notas/2026-09-21-MOTOR-LINAJE-1-cierre.md`.
