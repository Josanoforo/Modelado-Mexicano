ENCARGO · ACTO GEN2-RELEVO-TANDA-1 · EL CONTADOR QUE NO SE MUEVE: 182 LECTURAS LEGACY. SE ADOPTAN LOS 12 CANDIDATOS QUE MESA YA MANDÓ PRIMERO, Y LA VISTA DE RELEVO APRENDE LAS 22 MEDICIONES DE AYER
CABECERA (D-12) · SHA de redacción `a92126f0`; re-deriva al abrir · ENTORNO: NUBE; NO caja — todo insumo es un `RESULT` sellado · una sola sesión, rama propia; `/acto` PARA si ya está archivado en otra rama viva · compuerta: ninguna · MODELO: Opus (toca lecturas del motor) · FP/ADR/NC: deriva al cierre; renumera quien fusione segundo (en vuelo: TABLERO-SENAL-1, RECIBO-CODEX-6, PILOTO-3-P0 en caja) · CONTADOR: `cuenta_gen2` NO-APLICA; mueve `dependencias_numericas_legacy_activas` (hoy 182) y `adoptados_activos` (hoy 46) — reporta lo derivado, antes y después.
FIRMA DE MESA QUE ESTE ACTO EJECUTA (ya dada; no se pide otra)
`data/corrida0/decisiones.tsv:132-133`, 17/sep/2026, verbatim: «MEDICION-DEMANDA-3 se redefine al universo real de `relevo-usos-v1_0.tsv`: 153 `SIN-CANDIDATO`, con los 12 `CANDIDATO-GEN2` primero.» NC-0255 y NC-0256 cerraron con ese sucesor; el sucesor nunca se lanzó. La adopción la ejecuta el merge de mesa de este PR, por bloque (E.2).
VERIFICACIÓN DE EXISTENCIA (A.8; dirección contra `a92126f0`)

* (1) Gobiernan `data/corrida0/relevo-usos-v1_0.tsv` (208 filas), `tools/relevo_usos.py`, `tools/relevo_candidatos_delta.py`, `usos.tsv`, `milpa/tramite.yaml`. Cubren.
* (2) `veredicto`: `SIN-CANDIDATO` 154 · `YA-ADOPTADO` 24 · `CANDIDATO-GEN2` 12 · `NO-ADOPTABLE-POR-VEREDICTO-SELLADO` 8 · `LISTADO-PARA-MESA` 7 · `CONFLICTO-ENTRE-CANALES` 2 · `VETADO` 1. `generacion_hoy`: `LEGACY-GEN1` 182. Los 12 candidatos: RES-0025, 0026, 0028, 0031–0034, 0053–0056, 0066. En 11 de 12 el valor GEN2 coincide con el legacy a la sexta decimal; RES-0028 no (0.705687 → 0.732756). Columnas `delta`, `cambia_signo`, `materialidad`: vacías en las 12 — la comparación por script que E.1 exige no se ha corrido. `grep -cE "ENIGH2022|ENFIH2019|ENUT2024|ENSAFI2023|WBES2023|EDER2017-PRIMERA|ENCUCI2020" relevo-usos-v1_0.tsv` → 0: la vista no conoce ninguna medición del 19/sep.
* (3) La vista es anterior a esas mediciones: la cobertura retroactiva no existe, hay que derivarla.

PIEZAS (lote D-11)
P1 · Delta por script, antes de adoptar nada (E.1: la comparación contra GEN1 se calcula, no se mira). `tools/relevo_candidatos_delta.py` sobre los 12; llena `delta`, signo, grano, materialidad. Criterio de bloque escrito antes de correr: entra al bloque de adopción el slot con mismo estimando (P2) y `|Δ|` dentro de la tolerancia del tipo de RESULT; el resto va a mesa con su delta. P2 · Mismo estimando, por texto — la lección de `matrimonio_directo`. Para cada uno de los 12: evento, universo, unidad, ola y denominador del consumidor en `tramite.yaml` contra los de la spec del CALC candidato. Coincidir a seis decimales no prueba que el nombre sea correcto: prueba que es el mismo cálculo. Si el estimando del consumidor y su nombre no casan, no se adopta a ciegas: fila para mesa. P3 · RES-0028, aparte. Es `denuncia_por_otra_razon`, un complemento ya declarado (`complemento_de`; ADR-509 propuso renombrarlo). El valor GEN2 difiere 2.7 pp y el RESULT se llama `…COMPLEMENTO-C2-U1`: di qué universo (U1 vs U4) explica la diferencia, con cita de spec, y llévalo a mesa como FP con las dos opciones. No entra al bloque. P4 · Adopción del bloque por el mecanismo de la casa (`relevo_usos.py`; el que usó #880 para RES-0043/0044). `status` antes/después con worktree. Si el mecanismo exige editar `tramite.yaml`, solo el campo de procedencia de esas conductas, nunca el `p`. P5 · La vista aprende las mediciones del 19/sep. Re-deriva ofertas con los CALC sellados nuevos y emite, por slot `SIN-CANDIDATO` que gane oferta: candidato, estimando casado o no por texto, y delta. Pista leída por dirección, a verificar: el encargo Codex de ENIGH declara que `CORR-0076` enlaza RES-0165 a RES-0170 (cortes de `milpa/src/celdas.py`). Esta pieza lista; no adopta: la tanda 2 sale de esa lista. Reporta cuántos de los 154 siguen sin oferta y agrúpalos por corrida natural: esa es la demanda de medición de caja, de donde salen los siguientes encargos. P6 · Los 7 `LISTADO-PARA-MESA` y 2 `CONFLICTO`: una línea cada uno con lo que falta ("falta el pin, no la corrida", dice la vista de RES-0039–0042). Nota: `GEN2-PISOS-REJILLA` midió denuncia-con-seguro en ENVIPE 2024; verifica por ola si sirve o no a esos slots — no lo supongas.
PERÍMETRO
`relevo-usos` y derivados por comando · `usos.tsv` por comando · `milpa/tramite.yaml` / `procedencia.yaml` solo campos de procedencia de los slots del bloque · `forense/analisis/relevo-tanda-1/` (tablas P2, P5) · `decisiones.tsv`, `firmas-pendientes.tsv` (RES-0028), `no-corrido.tsv`, `hallazgos.md`, ADR, recifrado L0 · nota. No toca ningún CALC · marcador · `tools/corrida0.py` · tablero · celdas-D · yaml del árbitro. «Si te encuentras escribiendo fuera de esta lista, PARA.»
LO QUE NO HACE
No mide · no cambia ningún `p` · no renombra conductas · no adopta nada de P5 · no resuelve RES-0028.
SUCESORES
RELEVO-TANDA-2 (de la lista P5) · encargos de caja desde la demanda agrupada de P5 · FP de RES-0028.
MÓDULO DE AUDITORÍA (afirma sobre México: son lecturas del motor)
Relevar procedencia no valida el contenido: una cifra GEN2 idéntica a la legacy hereda su estimando, su universo y sus sesgos — ENIF y ENVIPE sobre-representan población con contacto financiero e institucional; `evasion_norma` es por delito, no por persona. P2 existe para que "mismo número" no se lea como "misma afirmación". Clase (a) en los 12. Ninguna cifra esperada: el 182 de partida y lo que quede se derivan. Peligroso leído simplista: "bajamos legacy" como "el motor mejoró" — mejora la trazabilidad; la validez es otra pregunta.
