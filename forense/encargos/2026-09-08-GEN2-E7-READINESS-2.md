# E7 · ACTO GEN2-E7 · READINESS-2 — marcador (M, R, L, agregado) y verificación adversarial de INFRA (antes de cualquier cálculo)

Cabecera: dos piezas de nube y una de caja, un encargo, dos PR (D-11: el lote es una corrida coherente; aquí son dos entornos) · Opus · COMPUERTA por producto: git show origin/main:tools/corrida0.py | grep -c "def cmd_registro\|def cmd_status" → 2 (E6 fusionado). Pieza C (caja) NO se lanza en NUBE; piezas A y B NO se lanzan en UBUNTU. Firmas: D11/D12 del ADR de E3.1 (motor.py y familia son scaffold; la readiness del marcador es E7); NC-0001 DIFERIDO-A:E7. Pieza A (nube) · Corredores M, R, L y agregado, envueltos para GEN2. (A1) Wrapper M: marco vigente → cada celda → tools/emite_m.py::emite_celda → RESULT-M, sin regenera_todos() histórico, con grado_DD por conducta (ya en emite_m.py tras M13) y ciego_a_R verificable; salida como CALC-M-<marco> bajo corrida0.py (spec.yaml + contrato; el marco es input de repo). (A2) R: tools/arbitra.py envuelto o adaptado para recibir payload_id exacto + spec + ruta resuelta por resolver_payload, sin heurística de «mejor archivo»; cada celda R como CALC-R-<celda> con inputs de manifiesto. (A3) L: corredor sucesor para L-spec-v1_2.json (14 celdas) en lugar de runner_l_cli.py + carga_l_v1_1.py (loader del universo v1.1, verificado el 7/sep); las capturas existentes de corridas-L/ son GEN1 y no se releen como GEN2. (A4) Agregado sucesor que consuma solo RESULT-R/M/L por celda como corrida derivada (CALC-AGG-<marco>), con ci_replayable: true si sus inputs quedan versionados; agregado_v1_3.py queda LEGACY y smoke. (A5) Verificar el paso 3 del marco como diseño, no como código: la unidad de celda (regla × ola × instrumento vs regla × segmento) es decisión de mesa; este acto entrega la tabla de opciones con el diagnóstico de M13 (diagnostico-14-celdas) y no la toma. Tests: uno por wrapper sobre fixtures, verify REPRODUCE/IDENTICO de cada CALC nuevo. Nada de esto produce un número nuevo del modelo: envuelve lo que existe para que el primero que se produzca ya nazca con cadena. Pieza B (nube) · Go/No-Go del marcador. Checks mecánicos: MARCO-VIGENTE-ÚNICO (una sola fuente del marco) · M-DESDE-CONTRATO · R-SIN-HEURÍSTICA · L-SPEC-v1_2 · AGREGADO-DERIVADO · LEGACY-NO-LEÍDO (ningún wrapper abre corridas-R/M/L ni agregado-v1_3 salvo como valor_legacy para delta). Salida: GO-MARCADOR o lista de fallos. Pieza C (caja) · Verificación adversarial de INFRA con muestra propia — cierra NC-0001 (V1·P3, 4/sep). Exactamente lo que V1 pedía y nadie hizo: (C1) tests/test_alta_relacion.py cinco casos con fixture propio; (C2) dos escritores simultáneos sobre un manifiesto de fixture (--registra concurrente) → el lock de tests/manifiesto.py debe impedir la doble escritura; (C3) tools/arbitra.py contra fixture (no contra corpus); (C4) git worktree add sin data/raw y resolver_payload → RAIZ_NO_CONFIGURADA, no excepción; (C5) el reporte de C1–C4 con salida cruda, y NC-0001 → CERRADA con este PR o ABIERTA con lo que falló y sucesor. No relee la «verificación post-hoc» del 4/sep: la contrasta. Perímetro A/B: tools/emite_m.py (solo envoltura; la emisión no cambia) · tools/arbitra.py (adaptador o wrapper nuevo tools/arbitra_gen2.py) · forense/prereg-duelo-v2/ (corredor L sucesor, nuevo archivo; nada existente se edita) · tools/corrida0.py (solo si un wrapper necesita un tipo de input nuevo, declarado) · tests/ · data/corrida0/CALC-M-*/, CALC-R-*/, CALC-AGG-*/ solo si se ejecutan sobre insumos de repo (M y agregado sí; R no: R necesita corpus y va a caja en E5 o después) · cascada. Perímetro C: tests/test_alta_relacion.py (nuevo) · fixtures · forense/no-corrido.tsv (NC-0001) · nota · cascada. No toca tramite.yaml, procedencia.yaml, tiers, corridas-R/M/L ni agregado-v1_3.py. Si te encuentras escribiendo fuera de esta lista, PARA. Contador: cero GEN2 del modelo (los CALC-M/AGG sobre insumos de repo nacen cuenta_gen2 = SI solo si mesa lo firma en su merge; por defecto PENDIENTE-DE-MESA). no_corrido_abiertas −1 si C cierra NC-0001. Cierre A.14; ramas borradas.
