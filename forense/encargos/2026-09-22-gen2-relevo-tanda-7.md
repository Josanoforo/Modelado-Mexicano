# ENCARGO · ACTO GEN2-RELEVO-TANDA-7 · LA GUARDA (b) RECONOCE LAS CAPTURAS SELLADAS COMO INSUMO CRUDO, Y ENTONCES ENTRAN LOS DIECIOCHO PINES DE L

> ENTORNO: **NUBE**. NO es CAJA.
CABECERA · SHA de redacción `bda6b60c`; una sesión, rama `acto/gen2-relevo-tanda-7` · MODO ABIERTO · CONTADOR: legacy baja en exactamente lo pineado (hoy 146); `adoptados_activos` antes/después por comando. **L0 de `canon/estado-programa-v1_14.md`: si choca, toma la de `main` y re-inserta solo tu anotación; `canon/L0/` existe.**
**MODELO: Sonnet por mandato de mesa.** Procedimiento congelado o receta; latitud logística; toda duda de procedimiento se pregunta a mesa en una línea con opciones.
NO tocar (TUBERÍA): `tests/check.py`, `tools/cierre_acto.py`, `tools/tablero_programa.py`, `.github/workflows/`, `.claude/commands/`. Cierre: `tests/check.py --rapido` antes de empujar; el CI corre la suite completa. Derivados («DERIVADO — NO EDITAR») no se commitean: los re-deriva el job de main.

## 1 · OBJETIVO
Tercer intento sobre los mismos 18 pines, y el diagnóstico ya es exacto: TANDA-6 (`#985`) probó por mutación que la guarda (b) los rechaza con `RECHAZADO-SIN-INSUMO-CRUDO` porque `_tiene_crudo` (`tools/pines_mesa.py:316-327`) solo reconoce `origen: manifiesto` o una ruta que contenga `manifiesto-capturas`, y `CALC-L-DESDE-CAPTURAS-v1_0` declara sus 224 capturas a través de un plan JSON con hash (`IN-F5C-PLAN`). La regla 4.1 dice «insumo crudo con hash — microdato o capturas selladas»: el CALC lo cumple; la guarda mecánica no lo ve. «Hecho»: la guarda (b) reconoce el caso por regla declarada (no por excepción nombrada); tests por mutación; los 18 pines `ACEPTADO`; F-L `FIRMADA`; el contador bajó en exactamente lo pineado; NC-…-7510-01 cerrada.

## 2 · FIRMAS — verbatim; el lanzamiento es el sello
4.1 (21/sep, TANDA-3 `:8`): «(i) su cifra la produce código GEN2 desde un insumo crudo con hash —microdato o capturas selladas—». F-L (TANDA-5 §2, en el repo): los 18 slots. **Precisión de dirección:** «Un input `origen: repo` cuya ruta apunte a un manifiesto o plan de capturas selladas con hash por captura (hoy `forense/prereg-duelo-v2/F5-completa-plan-v1_0.json` y los `manifiesto-capturas-*.json`) es insumo crudo a efectos de la guarda (b). La guarda lo reconoce leyendo el archivo —que enumere capturas con sha— y no por nombre. El `spec.yaml` congelado de `#973` no se toca.»

## 3 · LO QUE DIRECCIÓN SABE (contra `bda6b60c`)
- `[LEÍDO: nota de TANDA-6]` `verify` → `REPRODUCE / IDENTICO`; asiento existente; la guarda (a) ya en verde; las 18 filas construidas (`marco-M::…::{L-solo,L+corpus}`, `result_gen2=RESULT-LDESC-TABLA-JSON`, `via=i-CRUDO`) caen solo en (b). `[EJECUTADO]` `forense/prereg-duelo-v2/manifiesto-capturas-P3-v1_0.json` existe; el plan `F5-completa-plan-v1_0.json` está declarado en el spec con sha. `[SUPUESTO]` que el plan enumera las 224 rutas con hash por captura: verifícalo; si solo enumera rutas, el hash está en el sello del CALC (`input_sha256_efectivos`) y la guarda puede leerlo de ahí — dilo cuál.

## 4 · YA HECHO
`#983` (vía (i) eje RESULTADO), `#985` (replay asentado). 0 pines L. Repítela tú.

## 5 · PIEZAS
**P1 · Guarda (b).** `_tiene_crudo` acepta un input `origen: repo` cuyo archivo sea un manifiesto/plan de capturas con hash por captura; test por mutación: acepta el plan real; rechaza un JSON sin hashes; rechaza `origen: repo` a secas. Mensaje de rechazo sigue diciendo por qué.
**P2 · Los 18 pines** (F-L). `valida_pin` → `ACEPTADO`. Contador −18 o el real con razón.
**P3 · Cierre.** F-L `FIRMADA`; NC de TANDA-5 y TANDA-6 cerradas; nota: qué cambió en la guarda y por qué no es una excepción.

## 6 · LATITUD · 7 · PAROS · 8 · COMPUERTAS · 9 · PERÍMETRO
Latitud: implementación, orden. PAROS: a) editar `CALC-L-DESDE-CAPTURAS-v1_0/spec.yaml` o cualquier sello · b) relajar (b) para `origen: repo` sin hashes de captura · c) pinear las 7 L con reserva u otra lectura · d) el contador baja en más de lo pineado · e) `DIN-M-01:M` sale de legacy. Compuerta: «tests de P1 en verde antes de escribir un pin» protege adoptar. Perímetro: `tools/pines_mesa.py`, `tests/test_pines_mesa.py`, `pines-de-mesa.tsv`, FP/NC propias, nota, cascada. Fuera, PARA.

## 10 · CIERRE
No mide · no re-sella. `## NO-CORRIDO / RESERVAS` · `## CONSUMIDO`.

## NO-CORRIDO / RESERVAS

- **qué**: P2 · los 18 pines de L (F-L) vía (i) al RESULT de `CALC-L-DESDE-CAPTURAS-v1_0`, escritos en `data/corrida0/pines-de-mesa.tsv`. **por qué**: `PARO-PREMISA` — con P1 de este acto (guarda (b) fija), las 18 filas SÍ pasan `pines_mesa.valida_pin` (`ACEPTADO`, verificado por mutación directa, no por lectura, usando `corrida0._filas_registro(verifica=True, verifica_ids={"CALC-L-DESDE-CAPTURAS-v1_0"})`, que refresca el replay con `python3 tools/corrida0.py verify CALC-L-DESDE-CAPTURAS-v1_0` → `REPRODUCE`/`IDENTICO`). Pero el camino mecánico real de adopción, `python3 tools/corrida0.py registro --verifica --escribe --lote CALC-L-DESDE-CAPTURAS-v1_0`, levanta `ParoRegistro: USO-NO-APTO` para las 18 filas ("el origen numérico no está acreditado"). Causa raíz (leída en `milpa/src/linaje.py` y `tools/corrida0.py:3944-4030`): `CALC-L-DESDE-CAPTURAS-v1_0/spec.yaml` no declara `dependencias_numericas` por `RESULT`, así que `resuelve_resultado()` le asigna a CADA resultado del CALC —incluido `RESULT-LDESC-TABLA-JSON`, el que citan los 18 pines— los 7 inputs completos, incluyendo `IN-LEGACY-AGREGADO-V13`/`IN-LEGACY-EXTRAIDO-V12` (que solo alimentan `RESULT-LDESC-DIFERENCIAS-VS-GEN1-JSON`, un resultado distinto de comparación contra GEN1). Esos dos inputs son `FUNCION-INDETERMINADA`, así que `combina_origenes` deja a `RESULT-LDESC-TABLA-JSON` en `ORIGEN_INDETERMINADO` pese a no depender de ellos — verificado con `python3 -c "...corrida0._filas_registro(verifica=False)['resultados']..."` → `RESULT-LDESC-TABLA-JSON: origen_numerico=INDETERMINADO`. `aptitud_para_uso(INDETERMINADO, MEDICION-GEN2, …)` es `NO-APTA` por diseño (`milpa/src/linaje.py:57-58`), y `_filas_registro` aborta la escritura ENTERA con `ParoRegistro`, no solo estas 18 filas: si `registro --verifica --escribe` corre alguna vez sin `--lote` (su comportamiento por defecto verifica TODO CALC sellado), estas 18 filas ya aceptadas por `pines_mesa` tumbarían esa corrida completa para TODOS los CALC del árbol, no solo el de L. Se revirtieron las 18 filas de `data/corrida0/pines-de-mesa.tsv` (`git checkout`) antes de cerrar este acto, para no dejar ese riesgo latente en el árbol. No se tocó `CALC-L-DESDE-CAPTURAS-v1_0/spec.yaml` (congelado, PARO (a) de este encargo) ni `tools/corrida0.py`/`milpa/src/linaje.py` (fuera del perímetro §9 de este encargo — "Fuera, PARA"). **impacto**: 18 lecturas L (`marco-M::{CIV-M-01,02,04,10,12,13; FAM-M-05,06,07}::{L-solo,L+corpus}`) siguen legacy; `dependencias_numericas_legacy_activas` se queda en 146 en vez de bajar a 128; `N_resultados_gen2_adoptados_activos` sin mover. **sucesor**: `DIFERIDO-A: NC-260922-GEN2-RELEVO-TANDA-7-9a4c-01` (acto que declare `dependencias_numericas` por RESULT en un CALC sucesor de `CALC-L-DESDE-CAPTURAS-v1_0` —el sellado no se edita, D-18/PARO (a)—, o que enmiende `resuelve_resultado`/`combina_origenes` en `tools/corrida0.py` + `milpa/src/linaje.py` para que un input `FUNCION-INDETERMINADA` solo afecte el/los `RESULT` que de verdad lo citan; `DECISION-DE-MESA-PENDIENTE` sobre cuál de las dos rutas prefiere dirección).
- **qué**: P3 · Cierre — «F-L `FIRMADA`» como estado NUEVO producido por este acto (adopción efectiva). **por qué**: `SUSTITUIDO-POR:mismo-estado-de-TANDA-5/TANDA-6` — F-L ya estaba `FIRMADA` desde TANDA-5 (§2, lanzamiento = sello) y sigue `FIRMADA`; este acto no cambia ese estado, solo actualiza la nota de ejecución en `forense/firmas-pendientes.tsv` con el hallazgo de P2. Absorbido por: la fila `FP-260922-GEN2-RELEVO-TANDA-5-f54e-01` ya existente. Huérfano: ninguno. **impacto**: ninguno sobre el contador; la firma sigue disponible para cuando el sucesor de P2 la levante. **sucesor**: `SIN-ASIGNAR` (nada pendiente sobre la firma misma; el sucesor real es el de P2, arriba).
- **qué**: pinear las 7 L con reserva (`DIN-M-01`, `FAM-M-01`, `TRA-M-02`, `TRA-M-03`, `TRA-M-07`), y `DIN-M-01:M` saliendo de legacy. **por qué**: `FUERA-DE-PERÍMETRO` — PAROS (c) y (e) de este encargo lo vedan explícitamente. **impacto**: ninguno — siguen en propuesta con reserva. **sucesor**: firma de mesa sobre las 7 cuando haya más capturas (ya declarado en TANDA-5 §10).

## CONSUMIDO

`PR #995`. El PR no se fusiona en este acto: mesa fusiona.
