# ENCARGO VERIF-3 · Abrir lo que ya está en disco y decide cuatro reglas abiertas

Contadores que mueve este acto: 6 de las 14 reglas abiertas del Hito D (`R2.1`, `R7.3`,
`R7.4`, `R7.5`, `R8.1`, `R10.2`) pasan de estado heredado (`NO EXISTE` pre-A.4) a una
clasificación A.4 real — las 6 salen `EXISTE-NO-SATISFACE`. El contador `CUBIERTA` del Hito D
sigue en 0/14, sin cambio.

---

## ARRANQUE — crudo

**1 · REPO.**
```
$ cd /home/pc0/Modelado-Mexicano && git log -1 --format="%h %s" && git status
302ac5a Merge origin/main into sesion/cal-conf-faseb-pos4-envipe-paso1
On branch sesion/cal-conf-faseb-pos4-envipe-paso1
Untracked files:
	data/raw
```
Clon base confía en una rama ajena (`sesion/cal-conf-faseb-pos4-envipe-paso1`), no `main` — igual
que documenta la memoria del proyecto, nunca se asume. Worktree nuevo:
`/home/pc0/wt-verif3-1786042795` (`git worktree add -b verif3-1786042795 ... origin/main`, con
una colisión transitoria de `.git/config.lock` en el primer intento — sin proceso git vivo
sosteniéndolo, reintento inmediato exitoso, `HEAD` quedó en `aa87828`).

**2 · SHA.**
```
$ git cat-file -t 215af44   # commit
$ git merge-base --is-ancestor 215af44 origin/main && echo YES-ancestor
YES-ancestor
$ git log 215af44..origin/main --oneline
aa87828 Merge pull request #149 from Josanoforo/claude/explora-puertas-banxico-qnm3cn
d7f0651 Merge remote-tracking branch 'origin/main' into claude/explora-puertas-banxico-qnm3cn
d1fdd9f Merge pull request #150 from Josanoforo/map2-cruce-1786030513
b921155 Merge remote-tracking branch 'origin/map2-cruce-1786030513' into map2-cruce-1786030513
b2d6db0 Merge remote-tracking branch 'origin/main' into map2-cruce-1786030513
89fb737 Merge remote-tracking branch 'origin/main' into map2-cruce-1786030513
f9aa9ed Merge remote-tracking branch 'origin/main' into claude/explora-puertas-banxico-qnm3cn
25609db ENCARGO MAP-2: cruce de los cuatro universos (portal/inventarios/disco/reglas)
9ba1b25 ENCARGO EXPLORA-1: censo de puertas y sondeo de series Banxico CoDi/SPEI
```
`215af44` (PR #151) es ancestro confirmado de `origin/main` (hoy `aa87828`); la diferencia son
exactamente PR #150 (MAP-2) y PR #149 (EXPLORA-1), tal como el encargo anticipó. NO es PARO.

**3 · data/raw.**
```
$ ln -s /home/pc0/mm-corpus/raw data/raw
$ cp /home/pc0/Modelado-Mexicano/data/raices.local.yaml data/raices.local.yaml
$ ls data/raw | wc -l
239
```

**4 · ENTORNO (firma de tres partes).**
```
$ echo "CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=${CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE:-<unset>}"
CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=<unset>
$ curl -s -o /dev/null -w "HTTP %{http_code}, %{time_total}s\n" --max-time 10 https://www.inegi.org.mx
HTTP 200, 0.718068s
$ ls data/raw | head -1
BD_ENCUCI2020_dbf.zip
```
Los tres coinciden: Ubuntu con corpus y con red — entorno correcto, no PARO.

**5 · ESPEJO.** Acatado — ninguna cifra de este acto viene de memoria o de un documento-espejo;
todo lo que sigue trae su comando.

---

## §1 · Los cuatro archivos

### 1. `ACLED_HDX` — R7.4/R7.5

Localización (`data/manifiesto.yaml:9913`, id `r7_4_r7_5_acled_hdx_demonstration_events`,
`archivo: R7.4_R7.5_ACLED_HDX/mexico_demonstration_events_by_month-year.xlsx`).

```
$ python3 tests/manifiesto.py --verifica --id r7_4_r7_5_acled_hdx_demonstration_events
Entorno de verificación: Linux 6.6.87.2-microsoft-standard-WSL2 (x86_64) · Python 3.14.4

r7_4_r7_5_acled_hdx_demonstration_events [data_raw]: COINCIDE -- sha256 y tamaño (9075 bytes)
verificados contra data/manifiesto.yaml

Por raíz (sin colapsar):
  data_raw: coincide=1 · no_coincide=0 · ausente=0 · sin_configurar=0
```
(El resto de la salida de `--verifica` es un volcado de "procedencia derivada" de otras 57
entradas del manifiesto, sin relación con este id — omitido aquí por ruido, no por selección.)

Apertura real con `openpyxl`:
```
HOJAS: ['TOU', 'Data']
--- HOJA 'Data' dims=A1:D104 filas=104 cols=4 ---
0 ('Country', 'Month', 'Year', 'Events')
1 ('Mexico', 'January', '2018', 286)
...
103 ('Mexico', 'July', '2026', 333)

columnas: ('Country', 'Month', 'Year', 'Events')
valores unicos de Country: {'Mexico'}
rango de años: 2018 - 2026
num filas de datos: 103
suma total Events: 51263
```

**Variables reales**: `Country, Month, Year, Events` — 4 columnas, 103 filas (ene-2018 a
jul-2026), `Country` es constante (`Mexico`, un solo valor). La hoja `TOU` (9 filas) es solo el
texto de licencia ACLED, sin datos.

**La pregunta** (¿granularidad de evento + variable que permita codificar entorno
rural/urbano o geografía derivable?): **no** en ninguna de las dos dimensiones. El recurso
viene pre-agregado MES×AÑO — no hay una fila por evento — y no existe ninguna columna de
geografía sub-nacional (sin admin1/admin2/estado/municipio), sin coordenadas, sin
rural/urbano, sin tipificación de actor ni de tipo de evento.

**Clasificación A.4: `EXISTE-NO-SATISFACE`.** Universo: archivo completo (ambas hojas, 104
filas), términos de columna buscados `admin1,admin2,estado,municipio,rural,urbano,lat,lon,
coordenadas,actor,tipo_evento` — ninguno presente. Falta específicamente: (a) granularidad de
evento individual, (b) cualquier variable de entorno/geografía. Fecha de apertura: 2026-08-08.

---

### 2. `PUB` — padrón de beneficiarios — R7.3

Localización (`data/manifiesto.yaml:9865`, id `r7_3_pub_beneficiarios_bienestar_csv`,
`archivo: R7.3_PUB_Bienestar/padron_unico_bienestar.csv`).

```
$ python3 tests/manifiesto.py --verifica --id r7_3_pub_beneficiarios_bienestar_csv
r7_3_pub_beneficiarios_bienestar_csv [data_raw]: COINCIDE -- sha256 y tamaño (114046 bytes)
verificados contra data/manifiesto.yaml
Por raíz (sin colapsar):
  data_raw: coincide=1 · no_coincide=0 · ausente=0 · sin_configurar=0
```

Apertura real (`csv.DictReader`, 748 filas de datos):
```
columnas: ['CVEENT', 'entidad', 'beneficiarios', 'intervenciones', 'dependencias', 'padrones',
'programas', 'periodo', 'periodo_cve', 'trimestre', 'anio', 'fecha', 'entidad_etiqueta',
'entidad_etq']
num CVEENT unicos (entidades): 33 rango: 1 - 99
num periodo_cve unicos: 23 primero: 2019T1 ultimo: 2024T3
combinaciones unicas CVEENT x periodo_cve: 748 == filas? True
columnas nominales/geoloc-nivel-fino encontradas: []
```

**Variables reales**: 14 columnas, unidad de observación = entidad×trimestre (33 valores de
`CVEENT` × 23 `periodo_cve` = exactamente 748 filas, sin duplicar ni desagregar más). Ninguna
columna de folio, nombre, CURP, municipio, sección, localidad, latitud/longitud.

**La pregunta** (¿es nominal y geolocalizable a nivel sección/localidad?): **no**. Es agregado
por entidad federativa y trimestre — ni siquiera llega a nivel municipal, mucho menos
sección/localidad. Un RDD como el que pide `R7.3` no es construible desde este archivo tal
como está.

**Clasificación A.4: `EXISTE-NO-SATISFACE`.** Universo: archivo completo (748/748 filas),
términos de columna buscados `folio,nombre,municipio,seccion,localidad,lat,lon,curp,
direccion` — ninguno presente en las 14 columnas reales. Fecha de apertura: 2026-08-08.

---

### 3. `CONTRALORIA_SOCIAL_SICS` — R8.1

Localización (`data/manifiesto.yaml:9890`, id `r8_1_contraloria_social_2019_2025_csv`,
`archivo: R8.1_contraloria_social/contraloria_social_2019_2025.csv`, declarado 520 bytes).

```
$ python3 tests/manifiesto.py --verifica --id r8_1_contraloria_social_2019_2025_csv
r8_1_contraloria_social_2019_2025_csv [data_raw]: COINCIDE -- sha256 y tamaño (520 bytes)
verificados contra data/manifiesto.yaml
Por raíz (sin colapsar):
  data_raw: coincide=1 · no_coincide=0 · ausente=0 · sin_configurar=0
```

Contenido íntegro (520 bytes completos, pegado sin recortar):
```
ejercicio_fiscal,estrategias_validadas,programas_validados,comites_constituidos,integrantes,integrantes_mujeres,integrantes_hombres,beneficios_vigilados,capacitaciones
2019,106,68,78884,325137,222314,102823,86116,136
2020,97,68,68624,275410,180859,94551,76650,98
2021,92,60,62667,266724,171595,95129,74671,137
2022,125,72,89373,345273,235828,109445,103609,169
2023,161,80,149105,497309,357014,140295,178724,216
2024,163,83,144042,460842,333830,127012,148185,274
2025,167,83,162230,509416,369846,139570,165762,79
```
```
$ file data/raw/R8.1_contraloria_social/contraloria_social_2019_2025.csv
data/raw/R8.1_contraloria_social/contraloria_social_2019_2025.csv: CSV ASCII text
```

**Los 520 bytes NO son una cáscara ni un error de descarga** — el archivo está completo (hash
COINCIDE, `file(1)` lo confirma ASCII CSV, no HTML/página de error), es simplemente pequeño
porque son 8 filas reales (una por ejercicio fiscal 2019-2025) × 9 columnas de conteos
nacionales. Esto es `NO OBTENIDO` descartado explícitamente, no aplica.

**La pregunta** (¿trae variable de sanción o monitoreo a nivel comité?): **no**. `comites_
constituidos` y `beneficios_vigilados` son conteos agregados nacionales por ejercicio fiscal,
no hay identificador de comité individual ni columna de sanción.

**Clasificación A.4: `EXISTE-NO-SATISFACE`.** Universo: archivo completo (8/8 filas, 520/520
bytes), términos de columna buscados `sancion,monitoreo,comite_id,folio_comite,id_comite` —
ninguno presente en las 9 columnas reales. Fecha de apertura: 2026-08-08.

---

### 4. `ECCO` — R2.1/R10.2

Localización (`data/manifiesto.yaml:9840`, id `r2_1_ecco_reporte_se_2023`,
`archivo: R2.1_ECCO/SE_REPORTE_GENERAL_ECCO_2023.pdf`).

```
$ python3 tests/manifiesto.py --verifica --id r2_1_ecco_reporte_se_2023
r2_1_ecco_reporte_se_2023 [data_raw]: COINCIDE -- sha256 y tamaño (2407229 bytes) verificados
contra data/manifiesto.yaml
Por raíz (sin colapsar):
  data_raw: coincide=1 · no_coincide=0 · ausente=0 · sin_configurar=0
```

Apertura real con `pdftotext -layout` (45/45 páginas extraídas sin error, 2073 líneas):
```
$ grep -n -w "45" ecco.txt | tail -1
2073:                                              [45]
$ sed -n '880p;1896p' ecco.txt
De los 19 factores evaluados, 10 obtuvieron calificaciones por encima de los 79 puntos, ...
En 2023 la Unidad de Política de Recursos Humanos de la Administración Pública Federal,
distribuyó los 19 factores de la encuesta entre los 9 cuadrantes siguientes:
$ grep -n -i "corrup" ecco.txt
1204:cuentan con la finalidad de erradicar la corrupción e incrementar la efectividad en la
1212:       corrupción de acuerdo con la normativa.        77.4
1220:      denunciar actos de corrupción es confiable.     75.3
```

**Hallazgo no pedido pero verificado al abrir el instrumento**: el cierre citado por el
encargo dice "45 factores" — el documento real tiene **19** factores (Factor 110 a 128), y lo
declara así dos veces en su propio texto (líneas 880 y 1896 del extracto). 45 es el número de
páginas del PDF (confirmado por el pie de página `[45]` en la última página), no el número de
factores. Esto no cambia el veredicto de la pregunta central (ver abajo), pero es una cita mal
transcrita que vale la pena señalar igual que el encargo señala otras.

Barrido de todo el documento (45/45 páginas, 2073 líneas) por variable conductual, incluyendo
más términos de los que la pregunta original de CONF-17 cubrió (esa sesión solo revisó el
ítem 405 cruzado con nivel jerárquico):
```
$ grep -n -i -E "veces|frecuencia|presenci|reportó|reportar|denunci|testigo|sufri|conoce de
algún|alguna vez|mordida|irregularidad|rotaci|productividad|autoritari|benévol" ecco.txt
451: ... (estadística de escolaridad, sin relación)
1215/1217/1220: ítems 404/405 del Factor 116 (ya cubiertos arriba)
656: "... favoreciendo el logro de resultados y la productividad." (prosa descriptiva del
     Factor 110, no una variable/columna)
```
Cero variables de frecuencia conductual, ni siquiera aproximadas, en las 45 páginas. El
Factor 120 LIDERAZGO (ítems 444-448) tampoco trae tipología autoritario/benévolo — es un
promedio único de percepción de liderazgo, sin desagregar por estilo.

**La pregunta** (¿se sostiene "percepción 0-100, no tasa conductual" al abrir el instrumento?
¿hay alguna variable de frecuencia conductual, aunque sea próxima?): **la caracterización se
sostiene, confirmada de primera mano** — todo el documento son promedios de percepción 0-100
(19 factores + comparativo Tichy de los mismos 19 factores en 9 cuadrantes), y no existe
ninguna variable de frecuencia conductual, ni cercana, en ningún punto de las 45 páginas.

**Clasificación A.4: `EXISTE-NO-SATISFACE`.** Universo: documento completo (45/45 páginas,
texto extraído íntegro), términos buscados listados arriba — ninguno da una variable
conductual real. Fecha de apertura: 2026-08-08.

---

## Nota sobre la supersesión ENVIPE/ENDIREH (MAP-2 → REPAIR-1)

Ninguno de los cuatro archivos de este acto es `ENVIPE`/`ENDIREH` — la categoría
`EN-DISCO-DAÑADA` que MAP-2 (PR #150) les asignó y que REPAIR-1 (PR #151) sanó no aplica aquí
directamente (los cuatro archivos de VERIF-3 verificaron `COINCIDE` de entrada, sin daño). Se
deja constancia de haberlo revisado, per instrucción del encargo, no porque haya topado con
la categoría.

---

## §3 · Filas de `forense/cruce-catalogo-fichas-v2_0.md` que quedan superadas por esto

**No se edita ese archivo.** Se nombra aquí, con línea exacta, citando este TSV
(`data/verif3-variables-2026-08-08.tsv`) como evidencia.

- **`:58` (`R2.1`)** — texto sellado: *"Ninguna [fuente candidata] | — | — | — | **NO EXISTE**
  — dato organizacional propietario ... Ninguna de las 6 clases nuevas trae encuesta de clima
  organizacional por tipo de empresa."* **Superada**: sí existe una candidata
  (`ECCO`/`r2_1_ecco_reporte_se_2023`), en disco, abierta de primera mano por este acto — la
  premisa "ninguna... trae encuesta de clima organizacional" es falsa (si acaso no "por tipo de
  empresa": `ECCO` es de una sola institución, la SE). El veredicto final no cambia (sigue sin
  satisfacer `R2.1` — no trae tasa conductual de reporte, y es de una sola institución, no
  compara jerarquía tradicional vs. plana entre organizaciones), pero la razón declarada
  ("ninguna candidata") queda reemplazada por una razón distinta y más específica
  (`EXISTE-NO-SATISFACE`, no `NO EXISTE`).

- **`:59` (`R2.2`) — NO queda superada, verificado, no asumido.** El encargo la agrupa como
  candidata junto con `:58`, pero `R2.2` pide "Rotación y productividad ±10pp, liderazgo
  autoritario no-benévolo vs. benévolo" — se buscó explícitamente `rotaci`, `productividad`,
  `autoritari`, `benévol`/`benevol` en las 45 páginas de `ECCO` (ver barrido arriba): el único
  hit es una frase de prosa descriptiva sin relación ("... la productividad", línea 656, sobre
  balance trabajo-familia), y el Factor 120 LIDERAZGO no distingue estilos autoritario/benévolo,
  es un promedio único. `ECCO` no aporta nada a `R2.2` — la fila queda como está.

- **`:89` (`R7.3`)** — texto sellado: *"PUB (padrón de beneficiarios, **granularidad
  ambigua**) + INE ... podrían, en teoría, construir un RDD si el padrón fuera nominal y
  geolocalizable — **no verificado en este acto**."* **Superada**: la ambigüedad queda resuelta,
  no confirmada. `PUB` (`r7_3_pub_beneficiarios_bienestar_csv`), abierto de primera mano: NO es
  nominal, NO es geolocalizable por debajo de entidad federativa (33 entidades × 23 trimestres,
  cero columnas nominales o de geolocalización fina). El diseño RDD que la ficha necesita sigue
  sin ser construible con este archivo — el veredicto "NO EXISTE el diseño" no cambia, pero deja
  de estar condicionado a una ambigüedad sin resolver.

- **`:90` (`R7.4/R7.5`)** — texto sellado: *"Ninguna | — | — | — | **NO EXISTE** — registro de
  eventos (protesta/autodefensa) codificado por entorno. Ninguna de las 6 clases nuevas lo trae
  (transparencia/sociedad civil se buscó vía MCCI/Cero Desabasto, ninguna de las dos es un
  registro de eventos de conflicto)."* **Superada**: sí hay un registro de eventos de conflicto
  — `ACLED_HDX` (`r7_4_r7_5_acled_hdx_demonstration_events`) — que el cierre nunca consideró
  (buscó solo en transparencia/sociedad civil, no en fuentes internacionales de eventos de
  conflicto). Abierto de primera mano: viene pre-agregado mes×año, sin geografía sub-nacional
  — no satisface el Umbral, pero por una razón concreta y verificada, no por ausencia total de
  candidata.

- **`:91` (`R8.1`)** — texto sellado: *"Ninguna con inventario de comités | — | — | — | **NO
  EXISTE** — inventario de comités con/sin mecanismo de sanción. Ninguna de las 6 clases nuevas
  lo cubre (no hay padrón ni registro administrativo de comités vecinales identificado en el
  barrido)."* **Superada**: sí se identificó un registro administrativo relacionado
  (`CONTRALORIA_SOCIAL_SICS`, dataset oficial de SABG/Función Pública sobre comités de
  contraloría social), en disco, abierto de primera mano. No es un inventario por comité — es
  un agregado nacional anual — pero la premisa "ninguna... identificado en el barrido" queda
  falsificada; la razón real es agregación insuficiente, no ausencia de fuente.

- **`:109` (`R10.2`)** — texto sellado: *"Ninguna | — | — | — | **NO EXISTE** — dato
  organizacional propietario, mismo hueco que R2.1/R2.2."* **Superada, mismo mecanismo que
  `:58`**: `ECCO` es una candidata real, en disco, abierta. No satisface — el Factor 115
  (Evaluación del Desempeño) es un promedio de percepción del proceso, no una tasa de
  rotación/desempeño real, y `ECCO` no tiene ninguna contraparte del sector privado para el
  contraste "retro pública vs. privada" que pide la ficha — pero de nuevo, la razón cambia de
  "ninguna candidata" a "candidata insuficiente, por dos motivos concretos".

**Resumen**: 5 de las 6 filas candidatas quedan superadas en el sentido de A.6 (`:58`, `:89`,
`:90`, `:91`, `:109`); `:59` (`R2.2`) se revisó y NO queda superada — se declara explícitamente
para que no quede como omisión. Ninguna de las 6 pasa a `CUBIERTA` ni a ningún veredicto A-E;
eso es un acto de medición aparte, con sus dos commits, fuera del perímetro de VERIF-3.

---

## Qué no hace este acto (recordatorio de §4, cumplido)

No adjudicó ninguna ficha del Hito D, no escribió en `hitoD-preregistro-v2_0.md`, no descargó
nada (los 4 archivos ya estaban en disco desde CONF-17), no re-corrió integridad (heredó el
`COINCIDE` de `tests/manifiesto.py --verifica`, no volvió a correr `INT-1`), y no tocó
`data/manifiesto.yaml` (perímetro de `EXPLORA-2`, concurrente).
