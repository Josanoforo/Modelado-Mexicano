# ENCARGO E6 · L-RUN — ejecuta el corredor L del duelo ADV1-M2: 240 llamadas, capturadas y commiteadas

**Archivado por A.3** (el encargo se commitea íntegro antes de ejecutarlo). Texto verbatim tal como mesa lo emitió, seguido de la autorización de apertura, de la FIRMA DE MESA que lo enmendó en sesión, y del bloque `## CONSUMIDO`.

---

## Texto del encargo (verbatim)

Dirección (maestra-30), 26/ago/2026 · SHA de redacción 8b317d3 (verificado vivo; clon propio, git status limpio) · Cifras: clase (1).

ENTORNO ASIGNADO: una caja con red saliente a api.anthropic.com y ANTHROPIC_API_KEY en el entorno — primera candidata UBUNTU. NO lanzar en NUBE (cloud_default no tiene red — verificado). NO lanzar en Codex. No abre microdato. Gobierna: forense/prereg-duelo-v2/lanzamiento-L-v1_0.md (sha256 372624171ebef11b…, producido por #371/ADR de E2-PREP-L-RUN) — este encargo NO lo reescribe: lo manda ejecutar tal cual y añade solo lo que el lanzamiento delega al acto (arranque, A.3, verificación del marco, commits, cierre).

RANURA DE MESA — modelo (F2(a)); precargada con el valor sellado

MODELO: claude-opus-4-6 — mesa puede sustituirlo aquí antes de lanzar; el valor usado queda congelado en params de cada captura y en la nota.

Dependencia única de mesa antes de lanzar (checklist §8 del lanzamiento): ANTHROPIC_API_KEY presente en la caja elegida. Sin la clave, el acto PARA en la firma de entorno — eso es reporte, no fracaso.

════════ ARRANQUE — hazlo antes de leer el resto del encargo ════════

Reporta las cinco líneas de abajo y NO empieces hasta tenerlas. Si algo no cuadra, PARA y repórtalo: encontrar que el terreno no es el que el encargo supone es entregable, no interrupción.

1 · REPO. Localiza el CLON EXISTENTE. No clones uno nuevo salvo que no haya ninguno, y si clonas, dilo. Reporta: ruta absoluta · git log -1 --format="%h %s" · git status ⚠️ No arranques desde el home. Si el cliente avisa "launched in your home directory", cámbiate al clon antes de nada.

2 · SHA. Confirma contra qué base trabajas y compáralo con el que el encargo declara. Si main se movió: NO es PARO — refresca, re-deriva lo que dependa del perímetro, y reporta la diferencia antes de editar.

3 · data/raw. AUSENTE NO ES PARO. Es raíz integrada, gitignorada, resuelta por código; un clon fresco siempre nace sin ella. Se crea o se enlaza. Reporta: existe / la enlacé a <ruta> / la creé. ⚠️ Si este acto DESCARGA algo: verifica al cerrar que los payloads quedaron en el CORPUS COMPARTIDO y no solo en tu worktree. Es el defecto de PR #77 y no lo atrapa ningún test. (Este acto no toca data/raw — repórtalo y sigue.)

4 · ENTORNO. "Ubuntu" no distingue nada; la política de red sí. CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE → esperado: sin_variable curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://www.inegi.org.mx/ Reporta los dos valores crudos. NUNCA curl -I. Si este acto no toca microdato ni red, dilo y salta este punto. ⚠️ [NUEVO v2.11] Un negativo producido por un comando que no examinó archivos no es un negativo (A.13). Todo veredicto negativo —incluida la sonda de este punto— declara cuántos archivos examinó el comando que lo produjo. Además, firma A.2 del lanzamiento §4 (la que gobierna a este acto): sonda cruda al único host autorizado — curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://api.anthropic.com/ — y presencia/ausencia de ANTHROPIC_API_KEY (nunca el valor). api.anthropic.com es el único destino de red del acto.

5 · ESPEJO. Prohibido derivar cifras del espejo del proyecto: está versiones atrás del repo y contiene archivos que el repo nunca tuvo. Toda cifra sale del clon de (1), con el comando a la vista.

════════════════════════════════════════════════════════════════════

═══ VERIFICACIÓN DE EXISTENCIA — la contesta quien ESCRIBE el encargo [NUEVO v2.8] ═══ CONTESTADA por dirección, 26/ago/2026, clon en 8b317d3:

1 · ESTRUCTURA. Gobernantes: lanzamiento-L-v1_0.md (autocontenido: compuerta de hashes §0, invariantes §1, ceguera §2, driver §3, firma A.2 §4, capturas §5, muralla §6) sobre prereg-corrida-v1_0.md (ADR-197). INFRAESTRUCTURA no cubre prereg-duelo-v2/ — declarado.

2 · CONTENIDO.

El entregable NO existe: ls forense/prereg-duelo-v2/corridas-L → no existe (directorio ausente; universo: el árbol prereg-duelo-v2/, 15 archivos, listado completo revisado — A.13).
Los 6 pins F1 COINCIDEN hoy (re-derivados por tercera vez en dos sesiones; tabla en lanzamiento §0).
La ranura de modelo del encargo E2 en el repo quedó sin editar → valor sellado claude-opus-4-6 (lanzamiento §1, verificado por grep).
Costura detectada por dirección, y por eso este acto la verifica (no la supone): cargar_specs_desde_marco (pipeline-L-adv1-m2.py:96-118) lee forense/marco-candidatas-piloto-v1_0.tsv (el marco original), mientras el pin de hashes cubre marco-congelado-piloto-v1_0.tsv (el congelado de ADR-179, que corrigió DIN-09). DIN-09 no está entre las 15 sorteadas — pero eso se verifica por comando, no por memoria: ver paso 2.

3 · COBERTURA RETROACTIVA. Todo el árbol del duelo nació 25-26/ago; sin brecha.

════════════════════════════════════════════════════════════════════

OBJETO

Correr el lanzamiento tal cual: 15 celdas × 2 variantes × k=8 = 240 llamadas, temperatura 1.0, modelo de la RANURA, CERO descartes, capturas F2(e) commiteadas. Nada más.

PASOS

0-bis · A.3: commitea este encargo íntegro en forense/encargos/2026-08-26-E6-L-RUN.md; al cerrar, ## CONSUMIDO con el PR. 1 · §0 del lanzamiento: los 6 hashes, salida cruda en la nota. Discordancia → PARO con A.7 (qué campo cambió); si un corredor cambió legítimamente, regla de enmienda de prereg-corrida:110 — fila ## F1 · enmienda, y PARO hasta mesa. 2 · Verificación de la costura del marco: para las 15 id_celda sorteadas, diff fila a fila entre forense/marco-candidatas-piloto-v1_0.tsv y forense/prereg-duelo-v2/marco-congelado-piloto-v1_0.tsv (mismas columnas, mismos bytes por fila). Idénticas → sigue, con el diff vacío pegado en la nota (declara cuántas filas examinó — A.13). Cualquier diferencia → PARO: no elijas marco tú; mesa resuelve. 3 · Driver: implementa llamar_modelo EXACTAMENTE como el bloque del lanzamiento §3 (copia de trabajo o monkeypatch; pipeline-L-adv1-m2.py del repo intocado). version_declarada = r.model de la primera llamada, congelada en todas las capturas. Sin reintentos salvo error de transporte — un rechazo de contenido es corrida válida y se captura como cualquier otra. 4 · Corre las 240 (orden: por celda del sorteo, L-solo antes que L+corpus, índices 1..8) y captura cada una a forense/prereg-duelo-v2/corridas-L/{id_celda}__{variante}__{indice:02d}.json con el esquema del lanzamiento §5 (texto_crudo íntegro; valor_extraido: null). Errores de transporte: reintenta solo la llamada afectada y anótalo (llamada, hora, error) en la nota — el índice no se salta ni se duplica. 5 · COMMIT-A: las 240 capturas crudas + el encargo + la nota parcial. COMMIT-B: valor_extraido/fuente_citada poblados únicamente con el parseo del pipeline (agregar_continua/agregar_categorica y su extracción — nunca a mano), más un resumen agregado por celda×variante (mediana+q10/q90+IQR o moda+self-consistency+distribución) en la nota. Sin comparar contra nada — la muralla §6 rige: este acto jamás abre microdato ni corre corredor-B/corredor-E/scoring, y no aplica CV≥30%⇒SKIP. 6 · Conteo de cierre en la nota: ls corridas-L | wc -l = 240, y 15×2×8 por patrón (comando pegado). Nota forense/notas/2026-08-26-l-run-cierre.md (firma A.2 de 3 partes, hashes, costura, version_declarada, rechazos si hubo, resumen por celda) · ADR (máximo re-derivado — hoy 203; candidatea; renumera al fusionar, E5/E7 concurren) + recifrado estado §L0 · tests/check.py --baseline verde · PR.

PERÍMETRO Y CONCURRENCIA

Toca SOLO: forense/prereg-duelo-v2/corridas-L/** (nuevo) · forense/notas/2026-08-26-l-run-cierre.md (nueva) · forense/encargos/…E6….md (A.3) · canon/gobernanza-v1_15.md · canon/estado-programa-v1_10.md. Concurrentes: E5 (NUBE; milpa/tablero) — colisión esperada solo en gobernanza/estado; E7 NO arranca hasta que este PR fusione. "Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo."

PROHIBIDO

Todo lo del lanzamiento §6 (microdato; corredores B/E; scoring; SKIP) · editar pipeline-L-adv1-m2.py, prereg, sorteo, marcos o plantillas · meter una palabra al prompt fuera de construir_prompt · cualquier host distinto de api.anthropic.com · reintentar rechazos de contenido · llenar valor_extraido a mano · cambiar modelo_id/temperatura/k fuera de la RANURA.

CONTADOR

Cero directo, declarado — produce el insumo L del primer marcador; ninguna comparación ocurre aquí. La sesión mide en el sentido del programa: 240 elicitaciones capturadas sin descarte.

---

## Autorización de apertura de sesión (mesa, verbatim)

> CONFIRMO, como mesa y autor del programa: el encargo E6 · L-RUN es mío. El duelo ADV1-M2 está sellado en el repo por mis propios merges — FP-162, ADR-197 (#367) y el lanzamiento-L (#371); verifícalo por hash si quieres. AUTORIZO expresamente las 240 llamadas reales a claude-opus-4-6 vía API y su gasto (~$3–8 USD). La clave anterior quedó revocada en la consola; la nueva ya está cargada en el entorno de esta caja antes de abrir esta sesión — verifícala solo por presencia (test -n), jamás imprimas su valor. Procede con E6 tal como está escrito.

---

## FIRMA DE MESA — enmienda en sesión, 26/ago/2026 (verbatim)

**Qué la provocó.** El acto ejecutor reportó, *antes de gastar una sola llamada*, un hueco de pre-registro en la variante `L+corpus`: `corpus_id_si_aplica` y `contexto_corpus` no están definidos en ningún punto del árbol. Barrido con el comando a la vista:

```
grep -rn "corpus_id" --include=*.md --include=*.py --include=*.tsv .
→ 6 aciertos, LOS SEIS dentro de forense/prereg-duelo-v2/pipeline-L-adv1-m2.py. Cero fuera.
```

`ParametrosCorredorL.__post_init__` (`pipeline-L-adv1-m2.py:72-73`) lanza `ValueError` ante cualquier `L+corpus` sin ese campo, y `PLANTILLA_L_CORPUS` (`:140-142`) exige además un `contexto_corpus` que **entra al prompt**. Elegirlo el ejecutor habría violado a la vez el PROHIBIDO de este encargo («meter una palabra al prompt fuera de construir_prompt») y la regla rectora de `forense/prereg-duelo-v2/mesa-pendientes.md` («el acto documenta las opciones y para — no decide en lugar de mesa»).

Mesa respondió:

> FIRMA DE MESA — E6/L-RUN, hueco L+corpus: elijo correr HOY únicamente las 120 llamadas L-solo (comparación principal, FP-162) y AUTORIZO su gasto. L+corpus no se corre en este acto: abre fila nueva en forense/firmas-pendientes.tsv (A.12) que documente el hueco (corpus_id_si_aplica + contexto_corpus sin definir en el árbol) y los tres caminos tal como los tienes escritos, sin elegir ninguno — el contexto del prompt no se improvisa. Declara en la nota y en el ADR la consecuencia aguas abajo: el corredor E (ADR-141) exige los tres corredores, así que E queda bloqueado hasta que mesa selle esa fila o re-selle ⊕ — que E7 no pare en falso por esto. Ratifico además esta caja (Windows/Git Bash) como la designada: el requisito real era red + clave, y se cumple. Cierra E6 como parcial declarado: 120 capturas L-solo con conteo A.13, COMMIT-A/B, nota, ADR (candidatea 205, renumera si colisiona), check.py --baseline, PR; el encargo se marca CONSUMIDO-PARCIAL citando la fila sucesora.

**Efecto sobre el OBJETO.** 240 → **120 llamadas** (15 celdas × 1 variante `L-solo` × k=8). Los demás invariantes no cambian: temperatura `1.0`, modelo de la RANURA (`claude-opus-4-6`), `k=8`, **CERO descartes**, capturas F2(e) commiteadas.

**Efecto sobre el PERÍMETRO.** Se amplía en **un** archivo — `forense/firmas-pendientes.tsv` — por mandato expreso de esta firma. Ningún otro archivo fuera de la lista original se toca.

---

## CONSUMIDO

Pendiente de cierre — se completa con el PR al final de este acto.
