# ACTO REGISTRA-17AGO-II — tres firmas más, la reformulación de FP-29 y el texto que desbloquea FP-33

**Acto:** ACTO REGISTRA-17AGO-II · **Encargo:** `forense/encargos/2026-08-17-REGISTRA-17AGO-II.md` (archivado verbatim por A.3, primer commit) · **Entorno:** NUBE, repo-only, sin `data/raw` · **SHA de redacción del encargo:** `1282ae3` · **SHA real de arranque:** `d197465` (main posterior al merge de `#248`, ver §1) · **Depende de:** `ADR-91` (formato de `firmada_en`, tablero como fuente única), `ADR-92` (mismo formato, precedente inmediato de PR `#248`), `ADR-93` (este acto).

---

## §0 · ARRANQUE — las cinco líneas, crudas

**1 · REPO.** Ruta absoluta: `/home/user/Modelado-Mexicano`. Rama de trabajo: `claude/new-session-7avlfg` (asignada por el arnés, existente).

```
$ pwd && ls -la
/home/user/Modelado-Mexicano
$ git log -1 --format="%h %s" && git status
d197465 Merge pull request #248 from Josanoforo/claude/new-session-wk4z60
On branch claude/new-session-7avlfg
nothing to commit, working tree clean
$ git rev-parse --is-shallow-repository
true
```

Clon superficial detectado. Conforme al encargo (Bloque ARRANQUE 1), se corrió `git fetch --unshallow` **antes de cualquier veredicto**:

```
$ git fetch --unshallow origin
From https://github.com/Josanoforo/Modelado-Mexicano
 * [new branch]      codex/barrido-2 -> origin/codex/barrido-2
 + f8eb2e3...d197465 main            -> origin/main  (forced update)
```

**2 · SHA.** Base declarada por el encargo: el `main` posterior al merge de `#248`. Verificado, no supuesto:

```
$ git rev-parse origin/main && git rev-parse HEAD
d1974656ae786b47060e7f95c8c186f41364a204
d1974656ae786b47060e7f95c8c186f41364a204
$ git log -1 --format="%H %s" origin/main
d1974656ae786b47060e7f95c8c186f41364a204 Merge pull request #248 from Josanoforo/claude/new-session-wk4z60
$ git log d197465..origin/main --oneline
(vacío)
```

`origin/main` es exactamente el merge de `#248`, idéntico al HEAD local. **Sin deriva que clasificar** — `main` no avanzó más allá de `#248`.

**3 · `data/raw`.** Este acto **no toca microdato**. Dicho y saltado, per el encargo.

**4 · ENTORNO.**

```
$ date -u && echo "TZ=${TZ:-<unset>}"
Tue Aug 18 01:54:08 UTC 2026
TZ=<unset>
```

Nota de fecha: el reloj del contenedor marca UTC. Los commits de `#248` y de `codex/barrido-2` que este acto cita usan hora local `-0600` (p. ej. `2026-08-17 19:09:25 -0600` = `2026-08-18 01:09 UTC`), consistente con la convención de fecha que todo este clúster de actos ya usa (`17/ago/2026`). Este acto sigue esa misma convención local, no la fecha UTC cruda del arnés — declarado aquí para que quien audite después entienda por qué las fechas de este acto dicen `17/ago/2026` a pesar de que el reloj del contenedor ya marcaba `18/ago` en UTC.

**5 · ESPEJO.** Prohibido para cifras. No se usó.

---

## §1 · GATE — PR #248, y la base real

```
$ git fetch origin && git merge-base --is-ancestor d197465 origin/main && echo OK || echo "PARA: #248 sin fusionar"
OK
```

`PR #248` verificado además vía API: `state: closed`, `merged: true`, `merged_at: 2026-08-18T01:44:43Z`, `base.sha: 1282ae334604fe4e1a36188770c5d43a615c627b` (coincide con el SHA de redacción del encargo). **Gate satisfecho — el acto arranca.**

---

## §2 · Verificación de existencia — re-corrida contra el árbol real, no heredada

**Estampa de universo (A.10), re-verificada al arrancar.** El encargo estampó su verificación contra `origin/main = 1282ae3` y `origin/codex/barrido-2` con **12 commits**, el 17/ago.

```
$ git rev-list --count $(git merge-base origin/main origin/codex/barrido-2)..origin/codex/barrido-2
12
$ git log -1 --format="%H %s %ci" origin/codex/barrido-2
31a72e8b5926a20e2322261d71760f3d27619420 BARRIDO-2 C4(a): derivaciones previas a la curaduria... 2026-08-17 19:09:25 -0600
```

**12 commits — coincide exacto con la estampa del encargo.** El bloque de verificación de existencia del encargo **sigue vigente**, no `VENCIDO EN ALCANCE`; no manda uno propio.

**1 · ESTRUCTURA.**

```
$ head -1 forense/firmas-pendientes.tsv | tr '\t' '\n' | cat -n
1  id
2  qué_se_firma
3  dónde
4  creado
5  gatea
6  estado
7  firmada_en
```

7 columnas, confirmado.

**2 · CONTENIDO.**

```
$ grep -E "^FP-(24|29|30|31|33)\b" forense/firmas-pendientes.tsv | awk -F'\t' '{print $1, $6, "|firmada_en vacío:", ($7=="")}'
FP-24 ABIERTA |firmada_en vacío: 1
FP-29 ABIERTA |firmada_en vacío: 1
FP-30 ABIERTA |firmada_en vacío: 1
FP-31 ABIERTA |firmada_en vacío: 1
FP-33 ABIERTA |firmada_en vacío: 1
```

`FP-24`, `FP-30`, `FP-31` → `ABIERTA` con `firmada_en` vacío, confirmado (`EXISTE-NO-SATISFACE`). `FP-29`/`FP-33` existen, confirmado.

**3 · COBERTURA RETROACTIVA.** Sin brecha — confirmado por el propio encargo, no re-derivado (las cinco nacieron con el tablero el 14/ago o con `ADR-91` el 17/ago).

**Corrección de ruta encontrada al re-derivar, no heredada.** El encargo cita `data/relaciones.tsv` para el conteo de `FP-24`. Esa ruta **no existe**:

```
$ head -1 data/relaciones.tsv
head: cannot open 'data/relaciones.tsv' for reading: No such file or directory
$ find / -xdev -iname "*relaciones*" 2>/dev/null
/home/user/Modelado-Mexicano/data/curacion-registro/fusiones-relaciones.tsv
/home/user/Modelado-Mexicano/data/curacion-registro/relaciones.tsv
```

Se movió a `data/curacion-registro/relaciones.tsv` en un acto anterior a éste (fuera del alcance de este acto reconstruir cuál). No es discrepancia material — la tabla y sus columnas son las mismas que enlace2-clase-limbo.md §4 describe (citado sin backticks a propósito, mismo remedio que el propio §6 de esta nota) — pero es una deriva real contra lo que el encargo cita, declarada aquí y en `ADR-93(b)`, no silenciada.

---

## §3 · Commit 1 — las tres firmas + ADR-93

**Gate de `FP-31`: premisa derivada, no heredada.**

```
$ sed -n '120,125p' propuesta-motor-adaptativo-celda-v0_4.md
120
121  ## 8 · Preguntas para mesa — resueltas, 12/ago/2026
122
```
(línea 122 real del archivo, offset de lectura desde línea 115; el texto exacto exigido está en la línea de cabecera "## 8 · Preguntas para mesa — resueltas, 12/ago/2026")

El archivo **sí** dice el texto literal exigido. La firma procede. La propuesta no se edita — fuera de perímetro.

**Gate de `FP-24`: conteo 48/20 re-derivado contra `data/curacion-registro/relaciones.tsv`, definición de par de §4.**

```python
rows = list(csv.DictReader(open("data/curacion-registro/relaciones.tsv"), delimiter="\t"))
# total rows: 197
sior = [r for r in rows if r["capa2_manifiesto"] == "SI_O_REFERENCIADO"]
# SI_O_REFERENCIADO count: 20
# agrupa filas SI por (necesidad_id, fuente_canonica_normalizada); una SI_O_REFERENCIADO
# es CON PAR si existe una fila SI en el mismo grupo
```

Resultado:

```
total rows: 197
SI_O_REFERENCIADO count: 20
CON PAR: 20
SIN PAR: 0
reparto por fuente (CON PAR): {'ENSAFI': 9, 'ENFIH': 8, 'ENBIARE': 3}
necesidades afectadas: ['N10', 'N12', 'N13', 'N14', 'N3', 'N4']
```

**20 reproducido — la firma procede.** Nota metodológica, declarada porque cambia lo que significa el número: hoy **todas** las `SI_O_REFERENCIADO` vigentes son las 20 `CON PAR` (0 `SIN PAR`), no 48+20 como en la nota histórica del 14/ago — porque `ACTO ENLACE-2` ya promovió las 48 `SIN PAR` a `SI` en su momento, y esas filas salieron del universo `SI_O_REFERENCIADO`. El 20 coincide con la nota histórica porque el conjunto `CON PAR` nunca se tocó, no porque se copiara sin verificar.

**Gate de `FP-30`: defecto de `tests/check.py:286` verificado en el archivo real.**

```
$ sed -n '286p;289,298p' tests/check.py
286: CANONICO = {"FUERTE", "MEDIA", "MEDIA-FUERTE", "HIPÓTESIS"}
...
294:    if tok.strip().upper() not in CANONICO:
295:        ajenos[tok.strip()] += 1
```

Confirmado: `CANONICO` no incluye `narrativa popular` y sí incluye `MEDIA-FUERTE` (ausente del Bloque A). Confirmado el segundo defecto: el chequeo compara con `.upper()` (línea 294) pero acumula con `tok.strip()` sin mayusculizar (línea 295) — `Moderada`/`MODERADA` se cuentan dos veces. `FP-41` (creada por `#248`) ya lo registra; este acto no crea fila nueva, solo lo deja escrito en `ADR-93(d)` porque la firma de `FP-30` quedaría a medias sin decirlo.

**Escritura.** `forense/firmas-pendientes.tsv`: `FP-24`, `FP-30`, `FP-31` → `estado=FIRMADA`, `firmada_en` con cita + texto adoptado, formato idéntico al de `ADR-91`/`ADR-92` (`ADR-93, PR #249, 17/ago/2026 -- firma de mesa verbatim: "..." sobre el texto adoptado: "..."`). `canon/gobernanza-v1_15.md`: cabecera `92 ADR` → `93 ADR`; `ADR-93` insertado en §4, entre `ADR-92` y `## 5. Deuda declarada`, con (a)/(b)/(c) las tres firmas, (d) el inciso de `tests/check.py:286`, "Lo que este ADR NO hace", "Concurrencia con BARRIDO-2, declarada" y "Cascada" (`CASCADA NO ESCRITA` sobre `estado-programa-v1_10.md`, ver §6).

**T15 simulado tras la escritura** (misma receta `^\*\*ADR-(\d+)` que el test usa):

```
count: 93 unique: 93 max: 93
dups: []
gaps: []
```

Sin huecos, sin duplicados. `93` es el número — coincide con lo que el encargo predijo (`92` tras `#248`, +1).

---

## §4 · Commit 2 — FP-29 reformulada, FP-33 desbloqueada

Ninguna de las dos cambia `estado` — ambas siguen `ABIERTA`.

`FP-29`: `qué_se_firma` y `gatea` reescritos verbatim por instrucción de mesa citada en el encargo. La fila deja de pedir "elige una magnitud" y pasa a nombrar el problema real: procedencia del 22% (tres atribuciones distintas de la misma cifra en cuatro documentos) y escalas no comparables (ENCUCI 0-10 con corte vs. ítem binario en WVS/Pew/Latinobarómetro).

`FP-33`: se pega inline (A.3) el texto de las cuatro preguntas de `U3/DOC-BACKFILL` y la población cerrada por criterio, procedencia declarada **tipo (3)** (conversación de proyecto "003 - Maestra 25 (Fable)", 12/ago/2026, no el repo). Aviso re-derivado, no copiado:

```python
# data/curacion-registro/produccion-modelo.tsv
# distintos producciones (col 1, PROD-*): 12
# data/curacion-registro/celdas-d/*.yaml: 3 archivos
```

```
$ cut -f1 data/curacion-registro/produccion-modelo.tsv | tail -n +2 | sort -u | wc -l
12
$ ls data/curacion-registro/celdas-d/ | wc -l
3
```

**12 producciones, 3 celdas-D — confirmado contra el árbol de hoy, no 11.** La enumeración original del 12/ago ("las 11 producciones... ocho fichas") queda escrita en la fila como foto vencida, con la cifra de hoy al lado.

---

## §5 · PR — abierto temprano para conocer su propio número

Mismo mecanismo que `#248` usó sobre sí mismo: primer commit archiva el encargo (sin citar su propio PR), se empuja, se abre el PR en borrador, y los commits de contenido citan el número ya conocido.

```
$ git push -u origin claude/new-session-7avlfg
```
→ `PR #249` (`https://github.com/Josanoforo/Modelado-Mexicano/pull/249`), creado en borrador, título `REGISTRA-17AGO-II: tres firmas más, la reformulación de FP-29 y el texto que desbloquea FP-33`, base `main`. Coincide con el siguiente número disponible tras `#248` — no hubo PR/issue intermedio.

---

## §6 · Hallazgo — el propio encargo archivado dispara T03 sobre sí mismo, declarado y no corregido

Al correr `--baseline` tras el primer commit, `T03` marcó una entrada nueva sobre el archivo que este mismo acto acaba de archivar:

```
T03: forense/encargos/2026-08-17-REGISTRA-17AGO-II.md: cita PLAN-MULTIFASE-F0-F6-2026-08-13.md (entre backticks en el original) que no existe
```

Causa, verificada línea por línea (backticks retirados aquí a propósito, mismo remedio que esta nota aplica en todo lo demás, para no propagar el disparo a este archivo también):

```
$ grep -n "PLAN-MULTIFASE" forense/encargos/2026-08-17-REGISTRA-17AGO-II.md
46: ... PLAN-MULTIFASE-F0-F6-2026-08-13.md (entre backticks en el original) y los transfers **no están en el árbol** y T03 dispara sobre ellos ...
62: ... el texto se propuso en PLAN-MULTIFASE-F0-F6-2026-08-13.md §94 ... (sin backticks)
```

La línea 46 del encargo es la **advertencia del propio encargo** contra citar ese archivo con backticks — y la ilustra citándolo, con backticks, como ejemplo de lo que no hacer. La línea 62 (el texto que este acto sí escribió, dentro del mismo documento archivado) sigue la advertencia correctamente y **no** usa backticks — verificado arriba, T03 no dispara sobre ella.

**No se corrige.** `A.3` exige archivar el encargo **verbatim** — "el texto completo del encargo tal como se lanzó… no un resumen" (`forense/encargos/convencion.md`); editar la línea 46 para quitarle los backticks alteraría el registro histórico de lo que dirección pidió exactamente, que es lo que `A.3` existe para proteger. El archivo no está en mi perímetro de edición más que para su lanzamiento inicial (A.3) y su cierre a `CONSUMIDO` (ver §8) — no para correcciones de estilo. Se declara aquí como hallazgo, se dejará como entrada `ROJO` esperada y explicada, mismo criterio que el encargo pide para toda la sección de cierre ("Espera ROJO y decláralo").

---

## §7 · Resincronización de T16 en `gobernanza-v1_15.md` — commit de higiene

Tras el commit 1, `--baseline` mostró dos entradas nuevas de `T16` sobre citas **vigentes** (no históricas) de `**20 FAIL · 128 WARN**` dentro de `canon/gobernanza-v1_15.md` — desincronizadas porque las tres firmas nuevas quitan tres `WARN` de `T22` (128→126 reales). `canon/gobernanza-v1_15.md` está dentro de mi perímetro (es donde vive `ADR-93`), así que se resincroniza — mismo mecanismo que `ADR-91` ya usó sobre sí mismo y que `#248` ya aplicó sobre este mismo archivo ("cinco citas mutables… resincronizadas, cambio de dígito").

Localización exacta, replicando la regex de `T16` (`\*\*(\d+)\s*FAIL\s*·\s*(\d+)\s*WARN\*\*`, excluyendo líneas que empiezan con `> **vX.Y — DD/mon.**`) contra `real_fail=20, real_warn=126` (`_suite_real()` excluye a `T16` de sí mismo — por eso `20`, no el total visible de `29`):

```
764:  declara 20 FAIL · 128 WARN -- MISMATCH
856:  declara 20 FAIL · 128 WARN -- MISMATCH
1106: declara 18 FAIL · 104 WARN -- MISMATCH (permanente, ADR-76(f) -- NO se toca)
1136: declara 18 FAIL · 104 WARN -- MISMATCH (permanente, ADR-77 -- NO se toca)
1274: declara 20 FAIL · 128 WARN -- MISMATCH
1387: declara 20 FAIL · 128 WARN -- MISMATCH
1393: declara 20 FAIL · 128 WARN -- MISMATCH
```

Cinco líneas mutables (764, 856, 1274, 1387, 1393), todas con el mismo patrón exacto `**20 FAIL · 128 WARN**` (verificado `grep -c` antes de tocar: exactamente 5 ocurrencias en todo el archivo):

```
$ sed -i 's/\*\*20 FAIL · 128 WARN\*\*/**20 FAIL · 126 WARN**/g' canon/gobernanza-v1_15.md
$ grep -c '\*\*20 FAIL · 126 WARN\*\*' canon/gobernanza-v1_15.md   # 5
$ grep -c '\*\*20 FAIL · 128 WARN\*\*' canon/gobernanza-v1_15.md   # 0
```

Las dos "permanentes" (`:1106`, `:1136`, `18 FAIL · 104 WARN`) **no se tocan** — son historia sellada de `ADR-76(f)`/`ADR-77`, la razón por la que `T16` las flaguea siempre está clasificada aparte en `tests/baseline.json` (normalización de `_baseline_key`, `ADR-90`), no es un defecto de este acto.

---

## §8 · Cierre — `tests/check.py --baseline`, antes, entre medias y después

**Antes de tocar nada:**

```
23 FAIL · 128 WARN
LÍNEA BASE: VERDE — nada nuevo frente a tests/baseline.json (HEAD congelado c2bf325725831bee68628d297f34a82911c860d6)
```

**Después de los dos commits de contenido (antes de la resincronización de §7):**

```
29 FAIL · 126 WARN
LÍNEA BASE: ROJO — 5 entradas nuevas
· T03: forense/encargos/2026-08-17-REGISTRA-17AGO-II.md (ver §6)
· T15: canon/estado-programa-v1_10.md: cita 91 ADR; gobernanza tiene 93 únicos
· T16: canon/estado-programa-v1_10.md: declara 128 WARN vigente
· T16: canon/gobernanza-v1_15.md: declara 20 FAIL · 128 WARN vigente   (resuelto en §7)
· T22: FP-29 ABIERTA (cambio de fila, señal no defecto)
```

**Después de la resincronización de §7 — estado final:**

```
24 FAIL · 126 WARN
LÍNEA BASE: ROJO — 4 entradas nuevas frente a tests/baseline.json (HEAD congelado c2bf325725831bee68628d297f34a82911c860d6)
· T03: forense/encargos/2026-08-17-REGISTRA-17AGO-II.md: cita PLAN-MULTIFASE-F0-F6-2026-08-13.md (entre backticks en el original), que no existe   [§6 -- inherente al archivado verbatim de A.3, no se corrige]
· T15: canon/estado-programa-v1_10.md: cita 91 ADR; gobernanza tiene 93 únicos   [CASCADA NO ESCRITA -- fuera de perímetro, BARRIDO-2]
· T16: canon/estado-programa-v1_10.md: declara 128 WARN vigente; la corrida real da N WARN   [misma cascada, mismo archivo fuera de perímetro]
· T22: FP-29 ABIERTA desde 2026-08-17 (N días): ... [cambio de fila -- señal, no defecto]
```

**Conteo de entradas nuevas por test, no agregado** (per el encargo, sección Cierre): `T03` **1** nueva · `T15` **1** nueva · `T16` **1** nueva · `T22` **1** nueva. Las cuatro explicadas arriba; ninguna es un defecto introducido sin explicación.

## §9 · Recongelado — autorizado por mesa tras el cierre, CI en verde

El encargo instruía **no recongelar** por defecto (`--freeze` exige `ADR-76(f)` y el encargo no lo traía firmado) — cumplido en el cierre de §8. Tras empujar el PR, CI (`.github/workflows/verify.yml`, job `check`) corrió `python3 tests/check.py --baseline` sobre `refs/pull/249/merge` y salió con `exit 1` — **exactamente** las mismas cuatro entradas de §8, verificado contra el log crudo del job, no supuesto:

```
$ (mcp github get_job_logs, job_id=95573299612)
...
LÍNEA BASE: ROJO — 4 entradas nuevas frente a tests/baseline.json (HEAD congelado c2bf325725831bee68628d297f34a82911c860d6)
· T03: forense/encargos/2026-08-17-REGISTRA-17AGO-II.md: cita PLAN-MULTIFASE-F0-F6-2026-08-13.md (entre backticks en el log real), que no existe
· T15: canon/estado-programa-v1_10.md: cita 91 ADR; gobernanza tiene 93 únicos
· T16: canon/estado-programa-v1_10.md: declara 128 WARN vigente; la corrida real da N WARN
· T22: FP-29 ABIERTA desde 2026-08-17 (N días): ...
##[error]Process completed with exit code 1.
```

Se pidió resolver el CI. Ninguna de las cuatro es corregible dentro de este acto sin violar algo ya declarado (T03: violaría el archivado verbatim de A.3; T15/T16: violarían el perímetro declarado fuera por colisión con BARRIDO-2; T22: no es un defecto). La única vía que el propio repo ofrece para poner `--baseline` en verde sin tocar ninguna de las tres es `--freeze`, y `--freeze` exige autorización de mesa citando `ADR-76(f)` — mismo criterio que `ADR-86`/`ADR-88`/`ADR-90`/`ADR-91(f)`/`ADR-92(h)` ya sentaron. Presentadas dos opciones vía `AskUserQuestion` (recongelar ahora en commit propio · dejar `ROJO` y declarado), escritas antes de la respuesta: el usuario eligió *"Freeze baseline now (Recommended)"*.

```
$ python3 tests/check.py --freeze
[--freeze] escrito tests/baseline.json — HEAD 45d7d2f541fca5edbc6205d2c8ea713a2276f8cc · 22 fail · 115 warn congelados
$ python3 tests/check.py --baseline
24 FAIL · 126 WARN
LÍNEA BASE: VERDE — nada nuevo frente a tests/baseline.json (HEAD congelado 45d7d2f541fca5edbc6205d2c8ea713a2276f8cc)
```

`ADR-93` gana inciso `(e)` documentando la autorización, mismo commit que el recongelado — no se mezcla con los commits de contenido ni con el de higiene de §7.

**`git diff --check`**, corrido sobre cada commit antes de escribirlo:

```
forense/firmas-pendientes.tsv:30: trailing whitespace.
forense/firmas-pendientes.tsv:34: trailing whitespace.
```

Verificado antes de asumir que es un defecto nuevo: el archivo tiene una columna final (`firmada_en`) que queda vacía en toda fila `ABIERTA`/`FIRMADA-CONDICIONAL` sin firma, lo que deja un tab final en la línea — propiedad estructural del TSV, no algo que este acto introdujo.

```
$ git show d197465:forense/firmas-pendientes.tsv | grep -cP '\t$'
16
$ grep -cP '\t$' forense/firmas-pendientes.tsv   # tras las tres firmas
13
```

16 líneas con tab final **antes** de este acto (todas las filas sin `firmada_en`), 13 **después** (bajó exactamente en 3, las que este acto firmó). Las dos líneas que `git diff --check` marca (`FP-29`, `FP-33`) ya tenían esa propiedad en el archivo original — el diff las marca porque la línea entera se reemplaza, no porque este acto haya añadido espacio en blanco nuevo. No se corrige: hacerlo exigiría cambiar la convención de las 13 filas restantes, fuera de perímetro y fuera de lo que este acto pide.

---

## Contadores del programa

**0.** Ningún contador de medición sobre México se mueve. Tablero: `FIRMADA` 16→19, `ABIERTA` 16→13 (dos de ellas, `FP-29`/`FP-33`, cambian de forma sin cambiar de estado), `FIRMADA-CONDICIONAL` sin cambio en 10. Dicho así, sin justificarlo.
