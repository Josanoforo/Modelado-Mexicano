# ENCARGO · ACTO GEN2-RELEVO-TANDA-4 · LAS TRES DECISIONES QUE TANDA-3 DEJÓ EN MESA SE EJECUTAN: TRES PINES M DE ENIGH, RES-0028 EN CLASE PROPIA, Y LOS 9 L DEJAN DE PARECER CANDIDATOS

> ENTORNO: **NUBE** — no abre microdato: todo lo que lee (corridas, specs, asientos de replay, consumidores) está en el repo. NO es CAJA; si crees necesitar el corpus, una premisa de este encargo está mal: dilo.

CABECERA · SHA de redacción `deddfd42`; re-deriva al abrir · una sola sesión, rama `acto/gen2-relevo-tanda-4` · MODELO: Opus (toca guardas de relevo) · MODO: **ABIERTO** — no hay reserva de evaluación ni spec congelada · CONTADOR: `dependencias_numericas_legacy_activas` baja en **exactamente** las lecturas pineadas (hoy 150 → 146 si entran las cuatro; re-deriva el punto de partida, no heredes el 150); `N_resultados_gen2_adoptados_activos` se reporta antes y después — la adopción es el merge de mesa (E.2), no tu commit · FP/ADR/NC: raíz de acto (D-2); busca por objeto.

## 1 · OBJETIVO
Cerrar lo que `GEN2-RELEVO-TANDA-3` (#942) dejó en tres firmas pendientes, para que cuatro lecturas salgan de legacy por la puerta correcta y nueve dejen de estar mal rotuladas. Habilita: el marco del duelo pasa de 46 a 43 lecturas legacy y el contador muestra por primera vez sus tres clases de relevo sin fundirlas.
«Hecho» significa: las tres FP `FP-260921-GEN2-RELEVO-TANDA-3-7bf5-01/02/03` en `FIRMADA` con este PR · los tres pines M escritos y `ACEPTADO` por `valida_pin` · RES-0028 relevada en clase propia y visible como tal en `status` · los 9 L con rótulo que diga lo medido · `tests/test_pines_mesa.py` en verde con los casos nuevos · el contador bajó en exactamente lo pineado.

## 2 · FIRMAS DE MESA
Ya dadas, verbatim `[LEÍDO: forense/encargos/2026-09-21-GEN2-RELEVO-TANDA-3.md:8]`: 4.1 (21/sep): «Una lectura sale de legacy solo por una de dos vías: (i) su cifra la produce código GEN2 desde un insumo crudo con hash —microdato o capturas selladas—; o (ii) es la lectura de una conducta del motor que ya tiene procedencia GEN2, y entonces el pin cita el RESULT de esa conducta, no una foto. Ingerir un número GEN1 como insumo, con hash o sin él, no cuenta nunca. El contador muestra las clases sin fundirlas.» FP-391 (20/sep): «Se conserva el estimando sobre personas (U4). RES-0028 se aparea con el complemento sobre personas, no con el de delitos.» Firma 3A (21/sep, #940) `[EJECUTADO: grep en data/corrida0/decisiones.tsv]`: la fila `CALC-ENVIPE-RES0028-U4-DERIVADO-0001 · cuenta_gen2=SI` **ya existe** — no se escribe otra.
**Resueltas por dirección bajo el mandato de mesa del 21/sep («dámelas resueltas») — tu lanzamiento con este archivo es el sello:**
- **7bf5-01 · los tres M de ENIGH:** «Para la vía (ii) de 4.1 la guarda lee el eje RESULTADO del replay; el CONTEXTO se declara en la nota del pin. Se pinean FAM-M-05, 06 y 07 a `RESULT-B-ENIGH-2022-P`. La vía (i) no cambia: sigue exigiendo REPRODUCE.»
- **7bf5-02 · RES-0028:** «`CALC-ENVIPE-RES0028-U4-DERIVADO-0001` ya cuenta por la firma 3A. Un derivado determinista de un RESULT GEN2 releva en clase propia, `iii-DERIVADO-DE-GEN2`, que el contador muestra sin fundir con (i) y (ii). La clase exige que TODO lo ingerido sea RESULT de un CALC sellado, que cuenta y cuyo replay es afirmativo en RESULTADO; un derivado de un derivado no entra. "Ingerir un número GEN1 no cuenta nunca" queda intacto.»
- **7bf5-03 · los 9 L:** «Gobierna la mediana que la spec sellada declara; la media de GEN1 queda como historia (E.1). El relevo de L exige un CALC propio que mida desde las capturas. Los 9 se re-rotulan para que no parezcan candidatos a pin.»

## 3 · LO QUE DIRECCIÓN SABE (contra `deddfd42`)
- `[EJECUTADO]` `corrida0.py status`: legacy 150 (motor 35 · procedencia 40 · catálogo 23 · marco del duelo 46 · celdas-D 6), selladas 119.
- `[EJECUTADO]` las tres FP siguen `PENDIENTE` en `forense/firmas-pendientes.tsv`; su texto es la fuente de las premisas `[REPORTADO]` de abajo.
- `[LEÍDO]` `tools/pines_mesa.py:287-290`: la guarda (a) rechaza todo replay que no empiece por `REPRODUCE`. `:296-300`: la guarda (d) rechaza todo CALC que `_ingiere` otro resultado, sin distinguir la generación de lo ingerido. `VIAS` hoy: `i-CRUDO` (14 pines) y `ii-CONDUCTA-GEN2` (12) `[EJECUTADO sobre pines-de-mesa.tsv]`.
- `[EJECUTADO]` vocabulario real de `forense/replay-evidencia.tsv` (148 asientos): `REPRODUCE`/IDENTICO 123 · `REPLICA-RESULTADO · CONTEXTO-DISTINTO` 17 · `NO-EJECUTABLE` 3 · `NO-REPRODUCE · CONTEXTO-DISTINTO` 3 · `NO-REPRODUCE` 1 · `REPRODUCE`/DISTINTO 1. **Consecuencia:** abrir el eje RESULTADO en la vía (ii) vuelve pineables las conductas de hasta 17 corridas, no solo `CALC-B-0001`. Este acto escribe **solo los tres pines firmados**; reporta la lista de lo que quedó pineable y no lo pinees.
- `[LEÍDO]` `replay-evidencia.tsv:29`: `CALC-B-0001` replica resultado; contexto distinto porque `tools/baseline_temporal.py` cambió en `67aa13d` después del sello; los cuatro inputs ENIGH COINCIDEN.
- `[EJECUTADO]` `corridas.tsv`: `CALC-ENVIPE-0001` SELLADA · GEN2 · cuenta SI (el padre). El hijo `CALC-ENVIPE-RES0028-U4-DERIVADO-0001` aparece `PENDIENTE-DE-MESA`, **pero** `decisiones.tsv` ya trae su `cuenta_gen2=SI` (commit `00ac06e9`, 21/sep 03:12) y el último commit que tocó `corridas.tsv` es anterior (`56dc64ca`, 20/sep 21:37): el derivado está atrasado respecto de la firma. Re-deriva el registro y di qué sale; si sigue `PENDIENTE-DE-MESA` con la fila presente, ése es un hallazgo sobre la precedencia y lo reportas — no escribas una segunda fila.
- `[REPORTADO por FP 7bf5-02]` el bloqueo técnico de #914 ya está arreglado; con el pin escrito `status` no PARA, pero el contador bajaba sin que subieran los adoptados porque entonces `cuenta_gen2` no estaba firmada (hoy sí). `[EJECUTADO]` en `milpa/tramite.yaml:637` hoy no hay pin: búscalo por la conducta, no por la línea.
- `[EJECUTADO]` el rótulo `REHECHO-CON-DIFERENCIA` vive en `forense/analisis/relevo-reconcilia-1/reconcilia-173-v1_0.tsv` (y su informe y script), en `data/adq-demanda-activa-v1_0.json`, en canon y en `registro-rotulos.tsv`. El `v1_0` es un análisis fechado: no se edita (A.10).

## 4 · YA HECHO / YA DECIDIDO
Busqué por objeto con `git grep` sobre todo el árbol y en las ramas remotas (5 vivas, ninguna de relevo): «TANDA-4» → 6 archivos, todos menciones como sucesor, 0 encargos con ese nombre · «DERIVADO-DE-GEN2» → 0 · `pines-de-mesa.tsv` trae FAM-M-05/06/07 solo en su lectura `::R` (vía i), no en `::M` · «RES0028» en `decisiones.tsv` → 2 filas (el estimando de FP-391 y la `cuenta_gen2=SI` de 3A). #942 dejó las tres decisiones en mesa y no las ejecutó; nadie lo ha hecho después. **Repítela tú.**

## 5 · PIEZAS
**P1 · Guarda (a), eje RESULTADO para la vía (ii).** Para qué: que un sello cuyo resultado replica no quede fuera por un cambio de contexto ajeno a la lectura. Queda bien si: la vía (i) sigue rechazando todo lo que no sea `REPRODUCE`; la vía (ii) acepta los tokens afirmativos en RESULTADO y rechaza `NO-REPRODUCE*` y `NO-EJECUTABLE`; el conjunto de tokens afirmativos se deriva del vocabulario del propio `corrida0` (no una lista tecleada aparte); el mensaje de rechazo sigue diciendo por qué; tests nuevos: un caso por token del vocabulario real.
**P2 · Los tres pines M.** Las lecturas `::M` de FAM-M-05/06/07 (las `::R` ya están pineadas por vía i) → `RESULT-B-ENIGH-2022-P`, vía (ii), con el CONTEXTO declarado en `nota` (qué input cambió, en qué commit, y que los cuatro ENIGH coinciden). Queda bien si `valida_pin` los devuelve `ACEPTADO` y el contador baja 3.
**P3 · Clase `iii-DERIVADO-DE-GEN2`.** Para qué: que RES-0028 salga de legacy **y** entre a los adoptados en el mismo acto, por una puerta que no sea la de crudo ni la de conducta. La fila de `cuenta_gen2` ya existe (§2): no se duplica; vía nueva en `VIAS`; la guarda (d) admite la ingestión solo bajo las cuatro condiciones de la firma, verificadas contra `corridas.tsv` y el asiento de replay del padre; `status` muestra las tres clases por separado y su suma es el total. Tests: acepta RES-0028 · rechaza un derivado cuyo padre no cuenta · rechaza un derivado de un derivado · rechaza un derivado que además ingiere algo GEN1. Queda bien si el contador baja 1 y los adoptados suben en lo que el registro diga — reporta el número, no lo fuerces.
**P4 · Los 9 L.** Rótulo sucesor que diga lo medido (p. ej. que difieren por agregador y por conjunto de extracción, y que su relevo exige CALC propio), en un sucesor `v1_1` o adenda — sin tocar el `v1_0`. Inscríbelo en `registro-rotulos.tsv`. Queda bien si ningún derivado ni la vista los ofrece ya como candidatos a pin. Deja NC con sucesor para el CALC de L desde capturas: no lo construyas.
**P5 · Cierre.** Las tres FP a `FIRMADA`; re-deriva vista, tablero y marcador por comando; nota de una página: qué salió de legacy, por qué puerta, qué quedó pineable y sin pinear tras P1.

## 6 · LATITUD
Decides tú: nombres de tokens y constantes · si la derivación de «afirmativo en RESULTADO» vive en `pines_mesa` o se importa de `corrida0` · forma del rótulo de los 9 L · orden de las piezas (P3 no depende de P1) · arreglar lo adyacente de ≤ 10 líneas que impida terminar. Replantea y sigue si: main se movió · una línea citada ya no está donde dije · una FP fue renumerada. Una pieza que no sale no tumba las otras (D-11): NC con sucesor y sigue.

## 7 · PAROS — lista cerrada
a) el contador baja en **más** lecturas que las pineadas en este acto · b) para que algo pase habría que relajar la vía (i), o admitir en la clase (iii) algo que ingiere GEN1 · c) escribir un pin que no sea uno de los cuatro firmados aquí · d) editar una spec sellada, un `resultados.json` o el análisis `v1_0` · e) `DIN-M-01:M` deja de ser legacy (mesa lo vetó) · f) entorno equivocado.

## 8 · COMPUERTAS
«Los tests de P1 y P3 en verde antes de escribir un pin» protege: **adoptar**. Ninguna más.

## 9 · PERÍMETRO
Propio: `tools/pines_mesa.py` · `data/corrida0/pines-de-mesa.tsv` · `tests/test_pines_mesa.py` · la porción de `tools/corrida0.py` que muestra las clases en `status` · el pin de RES-0028 en su consumidor · el sucesor del análisis de los 9 L · `registro-rotulos.tsv` · derivados por comando · nota · cascada. Ajeno: specs y resultados sellados · `tools/relevo_usos.py` salvo lo adyacente que impida terminar · `milpa/src/**` · `matriz`, `motor`, `celdas`. Perímetro de cierre: el permanente. Si te encuentras escribiendo fuera de esta lista, PARA.

## 10 · NO HACE · SUCESORES · CIERRE
No pinea nada más de lo que P1 vuelve pineable · no re-sella `CALC-B-0001` · no construye el CALC de L · no toca `DIN-M-01`. Sucesores: decisión de mesa sobre lo que quedó pineable tras P1 (dirección la lleva con la lista de este acto) · CALC de L desde capturas (sin dueño; NC). Falsador a tres meses: si la clase (iii) no vuelve a usarse, se anota y no se generaliza. `## NO-CORRIDO / RESERVAS` · `## CONSUMIDO`.

## NO-CORRIDO / RESERVAS

| id | pieza | qué no se corrió | razón (token al principio) | impacto | sucesor |
|---|---|---|---|---|---|
| `NC-260921-GEN2-RELEVO-TANDA-4-dedd-01` | P4 | El CALC propio que mida los 9 puntos L desde las 224 capturas, con el extractor y el agregador declarados (no `CALC-TRIADA-0001`, que ingiere `snapshot-M-triada-v1_0.json`). | `DIFERIDO-A:<sucesor sin dueño — CALC de L desde capturas>` — §10 de este encargo lo prohíbe explícitamente («no construye el CALC de L»). | Los 9 L siguen `LEGACY-GEN1`, dentro de `legacy_marco_M_por_campo__L = 28`. **El contador no se mueve por esto**: ya contaban y siguen contando. Lo que sí cierra este acto es que dejen de **parecer** candidatos a pin. | Sin dueño; dirección asigna. Insumos listos: `forense/analisis/relevo-tanda-4/P4-los-9-L-rotulo-sucesor-v1_1.md` y el `v1_0` de TANDA-3. |
| `NC-260921-GEN2-RELEVO-TANDA-4-dedd-02` | P1 | Pinear las diez corridas restantes que el eje RESULTADO habilitaría si la vía (i) también lo leyera: ocho `CALC-R-*` (`DIN-M-01`, `FAM-M-01`, `FAM-M-05`, `FAM-M-06`, `FAM-M-07`, `TRA-M-02`, `TRA-M-03`, `TRA-M-07`) y dos de ENCIG (`…CRUCES-HISTORICOS-0003`, `…-0002`). | `DECISIÓN-DE-MESA-PENDIENTE` — su puerta natural es la vía (i), y relajarla es **PARO (b)** de §7. Ninguna tiene un RESULT declarado por una conducta GEN2, así que la vía (ii) tampoco las alcanza. | Cero lecturas extra fuera de legacy. El contador bajó en exactamente lo firmado (−4). | Dirección lleva la pregunta a mesa con la lista derivada de este acto (`forense/notas/nota-2026-09-21-gen2-relevo-tanda-4-cierre.md` §3). |

Todo lo demás del encargo **se corrió**: P1, P2, P3, P4 (la parte que sí autoriza) y P5 completas; las tres FP a `FIRMADA`; vista, tablero y marcador re-derivados por comando; cascada de cierre completa. **Paso anti-`PR #77`: no aplica** — este acto no descarga nada y no escribe en `data/raw` (`ENTORNO: NUBE`, corpus no montado, `archivos_examinados = 0`). **PAROS de §7 disparados: ninguno.**

## CONSUMIDO

**PR #959** — `https://github.com/Josanoforo/Modelado-Mexicano/pull/959`, rama `acto/gen2-relevo-tanda-4`, 21/sep/2026, `ADR-590` (renumerado 589 → 590 tras fusionar `origin/main` `33d97e12`). Las tres FP `FP-260921-GEN2-RELEVO-TANDA-3-7bf5-01/02/03` quedan **FIRMADA** con este PR (A.12). `dependencias_numericas_legacy_activas` **150 → 146** (exactamente las cuatro lecturas pineadas); `N_resultados_gen2_adoptados_activos` **71 → 72**; `N_corridas_selladas` sin mover. Clases de relevo, sin fundirse: `i-CRUDO = 14 · ii-CONDUCTA-GEN2 = 13 · iii-DERIVADO-DE-GEN2 = 1`. Ningún PARO de §7 se disparó. **Mesa fusiona; el PR se publica propuesto.**
