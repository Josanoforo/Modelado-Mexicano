# ACTO SELLA-AGO24-D — nota de cierre (24/ago/2026)

Encargo: `forense/encargos/2026-08-24-SELLA-AGO24-D.md`. ADR: `ADR-150`
(`canon/gobernanza-v1_15.md`).

## ARRANQUE

1. **Repo.** `/home/user/Modelado-Mexicano`, clon existente, no se arrancó desde el
   home. `git log -1` → `a664ea5 Merge pull request #317 from
   Josanoforo/recenso-diseno-14` (`ACTO RECENSO-DISEÑO-14`). `git status` → limpio.
2. **SHA / gate.** El encargo declara `⛔ ORDEN`: lanzar tras fusionar `SELLA-AGO24-C`
   con `FP-112` ya firmada. **Ninguna de las dos condiciones es cierta contra el árbol
   real**: `git log --all --oneline | grep -i "sella-ago24-c"` → 0 resultados (verificado
   tras `git fetch --unshallow`, ver punto 4); `FP-112` existe en
   `forense/firmas-pendientes.tsv` pero está `ABIERTA`, no firmada (gatea Hito D 15→16,
   objeto distinto de este acto). Se interpretó como referencia equivocada del propio
   encargo, no como bloqueo: el commit real en `HEAD`/`origin/main` (`a664ea5`,
   `PR #317`/`ACTO RECENSO-DISEÑO-14`) es la base correcta y ya está fusionado, así que
   este acto **continuó** en vez de `PARO`, declarando la discrepancia aquí y en
   `ADR-150` en vez de forzarla en silencio. `origin/main` no se movió más allá durante
   el acto (`git fetch origin` re-verificado antes de cerrar).
3. **`data/raw`.** No aplica — entorno NUBE, sin corpus montado. Declarado, no
   sondeado.
4. **Entorno.** `echo $CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE` → esperado `cloud_default`.
   Sin red de datos ni microdato: declarado, sonda de red saltada conforme al propio
   encargo. **Hallazgo de arranque, no anticipado por el encargo:** el clon era
   **parcial** — `git log --all --oneline | grep -i codex` daba **0** antes de
   `git fetch --unshallow`. El fetch trajo **1152 commits** nuevos y varias ramas
   remotas que no estaban (`origin/claude/fp57-declara-corpus-evidence-q6ewfo`,
   `origin/rescate/reconcilia-puertas-local`, entre otras); tras el fetch,
   `git log --all --oneline | grep -i codex` → **9**. Esto es la raíz técnica del
   hallazgo CODEX (ver abajo) y del mismo defecto que `AMPLIA-MARCO-SATURA` ya había
   sufrido el mismo día sin diagnosticarlo.
5. **Espejo.** Ninguna cifra de esta nota viene del espejo; todo del clon, comando a la
   vista.

## Verificación de existencia (repetida aquí, resumen — bloque completo en el encargo)

- `tools/curador_registro/`: `ls` → 28 entradas.
- `git log --all --oneline | grep -i codex` → 9 (tras el fetch), incluye `PR #164` y
  `PR #244`.
- Las 13 filas `FP-71/85/92/107/108/110/111/113/114/115/116/117/118`: leídas íntegras
  con `awk -F'\t' -v id=FP-XX '$1==id' forense/firmas-pendientes.tsv`, una por una,
  antes de escribir ninguna ejecución.
- `hitoD-R7_2-bbis` §2: `grep -c envipe
  data/curacion-universo/ledger-inspecciones-barrido2.tsv` → 76 (no 44), mismo `sha256`
  `81b72932b406753a` que la Entrada 3 de `registro-recalculo` cita, verificado también
  como el SHA histórico contra el que la ficha corrió (`f3d3f95`).
- Los 4 pares duplicados de `data/universo-puertas-2026-08-14.tsv`: `grep -n` sobre los
  8 nombres exactos que `FP-71` cita — los 8 presentes.

**Archivos examinados en total durante la verificación de existencia:** `tools/curador_registro/`
(28 entradas), `git log --all` (1386 commits tras el fetch), `firmas-pendientes.tsv`
(13 filas FP leídas íntegras), `ledger-inspecciones-barrido2.tsv` (2078 líneas),
`universo-puertas-2026-08-14.tsv` (107 líneas).

## Mapeo de las trece respuestas de mesa a filas FP

Cuatro de las trece traen flecha explícita en el encargo (1, 2/3, 4, 6, 8, 9 — seis
firmas con destino nombrado). Las seis restantes (5, 7, 10, 11, 12, 13) son respuestas
breves sin flecha; el mapeo se derivó leyendo `firmas-pendientes.tsv` completo y
cruzando el **orden de PARTE 1** (que numera sus ejecuciones (5), (7), (10), (11), (12)
— los mismos números de las respuestas de mesa) contra el contenido léxico de cada
respuesta:

| # | Respuesta de mesa (verbatim) | FP | Razonamiento |
|---|---|---|---|
| 1 | CODEX, texto íntegro | — (sucesor `FP-121`) | Explícito en el encargo. |
| 2/3 | «no está descargado…» / «no la hemos encontrado» | `FP-118`, `FP-114` | Explícito. |
| 4 | (GO, sin texto) | `FP-115` | Explícito. |
| 5 | «Si, agreguémosla.» | `FP-116` | PARTE 1 (5) es exactamente "añade el valor... al vocabulario" — "agreguémosla" = el valor nuevo al vocabulario de `diseno-muestral.yaml`. |
| 6 | «Damos de alta TODO…» | `FP-117` | Explícito. |
| 7 | «corregirla. A.» | `FP-85` | PARTE 1 (7) es la enmienda 44→76; "corregirla" = corregir la ficha, "A" = la opción que la propia fila `FP-85` nombra ("se corrige por acto propio"). |
| 8 | «…bajarlos no cuesta nada» | `FP-110` | Explícito. |
| 9 | «B todos!!!» | `FP-111` | Explícito. |
| 10 | «Si a las tres cosas.» | `FP-107`, `FP-108` + fila nueva | PARTE 1 (10) tiene **tres** componentes (D-07, D-08, fila GO nueva) — "las tres cosas" encaja exacto. |
| 11 | «si, se matizan.» | `FP-92` | PARTE 1 (11) es el matiz de baja precisión — "se matizan" (plural, dos celdas) es literal. |
| 12 | «Si, marcados.» | `FP-71` | PARTE 1 (12) es "marca los 4 pares" — "marcados" es literal. |
| 13 | «Si…» | `FP-113` | Dado explícitamente: sigue `ABIERTA`. |

## Ejecuciones (PARTE 1), comando por comando

**(5) `data/diseno-muestral.yaml`.** Vocabulario de estado extendido de tres a cuatro
valores (`NO_APLICA_REGISTRO_ADMINISTRATIVO` añadido en la cabecera). Fila `EDR`:
`estado` cambiado de `SIN_DISEÑO_PUBLICADO` a `NO_APLICA_REGISTRO_ADMINISTRATIVO`, nota
fechada añadida que preserva el texto original de `RECENSO-DISENO-14`/`ADR-149`
íntegro. Verificación: `grep -n "NO_APLICA_REGISTRO_ADMINISTRATIVO" data/diseno-muestral.yaml`
→ 2 coincidencias (cabecera + fila EDR).

**(7) `forense/hitoD-R7_2-bbis-triage-v1_0.md` §2.** Enmienda in-situ fechada
2026-08-24: `44` tachado (`~~44~~`), `76` insertado, texto original de abajo intacto
(no borrado). Cita `FP-85` y el `sha256` `81b72932b406753a`. Verificación:
`grep -c "ENMIENDA fechada" forense/hitoD-R7_2-bbis-triage-v1_0.md` → 1.

**(10) `forense/hitoD-preregistro-v2_0.md`, tabla cerca de línea 313.** Entran `D-07`
(R8.1) y `D-08` (R10.1), redacción derivada de `forense/hitoD-R8_1-especificacion-v1_0.md`
§3, `forense/hitoD-R8_1-veredicto-v1_0.md` §4, `forense/hitoD-R10_1-defecto-spec-v1_0.md`
y `forense/hitoD-R10_1-veredicto-v1_0.md` — leídos antes de escribir, no de memoria.
Fila nueva: "spec sucesora v2.0 de R10.1 — GO de mesa", `FIRMADA`, `ejecutada_en` vacío
(el texto operativo llega en encargo aparte). Verificación: `grep -c "^| \*\*D-0"
forense/hitoD-preregistro-v2_0.md` → 8 (D-01 a D-08).

**(11) `resultado.tsv` de `ESP-OPACA-B-d13ec4fe`.** Localizado derivando de la fila
`FP-92` (cita `forense/hallazgos.md` línea 2026-08-19 y
`forense/notas/2026-08-19-lote-ubuntu-adq-1-cierre.md` §(c)) y confirmando con
`grep -rl "ESP-OPACA-B-d13ec4fe" .` — un único `resultado.tsv` en el árbol:
`data/curacion-registro/expedientes-produccion/t0-89f4c3a49c00c0e1/ESP-OPACA-B-d13ec4fe/resultado.tsv`.
Aritmética CV=EE/estimación reproducida sobre las 4 celdas de la fila `P7_12_7`:
código 1 (De acuerdo) 1.53%, código 2 (Desacuerdo) 3.53% → alta precisión; código 8
(No responde) 0.0013053467/0.0034694317 = **37.62%**, código 9 (No sabe)
0.0012719025/0.0037353188 = **34.05%** → baja precisión (rangos INEGI: alta
[0%-15%), moderada [15%-30%), baja ≥30%). Nota fechada añadida a la columna `reserva`
(único campo de texto libre de la fila); `estado_celda` de las 4 celdas queda sin
cambio — matiz, no re-etiquetado, conforme a lo que `FP-92` misma declara como
perímetro de un acto de producción distinto.

**(12) `data/universo-puertas-2026-08-14.tsv`.** Columna `duplicado_de` añadida al
final (16ª columna; el archivo tenía 15). Los 4 pares que `FP-71` lista: la fila
original de cada par marcada `DUPLICADO -- fila retenida es
<nombre>_adquisicion_PLOTE2, mas completa (bytes+sha256 verificados por P·LOTE-2)`; las
filas `_adquisicion_PLOTE2` (más completas, decisión de mesa) quedan sin marca — son
las retenidas. Ningún índice se movió. Re-verificado que ningún script lee este TSV:
`grep -rl "universo-puertas-2026-08-14" .` → **14** resultados, todos documentales
(`.md`) o el propio `.tsv`/`.git/index` — cero código:

```
./.git/index
./forense/firmas-pendientes.tsv
./forense/hallazgos.md
./forense/notas/2026-08-20-lote-motor2-reverificacion.md
./forense/notas/2026-08-19-fp10-precedencia.md
./forense/notas/2026-08-19-fusion-puertas.md
./forense/notas/2026-08-13-reconcilia-puertas-local-historico.md
./forense/notas/2026-08-14-sanea-mapeo.md
./forense/notas/2026-08-18-adquisicion-material-15-fuentes.md
./forense/encargos/2026-08-18-FUSION-PUERTAS.md
./forense/encargos/2026-08-18-ADQ-15.md
./forense/encargos/2026-08-18-FP10-PRECEDENCIA.md
./canon/estado-programa-v1_10.md
./canon/gobernanza-v1_15.md
```

## Hallazgo CODEX

Línea nueva en `forense/hallazgos.md` (2026-08-24) y enmienda fechada (append, no
reescritura) en `forense/notas/2026-08-24-amplia-marco.md`: la infraestructura CODEX
existe (`tools/curador_registro/`, ramas `codex/*` — `PR #164`, `PR #244`); el
`NO-ENCONTRADO` de `AMPLIA-MARCO-SATURA` fue del término buscado ("codex" contra el
nombre real `curador_registro`) **y**, hallazgo propio de este acto, de un clon
parcial que no traía esas ramas fusionadas hasta `git fetch --unshallow`. El texto
original de `2026-08-24-amplia-marco.md` queda intacto arriba de la enmienda.

## Filas sucesoras nuevas

`FP-119` (`ADQ-DISENO-1`), `FP-120` (`RECENSO-DISENO-2`), `FP-121`
(`MARCO-SATURA-CODEX`), `FP-122` (`PYPDF-REBARRIDO-B`) — las cuatro `FIRMADA`,
`ejecutada_en` vacío, cada una citando en `firmada_en` la respuesta de mesa (1-13) que
la autoriza. Máximo previo verificado por `grep -oE '^FP-[0-9]+' forense/firmas-pendientes.tsv
| sort -t- -k2 -n | tail -1` antes de este acto: `FP-118` — sin huecos, `FP-119`…`FP-122`
únicas.

## Resultado del tablero

`FIRMADA+ejecutada`: `FP-71`, `FP-85`, `FP-92`, `FP-116` (4).
`FIRMADA` sin ejecutar: `FP-107`, `FP-108`, `FP-110`, `FP-111`, `FP-114`, `FP-115`,
`FP-117` (7).
`ABIERTA` sin cambio: `FP-113` (1).
Nuevas `FIRMADA` (sucesoras): `FP-119`, `FP-120`, `FP-121`, `FP-122` (4).

## Suite

`python3 tests/check.py --baseline`, corrida final tras todas las ediciones:

```
════════════════════════════════════════════════════════════════════════
  19 FAIL · 149 WARN
════════════════════════════════════════════════════════════════════════

────────────────────────────────────────────────────────────────────────
  LÍNEA BASE: VERDE — nada nuevo frente a tests/baseline.json (HEAD congelado e24d033ed3c095f1e81c2fbb8248f108e9d3ef65)
  (5 entradas de la línea base ya no aparecen — mejora, no bloquea, no baja la cifra congelada sin --freeze explícito)
────────────────────────────────────────────────────────────────────────
```

El total de `WARN` bajó de 151 a 149 (`T22` reflejando 7 filas `FP-*` cerradas de
`ABIERTA` a `FIRMADA` sobre las 13, neto −2 tras las 4 filas sucesoras nuevas que
también cuentan). `FAIL` sin cambio, `19`. `canon/estado-programa-v1_10.md` recifrado
para converger (`:207`, `:299`, y el conteo de ADR `149→150` en `:27`/`:103`),
verificado por corridas sucesivas hasta punto fijo — self-check de `T16` incluido.

## Perímetro

Archivos tocados, todos dentro de lo declarado por el encargo:

- `forense/firmas-pendientes.tsv`
- `data/diseno-muestral.yaml`
- `forense/hitoD-R7_2-bbis-triage-v1_0.md`
- `forense/hitoD-preregistro-v2_0.md`
- `data/curacion-registro/expedientes-produccion/t0-89f4c3a49c00c0e1/ESP-OPACA-B-d13ec4fe/resultado.tsv`
- `data/universo-puertas-2026-08-14.tsv`
- `forense/notas/2026-08-24-amplia-marco.md` (solo enmienda append)
- `forense/hallazgos.md`
- `canon/gobernanza-v1_15.md` (ADR-150 + recifrado de cabecera)
- `canon/estado-programa-v1_10.md` (recifrado de conteo de ADR y suite, cascada
  mecánica igual que `ADR-147(c)`/`ADR-149(f)`)
- `forense/notas/2026-08-24-sella-d.md` (esta, nueva)
- `forense/encargos/2026-08-24-SELLA-AGO24-D.md` (nueva, `CONSUMIDO`)

No se tocó ningún archivo fuera de esta lista.

## Desviaciones del encargo

1. **Gate falso.** El encargo asumía `SELLA-AGO24-C` fusionado y `FP-112` firmada;
   ninguno existe así. Se continuó sobre el gate real (`a664ea5`), declarado arriba, no
   forzado.
2. **Clon parcial.** El árbol de partida no traía las ramas `codex/*` — corregido con
   `git fetch --unshallow` antes de escribir la verificación de existencia, declarado
   como hallazgo (CODEX), no oculto.
3. **Convergencia de cifras auto-referenciales.** Las líneas de `canon/estado-programa-v1_10.md`
   que declaran el conteo de `FAIL`/`WARN` de la propia suite cambian el conteo real al
   editarse (afectan `T15`/`T16`); se iteró hasta punto fijo (declarado = medido)
   en vez de dejar una cifra aproximada, siguiendo el mismo criterio que `ADR-149(f)`
   ya documentó para este mismo tipo de cascada.

Ninguna desviación requirió escribir fuera del perímetro declarado.
