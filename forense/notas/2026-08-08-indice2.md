# ENCARGO ÍNDICE-2 — parsear las canastas y conectar el índice (arranque 6/ago/2026)

**Contadores movidos por este acto: 0.** Sin justificarlo (instrucción explícita del propio
encargo) — pero se deja dicho el porqué es coherente con su propio perímetro: no descarga, no
escribe `data/manifiesto.yaml`, no decide qué bajar. Los contadores que este acto SÍ re-deriva
(MANIFEST_SIN_MATCH, operables sin bajar) habían cambiado, pero por actos anteriores a éste, no
por éste — ver §0.

Worktree `/home/pc0/wt-indice2-1786050152`, rama `sesion/indice2-1786050152`.

## ARRANQUE

### 1 · REPO

```
$ pwd
/home/pc0/Modelado-Mexicano
$ git log -1 --format="%h %s"
302ac5a Merge origin/main into sesion/cal-conf-faseb-pos4-envipe-paso1
$ git status
On branch sesion/cal-conf-faseb-pos4-envipe-paso1
Untracked files: data/raw, data/secretos.local.yaml
```

El clon base NO estaba en `main` (patrón ya documentado del proyecto — nunca asumir que lo
está). Worktree nuevo creado sobre `origin/main`:

```
$ git fetch origin main
$ git log -1 --format="%h %s" origin/main
3d43c8b Merge pull request #154 from Josanoforo/explora2-1786042858
$ git worktree add ~/wt-indice2-1786050152 origin/main -b sesion/indice2-1786050152
HEAD is now at 3d43c8b Merge pull request #154 from Josanoforo/explora2-1786042858
```

### 2 · SHA

Base declarada en el encargo: `9fd49db`. Real de `origin/main` al momento de arrancar:
**`3d43c8b`** (Merge PR #154, explora2). NO es PARO — el encargo mismo lo anticipa
("Refresca y reporta el real"). El worktree se creó sobre el SHA real, no sobre el declarado.

### 3 · data/raw + descargas_mx

```
$ ln -s /home/pc0/mm-corpus/raw data/raw
```

`descargas_mx` resuelto vía `data/raices.local.yaml` (copiado de un worktree hermano existente,
mecanismo ya documentado del proyecto — gitignorado, per-máquina):

```
ruta absoluta: /mnt/c/Users/PC0/Descargas MX
```

`ls -la` completo (41 archivos) — de ellos, **las 5 canastas de hoy** (mismo minuto, 09:53–09:54
del 6/ago, patrón `DescargaMasiva_682026_*.zip`, distintas de las de 30/jul y de la del 5/ago
17:56 ya usada por DESC-1):

```
-rwxrwxrwx 1 pc0 pc0  605512 Aug  6 09:53 DescargaMasiva_682026_95355.zip
-rwxrwxrwx 1 pc0 pc0  604834 Aug  6 09:54 DescargaMasiva_682026_9540.zip
-rwxrwxrwx 1 pc0 pc0  688618 Aug  6 09:54 DescargaMasiva_682026_95418.zip
-rwxrwxrwx 1 pc0 pc0  602522 Aug  6 09:54 DescargaMasiva_682026_95423.zip
-rwxrwxrwx 1 pc0 pc0  602173 Aug  6 09:54 DescargaMasiva_682026_9548.zip
```

Cada una contiene `DescargaMasivaApp.exe` (850,432 B, idéntico en las 5 — el ejecutable de la
herramienta, no dato), `leeme.txt` (434 B) y `DescargaMasivaOD.xml` (el índice real, tamaño
variable). No aparecen canastas insumo, no hay PARO.

### 4 · ENTORNO

```
$ echo "${CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE:-sin_variable}"
sin_variable
$ ls data/raw/ | head -1
BD_ENCUCI2020_dbf.zip
```

Sonda de red no aplica — este acto no descarga nada (el propio encargo lo declara así).

### 5 · ESPEJO

Ninguna cifra del espejo se usó en este acto.

---

## §0 · Re-derivación de las tres cifras citadas por el encargo, ANTES de citarlas

El encargo cita "hoy: 492 entradas → 410 emparejadas · 82 MANIFEST_SIN_MATCH · 43 operables · 27
en disco · 16 sin bajar" y pide re-derivar con receta propia antes de repetirlas.

```
$ python3 tests/catalogo.py | head -1
SIN_RESOLVER: 4
$ python3 tests/dedup.py | tail -12
CRUCE CONTRA data/manifiesto.yaml (vía tabla de alias)
  entradas de manifiesto: 514
  ids emparejados a alguna fuente canónica: 415
  MANIFEST_SIN_MATCH (id sin fuente canónica que lo explique): 99
  fuentes del catálogo ya registradas: 31 (...)
  OPERABLES ya en disco:      28
  OPERABLES sin bajar:        15
```

**No coinciden con lo citado.** `data/manifiesto.yaml` tiene hoy **514** entradas, no 492
(verificado por dos caminos independientes: `grep -c "^- id:"` y `len(yaml.safe_load(...))`,
ambos dan 514 — la receta es consistente, el manifiesto sí creció). MANIFEST_SIN_MATCH subió de
82 a **99** (+17), OPERABLES sin bajar bajó de 16 a **15** (−1), operables en disco subió de 27 a
**28** (+1). El total de operables (43) no cambió. Esto ocurrió por actos posteriores al 5/ago
que registraron payloads nuevos en el manifiesto (VERIF-3, EXPLORA-2, REPAIR-1 entre otros) — no
por este acto, que aún no ha tocado nada. TAREA C usa las cifras de HOY (99 / 15), no las
citadas por el encargo.

---

## §1 · TAREA A — Parsear las cinco canastas

Verificación de las cifras del plan (declaradas: Tabulados 15,318 · Indicadores 392 · DENUE
486 · Sala de prensa 71 · INV 3) contra el conteo crudo de `<Archivo>` en cada XML:

```
denue            n_raw=970    n_uniq=970    (plan decía 486 — NO COINCIDE, casi el doble)
indicadores      n_raw=568    n_uniq=557    (plan decía 392 — NO COINCIDE, 11 duplicados internos)
tabulados        n_raw=15599  n_uniq=15563  (plan decía 15,318 — NO COINCIDE, 36 duplicados internos)
sala_de_prensa   n_raw=71     n_uniq=70     (plan decía 71 — COINCIDE en crudo, 1 duplicado interno)
inv              n_raw=3      n_uniq=3      (plan decía 3 — COINCIDE)
```

Solo 2 de 5 coinciden. Las cifras del plan son tipo (3) tal como el encargo advertía — no se
heredaron, se verificaron y se reportan las propias (970/557/15563/70/3, post-dedup interno por
canasta = **17,163 filas**).

Identidad de canasta confirmada por contenido de URL, no por nombre de zip (denue → todas
`/masiva/denue/`; indicadores → 561/568 bajo `/masiva/indicadores/` + 4 cemabe + 3 ce; tabulados
→ mezcla `/programas/ce,dutih,ccpv,ena,sit/` + `/investigacion/mcsm/`; sala_de_prensa → todas
`/masiva/saladeprensa/`; inv → todas `/masiva/indicadores/inv/`).

Metadata cruda de cada `<Descarga>` (A.7: el `aut` es un token de sesión, no identidad — se
registra pero no se usa como llave de comparación):

```
denue           aut=de85cb42-...  totalMb=27.48 GB
indicadores     aut=8f31e09a-...  totalMb=706.69 MB
tabulados       aut=74ae2c7b-...  totalMb=14.50 GB
sala_de_prensa  aut=4ad4b180-...  totalMb=18.43 GB
inv             aut=b2f7f677-...  totalMb=1.21 GB
```

Salida: `data/indice-canastas-2026-08-08.tsv`, 17,163 filas + cabecera, columnas
`url · canasta · programa · anio · carpeta · archivo · formato · bytes_declarados ·
en_manifiesto` (orden literal del encargo). `bytes_declarados` queda vacío en las 17,163 filas —
igual que en el índice del 5/ago (verificado: su propia columna `bytes_declarados` también está
vacía en las 7,930 filas) — el XML de canasta nunca trae tamaño por archivo, solo el total
agregado por canasta (`totalMb` de arriba). No se inventa el dato.

`en_manifiesto` (primera pasada, URL exacta contra `url_origen` del manifiesto vivo): **0 / 17,163
"sí"**. Cero — consistente con el hallazgo de TAREA B (§2): `url_origen` en el manifiesto son en
su mayoría URLs de página, no el enlace profundo al zip/csv que trae la canasta.

Cero bytes de microdato descargados — solo se leyeron los 5 XML ya presentes en
`descargas_mx`.

---

## §2 · TAREA B — `tests/indice.py`

Script nuevo, misma disciplina que `catalogo.py`/`dedup.py` (deriva, imprime receta, no escribe
canon). Salida completa de `python3 tests/indice.py`:

```
======================================================================
FUNCIÓN 1 · UNIVERSO UNIDO
======================================================================
filas indice-2026-08-05:        7930
filas indice-canastas-2026-08-08: 17163
filas totales (con repetición):  25093

URLs únicas en la unión:         25093
URLs que aparecen en más de un índice/canasta: 0

por canasta (raw, sin deduplicar entre sí):
   15563  tabulados
    7930  descarga_masiva_5ago
     970  denue
     557  indicadores
      70  sala_de_prensa
       3  inv

programas distintos (incluye '' = fuera de /programas/): 203
URLs con programa vacío (canastas masiva/, investigacion/, etc.): 2900

======================================================================
FUNCIÓN 2 · en_manifiesto REFRESCADO (manifiesto vivo, no el TSV congelado)
======================================================================
entradas en manifiesto vivo: 514  (url_origen únicos: 467, archivo únicos: 509)

CIFRA 1 — emparejamiento por URL EXACTA: 300 / 25093
CIFRA 2 — emparejamiento por NOMBRE DE ARCHIVO (no se colapsa con la 1): 60 / 25093
  de esos, cuántos NO tenían ya match por URL exacta (ganancia atribuible solo al nombre, el
  emparejamiento que el proyecto ya pagó como defecto): 4
  ambas cifras se reportan aparte; ninguna reemplaza a la otra.

======================================================================
FUNCIÓN 3 · CRUCE CONTRA EL CATÁLOGO UNIFICADO
======================================================================
regenerando data/catalogo_derivado.json (corriendo tests/catalogo.py)...
  ok — catalogo.py corrió limpio, catalogo_derivado.json regenerado.

fuentes canónicas con slug_portal verificado (data/inventarios/alias-fuentes.yaml): 34 de 128

VERIFICACIÓN DE RECETA contra caso conocido:
  mapa-fuentes-2026-08-06.tsv, suma de n_urls_portal:            3754
  re-derivado aquí (URLs del índice 5/ago cuyo programa cae en un slug_portal): 3754
  COINCIDEN. La receta es consistente — la cifra de mesa no estaba mal contada.
  Respuesta a la pregunta del encargo: el mapa cubre menos de la mitad porque solo 34/128
  fuentes canónicas tienen slug_portal asignado (ENCARGO MAP-1 solo lo verificó donde el slug
  coincidía Y el contenido corroboraba — no por falta de aritmética), no porque la suma esté mal.

URLs del índice unido bajo una fuente canónica del catálogo: 7820 / 25093
URLs del índice unido que ninguna fuente canónica explica:   17273 / 25093

programas/canastas del índice que ninguna fuente del catálogo explica (170 claves distintas,
top 25 por volumen de URLs):
    2069  ce
    1296  (sin programa; canasta=tabulados)
     970  (sin programa; canasta=denue)
     945  dutih
     643  ena
     613  sit
     558  justiciapenal
     509  modutih
     481  meitef
     476  cnije
     462  pibact
     451  esidet
     431  cngspspe
     420  (sin programa; canasta=descarga_masiva_5ago)
     350  cnpje
     330  ptf
     288  enpecyt
     276  si
     270  tod
     256  pibgob
     234  ca
     207  cnijf
     197  cleu
     189  cae
     184  mcs
```

**Solapamiento entre índices: 0**, no el "casi seguro" que el encargo sugería como hipótesis —
verificado (7,930 + 17,163 = 25,093 = únicas exactas). Explicación: el índice del 5/ago es
microdatos de encuesta bajo `/programas/X/AAAA/microdatos|doc/`; las 5 canastas de hoy son
`/masiva/` (denue, indicadores, sala de prensa, inv) o tabulados estadísticos bajo
`/programas/X/AAAA/tabulados/` — estructuras de ruta disjuntas incluso cuando comparten programa
(p.ej. `ccpv` aparece en ambos índices pero apunta a archivos distintos: microdato en uno,
tabulado en otro). No es una casualidad de muestreo, es que las canastas de hoy nunca fueron
"la misma oferta, otra vez" — son tipos de contenido distintos del portal.

**Nota sobre la Función 3**: el catálogo (128 fuentes canónicas, todas provenientes de los 11
inventarios de encuestas/censos/registros) no tiene NINGUNA entrada para DENUE, INV, "Tabulados"
o "Sala de prensa" como tipo — ni como canónico ni como alias (`grep` sobre
`alias-fuentes.yaml` confirma 0 coincidencias para los 4 términos). No es un vacío accidental de
cobertura, es que el catálogo modela programas de encuesta/censo, y estas 4 canastas son otra
categoría de oferta del portal (directorio de negocios, indicadores, boletines de prensa,
capas geoestadísticas) que ningún inventario documentó nunca. De las 17,273 URLs "no cubiertas",
2,900 no tienen programa (denue/tabulados-sin-programa/sala de prensa/inv/descarga_masiva_5ago
sin `/programas/`) y son estructuralmente inexplicables por este catálogo; el resto (14,373) sí
cuelgan de `/programas/` pero ese programa no tiene `slug_portal` verificado — mezcla de fuentes
genuinamente ausentes del catálogo (ver TAREA C) y fuentes que sí están en el catálogo pero cuyo
slug nunca se verificó contra estas canastas nuevas (verificación pendiente, no defecto).

---

## §3 · TAREA C — Los tres huecos

### C.1 — Los 99 (no 82) MANIFEST_SIN_MATCH, clasificados

Re-derivados en §0: **99**, no 82. Clasificación por familia (script ad hoc, sustitución amplia
de subcadena contra los tokens de `alias-fuentes.yaml` — diagnóstico, no cambia `dedup.py`):

```
NO_DATO (nota/artefacto administrativo, no es payload de dato):                    3
  encargo, hito, nota_metodologica_rotulo_pareada

LEGÍTIMO SIN FUENTE ÚNICA POSIBLE (canastas/XML de Descarga Masiva registradas como
tal, no como un programa — no pueden tener una fuente canónica única por diseño):    6
  descargamasiva_3072026_105543, _10560, _105617, _105625, _10567,
  descargamasiva_382026_131650_xml

FALLO DE ALIAS (la fuente SÍ existe en el catálogo canónico; el id no empieza con su
acrónimo así que el heurístico de prefijo de dedup.py no la conecta):                23
  16 × ENSANUT 2024 (ids empiezan por unidad de observación — nse_/adultos_/hogar_/
      integrantes_/menores_/utilizadores_ — no por "ensanut"; ENSANUT sí es canónico)
  5 × banxico_sie_* (canónico SIE existe, id empieza por "banxico" no "sie")
  1 × r7_3_pub_beneficiarios_bienestar_csv (canónico PUB existe, id empieza por "r7_3")
  1 × datosgobmx_ckan_enaproce_enestyc_cero (contiene ENAPROCE y ENESTYC, ambos
      canónicos — pero el id no empieza por ninguno)

LEGÍTIMO, SIN FUENTE EN EL CATÁLOGO (ningún canónico ni alias aparece en el id en
absoluto — la fuente no está en los 11 inventarios, punto):                          67
  46 × ENGASTO (dos convenciones de id EN PARALELO: engasto_2012_* [25] y
      engasto2012_* [18, sin guión bajo entre nombre y año] + engasto_2013_* [3] —
      probable registro duplicado del mismo contenido bajo dos esquemas de id
      distintos, de sesiones distintas; no se resuelve aquí, se nombra. ENGASTO/
      ENASIC ya estaba señalado como hueco de catálogo por MAP-2, 2026-08-06)
   3 × ENASIC 2022 (enasic2022_bd_csv_zip, enasic2022_fd_xlsx, enasic_2022_...)
   2 × ENBIARE 2021
   2 × MMSI 2016 (Movilidad Social Intergeneracional — cuestionario + manual)
   9 × banxico_codi_* / banxico_informe_anual_idmf_* / codi_org_mx_estadisticas_landing
      (CoDi e Informe Anual de Inclusión Financiera de Banxico — sin fuente en catálogo)
   3 × r2_1_ecco_reporte_se_2023, r8_1_contraloria_social_2019_2025_csv,
      r7_4_r7_5_acled_hdx_demonstration_events (ECCO/Contraloría Social/ACLED-HDX —
      cada uno SÍ es canónico en alias-fuentes.yaml pero sin slug_portal ni alias corto
      que el barrido de substring detecte automáticamente vía el id con prefijo "r*_*")

TOTAL: 3 + 6 + 23 + 67 = 99 ✓
```

### C.2 — Los 15 (no 16) operables sin bajar, con cobertura del índice

Re-derivado en §0: **15**, no 16 (una se bajó entre el 5/ago y hoy). Para cada uno, cobertura en
la unión de los dos índices (búsqueda por `slug_portal` cuando existe, si no por palabra clave
del nombre, verificada contra muestra de URLs reales — no solo por conteo):

```
SÍ, el índice ya tiene sus URLs:
  CNGF     28 URLs (slug_portal=cngf verificado, índice 5/ago)
  CNGMD    219 URLs (slug_portal=cngmd, 5/ago) + 84 adicionales en canasta de hoy
  EIC      131 URLs bajo /programas/intercensal/2015/ (5/ago) — "eic" no tiene
           slug_portal en la tabla de alias pero el índice sí trae el programa real
           bajo su slug verdadero, "intercensal"
  ENCUESTA NACIONAL DE BIENESTAR (BIARE)  234 URLs bajo /investigacion/bienestar/
           (5/ago) + 192 en canasta tabulados de hoy
  ESTADÍSTICAS DE NATALIDAD  40 URLs bajo /programas/natalidad/ (5/ago) + 6 en
           canasta indicadores de hoy

PARCIAL — el índice tiene URLs pero mezcladas con un programa hermano, sin
desambiguar:
  ECOVID-ML  20 hits en 5/ago y 31 en canastas de hoy, pero la búsqueda por "ecovid"
           mezcla ECOVID-ED (educación) y ECOVID-ML (mercado laboral) — no se puede
           dar un "sí" limpio sin separar por sub-slug primero

NO, el índice no lo tiene:
  ESTADÍSTICA EDUCATIVA (Formato 911/SIGED)  la única coincidencia de palabra clave
           son tabulados del Censo sobre características educativas de la población —
           fuente distinta, falso positivo descartado
  ENCUESTA NACIONAL PARA EL SISTEMA (DE CUIDADOS)  sin coincidencia real (la palabra
           clave "sistema" solo produce falsos positivos de otros programas)
  REGISTROS ADMINISTRATIVOS DE ESTADÍSTICAS VITALES Y NUPCIALIDAD  sin coincidencia
  GLOBAL FINDEX DATABASE  fuente externa (Banco Mundial) — el portal de Descarga
           Masiva es exclusivamente INEGI, esta fuente no puede aparecer en NINGUNA
           canasta de este mecanismo, sin importar cuál se genere
  SAEH  fuente externa (DGIS/Secretaría de Salud) — mismo argumento
  ACS   fuente externa (US Census Bureau) — mismo argumento
  CPS   fuente externa (US Census Bureau) — mismo argumento
  CLUES fuente externa (DGIS/Secretaría de Salud) — mismo argumento; coincide con
           el hallazgo ya registrado de B-4b-alpha ("CLUES unreachable")
  CONEVAL  fuente externa (CONEVAL, no INEGI) — mismo argumento
```

5 de 15 tienen cola de descarga lista y verificada hoy mismo (403 URLs entre las 5); 1 necesita
desambiguación previa; 9 son estructuralmente inalcanzables por este mecanismo — 6 de esas 9
porque la fuente ni siquiera es de INEGI, así que ninguna canasta futura del mismo portal las
va a traer.

### C.3 — Los 4 SIN_RESOLVER de `catalogo.py`

```
inventario_fuentes_clase-fuente-mexico.md:215
  sugerencia(no-usada-como-identidad)='ENCUESTA NACIONAL DE CALIDAD D'
  titulo='Encuesta Nacional de Calidad de la Atención del Servicio de Salud (ENCAL, IMSS)'

inventario_fuentes_cultura_valores_opinion_mexico.md:234
  sugerencia(no-usada-como-identidad)='ENCUESTA NACIONAL DE CALIDAD D'
  titulo='Encuesta Nacional de Calidad de la Ciudadanía'

inventario_fuentes_trabajo_ingreso_formalidad_mexico.md:36
  sugerencia(no-usada-como-identidad)='REGISTROS ADMINISTRATIVOS DE A'
  titulo='Registros administrativos de aseguramiento — IMSS'

inventario_fuentes_trabajo_ingreso_formalidad_mexico.md:48
  sugerencia(no-usada-como-identidad)='REGISTROS ADMINISTRATIVOS DE A'
  titulo='Registros administrativos de aseguramiento — ISSSTE **[no verificado]**'
```

Mismas 4 que `alias-fuentes.yaml` ya documenta como dejadas fuera a propósito (dos pares
ambiguos truncados a 30 caracteres) — sin cambios desde MAP-1, confirmado hoy, no repetido a
ciegas.

---

## Criterio de éxito del encargo

El comando que responde "¿cuántas puertas conocemos, cuántas están bajadas, y cuáles de las que
faltan gatean algo que nos importa?" es `python3 tests/indice.py` (universo + en_manifiesto +
cruce contra catálogo) compuesto con `python3 tests/dedup.py` (operables/en_disco/sin_bajar) —
ambos derivan, ninguno se teclea. Hoy: 43 puertas operables conocidas, 28 bajadas, 15 sin bajar
(5 con cola lista en el índice, 1 por desambiguar, 9 inalcanzables por este mecanismo — 6 de
ellas por ser de fuera de INEGI).

---

## Suite ×3, antes del push

`python3 tests/check.py --baseline` corrido tres veces: (1) estado normal, con corpus enlazado;
(2) con `data/raw` desenlazada (`mv data/raw data/raw.bak` antes de correr, restaurada después);
(3) con `data/raices.local.yaml` y `data/secretos.local.yaml` retirados (la combinación que
atrapó el T03 de PR #154). **Las tres corridas dan salida byte a byte idéntica** (`diff` entre
los tres logs, exit 0 en los dos pares) — 21 FAIL · 95 WARN, T03 en 29 (no sube, no es el
landmine de PR #154 repitiéndose aquí). Salida cruda de la corrida 1 (representativa de las 3):

```
════════════════════════════════════════════════════════════════════════
  VERIFICACIÓN DEL CORPUS
════════════════════════════════════════════════════════════════════════
  [ ok ]  T01 fuente única de verdad
  [FAIL]  T02 duplicados nombre/contenido  (1 fail)
  [warn]  T03 referencias colgantes  (29 warn)
  [ ok ]  T04 ADR-33 diagonal en ENTONCES
  [FAIL]  T05 ADR-32.c constructos en glosario  (5 fail)
  [FAIL]  T06 consistencia numérica  (2 fail)
  [FAIL]  T07 vocabulario de tiers  (1 fail)
  [FAIL]  T08 mapa de evidencia por report  (1 fail)
  [FAIL]  T09 marco (c) usado como causa  (8 fail)
  [warn]  T10 diáspora (b) sin marcar  (65 warn)
  [FAIL]  T11 afirmaciones de estado absolutas  (1 fail)
  [ ok ]  T12 conteos del motor
  [warn]  T13 cabecera de versión ADR-36  (1 warn)
  [ ok ]  T14 T-INVENTARIO
  [ ok ]  T15 T-ADR-COUNT
  [ ok ]  T17 T-FICHAS-COUNT
  [ ok ]  T18 T-PASO2-EJECUCION
  [ ok ]  T19a cabecera cruzada estado→modelo
  [ ok ]  T19b contador 14 cruzado (modelo)
  [ ok ]  T19c portada derivada (README)
  [ ok ]  T20 T-CASCADA-MARCADA
  [FAIL]  T16 T-SUITE-SELF-CHECK  (2 fail)

════════════════════════════════════════════════════════════════════════
  21 FAIL · 95 WARN
════════════════════════════════════════════════════════════════════════

────────────────────────────────────────────────────────────────────────
  LÍNEA BASE: ROJO — 3 entradas nuevas frente a tests/baseline.json (HEAD congelado 837d5fe56d523615259efb77c17c84fee86ac684)
  · T02: nombre normalizado colisiona: forense/notas/2026-08-08-indice2.md · forense/encargos/2026-08-08-indice-2.md
  · T16: canon/estado-programa-v1_10.md: declara 18 FAIL · 95 WARN vigente; la corrida real da 19 FAIL · 95 WARN
  · T16: canon/gobernanza-v1_15.md: declara 18 FAIL · 95 WARN vigente; la corrida real da 19 FAIL · 95 WARN
────────────────────────────────────────────────────────────────────────
```

**Las 3 entradas nuevas frente a `tests/baseline.json` (18→19 FAIL) las causa este acto, y son
irreductibles dentro de su propio perímetro.** El propio encargo especifica, verbatim, los dos
nombres de archivo del perímetro de escritura: `forense/notas/2026-08-08-indice2.md` (sin guión
antes del 2) y `forense/encargos/2026-08-08-indice-2.md` (con guión antes del 2). `T02` normaliza
nombres quitando todo carácter no alfanumérico (`tests/check.py:100-102`) — `indice2` e
`indice-2` colisionan bajo esa normalización aunque son nombres distintos y ninguno duplica al
otro en contenido. Renombrar cualquiera de los dos violaría el perímetro explícito del encargo
("si te encuentras escribiendo fuera de esta lista, PARA"); corregir `T02` o los dos documentos
`canon/*` que declaran "18 FAIL vigente" está fuera del perímetro de escritura de este acto
(`canon/**` explícitamente prohibido). Se reporta, no se arregla — mismo criterio que el propio
encargo pide para el CI caído.

No hay ninguna otra entrada nueva: T03 (el landmine de PR #154, gitignorados de config
retirados) no sube en ninguna de las 3 corridas, y `data/raw` desenlazada tampoco cambia nada —
ninguno de los checks de esta suite depende de su contenido.
