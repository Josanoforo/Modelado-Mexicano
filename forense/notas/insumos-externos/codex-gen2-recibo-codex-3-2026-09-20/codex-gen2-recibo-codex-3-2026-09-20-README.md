# codex/gen2-recibo-codex-3 — insumo externo (F-C, GEN2-LIMPIEZA-RAMAS-LOCALES-4)

4 commits propios, ninguno trae `corrida0/CALC-*` sellado (confirmado). Contenido no está
íntegro en `origin/main` (recibo/adjuntos de GEN2-RECIBO-CODEX-3, cambios a `relevo_usos.py`
y varias tablas de `data/corrida0/` con valores distintos a los de main). Se archiva el diff
completo contra `origin/main` antes de borrar la rama, por regla F-C.
