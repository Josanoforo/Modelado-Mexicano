**ADR-260924-GEN2-TUBERIA-TABLERO-EN-CANAL-1-3dd4-01** (raíz de acto: `3dd4` = 4 hex del commit de 0-bis `3dd47a4f`, archivo de la ADENDA-1; no se renumera), `ACTO GEN2-TUBERIA-TABLERO-EN-CANAL-1 · ADENDA-1 · el canal vive`, 24/sep/2026, NUBE (hook: `ENTORNO-DERIVADO = NUBE`, corpus montado=NO examinados=0). Base `origin/main = 8358b891` (= SHA de redacción), 0 detrás. Rama `claude/new-session-e85tdt` (la fijada por la plataforma). MODO AUTÓNOMO (cláusula v1.0). **Archivo.** El encargo recibido es una reescritura (24/sep 17:00) de uno ya ejecutado y CONSUMIDO por PR #1117 (`ADR-…-3726-01`); se archiva como `forense/encargos/2026-09-24-GEN2-TUBERIA-TABLERO-EN-CANAL-1-ADENDA-1.md` con su `.cuerpo.sha256` (`7ac99931…`). **Incidente declarado:** el primer 0-bis de esta sesión (`35c7111d`) sobrescribió el encargo sellado y su sidecar; se revirtió con `git revert` (`c2f169ac`, sin forzar, sin reescribir historia) y `verifica_sidecars.py` vuelve a dar `CASA` para el sello original.

**Diagnóstico del canal (premisa de dirección corregida, EJECUTADO por la API de Actions).** El paso de derivados **sí** corre en cada push a main: el `if` nunca dependió de un asiento nuevo. Run `36068464848` (push de #1120): job `guardias` `cancelled` por `timeout-minutes: 10` a mitad de `registro --escribe --lote` (61 CALC). Run `36066873728` (push de #1121): escribió las vistas y **GUARDA-TAMANO** falló: `resultados.tsv` 92 MB > 50 MB. Reproducido aquí con el mismo lote (`lote_desde_asientos.py --incluir-pendientes`): `resultados.tsv` 96 768 436 bytes, 71 301 filas; la columna `valor` pesa 65 MB porque CALC pendientes del lote (CALC-ENOE-PERSISTENCIA-0001 11 MB, CALC-ENDIREH-PISOS-2011-MODULOS-0001 8 MB, -2016-RESTANTES 6 MB, CALC-ENOE-PISOS-0003 6 MB, …) guardan listas enteras en una celda. `usos.tsv` re-derivado: 231 filas, byte-idéntico salvo cabecera; `status` tras derivar: `N_resultados_gen2_adoptados_activos=72`, `N_resultados_pendientes=211`. **La vista de usos no estaba vieja**: el 72 es lo que deriva el árbol; el «> 72» que esperaba dirección no sale del canal.

**Hecho en este acto.** (P0) `guardias` `timeout-minutes` 10→30; el paso de derivados corre también en `workflow_dispatch` sobre main (receta de mesa: Actions → «Verificación del corpus» → Run workflow → main). `usos.tsv` ya estaba en el `git add` (#1050). (P2) `--actualiza` además se niega con árbol sucio fuera de los derivados que el propio canal escribe antes del tablero. (P4) test de determinismo del render + prueba de suciedad + rótulo de P5 en `tests/test_tablero_programa.py` (16/16, 0.27 s). (P5) la línea GEN2 del bloque se rotula «EN ÁRBOL», añade adoptados activos y pendientes de adopción, y lista las vistas del árbol distintas de HEAD. (P1, P3, T0) ya en main por #1117.

**No hecho — PARO parcial §7(b).** Bajar `resultados.tsv` de 50 MB exige cambiar `registro` (cómo se persiste un `valor` lista) o la guarda de tamaño: ambos vedados. La cláusula de autonomía (7) pide arreglar «otro archivo > 50 MB», pero §7(b) es PARO de lista cerrada y manda (D-19). P6 no se puede alcanzar hasta que otro acto resuelva el tamaño: NC `…-3dd4-01`. Fragmento L0 `canon/L0/ADR-260924-GEN2-TUBERIA-TABLERO-EN-CANAL-1-3dd4-01.md`.
