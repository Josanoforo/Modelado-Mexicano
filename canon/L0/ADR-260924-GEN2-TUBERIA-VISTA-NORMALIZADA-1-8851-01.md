# ADR-260924-GEN2-TUBERIA-VISTA-NORMALIZADA-1-8851-01

24/sep/2026 · `ACTO GEN2-TUBERIA-VISTA-NORMALIZADA-1`. P1 completo: confirmado por comando el `GH001` real (`run 36034606289`, `resultados.tsv` re-derivado a 70 609 filas / 155.02 MB, sobre el límite de 100 MB de GitHub). Verificación de constancia por `corrida_id` (D-15) rompe la premisa `[SUPUESTO]` del encargo: `tolerancia`, `funciones_dependencia` y `fuente_replay` sí son constantes por corrida (0/313 no-constantes); `camino_linaje` **no** lo es (262/313 corridas con valor no constante — varía por RESULT, no por corrida). Proyección real: normalizar solo los tres campos constantes baja el archivo re-derivado a ≈69.75 MB — destraba `GH001` (bajo 100 MB) pero no cumple el umbral de 50 MB que la firma de mesa de §2 compromete textualmente. PARO antes de P2 (D-19: toca una firma de mesa) — no se ajusta el umbral sin que mesa lo diga. Tres opciones para mesa, con recomendación (a): aceptar el umbral de facto (~70 MB) porque el objetivo real (destrabar el canal) se cumple igual. Cero código tocado, cero campos movidos. Suite `--rapido` VERDE, 0 FAIL.
