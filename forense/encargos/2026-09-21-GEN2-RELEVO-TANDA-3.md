ENCARGO · ACTO GEN2-RELEVO-TANDA-3 · LA FIRMA DE MESA SE VUELVE UN CANAL DE RELEVO — PORQUE NI EL MARCO SELLADO NI LAS SPECS SELLADAS PUEDEN CARGAR UN PIN — Y CON ÉL SALEN DE LEGACY LAS LECTURAS QUE YA SE RE-HICIERON
ENTORNO: NUBE — el hook de arranque imprime `ENTORNO-DERIVADO`; si dice `CAJA` puedes correrlo igual y lo dices.
CABECERA · SHA de redacción `fa43ec36`; re-deriva al abrir · una sola sesión, rama propia · MODELO: Opus (toca `tools/corrida0.py` y decide qué lectura cuenta como GEN2) · MODO: ABIERTO, con una regla rígida: la de §2-4.1 · CONTADOR: `cuenta_gen2` NO-APLICA. Mueve `dependencias_numericas_legacy_activas` (hoy 173) y `N_resultados_gen2_adoptados_activos` (hoy 57); reporta los dos derivados, antes y después, con worktree. La adopción la ejecuta el merge de mesa de este PR (E.2). `N_corridas_selladas` no debe moverse.
1 · OBJETIVO
El contador de legacy mide qué mediciones de GEN1 ya se re-hicieron en GEN2 con trazabilidad. Hoy subcuenta: hay lecturas re-hechas que no pueden declararse relevadas porque no existe dónde escribir el pin — el consumidor (`marco-M-sorteado-v1_3.tsv`) está sellado, y las specs de los CALC también. El objetivo es crear ese lugar, gobernado por firma de mesa y por la regla de 4.1, y usarlo para las lecturas que mesa nombra abajo. «Hecho» significa: existe un canal de pines firmados, por llave lógica (nunca por `RES-####`); `relevo_usos.py` y `status` lo leen; los pines de §2 están escritos; un test impide que por ahí entre un número GEN1; y el contador baja en exactamente las lecturas pineadas, ni una más.
2 · FIRMAS DE MESA
Ya dadas (verbatim; no se piden otra vez):
4.1 (21/sep): «Una lectura sale de legacy solo por una de dos vías: (i) su cifra la produce código GEN2 desde un insumo crudo con hash —microdato o capturas selladas—; o (ii) es la lectura de una conducta del motor que ya tiene procedencia GEN2, y entonces el pin cita el RESULT de esa conducta, no una foto. Ingerir un número GEN1 como insumo, con hash o sin él, no cuenta nunca. El contador muestra las clases sin fundirlas.» — confirmada: «confirmo 4.1 y 4.8». 4.8 (21/sep): «El pin de RES-0047 y RES-0049 es `CALC-ENIF-0001` […] entre dos candidatos con el mismo valor, manda el que está más cerca del dato crudo.» FP-391 (20/sep): «Se conserva el estimando sobre personas (U4). RES-0028 se aparea con el complemento sobre personas, no con el de delitos.» Propósito del contador (20/sep): «qué mediciones de Gen1 se hicieron y cuáles se re-hicieron en Gen2 pero ahora con la trazabilidad que nos faltaba.»
Propuesta por dirección — los pines, por nombre, como exige el inciso (iii) de la ADENDA del 15/sep para insumos del marcador. Tu lanzamiento con este archivo es el sello; sin él, P3 escribe el canal vacío y P4 no corre:
«Se relevan, por la vía (i), los 14 árbitros R del marco M, cada uno a su `RESULT-R-<celda>-PUNTO` de la corrida `CALC-R-<celda>` vigente: CIV-M-01, 02, 04, 10, 12, 13 · DIN-M-01 · FAM-M-01, 05, 06, 07 · TRA-M-02, 03, 07. Se relevan, por la vía (ii), 13 lecturas M, cada una al RESULT de la conducta del motor que consulta: las seis CIV-M → `RESULT-ENVIPE-DEN-P-C2-U4` · FAM-M-01 → `RESULT-DFVEJEZ-A-P-RECIBE` · FAM-M-05, 06, 07 → `RESULT-B-ENIGH-2022-P` · TRA-M-02, 03, 07 → `RESULT-ENCIG-MOR-A-P-SOL1`. `DIN-M-01:M` NO se releva: consulta `tiene_ahorros`, que sigue GEN1 (ENNViH). Las lecturas L y AGREGADO no entran en esta tanda.»
3 · LO QUE DIRECCIÓN SABE (contra `fa43ec36`, 21/sep)

* `[LEÍDO]` `tools/relevo_usos.py:24-50`: cuatro canales, los cuatro leen la spec del CALC (`parametros.adopcion*.consumidores`, `resultados[].unidad`, `etiquetas.demanda_que_releva`) y casan por el token literal `RES-####`. No existe archivo externo de pines (`git ls-files | grep -iE "relevo.*mapa|pines"` → 0).
* `[LEÍDO]` Informe de #928 §2: causas con conteo — `SIN-OFERTA-QUE-VER` 82 · `CALC-SIN-PIN` 64 · `CANDIDATO-POR-CLAVE…` 13 · `VEREDICTO-SELLADO-OBEDECIDO` 8 · `PIN-A-RES-AJENO` 6 (los seis `CALC-R-CIV-*`, corridos +2 uniforme). `RES-####` y `CORR-####` son posicionales (`tools/corrida0.py:702, :855`).
* `[LEÍDO]` `tools/corrida0.py:2836-2867`: `generacion_leida` de un uso se deriva de `corrida0_generacion` escrito en el consumidor; `:396-443` construye los usos del marco desde `MARCO_VIGENTE` (`:115`), que no puede llevar esa marca. `:4397`: el contador suma usos activos con `generacion_leida == LEGACY-GEN1`.
* `[EJECUTADO]` Los 14 `CALC-R-<celda>` vigentes (`CIV-*` sin sufijo, `DIN-M-01-v4`, los demás `-v3`): todos `SELLADA · cuenta_gen2 = SI · REPRODUCE`, cada uno con su `RESULT-R-<celda>-PUNTO`. `[LEÍDO]` en #928 §1.2: delta contra el valor GEN1 = `0.0` en 14 de 14, y los medidores leen el microdato de cero.
* `[EJECUTADO]` Los 14 valores M son cinco constantes de `milpa/tramite.yaml`: `:636` (GEN2, `RESULT-ENVIPE-DEN-P-C2-U4`) · `:856` (GEN2, `RESULT-DFVEJEZ-A-P-RECIBE`) · `:910` (GEN2, `RESULT-B-ENIGH-2022-P`) · `:62` (GEN2, `RESULT-ENCIG-MOR-A-P-SOL1`) · `:661` `tiene_ahorros = 0.174804`, sin marca GEN2. `CALC-TRIADA-0001` los toma de un snapshot (`snapshot-M-triada-v1_0.json`): eso es ingestión, y por 4.1 no cuenta.
* `[LEÍDO]` en #914: escribir el pin de RES-0028 en `tramite.yaml:637` hizo que `status` PARARA porque un input del CALC derivado (`IN-ENVIPE-0001-SELLO-SHA256`) cae en `FUNCION-INDETERMINADA` (`corrida0.py:3449-3470` infiere función por tokens del id; «SELLO» no es ninguno). La línea quedó intacta.
* `[EJECUTADO]` RES-0047/0049: `veredicto = CONFLICTO-ENTRE-CANALES` (C1 de `CALC-ENIF-0001` vs. C2 de `CALC-HORIZONTE-VIA-DERIVADOS-0001-v1_1`); `tramite.yaml:1305, :1352` ya citan los RESULT de `CALC-ENIF-0001`.
* `[REPORTADO]` por la mesa TUBERÍA (transfer del 20/sep): su caso sintético de rotación dio que la única forma de cita que sobrevive a una rotación de versión es la llave lógica sin ruta ni versión, p. ej. `marco-M::CIV-M-01::R`; 93 de 210 consumidores traen hoy ruta con versión.
* `[SUPUESTO]` que `tests/test_corrida0_oro.py` (#922) compara `status` byte a byte. Si es cierto: este acto mueve contadores a propósito, así que el oro se regenera al final, declarado, en commit propio.
* `[SUPUESTO]` que NC-0217/NC-0319 se ejecutan mecánicamente con la firma F-2 del 15/sep y el criterio de grano que cerró NC-0214. No leí F-2 verbatim: léela en `decisiones.tsv` antes de tocar nada.

4 · YA HECHO / YA DECIDIDO
TANDA-1 (#905) casó estimandos; TANDA-2 (#914) adoptó 11 por la vía del consumidor `tramite.yaml`; RECONCILIA-1 (#928) midió la ceguera y no escribió pines a propósito. Nadie ha creado un canal externo. TUBERÍA está diseñando `RES`/`CORR` con llave lógica: este acto no rediseña esos ids ni los toca — crea un canal que no los usa.
5 · PIEZAS
P1 · El canal de firma. Un archivo versionado de pines de mesa (propongo `data/corrida0/pines-de-mesa.tsv`), una fila por lectura, con: `llave_logica` (sin ruta, sin versión, sin `RES`: `marco-M::<celda>::<campo>` o `tramite::<regla>::<conducta>`) · `result_gen2` · `calc_gen2` · `via` (`i-CRUDO` | `ii-CONDUCTA-GEN2`) · `firma` (archivo y fecha) · `nota`. `relevo_usos.py` lo lee como canal de mayor precedencia —resuelve también los `CONFLICTO-ENTRE-CANALES`— y `corrida0.py` deriva de él la `generacion_leida` de los usos cuyo consumidor no puede llevar marca. La función que traduce `consumidor` ↔ `llave_logica` vive en un solo sitio, con test de ida y vuelta sobre las 210 filas de hoy. P2 · Las guardas de 4.1, en código — esto es lo rígido. Un pin se rechaza, con mensaje que diga por qué, si: (a) su CALC no está `SELLADA`, `cuenta_gen2 = SI` y con replay `REPRODUCE`; (b) `via = i-CRUDO` y el CALC no tiene al menos un input `origen: manifiesto` o de capturas selladas; (c) `via = ii-CONDUCTA-GEN2` y la conducta citada no lleva hoy `corrida0_generacion: GEN2` en `tramite.yaml`, o el RESULT pineado no es el de esa conducta; (d) el RESULT pineado viene de un snapshot o de la ingestión de otro resultado (`RESULT-TRIADA-*-M` es el caso testigo: debe fallar). Tests por mutación: un caso que dispare cada rechazo, más el caso real `DIN-M-01:M → tiene_ahorros`, que debe ser rechazado. P3 · Los pines de §2. Escritos verbatim de la firma. Por cada uno, antes de escribirlo: valor GEN2 vs. valor legacy del slot por script, y estimando casado por texto en cinco dimensiones (evento, universo, unidad, ola, denominador) — para los 14 R contra la spec de su `CALC-R`; para los 13 M, que la conducta pineada es la que `emitir_binaria` consulta para esa celda (`corridas-M/*.json`, campo `cita_p`). Un pin que no pase no se escribe y no tumba a los demás. P4 · Los dos casos de `tramite.yaml`. RES-0047/0049: pin de mesa a `CALC-ENIF-0001` → la vista deja de reportar conflicto; NC-0244 cierra. RES-0028: diagnostica `FUNCION-INDETERMINADA` en `corrida0.py:3449-3470` y propone el arreglo mínimo. Si el arreglo es añadir un token y reclasifica otros inputs del árbol, cuenta cuántos y cuáles antes de aplicarlo; si ningún contador ajeno se mueve, aplícalo y escribe el pin (`RESULT-ENVIPE-RES0028-Q-C2-U4`, unidad persona, IC heredado del padre, dicho en el comentario); si alguno se mueve, PARA esa pieza y pregunta. P5 · F-2. NC-0217 y NC-0319, según la firma del 15/sep leída verbatim. Si su condición se cumple mecánicamente, ejecuta y cierra; si exige juicio, tabla para mesa. P6 · Los 9 L «rehechos con diferencia» — no se pinean aquí. Casa su estimando por texto (qué agrega GEN1 y qué agrega «mediana de réplicas EXTRAIBLE» de la spec sellada; por qué difieren hasta 1.1 pp) y entrégalos a mesa uno por uno, con delta y recomendación. Los 19 L y 14 AGREGADO `SIN-REHACER` quedan como demanda de nube con su receta. P7 · Contador con las clases a la vista. `status` y el tablero muestran, sin fundir: relevadas por vía (i), por vía (ii), y las que siguen legacy por qué (`SIN-OFERTA`, `FUENTE-GEN1-SIN-ACCESO`, `ESPERA-FIRMA`…). `DIN-M-01:M` aparece nombrada: es el recordatorio de que ENNViH sigue pendiente.
6 · LATITUD
Decides tú: nombre y forma del archivo de pines; cómo se integra el canal en las dos herramientas; la forma exacta de la llave lógica, siempre que no lleve ruta, versión ni `RES`. Preguntas a mesa y sigues: un pin de §2 que no pase P3; el arreglo de RES-0028 si mueve otros contadores. No decides: añadir un pin que §2 no nombre; relajar una guarda de P2.
7 · PAROS (lista cerrada)
Editar cualquier archivo bajo `forense/prereg-duelo-v2/` o la spec, medidor o resultados de un CALC · escribir un pin no nombrado en §2 · que el contador baje en más lecturas que las pineadas · cambiar un `p` en `milpa/` · renumerar o rediseñar `RES`/`CORR` · objetivo inalcanzable.
8 · COMPUERTAS
«Guardas de P2 en verde, incluido el rechazo de `DIN-M-01:M` y de `RESULT-TRIADA-*-M`» protege: adoptar. P3 no escribe un solo pin antes de eso.
9 · PERÍMETRO
Propio: el archivo de pines (nuevo) · `tools/relevo_usos.py` · `tools/corrida0.py` (el derivado de usos, `status`, y —solo para RES-0028— la inferencia de función) · `milpa/tramite.yaml` solo el campo de procedencia de `:637` si P4 lo logra · derivados por comando · tests propios y el oro de `status` · tablero · `decisiones.tsv`, NC, FP, ADR, `INFRAESTRUCTURA`. Ajeno: `forense/prereg-duelo-v2/**` · todo `CALC-*` · `tools/emite_m.py` y `milpa/src/**` (mesa MOTOR) · los generadores de `RES`/`CORR` (mesa TUBERÍA). Perímetro de cierre: el permanente.
10 · NO HACE · SUCESORES · AUDITORÍA · CIERRE
No re-mide nada · no pinea L ni AGREGADO · no toca ENNViH · no rediseña identificadores. Sucesores: a mesa TUBERÍA, como hallazgo: el canal usa llave lógica; si su diseño de `RES`/`CORR` fija otra forma, una migración del archivo de pines, no un rediseño · demanda de nube: los 33 `SIN-REHACER` del marco · `tiene_ahorros`: espera el acceso a ENNViH de la bandeja del titular. Auditoría (afirma sobre el programa): bajar el contador da trazabilidad, no validez: los 14 R reproducen el mismo número que GEN1 porque miden lo mismo con el mismo universo, sesgos incluidos (delitos declarados por víctimas en ENVIPE; hogares en ENIGH; trámites en ENCIG). Y el duelo que estas lecturas alimentan es retrospectivo para sus tres contendientes (mesa MOTOR, 21/sep): relevarlo no lo convierte en una prueba de pronóstico. Peligroso leído simplista: «el marco ya es GEN2» como «el duelo ya vale». Cascada de `/acto` · `## NO-CORRIDO / RESERVAS` · `## CONSUMIDO`.

## NO-CORRIDO / RESERVAS

| qué (verbatim del encargo) | por qué | impacto | sucesor |
|---|---|---|---|
| P3 · «Se relevan, por la vía (ii), 13 lecturas M» — los tres de `FAM-M-05`, `FAM-M-06` y `FAM-M-07` a `RESULT-B-ENIGH-2022-P` | `DECISIÓN-DE-MESA-PENDIENTE`. `CALC-B-0001` trae replay `REPLICA-RESULTADO · CONTEXTO-DISTINTO`, no `REPRODUCE` (evidencia: `forense/replay-evidencia.tsv:29` — el input `IN-B-SELECTOR`, `tools/baseline_temporal.py`, cambió en `67aa13d` después del sello). La guarda (a) de 4.1 pide `REPRODUCE` y §6 prohíbe relajar una guarda de P2. | `dependencias_numericas_legacy_activas` se queda en **149** en vez de **146**; las tres lecturas M de FAM siguen contando como legacy aunque `recibe_remesas` ya sea GEN2 en `milpa/tramite.yaml:910`. | `FP-406` · `NC-0441` |
| P4 · «escribe el pin (`RESULT-ENVIPE-RES0028-Q-C2-U4`, unidad persona, IC heredado del padre…)» | `DECISIÓN-DE-MESA-PENDIENTE`. El blocker de `PR #914` **sí** se arregló y se comprobó (escrito y revertido: `status` ya no PARA). No se escribe porque el contador bajaría a **148** —una lectura 25ª que §2 no pinea— **sin** que `N_resultados_gen2_adoptados_activos` suba: `CALC-ENVIPE-RES0028-U4-DERIVADO-0001` declara `cuenta_gen2 = PENDIENTE-DE-MESA`. §7 PARA si el contador baja en más lecturas que las pineadas. | `RES-0028` sigue legacy. El arreglo del blocker queda aplicado y disponible. | `FP-407` · `NC-0442` |
| P6 · «entrégalos a mesa uno por uno, con delta y recomendación» — entregado; lo que NO se corrió es la decisión | `DECISIÓN-DE-MESA-PENDIENTE`. La nota está completa (`forense/analisis/relevo-tanda-3/P6-los-9-L-con-diferencia-v1_0.md`) con la tabla derivada y el casado en cinco dimensiones. Lo que falta es la firma sobre qué agregador gobierna L, que no es del ejecutor. | Los 9 L siguen legacy y conservan un rótulo (`REHECHO-CON-DIFERENCIA`) que los hace parecer candidatos a pin cuando no lo son. | `FP-408` · `NC-0443` |
| Perímetro de cierre permanente (D-21) · «derivados por comando» — `corrida0 registro --escribe` | `FUERA-DE-PERÍMETRO`: es de `NC-0094`, no de este acto. PARO `REPLAY-PISADO` por `CALC-ENIF-0001--afbf3c76d71b` (`contexto_replay: IDENTICO -> DISTINTO`), **verificado pre-existente con `git stash`** sobre el árbol limpio de `67e157a`. No se usó `--lote`: nombrar una corrida ajena para que el escritor la pise es mover un veredicto ajeno, y escribir desde nube degradaría un contexto verificado en caja. | `corridas.tsv`, `resultados.tsv` y `usos.tsv` siguen con la derivación del 20/sep y no muestran las columnas `via_relevo` / `pin_de_mesa`. `relevo-usos-v1_0.tsv` **sí** se re-derivó; `status` y el tablero se derivan en memoria y están al día. | `NC-0444` → `NC-0094` |
| P6 · los 33 `SIN-REHACER` del marco (19 L + 14 AGREGADO) | `DIFERIDO-A:demanda-de-nube`. El encargo los declara demanda, no pieza a ejecutar; su receta y entorno ya viven en `forense/analisis/relevo-reconcilia-1/reconcilia-173-v1_0.tsv` y no se reescriben aquí. | Ningún contador se mueve por esto; es cola declarada, no deuda nueva. | demanda de nube (§10 del encargo) |

## CONSUMIDO

Ejecutado por **PR #942** (`https://github.com/Josanoforo/Modelado-Mexicano/pull/942`), rama `claude/focused-archimedes-fjyqf5`, 21/sep/2026, entorno NUBE, Opus 5. `ADR-574`. El merge de mesa de ese PR ES la adopción (E.2).
