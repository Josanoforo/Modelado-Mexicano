**ADR-260924-GEN2-TUBERIA-VISTA-NORMALIZADA-4-574d-01** (raíz de acto: `574d` = 4 hex del commit de 0-bis `574d24fe`; no se renumera), `ACTO GEN2-TUBERIA-VISTA-NORMALIZADA-4 · un RESULT es un número con unidad; una lista es una tabla`, 24/sep/2026, NUBE (hook `ENTORNO-DERIVADO = NUBE`, corpus montado=NO examinados=0). Base `8358b891` (= SHA de redacción); durante el acto #1122 fusionó (su head incluía el 0-bis de este acto) y se trajo `origin/main = 7fe2040d` por merge. Rama `claude/new-session-e85tdt` (la fijada por la plataforma; PR nuevo, no el fusionado). MODO AUTÓNOMO (cláusula v1.0 `3fbc487684b77b7f`). Firma de dirección sobre la FP `…TABLERO-EN-CANAL-1-3dd4-01`, verbatim: «Opción A —valor por referencia cuando pasa de 1 KB, y regla en el conducto para CALC futuros—, con la guarda a 100 MB solo hasta que A fusione; C descartada por E.7.» Firma de VISTA-2: «…ningún archivo derivado vuelve a superar 50 MB; sin Git LFS y sin sacar la vista del canal.»

**COMMIT-B (`566fb049`).** `registro --escribe` (y su diff en seco) pasa cada fila de `resultados.tsv` por `tools/vista.py::referencia_valor`: si el texto de `valor` supera 1 024 bytes UTF-8 se escribe byte a byte a `data/corrida0/<spec_id>/valores-vista/<resultado_id>.<json|txt>` y la celda lleva `REF:<ruta>#sha256:<hex>`. `vista.py` gana `resuelve_valor`, `resuelve_fila`, `valor_de(resultado_id[, corrida_id])` y `ReferenciaRota` (falla en voz alta); `leer_resultados_join` ya resuelve. Lectores directos adaptados: `milpa/src/emisor.py`, `tools/relevo_usos.py`, `forense/analisis/catalogo/genera_catalogo.py`; `delta_pin_superado` y `escribe_relevo_consumo` heredan por `leer_resultados_join`; `consulta_gen2`/`snapshot_motor_gen2` sólo hashean el archivo y `test_astra5_dominios` sólo busca ids (sin cambio). **Medido con el lote de CI** (`lote_desde_asientos.py --incluir-pendientes`, 61 CALC): `resultados.tsv` 96 768 436 → 28 879 495 bytes; 127 celdas referenciadas (66 MB en `valores-vista/`, ningún archivo > 40 MB); celda en línea máxima 1 005 bytes. **Equivalencia:** las 71 301 filas, comparadas contra la derivación previa del mismo lote sin referencias, dan **0 filas distintas** tras resolver. `adoptados_activos` = 72 antes y después (no cambia ningún número). Guarda de tamaño: **sigue en 50 MB**, ahora también sobre cada `valores-vista/*`; el paso `[deriva]` añade esos archivos.

**COMMIT-C (mismo commit).** `corrida0._problemas_valor_largo` entra a `_fallas_run`: `run` rechaza todo `valor` no escalar o texto > 1 KB salvo `REF:data/corrida0/<este CALC>/tablas/<archivo>#sha256:<hex>` con el archivo presente y sha correcto (queda bajo el sello del CALC). En `verify` de un CALC ya sellado es `WARN VALOR-LARGO-LEGADO`, nunca NO-EJECUTABLE (E.3). Spec en `data/INFRAESTRUCTURA-v1_0.md` (fila de `resultados.tsv` + fila nueva de `valores-vista/`). Test `tests/test_valor_por_referencia.py` (ida y vuelta, idempotencia, sha roto, CALC sintético con lista rechazado sin `tablas/` y aceptado con ella, derivado por ruta, vista real): VERDE; `test_linaje_superado`, `test_vista`, `test_registro_lote_estricto`, `test_corrida0`: 19/19; `test_relevo_encuci_f2` 6/6; `test_derivados_protegidos` 3/3; `test_tablero_programa` 16/16.

**Discrepancias declaradas.** (1) §4 «`tablas` → 0»: falso — `CALC-EDER2017-PRIMERA-UNION-SEXO-COHORTE-000{1,2}/tablas/` existen **selladas** (8 archivos). El derivado va a `valores-vista/` y `tablas/` queda como lo sellado (es donde COMMIT-C exige la tabla antes de sellar). (2) §9 «`derivados_protegidos.py` lo cubre»: falso — sólo reconocía la cabecera, que un archivo byte-exacto no puede llevar; se añade el reconocimiento por ruta (≤10 líneas). (3) COMMIT-A (puente a 100 MB) no se hizo: A y B viajan en el mismo PR y la vista queda en 28.9 MB; subir la guarda sólo habría abierto la ventana de riesgo. (4) B y C en un solo commit (tocan las mismas funciones). (5) El «~18 consumidores» de la premisa: 26 archivos mencionan la vista; 3 leen `valor` crudo y se adaptaron; el resto no lee `valor` o pasa por `vista.py`. Cierra la FP `…TABLERO-EN-CANAL-1-3dd4-01` (FIRMADA, DECISIÓN-DADA: A + puente B) y la NC `…-3dd4-01`. Fragmento L0 `canon/L0/ADR-260924-GEN2-TUBERIA-VISTA-NORMALIZADA-4-574d-01.md`.
