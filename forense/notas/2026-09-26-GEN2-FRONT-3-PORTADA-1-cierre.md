# GEN2-FRONT-3-PORTADA-1 · cierre técnico

Encargo: `forense/encargos/2026-09-26-GEN2-FRONT-3-PORTADA-1.md` (0-bis `89148f00`, `.cuerpo.sha256` `ae0ee16a…`). SHA de redacción `34949751` = base (0 detrás). NUBE (hook: `ENTORNO-DERIVADO = NUBE`; no toca microdato ni red). Rama `claude/new-session-7p7kao`, una sesión. MODO AUTÓNOMO-AMPLIO. ADR `ADR-260926-GEN2-FRONT-3-PORTADA-1-8914-01`.

CONTADOR: cero mediciones; no adopta. celdas_validadas: 219 → 219 (Δ0) @ 4761ce3a. Contador propio: archivos de primer nivel en la raíz **46 → 11** (`ls -p | grep -v / | wc -l`; ver §Premisas); enlaces relativos rotos en README, CONTRIBUTING y `docs/`: **0** (`python3 tests/test_portada.py`).

## Piezas

- **P1 · raíz limpia.** `1734006a`: 35 `git mv` (ningún `rm`). `instrucciones-proyecto-v2_16.md`, `-HISTORIA.md` y sus `.sha256` → `gobierno/` (`sha256sum -c` OK en la ruta nueva: A.9, la ruta no es parte del sello); `v2` … `v2_15` con `-HISTORIA`/`-DELTA`/`.sha256` → `archivo/instrucciones/`; `propuesta-*`, `PROPUESTA-*`, `revision-*` → `archivo/propuestas/`. `archivo/INDICE.md`: 35 filas `nombre → ruta → commit`, derivadas por `python3 tools/resuelve_cita.py --genera 1734006a` (no tecleadas). `tools/resuelve_cita.py <nombre>` resuelve una cita (exit 1 si no). Referencias operativas en el mismo commit: `CLAUDE.md` (`@gobierno/instrucciones-proyecto-v2_16.md`), `tests/test_arnes_sesion.py` (13/13), `tools/tablero_programa.py` (glob de `instrucciones_vigentes`), `tests/bitacora.py`. Citas en texto sellado (`canon/`, `forense/`, ADR, `data/`) **intactas**; las resuelve el índice. Las citas por nombre sin ruta en `.claude/commands/*.md` y docstrings de `tools/` tampoco se editan: T03 resuelve por basename recursivo (`tests/check.py:535`) y el índice por nombre. `tests/test_portada.py`: raíz ≤ 12, índice apunta a archivos vivos, toda cita de `canon/`+`forense/` a un nombre que estaba en la raíz antes de `1734006a` resuelve, 0 enlaces rotos. `gobierno/README.md` enlaza (no duplica) PLANTILLA-ENCARGO v2.1 y CLÁUSULA-AUTONOMÍA v1.0 en `forense/encargos/`; `canon/MEMORIA-OPERATIVA.md` y `canon/REGLAS-DE-LECTURA.md` los dejó CABLEADO-SESIONES-1 (#1165), fusionado durante el cierre; `gobierno/README.md` los enlaza (merge de `origin/main` sin conflicto en `CLAUDE.md`: conserva el `@gobierno/…` de este acto y los dos `@canon/…` de CABLEADO).
- **P2 · README de una pantalla.** Texto de dirección §11 con dos cambios firmados por mesa en esta sesión (abajo). Toda cifra visible va como `**valor** <!-- deriva[clave]: comando -->`, y el comando visible es el que declara `tools/readme_derivado.py` (FUENTES). Valores @ `412b8146`: 285 corridas selladas y 20 predicciones (`corrida0.py status`), 28 de 31 reports (`cobertura-v1_1.tsv`, `corpus/reports`), seis evaluaciones / cuatro instrumentos / 137 celdas (`forense/analisis/informe-v1_3/censo_evaluaciones.py`, el censo del informe v1.3), DOI «pendiente» (`CITATION.cff` sin campo `doi`). Las dos cifras de `status` viven en el bloque TABLERO-DERIVADO, lo único que `derivados_protegidos.py --solo-derivados` deja cambiar al job `guardias`. `docs/estado.md` hereda del README anterior «La prueba», «Estado derivado» (claves y comandos, sin valores que envejezcan), «Estado del modelo» (T19c lo lee ahí; `tests/check.py`), «Verifica», «Cobertura» y «Uso». Badges `docs/data/badges/*.json` (formato `endpoint` de shields.io, servidos desde la URL cruda de `main`): evaluaciones, instrumentos, celdas y reports, nunca cifras de `status` (el job no hace commit de esa carpeta). `tests/test_readme_derivado.py` reescrito: marcas con comando de FUENTES y valor derivado, cero cifras visibles sin marca, `status` solo dentro del bloque, badges.
- **P3 · metadatos y vista previa.** `docs/assets/social-preview.png` (1280×640) por `python3 tools/readme_derivado.py --vista-previa` (headless_shell de Chromium, sin red): título, tagline y las tres cifras de la prueba, sin logos. Descripción y homepage en `docs/_config.yml`. La sesión no tiene `gh` ni herramienta MCP de edición del repo: no se aplicó. **Receta de un minuto para mesa** (FP `…-8914-03`): GitHub → Settings → General → *Social preview* → *Edit* → subir `docs/assets/social-preview.png`; en la portada del repo, engrane de *About*: Description = «Benchmark auditable del comportamiento del mexicano con encuestas oficiales: pisos por segmento con intervalo calibrado, predicciones selladas antes de cada ola, todo reproducible.», Website = `https://josanoforo.github.io/Modelado-Mexicano/`, Topics = `mexico inegi benchmark survey-data reproducible-research behavioral-science spanish`.
- **P4 · landing.** `docs/index.md`: mismo tagline, mismos tres botones (Leer el informe · Consultar el catálogo · Verificar en cinco minutos), barra Inicio · Informe · Catálogo · Consultar · Verificar · Reto · Contacto y «Más». `docs/_config.yml`: `theme: minima` con `header_pages`. `docs/consultar.md` no existe (PRODUCTO-CONSULTA-1 sin fusionar): «Consultar» apunta a `catalogo.md`. Las tres cifras de la landing son las de la prueba (6/4/137), no las de la línea de cifras del README: ver NC `…-8914-01`.
- **P5 · CONTRIBUTING.** Tres párrafos para externos (citar y verificar · retar, con la mecánica leída de `docs/reto.md` §«Qué entregar» · qué no se acepta). El protocolo interno anterior pasa íntegro a `gobierno/protocolo-de-cambio.md`.

## Firmas de mesa en sesión (verbatim de la respuesta; asentadas en FP `…-8914-02`)

Pregunta 1 (README, «⟨N⟩ predicciones verificadas antes de que INEGI publicara la ola»: lo único derivable es `celdas_validadas_prospectiva`, sellada antes de abrir R, no antes de la publicación de INEGI): **«Reformular (Recomendado)»** — «⟨N⟩ predicciones selladas antes de abrir la ola contra la que se comparan».
Pregunta 2 («⟨N⟩ de 31 dominios con medición»: los 31 son reports, no dominios; no hay tabla de «report con medición sellada»): **«Reports medibles (Recomendado)»** — «⟨28⟩ de ⟨31⟩ reports con afirmaciones medibles en el corpus».
Mesa, sobre el bloqueo de permisos de los `git mv`: «aprobado».

## Premisas que cayeron (logística; se replantea y se declara)

- «Hecho»: `ls -1 | grep -v '/' | wc -l` ≤ 12. `ls -1` no marca directorios, así que cuenta también las 10 carpetas: da 21 con la raíz ya limpia (y el «55» de §3 es ese mismo conteo con carpetas). El test cuenta solo archivos (`ls -p | grep -v /`): 11. Hallazgo en `forense/hallazgos.md`.
- §3 «`docs/` … informe»: `docs/informe.md` no existía; el README firmado lo enlaza. Se creó como puntero de 9 líneas (D-21).
- La tabla de seis evaluaciones heredada de FRONT-1 (pilotos 1–3, lote ENIF, duelos ENVIPE 2026 y ENCIG) no coincide fila a fila con el censo del informe v1.3 que da las cifras de la frase (incluye el duelo ENIGH 2024 y el piloto 4 ENVIPE; no los pilotos 1 y 2). «cuatro instrumentos» son ENCIG, ENIF, ENIGH y ENVIPE. Declarado en la cabecera de `docs/estado.md`; hallazgo.

## Defectos adyacentes corregidos (D-21)

- `docs/one-pager.md:15` contaba evaluaciones sobre la tabla del README (`rg -c … README.md`): ahora sobre `docs/estado.md` (mismo 6). `docs/one-pager.md:26` y `docs/verificar.md:11` enlazaban anclas del README viejo: ahora `estado.html#…`.
- T02 (`tests/check.py`): dos grupos de nombre exentos con su razón (`README.md` / `gobierno/README.md`; `docs/informe.md` / `…/INFORME.md`).
- `forense/analisis/ci-guardias/censo-tests.tsv`: fila manual de `tests/test_portada.py` (CORRE-EN-CI, 1.7 s), mismo patrón que `528d6937`.

## Suite

`python3 tests/check.py --rapido` @ `4761ce3a`: `0 FAIL · 516 WARN`. `tests/test_portada.py`: OK (raíz 11, índice 35, enlaces sin rotos). `tests/test_arnes_sesion.py`: 13/0. `tests/test_readme_derivado.py`: ver el cierre del encargo. `--baseline` completo: ver el cierre del encargo.
