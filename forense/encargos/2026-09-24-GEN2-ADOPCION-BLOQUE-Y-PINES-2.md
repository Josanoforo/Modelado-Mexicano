# ENCARGO · ACTO GEN2-ADOPCION-BLOQUE-Y-PINES-2 · Sucesor del -1: acredita el tercer CALC (S), asienta las once firmas de Astra (T), cablea los diez RESULT pendientes al consumidor que ya los espera y extiende el escritor a procedencia, catálogo y celdas-D según sus contratos — sin repetir el pin que el -1 probó y revirtió

> ENTORNO: **NUBE** — RESULT sellados, `decisiones.tsv`, `milpa/*.yaml` solo por escritor; cero microdato. Hook imprime ENTORNO-DERIVADO; si dice CAJA, PARA.

CABECERA · SHA de redacción `c12a0d87` (re-deriva al abrir) · una sola sesión, rama propia `acto/gen2-adopcion-bloque-y-pines-2` (D-17) · MODELO: Opus · MODO: ABIERTO (lote D-11: cuatro piezas; una que PARA no tumba las otras) · ids con raíz de acto (D-24) · D-21 aplica · «Si te encuentras escribiendo fuera de la lista de §9, PARA.» · cierre por /acto: `## NO-CORRIDO / RESERVAS` («Ninguno.» obligatorio si aplica) y `## CONSUMIDO` al pie.
CONTADOR: cero mediciones. Mueve por derivación y solo con firma verbatim: `N_resultados_gen2_pendientes_adopcion` (10 → 0), `dependencias_numericas_legacy_activas` (146 → menos; por consumidor), `N_resultados_gen2_adoptados_activos` (72 → lo que el marcador dé cuando el canal publique; en rama se mide con el marcador escrito en el árbol sin commitear, como #1101). Antes/después por pieza en la nota; nada a mano.

## 1 · OBJETIVO
Cerrar lo que el -1 (#1102) dejó por dos hallazgos de premisa y una firma pendiente: (S) el tercer CALC sin `origen_numerico` que hace fallar 16 T-REPRO(g) y frena el marcador; (T) once decisiones de Astra ya firmadas sin ejecutor; (P-W) los diez `pendientes_adopcion` reales (`RESULT-BANXICO-2024-*` ×5, `RESULT-CTX-2019/2023-P-ALTO`, `RESULT-MOTRAL15-*` ×3) que son un conjunto **disjunto** del bloque ENIGH y solo se adoptan cableándolos al consumidor `milpa/tramite-ola5-propuesta-v0.yaml` (objeto de `NC-…-FIRMAS-7-369b-01`); (P-E) el escritor de consumo, hoy limitado a RES-0028, extendido a los tres consumidores que `forense/analisis/astra4-relevo/contratos-otros-consumidores.md` describe (procedencia 40 filas, catálogo 23, celdas-D 6) — con la lección del -1: un pin al catálogo por `i-CRUDO` materializa un valor y rompe `T-REPRO(c)`; la vía correcta para el catálogo es la que su contrato diga, no un pin.
«Hecho» sobre el commit final con origin/main fusionado: `decisiones.tsv` con la fila de S y las once de T citando FP por id · `status` en rama (marcador en árbol): `pendientes_adopcion` = 0, `legacy_activas_por_consumidor__motor` < 34, `__procedencia` < 40 o `__catalogo_de_momentos` < 23 (al menos uno baja; los otros con NC y razón por llave) · `T-REPRO(c)` y `(g)` VERDE con el marcador re-derivado en el árbol · test por llave del escritor (el yaml gana la cita del RESULT, nunca un número) · `check.py --baseline` VERDE sin `--force`. El valor en `main` llega con el primer `[deriva]` tras VISTA-NORMALIZADA-2; la NC lo dice con `DIFERIDO-A` y no es PARO.

## 2 · FIRMAS DE MESA — dadas (FIRMAS-15 + ADENDA-1, 24/sep, «formado con tus recomendaciones»); viajan aquí verbatim y las asienta este acto al ejecutarlas
- **S** «Se acredita origen_numerico de CALC-ENCIG-DUELO-2025-ADJUDICACION-0001 según sus inputs (manifiesto = NUEVO; CALC sellados = HEREDADO), fila en decisiones.tsv citando FP-260923-GEN2-CONTADORES-CONSUMO-2-749c-01.»
- **T** (ocho adopciones + tres dictámenes, texto de FIRMAS-15 §1 T, verbatim por fila): ENDUTIH 2023–2025 (1 551 celdas; excluidas 141 de empleo) · MOCIBA 2015–2017 (279 celdas; 2015 no estimable) · ENOE pisos y dictamen de límites (último trimestre reservado) · ENDIREH 2021, 2016, 2011, 2006 por instrumento con ventanas propias, sin equiparar al 70.1 % · ENDIREH 2003 ratificado sin IC · INE cómputos definitivos como universo · ENCUP descriptivo en su escala · LAPOP sin serie hasta dictamen de equivalencia. Todas «piso descriptivo retrospectivo, sin uso predictivo».
- **N** (24/sep, ADOPCION-1 §2, vigente): «Momento 08 se acota a unidad DELITO (BPCOD=01) con discrepancia NO-REPRODUCE-GEN1 rotulada; momentos 01 y 02 adoptan la pregunta y el registro que el cotejo documental cita por texto; el escritor de consumo se extiende a esas llaves según contratos-otros-consumidores.md.»
- **Bloque de diez (FIRMAS-7, 21/sep, `…369b-01`):** los diez RESULT (Banxico ×5, CTX ×2, MOTRAL ×3) están ADOPTADOS por firma; solo faltaba el acto con `milpa/` en perímetro. Este es ese acto.
- **Decisión de dirección (no requiere firma; se declara):** el catálogo de momentos no se releva por pin `i-CRUDO`; se releva por el escritor según su contrato, con `T-REPRO(c)` como guarda. Si el contrato del catálogo exige que el valor materializado y la cita convivan, ese es el diseño del escritor, no un parche al test.

## 3 · LO QUE DIRECCIÓN SABE
- `[LEÍDO]` Cierre del -1 (`forense/notas/2026-09-24-GEN2-ADOPCION-BLOQUE-Y-PINES-1-cierre.md`): hallazgo 1 (l.90-104: los 10 pendientes son Banxico/CTX/MOTRAL, disjuntos de ENIGH; `cuenta_gen2` y `adoptados` son dos ejes); hallazgo 2 (l.105-118: son **6** NO-VERIFICADO, no 4: EDER ×2, ENFIH ×2, ENSAFI, WBES — van a `GEN2-REPLAY-NO-VERIFICADOS-1`, no a este acto); P-N (l.18-33): pin `M08` vía `i-CRUDO` pasó las cuatro guardas y rompió `T-REPRO(c)`; se revirtió; el dictamen de momento 08 quedó en `decisiones.tsv:catalogo:M08`; con marcador en árbol `adoptados` 72 → 103 (l.69-80). NC del -1: `ec71-01/02/03`.
- `[LEÍDO]` `forense/analisis/astra4-relevo/contratos-otros-consumidores.md` (§ procedencia 40 · catálogo 23 · celdas-D 6 · separación de contadores) y `contrato-escritor-consumo.md`: son la spec del escritor extendido; se citan, no se reinterpretan.
- `[EJECUTADO]` `status` a `c12a0d87`: 244 corridas · 72 · 10 · 146 (motor 34 · procedencia 40 · catálogo 23 · marco 43 · celdas-D 6) · `celdas_validadas` 219. FP abiertas: 3 (ninguna de este acto). #1109 fusionado; VISTA-NORMALIZADA-2 en vuelo.
- `[SUPUESTO]` que `milpa/tramite-ola5-propuesta-v0.yaml` tiene, para cada uno de los diez RESULT, una regla ya existente que los cita por id (el -1 lo verificó con `_resultados_citados_en`); si alguno no tiene regla, es `NO-CONSTRUIBLE` con NC, no se inventa regla.
- ADJUNTOS: ninguno.

## 4 · YA HECHO / YA DECIDIDO
`git ls-remote --heads origin | grep -i 'adopcion'` → 0 (el -1 cerró; rama borrada). `grep -c 'ENCIG-DUELO-2025-ADJUDICACION-0001' milpa/decisiones.tsv` → reporta (esperado 0 filas `origen_numerico`). `grep -c 'ASTRA5' milpa/decisiones.tsv` → reporta. **Repítelo.**

## 5 · PIEZAS
- **P-S · Tercer CALC.** Fila `origen_numerico` en `decisiones.tsv` con la firma S verbatim; `marcador_segmento.py --escribe` en el árbol (sin commitear) → `check.py --baseline`: los 16 T-REPRO(g) deben desaparecer; si quedan otros, tabla con causa. Cierra `749c-01`.
- **P-T · Once filas.** Una fila por FP de Astra en `decisiones.tsv` (adopción descriptiva retrospectiva; `adopta: SI`, `uso_predictivo: NO`, `vence:` ola siguiente), citando FP por id y el veredicto de AUDITORIA-POST-HOC (`RECIBO-INCOMPLETO` o el que tenga: no impide adoptar, la auditoría dio cero REVERTIR). Los tres dictámenes de política se asientan como regla en `decisiones.tsv` con `tipo: DICTAMEN`. El catálogo (`canon/catalogo-del-mexicano-v1_0.tsv`) los consume por su propio acto; aquí no se edita.
- **P-W · Diez pendientes al consumidor.** Escritor de consumo extendido a `milpa/tramite-ola5-propuesta-v0.yaml` (mismo contrato que RES-0028: etiqueta `corrida0_resultado_id`/`corrida0_generacion` en la regla existente); diff seco pegado; `--apply`; test por RESULT; `status` → `pendientes_adopcion` 0. Cierra `369b-01` y `ec71-01`.
- **P-E · Escritor a los tres consumidores.** Por contrato y en este orden: (1) **procedencia** (40): las filas con RESULT sellado y replay afirmativo (vías i/ii/iii leen eje RESULTADO, D6) → el escritor escribe la cita; (2) **catálogo** (23): lo que el contrato diga para convivir valor materializado y cita, con `T-REPRO(c)` VERDE como criterio; momento 08 según N; momentos 01–02 solo si el cotejo cita texto de pregunta (A.15), si no NO-CONSTRUIBLE; (3) **celdas-D** (6 YAML + 6 referencias de código): las referencias de código se relevan por cita; los YAML no se tocan a mano. Cada llave que quede: NC `DIFERIDO-A: -3` con razón. Cierra `ec71-03`.

## 6 · LATITUD
Orden sugerido S → T → W → E. ≤ 10 líneas adyacentes: sí. Pregunta a mesa (sigues con el resto): solo si un contrato de consumidor exige decidir entre dos formas de convivencia valor/cita que el contrato no resuelve — con opciones y recomendación.

## 7 · PAROS — lista cerrada
a) no aplica · b) editar `milpa/*.yaml` a mano, commitear vistas, `--force`, `--excluye`, bajar `T-REPRO(c)`/(g), reescribir un sello o una fila FIRMADA · c) escribir una fila de `decisiones.tsv` sin texto verbatim de mesa; tocar los 6 NO-VERIFICADO (son de REPLAY-NO-VERIFICADOS-1) · d) no aplica · e) CAJA · f) las cuatro piezas ya están en origin/main.

## 8 · COMPUERTAS
«Cada fila de `decisiones.tsv` cita firma por id» protege: **adoptar** (E.2). «El motor y sus consumidores cambian solo por el escritor, con diff seco y test por llave; `T-REPRO(c)` es guarda, no obstáculo» protege: **adoptar / congelar**. «Los 6 NO-VERIFICADO no se tocan» protege: **adoptar** (no han pasado la segunda pregunta de E.2).

## 9 · PERÍMETRO Y CONCURRENCIA
Propio: `milpa/decisiones.tsv` (append), `tools/escribe_relevo_consumo.py` + tests, `milpa/tramite-ola5-propuesta-v0.yaml`, `milpa/procedencia.yaml`, `milpa/catalogo-momentos-v0_1.tsv` y las 6 referencias de código de celdas-D — **todo vía escritor**, `no-corrido.tsv`/`firmas-pendientes.tsv` (estado/append), nota, L0, cascada. Ajeno: vistas derivadas (no se commitean), CALC, YAML de celdas-D, `marcador_segmento.py`, `registro`, `verify.yml`, catálogo de Astra. En vuelo: VISTA-NORMALIZADA-2 (toca `corrida0.py registro` y `verify.yml`: no chocan), U0 mapa-dominios (mesa; no toca `milpa/`), REPLAY-NO-VERIFICADOS-1 (caja; no toca `milpa/`).

## 10 · LO QUE NO HACE · SUCESORES
No mide, no adopta los 6 NO-VERIFICADO, no edita el catálogo de Astra ni el contrato celda-D (U de FIRMAS-15 es del próximo acto que lo edite). Sucesores: `-3` para llaves `DIFERIDO-A`; FIRMAS-16 asienta los contadores nuevos cuando el canal publique.
