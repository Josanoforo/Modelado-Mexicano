# Preregistro · WBES2023 precisión de interacciones

Identidad: `GEN2-WBES2023-PRECISION-INTERACCIONES-CLI-2`; CALC `CALC-WBES2023-PRECISION-INTERACCIONES-0001`. Exposición previa: se conocían los puntos del descriptivo y la precisión del compuesto; no hubo ceguera de éstos. Esta extensión no selecciona escenario por error ni reabre el compuesto.

Antes de ejecutar se fijaron los seis pares y sus ventanas: electricidad, agua, construcción, importación y operación: últimos dos años; fiscal: último año. Para cada interacción se medirán total y `a6a` 1–4, con el marco completo y `strata` como diseño. Se producirán los dos escenarios singleton heredados, IC logit-t para tasas y límites por faltantes separados.

Único contraste: `j5 - j15`, sólo donde `j3=j13=1` y ambos desenlaces son sí/no. Se estimará su covarianza por linealización en el mismo establecimiento y IC t de diferencia sin truncar. Su diferencia no es una comparación anualizada.
