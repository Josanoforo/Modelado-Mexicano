# Nota del acto · MESA-18AGO — nueve firmas propagadas, las vencidas cerradas, el disparador partido

**Acto:** ACTO MESA-18AGO · **Encargo:** `forense/encargos/2026-08-18-MESA-18AGO-nueve-firmas.md` (archivado verbatim por A.3, en este mismo commit) · **Entorno:** NUBE, repo-only, sin `data/raw` · **SHA de redacción del encargo:** `ae25137` · **SHA real de arranque:** `93a4dd9` (`origin/main`, merge de `#256`/`ACTO INTEGRATE-T23`).

---

## §0 · ARRANQUE

```
$ pwd
/home/user/Modelado-Mexicano
$ git log -1 --format="%h %s"
93a4dd9 Merge pull request #256 from Josanoforo/claude/acto-t23-integrador-cableado-is3bv2
$ git status
On branch claude/new-session-yzskdx
nothing to commit, working tree clean
$ git rev-parse --is-shallow-repository
true
```

Clon superficial. `git fetch --unshallow` corrido antes de cualquier veredicto — `main` traía además `forced update` (el remoto se había movido desde el último fetch del contenedor).

**SHA de redacción vs. SHA real: main avanzó tres commits desde `ae25137`.** `git log --oneline ae25137..origin/main` da `93a4dd9`/`2903c2d`/`1537ae4`/`53b9477`/`53b6ee8`. `ae25137` resulta ser exactamente el merge de `GATE-DURABLE-V7` (`PR #255`) — el propio encargo lo describe como "merge #255", correcto. Lo que el encargo no podía saber al escribirse: `INTEGRATE-T23` (`PR #256`) fusionó **después**, así que ambos carriles concurrentes que el encargo declaraba "corriendo en paralelo" **ya habían cerrado los dos** para cuando este acto arrancó. Esto cambia el estado de gate de varias filas (ver §2) — re-derivado, no heredado, antes de escribir nada.

**Incidente propio, corregido en el mismo arranque:** un `git checkout main -- .` temprano usó la rama local `main`, que estaba desactualizada (`f8eb2e3`, no `origin/main` real) porque el clon nunca la había actualizado tras el `--unshallow`. Esto revirtió 48 archivos a un estado stale (incluido `canon/gobernanza-v1_15.md`, que pasó de 99 ADR a 71 visibles) antes de que se hiciera ningún commit. Detectado por el propio flujo de trabajo (el conteo de ADR no cuadraba con lo que `forense/firmas-pendientes.tsv` ya citaba), corregido con `git restore --source=HEAD --staged --worktree -- .` (no `reset --hard`, bloqueado por el clasificador de este entorno) antes de escribir una sola línea. `git branch -f main origin/main` deja la rama local sana para el resto del acto. Ninguna escritura de este acto se hizo sobre el árbol corrupto.

**data/raw:** ausente, no usado — este acto no lo necesita.

**ENTORNO (A.2):**
```
$ curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://www.inegi.org.mx/
000  (exit 56, conexión rechazada por el proxy del arnés)
$ ls data/raw/ 2>/dev/null | head -1
(vacío)
```
Firma de entorno de nube repo-only: sin red de datos, allowlist de la caja, no de INEGI (A.5).

---

## §1 · Los cuatro prompts a mesa, respuesta verbatim

Mesa autorizó ser preguntada (*"que la sesión de cloud lo promptee para aprobarlo"*). Los cuatro se preguntaron por `AskUserQuestion`; en tres de los cuatro, la investigación previa encontró que la palabra de mesa **ya existía en el árbol** (defecto de puntero, no decisión pendiente) — se presentó el hallazgo para confirmación en vez de re-preguntar en blanco.

| # | pregunta | hallazgo presentado | respuesta de mesa |
|---|---|---|---|
| **P1** | Firma para la ranura `[FIRMA M6 — VACÍA]` | `ADR-91` ya trae el texto de M6 dentro del lote M1-M6 | **"Firmo M6 tal cual (ADR-91)"** |
| **P2** | Lectura de `D-3` sobre `FP-51` | (genuinamente abierta — `FP-51` sin ADR) | **"Confirmo esa lectura, séllala"** |
| **P3** | `FP-18`/T20 — ¿se instrumenta? | `FP-18` ya `FIRMADA` desde `ADR-91`, columna `gatea` desactualizada | **"Sí, procede con la respuesta ya dada"** |
| **P4** | Texto de `FP-33`/`FP-42` — ¿existe? | Ambas mitades ya resueltas (`ADR-93` para `FP-24`; `REGISTRA-17AGO-II` ya pegó el texto de `FP-33` inline) | **"Coincido, ambas ya resueltas"** |

Las cuatro contestadas — ningún inciso queda `PROMPT-SIN-RESPUESTA`.

---

## §2 · Disposición completa, por inciso — el detalle vive en `ADR-100`/`ADR-101`

Esta nota no repite la prosa de los ADR; enumera qué se tocó y dónde verificarlo.

- **`ADR-100`** — sello de `ADR-MOTOR-2`: M1-M6 firmadas (texto de `ADR-91`), M6 deja de bloquear, declaración explícita de que la re-verificación de M2/M4/M5 no está sustituida. `FP-01`..`FP-06`: `FIRMADA-CONDICIONAL` → `FIRMADA`.
- **`ADR-101`** — las diez citas de mesa, registradas y propagadas: `FP-49`/`FP-51`/`FP-52`/`FP-42` selladas · `FP-17`/`FP-44`/`FP-45` ejecutadas · `FP-24`/`FP-29`/`FP-18` con puntero corregido, sin reabrir · `FP-26` partida en DISPARADOR-A/DISPARADOR-B, `FP-10`/`FP-12` asignados a B · `FP-47` medida (`baseline_sha256` re-derivado, sin cambio) y parada por falta de microdato · `FP-48` sellada con la decisión de mesa (D-7, partir la línea), ejecución diferida · hallazgo de que `ACTO MOTOR-3/E0` (`PR #237`) solo corrió FASE-PLAN, `E5` no se ejecuta este acto.
- **`canon/gobernanza-v1_15.md:764/:856/:1387/:1393`** — restauradas a su cifra de sello, `{cita-historica}` conservado, rastro `*(restaurada a su valor de sello por ACTO MESA-18AGO, D-5)*` añadido. `:1274` — **PARA, mixta** (su prosa narra su propia trayectoria en la misma oración; tocar solo la cifra la habría dejado inconsistente consigo misma). Verificado antes/después: `T16 [ok]`, línea base VERDE, cero `--freeze`.
- **`canon/estado-programa-v1_10.md:136-137`** — pegadas verbatim tal como `CIERRA-17AGO` las dejó redactadas (`ADR-94` ya había movido las cifras que citan; este acto solo propaga el texto al casillero, verificado que el perímetro de `BARRIDO-2` ya no lo bloquea).
- **`canon/estado-programa-v1_10.md:27/:101/:129/:221`** — cascada de ADR (99→100→101, dos sitios) y recifrado de las dos citas mutables de WARN (135→127, consecuencia aritmética de mover filas de `ABIERTA`/`FIRMADA-CONDICIONAL` a `FIRMADA`/ejecutada — cero contenido nuevo del corpus).
- **Nueve encargos escritos, `VIVO`, ninguno lanzado:** `T20-LLAVES` · `ADQ-15` (las 15 filas derivadas y ordenadas por `palanca`, comando en `ADR-101(g)`) · `LANE-A-E0-E5` · `REFIRMA-OPACA` · `CENSO-CMD` · `ESTADO-SPLIT` · `FP10-PRECEDENCIA` · `FUSION-PUERTAS` · `E3-TRIAGE`.
- **Dos defectos de test propios, encontrados y corregidos antes de commitear:** `forense/encargos/2026-08-18-CENSO-CMD.md` citaba un archivo futuro entre comillas invertidas (el censo v1.2 que ese mismo encargo generaría) — T03 lo marcaba colgante; corregido quitando las comillas invertidas, mismo patrón que otros actos ya usaron para citas deliberadas a nombres que no existen todavía. `forense/encargos/2026-08-18-LANE-A-E0-E5.md` hereda, del encargo original de `MOTOR-3/E0`, la palabra en mayúsculas que el mecanismo de auto-protección de T22(b) vigila como marcador de espacio vacío por llenar — T22(b) lo marcaba como archivo nuevo con ese marcador sin fila que lo cite; corregido añadiendo la ruta a la columna `dónde` de `FP-15`, la fila real a la que pertenece.

---

## §3 · Tablero — re-conteo derivado, no heredado

```
$ python3 -c "
import csv
rows = list(csv.DictReader(open('forense/firmas-pendientes.tsv', encoding='utf-8'), delimiter='\t'))
from collections import Counter
print('total:', len(rows))
print(Counter(r['estado'] for r in rows))
print('ABIERTA:', [r['id'] for r in rows if r['estado']=='ABIERTA'])
"
total: 52
Counter({'FIRMADA': 39, 'FIRMADA-CONDICIONAL': 4, 'ABIERTA': 9})
ABIERTA: ['FP-15', 'FP-26', 'FP-29', 'FP-32', 'FP-33', 'FP-35', 'FP-37', 'FP-38', 'FP-47']
```

Al abrir el acto: 52 filas, 4 `FIRMADA` de menos (`FP-42`/`FP-49`/`FP-51`/`FP-52` estaban `ABIERTA`), 6 `FIRMADA-CONDICIONAL` de más (`FP-01`..`FP-06`). Cierra con **39 `FIRMADA`, 4 `FIRMADA-CONDICIONAL` (`FP-10`/`FP-11`/`FP-12`/`FP-14`, sin tocar por diseño), 9 `ABIERTA`**.

---

## §4 · Suite de verificación — antes/después

```
ANTES (93a4dd9, sin editar):          19 FAIL · 135 WARN — LÍNEA BASE: VERDE
DESPUÉS (los commits de este acto):   19 FAIL · 127 WARN — LÍNEA BASE: VERDE
```

Ocho WARN menos, cero FAIL de diferencia neta, `tests/baseline.json` **sin tocar en ningún momento**, cero `--freeze`. La caída de WARN es aritmética directa de `T22`: menos filas `ABIERTA`/`FIRMADA-sin-ejecutar` emiten menos señal — no es mejora de contenido del corpus, es el tablero reflejando lo que este acto ya cerró.

---

## §5 · Auditoría (A.4/v2.1)

**Contadores de medición sobre México: cero.** `ACTO MOTOR-3/E0` no cerró completo (`milpa/src/` no existe, verificado) — por diseño de C6, `E5` **no se ejecuta** en este acto; el contador de entradas de `registro-recalculo` adjudicadas no se mueve. Ningún otro inciso mide México. Ninguna cifra tecleada: los cuatro prompts llevan respuesta verbatim de `AskUserQuestion`; los punteros y restauraciones llevan comando o cita a la fuente de sello; las 15 filas de `FP-17` llevan el comando que las derivó.

**Lo que este acto NO hace** (idéntico a lo que el encargo declaró, verificado que se cumplió): no ejecuta `FP-46` · no sella `ficha-id-g3` (`FP-11`) · no lanza ninguno de los nueve encargos que escribe · no toca `CONSOLIDA-2` · no congela `tests/baseline.json`.

---

## Cierre

`ADR-100`, `ADR-101` sellados · cascada `estado-programa:27/:101` a 101 · `firmas-pendientes.tsv` con 52 filas, re-contadas · nueve encargos `VIVO` archivados · línea en `hallazgos.md` · `forense/encargos/2026-08-18-MESA-18AGO-nueve-firmas.md` → `CONSUMIDO`.
