# ACTO GEN2-DUELO-ENVIPE2026-COMMIT-1 — nota de cierre (una página para mesa)

**21/sep/2026 · CAJA · Opus · rama `acto/gen2-duelo-envipe2026-commit-1` · base `55c8d57c` (= SHA de redacción del encargo, 0 detrás) · encargo archivado `forense/encargos/2026-09-21-GEN2-DUELO-ENVIPE2026-COMMIT-1.md` (0-bis `8796ed76`, sello de cuerpo `012e57b4…`).**

**Contadores.** `N_corridas_selladas` 131 → 134 (tres CALC nuevos, `cuenta_gen2 = SI`, sin adopción) · `adoptados_activos` no se mueve · ningún sello de ENVIPE 2026: la ola no se abrió ni se descargó.

## 1 · Qué quedó construido y ensayado

- **P0.** Diseño de MOTOR v1.0 archivado verbatim en `forense/prereg-caja/DISENO-duelo-prospectivo-ENVIPE2026-v1_0.md`, `sha256 e88d3192…` = el del encargo (comprobado por comando). Enmienda v1.1 con la reserva, F7 (A)(B)(C), D-22 ampliada, la regla v0.3 y la respuesta de mesa de hoy. `envipe2026_csv`: **0 entradas** en `data/manifiesto.yaml`; ausente en `data/raw` y en el censo de raíz del día (`forense/censo-raiz/2026-09-21.txt`, 606 archivos, 144 nuevos, ninguno ENVIPE 2026); en la cola `data/cola-adquisicion-v1_0.tsv:158` como `SOLICITUD-PRE-CONFIRMADA`, prioridad 1. No se bajó ni se abrió (PARO a).
- **P1.** Spec humana `DUELO-PROSPECTIVO-ENVIPE2026-spec-v1_0.md` + sidecar: primera línea universo/unidad/escala; dos estimandos entran (ND 15 olas, 2011 fuera por su columna `comparabilidad`; EN 3 olas, T5/TC NO-CONSTRUIBLE); K ≡ constante del motor comprobado (`|Δ| = 4.8e-7`); K/T3/T5/TC con fórmula cerrada y TC exigiendo ≥ 6 olas; E+ en logit; familia de cruces y regla v0.3; regla del LLM; B-bis íntegro; qué no significa.
- **P2.** `tools/duelo/cruces_familia.py` — **implementación de referencia** genérica sobre (instrumento, par): C2, P, S1, Sλ (λ por momentos, como el piloto 3), AP, soporte y `adjudica()` v0.3, sin nada de ENVIPE. `tools/duelo/tendencia_nacional.py` — K/T3/T5/TC, E+, origen móvil. `tools/duelo/envipe_duelo.py` — todo el cableado ENVIPE, parametrizado por `ola_nueva`, depositado **byte a byte** como `medidor.py` en los cinco CALC (E.5). Guardia de una sola variable: la ola nueva se carga `reservada=True` por el guardián ajeno; el medidor prueba en cada corrida que `cruce()` lanza `ReservaRota` y que el par vetado de 2025 también. Emisiones no emite marginales, nacional ni numerador de la ola nueva.
- **P3 (i)** «Como si 2025 fuera la ola nueva» — `CALC-DUELO-ENSAYO-ENVIPE2025-EMISIONES-0001` (`--7e43b64c016a`, 755 RESULT) y `…-ADJUDICACION-0001` (`--18f4f386f0e3`, 761 RESULT), sellados, `verify` REPRODUCE/IDENTICO, asiento aislado en `forense/replay-evidencia.tsv`. Reproducen al piloto 2: `C2` re-derivado a `1.1e-16`, `P = C1` a `0.0`, `R` (12 celdas, punto e IC95) a `0.0`, `MAE(C1)` exacto, nacional `0.5627744787844097` exacto, `K(ND) = fila 2024`. `S1` contra `C7` da `1.1e-6` y `MAE(C2)` `2.2e-5` pp: **atribuido exactamente** al grano de seis decimales con que el piloto 2 fijó sus marginales sellados (con ese `C2` redondeado y mi `Ī`, Δ = 0 en las 12 celdas; `hallazgos.md`). Regla v0.3 sobre 2025: Sλ (λ = 0.827) `NADIE-VENCE` (ΔMAE −0.79 pp, IC95 [−1.73, 0.17]); P, S1 y AP: el piso se sostiene (IC95 sup < 0) — coherente con el `SIN-CANDIDATO-SUPERIOR` sellado. EXD sale `NO-ADJUDICABLE-VETO` (rama terminal sobre oro). Lectura mecánica del B-bis nacional ND 2025: «FILA-3 falsador débil: T5 gana en punto, el IC no despeja» (errores K −0.47, T3 −1.36, T5 +0.28, TC −0.75 pp).
- **P3 (ii)** `CALC-DUELO-ORIGEN-MOVIL-0001` (`--adf2d06713c3`, 621 RESULT, sólo insumos versionados con sha), sellado, REPRODUCE. **RETROSPECTIVA-MECÁNICA, sin selección (PARO c)** — ND, ventana común 2018–2025 (8 olas): MAE K 1.24 pp · T3 2.18 · T5 1.48 · TC 1.12; sesgo K +0.31, T3 −0.16, T5 −0.41, TC −0.49; cobertura del IC95 del candidato 75 % / 62 % / 62 % / 50 % (Wilson ancho: n = 8) — los IC de T* cubren muestreo, no especificación. Sobre todas sus olas: K 1.48 (13), T3 2.32 (11), T5 1.50 (9), TC 1.12 (8). EN: sólo K construible (2024: −1.72 pp; 2025: −2.40 pp), sin IC sellado en 2023/2024. Las cuatro variantes van a COMMIT-2.
- **P3 (iii)** `tests/test_duelo_prospectivo.py` 25/25: fórmulas cerradas, `piso_log_aditivo` del piloto 2 importado, λ ∈ {0, →1}, regla v0.3 en sus tres bandas, guardia y veto sobre zips fabricados, **celda rara** (ola de 40 delitos → `C2` con celdas `null`) y `NO-ADJUDICABLE-VETO`, y `corrida0._valida_outputs` **vacío** contra el `spec.yaml` de cada uno de los cinco CALC; cero no finitos (`NaN` → `null` declarado). Censado en `forense/analisis/ci-guardias/censo-tests.tsv` (una fila propia; corre en CAJA, se salta en CI hasta FP-398 a).

## 2 · Bifurcación que fue a mesa (D-19)

El guardián `tools/celda_d/marginales_reproduccion.py` (ajeno) veta **por nombre** `edad × dominio_urbano_rural` de 2025 (`PARES_VETADOS`, NC-0328) y no hay R 2025 sellado de ese par; F7(B) lo mete al duelo. Pregunta con tres opciones (ola anterior = 2024 · levantar el veto por firma · sólo C2) y recomendación. **Respuesta de mesa, verbatim: «Ola anterior = 2024 (Recommended)».** Cableado: SXD persistencia t−1 (2025) e interacción 2024/2025; EXD persistencia t−2 (2024) e interacción 2023/2024, rotulado `PERSISTENCIA-t−2`. `FP-260921-GEN2-DUELO-ENVIPE2026-COMMIT-1-8796-01` FIRMADA. Mientras contestaba se construyó todo lo demás.

## 3 · «Congelado» según D-22 ampliada — salida cruda

| requisito | ORIGEN-MOVIL · ENSAYO-EM · ENSAYO-ADJ | ENVIPE2026-EM · ENVIPE2026-ADJ |
|---|---|---|
| 1 `preflight` VERDE con main fusionado | `PRE-FLIGHT: VERDE` (×3, aviso `spec_md_no_esta_en_origin_main`, primera corrida) | `PRE-FLIGHT: BLOQUEADO input_manifiesto_AUSENTE=envipe2026_csv` (+ `input_repo_ausente=emisiones_selladas` en ADJ, por cadena COMMIT-3a) |
| 2 `_valida_outputs` en cada rama terminal, sintético y oro | vacío (tests 25/25; `run` selló → outputs válidos) | vacío sobre sintético incl. celda rara; **oro no existe** (reserva) |
| 3 todo id posiblemente nulo declarado | sí (`permite_no_estimable` en flotantes/proporciones y en `R-*-N` de un par vetado) | sí |
| 4 ningún input con hash sobre archivo vivo | sí (sellos y TSV sellado) | sí (2026: sin hash posible) |

**Conclusión: procedimiento construido, ensayado y sellable; el COMMIT-1 de 2026 queda NO-CONGELADO por el requisito 1 (payload ausente).** Sucesor «congelar cuando el payload entre»: al entrar `envipe2026_csv` al manifiesto con sha, la única edición autorizada es `preflight` VERDE sobre ese input — ningún parámetro, fórmula ni id cambia (spec §10). Si `preflight` bloquea por otra causa (p. ej. el sufijo de miembro `conjunto_de_datos_tmod_vic_envipe2026.csv` o una columna renombrada), es v1.1 por otra sesión, no parche.

## 4 · Qué falta para el COMMIT-2 (otra sesión, antes del 31/oct/2026)

1. `envipe2026_csv` en `data/manifiesto.yaml` con sha (cola prioridad 1; caja lo baja y hashea; **no** se descomprime ni se lista).
2. `python3 tools/corrida0.py preflight CALC-DUELO-ENVIPE2026-EMISIONES-0001` VERDE → `run` (emisiones, ciegas a R) → sellar.
3. COMMIT-3a: escribir el sha de `…-EMISIONES-0001/resultados.json` en `…-ADJUDICACION-0001/spec.yaml` (`parametros.commit_3a`), nada más. COMMIT-3: `run` de la adjudicación (abre 2026), sellar, registrar, celda-D/marcador por su dueño.
4. Fila L: `NO-ENTRA` salvo firma de mesa con la cita del proveedor (spec §5).
5. El veredicto nacional se lee con `CALC-DUELO-ORIGEN-MOVIL-0001` a la vista (F7 A); el de cruce por la regla v0.3 emitida.

## 5 · Perímetro, pisadas, deuda

Propio: diseño/enmienda/spec + sidecars · `tools/duelo/` (3) · `tests/test_duelo_prospectivo.py` · cinco `data/corrida0/CALC-DUELO-*` (tres sellados, dos `SPEC-FIJADA`) · filas propias en `corridas.tsv`/`resultados.tsv`/`usos.tsv`/`replay-evidencia.tsv`/`censo-tests.tsv` · `forense/analisis/gen2-duelo-envipe2026-commit-1/evidencia-replay-duelo-2026-09-21.json` · fila en `INFRAESTRUCTURA` · FP (2 `ejecutada_en`, 1 nueva) · hallazgo · NC · cascada. Ajeno intacto: ningún CALC existente, `milpa/`, celdas-D, marcador, el guardián. **Pisadas en vistas derivadas: 0** (medido columna por columna contra `origin/main` excluyendo las filas propias; `registro --verifica --escribe --lote` con los tres CALC). Reserva: `registro` deja `origen_numerico`/`envuelto_legacy = INDETERMINADO` para los tres sellados porque su input `serie_nd` es un TSV que cita `CALC-…` por id y no por ruta (NC propia; la cadena real es GEN2).

## 6 · Auditoría (la spec afirma sobre México)

Denunciar o no responde a confianza institucional, costo del trámite y tipo de delito —estructura e instituciones— antes que a disposición; nada aquí se lee como rasgo. Unidad DELITO: ninguna cifra se lee como «proporción de personas». El promedio nacional esconde dónde se concentra el error: el origen móvil ya muestra que K acierta en el agregado con cobertura de IC de 75 % y que TC tiene el menor MAE pero sesgo −0.49 pp y cobertura 50 %; por eso el desglose por eje (E+ vs persistencia; SXD/EXD) es obligatorio antes de cualquier frase de producto. Si en 2026 todos los contendientes yerran en el mismo sentido, es cambio de nivel del instrumento o del mundo, no veredicto. Contadores movidos: `N_corridas_selladas` +3; adopción 0.
