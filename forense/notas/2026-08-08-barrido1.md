# Nota forense · ENCARGO BARRIDO-1 · 2026-08-08

**Cuántos contadores movió este acto: 0.** No abre microdato, no adjudica, no toca manifiesto ni mapa. Es censo de puertas.

Encargo archivado en `forense/encargos/2026-08-08-barrido-1.md` (commit `484ced5`, Estado `VIVO` al arrancar). Ejecutado en worktree `/home/pc0/wt-barrido1-1786050583`, rama `barrido1-1786050583`.

## ARRANQUE (crudo)

```
$ git -C /home/pc0/Modelado-Mexicano status
On branch sesion/cal-conf-faseb-pos4-envipe-paso1
Untracked files: data/raw, data/secretos.local.yaml
$ git -C /home/pc0/Modelado-Mexicano log -1 --format="%h %s"
302ac5a Merge origin/main into sesion/cal-conf-faseb-pos4-envipe-paso1
```
Clon existente confirmado en `/home/pc0/Modelado-Mexicano`, no se clonó uno nuevo. Base no confiable (rama de sesión ajena) — todo lo posterior sale de `origin/main` refrescado, nunca de este checkout.

```
$ git -C /home/pc0/Modelado-Mexicano fetch origin --prune
$ git -C /home/pc0/Modelado-Mexicano log -1 --format="%h %s" origin/main
3d43c8b Merge pull request #154 from Josanoforo/explora2-1786042858
```
Base declarada por el encargo: `9fd49db`. Real: `3d43c8b` — un PR por delante (`#154`, ENCARGO EXPLORA-2, fusionado 2026-08-06T20:58:48Z). `9fd49db` es ancestro directo (parent del merge de #154). No es PARO (§ARRANQUE.2 lo anticipa explícitamente).

Worktree creado sobre `origin/main` real: `git worktree add -b barrido1-1786050583 ../wt-barrido1-1786050583 origin/main` (con dos `error: could not write config file .git/config: Device or resource busy` transitorios — mismo síntoma ya documentado por EXPLORA-2, causado por creación concurrente de otro worktree en la misma máquina; verificado íntegro con `git worktree list` + `git status` limpio).

```
$ git status && git log -1 --format="%h %s"
On branch barrido1-1786050583
nothing to commit, working tree clean
3d43c8b Merge pull request #154 from Josanoforo/explora2-1786042858
```

**ARRANQUE 3 · data/raw:**
```
$ ls data/raw/
ls: cannot access 'data/raw/': No such file or directory
```
Ausente. No es paro — este acto no abre microdato, no se enlazó al corpus compartido (no se necesita).

**ARRANQUE 4 · firma de entorno, cuatro crudos:**
```
$ echo "${CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE:-sin_variable}"
sin_variable
$ curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://www.inegi.org.mx/
200
$ curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://example.com/
200
$ ls data/raw/ 2>/dev/null | head -1
(vacío/ausente)
```
Firma: `sin_variable` + INEGI `200` + control neutro `example.com` `200` + `data/raw` ausente — coincide con la firma Ubuntu-con-red asignada, NO con la firma de bloqueo total reproducida el 7/ago (inegi 403 · banxico 403 · github 200). El control neutro respondió: no hay PARO. `wt-indice2-1786050152` (rama `sesion/indice2-1786050152`) confirmado como worktree separado y activo en la misma máquina — no se tocó.

**ARRANQUE 5 · espejo:** ninguna cifra de este acto sale del espejo del proyecto. Todo lo citado abajo sale de `git show origin/main:<ruta>` contra el clon de (1), con comando a la vista, o de fetches en vivo hechos en esta sesión (declarados puerta por puerta, §2).

**Reconocimiento previo al Paso 1, con comando:** `forense/encargos/convencion.md`, los dos encargos archivados más recientes (`2026-08-07-explora-2.md`, `2026-08-07-verif-3.md`) y sus notas (`2026-08-08-explora2.md`, `2026-08-08-verif3.md`), y `gh pr list` confirmaron que MAP-2 (#150), REPAIR-1 (#151), CI-endurecido (#152), VERIF-3 (#153) y EXPLORA-2 (#154) ya están fusionados — la memoria de sesiones previas que los daba como "abiertos" estaba obsoleta ([[feedback_verify_via_origin_not_worktree]] aplicado de nuevo). Banxico/SIE (CoDi, SPEI) ya está localizado por EXPLORA-1/2: no se re-busca en este acto. El outage de CI está confirmado por PR #155 (`Actions major_outage`) — no se re-diagnostica.

---

## §1 · TAREA A — La lista de necesidades, derivada con receta

### Receta

```
$ cat forense/censo-estimabilidad-coeficientes-v1_0.md   # §5, tabla de 15 filas
$ grep -n 'clase:' milpa/procedencia.yaml                 # bloques condicionales_*
$ sed -n '530,659p' milpa/procedencia.yaml                 # asignados_probabilidad (con calibrable_con:)
$ sed -n '883,902p' milpa/procedencia.yaml                 # rutas_estimabilidad_coeficiente (cruce contra el censo, idéntico)
$ cat milpa/tramite.yaml                                   # 5 reglas, 10 valores, clase: ASIGNADO todos
$ sed -n '1054,1071p' forense/hitoD-preregistro-v2_0.md    # Registro de veredictos archivados -- confirma que ninguna de las 14 tiene veredicto emitido
$ sed -n '59,327p' forense/hitoD-preregistro-v2_0.md       # fichas R1.4..R10.3
$ sed -n '791,850p' forense/hitoD-preregistro-v2_0.md      # ficha R3.4 (Nota 15)
```

Verificado: el reparto `9 SIN-RUTA · 3 RUTA-A · 2 RUTA-C · 1 RUTA-I` del censo v1.0 (4/ago) sigue vigente hoy — no existe `v1_1`, y `procedencia.yaml:883-902` (`rutas_estimabilidad_coeficiente`) cruza idéntico contra la tabla del censo, fila por fila. El registro de veredictos archivados (`hitoD-preregistro-v2_0.md:1054-1071`, único lugar que un test puede leer per ADR-40) confirma que ninguna de las 14 reglas declaradas abiertas por el encargo tiene entrada ahí — siguen las 14, sin veredicto.

### A · Los 15 coeficientes de generador (censo v1.0 §5 / `procedencia.yaml:883-902`)

| # | Necesidad | Ruta hoy | Prioridad |
|---|---|---|---|
| N1 | G1·confianza_institucional — ninguna, RUTA-A ya alcanzada (β̂ marginal, Encargo W) | RUTA-A | — sin necesidad nueva |
| N2 | G1·radio_confianza — ninguna, RUTA-A ya alcanzada; pendiente de signo es W1-P, fuera de este acto | RUTA-A | — sin necesidad nueva |
| N3 | G2·sens_estatus — reactivo. **CERRADO-POR-ADR (ADR-54)** | SIN-RUTA | no se persigue |
| N4 | G2·aversion_riesgo — reactivo. **CERRADO-POR-ADR (ADR-52 A)** | SIN-RUTA | no se persigue |
| N5 | G3·horizonte_temporal — panel (o corroboración de otro panel) que co-observe transición formal/informal de crédito del hogar con un desenlace de crédito, misma muestra, multi-ola. Llave YA sellada vía ENNViH/MxFLS (CAL-G3); falta diseño intra-persona, no instrumento nuevo — pero un panel adicional triangularía | RUTA-I | **ALTA** |
| N6 | G3·aversion_riesgo — mismo reactivo que N4. **CERRADO-POR-ADR (ADR-52 A)** | SIN-RUTA | no se persigue |
| N7 | G3·familismo_apoyo — ninguna, RUTA-A ya alcanzada (β̂ marginal, Encargo W) | RUTA-A | — sin necesidad nueva |
| N8 | G4·exposicion_violencia — reactivo de exposición a violencia (o de "ver/oír/callar") SIN la dependencia estructural de `BP1_23` (solo se pregunta a quien ya fue víctima) — idealmente panel o grupo de comparación no seleccionado por victimización | RUTA-C | MEDIA |
| N9 | G4·confianza_institucional[justicia] — mismo bloqueo y mismo candidato que N8 | RUTA-C | MEDIA |
| N10 | G4·horizonte_temporal — reactivo de horizonte temporal co-observable EN EL MISMO instrumento que los desenlaces de G4 (ENVIPE/ENDIREH); hoy solo vive en ENIF, instrumento distinto sin muestra común | SIN-RUTA | BAJA |
| N11 | G4·sens_estatus — mismo reactivo que N3. **CERRADO-POR-ADR (ADR-54)** | SIN-RUTA | no se persigue |
| N12 | G5·familismo_apoyo — reactivo de familismo de apoyo NO circular con el desenlace de G5 (`familia.seguro.volatilidad_ausencia_estado`); el único conocido (ENIF `p9_9_4`) es la misma batería que el desenlace (marca C3) | SIN-RUTA | BAJA |
| N13 | G5·familismo_obligacion — desenlace co-observable con el proxy (ENUT 6.11/6.11a) — ningún cruce intentado en el corpus | SIN-RUTA | BAJA |
| N14 | G5·radio_confianza — desenlace de G5 co-observable EN EL MISMO instrumento que radio_confianza (ENCUCI); hoy vive en ENIF, instrumentos distintos sin muestra común | SIN-RUTA | BAJA |
| N15 | G6·deferencia — desenlace de G6 co-observable con un proxy de deferencia en un instrumento CON diseño muestral publicado; hoy el único proxy (Latinobarómetro `P4NOIJ`) no trae desenlace propio Y está `SIN_DISEÑO_PUBLICADO` | SIN-RUTA | BAJA |

CERRADO-POR-ADR, confirmado y no reabierto: **N3/N6/N11** (`sens_estatus`, ADR-54) y **N4/N6** (`aversion_riesgo`, ADR-52 A) — dos parámetros, cuatro filas de coeficiente.

### B · Parámetros ASIGNADO de `milpa/tramite.yaml` (5 reglas, 10 valores, ninguno calibrado) + `procedencia.yaml:asignados_probabilidad` (`calibrable_con:`)

| # | Necesidad |
|---|---|
| N16 | `tramite.mordida.discrecional`/`con_registro` — calibración de mordida discrecional bajo sanción creíble/testigo, a granularidad MUNICIPAL (ENCIG solo llega a estatal, `calibrable_con` lo declara) y/o triangulación no-INEGI |
| N17 | `tramite.gobierno_digital.coercitivo`/`util_sin_coercion` (CoDi/SPEI) — **YA con candidata fuerte vía EXPLORA-1/2** (Banxico Informe Anual + SIE, PR #154). No se persigue de nuevo en este acto |
| N18 | `tramite.evasion_norma` — frecuencia de evasión de norma por credibilidad de sanción × utilidad percibida, separando evasión de subsistencia (informalidad) de evasión por cinismo de clase alta |
| N19 | `dinero.ahorro.con_puente_y_respaldo` / `dinero.credito.scoring_alternativo` — el propio archivo pide "reportes trimestrales CNBV por institución" / "CNBV: tasas de rechazo por segmento sin historial" — puerta nombrada explícitamente por el corpus |
| N20 | `civico.denuncia.con_seguro` — el propio archivo declara "ENVIPE no publica esta condicional en esa forma" — necesita fuente que cruce denuncia con tenencia de seguro |

### C · Las 14 reglas abiertas del Hito D (`hitoD-preregistro-v2_0.md`, fichas §3.x — ninguna con veredicto emitido, confirmado contra el registro append-only)

| # | Regla | Necesidad (el "C" de la escala propia de la ficha) |
|---|---|---|
| N21 | R1.4 | Panel de consumo popular D/E con dato de compra/prima de marca vs. sustituto — "hueco declarado" en la propia ficha |
| N22 | R2.1 | Tasa de disenso/reporte de errores ascendente, por tipo de organización (jerarquía tradicional vs. plana), pareada por canal — dato organizacional, probablemente propietario |
| N23 | R2.2 | Panel de clima organizacional con desempeño auditado (rotación/productividad) — dato propietario |
| N24 | R3.4 (el gate) | Condición A ahora con candidata fuerte (series Banxico, vía EXPLORA-2); condiciones B/C necesitan un instrumento que aísle riesgo fiscal percibido de fricción de uso — la propia ficha pre-registra esto como "probablemente inejecutable con fuentes públicas" |
| N25 | R7.1 | Serie municipal pareada de participación electoral por tipo de elección (concurrente/no concurrente) — "granularidad municipal es hueco declarado" |
| N26 | R7.3 | RDD sobre Pensión del Bienestar con efecto electoral independiente de aprobación presidencial — "no existe en fuentes públicas" (ya con candidato ECOLÓGICO parcial vía ENIF+INE, MAP-2) |
| N27 | R7.4/R7.5 | Registro sistemático de respuestas colectivas ante agravio, codificado por entorno urbano/rural — candidato conocido ACLED_HDX ya abierto por VERIF-3 (EXISTE-NO-SATISFACE); necesidad residual es variable de entorno o fuente alterna |
| N28 | R8.1 | Inventario de comités con/sin mecanismo de sanción (fuera de usos y costumbres) — candidatos PUB/CONTRALORIA_SOCIAL_SICS ya abiertos por VERIF-3 (EXISTE-NO-SATISFACE); necesidad residual es tasa de contribución sostenida sin sanción |
| N29 | R8.2 | Dato de plataforma de tandas digitales/ROSCA con tasa de incumplimiento — dato propietario, probablemente inaccesible |
| N30 | R8.3 | Depende de reconciliar `conf.06` — **YA CERRADO por ADR-64** (memoria de sesión), pero la condición C3 específica (`radio_confianza` vs. `cooperacion.confianza.puente_personal`) sigue viva — necesita disposición a transar con desconocidos por nivel de enforcement |
| N31 | R10.1 | Replicación fuera de muestra universitaria (hoy solo Tlaxcala, Félix-Brasdefer) |
| N32 | R10.2 | Panel organizacional con rotación/desempeño — dato propietario |
| N33 | R10.3 | Solo dato secundario YA publicado y agregado sobre disposición a testificar pre/post protección a testigos — nunca recolección primaria (riesgo ético alto si se ejecuta mal) |

**⚠️ ADR-57(c) presente en cada evaluación de puerta**: la pregunta no es "¿tiene el reactivo?" sino "¿tiene reactivo Y desenlace en la misma muestra, y alguna de las tres llaves (panel con desenlace en el instrumento · experimento natural con grupo de comparación · diseño experimental de terceros)?". Columna `llave_ADR57c_si_alguna` del TSV lo registra puerta por puerta.

---

## §2 · TAREA B — El barrido por clase

27 puertas abiertas (o su intento fallido documentado) en las 5 clases, en el orden del encargo. Detalle completo, columna por columna, en `data/universo-puertas-2026-08-08.tsv`; aquí solo el resumen y lo que no cupo en una celda.

**Clase 1 · Otras canastas/descargas masivas de portales públicos (7 puertas).** CNBV (Portafolio de Información real pero es dashboard JS que ninguna herramienta de este acto pudo renderizar — el dataset "Serie R13" hallado por buscador en datos.gob.mx da 404 real, verificado con `curl -kL`), IMSS (`datos.imss.gob.mx` real, 503 en 2 intentos WebFetch, confirmado 200 al tercer intento con `curl -k`), STPS/OLA (real pero agregado, sin aportar puerta nueva sobre ENOE/IMSS), INE (hallazgo fuerte: 6 ZIP reales de participación censal 2009-2024 a nivel municipal — sirve `N25`), CONEVAL (real, solo PDF/agregado en la página abierta), CONAPO (real, remedio de CA de EXPLORA-2 aplicado con éxito — certificado distinto, `*.segob.gob.mx`/GoDaddy, mismo mecanismo de cadena incompleta), Mejoredu/INEE (**NO OBTENIDO EN 3 INTENTOS** — las tres variantes de dominio fallan: sinkhole, DNS ESERVFAIL, timeout). DGIS/CLUES/SESTAD no se re-sondeó — ya conocido por B-4b-alpha (CLUES inalcanzable, SESTAD solo agregado), per instrucción de no repetir sondeos ya hechos.

**Clase 2 · Repositorios académicos (9 puertas) — la de mayor esfuerzo, por ser la de mayor prioridad (`N5`).** Encontrados y verificados: CIDE Panel México 2005-2006 (panel real de 3 olas, pero electoral, host primario caído — **NO OBTENIDO EN 2 INTENTOS**, contenido solo por mirror indirecto), ICPSR Mexico Panel Study 2012 (hallazgo más valioso de la clase: panel real de 2 olas que co-observa compra de voto/violencia CON elección de voto en el mismo instrumento — candidata genuina a llave ADR-57(c) para `N26`/`N27` — pero **NO-ACCESIBLE**, exige Restricted Data Use Agreement), ESRU-EMOVI (real, libre, gratuito, Stata — pero transversal retrospectivo, no panel, y declara explícitamente que NO cubre consumo/crédito/marca, el desenlace exacto de `N21`), COLEF EMIF (real, registro requerido), IPUMS (**hallazgo negativo real**: confirma que no hay panel para México, solo censos y ENOE transversales — cierra la puerta de IPUMS para `N5`, no la abre). UNAM, COLMEX, ITAM y Harvard Dataverse: buscados con términos explícitos, ningún panel propio localizado (`NO-ENCONTRADO`, no "no existe").

**Clase 3 · ONG, sociedad civil y observatorios (5 puertas).** MCCI (hallazgo real: URL de descarga directa `.xlsx` 2019-2024, libre y gratuita — sirve `N16`, aunque mide percepción, no confirmado si mide conducta de mordida ni granularidad municipal), Data Cívica (**NO OBTENIDO EN 2 INTENTOS**, bloqueo 403 en dos rutas — el "Explorador de Datos de Violencia" que serviría a `N8`/`N9` queda SIN-FETCH real), Cero Desabasto (real, sección de datos abiertos confirmada, pero no mapea a ninguna de las 33 necesidades — R4.1/R9.1/R9.2 ya están D-sellados y no son de las 14 reglas abiertas). México Evalúa, IMCO, Signos Vitales e Intersecta quedan en una fila conjunta `SIN-FETCH` — IMCO es la más prometedora (índices de competitividad estatal/urbana reales) pero ninguna de las cuatro se abrió byte a byte, declarado así explícitamente.

**Clase 4 · Organismos internacionales (6 puertas).** BID (defecto técnico real y verificado: `microdatos.iadb.org` redirige 301 a una URL mal formada y autoreferencial, confirmado con `curl -L`/`curl -D-` GET, no HEAD), CEPAL BADEHOG (real, libre, México entre 18 países 2000-2024, variables de ocupación/seguridad social/pobreza — sirve `N18`), OIT/ILOSTAT (bloqueado 403, cifra de 52.4% de informalidad conocida solo por prensa, fuente ENOE ya conocida), Gallup World Poll (**NO-ACCESIBLE real**: microdato a nivel de encuestado es de pago, confirmado por buscador), WVS (portal real y alcanzable 3 veces, pero cobertura de México en la ola vigente no se pudo confirmar — contenido renderizado por JS). Banco Mundial/Findex, LAPOP y Latinobarómetro no se re-sondearon: ya catalogados/en disco per el propio encargo.

**Clase 5 · Transparencia y descarga masiva (2 puertas).** PNT (real, 30M descargas/17M consultas declaradas, pero es infraestructura de solicitudes, no encuesta temática — no mapea a ninguna de las 33 necesidades), Tandas para el Bienestar (**NO OBTENIDO EN 2 INTENTOS** — el dominio no resuelve en ninguna de sus dos variantes, consistente con ser programa del sexenio anterior posiblemente descontinuado; hubiera sido el candidato más natural para `N29` si el dominio viviera). datos.gob.mx/CKAN ya fue sondeado a fondo por EXPLORA-2 (incluida la falla TLS, remedio documentado y reutilizado aquí para CONAPO) — no se repitió esa sonda; sí se usó `datos.gob.mx` puntualmente dentro de otras clases (CNBV, CONAPO) para localizar datasets específicos.

## Suite local y un defecto real encontrado por ella — declarado, no arreglado

`python3 tests/check.py --baseline`, corrida 1/3 (estado natural del worktree: sin `data/raw`, sin `data/raices.local.yaml`): **ROJO — 21 FAIL · 95 WARN, 3 entradas nuevas frente a `tests/baseline.json`** (HEAD congelado `837d5fe`). Diagnosticado línea por línea, no solo citado:

- **T02, la única causada por este acto**: `nombre normalizado colisiona: forense/notas/2026-08-08-barrido1.md · forense/encargos/2026-08-08-barrido-1.md`. Causa raíz: T02 normaliza (aparentemente despoja guiones/puntuación) antes de comparar, y el propio texto del encargo pide los dos nombres con hifenación inconsistente entre sí — `forense/encargos/2026-08-08-barrido-1.md` (PASO 0, con guion antes del "1") y `forense/notas/2026-08-08-barrido1.md` (§5, sin guion) — que normalizan a la misma clave. Ninguno de los dos nombres es discrecional: los dos están fijados verbatim por el propio texto del encargo (PASO 0 y §5 respectivamente) y `tests/**` está fuera del perímetro de este acto. Se declara, no se corrige — mismo precedente que el T02 de directorios-con-punto que DESC-1 declaró sin arreglar (PR #142).
- **T16 ×2 (`canon/estado-programa-v1_10.md:220`, `canon/gobernanza-v1_15.md:760`)**: ambos declaran "18 FAIL · 95 WARN vigente"; excluyendo las propias dos líneas de T16 y la única línea nueva de T02, la corrida real da exactamente 18 FAIL — es decir, **estas dos entradas de T16 no son un defecto preexistente ni de este acto: son consecuencia aritmética directa y esperada de la única fila nueva (T02)**, no una tercera causa independiente. Antes de este acto el baseline real era 18 FAIL · 95 WARN, igual que la última medición registrada (REPAIR-1, PR #151: "18 FAIL · 95 WARN idéntico en ambas corridas").

**Efecto neto medido, no supuesto: este acto mueve el baseline de 18 FAIL a 19 FAIL, por una sola causa (T02), con causa raíz en la propia redacción del encargo, no en el contenido de las 27 puertas ni en la lista de necesidades.** Se abre el PR con este rojo declarado y explicado, per la misma disciplina que DESC-1/VERIF-3 ya establecieron para defectos fuera de perímetro.

## Frontera — dónde paré, y por qué

Paré por la primera de las tres condiciones del §4: **se agotaron las cinco clases en su primera pasada.** No llegué a 40 puertas (27) ni a dos clases seguidas sin hallazgo — las cinco clases dieron al menos una puerta real con necesidad servida. Parar aquí, no seguir profundizando cada clase, es la decisión correcta per §4: "un barrido parcial con frontera declarada vale; uno que se persigue a sí mismo hasta agotar la sesión, no."

**Lo NO barrido, explícito, material de BARRIDO-2:**
- Clase 3: México Evalúa, IMCO, Signos Vitales, Intersecta — ninguna abierta byte a byte, solo buscadas.
- Clase 2: el buscador interno de Harvard Dataverse, ICPSR y CIDE no se abrieron pese a hallar candidatas reales (ICPSR 35024 y CIDE Panel México 2006) — ICPSR quedó en NO-ACCESIBLE por acuerdo restringido, no por falta de intento, pero nadie leyó el codebook de variables completo.
- Clase 4: OCDE se buscó pero no arrojó una puerta propia distinta de ENIF (ya conocido) — no se investigó más a fondo el indicador OCDE/INFE ni el "How's Life" wellbeing dataset.
- No se sondeó ningún dato a nivel CDMX-específico (Portal de Datos Abiertos CDMX, Sistema de Indicadores CDMX) pese a aparecer repetidamente en los buscadores — quedó fuera por ser subnacional-capital, no nacional.
- El MCCI `.xlsx` y el catálogo real de CONAPO (portal SPA) no se descargaron/parsearon — se confirmó la puerta, no se leyó el contenido, per el propio criterio de censo ("saber qué existe... no leer el dato").
- No se re-exploró DGIS/CLUES/SESTAD (ya conocido) ni Banxico/SIE (ya conocido) ni ACLED_HDX (ya abierto por VERIF-3) ni ENNViH/MxFLS (ya sellado) — deliberado, per el propio encargo.

## Qué necesidades de §1 ganaron candidata, y cuáles quedan huérfanas — el saldo real del acto

**Ganaron candidata real esta sesión (7 de 33):** `N16` (MCCI), `N18` (IMSS + CEPAL BADEHOG + OIT/ILOSTAT + BID, ninguna satisface del todo), `N19` (CNBV, débil — portal confirmado pero dashboard ilegible), `N21` (ESRU-EMOVI, parcial — le falta el lado de consumo), `N25` (INE, fuerte — falta solo la comparación con elección local concurrente), `N26` y `N27` (ambas vía ICPSR Mexico Panel Study 2012 — candidata conceptualmente muy fuerte, con llave ADR-57(c) plausible, pero NO-ACCESIBLE por acuerdo de datos restringido).

**Tocadas pero sin candidata resuelta (4 de 33):** `N8`/`N9` (Data Cívica bloqueada 403, contenido solo SIN-FETCH), `N15` (Gallup es de pago, WVS no confirmó México), `N29` (Tandas para el Bienestar parece dominio muerto).

**Ya servidas antes de este acto, no perseguidas (4 de 33):** `N1`, `N2`, `N7` (RUTA-A), `N17` (CoDi/SPEI vía EXPLORA-1/2).

**Cerradas por ADR, no reabiertas (4 de 33):** `N3`, `N4`, `N6`, `N11` (`sens_estatus`/ADR-54, `aversion_riesgo`/ADR-52 A).

**Ya tocadas por otros actos recientes, no perseguidas de nuevo (2 de 33):** `N24`/R3.4 (condición A servida por EXPLORA-2; B/C pre-registradas como probablemente inejecutables), `N28`/R8.1 (candidatos ya abiertos por VERIF-3).

**Huérfanas — sin ninguna candidata, ni antes ni en este acto (12 de 33):** `N5` **(la de mayor prioridad — ALTA — y la que más esfuerzo de barrido recibió en este acto; el resultado real es que NO existe hoy un panel mexicano públicamente conocido que co-observe horizonte temporal de crédito del hogar con un desenlace de crédito, más allá de ENNViH/MxFLS, que ya se conocía)**, `N10`, `N12`, `N13`, `N14`, `N20`, `N22`, `N23`, `N30`, `N31`, `N32`, `N33`.

El saldo honesto: este acto amplió el universo conocido de puertas en 7 necesidades (de las cuales solo 2 — `N25` y las dos de `N26`/`N27` — tienen una candidata que vale la pena que otro acto abra a profundidad), confirmó por qué 4 necesidades quedan cerradas por diseño, y dejó documentado con evidencia fresca (no supuesta) que la necesidad de mayor prioridad del programa (`N5`) sigue sin puerta — no por falta de búsqueda, sino porque, hasta donde este acto pudo verificar hoy, esa puerta no existe en el universo público conocido.

## forense/hallazgos.md

Ver commit de cierre — una entrada, append.

