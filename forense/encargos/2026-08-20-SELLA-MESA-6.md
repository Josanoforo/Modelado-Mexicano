# Encargo · SELLA-MESA-6

**Estado: CONSUMIDO** — ejecutado íntegro por `ACTO SELLA-MESA-6`, 2026-08-20. Detalle completo: `forense/notas/2026-08-20-sella-mesa-6-cierre.md`. Decisión sellada en `canon/gobernanza-v1_15.md`, `ADR-135`.

## Encargo, verbatim (resumen operativo)

Sellar las seis filas `FP-79`..`FP-84`, abiertas por `ACT-PIL-2` el 20/ago/2026, con un solo ADR:

- **`FP-79`** → `FIRMADA`. Adoptar la escala oficial de INEGI para CV, cortando en «baja precisión»: dos escalas por tipo de encuesta (hogares / unidades económicas), citando `ADV1-M1` y la ficha RNM 922. Abrir fila nueva `ABIERTA` sobre el matiz pendiente de `ESP-OPACA-B-d13ec4fe` sin re-etiquetar (perímetro de producción, fuera de este acto).
- **`FP-80`** → `FIRMADA`. Sin piso de `n` ex ante como filtro de admisión.
- **`FP-81`** → `CERRADA POR PREMISA REFUTADA`. El filtro (i) sí es ejecutable desde una caja sin navegador, en dos pasos. Abrir fila nueva `ABIERTA` con ese diseño, asignada a Ubuntu.
- **`FP-82`** → `FIRMADA`. Marco a saturación antes de sortear; semilla anterior anulada.
- **`FP-83`** → `FIRMADA`. Árbitros sin error muestral llevan banda propia pre-registrada; regla sellada, banda por celda diferida a fila nueva `ABIERTA`.
- **`FP-84`** → `FIRMADA`. Cruce contra censo nuevo; re-censo acotado a las fuentes que cambiaron de estado, diferido a fila nueva `ABIERTA`, asignada a Ubuntu.

Perímetro estricto de escritura: `canon/gobernanza-v1_15.md`, `forense/firmas-pendientes.tsv`, `forense/hallazgos.md`, `forense/encargos/` (este archivo), `forense/notas/2026-08-20-sella-mesa-6-cierre.md`. Prohibido tocar `milpa/`, `data/`, `tests/`, `corpus/`, `canon/modelo-decision-v4_0.md`, o cualquier `resultado.tsv`.

## Resultado

- `ADR-135` en `canon/gobernanza-v1_15.md` (máximo ADR verificado antes de escribir: `134`).
- `forense/firmas-pendientes.tsv`: `FP-79`/`FP-80`/`FP-82`/`FP-83`/`FP-84` → `FIRMADA`; `FP-81` → `CERRADA POR PREMISA REFUTADA`; cuatro filas nuevas `ABIERTA` — `FP-92`, `FP-93`, `FP-94`, `FP-95` (máximo FP verificado antes de escribir: `91`).
- `forense/hallazgos.md`: tres líneas nuevas (patrón de negativo por conteo sin inspección; el índice de canastas resuelve una prueba declarada inejecutable; los tres números del encargo se re-verificaron y coincidían).
- `forense/notas/2026-08-20-sella-mesa-6-cierre.md`: nota del acto, comando por comando, con la sección de citas no verificables omitidas en vez de inventadas.
- `python3 tests/check.py --baseline`: `LÍNEA BASE: ROJO`, dos entradas nuevas (`T15`/`T16`) sobre `canon/estado-programa-v1_10.md`, que queda fuera del perímetro de este acto — declarado, no corregido aquí.

No se tocó `milpa/`, `data/` (salvo lectura), `tests/` (salvo ejecución sin escritura), `corpus/`, `canon/modelo-decision-v4_0.md`, ni ningún `resultado.tsv`. `data/diseno-muestral.yaml` se cita, no se edita.

## CONSUMIDO

Derivado por auditoría 2026-09-03 (ACTO MAESTRA37-N9 · AUDITA-ENCARGOS-166). `grep -Fc -- "2026-08-20-SELLA-MESA-6.md" canon/gobernanza-v1_15.md` → 1: citado bajo ADR-135 en canon/gobernanza-v1_15.md, con lenguaje de ejecución (archivado/ejecutado) en el bloque correspondiente. Marca ausente en el archivo era defecto de trámite, no evidencia de no-ejecución.
