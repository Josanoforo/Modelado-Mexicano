# ADR-260924-GEN2-RELEVO-MOTOR-34-1-a157-01

24/sep/2026 · `ACTO GEN2-RELEVO-MOTOR-34-1` (NUBE). De las 34 lecturas legacy con que el motor todavía contesta, 9 salen a GEN2 por el escritor (`tools/escribe_relevo_consumo.py --relevo-motor-34`) con las guardas de 4.1: cuatro por vía (i) desde ENVIPE 2025 (denuncia con/sin seguro, `p` al RESULT a seis decimales) y cinco complementos `1−p` por vía (iii) desde dos CALC derivados nuevos y sellados sobre `CALC-ENCIG-0001` y `CALC-ENCUCI-0001` (`p` sin cambio). `legacy_activas_por_consumidor__motor` 34 → 25, `adoptados_activos` 72 → 81, en árbol re-derivado y pendientes de merge de mesa. Residuo 25, una NC por lectura: 8 ASIGNADO que `emitir_binaria` devuelve aunque la regla ya tiene su conducta medida, 4 NO-ADOPTAR-NC-0107, 6 cortes categóricos sellados (mesa, FP `a157-01..03`) y 7 que exigen re-medición en CAJA (L8 ingiere un JSON GEN1; ENNViH; gobierno digital coercitivo). Nota: `forense/notas/2026-09-24-GEN2-RELEVO-MOTOR-34-1-nota.md`.
