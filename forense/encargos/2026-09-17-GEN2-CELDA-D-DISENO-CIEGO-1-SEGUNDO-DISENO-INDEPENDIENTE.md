ENCARGO · ACTO GEN2-CELDA-D-DISENO-CIEGO-1 · SEGUNDO DISEÑO INDEPENDIENTE DEL PRIMER PILOTO CELDA-D
CABECERA · redactado contra `b881ee6` (re-deriva al abrir; `main` se habrá movido por M1-ALCANCE-1 — no es PARO, cita el SHA real) · ENTORNO: cualquiera sin microdato — aunque `data/raw` esté montado, no se abre ningún payload (esto es diseño antes del dato; A.15b: la construibilidad se lee de inventarios, FD y catálogos del repo) · COMPUERTA: ninguna · MODELO SUGERIDO: Opus · FP/ADR/NC: deriva al cierre, no heredes · vehículo: `/acto`.
CIEGO — la regla que define este acto. Dirección escribió su propio diseño v1.0 el 17/sep y no está en el repo. Este acto no lo lee, no lo pide y no lo infiere. Si mesa lo pega en el lanzamiento, PARA y repórtalo: el valor de este acto es que dos diseños se carean sin haberse visto. Tampoco se lee ningún retorno externo sobre el mismo tema (no hay ninguno archivado; verifica con `ls forense/notas/insumos-externos/ 2>/dev/null`).
FIRMA DE MESA que gobierna (verbatim, del acto M1-ALCANCE-1 — mesa la pega aquí tal como la lanzó): [FIRMA M1 — A o B]. En sustancia: cada celda tiene su estimador, su ruta y su dato; el motor no tiene un estimador único; la matriz compone y, donde compita, es un candidato más — nunca el estimador por defecto.
VERIFICACIÓN DE EXISTENCIA (A.8, dirección, contra `b881ee6`):

* (1) Estructura: Dominio 5 del índice (`data/INFRAESTRUCTURA-v1_0.md:157`, "Registrar una celda-D del piloto") y Dominio 8 (notas). Cubre.
* (2) Contenido: `grep -rln "CELDA-D-PILOTO\|piloto celda-D\|piloto de celda-D" --include=*.md --include=*.tsv .` → 1 archivo, `PROPUESTA-remediacion-brecha-documental.md:22`, que solo dice "no toca el piloto celda-D" (2 132 archivos examinados). NO-ENCONTRADO: no existe diseño del primer piloto celda-D en el árbol. Las 3 celdas-D registradas (`data/curacion-registro/celdas-d/`) son insumos θ, no pilotos.
* (3) Cobertura retroactiva: el contrato celda-D nació el 11/ago (ADR-68) y nunca ha corrido una celda con ≥2 candidatos ejecutables; no hay trabajo anterior que pueda estar oculto a las tablas.

PIEZAS (las cuatro del brief externo, adaptadas a quien tiene el clon)
P1 · Elige la primera celda-D y di por qué no las otras. Universo obligatorio, derivado por comando y con conteo: las 3 celdas-D; las entradas del árbitro con puntos por eje (`milpa/tramite-ola5-propuesta-v0.yaml`, patrón `- id: *_ejes_*`); el catálogo de momentos (`milpa/catalogo-momentos-v0_1.tsv`); la demanda activa (`data/corrida0/demanda-resultados.tsv`, tipo `conducta_p_medido` con consumidor). Siete criterios, todos necesarios y cada uno con `archivo:línea`: (a) estimando con escala y universo declarados; (b) población expresable en cortes del modelo y en celdas del árbitro vía `data/crosswalk-ejes-arbitro-modelo-v1_0.tsv` (di qué resolución se pierde en los MAPEO-N-A-1); (c) dato y su ola anterior ya en `data/manifiesto.yaml`; (d) ≥2 candidatos elegibles de familias distintas; (e) persistencia construible — verificada contra `data/inventario-reactivos-v1_2.tsv` con patrón y conteo de filas (A.15a), nunca contra una versión superada; (f) consumidor identificado en la demanda; (g) evaluación no vista por el candidato evaluado (U3 ya fue observada: no cuenta). Entrega tabla de eliminación y una sola ganadora. Si ninguna pasa los siete, ese es el hallazgo y P2 no se escribe.
P2 · Pre-registro de esa celda: YAML conforme a v0.5 (`propuesta-motor-adaptativo-celda-v0_5.md` §3; v0.3 §2-3 para lo que remite) + prosa. Obligatorio: estimando con escala única y universo (restringido → a quién); población en cortes del modelo y celda del árbitro; candidatos mínimo tres — persistencia como `BASELINE_INGENUO`, la matriz `B·θ(x)→h_r` como challenger de `estrategia: momentos` diciendo con nombre qué le falta para siquiera correr (θ por nombre, `h_r`, magnitud de G5 si la ruta la toca), y al menos uno más (L con dieta declarada, medición transversal directa si no es circular con el árbitro); criterio de adjudicación antes del dato con escala y umbral, citando verbatim las dos condiciones `INDECIDIBLE` de `forense/CAREO-ADV-DUELO-diseno-v2-2026-08-19.md` §B (M3) y diciendo si adoptas `skill = 1 − error/error(b)`; qué significa que nadie refute a b (B-bis: fila para "sobrevivió", o la escala está mal); incertidumbre tipada por candidato (una familia cada uno, sin banda universal); emitir ≠ decidir (qué se emite si cruza el umbral del consumidor y cuándo se abstiene la decisión automática); desarrollo vs evaluación, con lo que contamina dicho sin adorno; parada (factibilidad vs desempeño); y lo que el diseño no decide. Escala: nunca comparar proporción contra índice sin enlace (A-bis 3); universo restringido nunca contra poblacional (A-bis 4).
P3 · Adversarial de la política "cada celda su estimador" como política de motor. Modos de falla concretos, cada uno con `nombre · mecanismo (una frase) · ejemplo mínimo con números · síntoma observable · mecanismo mínimo que lo atrapa (una regla o un test, no una capa) · estado en el contrato: YA-PREVISTO (cita v0.3/v0.5/`forense/RONDA1-motor-adaptativo-celda-veredicto-fable-2026-08-11-v1_0.md`) / PREVISTO-SIN-MECANISMO / NO-PREVISTO`. Mínimo: selección post-hoc; comparaciones múltiples; incoherencia entre celdas (¿basta D8?); persistencia como piso insuperable; sobreajuste al árbitro (cómo se detecta, no solo que está prohibido); circularidad en corpus de una ola; heterogeneidad de forma (banda vs punto) al componer; extrapolación fuera de soporte; y el costo del contrato: cuántas celdas puede adjudicar el programa con el dato que tiene antes de que adjudicar cueste más que lo que decide. Cruza cada falla con `forense/hallazgos.md`: `YA-OCURRIÓ-AQUÍ (cita)` / `CONCEBIBLE` / `NO-APLICA`. Cierra con la pregunta: ¿cuáles de estas fallas hacen que "cada celda su estimador" sea peor que "un estimador único con etiqueta" para quien lee salidas por celda? — con argumento.
P4 · La interfaz que la composición exige (acotada, ≤3 páginas). Si la matriz compone insumos heterogéneos (`propuesta-motor-matriz-v0_1.md:73-79`), qué campos mínimos necesita cada entrada del catálogo de momentos para que un `h_r` consuma una celda que entrega banda y otra que entrega punto, con π(x) sobre cortes que el árbitro cubre a mitad de resolución; qué está prohibido (colapsar una banda a su punto medio en silencio, entre otros); qué prueba pre-composición detecta una escala mal declarada. Si la composición no es viable con estimadores de forma distinta, dilo con el argumento.
ENTREGABLE: `forense/notas/2026-09-17-GEN2-CELDA-D-DISENO-CIEGO-1.md` (P1–P4, YAML de P2 inline) con cabecera de universo (A.10): SHA real al abrir, archivos examinados por pieza, conteos. Módulo de auditoría de rigor extremo: sí carga (P3 afirma sobre el modelo), incluida la pregunta v2.1 y la v2.4 (escala). Toda cita `archivo:línea` re-verificada por comando antes de cerrar (precedente ADR-530: 31 citas re-verificadas).
PERÍMETRO Y CONCURRENCIA: `forense/notas/2026-09-17-GEN2-CELDA-D-DISENO-CIEGO-1.md` (nuevo) · `forense/encargos/` (archivo verbatim de este encargo, 0-bis A.3) · cascada de `/acto` (ADR, cabeceras, `registro-rotulos` si aparece rótulo pelado, `tests/check.py` solo por T25). No toca `data/curacion-registro/celdas-d/` (no registra la celda: eso es del acto que la lance tras el careo), `milpa/`, `data/corrida0/`, `firmas-pendientes.tsv`, `no-corrido.tsv` (el careo abre las filas de mesa, no este acto), specs ni resultados. En paralelo: GEN2-M1-ALCANCE-1 (gobernanza, tablero, `matriz.py`, propuestas v0.1–v0.5, `hallazgos.md`) — si al abrir ya fusionó, cita su ADR y lee las propuestas con sus enmiendas; si sigue corriendo, no toques ninguno de sus archivos; `/deriva` y el servicio de adquisición — sin archivo común. «Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.»
CONTADOR: cero mediciones, dicho sin disfraz; produce diseño. LO QUE NO HACE: no abre microdato · no registra celda-D · no adjudica · no elicita a L · no propone números del modelo · no abre filas de mesa · no lee el diseño de dirección. SUCESOR: `GEN2-CELDA-D-CAREO-1` (nube, Opus): archiva el diseño de dirección (sha256 `c7d55e61…`, A.3) junto a éste, carea fila por fila (misma celda o no, criterios que uno tiene y el otro no, fallas de P3 cruzadas), y escribe la hoja de decisión para mesa; ahí nacen las filas FP/NC. CIERRE: cascada + `## NO-CORRIDO / RESERVAS` + `## CONSUMIDO`.

## NO-CORRIDO / RESERVAS

| qué | por qué | impacto | sucesor |
|---|---|---|---|
| **`P2`** — «Pre-registro de esa celda: YAML conforme a v0.5 … + prosa» | **EJECUTADO COMO `P1` LO MANDA, no omitido.** `P1` ordena literalmente: «*Si ninguna pasa los siete, ese es el hallazgo y P2 no se escribe.*» Ninguna de las 12 grupos de candidatas del universo obligatorio pasa los siete criterios — y el negativo se sostiene bajo **las dos** lecturas del criterio (g), lo que quita la interpretación de en medio (nota §1.5, §1.7) | No hay celda-D pre-registrada, no hay criterio de adjudicación, no hay umbral, y **no se adopta ni se rechaza** `skill = 1 − error/error(b)`. El contador de celdas-D pre-registradas no se mueve: sigue en 0 | `DIFERIDO-A: GEN2-CELDA-D-CAREO-1` — el careo decide si dirección sí encontró una celda y bajo qué operacionalización; las tres divergencias candidatas quedan nombradas en el Cierre de la nota |
| **Filas `NC-NNNN` en `forense/no-corrido.tsv`** que la cascada `/acto` paso 10 exige normalmente | `FUERA-DE-PERÍMETRO`, por instrucción explícita de este encargo: «No toca … `no-corrido.tsv` (**el careo abre las filas de mesa, no este acto**)». El perímetro manda sobre el paso 10 de la skill, y decirlo vale más que el atajo | `tools/cierre_acto.py` Fase A puede reportar `NO-CORRIDO-AUSENTE`/`NC-HUÉRFANA` sin filas nuevas de este acto; ninguno bloquea el commit. Las reservas viven aquí y en la nota, verbatim, para que el careo las transcriba | `DIFERIDO-A: GEN2-CELDA-D-CAREO-1` — «ahí nacen las filas FP/NC», palabra del propio encargo |
| **Filas `FP-NNN` en `forense/firmas-pendientes.tsv`** | `FUERA-DE-PERÍMETRO`, misma cláusula. Las cuatro condiciones (i)–(iv) de la nota §1.7 son candidatas naturales a firma de mesa —en particular firmar `localidad` en el crosswalk, que hoy es `MAPEO-N-A-1` **sin firma**— y **no se abren aquí** | Mesa no recibe hoy ninguna fila firmable de este acto; las recibe del careo, con las de dirección al lado, que es el punto | `DIFERIDO-A: GEN2-CELDA-D-CAREO-1`; `FP-376` (ya abierta por `GEN2-CROSSWALK-EJES-1`) es el sitio natural de la condición (i) |
| **La `FIRMA DE MESA` verbatim** que la cabecera dice pegar: llegó como marcador literal `[FIRMA M1 — A o B]`, sin sustituir | `DECISIÓN-DE-MESA-PENDIENTE`. **No es PARO**: los dos PARO que este encargo declara son (i) que mesa pegue el diseño de dirección y (ii) escribir fuera del perímetro, y ninguno ocurrió. El acto tomó como gobernante la **sustancia** que el propio encargo escribe en la línea siguiente, citada verbatim en la nota §0.3 | Todo `P3` se escribió contra la paráfrasis, no contra la firma. Si el verbatim A-o-B difiere en sustancia, `P3` se lee contra el texto equivocado y hay que re-leerlo — no re-correrlo: ninguna medición depende de él | `DIFERIDO-A: GEN2-CELDA-D-CAREO-1` — pega el verbatim junto a los dos diseños y dice si cambia algo |
| **`A.8 (3)` de la cabecera**, «nunca ha corrido una celda con ≥2 candidatos ejecutables» | **DISTINTO DE LO MEDIDO, corregido sin reescribir la cabecera.** `G5.radio_confianza.encuci_vs_enbiare` tiene dos candidatos y los dos corrieron (`:83` `vigente`, `:106` `EJECUTADO — ENCARGO 9`). Lo que nunca ha ocurrido es que **una celda-D se adjudique por competencia de estimadores contra una evaluación**: esa celda cerró por vinculación-invarianza y `champion_actual` no cambió; y las tres celdas-D traen `momentos_holdout_refs: []` | La premisa de cobertura retroactiva se sostiene igual, pero por otra razón; el careo debe carear «≥2 candidatos» contra «una evaluación», que no es lo mismo | Ninguno abierto — corregida en la nota §0.2, se dice para que no se herede |
| **Dos citas del árbol encontradas vencidas y NO corregidas** | `FUERA-DE-PERÍMETRO`. (a) `data/INFRAESTRUCTURA-v1_0.md:159` dice que `celdas-d/` tiene «hoy **2 archivos**»; hay **3** desde el 13/ago. (b) `milpa/tramite.yaml:712` cita el origen de la segmentación como `milpa/tramite-ola5-propuesta-v0.yaml:1162-1346`; la entrada vive hoy en `:1415-1599` — resuelve por `id`, no por línea. Ninguno de los dos archivos está en la lista de este acto | Dos negativos falsos en potencia para quien derive de esas líneas. Ninguno afecta a `P1`–`P4`: ambas se re-derivaron por comando aquí | `SIN-ASIGNAR` — se asientan en la nota (§0.2, módulo de auditoría v2.1) para que un acto de mantenimiento las tome |
| **`P1`, la pieza «una sola ganadora»** | **DISTINTO DE LO PEDIDO porque el propio `P1` lo previó.** Se entrega la tabla de eliminación completa (12 grupos × 7 criterios) y **cero ganadoras**, más las dos más cercanas nombradas con qué les falta exactamente | Quien esperara una celda para lanzar no la tiene. Quien quiera lanzarla sabe hoy, por comando, las cuatro cosas que faltan y quién las cierra (nota §1.7) | `DIFERIDO-A: GEN2-CELDA-D-CAREO-1` |
| **El guion de las 185 anclas de la pasada 2 de re-verificación** | `FUERA-DE-PERÍMETRO` — vive en el scratchpad de la sesión; `tools/` no está en la lista de este acto | La pasada 1 (**78/78** citas) **sí** es reproducible desde la nota, con el comando inline. La pasada 2 se puede reconstruir leyendo la nota, que cita cada ancla en su sitio | `SIN-ASIGNAR` — si mesa quiere el verificador de citas como herramienta del árbol, es acto propio |
