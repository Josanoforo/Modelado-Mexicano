ENCARGO · ACTO MAESTRA32-E11 · COBERTURA-15 (re-emisión de E10)
SHA de redacción: `2799132` (main, merge PR #397 / `ADR-225`) · Redactado: 30/ago/2026, dirección maestra-32 · Instrucciones vigentes: v2.11 · Estado: LISTO PARA LANZAR — sin compuerta y sin ranuras. Sustituye a `MAESTRA32-E10 · COBERTURA-15`, que cerró por hallazgo antes de COMMIT-1 (`ADR-224`, PR #396): su compuerta era "E9 fusionado" y se lanzó antes del merge de E9. El objeto, la receta y el B-bis son los mismos; cambia solo lo que el árbol cambió.
ENTORNO ASIGNADO: NUBE (`cloud_default`). NO se lanza en UBUNTU — lee tablas versionadas; no abre payloads; no mide nada.
CARRILES EN PARALELO (declarado): carril NUBE = `E11 (este)`; carril CAJA = `E3 · EXTRACTOR-DTA → E8 · MEDICION-COMPUESTA`. Compartidos: solo la cascada (`gobernanza`, `estado-programa`, `registro-rotulos`, `firmas-pendientes`, `tests/check.py` T25). Renumera quien fusiona segundo.
Rótulos. `MAESTRA32-E11`. `E10` queda consumido por `ADR-224` (no se reutiliza). `E4` sigue reservado (RE-EMPAREJA).
EMISIÓN CIEGA (firma D1, condición 4). Este acto no abre `forense/prereg-duelo-v2/corridas-R/` ni ningún archivo con valores publicados de las 15 celdas. Del marco congelado lee solo las columnas que definen la celda (`id … estrato`; ninguna trae el valor). Lista en la nota todos los archivos abiertos.
FIRMAS QUE ESTE ACTO EJECUTA Y PROPAGA — sin ranuras
D1 = (i), registrada por `ADR-225` (E9) con este texto: mesa autoriza crecimiento acotado del motor antes del GO —excepción declarada a `APERTURA-FASE-CALCULO-v1_2.md` §48 / OLA 5 post-GO— para sellar los enlaces regla→desenlace que las 15 celdas sorteadas ya anticipan en su `frase_discriminacion`; acotado a esas 15; sin tocar el marco congelado ni R; cada enlace se sella con desenlace medido en caja bajo pre-registro de dos commits; el acto que escribe enlaces no lee `corridas-R`. Este acto produce el plan celda por celda; las mediciones que sellan son actos de caja sucesores.
`FP-181` → FIRMADA, opción (a). `FP-181.gatea` pedía a mesa elegir entre (a) redactar y fusionar E9 y relanzar el encargo, (b) levantar la compuerta con firma, (c) reordenar el carril. Mesa, 30/ago, tras revisar #396/#397: "Revísalo y dame los siguientes encargos." — E9 ya fusionó (`ADR-225`), la condición de (a) está cumplida y este acto es el relanzamiento. Se propaga como (a), con la cita al hallazgo (`ADR-224`) intacta.
════════ ARRANQUE — hazlo antes de leer el resto del encargo ════════
Reporta las cinco líneas de abajo y NO empieces hasta tenerlas. Si algo no cuadra, PARA y repórtalo: encontrar que el terreno no es el que el encargo supone es entregable, no interrupción.
1 · REPO. Localiza el CLON EXISTENTE. No clones uno nuevo salvo que no haya ninguno, y si clonas, dilo. Reporta: ruta absoluta · git log -1 --format="%h %s" · git status ⚠️ No arranques desde el home. Si el cliente avisa "launched in your home directory", cámbiate al clon antes de nada.
2 · SHA. Confirma contra qué base trabajas y compáralo con el que el encargo declara. Si main se movió: NO es PARO — refresca, re-deriva lo que dependa del perímetro, y reporta la diferencia antes de editar.
3 · data/raw. AUSENTE NO ES PARO. Es raíz integrada, gitignorada, resuelta por código; un clon fresco siempre nace sin ella. Se crea o se enlaza. Reporta: existe / la enlacé a <ruta> / la creé. ⚠️ Si este acto DESCARGA algo: verifica al cerrar que los payloads quedaron en el CORPUS COMPARTIDO y no solo en tu worktree. Es el defecto de PR #77 y no lo atrapa ningún test.
4 · ENTORNO. "Ubuntu" no distingue nada; la política de red sí. CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE → esperado: sin_variable curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://www.inegi.org.mx/ Reporta los dos valores crudos. NUNCA curl -I. Si este acto no toca microdato ni red, dilo y salta este punto. ⚠️ [NUEVO v2.11] Un negativo producido por un comando que no examinó archivos no es un negativo (A.13). Todo veredicto negativo —incluida la sonda de este punto— declara cuántos archivos examinó el comando que lo produjo.
5 · ESPEJO. Prohibido derivar cifras del espejo del proyecto: está versiones atrás del repo y contiene archivos que el repo nunca tuvo. Toda cifra sale del clon de (1), con el comando a la vista.
════════════════════════════════════════════════════════════════════
A.2, tercera parte: `ls data/raw/ 2>/dev/null | head -1` — se espera ausente. Los TSV de inventario llevan cabecera `#`: sáltala antes de `csv.DictReader`. Búsquedas en Python UTF-8 (A.13). T03: toda cita de archivo con ruta completa entre backticks.
═══ VERIFICACIÓN DE EXISTENCIA — contestada por dirección (maestra-32, 30/ago/2026, contra `2799132`) ═══
1 · ESTRUCTURA. Familia `forense/prereg-duelo-v2/` (marco congelado, enlace-M, corridas-L/M/R, scoring). Ya indexada: `command grep -c "duelo\|prereg" data/INFRAESTRUCTURA-v1_0.md` → 5 (verificado por el propio E10, `forense/notas/2026-08-30-cobertura-15-cierre.md:17`). No aplica regla de conducto; este acto no toca el índice.
2 · CONTENIDO.

* Las 15 celdas, leídas del marco congelado por `id_celda` de los 15 `corridas-M/M-*.json`: `CIV-08` ENVIPE 2023 `AP4_4_03` · `DIN-03` ENIF 2012 `P7_1` · `DIN-05` ENFIH 2019 `P8_1_1` · `DIN-07` Banxico/CNBV competencias financieras 2019 `SF2` · `DIN-11` ENIF 2018 `P5_3` · `SFT-04` ENASEM 2018 `H16D_18` · `SFT-06` ENASEM 2024 `F55_24` · `TIC-01` ENOE 2024-T1 `p3i` · `TIC-06` ENTI 2022 `P2` · `TIC-08` ENDUTIH 2024 `P7_15` · `TIC-12` ENOE 2024-T1 `p3n` · `EMP-02`/`EMP-04` ENAFIN 2024 (razones derivadas, unidad empresa) · `EMP-05` CPV 2020 `SITUA_CONYUGAL` · `DOC-06` BMV/HR Ratings, desenlace documental.
* Obra previa, y es toda: `forense/prereg-duelo-v2/enlace-M-v1_0.md:33-35` ("1 EMITE (`CIV-01`), 59 NO-EMITE; sobre las 15 sorteadas: 0 EMITE"); `data/crosswalk-pregunta-regla-v1_1.tsv` (`ADR-208`); y el intento E10 (`ADR-224`), que no corrió COMMIT-1 ni COMMIT-2, no creó tabla ni spec, y no leyó ninguna columna de valor (`cobertura-15-cierre.md:48`). Plan por celda: NO-ENCONTRADO (listado completo de `forense/prereg-duelo-v2/` hoy: sin `cobertura-15-v1_0.tsv`; `forense/notas/`: existe el cierre de E10, no existe `cobertura-15-spec.md`).
* Motor hoy: `coeficientes_generador_sellados` = 6 entradas, 4 con `valor_ejecutable` (`yaml.safe_load`, hoy).

3 · COBERTURA RETROACTIVA. El enlace-M (26/ago) se derivó antes de `inventario-fd` (27/ago), `v1_1`/`v1_2` (28-30/ago) y de los 4 coeficientes ejecutables (28-30/ago). Su "0 de 15" es correcto sobre su universo y VENCIDO EN ALCANCE (A.10): este acto es su re-sello.
═══════════════════════════════════════════════════════════════════
0-bis · A.3
Primer commit: este encargo verbatim en `forense/encargos/2026-08-30-MAESTRA32-E11-COBERTURA-15.md`. Al cerrar, `## CONSUMIDO` con el PR.
Objeto
Para cada una de las 15 celdas, contestar cuatro preguntas con comando y veredicto A.4, y entregar el programa de medición que la firma D1 ordena: (1) ¿qué `(regla, conducta)` del motor anticipa la frase, y existe esa regla en `milpa/tramite.yaml`/`milpa/procedencia.yaml`/`canon/modelo-decision-v4_0.md`? (2) ¿qué θ (parámetro del motor) mueve esa regla, según `modelo-decision` §2.1-2.2? (3) ¿esa θ tiene operacionalización en el mismo instrumento y ola de la celda (`data/inventario-reactivos-v1_2.tsv` + `data/inventario-fd-v1_1.tsv`, filtrados por instrumento)? — co-observación por construcción, porque el desenlace es la variable de la celda; (4) ¿la variable de la celda está en el corpus a nivel variable (el payload está abierto)?
COMMIT-1 — receta congelada ANTES de tocar las tablas
`forense/notas/2026-08-30-cobertura-15-spec.md`: (a) cómo se extrae el id de regla de cada frase (regex sobre `[a-z_]+(\.[a-z_]+)+` + lectura humana citada; si la frase no nombra regla, se deriva del generador por dominio y se dice que es derivación de este acto); (b) criterio de "regla existe" (clave presente en `tramite.yaml`/`procedencia.yaml` o regla SI-ENTONCES en `modelo-decision`, citada archivo:línea); (c) por celda, la θ y su lista cerrada de términos/regex (con y sin acentos) sobre `variable_id` y `texto_reactivo`, filtradas a `instrumento == encuesta+ola de la celda`; (d) criterio de CANDIDATO y exclusión de circularidad (θ operacionalizada por la propia variable de la celda = `CIRCULAR-EXCLUIDO`, precedente ENIF p9_9_4); (e) tratamiento declarado de las celdas cuya unidad no es persona/hogar (`EMP-02`, `EMP-04`: empresa; `DOC-06`: documento): `EXISTE-NO-SATISFACE`, razón "unidad de análisis fuera del dominio del motor", sin buscar más — declarado antes de correr; (f) B-bis: ≥8 de 15 `EXISTE-SATISFACE` ⇒ el programa D1 cabe en un lote de caja; 3-7 ⇒ parcial, se dice cuáles y por qué las otras no; ≤2 ⇒ D1=(i) no es viable con el corpus abierto hoy y mesa recibe ese número (la firma no obliga a inventar reglas). Los tres desenlaces son informativos; ninguno adjudica. Cierra con: "el primer resultado que produzca este procedimiento es el que se reporta."
COMMIT-2 — corrida única
`forense/prereg-duelo-v2/cobertura-15-v1_0.tsv`: una fila por (celda × candidato θ) con `id_celda, encuesta, ola, variable_celda, regla, regla_existe, theta, theta_candidato_id, theta_texto(≤200), via, veredicto_candidato, razon`. Resumen por celda con vocabulario A.4 y A.13 (filas examinadas por tabla y por instrumento; conteo que prueba que cada búsqueda corrió). Programa de medición en `forense/notas/2026-08-30-cobertura-15-cierre-E11.md` (nombre distinto del cierre de E10, que queda como historia): por celda `EXISTE-SATISFACE`, la mini-spec que el acto de caja congelará en su COMMIT-1 — instrumento, payload (id del manifiesto), θ y desenlace (variable de la celda), universo y ponderador tomados de las columnas `universo`/`ponderador` del marco (el punto M nace comparable con R sin mirar R), estimador (diferencia de proporciones o pendiente LPM según θ), ejes de condicionamiento del patrón del 4/ago. Nada de esto se ejecuta aquí.
Falsador del acto
Si la receta (a)-(c) resulta inaplicable a ≥5 celdas porque el motor no tiene regla ni generador para su dominio, se reporta como hallazgo sobre el alcance del motor, no se parcha la receta. Una vuelta, cero iteración.
PERÍMETRO Y CONCURRENCIA
Archivos: `forense/encargos/2026-08-30-MAESTRA32-E11-COBERTURA-15.md` · `forense/notas/2026-08-30-cobertura-15-spec.md` · `forense/notas/2026-08-30-cobertura-15-cierre-E11.md` · `forense/prereg-duelo-v2/cobertura-15-v1_0.tsv` (nuevo) · `forense/firmas-pendientes.tsv` (`FP-181` → FIRMADA (a); fila nueva de recibo) · cascada. No toca: `data/INFRAESTRUCTURA-v1_0.md`, el marco congelado, `CONGELADO-v1_0.sha256`, `enlace-M-v1_0.md`, `corridas-*`, `scoring-adv1-m3.py`, `milpa/**`, `canon/modelo-decision-v4_0.md`, ni el encargo/cierre de E10. No abre `corridas-R/`. Concurrencia: carril CAJA en paralelo. "Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo."
FP pre-asignadas
`FP-183`–`FP-184` — E10 las dejó "reservadas sin consumir" (`ADR-224`); este acto las toma y lo declara. Re-deriva el máximo real (hoy `FP-182`); si el carril CAJA las tomó, siguiente libre, declarado.
ADR y cascada
Candidato re-derivado con el comando de la casa (hoy daría 226 — deriva, no heredes; renumera quien fusiona segundo). El ADR declara: re-emisión de E10 (`ADR-224`) con compuerta ya cumplida (`ADR-225`), `FP-181` = (a), re-sello del enlace-M por universo ampliado (A.10, corolario 1: el texto del 26/ago intacto), emisión ciega con listado de archivos abiertos, y el B-bis con su desenlace. `registro-rotulos`: `MAESTRA32-E11` (token pelado `E11` — censar, no reclamar). T25.
CONTADOR
Celdas con enlace sellable: N de 15 (la cifra es la medición, incluido cero) · mini-specs de medición entregadas: N.
Lo que este acto NO hace
No mide. No emite puntos M. No escribe reglas nuevas en el motor ni en el canon. No re-sortea. No descongela. No lee R. No edita nada de E10.
Sucesores declarados, no lanzados
Un acto de caja por celda `EXISTE-SATISFACE` (`MAESTRA32-E12…`, dos commits cada uno, redactados por dirección con las mini-specs de este cierre) · re-pase del enlace-M (`v1_1`) cuando el primer enlace quede sellado · re-intento de scoring con `FP-168` firmada cuando M tenga ≥1 punto.

## CONSUMIDO

`COMMIT-1`/`COMMIT-2` corridos completos. Resultado: `0` de `15` celdas `EXISTE-SATISFACE` — ninguna mini-spec de medición entregada, ningún acto de caja sucesor lanzado. Falsador del acto `DISPARADO`: 5 de las 12 celdas evaluables (`CIV-08`, `DIN-11`, `SFT-04`, `SFT-06`, `TIC-06`) llegan a `regla_existe = NO` tras derivación de dominio agotada (≥5) — hallazgo sobre el alcance del motor, no defecto de receta, sin iterar. Las 7 celdas restantes con regla no llegan a `EXISTE-SATISFACE` (tres sin cita `G#` explícita de θ, una con su candidato dominante `CIRCULAR-EXCLUIDO` por precedente ya sellado en `milpa/procedencia.yaml:315-320` — el precedente ENIF `p9_9_4` que este mismo encargo anticipaba —, y tres sin operacionalización dentro del instrumento+ola propio de la celda). Las 3 celdas fuera de unidad persona/hogar (`EMP-02`, `EMP-04`, `DOC-06`) recibieron `EXISTE-NO-SATISFACE` directo por (e), sin correr (a)-(c). `B-bis` ≤2: `D1=(i)` no es viable con el corpus abierto hoy, mesa recibe este número. `FP-181` → `FIRMADA` (opción (a)); `FP-183` nueva (recibo del resultado); `FP-184` reservada, sin consumir. `ADR-226`, `canon/registro-rotulos.tsv` (`MAESTRA32-E11` censado), `tests/check.py` `_T25_ARCHIVOS_CONOCIDOS` actualizado (verde). Detalle completo: `forense/notas/2026-08-30-cobertura-15-spec.md` (COMMIT-1), `forense/prereg-duelo-v2/cobertura-15-v1_0.tsv` (COMMIT-2), `forense/notas/2026-08-30-cobertura-15-cierre-E11.md` (cierre, con listado completo de archivos abiertos bajo emisión ciega). PR: N/D desde esta sesión — commitea y pushea a la rama designada (`claude/maestra32-e11-reemision-bezu0m`), mismo patrón que `MAESTRA32-E1`/`E2`/`E5`/`E6`/`E9`/`E10`; quien fusione llena este campo al abrir el PR.
