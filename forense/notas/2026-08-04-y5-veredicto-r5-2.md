# `R5.2` · Cuidado → mujeres 40+ · Encargo Y, commit 2 · corrida completa, propuesta, no adjudicada

*(Mismo criterio que Nota 16/`R5.1`: se propone, no se archiva. El veredicto archivado y el contador `3 de 27` no cambian por esta nota.)*

Operacionalización pre-registrada: `forense/notas/2026-08-04-y1-operacionalizacion-r5-2-enut.md`, corrida sin editarla después de ver el resultado.

## Tabla

| Grupo | n (sin ponderar) | Σpeso (`FAC_PER`) | media horas/semana cuidado | SE (conglomerado último `EST_DIS`/`UPM_DIS`) |
|---|---|---|---|---|
| Ocupada formal TC, varón disponible (T) | 1,872 | 2,959,958 | 7.9553 | 0.4341 |
| No ocupada, varón disponible (C) | 8,150 | 11,943,155 | 10.4646 | 0.3575 |

Universo: mujeres 40+ (`tsdem`/`tvar_crea`, `SEXO=2 ∧ EDAD≥40`) en hogares con varón 18+ presente (`hogar_varon`, 23,339 hogares) — 14,756 de 19,292 mujeres 40+ (76.5%) caen en hogares con varón adulto disponible; T+C = 10,022 de esas 14,756 (el resto son ocupadas informales/tiempo parcial, ni T ni C por diseño — no se fuerzan a ningún grupo).

**Reducción% = (media_C − media_T) / media_C = 23.98%.**
**SE (método delta, grupos independientes, misma convención que `R7.2`/Nota 12) = 4.89pp.**
**IC95% = [14.39%, 33.57%].**

## Lectura contra la escala propia (línea 162)

El punto estimado (23.98%) satisface literalmente la fila `A` ("reducción ≥20% → la estructura basta y el guion sale del `PORQUE`"). **Pero el IC95% no despeja el umbral: el límite inferior (14.39%) queda por debajo de 20%.** A diferencia de `R7.2` (Nota 12: IC95% [6.4,17.4]pp, ninguno de los dos extremos cruzaba 20), aquí el punto y el IC95% dan lecturas distintas — la brecha entre el punto (23.98%) y 20% es de solo 0.81 SE, no una diferencia estadísticamente distinguible de "no cruza".

**Verificado, no hay solape con fila `B`:** `B` exige "reducción parcial **sin** control de disponibilidad del varón" — el diseño de este acto **sí** incluye ese control (ambos grupos restringidos a hogares con varón adulto disponible), así que `B` no se satisface bajo ninguna lectura, cruce el punto donde cruce.

**Fila `C`/`D` no aplican:** el cruce (`ENUT × ocupación × composición del hogar`) que `C` describe **es exactamente el que este acto ejecutó** — no es un hueco declarado, es el diseño usado. `D` (`ENUT no permite el cruce`) es falso: sí lo permite, con las salvedades declaradas en el commit 1 (proxy de formalidad `P5_6_7`, cobertura 96.2% del universo 12+).

**Nota de asimetría de la propia ficha (línea 160), preservada:** si el resultado hubiera sido claramente <20%, ese es "el resultado más valioso que esta ficha puede dar" y no un fracaso — no aplica aquí en su forma pura porque el punto cruza 20%, pero la incertidumbre estadística deja abierta la posibilidad de que el guion sí sobreviva. **Este acto no fuerza esa lectura ni la contraria.**

## Propuesta y reserva

**Propuesta: fila `A`, con reserva estadística fuerte** — el punto cruza el umbral, la escala no especifica una convención de significancia para "cruzar", y el IC95% no excluye valores por debajo de 20%. Reservas adicionales: proxy de formalidad (`P5_6_7`, derecho a servicio médico, no afiliación activa verificada); "varón adulto disponible" leído en su forma mínima (presencia, sin condición de relación ni ocupación); diseño transversal, no panel (declarado desde el commit 1, coherente con la fila `C` de la propia ficha).

**El veredicto archivado (3 líneas, sin `R5.2`) y el contador `3 de 27` no cambian por esta nota.** Si mesa decide adjudicar `A` pese al IC95%, o declarar el resultado no concluyente, se registra como entrada nueva fechada en el bloque append-only — no editando esta nota.

Script y datos intermedios: `run_r5_2.py` (ejecutado sobre `enut2024_bd_csv.zip`, tablas `tsdem.csv`, `tmodulo.csv`, `tvar_crea.csv`).
