# ACTO CONSOLIDA-17AGO — la compuerta no se abrió: `CIERRA-17AGO` fusionó a medio acto

**Acto:** CONSOLIDA-17AGO · **Encargo:** `forense/encargos/2026-08-17-CONSOLIDA-17AGO.md` (archivado verbatim por A.3, este commit) · **Entorno:** NUBE, repo-only, sin `data/raw` · **SHA de redacción del encargo:** `d0019a2` (`origin/main`, merge #249) · **SHA real de arranque:** `4415e04` (`origin/main`, merge #250) — avanzó exactamente por el commit que la compuerta esperaba, ver §1.

---

## §0 · ARRANQUE — las cinco líneas, crudas

**1 · REPO.** Clon existente, no desde el home. Ruta absoluta: `/home/user/Modelado-Mexicano`. Rama de trabajo: `claude/new-session-1st0g2` (asignada por el arnés).

```
$ pwd && git status && git log -1 --format="%h %s" && git branch --show-current
/home/user/Modelado-Mexicano
On branch claude/new-session-1st0g2
nothing to commit, working tree clean
4415e04 Merge pull request #250 from Josanoforo/claude/new-session-w9nxfz
claude/new-session-1st0g2
$ git rev-parse --is-shallow-repository
true
```

Clon superficial detectado. `git fetch --unshallow` antes de cualquier veredicto:

```
$ git fetch --unshallow
From https://github.com/Josanoforo/Modelado-Mexicano
 * [new branch]      codex/barrido-2 -> origin/codex/barrido-2
 + f8eb2e3...4415e04 main            -> origin/main  (forced update)
$ git rev-parse --is-shallow-repository
false
```

**2 · SHA.** Base declarada por el encargo: `d0019a2` (merge #249). Base real al arrancar: `4415e04` (merge #250) — `main` avanzó exactamente por el PR que la compuerta esperaba. No es PARO por deriva (el encargo mismo lo prevé: *"Si avanzó más, no es PARO — clasifica la deriva y repórtala"*); es el hallazgo de §1: avanzó, pero no como la compuerta asumía.

**3 · `data/raw`.** Este acto no toca microdato. Dicho y saltado.

**4 · ENTORNO.**

```
$ env | grep -i sandbox
IS_SANDBOX=yes
$ curl -sS -o /dev/null -w "HTTP_STATUS:%{http_code}\n" https://github.com/ --max-time 15
HTTP_STATUS:400
```

`IS_SANDBOX=yes` — firma correcta de un acto de nube. La sonda cruda a `https://github.com/` da `400` (proxy de agente de la sesión, mismo patrón ya precedentado en `forense/notas/2026-08-17-registra-17ago-comandos.md`); el acceso real por `git fetch`/`git ls-remote` y por MCP de GitHub funcionó sin problema (ver abajo).

**5 · ESPEJO.** Ninguna cifra de esta nota viene del espejo del proyecto. Todo sale del clon y de la API de GitHub vía MCP, con el comando a la vista.

---

## §1 · La compuerta — control positivo, investigado hasta el fondo, no es un "no busqué bien"

**El encargo instruye:**

```
git fetch origin && git log --oneline -1 origin/main
grep -c "ejecutada_en" forense/firmas-pendientes.tsv   # control positivo: debe dar >=1 si CIERRA fusionó
```

Corrido contra el árbol real:

```
$ git log --oneline -1 origin/main
4415e04 Merge pull request #250 from Josanoforo/claude/new-session-w9nxfz
$ grep -c "ejecutada_en" forense/firmas-pendientes.tsv
0
$ head -1 forense/firmas-pendientes.tsv
id	qué_se_firma	dónde	creado	gatea	estado	firmada_en
```

**El control da `0`. La columna no existe.** Por la letra de la compuerta ("Si la columna `ejecutada_en` no existe, PARA"), este acto no arranca. Antes de aceptarlo, se investigó como una de las cuatro recetas rotas que el propio encargo advierte (§ MÉTODO) — con dos preguntas: ¿la columna vive en otro lado o con otro nombre? ¿"CIERRA no ha fusionado" es literalmente cierto?

**Búsqueda exhaustiva de `ejecutada_en`, historia completa, no solo árbol de trabajo:**

```
$ git log --all -p -S"ejecutada_en" --oneline | head -50
[vacío]
$ grep -rn "ejecutada_en" --include="*.md" --include="*.tsv" --include="*.yaml" .
[vacío]
```

La columna nunca existió, ni en el árbol ni en ningún commit de la historia completa. No es un patrón roto: es ausencia real.

**`PR #250` sí fusionó — pero a medio acto.** `mcp__github__pull_request_read` contra `Josanoforo/Modelado-Mexicano#250` (`get` y `get_commits`):

- `merged: true` · `merged_by: "Josanoforo"` (mesa, no automatizado) · `commits: 1` · `changed_files: 4` · `additions: 5` · `deletions: 5`.
- Cuerpo del PR, verbatim, íntegro — evidencia del fallo de compuerta, queda aquí y no en captura:

> Ejecuta `forense/encargos/2026-08-17-CIERRA-17AGO.md` contra `origin/main = d0019a2` (PR #249 fusionado, `FP-30` confirmada `FIRMADA`).
>
> **En progreso — este PR se actualiza con más commits antes de quedar listo para revisión de mesa. No fusionar todavía.**
>
> Trabajo en curso, se completará el resumen cuando termine el acto.
>
> ---
> *Generado por Claude Code*
>
> ---
> *Generated by [Claude Code](https://claude.ai/code/session_01GRNkHDps8fxTHwdrNQ71Cn)*

El único commit fusionado es `2e6f2d0`, titulado **"COMMIT 1 (CIERRA-17AGO) — cascada de conf.02 (ADR-92d) al corpus"** — el propio número de commit lo declara parte de una secuencia. Toca cuatro archivos (`canon/glosario-v5_6.md`, `canon/integrador-psicologia-mexicano.md`, y dos reportes de `corpus/reports/`) para ejecutar la cascada de `conf.02`/`ADR-92(d)` al corpus. No toca `forense/firmas-pendientes.tsv`, no escribe nota propia, no archiva su propio encargo, no firma `FP-38`.

**Lectura material, no literal.** La compuerta dice "Si la columna no existe, PARA: CIERRA no ha fusionado" — y en sentido literal sí fusionó (el commit está en `main`). Pero el propósito declarado del control positivo era verificar que el trabajo de `CIERRA` **quedó completo**, no solo que un PR con ese nombre cerró. `PR #250` fusionó con **mesa saltándose la advertencia explícita de su propio cuerpo** ("No fusionar todavía"), a medio plan de commits. El control positivo hizo exactamente lo que estaba diseñado para hacer: detectar que no hay dónde escribir lo que este acto necesitaba. Se trata como `PARA`.

**Camino de recuperación, cerrado con dos derivaciones independientes.** Pregunta: ¿existen, sueltos en algún ref, los commits 2/3/… que `PR #250` prometía y no llegó a incluir?

```
$ git log --all --oneline | head -20
4415e04 Merge pull request #250 from Josanoforo/claude/new-session-w9nxfz
2e6f2d0 COMMIT 1 (CIERRA-17AGO) — cascada de conf.02 (ADR-92d) al corpus
d130a3f BARRIDO-2 C4(b): dos reglas no lexicas mas de cobertura, y una tercera retirada por no ser funcion
4abfccd BARRIDO-2 C3-bis(3): reejecucion E2 completa sobre el parser y el resolvedor corregidos
d0019a2 Merge pull request #249 from Josanoforo/claude/new-session-7avlfg
[... resto de la historia, sin nada entre 4415e04 y 2e6f2d0 ...]
$ git ls-remote --heads origin
d130a3f2a44dc4882e4bb0fd94d573ad7778477b	refs/heads/codex/barrido-2
4415e047215a605298f45dd6bb8a526d76db1e3a	refs/heads/main
```

`2e6f2d0` es el único commit del acto en cualquier ref alcanzable desde este clon (`--all`, historia completa, no solo `main`). El remoto solo conserva dos ramas: `main` (con ese commit ya fusionado) y `codex/barrido-2` (BARRIDO-2, no se toca). La rama de cabecera del PR (`claude/new-session-w9nxfz`) ya no existe en `origin` — borrada al fusionar — y el propio API de GitHub confirma que nunca tuvo más de un commit (`get_commits` → un solo elemento, `2e6f2d029896ba29bcf7dccd2090c01179201603`). **No hay nada que recuperar: lo que `PR #250` iba a agregar nunca se escribió en ningún commit, en ninguna rama.** Dos derivaciones independientes — `git log --all` local y `git ls-remote` contra el remoto — coinciden entre sí.

---

## §2 · Hallazgo adicional, no anticipado por el encargo: `ADR-94` se cita cuatro veces y no existe

El único commit de `CIERRA-17AGO` deja cuatro citas a `ADR-94` fuera del árbol de gobernanza. Control positivo primero — formato real de una cabecera de ADR que sí existe:

```
$ grep -n "\*\*ADR-92\|\*\*ADR-93" canon/gobernanza-v1_15.md
1576:**ADR-92 · Se registran, verbatim, las cinco firmas de mesa [...]
1606:**ADR-93 · Se registran, verbatim, las tres firmas de mesa [...]
```

Patrón confirmado: `^\*\*ADR-N · `. Contra ese patrón, el comando pedido:

```
$ grep -c "^\*\*ADR-94" canon/gobernanza-v1_15.md
0
$ grep -oE "^\*\*ADR-[0-9]+" canon/gobernanza-v1_15.md | sed -E 's/\*\*ADR-//' | sort -n | tail -5
89
90
91
92
93
```

`ADR-93` es la entrada más alta que existe. `ADR-94` nunca se escribió — es el siguiente número libre, no una entrada. Y sin embargo ya se cita como si fuera vigente:

```
$ grep -rn "ADR-94" --include="*.md" --include="*.yaml" --include="*.tsv" .
./corpus/reports/La_arquitectura_invisible_de_la_interacción_social_en_México.md:155: [...] orientación policrónica —preferencia individual medible, sin adscripción nacional establecida (ADR-94)— [...]
./canon/glosario-v5_6.md:316: [...] ✅ Resuelto por ADR-94, 18/ago/2026. [...]
./canon/integrador-psicologia-mexicano.md:245: [...] (VACÍO grande, ADR-94). [...]
./canon/integrador-psicologia-mexicano.md:351: [...] no dato reciente (ADR-94). [...]
$ grep -rn "ADR-94" --include="*.md" --include="*.yaml" --include="*.tsv" . | wc -l
4
```

**Cuatro citas, cero entradas — confirmado.** No es una decisión pendiente de nuevo: la sustancia ya está adjudicada, verbatim, en `ADR-92(d)` (línea 1584 de `gobernanza`, firma de mesa *"FP-27 tercera opción"*), que el propio título del commit de `CIERRA` cita correctamente ("cascada de conf.02 (ADR-92d) al corpus"). Lo que falta es la escritura: `CIERRA` reservó el número 94 —probablemente para un ADR propio que formalizara la ejecución, en un commit posterior que nunca llegó— y sus cuatro ediciones a `canon/`/`corpus/` ya citan ese número como si el párrafo existiera. Es un artefacto de acto cortado a medias, no una nueva adjudicación abierta: la corrección (escribir `ADR-94` o retargetear las cuatro citas a `ADR-92(d)`) es trabajo del acto sucesor — esta nota no adjudica.

**Búsqueda adicional, misma causa.** `forense/encargos/2026-08-17-CIERRA-17AGO.md`, el archivo que el propio cuerpo del PR dice ejecutar, no existe en el árbol:

```
$ find forense/encargos -iname "*cierra*"
[vacío]
```

`A.3` (un encargo sin archivo es un defecto de la misma clase que un pendiente sin fila) también quedó incumplida por el mismo corte. No se reconstruye aquí: no se tiene el texto fuente, y fabricarlo sería inventar evidencia.

---

## §3 · Declaración — las Partes 1 a 4 no se ejecutaron

Este acto **no arrancó**. No se tocó `milpa/refutations.yaml`, no se reescribió `glosario:398`, no se firmó `FP-38`, no corrió el barrido de la Parte 3, no se redactó el ADR de la Parte 4. Ninguna cascada se propagó, ninguna adjudicación se tomó. Lo único que este acto escribe es el registro de por qué no pudo arrancar, con comando, control positivo y salida cruda en cada paso, y dos hallazgos nuevos que la propia investigación de la compuerta destapó (§2).

**Perímetro real, escrito por este acto:** `forense/encargos/2026-08-17-CONSOLIDA-17AGO.md` (A.3, Estado actualizado a `VIVO — no ejecutado`) · `forense/notas/2026-08-17-consolida.md` (esta nota) · `forense/hallazgos.md` (una entrada).

**No escrito, verificado:** `milpa/refutations.yaml` · `canon/glosario-v5_6.md` · `canon/gobernanza-v1_15.md` · `forense/firmas-pendientes.tsv` · `forense/hitoD-preregistro-v2_0.md` · `data/**` · `tools/**` · `corpus/**` · `tests/**` — cero archivos de estas rutas en este commit.

**Sobre la fila recomendada para `ADR-94` (§2).** No se abre aquí. Es exactamente el tipo de juicio de triaje que la Parte 3 de este mismo encargo —bloqueada por la compuerta— habría hecho con método (`YA RESUELTO` / `FILA` / `SOLO ANOTADO`); abrir una fila nueva, sin ese triaje y con `BARRIDO-2` corriendo en paralelo sobre el mismo tablero, arriesga la misma colisión de numeración que `ADR-92(g)` ya declaró una vez. Queda en `forense/hallazgos.md` (una línea) y señalado aquí con máxima visibilidad para el acto sucesor.

## Cierre

**Antes** de este commit (árbol tal como llegó de `PR #250`):

```
$ python3 tests/check.py --baseline
24 FAIL · 126 WARN
LÍNEA BASE: VERDE — nada nuevo frente a tests/baseline.json (HEAD congelado 45d7d2f541fca5edbc6205d2c8ea713a2276f8cc)
```

**Después**, con los tres archivos de este acto ya escritos (`forense/encargos/2026-08-17-CONSOLIDA-17AGO.md`, `forense/notas/2026-08-17-consolida.md`, la entrada de `forense/hallazgos.md`):

```
$ python3 tests/check.py --baseline
30 FAIL · 126 WARN
LÍNEA BASE: ROJO — 2 entradas nuevas frente a tests/baseline.json (HEAD congelado 45d7d2f541fca5edbc6205d2c8ea713a2276f8cc)
  · T16: canon/gobernanza-v1_15.md: declara 20 FAIL · 126 WARN vigente; la corrida real da N WARN
  · T22: forense/encargos/2026-08-17-CONSOLIDA-17AGO.md trae un marcador de ranura/pendiente-de-mesa nuevo y ninguna fila de `forense/firmas-pendientes.tsv` lo cita
```

(La cifra `N WARN` en la línea resumen de `T16` es un defecto cosmético del propio formateo de `check.py` — el detalle completo, un renglón abajo en la corrida cruda, sí trae el número: "la corrida real da 21 FAIL · 126 WARN". No es `tests/**`, no se toca.)

**Dos entradas nuevas, reportada por test, no agregada — y las dos son consecuencia directa de la verbatimidad que `A.3` exige, no de un contenido nuevo mal escrito:**

- **`T22` (0 → 1 fail).** `forense/encargos/2026-08-17-CONSOLIDA-17AGO.md` contiene, verbatim, el bloque de patrón de la Parte 3 (`"pendiente de mesa"`, `RANURA`, `[FIRMA`, etc. — son los *ejemplos* que ese mismo encargo usa para describir su propio barrido) y la fila de la tabla de candidatos que cita *"requiere decisión de mesa"* como ejemplo de `YA RESUELTO`. `T22` no distingue "texto que discute el patrón" de "un pendiente real": dispara igual. **Mismo mecanismo, mismo diagnóstico, ya precedentado en este archivo el mismo día** para `T03` (ver la entrada de `forense/hallazgos.md` sobre `ACTO REGISTRA-17AGO-II`): el riesgo es estructural a la verbatimidad de `A.3`, no evitable editando el archivo archivado sin violar la razón de ser de `A.3` (proteger la prueba exacta de qué se pidió). El mensaje del test ofrece dos salidas — "añade la fila (A.12)" o "explica la exclusión... y súmalo a `_T22_ARCHIVOS_CONOCIDOS`". Ninguna de las dos se ejecuta aquí: la primera fabricaría una fila para un pendiente que no existe (el texto es ejemplo, no un caso real sin registrar); la segunda requiere editar `tests/check.py`, fuera del perímetro declarado (`NO escribe: ... tests/**`). Queda declarado como `ROJO` esperado y explicado — igual que `T03` ese mismo día —, y el ajuste a `_T22_ARCHIVOS_CONOCIDOS` queda para un acto sucesor con `tests/**` en su perímetro.
- **`T16` (4 → 9 fail, dentro de su propia cuenta ya-en-rojo por la `CASCADA NO ESCRITA` de `estado-programa`, bloqueada por BARRIDO-2).** Es el efecto de cascada que el propio encargo pidió esperar: *"Espera T22 (filas nuevas: señal) y T15/T16 (cascada de ADR que no puedes escribir)."* `T16` compara el conteo de `FAIL`/`WARN` que cada documento declara como "vigente" contra la corrida real; al subir el total real de 20 a 21 (por el nuevo fail de `T22`), cada declaración congelada en `canon/estado-programa-v1_10.md` y `canon/gobernanza-v1_15.md` que ya citaba un número desactualizado queda un poco más desactualizada, y el propio conteo de `T16` (que es parte del total que otros documentos intentan declarar) se mueve con ella — de ahí el salto de 4 a 9 dentro de la misma categoría, no seis defectos nuevos y distintos. `canon/estado-programa-v1_10.md` es perímetro de BARRIDO-2, no de este acto. `canon/gobernanza-v1_15.md` está en mi perímetro nominal, pero el número que declara ("20 FAIL") es un contador interno — `A.6`/AGENTS.md lo clasifica como defecto no material ("conteos internos": regístralo y continúa) —, y perseguirlo aquí sería abrir `gobernanza` para algo que el encargo no autorizó (no hay ADR nuevo que escribir; ver §3). Se declara, no se persigue.

**No recongela.** `tests/baseline.json` no se toca — el `ROJO` queda declarado, con receta y comando, para que `--freeze` lo levante cuando corresponda con la autorización de mesa que `ADR-76(f)` exige.

**Contadores de medición sobre México: 0.** Este acto no mide, no adjudica, no propaga. Documenta por qué la compuerta no se abrió, cierra la vía de recuperación con dos derivaciones independientes, deja dos hallazgos nuevos (`ADR-94` fantasma, encargo de `CIERRA` sin archivar) con fila recomendada para el acto sucesor, y dos entradas de test nuevas explicadas por la propia mecánica de `A.3`, no por contenido mal escrito. Dilo así, sin justificarlo.

---

## ADENDA FECHADA — 18/ago/2026, ~04:35 UTC — la recuperación sí ocurrió

**No se edita §1 de arriba: sigue siendo la derivación correcta contra el estado que existía cuando se escribió.** `git log --all --oneline` y `git ls-remote --heads origin`, corridos en ese momento, mostraban de verdad cero commits recuperables — dos derivaciones independientes, ambas ciertas contra ese instante. Lo que cambió no fue el método: fue el mundo. Después de escrita esta nota, mesa señaló que la recreara y volviera a mirar, y un `git fetch` nuevo trajo algo que no estaba ahí antes.

**Recreación, confirmada, no supuesta.** `git fetch origin` trajo `claude/new-session-w9nxfz` de vuelta — la misma rama que `PR #250` había fusionado y GitHub había borrado — apuntando ahora a un commit distinto (`508f675`, no `2e6f2d0`), con seis commits nuevos encima del que sí llegó a `main`. La sesión que redactó `CIERRA-17AGO` había seguido trabajando después de que mesa cerrara `PR #250` a medio camino, completando los seis commits que su propio encargo (recuperado del árbol de esa rama: `forense/encargos/2026-08-17-CIERRA-17AGO.md`, ausente de `main` hasta ahora) siempre había planeado.

**Verificación independiente, no confianza en el mensaje de los commits.** Antes de tocar nada, se corrió una verificación adversarial en paralelo (cuatro derivaciones, cada una releyendo el diff real y — donde aplicaba — ejecutando la prueba real en un worktree aislado, no solo leyendo el código) sobre las tres afirmaciones concretas que mesa citó, más una cuarta:

- **`T07` cierra de verdad** → `CONFIRMED`. Corrida real antes (`FAIL`, 7 vocabularios ajenos) y después (`ok`, 0 ajenos) en el commit que dice cerrarlo.
- **Las dos columnas del tablero (`ejecutada_en`/`encargo`) funcionan de verdad** → `PARTIAL`. Mecánicamente reales — el `WARN` nuevo de `T22` se probó ejecutando el código, no leyéndolo, y dispara exactamente sobre las filas `FIRMADA sin ejecutar` — pero con un defecto real: las 13 citas a `PR #250` dentro de la propia rama (10 en el tablero, una en la atribución de `ADR-94`, una en la cabecera del encargo `CIERRA-17AGO`, una en la nota) asumían que todo este trabajo entraría en `PR #250`. Ya no es cierto: `#250` cerró con solo el `COMMIT 1`.
- **`ADR-94` respalda las cuatro citas huérfanas** → `CONFIRMED`. Entrada real, formato idéntico a `ADR-92`/`ADR-93`, sustancia consistente con lo ya sellado, sin re-litigar nada, con su única decisión nueva (retener `MEDIA-FUERTE`) declarada sin ocultarla. Las cuatro citas que esta misma nota (§2) encontró huérfanas ahora resuelven contra texto real.
- **Fidelidad del resto del contenido** (`COMMIT 2`-`4`) → `CONFIRMED`. Ejecutan lo ya sellado en `ADR-92(d)`/`(e)` sin re-adjudicar, sin tocar rutas prohibidas.

Corrida completa de la suite contra la punta de la rama, y `git merge-tree` contra `origin/main` actual: **`29 FAIL · 130 WARN`, mezcla limpia, sin conflictos.**

**El defecto encontrado, corregido — con una excepción verificada caso por caso, no en bloque.** De las 13 citas a `PR #250`, doce corresponden a trabajo que solo existe en los seis commits nuevos (nunca llegaron a `PR #250`) y se corrigieron a `PR #251` — el PR abierto sobre esta misma rama para llevarlos a `main`. La decimotercera (`FP-27`, fila de `ejecutada_en`) se dejó **sin tocar**: su ejecución real —la cascada de `conf.02` al corpus— es exactamente `COMMIT 1` (`2e6f2d0`), y ése sí fusionó dentro de `PR #250`. Tratar las 13 como un bloque uniforme habría introducido un error nuevo al corregir uno viejo. Commit de corrección: `718732c`, sobre `claude/new-session-w9nxfz`, con su propio mensaje explicando el porqué fila por fila. Suite re-corrida tras la corrección: mismos `29 FAIL · 130 WARN` — es una corrección de cita, no de sustancia.

**`PR #251` abierto, no fusionado.** `https://github.com/Josanoforo/Modelado-Mexicano/pull/251`. CI (`check`) da rojo — mismo `LÍNEA BASE: ROJO` de diez entradas ya medido arriba antes de tocar nada, no un defecto que este PR introduzca; declarado en un comentario del propio PR, no silenciado con un `--freeze` que `ADR-76(f)` no autoriza. **Nadie se auto-fusiona esto.** Queda para que mesa lo revise y apriete el botón.

**Qué desbloquea, y qué sigue sin arrancar.** Cuando `PR #251` fusione, `forense/firmas-pendientes.tsv` tendrá por fin la columna `ejecutada_en` — el control positivo exacto que la compuerta de §1 pedía — y la compuerta de `CONSOLIDA-17AGO` quedará satisfecha. **No fusionó todavía.** Este acto sigue `VIVO — no ejecutado`; las Partes 1-4 siguen sin correr. La cabecera del encargo archivado no se toca aquí — se corregirá, si corresponde, cuando `PR #251` fusione de verdad y la compuerta se vuelva a correr contra ese `main`.

**BARRIDO-2 — coordinación declarada, cero acción.** Mesa señaló que, una vez `PR #251` fusione, `gobernanza` tendrá `94` ADR únicos (ya verificado: `ADR-94` existe y cuenta), y que el ADR pendiente de `BARRIDO-2` debe numerarse `95`, no reclamar el hueco `94` que ya no existe. Cierto por lo ya verificado arriba, y exactamente el mismo mecanismo de "renumera quien fusione después" que `ADR-91`/`ADR-92`/`ADR-93` ya precedentaron para colisiones de número. **No se toca `codex/barrido-2` para ejecutarlo** — sigue fuera de perímetro, íntegro; queda declarado aquí para que el acto que sí tiene permiso de escribir esa rama lo aplique al fusionar.

**Cierre de esta adenda.** `python3 tests/check.py --baseline`, en el commit de esta adenda: `31 FAIL · 126 WARN`, `LÍNEA BASE: ROJO` — 3 entradas nuevas frente a la congelada (`T16`×1, `T22`×2). Ambas ya declaradas arriba en la nota original, no defectos nuevos de sustancia: `T16` sigue el mismo efecto de cascada (el total real sube en uno más, cada declaración congelada en `estado-programa`/`gobernanza` queda un poco más desactualizada). `T22` gana un **segundo** archivo, esta misma nota — por la misma causa estructural que ya atrapó al encargo archivado (§ arriba): el párrafo que *explica* el falso positivo de `T22` sobre el encargo cita, entre comillas, la palabra `RANURA` como ejemplo — y el patrón de `T22` no distingue "cita la palabra al explicar por qué no aplica" de "trae un pendiente real". Mismo mecanismo, mismo diagnóstico, ahora recursivo sobre su propia explicación: no se edita para evadirlo (perdería la explicación misma), no se abre fila (no hay pendiente real detrás), no se toca `tests/check.py` (fuera de perímetro). Declarado, no recongelado.

---

## ADENDA — 18/ago/2026, ~05:18 UTC — `PR #251` fusionó, la compuerta queda satisfecha

`PR #251` se fusionó (`63976f8`). `origin/main` trae ya la columna `ejecutada_en` en `forense/firmas-pendientes.tsv` — el control positivo exacto que la compuerta de §1 exigía. **La compuerta de `CONSOLIDA-17AGO` queda satisfecha en `main`.** Este acto no reabre las Partes 1-4 aquí: sigue fuera de su propio alcance declarado (§3), y reabrirlas sería una decisión nueva, no la corrección de este reporte.

**Se fusiona `main` a esta rama** (`git merge origin/main`, limpio, sin conflictos — `forense/hallazgos.md` fue el único archivo con cambios en ambos lados, auto-mezclado). Trae consigo el contenido completo de `CIERRA-17AGO` (`ADR-94`, las dos cascadas, el tablero con sus columnas) y su propio `tests/baseline.json` recongelado.

**Mismo patrón de `T16` que ya se vio en `PR #251`, mismo tratamiento.** El merge deja `gobernanza:764`/`:856` desactualizadas otra vez — la cifra real sube por los mismos dos marcadores de `T22` que este archivo ya declaró arriba como falso positivo estructural (el propio encargo y esta misma nota, citando `RANURA` al explicar por qué no aplica). Resincronizadas a `**21 FAIL · 130 WARN**`, verificado en punto fijo (cuatro corridas sin más movimiento). Quedan exactamente los dos `T22` ya declarados — ninguna entrada nueva, ningún hallazgo nuevo.

**Recongelado, autorizado por mesa.** Mesa ya autorizó este mismo tratamiento para `PR #251` (*"Autorizo el recongelado de tests/baseline.json en esta rama, bajo ADR-76(f)..."*, cita completa en `canon/gobernanza-v1_15.md` `ADR-94(h)`) y pidió aplicarlo aquí también (*"do the same for 252, fetch, solve CI"*). Los dos `T22` que quedan son exactamente la misma clase de deuda declarada, no ruido: falsos positivos estructurales de citar el propio patrón de detección al explicarlo, ya documentados arriba con control positivo y comando. `python3 tests/check.py --freeze` → `tests/baseline.json` congelado bajo las claves nuevas; `--baseline` → **LÍNEA BASE: VERDE**, verificado dos veces. El recongelado va en commit propio, posterior al de contenido de esta adenda.
