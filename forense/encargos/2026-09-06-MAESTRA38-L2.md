## REGLA COMÚN (aplica a los cuatro; se archiva con cada uno)

Worktree propio sobre `origin/main`. Antes de evaluar cualquier compuerta: enlazar `data/raw` y copiar `data/raices.local.yaml` desde el clon padre; verificar que no declara `downloads`. A.2 tres partes (`CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE`, sonda `curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://www.inegi.org.mx/` fuera del sandbox, `ls data/raw/ | head -1`). Guard de rama: `git ls-remote --heads origin | grep -i <rótulo>` → coincidencia = PARA. Push del 0-bis (archivo verbatim de este encargo en `forense/encargos/2026-09-0X-MAESTRA38-<ACTO>.md`) al primer minuto.

Spec sellada = COMMIT-1: se verifica su `.sha256`, se cita, no se edita; si estaba mal, el COMMIT-2 lo dice y abre fila. Frase de sello en todo acto que mida: «el primer resultado que produzca este procedimiento es el que se reporta».

`tools/ya_medido.py` sobre cada id que se mida, salida pegada en A.8.

Rótulos ADR/FP no vienen preasignados. Se derivan en el 0-bis, contiguos, contra `main` y `git ls-remote --heads origin`; un rótulo reclamado por rama viva no se toma; renumera quien fusiona segundo.

Concurrencia declarada, no serial. Cada acto lista los archivos del que corre en paralelo y no los toca. Al fusionar, si `main` se movió: refresca, re-corre la cascada completa, reporta la diferencia. Orden: L2 ∥ LOTE-ENSANUT → C1 ∥ LOTE-CRUCE.

Anti-PR#77 en todo acto que registre: `ls -la` del corpus compartido al cerrar, no del worktree. Cascada D-10 completa: ADR por comando de la casa, cabecera de gobernanza, recifrado L0, registro-rotulos, T25, `tests/check.py --baseline` VERDE o PARO-reporta; `## CONSUMIDO` con el PR.

Vocabulario: A.4 (EXISTE-SATISFACE / EXISTE-NO-SATISFACE / NO-ENCONTRADO con universo / NO-ACCESIBLE) + D5 (NO-ADQUIRIDA-POR-COSTO). Todo veredicto negativo declara cuántos archivos examinó el comando (A.13). Toda cantidad medida entra con escala y universo declarados (A-bis 3 y 4).

# ENCARGO · ACTO MAESTRA38-L2 · MPS-2012 — invoca `/acto`

SHA `693ea23` · COMPUERTA: ninguna · ENTORNO: UBUNTU con corpus (no nube) · MODELO: Opus · SPEC: `forense/prereg-caja/S2-L2-spec-v1_0.md` (+ `.sha256`), dos ramas escritas (A: MEDICIÓN, B: TEXTO); la caja declara cuál corre en la primera línea del COMMIT-2.

FIRMAS — verbatim: N3 «MAESTRA37-L2 · MPS-CODEBOOK-Y-P3 (caja; con las tablas exportadas por mesa, si las hay)»; FP-263 «sigue abierta hasta el codebook»; D6 «Creamos las cuentas y bajamos archivos»; mesa 6/sep: «bajé yo desde el apartado de tablas que existe en la página varios items a mano, una conversación me dijo que con eso podía hacer los cálculos»; FP-314 #1 (receta de A4): paquete Data de ICPSR 35024, `35024-0001-Data.dta`, MD5 `3f717dfcd8f3ba136c5d5ac0f571990c`, 1 555 × 374.

A.8 al arrancar — elección de rama (corregida). La rama NO se decide con `grep -c "35024-0001-Data" data/manifiesto.yaml` (hoy da 2 por `35024-0001-Data_partial.tab`, 59 KB, registrado por A4). Se decide así, salida pegada:

    f=$(find <raíces declaradas en raices.local.yaml> -name "35024-0001-Data.dta" 2>/dev/null | head -1); [ -n "$f" ] && md5sum "$f"

→ MEDICIÓN sólo si el MD5 es `3f717dfc…990c` (o, si difiere, 1 555 filas × 374 variables leídas del archivo y declarado); si `$f` está vacío → TEXTO. El parcial se abre en P0 como estructura (variables, filas) y se reporta; si trae `W2_P39B`/`W2_P40`/`W2_P36C`/`W2_P8` para un subconjunto se declara celda acotada a ese subconjunto, nunca MEDICIÓN de S2 rama A.

Además: `ya_medido.py` sobre R7.3, R7.6 y P3 → cita `ADR-329` (R7.3 MEDIA, `se_mueve_si` = medición de primera mano en este `.dta`; L9/L11 CONTRARIA-REPLICADA; L12 P2 REPLICA-DE-SEGUNDA-MANO-NO-SELLADA, P3 sin texto de ítems), `ADR-349` (LOTE-LAPOP: R7.6 tercera CONTRARIA en el otro brazo — no se reabre, se compara) y `ADR-350` (L2-LISTA: P3 p = 0.187 primera mano en subconjunto `list::mexico`, 1 004, sin ponderar — S10 §6: el veredicto sobre el `.dta` completo reemplaza, no promedia). A.8 cita también los **34 payloads que A6 dio de alta** (`ADR-353`) y declara que **ninguno es MPS** — la elección de rama no cambia por A6.

Insumos ya en manifiesto: tabulados T5–T9 del 2/sep (once tablas), LEEME, `35024-Questionnaire-spanish.pdf`, paquete v1 (14.6 MB, 0 datos según `unzip -l` de A4), codebook, DATS json, parcial `.tab`.

EJECUCIÓN.

- **Rama MEDICIÓN (si el `.dta` está):** S2 rama A íntegra — variables por número de ítem (R7.3 = `W2_P39B` × `W2_P8` | `W2_P36C`; R7.6 = `W2_P40` × `W2_P8` | `W2_P36C`; P3 = `P35A/B`, `W2_P35A/B`), ponderador del propio `.dta` declarado por nombre, universo, dicotomizaciones, celdas, IC. Tabulados de mesa pasan a control de consistencia (¿reproducen las proporciones sin ponderar de L12?). Si R7.3 se corrobora con IC fuera de 0: nota «candidata a volver a FUERTE» — el tier lo mueve un acto siguiente con firma. P3: el veredicto reemplaza al de `ADR-350` (S10 §6) y se dice.
- **Rama TEXTO (si no está):** S2 rama B — P0 universo (csv once tablas, LEEME, cuestionario → `data/l2-mps2012-cuestionario-v1_0.txt`, `data/l2-mps2012-items-v1_0.tsv`); P1 FP-263 adjudica con T9b (45 celdas, `W2_P38A` × `W2_P38B`, control `P46`) y ronda 1 (`P40`, `P39`, `P38B_oportunidades`); P2 R7.3/R7.6: SELLADA / RECHAZADA / NO-SELLADA con texto; P3 lista: ¿el ítem sensible es venta de voto? CORROBORADA-EN-TEXTO / VENCIDA EN ALCANCE (el texto en inglés de `man/mexico.Rd` ya lo dice; aquí se lee el español). Todo rotulado segunda mano, sin ponderar, sin IC. Sucesor declarado: L2-bis rama MEDICIÓN cuando el `.dta` llegue.

En ambas: enmiendas append en las dos entradas de L12 en `milpa/tramite-ola5-propuesta-v0.yaml` (`situacion` la cambia un acto siguiente con firma); FP-263 → EJECUTADA o razón escrita; fila B-bis (qué significa que el falsador no refute).

PERÍMETRO Y CONCURRENCIA. Toca: `data/l2-*` · propuesta (append) · `data/INFRAESTRUCTURA-v1_0.md` · `forense/notas/2026-09-0X-MAESTRA38-L2-{spec-rama,resultados}.md` · hallazgos · tablero · A.3 · cascada. NO toca: `milpa/tramite.yaml` · canon (salvo ADR) · `forense/prereg-duelo-v2/**` · manifiesto · salidas de L12 · `data/l2lista-*`. En paralelo corre LOTE-ENSANUT (`data/l16-*`, `data/l17-*`, propuesta append, notas ENSANUT, cascada): quien fusiona segundo re-corre la cascada. Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.

FP/ADR: derivados en el 0-bis. CONTADOR: rama MEDICIÓN → coeficientes de primera mano +3, medición: sí; rama TEXTO → piezas de L12 adjudicadas +2, FP-263 cerrada, medición: cero.

## CONSUMIDO

Ejecutado por `ACTO MAESTRA38-L2` (6/sep/2026), rama `maestra38-l2-mps2012`,
worktree `/home/pc0/mm-maestra38-l2`. Rama elegida: **TEXTO** (ver
`forense/notas/2026-09-06-MAESTRA38-L2-spec-rama.md`). `FP-263` → EJECUTADA.
P2 (R7.3/R7.6): NO-SELLADA. P3 (lista): CORROBORADA-EN-TEXTO. `ADR-356`
(candidato, puede renumerar por la carrera con `MAESTRA38-LOTE-ENSANUT`).
PR: https://github.com/Josanoforo/Modelado-Mexicano/pull/564.
