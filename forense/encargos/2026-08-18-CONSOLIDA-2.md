# ENCARGO · ACTO CONSOLIDA-2 (v2) — la firma de mesa sobre `FP-38`, ejecutada

v2 refrescada contra `68a3466` — la v1 (escrita contra `3e4c9f7`) nunca se lanzó y tres merges cayeron encima (#255/#256/#257)

SHA de redacción: `68a3466` (#257, `origin/main`, verificado por `ls-remote` el 18/ago/2026) Entorno asignado: NUBE (sesión nueva, clon fresco, no superficial). No toca microdato. Estado: CONSUMIDO, `PR #259` · Gatea a: `CONF-07-CIERRE` (lanzarlo cuando este fusione — comparten `glosario`). Delta v1→v2, para que nadie ejecute instrucciones muertas: (1) `estado-programa:136-137` sale del acto — MESA-18AGO/#257 ya ejecutó FP-44/45 ahí (verificado: las dos líneas ya citan conf.02/conf.05); (2) el máximo de ADR ya no es 97 sino 101 — re-derívalo igual; (3) la cita de mesa D-10 ("démosle fila") ya está registrada en `ADR-101` — la fila del corte que este acto crea la cita, no la re-pide; (4) todo lo demás de la v1 se sostiene, re-verificado hoy: `glosario:136` y `:137` siguen `(a)+(c)`, `recovery-plan:65` sigue ENIGH·6 olas, `conf.01` sigue resuelta en `:316`, `FP-38` sigue `ABIERTA` y `FP-43` sigue `FIRMADA` sin ejecutar.

════════ ARRANQUE — hazlo antes de leer el resto ════════ 1 · REPO. Clon fresco si no hay; `git rev-parse --is-shallow-repository` → `false` o `--unshallow`. ⚠️ Actualiza la rama local `main` a `origin/main` antes de cualquier `checkout` (defecto medido en MESA-18AGO: revirtió 48 archivos). Reporta ruta · `git log -1 --format="%h %s"` · `git status`. 2 · SHA. Contra `68a3466`. Si main se movió (hay carriles vivos): re-deriva las premisas del bloque de abajo por contenido, no por número de línea, y reporta la diferencia antes de editar. 3 · data/raw. AUSENTE NO ES PARO — este acto no la usa. Repórtalo y sigue. 4 · ENTORNO (A.2, tres partes): `echo "[${CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE:-sin_variable}]"` · `curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://www.inegi.org.mx/` (nunca `-I`) · `ls data/raw/ 2>/dev/null | head -1`. Un 403/000 es la allowlist de esta caja, no INEGI (A.5). 5 · ESPEJO. Toda cifra sale del clon, con el comando a la vista. ════════════════════════════════════════════════════════
═══ VERIFICACIÓN DE EXISTENCIA — contestada por quien escribe, contra `68a3466` ═══

```
glosario:136 (rama estatus)      → (a)+(c), tier Fuerte, cita única Velandia-Morales   EXISTE-NO-SATISFACE
glosario:137 (rama recompensa)   → (a)+(c), tier Hipótesis razonable                    EXISTE-NO-SATISFACE (sin examinar)
la firma de mesa, verbatim       → forense/encargos/2026-08-17-CONSOLIDA-17AGO.md §PARTE 2:
                                   "El expediente de F38, el experimento no aplica, no es México."  EXISTE-SATISFACE
recovery-plan:65                 → "ENIGH · 6 olas" (falsador R1.4)                     EXISTE-SATISFACE
conf.01 resuelta                 → glosario:316 "Resuelto por precedencia: A/B/C+ sí, D/E no"
                                   ⚠️ CONSOLIDA-17AGO la cita en :315 — desfase de UNA línea;
                                   verifica por contenido SIEMPRE                        EXISTE-SATISFACE
milpa/refutations.yaml           → conf.01.calidad_vs_precio en :94, entra:false en :112 EXISTE-SATISFACE
hitoD-preregistro R1.4           → :59-60, "[FUERTE como correlación]", bloque append-only en :892
fila del corte de edad           → grep -c "corte de edad" forense/firmas-pendientes.tsv → córrelo tú;
                                   al redactar: 0. ⚠️ receta probada: el patrón ancho "corte" da 7
                                   falsos por "cortes iniciales" (FP-02) — usa el patrón exacto      NO-ENCONTRADO
sitios del corte en el modelo    → 9 en canon/modelo-decision-v4_0.md, DOS redacciones distintas:
                                   "corte PENDIENTE" y "Corte de `edad` PENDIENTE" (:361) — prueba
                                   tu patrón contra :457 y :361 antes de reportar el número
estado-programa:136-137          → YA ejecutadas (#257). FUERA de este acto.
```

═══════════════════════════════════════════════════════
PERÍMETRO Y CONCURRENCIA. Este acto toca, y solo esto: `canon/glosario-v5_6.md` · `canon/integrador-psicologia-mexicano.md` · `corpus/reports/Psicología_del_Consumidor_Mexicano__…md` · `milpa/refutations.yaml` · `forense/hitoD-preregistro-v2_0.md` (solo nota fechada en el bloque append-only `:892`) · `forense/firmas-pendientes.tsv` · `canon/gobernanza-v1_15.md` (ADR) · `canon/estado-programa-v1_10.md` (solo cascada `:27`/`:101` — ⚠️ FP-48, cláusula por cláusula en cualquier merge) · `forense/notas/` · `forense/hallazgos.md` (append) · `forense/encargos/`. Carriles vivos: `gate-durable-v7` (Ubuntu — cero archivos en común) y posiblemente `SELLA-RUTAS`/otros en cloud (solo colisión de número de ADR, esperada: re-deriva al escribir y al fusionar). Si te encuentras escribiendo fuera de esta lista, PARA. 🚫 No corras `--freeze`. Rojo → desglose por test y reporta.
1 · COMMIT 1 · `FP-38` — el texto que mesa ya adoptó
No redactes nada nuevo. El texto adoptado vive en `CONSOLIDA-17AGO §PARTE 2`; cópialo, no lo mejores. 1.1 · Re-verifica las dos premisas que el propio encargo exige (`glosario:136` = `(a)+(c)`; `recovery-plan:65` = ENIGH·6 olas). Si alguna falla, PARA. 1.2 · `glosario:136`: procedencia `(a)+(c)` → `(c)`. El tier `Fuerte` de la rama estatus queda sin sostén por esa cita y se marca como tal — no se sustituye por otra. 1.3 · `glosario:137` (rama recompensa) también dice `(a)+(c)` y nadie la ha examinado. Deriva si su `(a)` tiene sostén propio o es el mismo arrastre. Si es arrastre: corrígela aquí y dilo. Si tiene sostén: dilo y no la toques. El examen es entregable en cualquiera de los dos sentidos. 1.4 · Propaga a los cuatro sitios del lector, mismo criterio, sin reescribir análisis: `integrador:36` · `integrador:204` · `report Consumidor:64` · `report Consumidor:84` (⚠️ números de `68a3466`; verifica por contenido). 1.5 · El falsador, declarado y no ejecutado: `R1.4` ↔ ENIGH 6 olas (en disco, caja ocupada). Regístralo como la ruta que devolvería el tier, con lo que tendría que mostrar.
2 · COMMIT 2 · Las tres propagaciones pendientes de `CONSOLIDA-17AGO`
Son propagación de decisiones ya resueltas; si alguna te obliga a decidir, PARA — te saliste. (b) `milpa/refutations.yaml:94-112`: propaga la resolución de `glosario:316` (segmentada A/B/C+ sí, D/E no); el yaml sigue `entra:false` con texto en modo propuesta. Verifica `:316` primero; si no dice eso, PARA. (c) `glosario:399` (lista "sin ADR"): re-deriva cuáles quedan de verdad — sabiendo que `conf.02` ya la resolvió ADR-92(d)/FP-27 y que `conf.07` tiene acto propio gateado a este (`CONF-07-CIERRE`): la lista debe reflejar el estado real sin adelantar sellos que aún no existen. (d) `hitoD-preregistro`, `R1.4`: nota fechada en el bloque append-only (mecanismo declarado en `:892`), nunca edición del cuerpo. Declara que `R1.4` consume la rama estatus de `conf.05` y que esa rama acaba de perder su sostén. No toques su veredicto ni el contador `13 de 27`. Esto cierra `FP-43` — márcala ejecutada con este PR. Lo que NO haces de CONSOLIDA-17AGO: su `PARTE 3` (el barrido de 212 notas). Sigue viva; dilo en la nota para que no se pierda otra vez.
3 · COMMIT 3 · El corte de edad — darle fila, no resolverlo
Deriva los sitios por comando (mi 9 no se hereda; dos redacciones, receta probada arriba), clasifícalos —regla operativa (`:457`, `:482`) / hipótesis / descriptor— y abre una fila `ABIERTA` con la pregunta de mesa redactada, la evidencia (`modelo:809` ya confiesa "un umbral de 'joven' que ningún inventario fija") y la cita D-10 de `ADR-101` como su firma de origen. No inventes el corte — definirlo exige dato mexicano y es acto propio.
4 · COMMIT 4 · Cierre
`FP-38` → `FIRMADA` (`firmada_en` = la cita del 17/ago + su encargo; `ejecutada_en` = este PR) · `FP-43` ejecutada · fila nueva del corte · `CONSOLIDA-17AGO` marcado parcialmente consumido (qué ejecutó este acto, qué sigue vivo — PARTE 3) · ADR re-derivado al escribir y al fusionar (máximo hoy: 101; colisión esperada) · cascada `:27`/`:101` · nota · línea en `hallazgos.md` · este encargo `CONSUMIDO`.
5 · Módulo de auditoría — este acto SÍ afirma sobre México: aplica entero

* ¿Qué parte está sesgada por marcos extranjeros? — es el hallazgo mismo. Di si `conf.05` es caso aislado o síntoma; si es patrón, nómbralo y abre fila, no lo barras aquí.
* ¿Qué conclusión sería peligrosa simplificada? — bajar el tier no dice que el consumo compensatorio no exista en México; escribe la diferencia, porque es la mala lectura que viene.
* ¿Qué afirmación describe el estado del corpus escrita a mano? — ninguna: conteos derivados, receta probada contra caso conocido.
* ¿Cuántos contadores movió? — cero en `13 de 27 · 0 de 15 · 1 de 2`; dilo en una línea al inicio, y di sin adorno que lo que mueve es la honestidad de un tier.

Lo que este acto NO hace: no corre el falsador ENIGH · no define el corte · no ejecuta PARTE 3 · no toca `estado-programa:136-137` (ya ejecutadas), `data/`, `tools/`, `tests/` · no cierra FP-47/48 ni nada de mesa · no congela.
