**ADR-260924-GEN2-TUBERIA-TABLERO-EN-CANAL-1-3726-01** (raíz de acto: `3726` = 4 hex del commit de 0-bis `372646f6`; no se renumera al fusionar), `ACTO GEN2-TUBERIA-TABLERO-EN-CANAL-1 · el tablero deja de tener dos productores`, 24/sep/2026, entorno **NUBE** (hook `ENTORNO-DERIVADO = NUBE`, `senal-corpus: montado=NO archivos_examinados=0`, `red: DENEGADA-POR-POLITICA` para la sonda a INEGI — no aplica: este acto no toca microdato ni red externa salvo GitHub, que sí respondió). Sonnet 5 (encargo sugería subir a Opus solo si el acto tocaba `tablero_programa.py` más allá de la guarda; se quedó en la guarda + copia a Pages, mismo nivel). MODO ABIERTO, **COMPUERTA: ninguna** (verificado: sin línea `GATED a` ni `COMPUERTA:` en el encargo — la sección "8 · COMPUERTAS" es D-20, no un gate de merge). Encargo archivado verbatim (A.3) y sellado en el 0-bis: `forense/encargos/2026-09-24-GEN2-TUBERIA-TABLERO-EN-CANAL-1.md` (`.cuerpo.sha256` = `dc1392b5…`). Base al abrir `origin/main = 3252aaec`, 0 commits de diferencia. Rama `claude/new-session-tydnvn` (la que la plataforma fijó; el propio encargo lo permite: "o la que la plataforma fije"). Duplicado (0.c): `git ls-remote --heads origin`, `git worktree list`, búsqueda de PR abierto por rótulo → 0 coincidencias.

**P1–P4.** El job `guardias` de `.github/workflows/verify.yml` corre `git fetch -q origin main` + `python3 tools/tablero_programa.py --actualiza` después de la guarda de tamaño de `VISTA-NORMALIZADA-2` (ya fusionada en `main` al abrir este acto — el encargo la describía "en vuelo"; logística, no PARO) y antes de `git add`/`gh pr create`, así que el bloque viaja en el mismo commit `[deriva]`. `--actualiza` se niega (código 1) si `git rev-parse HEAD` no resuelve igual a `origin/main`, salvo `--permitir-rama` explícito, que rotula el bloque `NO-ES-ORIGIN-MAIN`; verificado a mano desde esta misma rama sin la bandera: `error: --actualiza se niega -- HEAD (372646f…) != origin/main (82e16b4…). […] usa --permitir-rama para forzarlo — rotula el bloque NO-ES-ORIGIN-MAIN.` (código 1). `/tramite` T0 deja de pasar esa bandera; su refuso (rama administrativa siempre adelante de `origin/main`) queda documentado como esperado, no como fallo del ciclo. `docs/tablero.md` (Pages) es la MISMA derivación escrita dos veces por el mismo comando (`_actualiza_tablero`, ya probada por idempotencia/preservación) — no un segundo productor. `docs/PROTOCOLO-TABLERO.md` fija que una conversación lee, cita e interpreta, y **nunca regenera ni sube versiones**; un bloque `¿árbol == origin/main? False` sin `--permitir-rama` es inválido y se ignora.

**Corrección de premisa (logística, declarada, no PARO).** El encargo cita `canon/TABLERO-PROGRAMA.md` en cinco lugares; ese archivo no existe en el árbol (`ls canon/TABLERO-PROGRAMA.md` → `No such file or directory`). El archivo real es `forense/tablero/TABLERO-PROGRAMA.md` (`find . -name TABLERO-PROGRAMA.md`), y su contenido coincide exactamente con la premisa `[EJECUTADO]` del encargo (`SHA 8a867a04 · ¿árbol==origin/main? False`). Se usó la ruta real en las cuatro piezas y en `data/INFRAESTRUCTURA-v1_0.md`; el cuerpo del encargo archivado NO se edita (A.3) — la corrección va aquí y en la nota.

**Defecto de determinismo (P4, encontrado y corregido por el propio acto).** Un test temporal (dos llamadas reales a `derivar_indicadores()` sobre el árbol real, no committeado) reveló que dos derivaciones sobre el MISMO `HEAD`, separadas por minutos, producían bloques distintos: `ramas_remotas_detalle()` (`tools/estado_comun.py`) corre `git ls-remote --heads origin` y `git fetch --prune origin +refs/heads/*:...` EN VIVO contra un repositorio compartido con sesiones concurrentes — medido en este mismo acto, 3 ramas en una derivación y 4 en la siguiente, mismo commit. Esto no es función del árbol commiteado, así que rompe la garantía «regenerado sobre `origin/main` en un clon limpio es idéntico byte a byte» que el «Hecho» del encargo exige. Corrección (≤10 líneas, autorizada por el propio `[SUPUESTO]` del encargo): la línea "Ramas presentes en origin" sale de `render_bloque_vivo()` — restaura lo que el docstring de esa función ya prometía ("sin cifras efímeras… ramas remotas presentes, que siguen disponibles en la salida interactiva normal") y que el código no cumplía desde que `ACTO GEN2-TABLERO-SENAL-1` la añadió. El indicador `ramas_remotas_detalle` se sigue derivando y queda disponible en `--json` y `tools/tablero_vista.py`; solo deja de imprimirse en el bloque committeado. Test existente ajustado (`prueba_render_incluye_marcador_corridas_ramas_nc` → `prueba_render_incluye_marcador_corridas_nc`, invierte la aserción). **La prueba de dos pasadas NO quedó en el archivo committeado**: medida en `123.1s` UNA sola llamada a `derivar_indicadores()` (corrida0 status sobre 335 entradas), dos pasadas habrían corrido >240s contra el timeout de clasificación de `40s` que usa `tools/ci_guardias.py --censo` (`ejecuta()`), clasificando el ARCHIVO ENTERO `FALLA-DE-VERDAD (TIMEOUT>40s)` y sacando sus otras 13 pruebas de `--ejecuta-huerfanos` en silencio — el costo de una prueba le habría quitado cobertura a las demás (D-14). Confirmado tras el fix, versión committeada: `tests/test_tablero_programa.py`, 13 pruebas, 0 fallos, `0.32s`.

**Contadores.** Cero mediciones; `celdas_validadas: 219 → 219 (Δ0) @ 247720d5` (D-16; este acto no mide ni adopta). `check.py --rapido`: 0 FAIL, 410 WARN (WARN no adjudican, D-16), 9.16s.

**Cascada.** Nota `forense/notas/2026-09-24-GEN2-TUBERIA-TABLERO-EN-CANAL-1-cierre.md`. Fragmento L0 `canon/L0/ADR-260924-GEN2-TUBERIA-TABLERO-EN-CANAL-1-3726-01.md`. Rótulo `GEN2-TUBERIA-TABLERO-EN-CANAL-1` censado en `canon/registro-rotulos.tsv`. `forense/no-corrido.tsv`: `NC-260924-GEN2-TUBERIA-TABLERO-EN-CANAL-1-3726-01` (P5, prueba real post-merge, NO-VERIFICABLE-AQUÍ) y `…-3726-02` (Pages sirviendo `docs/tablero.md`, DIFERIDO-A `FP-260923-GEN2-FRONT-1-4296-01`, fin de semana 26-27/sep). Este ADR no mueve el espacio numérico cerrado. **El PR no se fusiona en este acto: mesa fusiona.**
