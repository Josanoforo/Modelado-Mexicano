ENCARGO · ACTO GEN2-MEDICION-DEMANDA-2
Corre en CAJA las specs congeladas del mapa-19 que quedaron sin correr (residuos de `GEN2-SPECS-DEMANDA-2`, `PR #781`) y las que `GEN2-FIRMAS-MESA-1` congele por tanda: dos commits por corrida, verify doble, firma de contador embebida

CABECERA · redactado contra `582d4e9` (merge de #779, `GEN2-MEDICION-DEMANDA-1` — base verificada en el ARRANQUE, 0 commits detrás de `origin/main`) · ENTORNO: **CAJA** Ubuntu/WSL2 con corpus compartido montado (`data/raw` enlazado a `/home/pc0/mm-corpus/raw`, `data/raices.local.yaml` copiado del worktree del predecesor con `descargas_mx` apuntando al espejo `/home/pc0/mm-corpus/descargas_mx_espejo`; `tools/entorno.py --sonda-red`: `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=sin_variable · red=200 · corpus=SI(examinados=413) · python 3.14.4 · numpy 2.3.5 · pandas 2.3.3 · pyreadstat 1.3.6`) · COMPUERTA: ninguna declarada por el lanzamiento (ni `GATED a` ni `COMPUERTA:`); la tanda 2 (`ENCIG-2023`, `DENUNCIA-SEGURO`) se consume cuando `GEN2-FIRMAS-MESA-1` la fusione, no antes ni desde su rama · MODELO: Opus (integral) · Estado: VIVO · candidatos FP/ADR/NC: deriva al cierre (`cierre_acto.py` Fase A al arrancar: ADR real 512, candidato 513, no redactado en ninguna rama remota; acto en vuelo que puede tomarlo primero: `GEN2-FIRMAS-MESA-1`, rama `origin/claude/epic-thompson-wbbmg7`, sin PR abierto a las 19:05 UTC).

LANZAMIENTO, verbatim (mesa, 15/sep/2026): el texto de PIEZAS de abajo es el mensaje de lanzamiento tal como llegó; este archivo lo fija por A.3 porque llegó pegado en el mensaje que invocó `/acto`, no como archivo del repo. Todo lo que sigue a PIEZAS (VERIFICACIÓN DE EXISTENCIA, PERÍMETRO, CONTADOR, LO QUE NO HACE, CIERRE) lo deriva el ejecutor del lanzamiento y de la casa; no es texto de mesa.

PIEZAS (texto de mesa, verbatim)

> CAJA — ACTO GEN2-MEDICION-DEMANDA-2 (Opus, integral; caja libre salvo el servicio de adquisición, que no comparte archivo)
>
> Munición verificada en corridas.tsv: 3 specs congeladas sin correr — CALC-TIENE-AHORROS-0001 (CORR-0009), CALC-DINERO-FAMILIARES-VEJEZ-0001 (CORR-0010), CALC-HORIZONTE-VIA-DERIVADOS-0001 (CORR-0015 — verificar el padre antes: su fila dice DEPENDE-DE-CORRIDA-PADRE). Más 2 que llegan por tanda cuando FIRMAS-MESA-1 fusione (ENCIG-2023 sucesora de ENCIG-0001 y DENUNCIA-SEGURO). Dos commits por corrida, D-11 leído como GEN2, verify doble. Firma de contador embebida desde el lanzamiento, para no repetir FP-375/NC-0198: «FIRMA DE MESA, mesa, 15 de septiembre de 2026 — OBJETO: cuenta_gen2 = SI para toda corrida que este acto selle sobre spec congelada del mapa-19; no adopta al motor.» Perímetro: corridas/resultados/decisiones, notas; no toca milpa, usos ni la cola. Contador: +3 a +5 corridas.

FIRMA DE CONTADOR con OBJETO (verbatim del lanzamiento, que es lo que se cita): autoridad = mesa; fecha = 15 de septiembre de 2026; texto = «FIRMA DE MESA, mesa, 15 de septiembre de 2026 — OBJETO: cuenta_gen2 = SI para toda corrida que este acto selle sobre spec congelada del mapa-19; no adopta al motor.» Cada corrida sellada aquí escribe su fila `cuenta_gen2=SI` en `data/corrida0/decisiones.tsv` citando este párrafo; los `spec.yaml` congelados por otros actos no se editan (E.3) — el registro lee la firma de `decisiones.tsv` con precedencia 1 (`_cuenta_gen2_resuelto`); el merge de mesa perfecciona la firma (patrón `FP-367`/`FP-368`, como en `GEN2-MEDICION-DEMANDA-1`).

VERIFICACIÓN DE EXISTENCIA (A.8, contestada por el ejecutor contra `582d4e9`; el lanzamiento llegó sin el bloque y A.8 manda pararse o contestarlo con comando, no suponerlo):

(1) ¿Existe ya la estructura? SÍ. `data/corrida0/demanda-corridas.tsv` (derivado) gobierna las 82 corridas requeridas; `data/corrida0/mapa-demanda-19-corr-v1_0.tsv` (a mano, `GEN2-SPECS-DEMANDA-1/-2`) gobierna las 19 sin candidato; `data/corrida0/{corridas,resultados,usos}.tsv` es el registro que `corrida0 registro --verifica --escribe` re-deriva; `data/corrida0/decisiones.tsv` es donde vive la firma de contador; `data/corrida0/CALC-*/` es la caja de cada corrida. Ningún hueco de índice.

(2) ¿Existe ya el contenido? Parcialmente, y con **una corrección de premisa y dos defectos de cableado** que se declaran aquí antes de correr nada:
```
$ python3 tools/corrida0.py status | grep corridas
N_corridas_requeridas=82
N_corridas_selladas=72
$ awk -F'\t' 'NR>2 && $4 ~ /SIN-CANDIDATO/' data/corrida0/demanda-corridas.tsv | wc -l   → 19
$ awk -F'\t' 'NR>2 && $4 ~ /SIN-CANDIDATO/ {s+=$5} END{print s}' data/corrida0/demanda-corridas.tsv   → 94
$ awk -F'\t' '$4=="SPEC-FIJADA"' data/corrida0/corridas.tsv | cut -f3
CALC-DINERO-FAMILIARES-VEJEZ-0001
CALC-EVASION-NORMA-0001
CALC-HORIZONTE-VIA-DERIVADOS-0001
CALC-TIENE-AHORROS-0001
```
**(2.a) Corrección de premisa — son CUATRO specs congeladas sin correr, no tres.** El lanzamiento nombra tres; `corridas.tsv` trae además `CALC-EVASION-NORMA-0001` (`CORR-0007`, `RES-0025`/`RES-0026`), congelada por el mismo `PR #781`, misma clase (`TASA-BASE-DE-REGLA-YA-EJECUTADA`, registro D-15 de una medición ya sellada en `milpa/` el 2/sep/2026) y misma fila del mapa-19. El OBJETO de la firma dice «toda corrida que este acto selle sobre spec congelada del mapa-19», así que la cuarta cae dentro del OBJETO por su letra: **este acto la corre también** y lo declara aquí y en `## NO-CORRIDO / RESERVAS` como lectura del ejecutor, no como texto de mesa. Si mesa la excluyó a propósito, la corrida sobra y se dice así; el costo de haberla corrido es un sello más, no una adopción.
**(2.b) Defecto de cableado 1 — ninguna de las cuatro carpetas trae `spec.md`.** Los cuatro `spec.yaml` declaran `spec_md: spec.md` con `spec_md_sha256` igual al sha256 de la spec humana sellada en `forense/prereg-caja/`, pero el archivo no está en la carpeta: `preflight` 4/4 → `BLOQUEADO ausente=data/corrida0/CALC-…/spec.md`.
**(2.c) Defecto de cableado 2 — tres `script:` apuntan a scripts legados de `tools/` que no exponen `medir(inputs, contrato)`** (`tools/medidor_ahorro_enif24.py`, `tools/tasas_base_fase1.py`, `tools/medidor_evasion_norma_envipe25.py`; `grep -c "^def medir(" → 0 en los tres`), y el runner los declara `NO-EJECUTABLE` por doctrina («no se adivina otra entrada ni se llama a `main()` por si acaso»). El propio mapa-19 dice para las tres filas «CAJA · escribir medidor.py», pero un `medidor.py` en la carpeta no corre si `script:` no lo nombra. La cuarta (`HORIZONTE-VIA-DERIVADOS`) declara `sha256: LEER-AL-EJECUTAR -- …` en sus dos inputs `origen: repo` → `preflight` lo lee como `DISCORDA` y bloquea siempre.
**Consecuencia (E.3: una spec congelada no se edita):** las cuatro se **suceden con `v1_1`** en carpeta propia (`CALC-<id>-v1_1/`), `repite_de` en la raíz del `spec.yaml` (para que el registro marque la `v1_0` `SUPERADO→v1_1`), `script:` al `medidor.py` de la propia carpeta, `spec.md` presente, sha256 reales en los inputs `repo`; universo, variables, ponderador, transformación, estimando, parámetros, tolerancia y los ids de `RESULT` se copian **verbatim** de la `v1_0`. Los medidores reutilizan por `import` el cargador y el estimador **del script legado que la `v1_0` nombra** (mismo código, misma semilla 42), redirigiendo sólo la ruta al payload que `preflight` resolvió (P1). Cada `v1_1` se congela en un commit propio ANTES de abrir un solo valor, probada contra payload sintético. La `v1_1` de `HORIZONTE` se congela **después** de sellar `TIENE-AHORROS-v1_1` (su padre, `CORR-0009`), tomando el `sha256` real de ese `resultados.json` como input: el encadenamiento `DEPENDE-DE-CORRIDA-PADRE` queda mecánico, no prosa.

(3) ¿La estructura es posterior al trabajo? La munición nació hoy (`PR #781`, 15/sep/2026); la brecha retroactiva de `demanda-corridas.tsv` ya la declaró `GEN2-SPECS-DEMANDA-1`. Este acto no clasifica reglas del motor ni cita ids de regla o `R-n` en este encargo: `tools/ya_medido.py` no aplica (T-YAMEDIDO no se dispara). **Tanda 2:** `GEN2-FIRMAS-MESA-1` (`origin/claude/epic-thompson-wbbmg7`, `2b8572a`, 7 commits, NUBE) todavía no trae ninguna carpeta `CALC-*` ni spec nueva en `forense/prereg-caja/` (`git diff --stat origin/main...` sin esas rutas a las 19:05 UTC); se consume cuando fusione.

PERÍMETRO (derivado por el ejecutor del lanzamiento «corridas/resultados/decisiones, notas»): `forense/encargos/2026-09-15-GEN2-MEDICION-DEMANDA-2.md` (este archivo) · `data/corrida0/CALC-*-v1_1/{spec.yaml,spec.md,medidor.py}` (COMMIT-1 de cada sucesión) y `{ejecucion.json,resultados.json,sello.json,sello.sha256}` (COMMIT-2) · `data/corrida0/decisiones.tsv` (una fila `cuenta_gen2=SI` por corrida sellada) · `data/corrida0/{corridas,resultados,usos}.tsv` (re-derivados por `corrida0 registro --verifica --escribe`; `usos.tsv` debe salir idéntico a la base) · `forense/notas/2026-09-15-GEN2-MEDICION-DEMANDA-2-cierre.md` · `forense/no-corrido.tsv` · `canon/gobernanza-v1_15.md`, `canon/estado-programa-v1_13.md`, `canon/registro-rotulos.tsv`, `tests/check.py` (`_T25_ARCHIVOS_CONOCIDOS`, si hace falta) (cascada). NO toca: `milpa/` · `usos` (ninguna adopción: sellar no es adoptar, E.2) · `data/cola-adquisicion-v1_0.tsv` · `data/manifiesto.yaml` · `tools/*.py` · ninguna spec congelada `v1_0` (ni sus `spec.yaml`) · ningún CALC ya sellado · ningún payload (cero descargas).

CONTADOR: `N_corridas_selladas` (72 al arrancar) y `N_resultados_gen2_sellados` (3 255 al arrancar); se reporta al cierre con `status` antes/después medido en un worktree limpio de `origin/main`, y con la lista de corridas selladas con `cuenta_gen2 = SI`. Lanzamiento: «+3 a +5»; con la cuarta spec del punto (2.a) el techo posible es +4 sin tanda 2 y +6 con ella.

LO QUE NO HACE: no redacta specs nuevas de fondo (sucede `v1_0 → v1_1` copiando el contrato verbatim; sólo cambia el cableado que impide correr) · no edita ninguna spec `v1_0`, `spec.yaml` ajeno ni script de `tools/` · no adopta ninguna cifra a ningún consumidor · no descarga · no firma por mesa nada distinto de lo que el lanzamiento trae verbatim · no consume la tanda 2 desde la rama de `FIRMAS-MESA-1` (sólo desde `origin/main`) · no fusiona el PR propio ni ajeno.

CIERRE: cascada completa (`tools/cierre_acto.py` preflight → ADR → L0 → `--aplica` → rótulos → T25 → suite → push → UN PR) + `## NO-CORRIDO / RESERVAS` + `## CONSUMIDO` con el PR.

## NO-CORRIDO / RESERVAS

| qué | por qué | impacto | sucesor |
|---|---|---|---|
| `CALC-ENVIPE-DENUNCIA-SEGURO-0001 · REPRODUCE-GEN1` — la spec fija `grano_gen1_decimales: 6` sobre valores legacy que sólo tienen 4 decimales (0.7909 / 0.2091 / 0.672 / 0.328, rellenados a `0.790900` / `0.672000` en `demanda-resultados.tsv`) (`NC-0221`) | `DECISIÓN-DE-MESA-PENDIENTE`. Corrido y sellado: `n` exactos (U 1 016 / CON 402 / SIN 614), `DELTA-VS-GEN1` máx 1.4e-5, los cuatro `round(p, 4)` coinciden con lo publicado; al grano 6 que la spec exige sale `NO-REPRODUCE` → `ADOPCION = LISTADO-PARA-MESA-NO-REPRODUCE`. Es un `NO-REPRODUCE` de grano, no de cifra; se atribuye y no se acepta. El medidor no se tocó («NO-REPRODUCE no autoriza tocar el medidor») | Los cuatro `RESULT` de `RES-0039..0042` están sellados con IC de diseño pero su etiqueta de adopción dice `NO-REPRODUCE`; `N_resultados_gen2_adoptados_activos` 16 → 16 | MESA: aceptar la reproducción al grano 4 en que GEN1 publicó (adoptar tal cual) o pedir sucesora `v1_1` con `grano_gen1_decimales: 4` — nunca editar la spec sellada |
| `CALC-ENCIG-2023-0001-v1_1 · B-ADOPCION = LISTADO-PARA-MESA-ESTIMABLE` — `B-COBERTURA = 1.0` no mide la autoselección de 8.3 en 2023 (`NC-0222`) | `DECISIÓN-DE-MESA-PENDIENTE`. Reserva, no omisión: el `sec_8` de 2023 trae fila (con `P8_4 = NA`) para **todo** trámite, así que «emparejadas / filas de sec_7» da 1.0; la autoselección se ve en `B-N-P84-BLANCO = 100 086` → cobertura efectiva `B-N-U / G-N-FILAS-SEC7 = 23 100 / 123 186 = 0.19`, la misma que 2025 (0.20). Por la regla declarada en el código (umbral 0.5 sobre `B-COBERTURA`) salió `ESTIMABLE`; leído con la cobertura efectiva sería `CON-RESERVA`. El estimando es condicional al grupo observado por 8.3, como en 2025. Además, la spec ya declara que `RES-0001/0002` son ASIGNADO **sin canal** y el medido es **por canal**: candidatura, no relevo | `CORR-0001` sigue con candidatura, no relevada; ninguna cifra de `milpa/` se mueve | MESA (lee `B-ADOPCION` como `CON-RESERVA`; decide la correspondencia medido-por-canal ↔ ASIGNADO-sin-canal) |
| `CALC-EVASION-NORMA-0001` (→ `-v1_1`) — cuarta spec `SPEC-FIJADA` del mapa-19 (`CORR-0007`, `RES-0025/0026`), **no nombrada en el lanzamiento** («3 specs congeladas sin correr») (`NC-0223`) | `DECISIÓN-DE-MESA-PENDIENTE`. Se corrió y selló como lectura del ejecutor: el OBJETO de la firma dice «toda corrida que este acto selle sobre spec congelada del mapa-19» y `CORR-0007` es fila del mapa-19; misma clase y mismo `PR #781` que las tres nombradas. Si mesa la excluyó a propósito, la corrida sobra | `N_corridas_selladas` +1 (79 en vez de 78) y `CORR-0007` AGOTADA 8/8 dependen de esta lectura | MESA: ratificar (nada que hacer) o retirar la fila `CALC-EVASION-NORMA-0001-v1_1` de `decisiones.tsv` (el sello queda; el contador baja 1) |
| `CALC-ENCIG-2023-0001` (v1_0) — sellada `NO-ESTIMABLE-CODIGO-FUERA-DE-MAPA:P8_4` y superada por `v1_1`, **cuenta en `N_corridas_selladas`** (`NC-0224`) | `DECISIÓN-DE-MESA-PENDIENTE`. El primer resultado es el que se reporta: se selló y se sucedió en vez de corregir hacia atrás. El contador de la casa cuenta `SUPERADO` sellado con `cuenta_gen2=SI`, y la fila de firma se escribió para las siete corridas selladas. Se dice sin disfraz: +7 en el contador, **+6 corridas con cifra** | `N_corridas_selladas` 79 (sería 78 sin la superada); `N_resultados_gen2_sellados` no cambia (los 59 ids son los mismos de la `v1_1`) | MESA: dejarla contada (precedente: `CALC-ENVIPE-U4-2012` v1.0 superada cuenta) o retirar su fila de `decisiones.tsv` |
| Adopción de los `RESULT` `LISTADO-PARA-MESA*` a `milpa/` | `FUERA-DE-PERÍMETRO` (sellar no es adoptar, E.2; el lanzamiento dice «no adopta al motor»; `usos.tsv` byte a byte igual a `origin/main`) | `N_resultados_gen2_adoptados_activos` 16 → 16; `N_resultados_gen2_pendientes_adopcion` 5 → 12 | MESA (ventanilla de adopción) |

## CONSUMIDO

PR [#789](https://github.com/Josanoforo/Modelado-Mexicano/pull/789), abierto contra `main` el 15/sep/2026 y **no fusionado por el ejecutor** (el merge es de mesa). Cascada de cierre completa en `82dcec9`: **ADR-516** en `canon/gobernanza-v1_15.md` (redactado como `ADR-514`; `PR #788` tomó 514 y `PR #790` tomó 515 al fusionar primero → renumerado a 516, y `NC-0211..0214` → `NC-0221..0224`), anotación L0 en `canon/estado-programa-v1_13.md`, los tres contadores mecánicos reconciliados por `tools/cierre_acto.py --aplica` (`513→514` al redactar; `515→516` tras fusionar #788/#790), rótulo `GEN2-MEDICION-DEMANDA-2` censado en `canon/registro-rotulos.tsv` (columna 2; el «SI» del preflight era un falso positivo de subcadena sobre la fila del predecesor), `NC-0193`/`NC-0200`/`NC-0201`/`NC-0204`/`NC-0207` CERRADAS por producto y `NC-0221..0224` nuevas en `forense/no-corrido.tsv`, nota de cierre en `forense/notas/2026-09-15-GEN2-MEDICION-DEMANDA-2-cierre.md`.

Producto: **siete corridas selladas** con `verify REPRODUCE · CONTEXTO=IDENTICO` ×2 (14/14) — tanda 1 (`PR #781`, vía sucesión `v1_1` de contrato verbatim porque las `v1_0` eran inejecutables como estaban): `CALC-TIENE-AHORROS-0001-v1_1`, `CALC-EVASION-NORMA-0001-v1_1`, `CALC-DINERO-FAMILIARES-VEJEZ-0001-v1_1` (las tres `RELEVA-SELLADO = SI`), `CALC-HORIZONTE-VIA-DERIVADOS-0001-v1_1` (7/7); tanda 2 (`PR #785`, fusionado a mitad del acto, medidores propios): `CALC-ENVIPE-DENUNCIA-SEGURO-0001` (`n` exactos; `NO-REPRODUCE` sólo de grano, `NC-0221`), `CALC-ENCIG-2023-0001` (primer resultado `NO-ESTIMABLE`: el blanco se escribe `NA`; sellado, no corregido hacia atrás) y `CALC-ENCIG-2023-0001-v1_1` (`TASA-REPORTADA`, `H1-SOSTENIDA`). Siete filas de firma de contador con OBJETO en `data/corrida0/decisiones.tsv`; registro re-derivado con `--verifica --escribe` sobre la base `5973f12` con cero pisadas fuera de filas propias y `usos.tsv` idéntico (cero adopciones): **`N_corridas_selladas` 72 → 79** (6 con cifra + 1 superada), **`N_resultados_gen2_sellados` 3 255 → 3 405**; `CORR-0007`, `CORR-0009`, `CORR-0010`, `CORR-0015` AGOTADAS; `CORR-0001` con candidatura. `TZ=UTC python3 tests/check.py --baseline` **VERDE** (`3 FAIL · 4230 WARN`, nada nuevo frente a `tests/baseline.json`).

**Este encargo queda consumido.** Las dos tandas que el lanzamiento nombraba existen y están corridas; lo que queda abierto son cuatro decisiones de mesa (`NC-0221..0224`), ninguna de ellas una corrida pendiente.
