ENCARGO · ACTO MAESTRA32-E2 · EMPAREJA-MOTOR-TEXTO
SHA de redacción: `2953716` (main, merge PR #391, 27/ago/2026 21:51) · Redactado: 28/ago/2026, dirección maestra-32 · Instrucciones vigentes: v2.11 · Estado: NO LANZADO — GATED a que `MAESTRA32-E1 · SELLA-ENLACE` fusione (serie estricta; ninguna dependencia sustantiva con su letra: este acto corre igual con A, B o C).
ENTORNO ASIGNADO: NUBE (`cloud_default`). NO se lanza en UBUNTU — los tres insumos están versionados en el repo (`data/inventario-reactivos-v1_1.tsv`, `data/inventario-fd-v1_0.tsv`, `milpa/procedencia.yaml`); no se abre ningún payload. Precedente de esta clase: `MAESTRA31-E7 · ETIQUETA` corrió en NUBE sobre tablas versionadas.
Serie MAESTRA32, posición 2 de 3. Nada corre en paralelo. `MAESTRA32-E3` no arranca hasta que este PR fusione.
════════ ARRANQUE — hazlo antes de leer el resto del encargo ════════
Reporta las cinco líneas de abajo y NO empieces hasta tenerlas. Si algo no cuadra, PARA y repórtalo: encontrar que el terreno no es el que el encargo supone es entregable, no interrupción.
1 · REPO. Localiza el CLON EXISTENTE. No clones uno nuevo salvo que no haya ninguno, y si clonas, dilo. Reporta: ruta absoluta · git log -1 --format="%h %s" · git status ⚠️ No arranques desde el home. Si el cliente avisa "launched in your home directory", cámbiate al clon antes de nada.
2 · SHA. Confirma contra qué base trabajas y compáralo con el que el encargo declara. Si main se movió: NO es PARO — refresca, re-deriva lo que dependa del perímetro, y reporta la diferencia antes de editar.
3 · data/raw. AUSENTE NO ES PARO. Es raíz integrada, gitignorada, resuelta por código; un clon fresco siempre nace sin ella. Se crea o se enlaza. Reporta: existe / la enlacé a <ruta> / la creé. ⚠️ Si este acto DESCARGA algo: verifica al cerrar que los payloads quedaron en el CORPUS COMPARTIDO y no solo en tu worktree. Es el defecto de PR #77 y no lo atrapa ningún test.
4 · ENTORNO. "Ubuntu" no distingue nada; la política de red sí. CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE → esperado: sin_variable curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://www.inegi.org.mx/ Reporta los dos valores crudos. NUNCA curl -I. Si este acto no toca microdato ni red, dilo y salta este punto. ⚠️ [NUEVO v2.11] Un negativo producido por un comando que no examinó archivos no es un negativo (A.13). Todo veredicto negativo —incluida la sonda de este punto— declara cuántos archivos examinó el comando que lo produjo.
5 · ESPEJO. Prohibido derivar cifras del espejo del proyecto: está versiones atrás del repo y contiene archivos que el repo nunca tuvo. Toda cifra sale del clon de (1), con el comando a la vista.
════════════════════════════════════════════════════════════════════
A.2, tercera parte: `ls data/raw/ 2>/dev/null | head -1` — reporta el valor crudo; para este acto se espera ausente y no importa.
Regla de lectura: todo campo de `milpa/procedencia.yaml` se abre completo con `yaml.safe_load`. Nunca `grep` de subcadena, nunca `sed` de una línea. En esta caja, si el `grep` de shell trae patrones con acentos, o corre en Python (UTF-8) o el negativo no vale (A.13 — le pasó a dirección el 28/ago con "función de enlace": 0 hits de shell contra 13 reales).
═══ VERIFICACIÓN DE EXISTENCIA — contestada por dirección (maestra-32, 28/ago/2026, contra `2953716`) ═══
1 · ESTRUCTURA. `data/INFRAESTRUCTURA-v1_0.md` (262 renglones): 0 menciones de "inventario" — las tablas gobernantes reales de este dominio (`inventario-reactivos-v1_1`, `inventario-fd-v1_0`, `cruce-inverso-v1_1`) nacieron el 27-28/ago, posteriores al índice (12/ago). Hueco declarado, y este encargo lo cierra por regla de conducto (`ADR-70(c)`): añade a la tabla "Si tu encargo hace X, escribe en Y" la fila de la familia de inventarios y de la tabla que este acto produce. No es inventar una vía: es la familia que `ADR-213`–`ADR-217` ya gobiernan.
2 · CONTENIDO. ¿Existe ya un emparejamiento requisito-de-par ↔ texto del corpus? NO-ENCONTRADO, con universo: `data/` (63 entradas, 0 con `emparej|pareo|match`); `forense/` (703 archivos `.md`; los 5 hits de "emparejamient" son `crosswalk-pregunta-regla-v1_1.tsv` y `enlace-M-v1_0.md` — dominio distinto, corredor M del duelo —, `hallazgos.md`, y las dos notas de `MAESTRA31-E5` que lo declaran sucesor pendiente; `ADR-215` verbatim: "no empareja el texto contra el motor… sigue siendo trabajo de un sucesor"; `FP-173.gatea` nombra exactamente este acto). `data/cruce-inverso-v1_1.tsv`: EXISTE-NO-SATISFACE para este objeto — mide otro eje (token exacto de variables YA citadas por el motor; su Q3: 0 de 30 parámetros citan token), no requisito-de-par contra texto libre.
3 · COBERTURA RETROACTIVA. La capa de texto (`inventario-fd`, 27/ago) y la etiqueta reparada (`v1_1`, 28/ago) no existían cuando los censos de ruta sellaron sus `SIN-RUTA` (pre-25/ago): esos cierres nunca vieron estas tablas — es la razón de ser del acto. Simétricamente: los veredictos de ESTE acto quedan sellados sobre el corpus abierto hoy (316/720 payloads con filas + capa FD 32/33) y vencerán en alcance cuando `MAESTRA32-E3` amplíe el universo — declarado aquí, no descubierto después (A.10).
═══════════════════════════════════════════════════════════════════
0-bis · A.3
Primer commit: este encargo verbatim en `forense/encargos/2026-08-28-MAESTRA32-E2-EMPAREJA-MOTOR-TEXTO.md`. Al cerrar, `## CONSUMIDO` con el PR.
Premisas, con procedencia

1. Los 9 pares objeto (los sin β̂, excluido el `RUTA-I`): `G2.sens_estatus` · `G2.aversion_riesgo` · `G3.aversion_riesgo` · `G4.horizonte_temporal` · `G4.sens_estatus` · `G5.familismo_apoyo` · `G5.familismo_obligacion` · `G5.radio_confianza` · `G6.deferencia` (`estado-motor` §0.4/§2, re-derivado por dirección). `G3.horizonte_temporal` queda FUERA: su tope es de identificación (GATE·ID-X), pregunta de mesa aparte, no de búsqueda.
2. Notas vigentes de las filas G5, leídas íntegras por dirección (`yaml.safe_load`): `G5.familismo_apoyo` — "único candidato (ENIF p9_9_4) excluido por circularidad, marca C3"; `G5.radio_confianza` — "reactivo (ENCUCI) y desenlace (ENIF) en instrumentos distintos, sin muestra común"; `G5.familismo_obligacion` — "sin magnitud asignada (ADR-30); condicional propia solo PROXY…". La búsqueda de G5 es por candidatos no circulares y por co-observación en un mismo instrumento.
3. Insumos: `data/inventario-reactivos-v1_1.tsv` (178,246 filas de datos, 109 instrumentos + 1 cubo) y `data/inventario-fd-v1_0.tsv` (17,094 filas con texto al 100%). Anclas de θ ya fijadas: `familismo_apoyo` (ENIF 2024) · `radio_confianza` (ENCUCI 2020).
4. Las cláusulas falsables y salidas de los generadores viven en `canon/modelo-decision-v4_0.md` §2.1 (tabla de siete) — G5 genera "pooling, corresidencia, carga de cuidado"; G2 "ansiedad de estatus, consumo compensatorio"; G6 "deferencia, iniciativa suprimida". El requisito de desenlace por par se deriva de ahí, no de memoria.

Verifica estas premisas antes de ejecutar (v2.1). Si alguna no se sostiene, PARA y repórtalo.
Objeto
Para cada uno de los 9 pares: derivar su pareja mínima (operacionalización de la θ + operacionalización de un desenlace del generador + co-observación en un mismo instrumento/muestra), buscarla contra las dos tablas, y sellar un veredicto A.4 por par con universo declarado. Es un censo con receta congelada, no una medición.
COMMIT-1 — especificación congelada ANTES de tocar las tablas
Archivo `forense/notas/2026-08-28-empareja-spec.md`, un solo commit, que contiene: (a) por par, el requisito de θ y de desenlace derivado de `modelo-decision` §2.1-§2.2 y de la fila B íntegra, con cita archivo:línea; (b) por par, la lista cerrada de términos/regex de búsqueda (español, sin acentos Y con acentos, sinónimos declarados) para `variable_id`, `instrumento` y `texto_reactivo`; (c) el criterio de CANDIDATO (qué combinación de hits cuenta) y el criterio de co-observación (mismo `instrumento`; la mera co-familia no basta); (d) la exclusión de circularidad: un candidato cuya operacionalización sea la propia θ (o su transformación trivial) se marca `CIRCULAR-EXCLUIDO`, precedente ENIF p9_9_4; (e) prioridad de corrida: `G5.familismo_apoyo`, `G5.radio_confianza`, luego los 7. Cierra con la frase: "el primer resultado que produzca este procedimiento es el que se reporta." Si la especificación estaba mal, un tercer commit lo dice; nunca se corrige hacia atrás.
COMMIT-2 — corrida única
`data/emparejamiento-motor-v1_0.tsv`: una fila por (par × candidato) con `gen`, `coef`, `lado` (θ|desenlace), `variable_id`, `instrumento`, `texto` (recorte ≤200 chars), `via` (id|texto), `veredicto_candidato` (CANDIDATO | CIRCULAR-EXCLUIDO | DESCARTADO-con-razón). Más el resumen por par, vocabulario A.4 obligatorio: `EXISTE-SATISFACE` (pareja mínima co-observada) · `EXISTE-NO-SATISFACE` (existe un lado, o ambos sin muestra común — se dice qué falta) · `NO-ENCONTRADO` (con términos y universo) · nunca "no existe". A.13 en cada veredicto: filas examinadas por comando (esperado: 178,246 y 17,094 — re-deriva, no heredes), archivos examinados, y para cada búsqueda en cero, el conteo que prueba que el comando corrió.
B-bis — qué significa cada desenlace, declarado antes de correr

* 0 de 9 con `EXISTE-SATISFACE` — corrobora el techo que los censos de ruta ya declararon, ahora contra la capa de texto que ellos no vieron: resultado informativo de primer orden, no fracaso; ordena el carril de adquisición y le pone nombre a lo que falta, par por par.
* ≥1 con `EXISTE-SATISFACE` — habilita un acto de medición sucesor (dos commits, UBUNTU), que NO es este acto. Este acto no mide ni cambia `ruta:`.
* Estampa (A.10): todos los veredictos se sellan "sobre corpus abierto al 43.9% (316/720) + FD 32/33, `main=<SHA del arranque>`"; vencen en alcance cuando E3 amplíe; el re-sello es `MAESTRA32-E4 · RE-EMPAREJA` con esta MISMA spec congelada, sucesor declarado, no lanzado.

Falsador del acto
Si la receta de COMMIT-1 resulta inaplicable a ≥3 pares porque el requisito no es operacionalizable desde §2.1 (la cláusula no nombra un desenlace medible), eso se reporta como hallazgo de especificación del modelo — no se parcha la receta ni se inventan desenlaces. Regla de tope: una vuelta, cero iteración.
PERÍMETRO Y CONCURRENCIA
Archivos que este acto puede tocar, y ningún otro: `forense/encargos/2026-08-28-MAESTRA32-E2-EMPAREJA-MOTOR-TEXTO.md` · `forense/notas/2026-08-28-empareja-spec.md` (COMMIT-1) · `data/emparejamiento-motor-v1_0.tsv` (COMMIT-2) · `forense/notas/2026-08-28-empareja-cierre.md` · `data/INFRAESTRUCTURA-v1_0.md` (solo las filas nuevas de la tabla X→Y) · `forense/firmas-pendientes.tsv` (fila nueva) · `canon/gobernanza-v1_15.md` · `canon/estado-programa-v1_10.md` · `canon/registro-rotulos.tsv` · `tests/check.py` (solo `_T25_ARCHIVOS_CONOCIDOS`). No toca `milpa/**`, `tools/**`, ningún inventario existente. Concurrencia: ninguna — serie estricta. "Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo."
FP pre-asignadas
Rango `FP-181`–`FP-182` (re-deriva el máximo del tablero al escribir; hoy `FP-177` + lo que E1 haya creado). Uso previsto: una fila "mesa recibe el emparejamiento, 9 veredictos" con el N de `EXISTE-SATISFACE` en `qué_se_firma`. Renumera quien fusiona segundo.
ADR y cascada
Candidato re-derivado con el comando de la casa contra el árbol ya fusionado con E1 (no esperes un número: deriva). Cascada estándar: cabecera de gobernanza, recifrado L0, `registro-rotulos` (`MAESTRA32-E2` censado; token pelado `E2` se censa, no se reclama), T25, nota de cierre.
CONTADOR
9 veredictos A.4 con universo declarado; N pares con co-observación — N es la medición de la sesión, sea cual sea, incluido cero.
Lo que este acto NO hace
No mide ningún β̂. No abre microdato ni `data/raw`. No edita `milpa/procedencia.yaml` (ni `ruta:` ni `nota:` — cambiar ruta exige acto sucesor con adjudicación, precedente `ADR-218`). No toca los inventarios existentes ni sus herramientas. No adjudica el GATE de `G3.horizonte_temporal`. No usa red ni API.
Sucesores declarados, no lanzados
`MAESTRA32-E3 · EXTRACTOR-DTA` (GATED a este merge + letra M-EXTRACTOR) · acto de medición por cada `EXISTE-SATISFACE` (si N≥1) · `MAESTRA32-E4 · RE-EMPAREJA` (tras E3, misma spec congelada).

## CONSUMIDO

Ejecutado en la rama `claude/maestra32-e2-motor-texto-c5grsj`. Esta sesión NO abre PR — commitea y pushea a la rama designada, y lo dice explícitamente aquí para no repetir el defecto ya visto en el acto E9. Número de PR: N/D desde esta sesión; quien fusione (dirección, u otra sesión con permiso de abrir PR) llena este campo al abrirlo. Resultado: `N=0` de 9 `EXISTE-SATISFACE`, `data/emparejamiento-motor-v1_0.tsv` (COMMIT-2), `forense/notas/2026-08-28-empareja-spec.md` (COMMIT-1), `forense/notas/2026-08-28-empareja-cierre.md` (detalle A.13 completo, discrepancias de cita reportadas en §2, hallazgo de corrección de spec en §5), `ADR-221`.
