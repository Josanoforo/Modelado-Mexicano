# Nota · ACTO SELLA-AGO25-E — palomear lo hecho, despertar lo dormido

25/ago/2026. Entorno **NUBE** (`cloud_default`). Modelo Opus. Rama `claude/sella-ago25-e-verify-recvrw`. Encargo: `forense/encargos/2026-08-25-SELLA-AGO25-E.md`.

## 1 · ARRANQUE

1. **REPO:** clon existente, `/home/user/Modelado-Mexicano`.
2. **SHA / PRs de bandas·scoring·eval.** `git fetch origin main` refresca `origin/main` a `7848b97` (desde `62e60e9`). Búsqueda de merges: `git log origin/main --merges --oneline | grep -iE "bandas|scoring|eval"` da:
   - **bandas** → `PR #329` (`a5f1bf6`, `Merge pull request #329 from Josanoforo/claude/bandas-doc-prereg-wzwm1q`) — **ya fusionado** antes de arrancar.
   - **eval** → `PR #331` (`7848b97`, `Merge pull request #331 from Josanoforo/eval-compartamos`) — **ya fusionado** antes de arrancar.
   - **scoring** → `git log --all --oneline | grep -i scoring` y `git branch -a | grep -i scoring` dan **cero resultados**, en cualquier rama del repo. No existe ningún PR ni rama con "scoring" en el nombre. Reportado como no-encontrado; **no es PARO**, se marca lo verificable y se sigue.
3. **`data/raw`:** no aplica a este acto — este acto no toca microdato ni corpus, declarado.
4. **ENTORNO:** boilerplate, sin sonda real que hacer — declarado, saltado.
5. **ESPEJO:** nada.

## 2 · Verificación de existencia — las 7 filas candidatas

Dirección clasificó 6 filas `FIRMADA`/`ejecutada_en` vacío como "trabajo-hecho-sin-palomear", más un séptimo candidato (`FP-102`) a verificar por grep. Cada una se leyó completa desde `forense/firmas-pendientes.tsv` (lector `csv` de 9 columnas) antes de escribir nada, y cada cita se verificó contra el árbol real, no se tomó por buena.

### FP-99 — VERIFICA
Cita de dirección: "sellado por ADR-141 y verificado por CORROBORA-MOTOR B6".
- `grep -rn "ADR-141"` → `forense/prereg-duelo-v2/mesa-pendientes.md:61`: *"Sello: `canon/gobernanza-v1_15.md` `ADR-141`, formalizado en el tablero como `FP-99` (`ABIERTA`→`FIRMADA`)."*
- `grep -rn "CORROBORA-MOTOR"` → `forense/notas/2026-08-24-corrobora-motor.md`, §`B6 · Operador ⊕ del corredor E` (línea 54), texto de cierre en línea 79: *"el operador `⊕` del corredor `E` está sellado e implementado, sólo pendiente de que el duelo lo ejecute (B6)"*.
- **Fecha de ejecución:** 24/ago/2026 (fecha del archivo `2026-08-24-corrobora-motor.md`, que es la verificación citada).
- **`ejecutada_en` escrita.**

### FP-100 — VERIFICA
Cita de dirección: "línea v2.11 del punto 4 en instrucciones-proyecto-v2_11.md".
- `instrucciones-proyecto-v2_11.md:189`: *"⚠️ [NUEVO v2.11] Un negativo producido por un comando que no examinó archivos no es un negativo (A.13)."*, dentro de "4 · ENTORNO" — el punto 4 del ARRANQUE.
- Sección completa `A.13` en `instrucciones-proyecto-v2_11.md:392`.
- `git log -S"se adopta Opción C"` no aplica aquí; para esta fila: `git log -p --follow` sobre el archivo, buscando `[NUEVO v2.11] Un negativo` → commit `5981d84`, **21/ago/2026**.
- **`ejecutada_en` escrita.**

### FP-101 — VERIFICA
Cita de dirección: "la cita 'B-bis' fue escrita por 'SELLA-C-v2'".
- No existe archivo ni acto llamado literalmente "SELLA-C-v2"; el atajo de dirección nombra `ACTO SELLA-AGO24-C-v2` (`ADR-155`).
- `canon/gobernanza-v1_15.md:3192`, inciso `(f)` de `ADR-155`: *"`FP-101` → `FIRMADA`. Resolución verificada, no asumida: la definición operativa de «el falsador no refute» es el Bloque B-bis, `instrucciones-proyecto-v2_11.md:111-113`"* — el Bloque B-bis mismo vive en `instrucciones-proyecto-v2_11.md:109-113` (rótulo `[NUEVO v2.4]`, anterior), pero es **este acto** el que la ata como resolución de `FP-101` y escribe la enmienda fechada en `forense/prereg-duelo-v2/mesa-pendientes.md` §1.
- **Fecha:** 24/ago/2026 (`ADR-155`, `PR #322`).
- **`ejecutada_en` escrita.**

### FP-102 — VERIFICA (séptima fila)
Búsqueda por grep de "precedencia de las cinco casillas" en la escala v2: `forense/escala-cinco-casillas-piloto-v2_0.md` §5 (línea 95): *"5 · Regla de precedencia — fijada al sellar y no después"*, línea 99: *"se adopta Opción C — precedencia por orden de evaluación ... vía la firma de mesa «Vamos con 1»"*.
- `git log -S"se adopta Opción C" -- forense/escala-cinco-casillas-piloto-v2_0.md` → commit `15165d1`, **20/ago/2026**.
- **`ejecutada_en` escrita.**

### FP-110 / FP-119 — VERIFICAN (evidencia compartida)
Cita de dirección: "declaración de canónica /microdatos/ y '28 olas re-adquiridas' en el ADR del PR #320" (FP-110) / "ADQ-DISENO-1 corrió y fusionó en PR #320" (FP-119).
- `canon/gobernanza-v1_15.md:3128`, cabecera de `ADR-152`: *"`ACTO ADQ-DISENO-1` declara `/microdatos/` ruta canónica de ENOE en adelante y cierra la re-adquisición de las 28 olas post-2019 ... Encargo, dirección, 24/ago/2026 ... `git log origin/main -1` (`5bef289`, `PR #319` ...)"*
- `forense/notas/2026-08-24-sella-c-v2.md:14`: *"`754eb862…` Merge pull request #320 from Josanoforo/adq-diseno-1, 24/ago/2026"*.
- `forense/notas/2026-08-24-sella-c-v2.md:15`: *"`ADR-152`/`ADQ-DISENO-1` (`b42355c`, `754eb86`/`#320`)"* — confirma commit y PR.
- Ambas filas verifican con la misma evidencia (`ADR-152`, `PR #320`, commit `b42355c`/merge `754eb86`).
- **Fecha:** 24/ago/2026.
- **`ejecutada_en` escrita en las dos.**

### FP-118 — NO SE TOCA (contradicción de premisa, no falla de evidencia)
Cita de dirección: "resolución 'buscar-primero': PASO 0 AGOTADO con universo declarado, corrida bajo plan B, en ADR-157".
- `ADR-157` existe: `canon/gobernanza-v1_15.md:3226` (`ACTO CAL-G3-PUNTUAL`). `forense/notas/2026-08-24-cal-g3-puntual-cierre.md:12`: *"PASO 0 · Agotar la búsqueda del campo de diseño"*, línea 48: *"Varianza (PASO 0 → AGOTADO, plan B firmado por el encargo)"*. `forense/encargos/2026-08-24-CAL-G3-PUNTUAL.md:10`: *"Firmas que ejecuta: respuesta 2 de mesa («…no está descargado, la descargamos y ya» — plan B solo si la búsqueda se agota) + FP-118."*
- La **sustancia** de la cita verifica: el hallazgo existe, tiene fecha, tiene ADR.
- Pero la **fila `FP-118` en el árbol trae `estado` = `ABIERTA`**, no `FIRMADA` — verificado leyendo la fila completa con el lector `csv` de 9 columnas, no de un grep aislado. El barrido original de dirección presuponía "estado FIRMADA y columna ejecutada_en vacío" para las 16 filas candidatas; `FP-118` no cumple esa premisa.
- **Decisión conservadora de este acto:** no se llena `ejecutada_en` sobre una fila `ABIERTA`. Hacerlo fabricaría en el tablero una "ejecución" sin la firma que su propio `estado` dice que falta — exactamente la clase de defecto que un tablero de auditoría existe para impedir. La fila queda **sin cambio**, con este hallazgo reportado para que mesa reconcilie el `estado` (firmarla `FIRMADA` si corresponde, o corregir la premisa del barrido).

**Resumen: 6 de 7 filas candidatas verifican y reciben `ejecutada_en` (`FP-99`, `FP-100`, `FP-101`, `FP-102`, `FP-110`, `FP-119`); 1 (`FP-118`) queda pendiente por contradicción de premisa, no por evidencia faltante.**

## 3 · Fila nueva A.12 — generador de candidatas del marco

El encargo daba `codex/generador-marco` por "no existe / pendiente". Verificado contra el árbol:
- `git log --oneline --all | grep -i generador-marco` → `9883f2c Merge pull request #332 from Josanoforo/codex/generador-marco`.
- `git log main --merges --oneline | grep -i generador` → mismo commit.
- El PR ya estaba fusionado en `main` (commit de contenido `a8bc940`, *"feat(curador): genera marco candidato y unifica extracción PDF"*) **antes de que este acto arrancara**.

La fila `A.12` entra `FIRMADA` con `ejecutada_en` = 24/ago/2026 (fecha del merge `9883f2c`), citando el commit — no vacía, como el encargo anticipaba para el caso "no fusionado".

## 4 · Letras de mesa — búsqueda dirigida, cero rulings verbatim

Búsqueda: `grep -rn` sobre `forense/notas/`, `forense/bitacora.md`, `canon/gobernanza-v1_15.md` de "L1", "L2", "L3", "L4", "L5", "FP-127", "FP-128", "FP-129", "FP-130" en contexto de ruling de mesa (verbatim, con opción a/b/c).

**Resultado: ninguna letra de mesa etiquetada `L1`–`L5` existe en el repo con un ruling verbatim de mesa sobre este lanzamiento.** Las cuatro filas correspondientes en el tablero:

- `FP-127` (β propuesto de CAL-G3, "-0.60" vs "+0.0146") — `estado` = `ABIERTA` en el árbol. Sin ruling. **L1 no se ejecuta.**
- `FP-128` (sella spec sucesora v2.0 de R10.1) — `estado` = `ABIERTA`. Sin ruling. **L2 no se ejecuta.** `forense/hitoD-R10_1-spec-v2_0-PROPUESTA.md` queda sin tocar (fuera del perímetro sin la firma).
- `FP-129` (función de enlace CoDi↔SPEI) — `estado` = `ABIERTA`. Sin ruling. **L3 no se ejecuta.** `forense/ficha-r34-conda-v2-spec.md` sin tocar.
- `FP-130` (equivalencia "cuentas activas") — `estado` = `ABIERTA`. Sin ruling. **L4 no se ejecuta.** `FP-104` sin tocar (ya está `ABIERTA`, sin firmar, y así queda).

**L5/FP-63.** Búsqueda exacta de la cadena verbatim `AUTORIZO DESTRUIR mm-purga.git` en todo el árbol (`grep -rn`). Aparece en:
- `forense/firmas-pendientes.tsv` (fila `FP-63` misma, citándola como la condición que falta).
- `forense/notas/2026-08-19-fp63-verificacion-espejo.md:267` y `forense/notas/2026-08-19-caja-residuos-cierre.md:93`: en los dos, la cadena aparece **dentro de la definición de la propia compuerta de firma**, no como autorización dada.
- `canon/gobernanza-v1_15.md:2349,2409`: mismo patrón — la compuerta "NO se abrió", dos veces, por la misma razón.
- `forense/encargos/2026-08-19-FP63-CIERRA.md` y `forense/encargos/2026-08-19-CAJA-RESIDUOS.md`: la cadena aparece en la especificación del encargo, no como firma.

**No existe ninguna aparición de la cadena como autorización dada.** `FP-63` queda **sin tocar**; **no se ejecuta ninguna acción destructiva** sobre `~/mm-purga.git`. Esto reproduce exactamente lo que la propia fila `FP-63` ya documentaba.

## 5 · FP-24 — LETRA L6 PARA MESA

Texto completo, copiado verbatim de `forense/firmas-pendientes.tsv` (columnas `qué_se_firma`, `gatea`, `estado`, `firmada_en`, `ejecutada_en`, `encargo`):

> **id:** FP-24
>
> **qué_se_firma:** RANURA (c) -- politica de pares para relaciones.tsv (fila 'gemela' SI + NO_DETERMINADO, mismo necesidad_id, distinto objeto_evidencia_id_canonico): sigue sin firma; ACTO ENLACE-2 solo enlazo las filas sin par (48) y dejo las 20 con par como PROPUESTA para cuando mesa firme
>
> **dónde:** forense/notas/2026-08-14-enlace2-clase-limbo.md §4; forense/notas/2026-08-14-enlace2-68-mas-19.md:39,42,106
>
> **creado:** 2026-08-13
>
> **gatea:** capa2 SI queda en 20 (de 68) hasta que se firme; declarada PROPUESTA por primera vez en AJUSTE-PLAN-v3_1-2026-08-13.md (fuera del repo) y de nuevo en PLAN-MULTIFASE-F0-F6-2026-08-13.md (fuera del repo) -- ningun ADR la ha sellado, verificado (grep sobre los 84 ADR de gobernanza, cero resultados) -- RATIFICADA SIN CAMBIO por mesa el 17/ago/2026, verbatim: "De acuerd." (ACTO FUENTE-UNICA-DECISIONES, ADR-91): sigue ABIERTA por diseno, la ratificacion no la cierra. BARRIDO-2 SS0 la reconfirma de forma independiente desde su propia rama: "FP-24: sigue ABIERTA; la dependencia se determina propuesta por propuesta por necesidad real de resolver la regla pendiente de pares, nunca por pertenencia a los 20 IDs historicos." -- PUNTERO AÑADIDO, ACTO MESA-18AGO, 18/ago/2026 (ADR-101(e)/(f), mismo patrón de FP-29): política sellada (ADR-93 texto; ADR-95(mantenimiento) aplicación dinámica); lo ejecutable de aquí en adelante es FP-46.
>
> **estado:** FIRMADA
>
> **firmada_en:** ADR-93, PR #249, 17/ago/2026 -- firma de mesa verbatim: "2: entonces vamos a cerrarla indicando de donde viene." sobre el texto adoptado: "Se adopta como política de pares el texto ya citado inline en forense/notas/2026-08-14-enlace2-clase-limbo.md §4, que pasa a ser el canónico: 'cada objeto_evidencia conserva su fila; la gemela NO_DETERMINADO se enlaza SOLO si su objeto es evidenciable con una entrada distinta del manifiesto'. Procedencia, declarada por instrucción de mesa: el texto se propuso en PLAN-MULTIFASE-F0-F6-2026-08-13.md §94, documento de dirección que vive fuera del repo; la nota de ENLACE-2 lo transcribió inline y esa transcripción es la que rige — el original deja de ser necesario, que es exactamente el remedio que A.3 prescribe. Las 20 filas con par (ENSAFI 9 · ENFIH 8 · ENBIARE 3) se adjudican con esta regla en acto propio; este acto no las escribe." -- re-derivado hoy, no heredado: el encargo cita data/relaciones.tsv, ruta movida a data/curacion-registro/relaciones.tsv en acto anterior (reportado en la nota de este acto); contra 197 filas vigentes, las SI_O_REFERENCIADO son exactamente 20 y las 20 son CON PAR (ENSAFI 9 · ENFIH 8 · ENBIARE 3; necesidades N3/N4/N10/N12/N13/N14) porque las 48 SIN PAR del 14/ago ya fueron promovidas a SI por ENLACE-2 y salieron del universo SI_O_REFERENCIADO -- 20 reproducido, coincide con la nota histórica.
>
> **ejecutada_en:** (vacío)
>
> **encargo:** SIN ENCARGO

### Opciones que el propio texto contiene

El texto de la política ya adoptada (`ADR-93`, citada arriba) es: *"cada objeto_evidencia conserva su fila; la gemela NO_DETERMINADO se enlaza SOLO si su objeto es evidenciable con una entrada distinta del manifiesto."* No trae una lista a/b/c de opciones para mesa — la ranura abierta no es "cuál regla" (eso ya se selló) sino **si mesa adjudica las 20 filas concretas con par** (ENSAFI 9 · ENFIH 8 · ENBIARE 3; necesidades N3/N4/N10/N12/N13/N14) bajo esa regla, en un acto propio que este acto no ejecuta.

### Contexto

- **Qué archivo/proceso gobierna:** `data/curacion-registro/relaciones.tsv` — específicamente las 20 filas `SI_O_REFERENCIADO` con "gemela" `NO_DETERMINADO` bajo el mismo `necesidad_id`. El acto sucesor que las escribiría es `FP-46` (según el puntero de `ADR-101(e)/(f)`).
- **Qué pasa si nadie decide:** las 20 filas quedan indefinidamente como `PROPUESTA`, sin adjudicar por la política ya sellada — la capa `SI` del universo de relaciones se congela en 20 (de 68 originales) hasta que un acto propio, con perímetro en `data/`, las escriba una por una.

Este acto **no adjudica** ninguna de las 20 filas ni decide nada sobre `FP-24`: solo la hace visible para la próxima sesión de mesa.

## 6 · Actualizaciones al tablero (`forense/firmas-pendientes.tsv`)

- `FP-99`, `FP-100`, `FP-101`, `FP-102`, `FP-110`, `FP-119`: `ejecutada_en` llenada con fecha real y cita, ver §2.
- Fila nueva `A.12`: `FIRMADA`, `ejecutada_en` = 24/ago/2026, cita `PR #332`/commit `9883f2c`.
- `FP-118`, `FP-63`, `FP-127`, `FP-128`, `FP-129`, `FP-130`: **sin cambio**.

## 7 · ADR y cascada

`ADR-164` (`canon/gobernanza-v1_15.md`), candidateado como `ADR-163` contra el máximo verificado por `grep -oE '^\*\*ADR-[0-9]+' canon/gobernanza-v1_15.md | sort -t- -k2 -n -u | tail -1` sobre `origin/main` re-fetched inmediatamente antes de commitear: único `163`, sin huecos. **Renumerado a `ADR-164` al fusionar**: `PR #333` (`ACTO BIBLIOTECARIO-56`) fusionó primero y se llevó `ADR-163` — regla de la casa, renumera quien fusiona segundo. `canon/estado-programa-v1_10.md`: conteo de ADR recifrado `163→164` (`:27`, `:103`); WARN/FAIL recifrados a punto fijo (`:208`, `:301`).

## 8 · Tests

`python3 tests/check.py --baseline`, corrida final tras todas las ediciones:

```
════════════════════════════════════════════════════════════════════════
  19 FAIL · 140 WARN
════════════════════════════════════════════════════════════════════════

────────────────────────────────────────────────────────────────────────
  LÍNEA BASE: VERDE — nada nuevo frente a tests/baseline.json (HEAD congelado e24d033ed3c095f1e81c2fbb8248f108e9d3ef65)
  (5 entradas de la línea base ya no aparecen — mejora, no bloquea, no baja la cifra congelada sin --freeze explícito)
────────────────────────────────────────────────────────────────────────
```

Las 19 FAIL son las diez categorías pre-existentes (T10:65·T03:55·T22:17·T13:3·T09:8·T05:5·T02:2·T06:2·T08:1·T11:1), confirmadas ajenas al perímetro de este acto corriendo la suite antes y después de cada edición — ninguna cae en un archivo que este acto tocó. `milpa/` no se tocó en ningún momento.

## 9 · Perímetro respetado

Tocado: `forense/firmas-pendientes.tsv`, `canon/gobernanza-v1_15.md`, `canon/estado-programa-v1_10.md`, `forense/notas/2026-08-25-sella-e.md` (este archivo), `forense/encargos/2026-08-25-SELLA-AGO25-E.md`.
No tocado (correctamente, porque ninguna letra L1-L4 verificó): `forense/hitoD-R10_1-spec-v2_0-PROPUESTA.md`, `forense/ficha-r34-conda-v2-spec.md`, el registro de llaves, `milpa/`.

**Desviación mecánica declarada, una línea fuera del perímetro nominal:** esta misma nota (`forense/notas/2026-08-25-sella-e.md`) dispara `T22`(b) al discutir `L1`–`L5`/`FP-24` con vocabulario de "ranura"/"pendiente" — todos ya rastreados en el tablero, ninguno un marcador nuevo. Sumada a `_T22_ARCHIVOS_CONOCIDOS` (`tests/check.py`), con razón in situ, mismo precedente declarado que `ADR-147(c)`/`ADR-149(f)`/`ADR-151` ya fijaron para este tipo de recifrado mecánico obligado por la propia suite (correr `python3 tests/check.py --baseline` es el propio deliverable 5 del encargo, y sin este ajuste la suite quedaba en ROJO por la existencia misma de esta nota). Es la única línea tocada fuera de la lista de siete archivos del perímetro declarado.

## 10 · Para mesa

- **`FP-118`** necesita reconciliación: o mesa la firma `FIRMADA` (la sustancia ya está resuelta, `ADR-157`), o corrige la premisa de que era una de las 16 filas del barrido.
- **`L1`–`L5`** siguen sin ruling verbatim en el repo — si mesa ya decidió estas letras fuera del repo, hace falta transcribirlas (mismo patrón que `FP-101` documentó para "documentos de mesa fuera del repo").
- **`FP-24` / LETRA L6** — arriba, §5: las 20 filas con par siguen sin adjudicar.
- **`scoring`** — el tercer PR que el encargo esperaba no existe en el árbol; si mesa lo lanzó por otro canal, no ha llegado a este repo.
