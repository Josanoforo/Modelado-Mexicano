ENCARGO · ACTO MAESTRA36-N1 · PROPAGA-FIRMAS-233-251-Y-RECIBOS
SHA de redacción: `9af8407` (`origin/main`, merge PR #494) · 2/sep/2026, dirección (Fable) · instrucciones v2.12 · Estado: LISTO PARA LANZAR — COMPUERTA: ninguna. Trámite de tablero; no mide, no abre microdato, no toca `milpa/`.
ENTORNO ASIGNADO: NUBE (`cloud_default`). NO se lanza en UBUNTU: la caja está ocupada por `MAESTRA36-A1` y este acto no necesita disco. MODELO SUGERIDO: Sonnet (propagación mecánica de firmas dictadas, sin juicio).
Invoca `/acto`. Punto 4 del ARRANQUE: este acto no toca microdato ni red — dilo y sáltalo.
CARRILES: en UBUNTU corre `MAESTRA36-A1 · ESCANEA-RECURSIVO-Y-REGISTRA-DESCARGAS` (perímetro `tests/manifiesto.py`, `data/manifiesto.yaml`, `data/curacion-registro/**`, notas propias) — disjunto salvo `forense/firmas-pendientes.tsv` y la cascada; renumera quien fusiona segundo. En NUBE, `/despacha` puede tomar `MAESTRA34-N3 · AGREGA-2` (`forense/prereg-duelo-v2/corridas-L/`, scoreboards) — disjunto. Este acto no corre `/despacha` ni `/tramite`.
FIRMAS DE MESA — verbatim, 2/sep/2026. El ejecutor propaga, no decide (SELLA-3).
«Confirmo que la tarea de Windows `MM-adquiere` (L–V 07:30) quedó instalada el 2/sep/2026 en la caja de mesa y que su primera corrida real produjo el PR #488 `[ADQ] 2026-09-02`: FP-233 → EJECUTADA. Enterado de `canon/estado-programa-v1_11.md` (PR #485): FP-251 → FIRMADA. Enterado de los recibos FP-253 (PR #490), FP-254 (PR #491) y FP-256 (PR #494): FIRMADA los tres; ninguno sella ni carga nada al motor — eso es N10.»
La fuente citable de estas firmas es este mismo encargo archivado por 0-bis (A.3): `forense/encargos/2026-09-02-MAESTRA36-N1-PROPAGA-FIRMAS-233-251-Y-RECIBOS.md`, la línea del bloque de arriba. `firmada_en` cita ese `archivo:línea`, no esta conversación.
═══ VERIFICACIÓN DE EXISTENCIA (A.8) — contestada por quien escribe, contra `9af8407` ═══
(1) ESTRUCTURA. Tablero: `forense/firmas-pendientes.tsv` (A.12), columnas `id·qué_se_firma·dónde·creado·gatea·estado·firmada_en·ejecutada_en·encargo`. Mecanismo vigente para mover filas: `.claude/commands/tramite.md` §3.1 (FIRMADA solo con firma verbatim en el repo, `archivo:línea`) y §3.2 (recibos, mismo criterio); `ejecutada_en` no se rellena por parecido (`ADR-94`, `T22(c)`). Este acto no crea ninguna vía nueva: usa la del `/tramite` con la firma que hasta hoy faltaba en el árbol.
(2) CONTENIDO.

* `awk -F'\t' '$1=="FP-233"||$1=="FP-251"{print $1,$6,$7,$8}' forense/firmas-pendientes.tsv` → `FP-233 ABIERTA` (firmada/ejecutada vacías) · `FP-251 ABIERTA` (vacías). 1 archivo, 247 filas examinadas.
* `grep -rn -E 'FP-233|FP-251' forense/encargos/ forense/notas/ canon/gobernanza-v1_15.md | grep -viE 'nueva|abre|recibo|renumerad'` → 0 líneas con firma o enterado de mesa (758 + 1 archivos examinados). El transfer de maestra-35 con las firmas de mesa no está en el repo (`git ls-files | grep -i TRANSFER-2026-09-02` → 0 sobre 2 916). → NO-ENCONTRADO (firma citable), y es la causa de que `/tramite` no las haya cerrado.
* Producto que FP-233 gatea: `git log --format=%h\ %s origin/main | grep -i '\[ADQ\]'` → `77115fe Merge pull request #488` (`1fb9d4a [ADQ] 2026-09-02`). EXISTE-SATISFACE.
* Producto de FP-251: `ls canon/ | grep estado-programa` → `estado-programa-v1_11.md`, único (v1_10 retirada por T01). EXISTE-SATISFACE.
* FP-253/254/256: `estado=ABIERTA`, `gatea: no gatea ningún acto en vuelo` en las tres; productos PR #490/#491/#494 fusionados (`git merge-base --is-ancestor` de cada merge contra `origin/main` → ancestro). EXISTE-SATISFACE.

(3) COBERTURA RETROACTIVA. `firmas-pendientes.tsv` nació el 14/ago (ADR-81/v2.9); `/tramite` el 31/ago (ADR-239); las cinco filas son del 1–2/sep. Todo posterior a las tablas: no hay trabajo invisible que declarar.
SPEC POR PIEZA (un PR, un ADR, un recibo — D-11)
P1 · FP-233 → EJECUTADA. `estado=EJECUTADA`; `firmada_en` = cita verbatim de la primera frase de la firma + `archivo:línea` del encargo archivado; `ejecutada_en` = `PR #488 (77115fe), [ADQ] 2026-09-02, cron MM-adquiere instalado por mesa 2/sep/2026`. No se toca `forense/agente-adquisicion-v1_0.md` ni `tools/adquiere_cron.sh`.
P2 · FP-251 → FIRMADA. `firmada_en` = cita verbatim de la segunda frase + `archivo:línea`; `ejecutada_en` vacío (es recibo: la firma resuelve, no escribe — `ADR-94`). Si `T22(c)` emite WARN «FIRMADA sin ejecutar», es el comportamiento esperado para un recibo y se declara, no se silencia.
P3 · FP-253, FP-254, FP-256 → FIRMADA. Mismo patrón que P2, tercera frase de la firma. Condicional: si la firma pegada en 0-bis no trae la tercera frase, P3 no se ejecuta y se declara «P3 NO-EJECUTADA: enterado no dado», sin inventar nada.
Edición del tablero por línea (`sed -i`/Python línea a línea sobre la fila por `id`), nunca con `csv` round-trip (§0.8 del transfer: altera el citado de filas ajenas — defecto medido por el cron). Control: `git diff --stat forense/firmas-pendientes.tsv` debe dar exactamente 3 líneas cambiadas si P3 no corre, 5 si corre; cualquier otra cifra es PARO de pieza.
Cierre: `python3 tests/check.py --baseline` en VERDE o PARO-reporta; los WARN de T22(c) sobre recibos recién firmados se listan con su id. Una línea en `forense/hallazgos.md`: «recibos cuya firma es "la fusión" nacen sin `archivo:línea` citable para `/tramite` §3.2; este acto los cierra con enterado escrito, no cambia la regla».
PERÍMETRO Y CONCURRENCIA Toca: `forense/firmas-pendientes.tsv` · `forense/encargos/2026-09-02-MAESTRA36-N1-*.md` (0-bis + `## CONSUMIDO`) · `forense/hallazgos.md` · cascada (`canon/gobernanza-v1_15.md`, `canon/registro-rotulos.tsv`, `canon/estado-programa-v1_11.md` L0, `tests/baseline.json` recifrado). NO toca: `milpa/**`, `data/**`, `.claude/commands/**`, `forense/prereg-duelo-v2/**`, `forense/encargos/cola/**`, `forense/digesto/**`, ningún runbook. Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.
FP/ADR CANDIDATOS (derivados contra `9af8407`, no heredados) ADR máx 308 → candidato ADR-309, pero `MAESTRA36-A1` está en vuelo en caja con el mismo candidato: re-deriva por el comando de la casa al fusionar y renumera si fusionas segundo. FP máx 256; A1 pre-asignó 257/258 → este lote toma FP-259 (recibo), re-derivado al arrancar.
CONTADOR Filas del tablero ABIERTA `10 → 8` (P1+P2) o `10 → 5` (con P3), reportado por `awk` al cierre. Medición de modelo: cero directo, declarado.
Lo que este acto NO hace No cierra FP-255 (conversión cívica: dirección presenta, mesa traduce pp→p), ni FP-179/235/240/246 (piden juicio). No sella ni carga ninguna entrada de `milpa/tramite-ola5-propuesta-v0.yaml` — eso es `N10 · SELLA-L9-L11`. No corre `/tramite` ni `/despacha`. No edita el transfer ni lo sube al repo.
Sucesores declarados, no lanzados `MAESTRA36-N10 · SELLA-L9-L11` (cuando mesa dé letras g1–k1 con la tabla de replicación a la vista) · `/tramite` diario sigue igual; con estas filas cerradas su digesto deja de listar FP-233 como vencimiento.
