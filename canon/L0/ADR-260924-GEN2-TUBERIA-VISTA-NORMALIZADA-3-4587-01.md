# ADR-260924-GEN2-TUBERIA-VISTA-NORMALIZADA-3-4587-01

24/sep/2026 · `ACTO GEN2-TUBERIA-VISTA-NORMALIZADA-3`. COMMIT-B: `camino_linaje` sale de `resultados.tsv` (23→22 columnas) sin mudarse a ningún lado — cardinalidad medida en `-2`, `65287/65287 = 1.0`. `tools/vista.py::linajes_por_resultado` reejecuta `corrida0._lee_oferta`+`_propaga_envuelto` (la misma máquina que la escribía, no una segunda) y `join_resultado`/`leer_resultados_join` la derivan por fila: patrón mecánico gratis para `origen=DEMANDA`, mapa explícito o cache de proceso (`_linajes_cache`, un cálculo caro por proceso, ~70s/~310 `CALC-*/`) para `origen=OFERTA`, sin disparo para un fixture legacy sin `origen`. Censo de consumidores: 22 archivos (no 18, crecieron desde `-2`), verificados uno por uno — 4 ya pasan por el join extendido sin editarse, 2 son pass-through, el resto matchea por substring ajeno o testea la función interna sobre oferta sintética. Verificado con una muestra de 500 filas reales (`tests/fixtures/vista-camino-linaje-muestra500.json`) contra el cálculo real, byte a byte idéntico (`tests/test_vista.py::PruebaCaminoLinajeBajoDemanda`, huérfana por costo, D-21). Sobre las 65287 filas completas de `main`: 65240/65287 reproducen; 47 (tres `spec_id` de corridas en curso hoy) difieren por `decisiones.tsv` (insumo vivo, no sellado) recibiendo filas nuevas entre publicación y lectura — declarado, ninguna en la muestra. **Hallazgo fuera de perímetro:** un `registro --escribe` fresco da 96.7 MB/71301 filas — quitar `camino_linaje` sigue ahorrando ~35-40 MB, pero el campo `valor` creció de 1.4 MB a 68.8 MB por corridas `-PISOS-*`/`-PERSISTENCIA-*` que serializan tablas completas en una celda (una sola, 11.56 MB); ajeno a este acto, no tocado. Guarda de `verify.yml` baja de todos modos a "falla a 50 MB" (firma de `-2` §2; confirmado con mesa vía pregunta directa, aceptando el riesgo de que vuelva a fallar por esta causa nueva). `data/INFRAESTRUCTURA-v1_0.md` actualizado antes del commit. `NC-260924-GEN2-TUBERIA-VISTA-NORMALIZADA-2-f1b2-01` → CERRADA. Suite `--rapido` VERDE, 0 FAIL.
