# ACTO SELLA-3 · Sella ADR-79 — ocho de nueve decisiones de mesa del 13/ago, la novena (D-M) registrada sin sellar por no venir firmada entre comillas

**Acto:** ACTO SELLA-3 · **Entorno:** repo-only, nube, sin red, sin `data/raw` · **Depende de:** ADR-76 (mismo patrón: "el ejecutor propaga y deriva, no decide"), ADR-72/`registro-recalculo-v1_0.md` (b, d), PROC-10/PR#220 (a), ADR-76(d) (b), ADR-50/51 (c), ADR-76(h)/A.7 (e), `censo-estimabilidad-coeficientes-v1_1.md` (f), ADR-69/70 (g), TRIAGE-63/PR#219 (i, no sellado).

## §0 · ARRANQUE

1. **REPO.** Clon existente en uso: `/home/user/Modelado-Mexicano`. Ningún clon nuevo.
2. **HEAD.** `git log -1 --format="%h %s"` → `1cb6e3e Merge pull request #219 from Josanoforo/triage-63`. Coincide exacto con el HEAD declarado en el encargo (`1cb6e3e`, merge #219, último ADR sellado ADR-78). `git status` → rama `claude/encargos-sellado-v4-sbs77i`, árbol limpio. Sin divergencia — no aplica el PARO de "main se movió".
3. **data/raw.** `ls data/raw` → no existe. No aplica a este acto (documentación pura, sin red). Declarado explícitamente, no comprobado innecesariamente.
4. **Baseline.** `python3 tests/check.py --baseline` corrido antes de tocar nada: **20 FAIL · 107 WARN — LÍNEA BASE: VERDE** (HEAD congelado `3d0d1e5`). Igual a la cifra que `ACTO PROC-10` ya reportó sobre el mismo congelado.

## §1 · Verificación de existencia, inciso por inciso

**(a) D-A — clase séptima de marginales sin eje.** `forense/notas/2026-08-13-proc-10.md` §1/§3, confirmado por lectura directa (no heredado): `grep -c 'clase: "MEDIDO·PARCIAL' milpa/procedencia.yaml` → 9, las nueve con `x` no vacío; ninguna admite `x = ∅`. PROC-10 (PR #220, fusionado como `dc75d74`) llegó a desenlace (B): no escribe la clase, la declara ausente y necesaria. Este ADR sella la autorización de crearla; `PROC-10-bis` (nombrado, no ejecutado aquí) la escribe.

**(b) D-B — "correrlo" (la invarianza).** `gobernanza-v1_15.md:1096` (ADR-76(d)) ya sella el *diseño* del acto de vinculación-invarianza ENCUCI↔ENBIARE — cinco puntos (invarianza parcial sobre el único par próximo, `AP5_1_3` fuera de alcance, corte ≥8/10, mesa decide el estándar antes de correr, `PROXY_PARCIAL` hasta que corra). No se ha ejecutado: `grep -rln "invarianza.*ENCUCI\|ENCUCI.*ENBIARE" forense/*.md` fuera de `benchmark-enlace-invarianza-v1_0.md` y las citas de ADR-76 → sin artefacto de corrida nuevo. D-B autoriza correr ese diseño ya sellado; no lo corre aquí.

**(c) D-C — "revisar y ajustar bajo el nuevo motor de matriz".** El motor de matriz nuevo (`Fable`) es un acto ajeno a este dispatch, fuera de perímetro. Contexto verificado: `registro-recalculo-v1_0.md` Entrada 5 (`ADR-50 / ADR-51 / ADR-57(c)`) trae gate = "entrada 1" (ya cerrada, `RECALCULADO — CAMBIA`, ADR-74(b)) y sigue `ABIERTA`. Este inciso no toca la Entrada 5 ni su gate — solo registra que Entrada 5 no corre antes ni en paralelo al sello del motor de matriz nuevo; territorio de ADR-50/ADR-51 (`gobernanza:479-539`, calibración por ajuste, identificabilidad condicionada).

**(d) D-D — "si no se ha hecho lancemos el acto" (Entrada 3).** Verificado contra `registro-recalculo-v1_0.md` fila 3: estado `ABIERTA`, gate "entradas 1 y 2" — Entrada 1 cerró con ADR-74(b), Entrada 2 cerró `RECALCULADO — SIN CAMBIO` (ACTO E2, commit `40ba303`, PR #213; `forense/notas/2026-08-13-e2-cierre.md:75`, verbatim: *"Con el cierre de la Entrada 2 aquí, la Entrada 3 queda desbloqueada. Este acto no la lanza — solo lo declara."*). Gate cumplido, acto no lanzado. **Cero fichas de re-examen**, verificado: `grep -rln "ficha.*re-examen\|re-examen.*ficha" forense/ canon/` → sin resultados (exit 1). Premisa de mesa se sostiene: el acto se lanza. `E3-TRIAGE` (nombrado, no ejecutado aquí) lo ejecuta.

**(e) D-E — "comitteemos el texto" (A.10).** `gobernanza-v1_15.md:866` (ADR-67), texto citado verbatim en el encargo, confirmado carácter por carácter. Numeración: A.7 disputado por dos (`instrucciones-proyecto-v2_6.md:265` y la propia estampa de universo — `gobernanza:1146`, ADR-78); A.8 sellado (ADR-77); A.9 sellado pero `PENDIENTE` (ADR-78). `grep -n "A\.10" canon/gobernanza-v1_15.md instrucciones-proyecto-v2_8.md` → sin resultados: **A.10 libre**, verificado, no asumido. Este ADR autoriza el commit del principio bajo ese rótulo; `A10-ESTAMPA` (nombrado, no ejecutado aquí) escribe la sección completa en instrucciones-proyecto.

**(f) D-F — "sellémosla" (taxonomía RUTA-A/RUTA-C/RUTA-I/SIN-RUTA).** `forense/censo-estimabilidad-coeficientes-v1_1.md:45`, verbatim confirmado: *"Misma taxonomía que v1.0 §1 (RUTA-A/RUTA-I/RUTA-C/SIN-RUTA), no re-declarada aquí — sigue sin ser canon, sigue sin regir nada hasta que una mesa la selle con ADR."* Este ADR registra la autorización de mesa de sellarla; el sellado con las cifras completas del censo es `RUTA-SELLO` (nombrado, no ejecutado aquí, no `ADR-82` todavía — ese número lo derivará el propio acto contra el `main` que tenga delante).

**(g) D-G — "fusionemos" (ADR-69/ADR-70).** `gobernanza-v1_15.md:952` (ADR-70, "Solape con ADR-69, declarado"), verbatim: *"reconciliar sus dos artefactos (`data/UNIVERSO-MINIMO-FUENTE-v1_0.md` y `data/universo-puertas-2026-08-12.tsv`) en una sola tabla queda pendiente nombrado, de mesa."* Este ADR autoriza la fusión; `FUSION-PUERTAS` (nombrado, no ejecutado aquí) la ejecuta.

**(h) D-H — "ajustemos el contador con base en lo que te puse en D-A".** El denominador que D-H referencia es el mismo que (a) — condicionales `9 de 14` (o el eventual `10 de 15` si la séptima clase se crea y esta θ entra por ella). Se registra como linkage a `PROC-10-bis`, no como acción propia: el ajuste de contador ocurre en el mismo acto que crea la clase, no antes ni después por separado (§5 de `2026-08-13-proc-10.md` ya declaró qué titular habría correspondido si el desenlace hubiera sido (A): "10 de 14", nunca "10 de 15" — la aritmética del ajuste ya está acotada, la ejecución no).

**(i) D-M — sondeo de CANDIDATA-A-SONDEO, sin firma entre comillas.** `forense/notas/2026-08-13-triage-63.md` §5 y §7: TRIAGE-63 COMMIT 1 (commit `f598e9b`, mergeado en PR #219, `1cb6e3e`) clasificó **27 filas** `CANDIDATA-A-SONDEO`; el PARO declarado ("Si CANDIDATA-A-SONDEO sale mayor a 20, para y reporta antes del commit 2") activó y COMMIT 2 no corrió. Verificado el corolario que el encargo cita: `grep -n "criterio de parada con una constante" instrucciones-proyecto-v2*.md` → presente en las seis versiones (v2 a v2_8), verbatim: *"un criterio de parada con una constante escrita a mano es el defecto que el criterio existe para atrapar."* Verificado que ningún ADR fija tope por acto: `grep -n "más de 20\|mas de 20" canon/gobernanza-v1_15.md` → sin resultados sustantivos (solo una cifra ajena, "20.3 millones de cuentas", en una nota de otro tema). Los únicos "20" de magnitud estadística del corpus son de umbral de brecha (20pp), verificados en `gobernanza-v1_15.md:682` (ADR-60(a), ficha de `R3.1`, línea 784 citada: "brecha ≥20 puntos") y `:962` (ADR-71 §(b), `R5.1-D2`: "'20pp' aparece una sola vez y es la cota superior de B") y en `modelo-decision-v4_0.md:636` (R3.1/R3.2, gate de 20pp). **No se re-derivó mecánicamente en este acto el cruce de las 27 filas contra `necesidad-objeto-modelo.tsv`** que produce el "17 gatean / 10 no" citado en el encargo — `necesidad-objeto-modelo.tsv` (38 filas, `necesidad_id`→`objeto_modelo_origen`) no trae nombres de fuente cruzables mecánicamente contra los 27 nombres de `CANDIDATA-A-SONDEO` sin la misma lectura semántica fila-por-fila que ya exigió `registro-recalculo` Entrada 0 — ese cruce es precisamente el trabajo que `E3-TRIAGE`/un sondeo futuro haría, no un grep de este acto. El "17/10" se reproduce aquí como texto del encargo, no como cifra re-derivada — declarado, no oculto. **Por la regla del propio proyecto ("si una firma no está entre comillas, se para"), D-M no trae firma verbatim de mesa entre las dos opciones (`SONDEO-ACOTADO` / `SONDEO-COMPLETO`) — este ADR NO sella ningún alcance para (i).** Se registra como pendiente, con el texto de alcance íntegro, a la espera de la firma de mesa.

## §2 · Contadores que no se mueven, verificados uno por uno antes de escribir el ADR

```
$ grep -c 'clase: "MEDIDO·PARCIAL' milpa/procedencia.yaml   → 9   (condicionales 9 de 14, sin cambio)
```
`13 de 27` (Hito D, `gobernanza:1171`/`estado:95,123,197`) · `0 de 15` (coeficientes) · `1 de 2` (llaves, `registro-llaves-identificacion-v1_0.md`) · `4 de 144` — ninguno tocado por este acto: ADR-79 no ejecuta ninguna corrida, solo sella autorizaciones.

## §3 · Cascada verificada antes de escribir

`git log --oneline -1` = `1cb6e3e` → `T15` (receta de conteo de ADR): 78 únicos en `gobernanza-v1_15.md`, contiguo 1..78, sin huecos. Este acto suma **ADR-79**, contiguo. Sitios de cascada: `gobernanza-v1_15.md:2` (cabecera) · `estado-programa-v1_10.md:27,101` (las dos citas de esa línea, mismo criterio que ADR-75/76/77/78 ya fijaron) · `forense/hallazgos.md` (una entrada nueva).

## §4 · Perímetro tocado

`canon/gobernanza-v1_15.md` (ADR-79) · `canon/estado-programa-v1_10.md` (cascada de conteo) · `forense/notas/2026-08-13-sella-3.md` (este archivo) · `forense/hallazgos.md` (una entrada). Nada más: no se tocó `milpa/procedencia.yaml`, ningún `instrucciones-proyecto-*.md`, `tests/`, ni ningún archivo de `data/`.

## §5 · Cierre

`python3 tests/check.py --baseline`, corrido antes y después de escribir: **20 FAIL · 107 WARN — LÍNEA BASE: VERDE** en ambas corridas, sin cambio (este acto no toca ningún archivo que T15/T19b/T16 vigilen salvo la cabecera de conteo de ADR de `gobernanza`, que sube en el sentido esperado, contiguo, sin hueco).
