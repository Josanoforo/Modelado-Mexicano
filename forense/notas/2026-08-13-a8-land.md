# ENCARGO A8-LAND · aterrizar A.8 en el repo — nota de acto

**Base real:** `8b30306` (`origin/main`, merge PR #216 "acto-sella-ocho-decisiones" — ADR-76), no `7ac523e` (merge #215) que el encargo citaba como base de redacción. **Entorno:** `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=cloud_default`, repo-only, sin `data/raw`, sin red más allá de `git fetch`/`git ls-remote`. **Clon:** `/home/user/Modelado-Mexicano`, rama `claude/new-session-dh6rm9`, creada ya sincronizada con `origin/main` en `8b30306` (sin commits propios previos, sin necesidad de merge de `main` hacia la rama antes de empezar).

Este acto es COMMIT 1 + COMMIT 2 del encargo, documentados juntos: el perímetro es chico (seis archivos) y ambos commits comparten la misma verificación de existencia.

---

## 1 · ARRANQUE

1. **REPO.** `pwd` → `/home/user/Modelado-Mexicano` (no home). `git log -1 --format="%h %s"` → `8b30306 Merge pull request #216 from Josanoforo/claude/acto-sella-ocho-decisiones-5fx2ft`. `git status` → limpio, rama `claude/new-session-dh6rm9`.
2. **SHA.** `git fetch origin main` movió `origin/main` de nada-conocido a `8b30306` (el clon ya lo tenía). Confirmado con `git ls-remote --heads origin`: el remoto **solo tiene `main`**, en `8b30306adecb0532c1de8944702483a1ac39c546` — sin rama `claude/new-session-dh6rm9` remota todavía (se crea al hacer push) y, crucialmente, **sin ninguna otra rama viva**: `ACTO SELLA` ya fusionó (es exactamente el PR #216 recién visto en el log), así que la advertencia de concurrencia del encargo ("no puede correr en paralelo con ACTO SELLA") ya no aplica — SELLA terminó antes de que este acto arrancara. `git rev-parse HEAD` == `git rev-parse origin/main` → misma rama, sin commits propios que fusionar. No es PARO: se re-deriva el número de ADR contra el real (§2 abajo).
3. **data/raw.** Ausente (`ls data/raw` → No such file or directory). No es PARO — este acto no la necesita, reportado y saltado.
4. **ENTORNO.** `cloud_default`. Sin red ni microdato salvo `git fetch`/`ls-remote` — sonda de INEGI saltada, declarado.
5. **ESPEJO.** Toda cifra de esta nota sale del clon de (1); comandos a la vista en cada sección.

---

## 2 · VERIFICACIÓN DE EXISTENCIA — re-derivada contra `8b30306`

El propio encargo (Parte 3) ya trae su verificación contra `7ac523e`; se repite aquí contra la base real, porque main se movió (ARRANQUE punto 2).

**1 · ESTRUCTURA.** Dominio 7 de `data/INFRAESTRUCTURA-v1_0.md` ("Sellar una decisión de gobierno") — verificado, existe en el índice, línea 135: gobierna `canon/gobernanza-v1_15.md` §4 + cascada obligatoria a `canon/estado-programa-v1_10.md` (cabecera `:2`, contador; línea citada por el índice como "27, 99" resultó desactualizada — la cascada de ADR-76 ya la había corregido a "27, 101"; re-derivada aquí por `grep`, no copiada del índice). El índice también advierte una trampa verificada real: cualquier ADR que no recalcule `N FAIL · M WARN` en ambos archivos arriesga romper `--baseline`. Este acto no toca `canon/modelo-decision-v4_0.md` (A.8 no cambia reglas ni tiers).

**2 · CONTENIDO**, comando y salida cruda:

```
$ ls instrucciones-proyecto-v2_7.md
ls: cannot access 'instrucciones-proyecto-v2_7.md': No such file or directory
$ grep -c "A\.8" instrucciones-proyecto-v2_6.md
0
$ grep -c "ADR-77" canon/gobernanza-v1_15.md
0
$ ls forense/encargos/2026-08-13-A7-indice-infraestructura.md
-rw-r--r-- 1 root root 11593 Aug 13 18:40 forense/encargos/2026-08-13-A7-indice-infraestructura.md
$ ls forense/notas/2026-08-13-a8-land.md
ls: cannot access 'forense/notas/2026-08-13-a8-land.md': No such file or directory
$ grep -c "VERIFICACIÓN DE EXISTENCIA" forense/encargos/convencion.md
0
```

`NO-ENCONTRADO` para `instrucciones-proyecto-v2_7.md`, el rótulo `A.8`, `ADR-77` y la nota propia — los cuatro objetos que este acto produce, ninguno existía. `EXISTE-SATISFACE` para el borrador del índice de infraestructura citado por la Parte 1 (existe, línea 18 confirmada — es el mismo borrador de A.7 que ADR-76(h) ya había verificado el mismo día).

**3 · COBERTURA RETROACTIVA.** `instrucciones-proyecto-v2_6.md` era la única vigente antes de este acto (no hay v2.7 previa) — sin brecha retroactiva que declarar para el propio A.8. Nota aparte, no encargada aquí: `data/INFRAESTRUCTURA-v1_0.md`/`acceso-puertas`/`crosswalk` nacieron el 12/ago (Parte 4 del encargo); `acceso-puertas` en particular no tiene columna que ligue al manifiesto (`grep -c "manifiesto" data/acceso-puertas-2026-08-13.tsv` → `0`, confirmado en este acto) — es el caso testigo que A.8 nombra, y su remedio (columna `estado_adquisicion`) queda declarado, no encargado (Parte 4, "no conviertas esto en un barrido").

⚠️ Ninguno de los dos puntos anteriores revela trabajo ya hecho: se procede.

---

## 3 · COMMIT 1 — el texto

`cp instrucciones-proyecto-v2_6.md instrucciones-proyecto-v2_7.md` (copia byte-exacta, verificada con `diff` antes de tocarla) + título a `v2.7` + al final de Bloque D-ter: tres frases "Por qué v2.7" (defecto GESIS/WVS + costo, redacción propia de este acto) + la Parte 1 del encargo pegada verbatim (`### A.8 · ...` hasta la nota de numeración de `A.7`, carácter por carácter contra el encargo fuente). No se editó `instrucciones-proyecto-v2_6.md`.

**No se incluyó la Parte 2 del encargo** (el bloque `VERIFICACIÓN DE EXISTENCIA` de código-fence) dentro de `instrucciones-proyecto-v2_7.md` — la receta de COMMIT 1 solo nombra "la Parte 1 ... + un bloque 'Por qué v2.7'", y Parte 2 describe dónde va dentro de *cada encargo futuro* ("después del bloque ARRANQUE de Bloque D"), no un texto para insertar a mitad de Bloque D en `instrucciones-proyecto` — insertar ahí habría roto la convención de append-only-al-final que v2.4→v2.5→v2.6→v2.7 respeta. **Hueco declarado, no oculto:** el texto literal de Parte 2 no queda commiteado en ningún archivo del repo por este acto — vive hoy solo en el encargo fuente (fuera del repo) y en el ejemplo trabajado de la Parte 3 del propio encargo. `forense/encargos/convencion.md` gana una línea que exige el bloque (§4 abajo) pero no reproduce su texto. Si hace falta un lugar canónico para copiarlo, es decisión de mesa, no de este acto — declarado aquí para que no se pierda.

---

## 4 · COMMIT 2 — el sello y la cascada

**Número de ADR, derivado al sellar, contra el `main` real (receta T15):**

```
$ python3 -c "
import re, collections
t=open('canon/gobernanza-v1_15.md',encoding='utf-8').read()
n=[int(x) for x in re.findall(r'^\*\*ADR-(\d+)', t, re.M)]
print('únicos:',len(set(n)),'max:',max(n),'dups:',sorted(k for k,c in collections.Counter(n).items() if c>1),'huecos:',sorted(set(range(1,max(n)+1))-set(n)))
"
únicos: 76 max: 76 dups: [] huecos: []
```

Contra `8b30306`, no contra el `7ac523e`/`únicos:75` que el encargo anticipaba — exactamente la diferencia que el propio encargo previó ("si SELLA ya fusionó, el máximo es 76 y el tuyo 77"). **ADR-77**, no ADR-76.

`ADR-77` incorpora `instrucciones-proyecto-v2_7.md` verbatim (precedente exacto: ADR-59 con v2.4 — no reproduce el texto completo dentro del ADR, solo declara la incorporación), declara la absorción del borrador de A.7 y los cinco contadores de medición sobre México sin mover (`13 de 27` · `9 de 14` · `0 de 15` · `1 de 2` · `4 de 144`) — verificado contra ADR-76(h), que ya traía los mismos cinco. Añadido al final de §4 (`canon/gobernanza-v1_15.md`), después de ADR-76 y antes de §5 "Deuda declarada" — mismo lugar donde vive cada ADR nuevo, todos dentro de la sección madre única (Dominio 7 de `INFRAESTRUCTURA`).

**Cascada, derivada por `grep`, no copiada:**

```
$ grep -noP '.{20}\d+\s*ADR\b.{0,10}' canon/*.md   # antes de editar
canon/estado-programa-v1_10.md:27:...76 ADR, protocol
canon/estado-programa-v1_10.md:101:...76 ADR, protocol
canon/gobernanza-v1_15.md:2:...**76 ADR**
canon/gobernanza-v1_15.md:968:...`71 ADR` {cita-historica}
```

Tres citas vigentes de "76 ADR" (`gobernanza:2`, `estado-programa:27,101` — coincide con lo que la propia cascada de ADR-76 ya había re-derivado, no con el "27,99" que cita `INFRAESTRUCTURA-v1_0.md` Dominio 7, desactualizado); la cuarta (`gobernanza:968`) ya es histórica y está marcada `{cita-historica}` — no se toca. Las tres vigentes se corrigen a "77 ADR"; `estado-programa:101` gana además la cláusula nueva de su propia cadena histórica ("a 77 después, con ADR-77, ..."), mismo patrón que cada ADR anterior desde el 48.

Re-derivado después de editar:

```
$ python3 -c "...misma receta..."
únicos: 77 max: 77 dups: [] huecos: []
$ grep -noP '.{20}\d+\s*ADR\b.{0,10}' canon/*.md
canon/estado-programa-v1_10.md:27:...77 ADR, protocol
canon/estado-programa-v1_10.md:101:...77 ADR, protocol
canon/gobernanza-v1_15.md:2:...**77 ADR**
canon/gobernanza-v1_15.md:968:...`71 ADR` {cita-historica}
```

Sin huecos, sin duplicados, las tres vigentes en 77, la histórica intacta.

**`forense/encargos/convencion.md`** gana una cuarta viñeta en la cabecera obligatoria: el bloque VERIFICACIÓN DE EXISTENCIA de la Parte 2, además de SHA/Entorno/Estado — una línea, como pide el encargo.

---

## 5 · Cierre

`python3 tests/check.py --baseline`, corrido tres veces en este acto — antes de tocar nada, después de `instrucciones-proyecto-v2_7.md`, y después de los dos commits completos —, siempre **`18 FAIL · 104 WARN`, LÍNEA BASE: VERDE**, sin entradas nuevas contra `tests/baseline.json` (`HEAD` congelado `2cb39c9ceff4abe76d895a9739242c2e5d056516`). T15 (conteo de ADR) y T16 (self-check de FAIL/WARN) verificados explícitamente arriba, no solo por la corrida agregada.

Línea en `forense/hallazgos.md` (defecto de origen: la cola GESIS/ISSP/WVS). Esta nota. Merge: la rama nació ya sincronizada con `origin/main` (`8b30306`), así que no hubo `main` que fusionar hacia la rama antes de este acto — se re-verifica antes de `push` por si algo llegó entre tanto.

**Perímetro real, verificado contra `git status` antes del push:** `instrucciones-proyecto-v2_7.md` (nuevo) · `canon/gobernanza-v1_15.md` · `canon/estado-programa-v1_10.md` · `forense/encargos/convencion.md` · `forense/hallazgos.md` · `forense/notas/2026-08-13-a8-land.md` (nuevo). No se tocó `data/`, `milpa/`, `tests/`, `canon/modelo-decision-v4_0.md` ni `instrucciones-proyecto-v2_6.md`.

**Hueco declarado (no encargado a este acto):** la columna `estado_adquisicion` en `data/acceso-puertas-2026-08-13.tsv` que la Parte 4 del encargo nombra como el único remedio que sí vale la pena, y el texto de la Parte 2 sin domicilio canónico en el repo (§3 arriba) — ambos quedan para acto propio si mesa lo autoriza.
