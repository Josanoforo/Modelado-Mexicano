ENCARGO · ACTO GEN2-GOBIERNO-DECISIONES · IMPLEMENTA LA PROPUESTA DE ASTRA, ADAPTADA — la vista de mesa en un comando, el bucle de cierre en la rutina, y la prueba de cobertura antes de toda pregunta
Cabecera: NUBE, Opus · COMPUERTA: ninguna · redactado contra `origin/main = b711ee05` (PR #647, Primera-Silla) — el MISMO corte que la propuesta consultó, verificado ancestro · candidatos: deriva con `cierre_acto.py`. CONCURRENCIA: CIERRES-CON-CITA puede tocar `no-corrido.tsv` en paralelo — si fusiona primero, sus 8 filas pasan de casos-vivos a casos-históricos de aceptación (la propuesta §8 ya lo contempla: «no es requisito dejarlos abiertos para probarlo»); re-deriva y sigue.
FIRMA DE MESA (circuito, 8-9/sep, verbatim): «O dame el encargo para que lo trabaje chatgpt y te lo traigo para el tailor made y lo mandemos al repo pasando por ti obviamente.» → propuesta entregada, validada por dirección (premisas reproducidas contra `b711ee05`: campos de FP/NC, `RE_VENCE` en digesto:306, exclusión de `no-corrido` en trámite, FP-362 con residual v4 real), este encargo la implementa. La propuesta viaja adjunta y P0 la commitea verbatim — no se re-lee de memoria.
PRINCIPIO RECTOR (de la propuesta, gobernante): «la máquina muestra qué falta y qué evidencia existe; dirección comprueba la cobertura; mesa decide y el merge sella.»

## Bloque VERIFICACIÓN DE EXISTENCIA (A.8, Parte 2)

Comprobado contra `origin/main` vigente (`9ce9774`, que incluye la fusión de `ACTO GEN2-CIERRES-CON-CITA` como `PR #648`) al arrancar:

- `tools/digesto_tramite.py` → existe (2092 líneas), con `RE_VENCE` en la línea 306 tal como la propuesta cita, y sección H incremental ya cableada.
- `tools/estado_comun.py` → existe (131 líneas), con `lee_tablero`/`es_abierta` ya compartidos por `digesto_tramite.py` y `tests/check.py`.
- `.claude/commands/tramite.md` → existe (382 líneas), excluye expresamente `no-corrido.tsv` de su perímetro (regla que P2 amplía, no reemplaza).
- `forense/firmas-pendientes.tsv` → existe; `FP-362` existe (línea 351), prefijo `ABIERTA` propio del contador CALC-0003-v3/v4, con residual v4 declarado en la propia glosa — control negativo verificado, no de memoria.
- `forense/no-corrido.tsv` → existe; los ocho casos que la propuesta cita ya se re-verificaron y cerraron por `ACTO GEN2-CIERRES-CON-CITA` (`PR #648`, fusionado antes de este arranque): `NC-0002`, `NC-0017`, `NC-0035` → `CERRADA`; `NC-0036` → `CERRADA-DESISTIDA`; `NC-0037`, `NC-0055` → siguen `ABIERTA` (`PARO-PREMISA` reportado por ese mismo acto); `GEN2-E5-0` → `CONSUMIDO` (`PR #629`); `PILOTO-CAJA` → trae `ESTADO: LISTO-CAJA`. Confirma la nota de concurrencia de la cabecera: las 8 filas ya pasaron de casos-vivos a casos-históricos de aceptación.
- `data/curacion-registro/cola-adquisicion-registro.tsv` → existe.
- `canon/registro-rotulos.tsv` → existe (253 líneas).
- `instrucciones-proyecto-v2_13.md` §D-14 → existe (línea 454, gate de automatización); distinta de la fila `id=D-14` de `forense/firmas-pendientes.tsv` (línea 349, `DECLINADA-POR-PROCESO`) — sin colisión, confirmado por lectura de ambas.
- Cobertura retroactiva: `forense/no-corrido.tsv` íntegro (74 líneas), `forense/firmas-pendientes.tsv` íntegro (355 líneas) y `PROPUESTA-GOBIERNO-DECISIONES-PENDIENTES.md` (adjunta por el operador, commiteada verbatim por P0 de este mismo encargo) leídos completos antes de tocar código.

---

LAS SEIS PREGUNTAS DE ASTRA §9, CONTESTADAS POR DIRECCIÓN (el ejecutor las hereda verificadas, no las re-investiga):

1. Los ocho casos: NC-0037 (10 vivas resueltas por firmas Grupo A/B/C — sustitución), NC-0002 (sucesora FP-343 resuelta — firma), NC-0017 (FP-346 FIRMADA — firma), NC-0035 (merge #624/#628 = aceptación — firma-por-merge), NC-0036 (rama desaparecida — verificar y cerrar según árbol), NC-0055 (relanzamiento fusionado #638 — ejecución), + 2 de bookkeeping (estado E5-0 rancio; PILOTO sin línea). Evidencias detalladas en `forense/encargos/…CIERRES-CON-CITA` (o su PR si ya fusionó).
2. Protocolo conversacional: vive en la cabecera de instrucciones del proyecto («revisa el repo… preséntalas con lenguaje de RH») + práctica de dirección. Reutilizable literal: contexto derivado nunca recordado; firmas verbatim dentro del encargo; merge = firma. El formato de conversación de la propuesta §4 se adopta como plantilla canónica y se registra en `.claude/commands/tramite.md` (sección nueva) para que toda sesión lo use igual.
3. Tokens vigentes (derivados hoy de FP): `FIRMADA · FIRMADA-PARCIAL · FIRMADA-POR-MERGE · FIRMADA-PENDIENTE-EJECUCION · FIRMADA-EJECUTADA · EJECUTADA · DECLINADA-POR-PROCESO · SUPERADA · CERRADA(-…)` — más variantes en plural y compuestos largos (`CERRADAS`, `FIRMADA-EN-MAGNITUD`…) que son exactamente la higiene a normalizar hacia adelante: la vista lee por prefijo (A.16), las escrituras nuevas usan el set canónico que P4 fija, la historia no se reescribe. NC: `ABIERTA · CERRADA(-…)` con columnas de cierre.
4. Dónde vive la evidencia de glosa: en la glosa de la propia fila + nota de conciliación en `forense/notas/` citada desde ella. Columnas originales de NC inmutables (regla vigente); las de estado/cierre son lo único editable — la ampliación de permiso de P2 se limita a ellas.
5. Metadatos de adquisición: el registro canónico es `data/curacion-registro/cola-adquisicion-registro.tsv` (fuente del generado); trae estado, receta y necesidad — basta para derivar responsable (mesa/máquina) y acción. Lo que falte por fila se señala «por aclarar», nunca se adivina.
6. ¿El ciclo completo alcanza? Sí, con presupuesto declarado: hoy son 6 FP + ~30 NC abiertas — legible en un tick. La regla de la propuesta queda intacta: si no alcanza, se publica la lista de no-revisados y el ciclo NO acredita cobertura completa.

PIEZAS (la propuesta §2-§8 es la especificación; abajo solo reparto y ajustes) P0 · Fichaje doble. 0-bis de este encargo + commit verbatim de `PROPUESTA-GOBIERNO-DECISIONES-PENDIENTES.md` en `forense/notas/` con su procedencia (ChatGPT/Astra, 9/sep, corte `b711ee05` declarado por ella misma). Tercer acto del linaje que ficha al entrar. P1 · La vista `--mesa` (propuesta §2, completa): extensión del digesto existente, lectores de `estado_comun` reutilizados, entradas FP+NC+registro canónico de adquisición, tabla con los siete campos, reglas de composición íntegras (residuales visibles, sin puntuaciones de urgencia, conjunto completo sin tope oculto, consulta por ID que también devuelve decididas). Etiquetas de presentación, jamás estados nuevos. Cero escritura: `--stdout` no modifica nada (falsador). P2 · El bucle de cierre en `/tramite` (§3): descubrimiento completo de abiertas cada ciclo; candidatas con fragmento y falta-por-verificar; enmienda contractual explícita a `tramite.md`: (a) permiso para actualizar SOLO estado/cerrado_por/fecha_cierre de NC con cobertura demostrada, (b) la vista/huella se emite antes de la compuerta de suite (el orden que la propuesta señaló y dirección adopta), (c) ciclo incompleto publica no-revisados, nunca «cero cerrables» global. Un solo PR de trámite, como hoy. P3 · Prueba de cobertura + plantilla de conversación (§4): la pregunta obligatoria y el formato «Necesito que decidas / Ya quedó cubierto / Falta / Desbloquea / Recomiendo / Revisar si / Si no respondes», registrados en `tramite.md`; «Necesito esta acción» para lo ya autorizado. P4 · Identidad y caducidad (§5-§6): convención `documento#ID` registrada en `canon/registro-rotulos.tsv` (una fila de frontera, como ya se hizo con A.N); set canónico de tokens para escrituras nuevas; `vence:` reutilizado; umbral de revisión: 7 días se adopta como propuso (elección de diseño reversible, así rotulada); decisiones tomadas no caducan por HEAD — A.10 gobierna su alcance. P5 · Falsadores dirigidos (§8, todos): los ocho casos (vivos o históricos según llegue CIERRES), FP-362 con su residual v4 REAL del árbol (la firma de v3 no cierra v4 — hoy es cierto y verificable), D-14 doc vs fila sin colisión, glosa-vs-token, sucesora-no-acredita-ejecución, cierre-por-evidencia-fuera-del-TSV, idempotencia de la vista, vencimiento-avisa-no-decide. Dentro de los tests existentes; cero capa nueva de vigilancia.
PERÍMETRO: `tools/digesto_tramite.py` · `tools/estado_comun.py` (solo extensión de lectores) · `.claude/commands/tramite.md` · `tests/` (dirigidos) · `canon/registro-rotulos.tsv` (una fila) · `forense/notas/` (propuesta verbatim + nota) · `forense/{firmas-pendientes,no-corrido}.tsv` SOLO por la rutina en su PR normal · 0-bis · cascada. Prohibido (de la propuesta, verbatim): segunda fuente maestra, motor semántico, cron nuevo, catálogo universal, migración de historia. Si escribes fuera, PARA. CONTADOR: cero GEN2 — y la línea de la propuesta se asienta tal cual: «la conciliación no cuenta como nueva medición: vale porque elimina bloqueos y preguntas redundantes». Gate D-14: contestado en su §7, aceptado por dirección contra el árbol. `## NO-CORRIDO / RESERVAS` obligatoria. Falsador del mecanismo entero: si en tres meses la vista no evitó ni una pregunta repetida ni el bucle asentó un cierre que dirección no hubiera cazado a mano, se retira y se anota — mismo criterio que todo el aparato.
