# ENCARGO · ACTO GEN2-DIN-LOTE-ENIF2024-COMMIT-2-3 · EL LOTE ABRE: 14 CRUCES DE ENIF 2024 SE VEN UNA SOLA VEZ, CONTRA TODOS LOS CONTENDIENTES SELLADOS

> ENTORNO: **CAJA**. NO es NUBE. **F3:** no la sesión de `#979` (congeló) ni la de `#967`.
CABECERA · SHA de redacción `99a43faf`; una sesión, rama `acto/gen2-din-lote-enif2024-commit-2-3` · MODO **RÍGIDO** (spec congelada, reserva) · CONTADOR: sella dos corridas; `cuenta_gen2 = SI` (F1); no adopta; el marcador levanta 14 `RESERVADA` por comando. **L0 de `canon/estado-programa-v1_14.md`: si choca al fusionar, toma la de `main` y re-inserta solo tu anotación; `canon/L0/` existe.**
**MODELO: Sonnet por mandato de mesa (presupuesto).** El procedimiento está congelado o es receta; tu latitud es logística. Toda duda entre dos interpretaciones del procedimiento se pregunta a mesa en una línea, con opciones; no se resuelve.
NO tocar (TUBERÍA): `tests/check.py`, `tools/cierre_acto.py`, `tools/tablero_programa.py`, `.github/workflows/`, `.claude/commands/`. Cierre: `tests/check.py --rapido` antes de empujar; la suite completa la corre el CI (firma 21/sep). Derivados («DERIVADO — NO EDITAR») no se commitean: los re-deriva el job de main (`#984`).

## 1 · OBJETIVO
La prueba prospectiva más grande del programa: 14 cruces, 96 celdas, 8 contendientes, congelada en `#979` con tres oros y potencia 0.85 sobre 44 celdas. «Hecho»: EMISIONES y ADJUDICACION selladas por `corrida0 run`, cada una en `origin` antes de la siguiente; veredicto por la regla v0.3 (primaria sobre los 5 pares; formalidad y `cuenta_formal` secundarias, rotuladas); cobertura de C2 **y de R2** por celda y por par «dentro de ENIF 2024»; vista, replay, marcador; nota de una página con la línea para la regla de θ.

## 2 · FIRMAS — verbatim
F1, F2, v0.3 (`FP-260921-GEN2-TRAMITE-FIRMAS-4-8a1f-01/-02`, v0.3 asentada por `#967`): cítalas del repo. Cobertura de R2 y oros RETROSPECTIVA (encargo de `#979`, sellado por lanzamiento).
**Decisión de mesa sobre los LLM — el lanzamiento con este archivo es el sello:** «L1 y L2 no se corren para este lote. La fila se sella `NO-EMITE — nadie corrió el mecanismo` (§2 de las instrucciones: no es una derrota). Razón: correr 224 capturas por CLI consume el presupuesto de mesa que hoy no hay, y esperar degrada la ceguera de la reserva. La pregunta del LLM sigue abierta en la fila L del duelo ENVIPE 2026 y en el marcador retrospectivo del piloto 1 (10.64 pp). Apartarse de la spec antes de abrir, declarado y por qué, es lo que E.6 permite.» *Si mesa selló capturas antes de lanzar: ignora esta firma y úsalas; dilo.*

## 3 · LO QUE DIRECCIÓN SABE (contra `99a43faf`)
- `[EJECUTADO]` `CALC-DIN-LOTE-ENIF2024-EMISIONES-0001` y `…-ADJUDICACION-0001` congelados (`#979`): preflight VERDE, tres oros sellados (`CALC-DIN-LOTE-ORO-*`), `secuencia_commits` con `commit_3a` escrito en `ADJUDICACION/spec.yaml:10696-10708` («la misma sesión del COMMIT-2, en commit aparte y ANTES de derivar R»; preflight esperado antes del 3a: `BLOQUEADO input_repo_ausente=emisiones_selladas`).
- `[LEÍDO: spec v1.0 :139-140, :206-210, :285]` L1/L2 secundarios, «mesa sella las capturas antes del COMMIT-2» — la firma de §2 los aparta.
- `[LEÍDO: nota de #979]` R2 con λ = ½ en los 14 pares; 9 pares con C2 sellado (5 primarios + 4 `cuenta_formal`… **re-lee la clasificación en la spec, no en este encargo**); los marginales 2024 vienen sellados de `#971` por sha.
- `[LEÍDO: piloto 3]` compuerta P2: puntos de candidatos y marginales no nulos antes de derivar R.

## 4 · YA HECHO
Por objeto: los dos CALC sin `ejecucion.json`; ninguna rama con COMMIT-2. Repítela tú.

## 5 · PIEZAS — orden estricto
**P0.** `preflight` EMISIONES VERDE con `enif2024_csv` COINCIDE; `pytest` de los ensayos del lote en verde; censo: nadie abrió ENIF 2024 por dos variables fuera de `localidad × edad` (rastro → PARO).
**P1 · COMMIT-2.** `corrida0 run CALC-DIN-LOTE-ENIF2024-EMISIONES-0001`. Sella; nulos por familia; fila L = `NO-EMITE` con la razón de §2. Push; `ls-remote`.
**P2 · Compuerta.** En lo sellado: puntos de C2/P2/R1/R2/R3 y marginales base no nulos en las celdas con soporte. Nulo → PARO antes de R.
**P3 · COMMIT-3a.** Exactamente lo que `spec.yaml:10702` dice. `preflight` ADJUDICACION → VERDE. Commit propio, push.
**P4 · COMMIT-3.** `corrida0 run …-ADJUDICACION-0001`. Reporta: ΔMAE C2−R2 con IC95 sobre los 5 pares y su salida (vence · con reserva · nadie); conteo de celdas como secundario; MAE de cada contendiente; cobertura de C2 y de R2 por celda y por par con intervalo binomial; formalidad y `cuenta_formal` en sus tablas aparte; B-bis palabra por palabra (si dos aplican, manda `falsador débil`). `EXPLORATORIO — NO ADJUDICA` después.
**P5 · Cierre.** Marcador y tablero por comando; nota: qué se probó, qué salió, con qué certeza, qué NO significa (acceso, ingreso y oferta antes que preferencia; adulto elegido; no comparable con ENCIG ni ENVIPE) y **la línea para θ**: ¿R2 venció al piso, sí o no?

## 6 · LATITUD (solo logística) · 7 · PAROS
Enlazar `data/raw`, instalar dependencias, reintentar sin salida producida. PAROS: a) leer ENIF 2024 por dos variables fuera de `corrida0 run` de estos CALC o antes de que emisiones esté en `origin` · b) editar spec, `spec.yaml` fuera de P3, o `.py` · c) `run` no sella → no se parcha · d) P2 halla un nulo · e) repetir una corrida · f) sesión/entorno equivocados · g) adoptar · h) correr un LLM.
Compuertas: P0 y «emisiones en origin» protegen abrir dato. Perímetro: `ejecucion/resultados/sello` de los dos CALC, la línea de P3, filas propias, marcador/tablero por comando, nota, cascada. Fuera de eso, PARA. `## NO-CORRIDO / RESERVAS` · `## CONSUMIDO`.
