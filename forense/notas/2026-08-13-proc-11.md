# ACTO PROC-11 · COMMIT 1 — el mapa congelado: renombre de la θ, celda-D de `obligación_medida`, `D`:14→15, y dos huecos de perímetro encontrados al derivarlo

**Acto:** ACTO PROC-11 · **Entorno:** repo-only, NUBE, `cloud_default`, sin `data/raw`, sin red · **SHA de redacción del encargo:** `origin/main` post-#220 declarado por dirección; el `origin/main` real al arrancar es `1cb6e3e` (ver §0.2) · **Depende de:** la enmienda in situ de ADR-75(b) (`canon/gobernanza-v1_15.md`, §1 abajo) y del insumo rector `forense/notas/2026-08-13-proc-10.md`.

Este commit **no escribe canon, ni `milpa/`, ni `tests/`**. Congela el mapa completo — qué se edita, qué no, con qué texto y por qué — para que el COMMIT 2 sea comparable línea por línea contra él. Todo número de línea de esta nota está re-derivado por comando contra el árbol de `1cb6e3e`, no heredado del encargo.

## §0 · ARRANQUE

1. **REPO.** Clon existente en uso: `/home/user/Modelado-Mexicano`. Ningún clon nuevo. Rama `claude/encargos-motor-v2-consolidada-4c2evp` (la designada, ya existía local y en `origin`), árbol limpio (`git status --porcelain` → 0 líneas).
2. **SHA — divergencia declarada, no material.** El encargo declara el corte en «`origin/main` post-#220 (PROC-10 fusionado)». El `origin/main` real es **`1cb6e3e`** (`Merge pull request #219 from Josanoforo/triage-63`), **un merge más allá** del que el encargo describe: `dc75d74` (#220, PROC-10) es ancestro de `1cb6e3e`, y #219 (TRIAGE-63) fusionó después. PROC-10 sí está en el terreno; el encargo simplemente se redactó contra un `main` de hace un merge. TRIAGE-63 no toca `canon/`, `milpa/` ni `tests/check.py` — la divergencia no mueve nada de este mapa. Declarada, no silenciada.
3. **`data/raw`.** Ausente. Este acto no toca microdato ni abre payload — no aplica.
4. **ENTORNO.** `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=cloud_default`. Sonda HTTP saltada: acto repo-only sin red, firma correcta para nube (ADR-59(b)).
5. **ESPEJO.** Toda cifra de esta nota sale del clon de (1), comando a la vista.
6. **Colisión.** `git fetch origin` + `git branch -r` → solo existen `origin/main` y `origin/claude/encargos-motor-v2-consolidada-4c2evp` (esta rama). Ninguna otra rama viva toca `canon/`, `milpa/` ni `tests/`. Sin PARA por concurrencia.
7. **Duplicado.** `grep -in "proc-11" forense/hallazgos.md forense/notas/*.md` → **0**. Este acto no se había ejecutado.
8. **Línea base, corrida ANTES de tocar nada.** `python3 tests/check.py --baseline` → **20 FAIL · 107 WARN — LÍNEA BASE: VERDE**, contra `tests/baseline.json` head `3d0d1e5fd05567e59f8df65b2e1495b43bbcfc3c` (ADR-76(f)). Coincide exacto con lo que el encargo declara. `T19b` y `T19c` ambos sin FAIL hoy.

### §0.9 · Bloque A.8 — verificación de existencia, contestado

| Comando | Salida | Lectura |
|---|---|---|
| `grep -c "norma_de_género" canon/modelo-decision-v4_0.md` | `0` | renombre pendiente — **esperado**, sigue |
| `ls data/curacion-registro/celdas-d/ \| grep -c obligacion` | **`1`** | el encargo dice «≥1 = PARA». **Falso PARA — ver §6.1** |
| `ls data/curacion-registro/celdas-d/ \| grep -c obligacion_medida` | `0` | la celda que este acto crea **no existe** — sigue |
| `grep -n "_CONTADOR_14" tests/check.py \| head -1` | `869:` | coincide con el `:869` que el encargo anticipa |
| `grep -in "proc-11" forense/hallazgos.md forense/notas/*.md \| wc -l` | `0` | no corrió antes |

---

## §1 · El rector del renombre, y el mapa EDITAR / NO-TOCAR

### 1.1 · La enmienda ADR-75(b), verbatim (`canon/gobernanza-v1_15.md`, enmienda in situ sobre el párrafo (b) de ADR-67(b))

> `P6_38` deja de ser sensibilidad condicional de `familismo_obligacion` y pasa a
> medida propia, nombrada: `obligación_medida` (ENASIC 2022, `P6_38`, tabla
> `TPOB_CUI`, θ categórica de 5 vías, n=5,677 — tabla y verificación en
> `forense/notas/2026-08-13-enasic-split-verificacion.md` §2.2/§3). El
> `familismo_obligacion` atitudinal (`P7_12_7`) se renombra en el mismo acto a
> `norma_de_género` para no seguir compartiendo nombre con una medida que ya
> no es la misma cosa. Las dos son celdas-D distintas, sin `rol: COMPLEMENTO`
> entre ellas (mismo criterio que ya separa la celda de actitud ENASIC de la
> celda de conducta ENUT, arriba) — dos medidas, dos análisis, sin fusión ni
> jerarquía.

Y, en el mismo párrafo, **la reserva que acota este acto**, verbatim:

> **Pendiente de mesa, no ejecutado aquí:** registrar la celda-D `G5.obligación_medida` (o el nombre que mesa fije en su momento) en `data/curacion-registro/celdas-d/`, y decidir **si `familismo_obligacion` sobrevive como nombre de generador** en `canon/modelo-decision-v4_0.md` o si también se desdobla ahí — fuera del perímetro declarado de este acto.

Más la política de denominador, también verbatim:

> `obligación_medida` entra, cuando se registre, como **condicional nueva** (`D`: 14 → 15), no como sub-componente de la existente (denominador intacto).

**Lectura que gobierna este acto:** la enmienda renombra **la θ condicional** (`P7_12_7`). La pregunta del **nombre de generador/coeficiente** queda explícitamente reservada a mesa. Por tanto el renombre de este acto es estrecho por rector, no por cautela propia.

### 1.2 · Firma de mesa recibida en este acto (D-renombre, 13/ago/2026), verbatim

> Opción 1. Se renombra toda aparición que denote la CONDICIONAL medida por P7_12_7 (:245, :251, :224, y el nombre en la fila de :271); NO se toca ninguna aparición de coeficiente/generador (:87, :375, :395, :598) ni prosa histórica. Precisión sobre procedencia.yaml: sus 7 apariciones son todas del coeficiente per el mapa de PROC-10 §1 — NO-TOCAR las 7, cero renombres ahí en este acto. La pregunta del generador NO se decide aquí ni se difiere al vacío: entra con nombre a la cascada de MOTOR-1 (qué tocaría desdoblar o renombrar G5 a nivel generador/coeficiente, con archivos y contadores medidos) y mesa la firma en MOTOR-2 con esa cascada enfrente — mismo criterio de ADR-72: no se decide una reescritura estructural sin conocer su universo.

### 1.3 · `grep -n "familismo_obligacion" canon/modelo-decision-v4_0.md` — 12 apariciones, clasificadas

| Línea | Qué denota | Clase | Razón |
|---|---|---|---|
| `:87` | Cambio 5 del esquema: `familismo` se desdobla | **NO-TOCAR** | Cita histórica de un cambio fechado; y es el nombre de generador, reservado a mesa (§1.1) |
| `:224` | **H-11**, `E[·\| informal, ingreso bajo]` — la condicional | **EDITAR** | Firmado por mesa. Denota la θ. Ver §1.4 (tensión declarada) |
| `:239` | Marca (b), escalas validadas en muestras mexicano-americanas | **NO-TOCAR** | Prosa de deuda de procedencia sobre el constructo heredado; fuera de la lista cerrada de mesa |
| `:245` | **Paso 1**, ítem 7 de los 15 parámetros | **EDITAR** | Firmado por mesa. Es la lista de condicionales |
| `:251` | **Paso 2**, los nueve que los generadores multiplican | **EDITAR** | Firmado por mesa |
| `:271` | **Paso 5**, fila «Proxy declarado, pendiente de medición» | **EDITAR (nombre + cita)** | Firmado por mesa; el RÓTULO de la fila espera D3 (§4) |
| `:375` | G5 · condición de refutación del generador | **NO-TOCAR** | Generador — reservado a mesa |
| `:395` | G5 · coeficientes del generador | **NO-TOCAR** | Coeficiente — reservado a mesa |
| `:598` | Tabla de conteo: coeficientes de generador, ADR-30 | **NO-TOCAR** | Coeficiente — reservado a mesa |
| `:772` | Prosa de riesgo: nombres psicológicos sobre celdas de estructura | **NO-TOCAR** | Prosa; fuera de la lista cerrada de mesa |
| `:779` | Prosa de riesgo: «el caso peor sigue siendo…» | **NO-TOCAR** | Prosa; fuera de la lista cerrada de mesa |
| `:784` | Marca (b), muestras de diáspora | **NO-TOCAR** | Prosa de deuda; fuera de la lista cerrada de mesa |

**EDITAR: 4 · NO-TOCAR: 8.**

### 1.4 · Tensión declarada dentro de `:224`, no resuelta aquí

`:224` (H-11) se renombra por firma de mesa, pero su **columna de proxy sigue citando «ENUT 6.11/6.11a, carga de cuidado a nivel persona (ADR-51 (c), M2)»**. ENUT es la capa **conductual** del `familismo_obligacion` viejo (ADR-67(b)), no de `norma_de_género` ni de `obligación_medida`. Tras el renombre, H-11 queda como hipótesis sobre `norma_de_género` con un proxy que ADR-67(b) asignó al constructo pre-split. **No se resuelve en este acto** — es exactamente la pregunta que la firma de mesa manda a la cascada de MOTOR-1 y a la firma de MOTOR-2. Se declara aquí para que llegue nombrada, no para taparla.

### 1.5 · `grep -n "familismo_obligacion" milpa/procedencia.yaml` — 7 apariciones, todas NO-TOCAR

`:83` · `:104` · `:105` · `:117` · `:629` · `:899` · `:936`. **Cero renombres en este archivo.** Razón, citando el mapa que PROC-10 §1 ya levantó y verificando de nuevo por lectura directa: las siete son del **coeficiente** (`rutas_estimabilidad_coeficiente`, `asignados_coeficiente`, vocabulario histórico), objeto distinto de la condicional — distinción que la propia celda-D hermana ya opera (`vocabulario_version: 0.4`, campo `procedencia_condicional` separado de `fuerza_coeficiente`). La firma de mesa de §1.2 lo confirma explícitamente.

**Consecuencia verificada y buscada:** `grep -c 'clase: "MEDIDO·PARCIAL' milpa/procedencia.yaml` sigue dando **9** después de este acto. El numerador no se mueve. T19b y T19c siguen cuadrando.

---

## §2 · La celda-D nueva — spec congelada

**Ruta:** `data/curacion-registro/celdas-d/G5.obligacion_medida.conducta.yaml` (sin tilde en el nombre de archivo, mismo criterio que la hermana `G5.familismo_obligacion.actitud.yaml`).

**Molde:** la hermana, campo por campo (esquema `propuesta-motor-adaptativo-celda-v0_3.md` §3, con §4 mandando donde difieran — mismo criterio declarado en la cabecera de la hermana).

**Campos materiales, congelados verbatim de `forense/notas/2026-08-13-enasic-split-verificacion.md` §2.2/§2.3/§3:**

| Campo | Valor congelado | Fuente |
|---|---|---|
| `id` | `G5.obligacion_medida.conducta` | encargo §2(b) |
| Variable | `P6_38`, ENASIC 2022 | §2.2, §3 Medida 2 |
| Tabla | `TPOB_CUI` | §2.2 |
| Filtro / universo | `FILTRO 6.10` (fila 1170) — población cuidadora; `FILTRO6_10='1'` en 100% de las filas, verificado contra microdato | §2.2 |
| Ponderador | `FAC_CUI` (fila 1483) | §2.2 |
| Diseño | `EST_DIS` / `UPM_DIS` (filas 1481-1482, viven directo en `TPOB_CUI`) | §2.2 |
| `n` | 5,677 · suma de pesos 31,652,127 · 148 estratos (0 singleton) · 861 UPM | §2.2 |
| θ | categórica de 5 vías, elección única, 0 blancos y 0 fuera de {1..5} | §2.2 |
| Periodo | **«la semana anterior a la fecha de referencia»** — **distinto** del de la hermana (`P7_12_7`: momento de la entrevista, sin ventana retrospectiva) | §2.2 |
| `dominio` | `TIE` | §2.4 abajo |
| `requiere_decision_mesa` | `false` | encargo §2(b) |
| `estado_operativo` | `PENDIENTE`, razón: «expediente por la vía — PROD-P638» | encargo §2(b) |
| `relacion_complemento` | **ninguna** | ADR-75(b): «sin `rol: COMPLEMENTO` entre ellas — dos medidas, dos análisis, sin fusión ni jerarquía» |

**Límite de escala, obligatorio en la celda (Bloque A-bis regla 3), verbatim de §2.2:**

> el 7.65% de `P6_38` (motivo propio, entre cuidadores ya identificados, ventana de una semana) y el 69.33% de `P7_12_7` (acuerdo con una norma general, población elegida completa, sin ventana retrospectiva) **no se restan ni se comparan como si fueran la misma escala** — universos distintos, preguntas distintas, escalas distintas.

### §2.4 · `dominio: TIE` — derivado, no tecleado

Vocabulario cerrado del esquema (`propuesta-motor-adaptativo-celda-v0_3.md:45`): `<FIN|MIG|TEC|CAP|CUL|SAL|SEG|TRA|EST|TIE>`. La hermana eligió `CUL` con razón escrita (Sección 7, «Percepción **cultural** de los cuidados») **y nombró en la misma línea el contraste**: «no tiempo/conducta (eso sería **TIE** — `uso_del_tiempo_cuidados_hogar`)». `P6_38` vive en Sección 6, dirigida a quien la sección ya identificó como cuidador/a, y mide motivo del cuidado **ya realizado**. Verificación independiente: `grep -ln "ENASIC" data/inventarios/*.md` → ENASIC está listada en `data/inventarios/inventario_fuentes_uso_del_tiempo_cuidados_hogar_mexico.md`. **`TIE`**, por el criterio que la propia hermana escribió.

**Matiz declarado:** la hermana nombra TIE pensando en «la celda `.conducta`/ENUT». Esta celda es `.conducta` pero **ENASIC**, no ENUT — el código de dominio se hereda por el eje conducta/cuidados, no por el instrumento. No colisiona con `G5.familismo_obligacion.conducta` (la celda ENUT), que sigue sin existir.

---

## §3 · `D`: 14 → 15 — la cascada completa, congelada sitio por sitio

### 3.1 · Firma de mesa recibida en este acto (D-cascada, 13/ago/2026): **cascada completa**

El encargo pedía «los seis sitios». Los seis del **titular** están verificados y coinciden exacto con el mapa de PROC-10 §6. Pero `D`=14 vive además en ~16 afirmaciones vigentes de derivación que ningún test cubre — mover solo los seis dejaría la suite VERDE y el `modelo` internamente contradictorio. Mesa firmó **cascada completa**.

### 3.2 · Corrección al mecanismo propuesto — por qué `obligación_medida` NO entra en el Paso 1

La opción firmada describía extender el Paso 1 (15→16 parámetros) y el Paso 2 (16−1=15). **Verificado contra el archivo, eso falsificaría el propio control aritmético del Paso 1.** `modelo:247`, verbatim:

> **Control aritmético contra §6:** el v1 tenía **9** parámetros (familismo único, confianza escalar). 9 + 5 (escalar → vector de 6) + 1 (familismo desdoblado) = **15**, y 15 × 6 perfiles = **90** — exactamente el `+36` y el `90` que §6 declara. ✔

El Paso 1 es una **reconstrucción histórica de la tabla 6×15 del v3.4**. `obligación_medida` no existía en esa tabla: meterlo ahí haría 16 × 6 = 96 y rompería el `90` que §6 declara y que ese control verifica. **Se ejecuta el fondo de la firma (cascada completa, derivación nombrada) con el mecanismo honesto:** Pasos 1-5 quedan intactos como historia, y se añade un **Paso 6** que declara la entrada post-reencuadre. Esto además satisface literalmente la regla que el propio `modelo:280` impone —«Si alguien mueve `D`, tiene que mover uno de esos cinco pasos y decir cuál»— con la respuesta honesta: **ninguno de los cinco se mueve; se añade un sexto, y se dice por qué.**

### 3.3 · Los seis sitios del TITULAR (`N de 14` → `N de 15`, numerador intacto en 9)

| # | Sitio | Texto vigente (fragmento) |
|---|---|---|
| 1 | `README.md:37` | `- **Condicionales medidas 9 de 14** <!-- grep -c ... -->` |
| 2 | `canon/estado-programa-v1_10.md:97` | ``No mueve `9 de 14` (corregido de `8 de 14`; …)`` |
| 3 | `canon/modelo-decision-v4_0.md:11` | cabecera v4.0 — `"condicionales medidas sobre atributos: ~~8~~ 9 de 14"`, con `` `D`=14 derivado en §1.1.F `` |
| 4 | `canon/modelo-decision-v4_0.md:277` | titular §1.1.F |
| 5 | `canon/modelo-decision-v4_0.md:621` | titular §6.1 |
| 6 | `canon/modelo-decision-v4_0.md:725` | bullet §7, con el reparto «De las 14: …» |

### 3.4 · Los sitios de DERIVACIÓN de `D` (cascada completa firmada)

| Sitio | Qué dice hoy | Acción |
|---|---|---|
| `modelo:15` | «Denominador nuevo derivado: **14 condicionales**» | **NO-TOCAR** — fila de la tabla de changelog `> **v4.0 — 3/ago/2026**`, historia fechada (y el formato que `_CAMBIO_FECHADO` reconoce) |
| `modelo:245` | Paso 1, los 15 parámetros del v3.4 | **EDITAR solo el renombre** (ítem 7). El conteo 15 **no se toca** (§3.2) |
| `modelo:247` | Control aritmético 9+5+1=15, 15×6=90 | **NO-TOCAR** (§3.2) |
| `modelo:251` | Paso 2, «de 15 parámetros a 14 condicionales» | **EDITAR solo el renombre**. El «14» del Paso 2 **no se toca**: es el resultado correcto del reencuadre v4.0 (§3.2) |
| `modelo:253` | `> ### **D = 14 condicionales** = 8 escalares + 6 componentes` | **EDITAR** → `D = 15` = 9 escalares + 6 componentes, citando el Paso 6 |
| `modelo:260` | «**no incluyen** las 14 condicionales» | **EDITAR** → 15 |
| `modelo:272` | `\| \| \| **14** ✔ *(9+0+2+3)* \|` | **EDITAR** → `**15** ✔ *(9+0+2+3+1)*` |
| `modelo:280` | `` **`D`=14 es derivado del diseño de §1.1** `` + la regla de los cinco pasos | **EDITAR** → `D`=15, y decir cuál paso se movió: **ninguno; se añadió el Paso 6** |
| `modelo:282` | «sustituidos por **14 condicionales** de dimensión indeterminada» | **EDITAR** → 15 |
| `modelo:284` | «son **14 familias de distribución** sin declarar» | **EDITAR** → 15 |
| `modelo:294` | «exige que cada una de las **14 condicionales**» | **EDITAR** → 15 |
| `modelo:603` | «**14 familias de distribución** sin declarar» (§6) | **EDITAR** → 15 |
| `modelo:604` | «para cada una de las **14 condicionales** de §1.1.F» | **EDITAR** → 15 |
| `modelo:614` | tabla `params_base` → «**14 condicionales** θ_k» | **EDITAR** → 15 |
| `modelo:624` | `` **`D`=14 sale del diseño de §1.1** `` | **EDITAR** → 15, apuntando al Paso 6 |
| `modelo:628` | «no incluyen las 14 condicionales … 14 es lo que hay que **medir**» | **EDITAR** → 15 (ambas) |
| `modelo:728` | «**Las 14 familias de distribución** exigidas por ADR-28.d» | **EDITAR** → 15 |
| `modelo:813` | `\| **`D` = 14 condicionales** \| 15 − 1 (`acceso_digital`…) \| **Sí** \|` | **EDITAR** → `D` = 15; la columna de derivación gana el Paso 6 |

**No se tocan** `modelo:541` («60 de 144»), `modelo:598` (coeficientes), `estado-programa:163` («60 de 144»), ni `README.md:39` («4 de 144») — otros contadores, congelados por decisión de mesa aparte.

### 3.5 · El Paso 6 nuevo y la fila nueva de la tabla de Paso 5 — texto congelado

**Paso 6 (nuevo, tras el Paso 5 y antes del titular de `:277`):**

> **Paso 6 — condicionales registradas después del reencuadre.** Los pasos 1-5 derivan `D` de la tabla 6×15 del v3.4 y son historia cerrada: no se mueven. Una condicional que **no existía en esa tabla** no cabe en ellos sin falsificar su control aritmético (`15 × 6 = 90`, Paso 1). Por eso este paso: `obligación_medida` (ENASIC 2022 `P6_38`, `TPOB_CUI`, n=5,677) entra como **condicional nueva** por firma de ADR-75(b) — *«entra, cuando se registre, como condicional nueva (`D`: 14 → 15), no como sub-componente de la existente»* — y su celda-D queda registrada por ACTO PROC-11. **`D`: 14 → 15.** Ningún paso 1-5 se movió; se añadió éste, que es la respuesta que la regla del titular exige.

**Fila nueva de la tabla de Paso 5 (`:271` en adelante), congelada:**

> `| **Medida sin clase de procedencia — decisión D3 pendiente** | `obligación_medida` (ENASIC 2022 `P6_38`, `TPOB_CUI`, n=5,677 — θ categórica de 5 vías, reproducida y adjudicada; **no** entra a `milpa/procedencia.yaml` porque `MEDIDO·PARCIAL(x)` exige eje condicionante y esta θ es marginal nacional, `x = ∅` — mismo muro que `ACTO PROC-10` documentó para `P7_12_7`, `forense/notas/2026-08-13-proc-10.md` §3) | **1** |`

**Total de la tabla:** `**15** ✔ *(9+0+2+3+1)*`.

**Contador declarado al cierre: `9 de 15`.** El numerador **no sube**: ninguna de las dos θ nuevas entra a `milpa/procedencia.yaml`, por la razón taxonómica de PROC-10 (`x = ∅`) que la decisión D3 es la única que puede levantar. Un «9 de 15» honesto vale más que un «10» inventado.

---

## §4 · El fix de `modelo:271` — SOLO la cita

Texto vigente de la fila (`Proxy declarado, pendiente de medición`, cuenta **3**): `` `familismo_obligacion` (ENUT 6.11/6.11a, M2) ``.

**Se cambia:** el nombre (→ `norma_de_género`, firma de §1.2) y la **cita** (→ ENASIC 2022 `P7_12_7`, con ADR-67(b) y ADR-75(a)). Razón, ya establecida por PROC-10 §4: ADR-67(b) fijó `P7_12_7`/ENASIC como la θ real de este generador el 10/ago; la cita a ENUT quedó obsoleta y PROC-10 la declaró sin corregirla por perímetro.

**NO se cambia:** el **RÓTULO de la fila** ni su cuenta (**3**). El rótulo «Proxy declarado, pendiente de medición» es hoy falso para este objeto — la celda-D niega explícitamente que sea proxy y la medición está calculada, reproducida byte a byte y adjudicada (PROC-10 §4). **Pero corregir el rótulo exige decidir a qué clase pasa, y esa es la decisión D3.** Se dice en el propio documento, no se tapa: la fila lleva marca de que su rótulo espera D3.

---

## §5 · RANURA D3 — **VACÍA en este acto**

**No hay firma de D3.** Firma de mesa recibida el 13/ago: *«Correr sin D3»*. En consecuencia, y exactamente como el encargo lo cablea:

- **Cero taxonomía nueva** en `milpa/procedencia.yaml`: no se abre `MEDIDO·NACIONAL` ni ninguna séptima clase. El header de clases **no se toca**.
- **Cero entradas nuevas** en `milpa/procedencia.yaml`. `grep -c 'clase: "MEDIDO·PARCIAL'` sigue en **9**.
- El contador se declara **`9 de 15`** con la razón escrita (§3.5).
- La fila nueva de Paso 5 y el rótulo de `:271` quedan **nombrando D3** como la decisión que los cierra.

Cuando D3 se firme, lo que cambia es acotado y ya está localizado: si **D3-A**, el header de clases de `procedencia.yaml` gana la definición nueva, entran las dos entradas (`norma_de_género` y `obligación_medida`), la fila nueva de Paso 5 y el rótulo de `:271` se reescriben, y el contador va a **11 de 15** (10 tras la primera, 11 tras PROD-P638). Si **D3-B**, cero taxonomía y traspaso nombrado a dos actos de medición condicionada. Si **D3-C**, nota de exclusión con razón.

---

## §6 · Dos huecos de perímetro, encontrados al derivar el mapa

### 6.1 · El comando de existencia del propio encargo produce un **falso PARA** — y el mismo predicado gatea otros dos actos

El encargo manda: `ls data/curacion-registro/celdas-d/ | grep -c obligacion` → «0 = celda no existe (esperado); ≥1 = PARA». **Da `1`.** Pero el que matchea es la celda **hermana**, `G5.familismo_obligacion.actitud.yaml`, que contiene la subcadena `obligacion` y que el propio encargo nombra como el molde a copiar. Verificado en `origin/main` (`git ls-tree`): la hermana ya estaba ahí antes de este acto. El predicado discriminante es `grep -c obligacion_medida` → **`0`**, la celda nueva no existe.

**Consecuencia que va más allá de este acto, y es la parte grave.** El **mismo predicado defectuoso** se usa como GATE en otros dos sitios del encargo:

- **§3, GATE de PROD-P638:** `ls data/curacion-registro/celdas-d/ | grep -c obligacion ≥1`. Ese gate **ya está satisfecho hoy**, antes de que PROC-11 escriba nada — es un gate que **siempre pasa**, y por tanto no gatea.
- **§4, gate de la cascada del commit 2 de MOTOR-1:** «celda `obligacion` ≥1», mismo defecto.

**Recomendación, no ejecutada aquí** (fuera del perímetro de este acto, que no escribe encargos ajenos): ambos gates deben usar `grep -c obligacion_medida`, o mejor `ls data/curacion-registro/celdas-d/G5.obligacion_medida.conducta.yaml`. Queda declarado para MOTOR-1 y PROD-P638.

### 6.2 · El perímetro de `tests/check.py` no alcanza a la regex gemela de T19c — y sin ella la suite se pone ROJA

El perímetro dice: «`tests/check.py` **SOLO la constante `_CONTADOR_14`**». Verificado: **T19c tiene su propia regex, independiente de esa constante**, que lee `README.md`:

```
tests/check.py:978    mcond = re.search(r"[Cc]ondicionales medidas\s*(\d+)\s*de\s*14", bloque)
tests/check.py:981                     "'condicionales medidas N de 14'")
tests/check.py:983        fail("T19c", f"README.md declara condicionales medidas {mcond.group(1)} de 14; "
```

El perímetro **sí** manda escribir `README.md:37`. Si se escribe «9 de 15» y solo se toca `_CONTADOR_14`, **T19c falla** («no se encontró 'condicionales medidas N de 14'») y la suite deja de estar VERDE — contra el criterio de cierre del propio acto. El perímetro, tal como está escrito, **no puede satisfacer su propio criterio de cierre**.

**Firma de mesa recibida (13/ago), verbatim:** *«Extender a la gemela»* — el perímetro de `check.py` se extiende a `:978` y sus dos mensajes (`:981`, `:983`); mismo cambio mecánico `14`→`15`, misma constante conceptual, **cero cambio de lógica de test**.

**Por qué el hueco sobrevivió:** el criterio de cierre del encargo nombra `T19b` y no `T19c`. Son dos tests que vigilan el mismo contador desde archivos distintos (`modelo` y `README`), y solo uno de los dos está detrás de la constante nombrada.

---

## §7 · Qué NO hace este commit

No escribe `canon/`. No escribe `milpa/procedencia.yaml` — **ni una línea**, en ninguno de los dos ejes (ni renombre, §1.5, ni entrada nueva, §5). No escribe `tests/check.py`. No crea la celda-D — nace en el COMMIT 2 con su contenido real, no como plantilla vacía aquí. No toca `canon/gobernanza-v1_15.md`: las citas «de 14» de los ADR viejos son historia y se quedan. No toca `tools/**` ni `data/curacion-registro/expedientes-produccion/`. No abre ningún expediente ni recalcula ninguna θ — las cifras de §2 se citan de `ENASIC-SPLIT`, que ya las verificó, y su reproducción es el encargo de **PROD-P638**, no de éste. No sella ADR. No mueve `4 de 144`, `13 de 27`, `0 de 15` (coeficientes), `1 de 2` (llaves) — verificados uno por uno, no heredados. No decide D3. No decide la pregunta del nombre de generador (§1.1, §1.4): entra nombrada a la cascada de MOTOR-1.

**Congelado.** El COMMIT 2 ejecuta exactamente este mapa. Si al ejecutarlo aparece un error de mapa, se levanta un **tercer commit** y esta nota **no se reescribe** — se declara la corrección ahí, mismo criterio append-only que el resto del corpus.

---

## ADENDA (COMMIT 3, post-ejecución) — un error de mapa, el cierre verificado, y lo que sale nombrado hacia los otros actos

Esta sección no edita nada de §0-§7. El mapa congelado se ejecutó; lo que sigue es lo que la ejecución encontró.

### A.1 · El error de mapa: **eran ocho, no siete**

§1.5 declara «7 apariciones» de `familismo_obligacion` en `milpa/procedencia.yaml`, citando el mapa de `ACTO PROC-10` §1 (`83, 104, 105, 117, 629, 899, 936`). La firma de mesa de §1.2 heredó la misma cifra desde ahí. **`grep -c "familismo_obligacion" milpa/procedencia.yaml` da `8`.** La omitida es `:630`:

```
629:    - {gen: G5, coefs: {familismo_apoyo: 0.50, familismo_obligacion: "signo negativo o no monotónico — SIN MAGNITUD", ...}}
630:    # ⚠ CHECK OBLIGATORIO (ADR-30): una configuración donde familismo_obligacion
```

**Ninguna acción cambia.** `:630` es un comentario del check obligatorio de ADR-30 sobre la configuración del coeficiente de G5 — del **coeficiente**, como las otras siete, así que su clase es **NO-TOCAR** por la misma razón, y la firma de mesa («NO-TOCAR las 7, cero renombres ahí») se cumple idéntica sobre ocho. Lo que cambia es la **cifra que dos documentos declaran**. La nota de PROC-10 no se reescribe (append-only); la corrección vive en `forense/hallazgos.md` y aquí. Verificado tras ejecutar: `git diff milpa/procedencia.yaml` → **0 líneas**.

### A.2 · Cierre verificado

- `python3 tests/check.py --baseline` → **20 FAIL · 107 WARN — LÍNEA BASE: VERDE**, cifra idéntica a la de §0.8 (antes de tocar nada), contra el head congelado `3d0d1e5`.
- **Criterio de cierre del encargo, cumplido:** `T19b contador 14 cruzado (modelo)` → `[ ok ]` con la regex nueva. Añadido: `T19c portada derivada (README)` → `[ ok ]`, `T15 T-ADR-COUNT` → `[ ok ]`.
- `grep -c 'clase: "MEDIDO·PARCIAL' milpa/procedencia.yaml` → **9**. El numerador no se movió, como estaba congelado.
- Tabla de Paso 5: `**15** ✔ *(9+0+2+3+1)*`.
- **Contador declarado: `9 de 15`.**

### A.3 · Desviación del mecanismo firmado, declarada

La opción firmada en D-cascada describía extender el **Paso 1** (15→16 parámetros) y el Paso 2 (16−1=15). **No se ejecutó así**, y la razón está en §3.2: el Paso 1 verifica `15 × 6 perfiles = 90` contra el `+36` y el `90` que §6 declara, y meter una decimosexta habría dado 96 — falsificando un control aritmético correcto. Se ejecutó el **fondo** de la firma (cascada completa, derivación nombrada) por la vía del **Paso 6**. El resultado es el mismo `D`=15 que mesa firmó; el camino es el que no rompe nada. Queda declarado por si mesa prefiere el otro.

### A.4 · Inconsistencia de referencia cruzada creada por el renombre — declarada, no resuelta

`modelo:251` (Paso 2) ahora dice que los nueve parámetros que los generadores multiplican incluyen `norma_de_género`, **y sigue citando `§2.2`**, donde ese objeto se llama `familismo_obligacion` porque §2.2 es la sección de **coeficientes de generador** y ese nombre no se tocó (reserva de ADR-75(b)). Lo mismo pasa con `modelo:224` (H-11), que se renombró por firma y **conserva en su columna de proxy la cita «ENUT 6.11/6.11a, M2»** — ENUT es la capa conductual del `familismo_obligacion` pre-split, no de `norma_de_género`.

Las dos son la **misma pregunta**, y es exactamente la que ADR-75(b) reservó a mesa: si el nombre de generador se desdobla o sobrevive. No se resuelve aquí, y no se difiere al vacío: por firma de mesa de este acto entra **con nombre** a la cascada de `MOTOR-1` (qué tocaría desdoblar o renombrar G5 a nivel generador/coeficiente, con archivos y contadores medidos) y se firma en `MOTOR-2` con esa cascada enfrente — mismo criterio que ADR-72: no se decide una reescritura estructural sin conocer su universo.

### A.5 · Lo que sale nombrado hacia los otros actos del paquete

| Destinatario | Qué se le manda |
|---|---|
| **MOTOR-1** | (i) La cascada del nombre de generador de G5 (§A.4), con dueño y firma en MOTOR-2. (ii) Su gate de cascada («celda `obligacion` ≥1») está roto: ya pasaba antes de este acto — usar `obligacion_medida` (§6.1) |
| **PROD-P638** | Su GATE está satisfecho de verdad ahora: la celda existe (`G5.obligacion_medida.conducta.yaml`) y `grep -c "de 15" canon/modelo-decision-v4_0.md` ≥1. Pero el gate **tal como está escrito** ya pasaba antes — usar el predicado discriminante (§6.1). La celda queda en `estado_operativo: PENDIENTE` esperando su expediente |
| **MOTOR-2** | La firma del nombre de generador (§A.4), sobre la cascada que MOTOR-1 produzca |
| **Acto de ranuras D3** | La ranura de §5 quedó cableada y vacía. Los dos sitios que la firma escribiría están localizados: el header de clases de `milpa/procedencia.yaml` y la fila «Medida sin clase de procedencia» de `modelo §1.1.F` Paso 5, más el rótulo marcado de la fila de proxy |

### A.6 · Lo que este acto sigue sin hacer

No selló ADR — no le corresponde: propaga decisiones ya firmadas (ADR-75(b)) a los archivos que ADR-75(a)/(b) declararon fuera de su propio perímetro, mismo criterio que las Entradas 0 y 4 de `forense/registro-recalculo-v1_0.md`. No decidió D3. No decidió el nombre de generador. No reprodujo la θ de `P6_38` — eso es PROD-P638. No tocó `milpa/procedencia.yaml`, `canon/gobernanza-v1_15.md`, `tools/**` ni ningún expediente. Contadores no tocados, verificados uno por uno y no heredados: `4 de 144` · `13 de 27` (Hito D) · `0 de 15` (coeficientes) · `1 de 2` (llaves).
