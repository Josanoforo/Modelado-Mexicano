**ADR-260921-GEN2-CUADERNO-DE-MESA-1-b6dc-01**, `ACTO GEN2-CUADERNO-DE-MESA-1`, 21/sep/2026, NUBE (`cloud_default`, red `DENEGADA-POR-POLITICA`, corpus no montado), Opus, sin sub-agentes, MODO ABIERTO, COMPUERTA: ninguna. Produce `forense/encargos/CUADERNO-DE-MESA-2026-09-21.md` (con `.sha256`, misma forma y lugar que el precedente del 21/sep): diecisiete grupos de decisión de mesa, cada renglón re-verificado por estado (A.17), en lenguaje de RH, con opciones, recomendación, razón y texto de firma listo. El universo se derivó y **no coincide** con el que el encargo suponía: las «~50 FP sin cerrar» no se reproducen (dos `awk` dan 12 y 146) porque `firmas-pendientes.tsv` trae campos citados con saltos de línea —415 líneas físicas, 413 filas reales—; con lector CSV y marcador por prefijo (A.16) son **6** FP no cerradas (5 por prefijo más `FP-332`, con `ABIERTA` escondida tras `FIRMADA-PARCIAL`) y **28** NC con token de mesa, de 87 que lo nombran sobre 176 abiertas. **Nueve de las 28 ya no esperan a mesa** y se entregan con evidencia para cerrarlas (cinco con FP sucesora FIRMADA cuyo texto contesta la pregunta, una firmada el 15/sep, una reasignada a mesa MOTOR, dos esperando ejecución): el 32 % de la cola. Dictamina, sin re-correr nada y leyendo el orden del sello —el de commits no es derivable, los dos llegaron en el merge `4dedab4`—: `FP-409` **DECLARADO** (medidor sucesor byte-idéntico salvo tres nombres de archivo; 23 de 26 RESULT idénticos, los 3 restantes rutas) y `FP-408` **INFERIDO**, admisible con nota (sucesión aditiva: 11 de 14 RESULT comunes idénticos, media 1.844649266183 en ambas, 84 de 84 estimaciones de `parejas.csv` idénticas; la nota es que `-v1_1` eliminó el campo `contaminacion_declarada` que `-0001` sí traía). Tres pendientes que dirección conoce salen `NO-ENCONTRADO` sobre 2 779 archivos (la regla de la etiqueta de la propia spec, el respaldo del corpus, si toda ola nueva nace reservada) y quedan asentados sin abrir fila, porque este acto no tramita. Señala que `FP-374` está `VENCIDA-EN-ALCANCE` y **no se firma: se re-sella** (A.10) — es el renglón que cada trámite vuelve a encolar por leerlo mal. `T25` censa el cuaderno en `_T25_ARCHIVOS_CONOCIDOS`: el rótulo pelado que el regex ve sale de la cita verbatim de `motor.py:20`, que es el objeto de uno de los renglones. **CONTADOR: ninguno** — no firma, no cierra, no adopta, no re-corre; `cuenta_gen2` +0, adopciones 0, mediciones sobre México 0 (auditoría §5 no aplica). Falsador a tres meses: si mesa no resuelve ocho de los diecisiete grupos con el cuaderno en la mano, el formato no sirve y se anota.
