- **SHA de redacción:** `3e071f0` (merge de #175, `origin/main`, 12/ago/2026)
- **Entorno asignado:** cualquiera con el repo (no toca microdato ni red)
- **Estado:** CONSUMIDO — PR `mesa/m6-sello-escala`

---

MULTI-ENCARGO · las cuatro decisiones firmadas (12/ago/2026) — tres actos: M-6 · V · J
Base declarada: `origin/main = 3e071f0` (merge #175), 69 ADR, suite `--baseline` VERDE 22 FAIL · 101 WARN — los tres verificados por comando al escribir este encargo
PROCEDENCIA. Mesa firmó las cuatro decisiones de §3.1–3.4 del transfer del 11/ago sobre `MESA-cuatro-decisiones-3_1-a-3_4.md`: (1) bloqueadas · (2) añadir la fila · (3) medir el tamaño y plan de remediación · (4) arreglarlo. Ninguna cifra de este encargo es criterio sin re-derivar en PASO 1.
ESTADO DEL TERRENO al escribir, verificado — cámbialo si se movió: `pr/176` (e4c) abierto · `remediacion/brecha-documental` abierta, reclama ADR-70, y su merge pone la línea base en ROJO (24 FAIL · 105 WARN, 6 entradas nuevas; su sello byte a byte sí aguanta — verificado). Ninguno de los tres actos de abajo depende de esa rama.
ORDEN: M-6 → V. `J` es independiente pero espera el merge de #176. Concurrencia: perímetros disjuntos salvo `forense/hallazgos.md`, que resuelve `merge=union`. Todo merge va local (`git merge` con `.gitattributes` en el árbol), nunca por el botón de GitHub ni por su editor web de conflictos — verificado el 12/ago en #175: GitHub no honra el driver del lado servidor.
════════ ARRANQUE — hazlo antes de leer el resto del encargo ════════
Reporta las cinco líneas de abajo y NO empieces hasta tenerlas. Si algo no cuadra, PARA y repórtalo: encontrar que el terreno no es el que el encargo supone es entregable, no interrupción.
1 · REPO. Localiza el CLON EXISTENTE. No clones uno nuevo salvo que no haya ninguno, y si clonas, dilo. Reporta: ruta absoluta · git log -1 --format="%h %s" · git status ⚠️ No arranques desde el home. Si el cliente avisa "launched in your home directory", cámbiate al clon antes de nada.
2 · SHA. Confirma contra qué base trabajas y compáralo con el que el encargo declara. Si main se movió: NO es PARO — refresca, re-deriva lo que dependa del perímetro, y reporta la diferencia antes de editar.
3 · data/raw. AUSENTE NO ES PARO. Es raíz integrada, gitignorada, resuelta por código; un clon fresco siempre nace sin ella. Se crea o se enlaza. Reporta: existe / la enlacé a <ruta> / la creé. ⚠️ Si este acto DESCARGA algo: verifica al cerrar que los payloads quedaron en el CORPUS COMPARTIDO y no solo en tu worktree. Es el defecto de PR #77 y no lo atrapa ningún test.
4 · ENTORNO. "Ubuntu" no distingue nada; la política de red sí. CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE → esperado: sin_variable curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://www.inegi.org.mx/ Reporta los dos valores crudos. NUNCA curl -I. Si este acto no toca microdato ni red, dilo y salta este punto.
5 · ESPEJO. Prohibido derivar cifras del espejo del proyecto: está versiones atrás del repo y contiene archivos que el repo nunca tuvo. Toda cifra sale del clon de (1), con el comando a la vista.
════════════════════════════════════════════════════════════════════
Los tres actos abren con este bloque verbatim y reportan sus cinco líneas antes de nada. No se repite tres veces abajo: es el mismo.
ACTO M-6 · SELLO Y ESCALA — un ADR con las cuatro cláusulas + la fila que falta
ENTORNO ASIGNADO: cualquiera con el repo. No toca microdato ni red → salta el punto 4 del ARRANQUE diciéndolo. NO se lanza en dos entornos a la vez.
PERÍMETRO. SOLO: `canon/gobernanza-v1_15.md` (append del ADR + cascada que la receta T15 derive) · `canon/estado-programa-v1_10.md` (solo los sitios de cascada del conteo) · `forense/r5-1-diseno-por-regla-preregistro-v1_0.md` (append de un apartado nuevo, jamás editar las filas A/B/C/D) · `forense/hallazgos.md` (append) · `forense/encargos/2026-08-12-M6-sello.md` (copia literal). NO toca `tools/`, `tests/`, `data/`, `milpa/`, ni ninguna rama viva. Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.
CONCURRENCIA: `e4c/r5-1-d2` y `remediacion/brecha-documental` vivas. Ninguna toca la ficha `r5-1-…-preregistro`. `remediacion` sí toca `gobernanza` — coordina: si está por fusionar, espera; si no, tú escribes y ella rebasa. Un escritor a la vez, decláralo en el cierre.
PASO 1 · Premisas — corre y pega crudo
```bash
git log -1 --format="%h %s"                                              # 3e071f0 o posterior
grep -c "^\*\*ADR-\[0-9\]" canon/gobernanza-v1_15.md                     # reporta el valor
# número libre: mira main Y las ramas abiertas — es el defecto que ADR-69 pagó
for b in origin/main origin/e4c/r5-1-d2 origin/remediacion/brecha-documental; do
  printf "%-45s " $b
  git show $b:canon/gobernanza-v1_15.md 2>/dev/null | grep -oE "^\*\*ADR-[0-9]+" | grep -oE "[0-9]+" | sort -n | tail -1
done
sed -n '/^## 6/,/^## 7/p' forense/r5-1-diseno-por-regla-preregistro-v1_0.md | grep -oE "^- \*\*[A-Z]"   # esperado: A B C D
grep -c "^- \*\*E " forense/r5-1-diseno-por-regla-preregistro-v1_0.md    # esperado: 0 — el hueco que este acto cierra
git log --oneline origin/e4c/r5-1-d2 | head -1                           # commit 5 NO debe existir
python3 tests/check.py --baseline                                        # esperado: VERDE
```
PARO si: existe ya un resultado de `R5.1-D2` (la enmienda dejaría de ser pre-dato) · o `grep -c "^- \*\*E "` ya da ≥1 · o `--baseline` arranca en ROJO.
El número del ADR se deriva, no se teclea: el máximo entre main y todas las ramas abiertas, +1. Reporta el número y de dónde salió.
PASO 2 · El ADR — cuatro cláusulas, una por decisión firmada
ADR-NN · Mesa firma las cuatro decisiones abiertas del transfer del 11/ago: producciones de `radio_confianza` siguen bloqueadas, la escala de `R5.1-D2` gana su fila de corroboración, se autoriza medir el alcance del defecto de `folioviv` sin comprometer reapertura, y el enum de `fuerza` del contrato celda-D se corrige antes del piloto. Decisión de mesa del autor, 12/ago/2026, sobre `MESA-cuatro-decisiones-3_1-a-3_4.md`.
(a) Las 8 producciones de `radio_confianza` (`ESP-OPACA-C`) siguen en `NO_LISTA_DECISION_HUMANA_PENDIENTE`. `integrate_production.py:284-289` decide sobre `supervisor_link.requiere_decision`, que espera la decisión de sustitución paramétrica —el acto de vinculación-invarianza ENCUCI↔ENBIARE, que no ha corrido— y no la de clasificación, que `DH-ea9e932f3970ce12` ya resolvió (ADR-67). Costo de esperar: cero, nada las consume hoy. Costo de voltear: la marca `PROXY_PARCIAL` viviría solo en la celda-D y el ADR, y quien lea `produccion-modelo.tsv` podría usarlas como sustitutas de ENCUCI sin ver la reserva. Consecuencia declarada, no una decisión nueva: `ESP-OPACA-B` también tiene `requiere_decision: SI`, así que la primera θ de `familismo_obligacion` que produzca E4b′ llegará a `CALCULO_DESCRIPTIVO_DISPONIBLE` y no a `LISTA_PARA_USO_MODELO`. Es el comportamiento correcto de la compuerta y se anota para que nadie lo lea como fallo de U1. Nota mecánica para cuando se decida voltear alguna: `supervisor_link` está en `FORBIDDEN` (`prepare_production.py:18`), así que se despoja de la spec cegada — voltearla no rompe el congelamiento byte a byte; lo único que se mueve es `config_hash` → `hash_especificacion_fuente` en las 11 filas.
(b) La escala del §6 de `r5-1-diseno-por-regla-preregistro-v1_0.md` gana su fila `E`, declarada antes de la corrida. Derivado y verificado: la escala tiene exactamente cuatro filas (A/B/C/D), `"20pp"` aparece una sola vez y es la cota superior de B, y `grep -c "^- \*\*E "` da 0 — no había fila donde anotar un DiD >20pp, es decir, el desenlace en que la regla sobrevive decisivamente a su prueba. Es el defecto que Bloque B-bis nombra y el que ADR-58 ya remedió para la escala de falsación ("gana su fila `E` de corroboración prospectiva y su regla de precedencia, declaradas al sellar"); esta ficha, sellada aparte, no lo heredó. Se agrava porque el propio commit 4 de E4c declaró, antes del dato, que la fila A es el desenlace menos esperado a priori (86% CDMX · 37% Amuedo-Dorantes/Juárez · ~30% Jensen): la ficha ya declaró que lo más probable es lo que su escala no podía registrar. Legitimidad de la enmienda: es pre-dato — ningún resultado de `R5.1-D2` existe (commit 4, verbatim: "Ningún resultado de R5.1-D2 se produce aquí"), verificado en PASO 1. Se apendiza; las filas A/B/C/D no se editan.
(c) Se autoriza medir el alcance del defecto de `folioviv`, sin comprometer la reapertura de ningún veredicto. Mecanismo verificado de primera mano: `tests/r5_1_pension_bienestar.py:165-168` hace `hogares.get(hkey)` y `continue` silencioso; `grep -rn "zfill\|rjust(10" tests/ tools/` = 0; `tests/p3_lca_data.py:131,136,155` cruza por la misma llave; `validar_contra_publicado()` suma columnas de `concentradohogar` y jamás ejercita el join — pasó verde con el join roto. El contador no puede moverse hacia atrás: T18 hace `reales.add(m.group(1))`, un set con llave en el ID de regla — hoy 14 líneas canónicas → 13 reglas (`R4.3` ya aparece dos veces), contador `13 de 27`. Apendizar una entrada corregida de `R5.1` da 15 líneas y sigue dando 13. Regla de sustitución, sellada aquí porque no había precedente: `R4.3` es precedente de desdoble, no de reemplazo. Una entrada fechada posterior vence en alcance a la anterior, la anterior se conserva verbatim, y la nueva lo dice en su propia línea. Ningún veredicto se reabre por este ADR: `R5.1 → A` (ADR-58) y `D5 — INESTABLE` (ADR-53) siguen vigentes hasta que exista magnitud medida y mesa adjudique en acto propio.
(d) El enum `fuerza` del contrato celda-D se corrige antes de E0. Derivado: el enum de v0.3 es `ASIGNADO | AJUSTADO | IDENTIFICADO`, y 2 de 2 celdas semilla escribieron fuera de él, con valores distintos y reserva escrita — `familismo_obligacion.actitud:58` → `NO_DETERMINADO` ("los tres caracterizan un VALOR existente… aquí no hay valor que caracterizar"), `radio_confianza:144` → `MEDIDO·PARCIAL(x)` ("ninguno nombra una medición condicional directa de microdato"). Diagnóstico de raíz, escrito por la propia celda de radio: el coeficiente que ese valor alimenta (`radio_confianza=0.15`) sí es `ASIGNADO`, "pero es un objeto distinto (el peso que el generador le da a la condicional), no la condicional misma que esta celda compara" — el enum es el vocabulario de coeficientes de `milpa/procedencia.yaml` (linaje ADR-49/50/51) aplicado a un objeto que no es un coeficiente. Costo: data-y-prosa. `grep -rln "celdas-d" tools/ tests/ --include=*.py` = vacío, no hay schema de celda-D, `grep -c "celdas-d" tests/check.py` = 0 — nada en código valida estos YAML, así que ADR-68(a) no aplica. Prohibido el cajón de sastre: no se añade `OTRO` ni `NO_DETERMINADO` como miembro del vocabulario. Práctica establecida (HL7 FHIR binding strength, adoptada independientemente por openEHR vía SPECAM-68): un vocabulario extensible que contiene un concepto de "otro" o "no especificado" vuelve a ser de facto cerrado, y esconde justo la señal de que el vocabulario no cubre el espacio. Ejecuta el ACTO V.
Cascada. Conteo de ADR en los sitios que la receta T15 derive, reportados uno a uno en el PR. Contadores que NO se mueven, declarado: `13 de 27` · `15 coeficientes, cero medidos` · `9 de 14` · llaves ejercidas `0 de 2` · producciones `LISTA_PARA_USO_MODELO` sigue en 2.
PASO 3 · La fila E en la ficha — apéndice, no edición
Apendiza al §6 un apartado nuevo, fechado, titulado "Apéndice 12/ago/2026 — fila `E` y su precedencia (ADR-NN(b))", con exactamente esto y nada más:

* E — DiD >20pp en al menos uno de los dos desenlaces, decisivo (IC95% que despeja el umbral por completo), y monto documentado como suficiente —la misma exigencia que ya pesa sobre A—, y identificación de §2 exitosa. Corroboración acotada: la regla sobrevive a su prueba en esta ventana, con este diseño. No es confirmación causal ni licencia para afirmar intervención — la compuerta de identificación de ADR-57 sigue rigiendo.
* Precedencia, declarada al sellar y no después (Bloque B-bis): el orden pasa a ser A → E → B → C → D. La cláusula de "monto insuficiente" de B gana sobre E, exactamente como ya gana sobre A: si el monto no está documentado como suficiente, el resultado es B (ambiguo) sea cual sea la magnitud del DiD. Razón escrita: un monto insuficiente es una objeción de atribución, no de tamaño — un DiD grande con transferencia trivial frente al gasto del hogar no puede atribuirse a sustitución inducida por ese dinero. Que E herede la misma condición que A mantiene la escala simétrica en sus dos extremos.
* La contraparte de A-bis sigue por encima de toda la escala: un punto estimado que satisface un umbral con un intervalo de confianza que no lo despeja no adjudica — se reporta como propuesta con la reserva escrita.
* Nota de alcance: este apéndice no edita las filas A/B/C/D ni la regla de precedencia original; la amplía. Si algún día chocan, gobierna el apéndice por ser posterior y explícito.

PASO 4 · Cierre
Hallazgos (una entrada), encargo archivado, cascada reportada sitio por sitio, `--baseline` cruda en el cuerpo del PR. PR `mesa/m6-sello-escala`, NO FUSIONAR sin mesa. Contadores de medición movidos: 0 esperado.

## INDETERMINADO

Derivado por auditoría 2026-09-03 (ACTO MAESTRA37-N9 · AUDITA-ENCARGOS-166). `grep -Fn -- "2026-08-12-M6-sello.md" canon/gobernanza-v1_15.md` cita ADR-78, pero el bloque mezcla lenguaje de ejecución y de encargo pendiente (o el rótulo del ADR es compartido entre varios encargos sin desenlace individual claro) — rastro parcial, no se decide aquí. Para mesa: verificar manualmente contra ADR-78 en canon/gobernanza-v1_15.md.
