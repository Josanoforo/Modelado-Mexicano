# ACTO FIRMAS-2 · Sella ADR-80 — las dos firmas de mesa que desbloquean el carril de caja, aportadas por el ejecutor porque los actos que las necesitan no pueden dárselas a sí mismos

**Acto:** ACTO FIRMAS-2 · **Entorno:** repo-only, nube (`cloud_default`), sin red, sin `data/raw` · **Depende de:** ADR-79(i) (a, enmienda in situ) · ADR-76(d)(4) (b, enmienda in situ) · `forense/benchmark-enlace-invarianza-v1_0.md` §D10 (b) · `forense/EDGE-CASES-y-literatura-reciente.md` §E5 (b).

## §0 · ARRANQUE

1. **REPO.** Clon existente en uso: `/home/user/Modelado-Mexicano`. Ningún clon nuevo.
2. **SHA.** `git log -1 --format="%h %s"` → `d55ae72 Merge pull request #221 from Josanoforo/inv-descmx`. Coincide exacto con el SHA de redacción declarado en el encargo (`d55ae72`, merge #221, INV-DESCMX). `git status` → rama `claude/acto-firmas-2-carril-caja-fc50vc`, árbol limpio antes de editar. `git ls-remote --heads origin` → única rama viva en `origin`: `refs/heads/main` en `d55ae72`. Sin otra rama viva que toque `canon/gobernanza` — no aplica el PARO del perímetro.
3. **data/raw.** `ls data/raw` → no existe (`AUSENTE`). Este acto no la necesita — documentación pura de firmas de mesa, sin corrida sobre microdato. Declarado, no comprobado más allá de eso.
4. **ENTORNO.** `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE` → `cloud_default`. Este acto no toca microdato ni red: se salta la sonda de Bloque D, declarado.
5. **Baseline, antes de tocar nada.** `python3 tests/check.py --baseline` (HEAD congelado `3d0d1e5`) → **20 FAIL · 107 WARN — LÍNEA BASE: VERDE**. Igual a la cifra que `ACTO SELLA-3`/`ADR-79` ya reportó sobre el mismo congelado.

## §1 · Verificación de existencia

**Estructura.** Dominio 7 de `data/INFRAESTRUCTURA-v1_0.md` — sellar una decisión de gobierno. Tablas gobernantes tocadas: `canon/gobernanza-v1_15.md` §4 (el ADR y las dos enmiendas in situ) + cascada obligatoria a `canon/estado-programa-v1_10.md`. Ninguna otra tabla.

**Contenido.** Verificado contra `d55ae72`, texto verbatim confirmado por lectura directa del archivo: `ADR-79(i)` (`gobernanza:1181`) dice, verbatim, *"REGISTRADO, NO SELLADO ... Este ADR no sella ningún alcance para (i)"*; `ADR-76(d)` inciso (4) (`gobernanza:1096`) dice, verbatim, *"mesa decide antes de correr el acto cuál estándar de éxito aplica"*. `EXISTE-NO-SATISFACE` en los dos casos, confirmado: la pregunta está escrita y sellada, la firma de mesa no estaba escrita. Este acto la aporta, sin decidir nada que no viniera firmado verbatim en el propio encargo.

**Cobertura retroactiva.** `ADR-76` y `ADR-79` son del mismo día (13/ago/2026). Sin brecha — no hay historia previa a cubrir.

## §2 · Inciso (a) — D-M, alcance del sondeo

Firma de mesa, verbatim, del encargo: *"Sondeo Completo."* Se sella `SONDEO-COMPLETO`, mediante enmienda in situ sobre `ADR-79(i)` (`gobernanza:1199`, insertada entre "Versión y nombre de archivo" y "→ Vigente" del propio ADR-79 — mismo punto de inserción que usó `ADR-78` para su enmienda de freeze, no interleaved antes de "Lo que este ADR NO hace" porque esa línea, sin tocar, sigue narrando correctamente lo que `ADR-79` no hizo *en su momento*; la enmienda, fechada el mismo día pero después, es la que la supera — declarado así en la propia enmienda, no oculto). Texto original de `ADR-79(i)` no se toca: sigue leyéndose "REGISTRADO, NO SELLADO" arriba, con su propia auditoría intacta.

Va ratificada —no re-sellada, `ADR-79(i)` ya la sella— la nota de corrección del umbral: el PARO de "más de 20 candidatas" del encargo original de `TRIAGE-63` no tiene fundamento en ningún ADR; los únicos umbrales de 20 del corpus son de magnitud estadística (20pp, `gobernanza:682`/`ADR-60(a)` y `gobernanza:962`/`ADR-71(b)`), verificado de nuevo en este acto por lectura directa de esas dos líneas, no heredado sin comprobar. `TRIAGE-63 COMMIT 1` no se invalida ni se reabre — su clasificación de 27 filas `CANDIDATA-A-SONDEO` se conserva íntegra.

## §3 · Inciso (b) — estándar de éxito de la invarianza ENCUCI↔ENBIARE

Firma de mesa del encargo: *"Benchmark web"*, aclarada por mesa en el propio encargo como *"usa lo que el benchmark ya dijo"* — es decir, se adopta el estándar que `BENCHMARK-ENLACE` (#210) identificó como el único ejecutable hoy contra el par ENCUCI/ENBIARE. Se sella `ARGUMENTO DE VINCULACIÓN DECLARADO` — la segunda de las dos opciones de `ADR-76(d)(4)`, verbatim: *"anclas de diseño + invarianza parcial"* —, mediante enmienda in situ sobre `ADR-76(d)` (`gobernanza:1122`, mismo punto de inserción que (a): entre "Versión y nombre de archivo" y "→ Vigente").

Fundamento verificado por lectura directa de `forense/benchmark-enlace-invarianza-v1_0.md` en esta sesión, no heredado sin comprobar:

- **§D10 Pregunta 4** (línea 85): la secuencia configural→métrica→escalar, ítems ancla, invarianza parcial como estado intermedio reconocido (Byrne-Shavelson-Muthén 1989) es, confirmado, el vocabulario correcto y estándar del campo — el mismo que `ADR-67(a)` ya usaba sin citar de dónde salía.
- **§D10 Pregunta 5(a)** (línea 95, 97): la secuencia clásica **no exige muestra compartida** — lo que falta en el par ENCUCI/ENBIARE es otra cosa: solo dos ítems de contenido próximo (`AP5_1_1`↔`PB1_01`, `AP5_1_2`↔`PB1_02`) y ninguna muestra puente (ENCUCI 2020, ENBIARE 2021, sin panel compartido).
- **§D10 Pregunta 5(b)** (línea 99): el programa ya adoptó este estándar para el par estructuralmente idéntico ENCUCI/ENCIG — `forense/EDGE-CASES-y-literatura-reciente.md` §E5 (línea 45), verbatim: *"todo pooling entre instrumentos exige un argumento de vinculación declarado (anclas, alignment, linking robusto, o juicio experto rotulado como tal), citando que la exigibilidad de la invarianza clásica está en disputa"*. No se inventa criterio — se aplica el precedente que `ADR-76(d)` ya señalaba por nombre.
- **Literatura citada por el benchmark** (§D10 Pregunta 5(b), línea 99): Robitzsch & Lüdtke (2023), *SEM* 30(6):859-870 — la invarianza total, parcial o aproximada no es prerrequisito para comparaciones válidas entre grupos; Raykov (2024), mismo volumen — responde que tampoco es suficiente; Kusano, Napier & Jost (2025), *PSPB* — el estándar de invarianza es frecuentemente inaplicable a investigación comparativa entre culturas.

**Lo que este inciso no hace, verificado antes de escribir:** no adjudica el resultado del acto de vinculación — `ADR-67(a)` sigue fijando `ENCUCI` *baseline* / `ENBIARE` *challenger* sin poder de sustitución (`PROXY_PARCIAL`) hasta que `INVARIANZA ENCUCI↔ENBIARE` corra; punto 5 de la Propuesta 2 de `BENCHMARK-ENLACE` (línea 138), intacto. Este acto fija contra qué se juzga, no qué sale.

## §4 · Contadores que no se mueven, verificados uno por uno antes de escribir el ADR

```
$ grep -c 'clase: "MEDIDO·PARCIAL' milpa/procedencia.yaml   → 9   (condicionales 9 de 14, sin cambio — archivo no tocado por este acto)
```

`13 de 27` (Hito D, `gobernanza:1171`/`estado:95,123,197`) · `0 de 15` (coeficientes) · `1 de 2` (llaves, `registro-llaves-identificacion-v1_0.md`) · `4 de 144` — ninguno tocado: este acto firma dos decisiones de mesa, no ejecuta ninguna corrida.

## §5 · Cascada verificada antes de escribir

```
$ python3 - <<'EOF'
import re, collections
t=open("canon/gobernanza-v1_15.md",encoding="utf-8").read()
n=[int(x) for x in re.findall(r"^\*\*ADR-(\d+)", t, re.M)]
print("únicos:",len(set(n)),"max:",max(n),"dups:",sorted(k for k,c in collections.Counter(n).items() if c>1),"huecos:",sorted(set(range(1,max(n)+1))-set(n)))
EOF
únicos: 80 max: 80 dups: [] huecos: []
```

Contra `d55ae72` (`únicos:79 · max:79 · huecos:[]`), este acto suma `ADR-80`, contiguo. Sitios de cascada, derivados con `grep -rn "[0-9]\+ ADR" canon/ README.md` (sin resultado en `README.md`): `gobernanza-v1_15.md:2` (cabecera, 79→80) · `estado-programa-v1_10.md:27` (tabla, 79→80) · `estado-programa-v1_10.md:101` (§L0, historia completa, se añade la cláusula "a 80 después, con ADR-80..."). Los tres editados, ninguno más.

## §6 · Perímetro tocado

`canon/gobernanza-v1_15.md` (ADR-80, más las dos enmiendas in situ sobre `ADR-79(i)` y `ADR-76(d)`) · `canon/estado-programa-v1_10.md` (cascada) · `forense/notas/2026-08-13-firmas-2.md` (este archivo) · `forense/hallazgos.md` (una entrada) · `forense/encargos/2026-08-13-FIRMAS2-carril-caja.md` (el propio encargo, archivado per A.3). Nada más: no se tocó `milpa/`, `tests/`, `canon/modelo-decision-v4_0.md`, `data/`, ni `ADR-67(a)`.

## §7 · Cierre

`python3 tests/check.py --baseline`, corrido antes y después de escribir: **20 FAIL · 107 WARN — LÍNEA BASE: VERDE** en ambas corridas, sin cambio (este acto no toca ningún archivo que T15/T16/T19b vigilen salvo la cabecera y la cascada de conteo de ADR de `gobernanza`/`estado`, que suben en el sentido esperado, contiguo, sin hueco). Merge local de `origin/main` hacia esta rama antes de empujar, sin editor web de conflictos.
