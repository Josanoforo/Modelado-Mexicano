ENCARGO · ACTO REPARA-REGRESION-CITA-LINEA — invoca /acto SHA de redacción: 91ec588 (merge PR #476). Redacta ejecutor a pedido explícito del operador en esta sesión, 2/sep/2026, contra v2.12. Estado: LISTO PARA LANZAR. COMPUERTA: ninguna — declaración explícita. ENTORNO ASIGNADO: NUBE (`cloud_default`) — no abre microdato, solo `tools/emite_m.py` y sus datos comiteados. MODELO SUGERIDO: el vigente de la sesión (corrección puntual y acotada, con regresión total obligatoria; no baja de lo que ya corre).

ORIGEN DEL ENCARGO: no es pieza de dirección/Fable. `ACTO MAESTRA35-N2 · F-DD-RANGOS-Y-M-DIN` (PR #474) reportó, dentro de su regresión de partida (P0/P1), un hallazgo NO atribuible a ese acto: `regresion()` en `tools/emite_m.py` compara `M-TRA-M-01.json`/`M-TRA-M-02.json` regenerados contra el comiteado, campo por campo, y el campo `cita_p` embebe el número de línea de `milpa/tramite.yaml` (`"milpa/tramite.yaml:45 -- ..."`). Entre la emisión original (`ACTO MAESTRA33-E6`, 1/sep/2026) y hoy, `milpa/tramite.yaml` creció por las enmiendas de `MAESTRA34-N9`/`MAESTRA35-N1` (edición autorizada y sellada del motor, no un defecto), corriendo esa línea de 45 a 50 — el TEXTO citado no cambió, solo el número. `regresion()` no exime esta clase de campo (solo exime `fuente` por fecha/acto y trata `correcciones_aplicadas_por_referencia` como valor-solo) y por eso hoy devuelve `False` de forma incondicional aunque no haya deriva de lógica. El operador pidió, en esta sesión, confirmar el hallazgo y repararlo con su propia gobernanza (encargo + regresión propia), no colarlo dentro de `MAESTRA35-N2`.

═══ VERIFICACIÓN DE EXISTENCIA (A.8) — contestada contra 91ec588 ═══ (1) ESTRUCTURA. `tools/emite_m.py::regresion()` (líneas ~294-336 antes de este acto): itera `sorted(set(regenerado) | set(original))`, exime `_CAMPOS_EXENTOS_FECHA = {"fuente"}` (comparación NO se hace, se imprime y se ignora) y declara `_CAMPOS_SOLO_VALOR = {"correcciones_aplicadas_por_referencia"}` (comparación se hace pero un mismatch no marca `ok_total=False`, solo se imprime como "DIVERGE EN REDACCION"). Ningún tratamiento existe para campos que citan `archivo:línea -- texto`. Los dos campos con esa forma son `cita_p` (`emite_celda`, línea ~258) y `cita_ola_calibracion` (línea ~257), ambos producidos por `cita_p()`/`cita_ola_calibracion()` (líneas ~135-160), que arman el string como `f"{ruta}:{lineno} -- {texto}"`. (2) CONTENIDO, comando y salida cruda:

* `python3 -c "import tools.emite_m as em; print(em.regresion())"` → `False`, con exactamente un `FALLA` real: `[cita_p] FALLA: original='milpa/tramite.yaml:45 -- {conducta: paga_mordida, p: 0.62, clase: ASIGNADO}' regenerado='milpa/tramite.yaml:50 -- {conducta: paga_mordida, p: 0.62, clase: ASIGNADO}'` — idéntico en TRA-M-01 y TRA-M-02 (misma regla `tramite.mordida.discrecional`, mismo campo `paga_mordida` en `milpa/tramite.yaml`, corrido 5 líneas por ediciones posteriores). `cita_ola_calibracion` da `OK` en ambas celdas HOY porque esa cita para `tramite.mordida.discrecional` sale de `_OLA_CALIBRACION_FIJA` (constante fija en el código, línea `milpa/tramite.yaml:64` tecleada a mano, no derivada de `_primera_linea` en esta regla) — pero para cualquier regla SIN entrada en `_OLA_CALIBRACION_FIJA`, `cita_ola_calibracion()` sí deriva el número de línea igual que `cita_p()` (misma función `_primera_linea`) y es igual de frágil ante ediciones futuras del motor. EXISTE-NO-SATISFACE: el defecto es real y no está limitado a `cita_p`, aunque hoy solo `cita_p` lo manifiesta.
* Todos los demás campos de TRA-M-01/TRA-M-02 dan `OK` o están en las dos categorías ya exentas — verificado en la misma salida cruda de arriba (ningún otro `FALLA`). El fallo es puntual: un solo patrón de campo, una sola causa (corrimiento de línea por edición posterior y autorizada del motor). (3) COBERTURA RETROACTIVA. `regresion()` nace con `tools/emite_m.py` (`f613f1c`, `MAESTRA33-E6`, 1/sep/2026); el corrimiento de línea que lo dispara lo introdujeron `MAESTRA34-N9`/`MAESTRA35-N1` (2/sep/2026), posteriores a `regresion()` — el defecto es real, no ceguera de fecha.

DECISIÓN (documentada aquí, no delegada al ejecutor de un futuro acto): `cita_p` y `cita_ola_calibracion` se tratan como VALOR-CON-PREFIJO-TOLERANTE, no como `_CAMPOS_SOLO_VALOR` (ese trato es demasiado laxo: aceptaría cualquier redacción distinta, no solo el número de línea) ni como campo exento (demasiado laxo: dejaría de detectar si el TEXTO citado cambia). La comparación nueva separa `ruta:línea -- texto` con una sola expresión regular (`^(?P<ruta>.+?):(?P<linea>\d+) -- (?P<texto>.*)$`, con `re.S` porque `cita_ola_calibracion` puede traer texto multilínea si la regla lo trae) y exige que `ruta` y `texto` coincidan byte a byte; solo el número de línea puede diferir, y esa diferencia se imprime (no se oculta) como "OK (línea corrida de N a M, texto citado idéntico)". Si CUALQUIER otra parte difiere (ruta, texto, o el string no calza el patrón `ruta:línea -- texto` en alguno de los dos lados), es `FALLA` real igual que hoy — no se debilita la detección de deriva de lógica, solo se elimina el falso positivo de número de línea.

PIEZAS P1 · Implementa en `tools/emite_m.py` una función `_compara_cita_con_linea(original: str, regenerado: str) -> tuple[bool, str]` (o equivalente) con la regex y regla de arriba, y úsala dentro de `regresion()` para los campos `cita_p` y `cita_ola_calibracion` (constante nueva `_CAMPOS_CITA_LINEA = {"cita_p", "cita_ola_calibracion"}`), ANTES de la rama `if regenerado[campo] == original[campo]` genérica (mismo lugar donde hoy se ramifica por `_CAMPOS_EXENTOS_FECHA`/`_CAMPOS_SOLO_VALOR`). No toca `_CAMPOS_EXENTOS_FECHA` ni `_CAMPOS_SOLO_VALOR` ni ninguna otra función del módulo (`cita_p()`, `cita_ola_calibracion()`, `calcula_grado_DD()`, `emite_celda()`, `camina()` quedan intactas — el defecto vive solo en `regresion()`, la función de comparación, no en lo que se emite). P2 · Verificación obligatoria, salida cruda pegada: `python3 -c "import tools.emite_m as em; print(em.regresion())"` → debe imprimir `True` y, para `cita_p` en TRA-M-01/TRA-M-02, la línea "OK (línea corrida...)" en vez de `FALLA`. Prueba negativa obligatoria (para no aflojar la detección real): con un texto citado deliberadamente distinto (script de un solo uso, no comiteado, o `python3 -c` inline que llame a `_compara_cita_con_linea` directo con dos strings donde el TEXTO difiere) se confirma que sigue devolviendo `FALLA`/`False` — pega esa salida también. P3 · `python3 tests/check.py --baseline` en VERDE, salida cruda pegada.

PERÍMETRO Y CONCURRENCIA: `tools/emite_m.py` (una función nueva + 2 líneas de uso en `regresion()`) · este encargo (A.3) · `forense/hallazgos.md` (una línea) · `forense/firmas-pendientes.tsv` (fila de recibo, sin decisión de mesa pendiente — es corrección mecánica de un falso positivo, no cambia ningún M ya emitido) · `canon/gobernanza-v1_15.md` (ADR) · `canon/estado-programa-v1_10.md` (L0) · `canon/registro-rotulos.tsv`. NO toca `milpa/tramite.yaml`, ningún `corridas-M/*.json` existente, `marco-M-*`, `corridas-R/`, `corridas-L/`, `tools/arbitra.py`, `tools/medidor_*`. Si te encuentras escribiendo fuera de esta lista, PARA.

FP/ADR CANDIDATOS: primer FP libre al arrancar (máximo verificado 246 en 91ec588 → candidato 247; re-deriva al arrancar por si algo fusionó primero). ADR candidato por el comando de la casa (293 en 91ec588 → candidato 294; re-deriva al arrancar).

CONTADOR: campos de `regresion()` con falso positivo por corrimiento de línea: 1 (`cita_p`, medido) + 1 (`cita_ola_calibracion`, expuesto pero no medido hoy) → 0 tras esta pieza. `regresion()`: `False` → `True`. Ningún M existente se re-emite; ningún valor de ningún M cambia.

Lo que este acto NO hace: no re-emite ningún `M-<id>.json`; no cambia ningún valor de `p`, `grado_DD` ni ninguna otra clave de esquema; no toca el motor (`milpa/tramite.yaml`); no decide si el corrimiento de línea en sí merece otra reparación (p.ej. citar por contenido en vez de línea desde el origen) — esa es una reescritura mayor de `cita_p()`/`cita_ola_calibracion()`, fuera de esta pieza puntual.
