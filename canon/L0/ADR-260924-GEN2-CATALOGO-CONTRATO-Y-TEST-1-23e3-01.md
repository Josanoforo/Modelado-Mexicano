**ADR-260924-GEN2-CATALOGO-CONTRATO-Y-TEST-1-23e3-01** (24/sep/2026, NUBE): P1 — T-REPRO(c) fallaba en M08 porque el catálogo no materializa ninguna cifra (`NO-DECLARADO-EN-EL-REGISTRO` contra el RESULT), no por la unidad DELITO; la cita lateral del contrato nunca podía pasar. P2 — el catálogo lleva valor GEN2 y cita (RESULT, CALC, sello) en columnas propias; T-REPRO(c) verifica valor = RESULT y rechaza valor sin cita; `discrepancia_gen1` no bloquea; un positivo (M08) y dos negativos en `tests/test_corrida0.py`. P3 — FP `23e3-01` con el esquema de procedencia. P4 — celdas-D: 6 sin sucesora. Catálogo real no escrito; cero contadores movidos.
