# ACTO GEN2-TRAMITE-4 · nota de cierre

**Entorno:** NUBE `cloud_default`, Opus. `tools/entorno.py` → `acceso_corpus.montado=NO`, `archivos_examinados=0`, `raices=data_raw:NO`, `red=no-ejecutada` (opt-in; el acto no toca red). `python=3.11.15`, `yaml=6.0.1`, `numpy/pandas/scipy/pyreadstat=AUSENTE`.
**Base:** declarada por el encargo `e4f5f77`; **base real `0189562`** — re-derivada al abrir, 26 commits de diferencia.
**ADR:** `ADR-534`. **CONTADOR: cero** mediciones, cero microdato, cero red, cero código, cero corridas registradas.

---

## 1 · La compuerta, dos veces

`COMPUERTA: el merge de GEN2-CELDA-D-CAREO-1`.

**Primera apertura — NO se cumplía. El acto cerró con cero commits.** Verificación **por producto**, cinco vías contra `origin/main` real (no por `grep` de rótulo, que `ADR-277` midió como falso positivo):

| # | comando | salida | A.13 · universo examinado |
|---|---|---|---|
| i | `git ls-tree -r --name-only origin/main -- forense/encargos/ \| grep -ci careo` | `0` | **591** archivos |
| ii | `git show origin/main:canon/gobernanza-v1_15.md \| grep -cE '^\*\*ADR-[0-9]+ · \`ACTO GEN2-CELDA-D-CAREO-1'` | `0` | **8 198** líneas |
| iii | `git ls-tree -r --name-only origin/main -- forense/notas/insumos-direccion/` | `0` entradas | universo `forense/notas/`: **785** archivos |
| iv | `git merge-base --is-ancestor e0c9563 origin/main` | `ANCESTRO=NO` | — |
| v | `pull_request_read #827` | `state=open`, `merged=false` | — |

**El falso positivo de `ADR-277`, reproducido y descartado:** `grep -c "CAREO"` daba **26** aciertos en `gobernanza` y **14** en `firmas-pendientes.tsv`. Todos menciones: `ADR-532` nombra el careo como `Sucesor:`, y `FP-74/75/78/80` citan `CAREO-ADV-*`, que es **otro** careo. Por eso la prueba es (i)–(v).

**Segunda apertura — se cumple.** Las cinco salen positivas: encargo archivado (1), `ADR-533` de cabecera (1), `insumos-direccion/` con 3 archivos, `e0c9563 ANCESTRO=SI`, `PR #827 merged`. Y **la base declarada quedó vencida**: `e4f5f77` → `0189562`. Se re-derivó la `A.8` entera; **no se heredó**.

---

## 2 · A.8 re-derivada contra `0189562`

**Reproduce:** 0 ADR nuevos y 0 filas `NC`/`FP` en el rango del carril; tres `CALC-*` nuevos y **no registrados** (`corridas.tsv=0`, `resultados.tsv=0` para los tres); `grep -ic "bloque"` → 0; `grep -ic "baseline\|T22\|T34"` → 0; `resultados_pendientes` → 0 filas; nombres de los tres `CALC` idénticos a los que la cabecera declara.

**Tres correcciones, derivadas y no heredadas:**

1. **El lote es de 7 PR, no 8.** `git log --oneline --merges 9dffd64..e4f5f77` → 9 merges, de los cuales **7** son PR (`#824`, `#825`, `#826`, `#828`, `#829`, `#830`, `#831`) y 2 son merges internos de base. **`#827` — el careo — no está en el rango.** Y sólo **`#830`** corre en rama `codex/`; las otras seis son `acto/`.
2. **`grep -c "PARA-v2.14" forense/hallazgos.md` → 2**, no la incógnita que la cabecera dejaba («verificar; era 0 antes de #822»). `#822` añadió una y el careo la segunda.
3. **`forense/notas/insumos-direccion/` ya no está vacío**: 3 archivos desde el merge del careo. La cabecera lo daba por `NO-ENCONTRADA` contra `e4f5f77`, y contra esa base era cierto.

---

## 3 · P1 · El recibo del carril Codex

### 3.1 · Una línea por PR

| PR | rama | producto | qué dejó a mesa |
|---|---|---|---|
| `#824` | `acto/gen2-digesto-correctivo-1` | 4 archivos, +275/−3 · correctivo de `tools/digesto_tramite.py` + regresión | La integración del cron, explícitamente fuera de su perímetro. **El correctivo funciona** (§3.4). |
| `#825` | `acto/gen2-f6-factibilidad-preparacion-1` | 8 archivos, +1195 · `forense/prereg-duelo-v2/F6-factibilidad-preparacion-v1_0/` + fixtures + tests | **R01 `CANDIDATO-NO-ELEGIBLE`, R09 `CONDICIONADA`, cero llamadas autorizadas.** `NC-0283`, **CERRADA** por `D3`. |
| `#826` | `acto/gen2-encrige-descriptiva-1` | 12 archivos, +861 · `CALC-ENCRIGE-CORRUPCION-DESCRIPTIVA-0001` + `forense/analisis/` | El `CALC` sin registrar. `NC-0284`. |
| `#828` | `acto/gen2-sin-candidato-rutas-1` | 9 archivos, +948 · `forense/produccion/sin-candidato-rutas-1/` | Rutas propuestas que esperan decisión de mesa; sin fila propia porque el acto no abrió ninguna (`NC-0287`). |
| `#829` | `acto/gen2-relevo-candidatos-delta-1` | 14 archivos, +11640 · `forense/relevo-usos/candidatos-delta-1/` | **Cero adopciones y cero cambio del snapshot de `M`**, por declaración propia. Íd. `NC-0287`. |
| `#830` | `codex/gen2-envipe-res0028-u4-1` | 10 archivos, +737 · `CALC-ENVIPE-RES0028-U4-DERIVADO-0001` | El `CALC` sin registrar — **y éste sí verifica**. `NC-0286`. |
| `#831` | `acto/gen2-encig2023-agregado-condicional-1` | 10 archivos, +1135 · `CALC-ENCIG2023-AGREGADO-CONDICIONAL-0001` + `forense/analisis/` | El `CALC` sin registrar **y su encargo fuera del directorio canónico**. `NC-0285`. |

### 3.2 · Por qué el lote abrió cero filas: no escriben `## NO-CORRIDO`

`awk '/^## NO-CORRIDO/{f=1} /^## CONSUMIDO/{f=0} f'` sobre los **seis** encargos del lote archivados en `forense/encargos/` → **0 líneas en cada uno**. Ninguno tiene `## CONSUMIDO` tampoco. Traen `## Cierre suficiente` o `## Parada y criterio de avance` — que dicen **cuándo termina el acto**, no **qué quedó sin correr**. Es la razón mecánica de que 7 PR de un día entero den 0 `NC` y 0 `FP` (`NC-0287`).

Y el séptimo, `#831`, **archivó su encargo fuera de `forense/encargos/`**:

```
git diff --name-only e4f5f77^1 e4f5f77 -- forense/encargos/   →  0 archivos
```

El texto **sí existe**, en `forense/analisis/encig2023-agregado-condicional-1/00-encargo-verbatim.md`. No es «sin encargo» — es un encargo invisible para toda auditoría que barra `forense/encargos/`, incluido el contador `N encargo(s) sin CONSUMIDO` del digesto, que hoy lee **23** sin verlo. Es el único de los siete en esa condición (`NC-0285`). `#826`, que también escribe en `forense/analisis/`, archivó además en el directorio canónico.

### 3.3 · Los tres `CALC`: `verify`, y por qué ninguno se registra

Salida cruda (`python3 tools/corrida0.py verify <CALC>`), resumida al veredicto; la completa está en el log de la sesión:

```
CALC-ENVIPE-RES0028-U4-DERIVADO-0001
  [1/5 SELLO] COINCIDE · [2/5 SPEC.YAML] IDENTICO
  [3/5] 4/4 INPUT COINCIDE (todos del repo)
  [4/5 CONTEXTO] codigo=IDENTICO parametros=IDENTICO seed=IDENTICO dependencias=IDENTICO
  [5/5] 11/11 RESULT REPRODUCE, delta=0.0 en los flotantes
  VERIFY: REPRODUCE   (CONTEXTO=IDENTICO · RESULTADO=REPRODUCE)

CALC-ENCRIGE-CORRUPCION-DESCRIPTIVA-0001
  [1/5 SELLO] COINCIDE · [2/5 SPEC.YAML] IDENTICO
  [3/5] 2 INPUT RAIZ_NO_CONFIGURADA · 1 INPUT AUSENTE
  [5/5] NO-EJECUTABLE -- TypeError: expected str, bytes or os.PathLike object, not NoneType
  VERIFY: NO-VERIFICABLE   (CONTEXTO=NO-VERIFICABLE · RESULTADO=NO-EJECUTABLE)

CALC-ENCIG2023-AGREGADO-CONDICIONAL-0001
  [1/5 SELLO] COINCIDE · [2/5 SPEC.YAML] IDENTICO
  [3/5] 1 INPUT AUSENTE (encig23_base_datos_csv) · 3 INPUT COINCIDE
  [4/5 CONTEXTO] dependencias=DISTINTO
  [5/5] NO-EJECUTABLE -- ModuleNotFoundError: No module named 'numpy'
  VERIFY: NO-EJECUTABLE   (CONTEXTO=DISTINTO · RESULTADO=NO-EJECUTABLE)
```

**Los dos negativos no son un `NO-REPRODUCE` piadoso, y se acreditan con A.13.** En los tres el **sello COINCIDE** y la **`spec.yaml` es IDÉNTICA**: lo que falta es el microdato, no la integridad. `tools/entorno.py` declara `archivos_examinados=0` — el comando que produjo el negativo **no examinó ningún archivo de corpus**, y eso es parte del negativo, no una nota al pie.

**`numpy` no se instala, y es deliberado.** `ADR-533` sentó el precedente de «corregir el instrumento, no el número» instalando `jsonschema` **porque estaba declarado en `requirements.txt`**. `numpy` **no lo está** (`requirements.txt` declara `PyYAML`, `xlrd`, `markdown`, `jsonschema` y nada más): su ausencia en NUBE es el reparto nube/caja del programa, no un defecto de instrumento que este acto deba reparar.

**Y el que sí verifica tampoco se registra — éste es el hallazgo de la pieza.** La regla del encargo es **por CALC**: «si un CALC no verifica, **no se registra**». `corrida0.py registro --escribe` es **todo-o-nada** sobre la derivación completa:

- no tiene bandera de exclusión por CALC — las cuatro son `--verifica`, `--escribe`, `--lote`, `--fuentes`, y `--lote` gobierna **transiciones de veredicto de replay** sobre corridas ya publicadas, no exclusiones;
- el diff en seco añade **las tres corridas en una sola escritura**, las tres como `OFERTA`/`SELLADA`/`GEN2`;
- los tres están hoy en `forense/replay-evidencia.tsv` como **`SIN-FUENTE`**.

Registrar la que verifica exigiría registrar las dos que no. **Así que no se corrió `--escribe`**, y cada `CALC` tiene su fila con su motivo (`NC-0284`, `NC-0285`, `NC-0286`).

### 3.4 · El digesto: `#824` sí cerró la huella de PARO

`forense/rutinas.tsv:64` declaraba: «digesto NO escrito — `tools/digesto_tramite.py --fecha 2026-09-16` termina con **código de salida 2** (confirmado idéntico en dos corridas independientes)».

```
python3 tools/digesto_tramite.py --fecha 2026-09-16 --sin-suite   →  rc=0 · escrito: forense/digesto/DIGESTO-2026-09-16.md
python3 tools/digesto_tramite.py --fecha 2026-09-16                →  rc=0
```

**Las dos en verde.** El correctivo funciona. **El cron no lo entregó** — `DIGESTO-2026-09-16.md` no existía en el árbol y los digestos saltaban del 15 al… nada —, así que este acto **lo produce**, como su encargo autoriza («existe **o se produce**»).

---

## 4 · El CONTADOR del encargo se corrige, no se ajusta al pronóstico

El encargo predice «registra tres corridas Codex si verifican (**80 → 83 selladas**)». **No puede ocurrir**, y no por las dos que no verifican:

```
tools/corrida0.py:4282
  "N_corridas_selladas": sum(1 for f in gen2(corridas)
                             if f["origen"] == "OFERTA" and sellada(f)),
```

`corridas` es la **derivación en memoria**, armada de los directorios de disco — **no** `data/corrida0/corridas.tsv`. Los tres `CALC` están sellados en disco (`corrida0.py estado <CALC>` → `SELLADA` en los tres), así que **`N_corridas_selladas=80` ya los cuenta**.

| | antes | después |
|---|---|---|
| `status` → `N_corridas_selladas` | **80** | **80** |
| `corridas.tsv`, filas `OFERTA+SELLADA+GEN2` | **72** | **72** (no se escribió) |

Lo que `registro --escribe` movería es la **vista publicada** (72 → 75), no el contador. **El CONTADOR queda corregido a 80 → 80.**

**Y el hueco es más ancho que este lote:** 72 publicadas contra 80 derivadas. Seis `CALC` con `sello.json` están fuera del TSV — los tres de esta tanda más `CALC-AGG-marco-M-sorteado-v1_3-ola-v3`, `CALC-C0D-ALCANCE-CORPUS-CAPTURA-SUCESOR` y `CALC-WBES2023-CORRUPCION-DESCRIPTIVA-0001`. Se deja **medido, no resuelto** (`NC-0288`).

---

## 5 · P2 · Las cuatro firmas fuera del limbo

Cinco filas en `data/corrida0/decisiones.tsv`. Las dos firmas de aparato **se citan desde donde ya viven y no se reescriben**, como el encargo ordena.

### 5.1 · `FP-376` → **FIRMADA** (D1)

Columna `firma` escrita en **las tres filas** del crosswalk y **sólo en ésas** — `localidad`, `dominio_urbano_rural`, `formalidad`. Las otras 12 siguen como PROPUESTA A MESA. Integridad verificada: 16 filas × 18 columnas.

**La consecuencia, medida y sin adornar:** de los **12** ejes `ÁRBITRO→MODELO` del crosswalk, **uno solo** autoriza consumo por el marcador —`formalidad`—, y cubre **6 de las 74** celdas del árbitro (**8.1%**).

- **Inciso (1), `localidad`:** MAPEO-N-A-1 **con su reserva**. La reserva es `NC-0270`, que **sigue ABIERTA** — la evidencia de `tloc.csv == tam_loc.csv` es REGISTRADA (ACTO cal-conf fase B, 3/ago/2026), no abierta de primera mano aquí. La firma autoriza el grano de 2 celdas, no el de 4.
- **Inciso (2), `dominio_urbano_rural`:** NO-EQUIVALENTE, **sin mapeo**. Firmar aquí no es autorizar consumo: es **cerrar la pregunta con un no**.
- **Inciso (3), `formalidad`, y enmienda a `F-17`:** `F-17` ratificaba `formalidad = formalidad`; la verificación por definición que **la misma firma autorizaba** dio MAPEO-N-A-1. `D1` resuelve la contradicción **a favor de la verificación**. La enmienda queda con fecha en `decisiones.tsv` (objeto `crosswalk-ejes-v1:F-17`); **lo que `F-17` selló no se reescribe**.

`NC-0269` **CERRADA**.

### 5.2 · `FP-374` → **VENCIDA-EN-ALCANCE** (D3)

Por **enmienda fechada, no reescritura**: el `ejecutada_en` anterior se conserva íntegro y la enmienda se añade detrás. La cadena, re-derivada: **7 RETENIDAS → 2 ejecutables** (`#815`, `F5-panel-candidatos-v1_3.tsv`) **→ 0 elegibles** (`#825`, `tarjetas.yaml:125,128` — `R01` CANDIDATO-NO-ELEGIBLE, `R09` CONDICIONADA). La premisa que `FP-374` acotaba («con 7 retenidas, factibilidad acotada AHORA») **ya no se sostiene: el conjunto elegible es vacío.**

`FP-380` **nace FIRMADA** con el texto de `D3`. `NC-0161` y `NC-0162` reciben línea fechada: **ESPERA confirmada por mesa, no resuelta** — no se cierran hasta que mesa **ordene** la adquisición dirigida. **Cero llamadas de F6** en este acto.

### 5.3 y 5.4 · Adopción en bloque y WARN→estado

`FP-381` y `FP-382` **nacen FIRMADAS**, cada una con fila en `decisiones.tsv` y entrada `PARA-v2.14` en `hallazgos.md` (que pasan de 2 a **4**).

**La adopción en bloque llevaba desde el 15/sep gobernando actos sin fila** — el `ADR` de PINS (`#809`) ya la había usado. La tabla de propagación de la adenda (`:131`) daba su paso 1 en **NO HECHO**; este acto lo ejecuta y lo asienta con **enmienda fechada fuera del bloque verbatim**. El paso 2 **queda a medias a propósito**: su mitad de `hallazgos.md` se hace y su mitad de ADR **no**, porque **el bin 1 de la regla está refutado por el árbol** — `GEN2-RELEVO-USOS-1` adoptó **2 de 7** NO-MATERIAL pendientes («un sellado o firma vigente prohíbe la cita con independencia de la materialidad»), así que la regla necesita una **cuarta condición** y sellarla con ADR propio sería sellar el defecto. Es decisión de dirección (`NC-0290`).

**WARN→estado no toca código**, por instrucción explícita del encargo (`NC-0291`).

---

## 6 · P3 · Tres hallazgos de aparato

**(a) `N_resultados_pendientes` es constante por construcción.** Las filas DEMANDA nacen `PENDIENTE` (`tools/corrida0.py:243`), `status` las cuenta (`:4287`), y **no existe transición que las saque**: `grep -c "SATISFECH\|ATENDID\|CUBIERT" tools/corrida0.py` → **0**, sobre **1** archivo examinado (A.13). Hoy `N_resultados_pendientes=207` es **idéntico** a `N_resultados_activos=207`: no informa de una cola que avanza, informa de que la cola nunca se marca. La lectura real del avance está en `relevo-usos`. **No se retira ni se redefine aquí** — eso es código (`NC-0289`).

**(b) La fecha.** `FP-377`/`FP-378` y `ADR-531`/`ADR-533` dicen «17/sep/2026»; la real es **16/sep/2026**. Reproduce: los commits de cierre son del **2026-09-16** (`6875b9d`, `29f6f1b`); `ADR-531` lo dice 2 veces y `ADR-533` 1. **Nada se reescribe.** Y hay que decir lo que el árbol ya traía: **las dos `FP` habían declarado la discrepancia sin poder resolverla** — su `firmada_en` dice, verbatim, «se registra la fecha de la firma, **que no es del ejecutor editar**». Esa declaración es lo que permitió a dirección cerrarla dos días después. **Un campo que dice «no sé, y por qué» vale más que uno que adivina bien.**

**(c) `NC-0271` CERRADA · `NC-0278` ABIERTA.** La premisa de `NC-0271` se cumplió: D-θ v1.1 llegó al tercer intento y está archivado con su `sha256` verificado byte a byte (careo §10.2); y su segunda mitad —«D-θ v1.2 es sucesor de dirección»— también queda hecha por `P4`. `NC-0278` **sigue ABIERTA sin reintento** (este acto también corre en NUBE; repetir la sonda no añadiría nada), con la **receta manual de un minuto pegada en la propia fila** para que mesa no tenga que abrir la nota.

---

## 7 · P4 · D-θ v1.2

`forense/notas/insumos-direccion/2026-09-16-D-THETA-v1_2.md`, con cabecera de procedencia tipo 3. Cuerpo **verbatim verificado por comando** contra las líneas 26-31 del encargo archivado: **0 diferencias**.

**No viajó como adjunto**, así que **no hay `sha256` declarado por dirección contra el cual cotejar** — a diferencia de v1.1 (`8a6472a72631dfdc…`, verificado byte a byte por el careo). La cabecera lo dice en vez de fabricar una procedencia que no existe: la fuente verificable es el encargo archivado, `sha256 31ae29bebf8094c6…`. **La v1.1 se conserva como historia y no se toca.**

---

## 8 · Concurrencia

Tres PR abiertos al cerrar: **`#834`** (`acto/gen2-enco-dos-olas-reservadas-1`), **`#835`** (`acto/gen2-relevo-remesas-f3-1`), **`#836`** (`acto/gen2-encrige-carga-intensidad-1`). Los tres están sobre una base **anterior** al merge del careo: máximo `ADR` **532** (heredado, ninguno redacta todavía), `FP` **377**, `NC` **0274**.

Este acto toma `ADR-534`, `FP-380`…`FP-382`, `NC-0283`…`NC-0291`. **Si alguno de los tres fusiona primero, este acto renumera** — regla de la casa: renumera quien fusiona segundo.
