# Registro de recálculo
### `registro-recalculo` · **v1.0** · 13 de agosto de 2026 · ENCARGO ADR-PROVISIONALIDAD (nube) · abre la cola que ADR-72 instituye

> | | |
> |---|---|
> | **ARCHIVO** | `registro-recalculo-v1_0.md` |
> | **NOMBRE ESTABLE** | **`registro-recalculo`** — cítalo así, nunca por nombre de archivo |
> | **QUÉ ES** | Registro append-only, una fila por entrada de recálculo (ADR-72, `canon/gobernanza-v1_15.md`). Cola de todo veredicto, coeficiente, contador, reparto o cierre de búsqueda declarado `PROVISIONAL` por ADR-72, ordenada por palanca — no por número de ADR. |
> | **QUÉ NO ES** | No adjudica ningún veredicto por sí mismo — cada entrada se cierra en acto propio, uno por entrada (Método, ADR-72). No es el censo de estimabilidad (`forense/censo-estimabilidad-coeficientes-v1_0.md`), que es la entrada 1 de aquí, acto aparte. No es el registro de llaves de identificación (`forense/registro-llaves-identificacion-v1_0.md`), población de conteo distinta — ninguna fila de aquí mueve `llaves de identificación ejercidas`. |
> | **VERIFICAS ASÍ** | §1 trae las cinco entradas iniciales (1-5), en el mismo orden y el mismo texto con que ADR-72 §2.4 las declaró al sellarse, más la entrada `0` que su enmienda de COMMIT 2 (ADDENDA 5) añadió el mismo día. |

---

## 0 · Qué es una entrada

`ADR-72` declara `PROVISIONAL` todo veredicto, coeficiente, contador, reparto y cierre de búsqueda producido por este programa antes del 13/ago/2026, por haberse derivado contra un universo de corpus que no era el universo real y que ninguno declaró. Este archivo es la cola citable donde cada entrada provisional se re-examina, un acto a la vez.

**Regla de señal, verbatim de ADR-72:** "cada acto de recálculo produce un veredicto de los tres, o produce nada. Un acto que vuelve con 'sigue pendiente' no cierra su entrada."

**Los tres veredictos de cierre — los tres cierre válido:**

| veredicto | significa |
|---|---|
| `RECALCULADO — SIN CAMBIO` | se re-examinó contra el universo declarado completo y se sostiene, ahora con universo declarado |
| `RECALCULADO — CAMBIA` | se re-examinó y cambia; se propaga con su propio ADR |
| `RECALCULADO — INDECIDIBLE` | se re-examinó y no se pudo decidir; se dice qué haría falta |

**No es cierre** *"se revisó y parece bien"* sin universo escrito — eso es exactamente lo que produjo la situación que ADR-72 declara.

---

## 1 · Tabla — seis entradas iniciales: cinco verbatim de ADR-72 §2.4, más la entrada `0` de su enmienda

| # | entrada | clase | por qué va aquí | gate | estado |
|---|---|---|---|---|---|
| 0 | **Cotejo censo ↔ relaciones.tsv** — las 15 filas del censo de coeficientes contra `capa4_apertura_mapeo` de las necesidades correspondientes en `data/curacion-registro/relaciones.tsv` | A | mide el desacuerdo entre las dos tablas consolidadas del programa; hoy son **≥3 filas confirmadas** (12·`familismo_apoyo`, 13·`familismo_obligacion`, 14·`radio_confianza` — las tres `SIN-RUTA` en el censo, las tres `EXISTE-SATISFACE`/`CONFIRMADA` en `relaciones.tsv` para N12/N13/N14) y nadie lo vigila — `grep -rln "censo-estimabilidad" tests/ tools/` → 0 | ninguno | `RECALCULADO — SIN CAMBIO` — ACTO ADJ-4, 13/ago/2026. Universo declarado: las 15 filas de `forense/censo-estimabilidad-coeficientes-v1_1.md` §5, mapeadas 1:1 contra `necesidad_id` por `data/curacion-registro/necesidad-objeto-modelo.tsv` (verbatim: fila N del censo = `N{N}`, `G1.confianza_institucional`=`N1` … `G6.deferencia`=`N15`, sin ambigüedad, las 15 decidibles por lectura) × `capa4_apertura_mapeo`/`clasificacion_relacion` de `data/curacion-registro/relaciones.tsv` para cada `necesidad_id`, @ `19d885d`. Las tres filas ya confirmadas en desacuerdo por ADR-72 ADDENDA 5(a) (12/13/14 · `N12`/`N13`/`N14`, `SIN-RUTA` contra `EXISTE-SATISFACE`+`CONFIRMADA`) quedan resueltas en el mismo sentido por la Entrada 1 de este mismo acto: `censo-v1_1` ya las reclasifica `SIN-RUTA` → `RUTA-C`, consistente con `relaciones.tsv` — sin desacuerdo remanente ahí. **Cotejo nuevo de las 12 filas restantes (`N1`-`N11`, `N15`), 62 filas de `relaciones.tsv` revisadas:** ninguna necesidad trae la combinación `capa4_apertura_mapeo=EXISTE-SATISFACE` + `clasificacion_relacion=CONFIRMADA` que marcó las tres de G5 — solo `CANDIDATA`/`NEGATIVA` sobre `SIN_APERTURA_EXPLICITA`/`NO-ENCONTRADO`/`EXISTE-NO-SATISFACE`/`INDEXADO-NO-DESCARGADO`/`SATISFACE-UMBRAL-DOCUMENTAL`/`MAPEADO-NO-SATISFACE`, ningún `EXISTE-SATISFACE`. Tabla completa (comando y salida cruda por `necesidad_id`) en `forense/notas/2026-08-13-adj4-cierre-firmas.md` §3. **Sin desacuerdo nuevo — el censo se sostiene, ahora con las 15 filas cotejadas** (antes: 3 de 15, cotejo parcial declarado explícitamente por la enmienda de ADR-72). Ninguna fila resultó indecidible por lectura. |
| 1 | **Censo v1.1** — cruce de ENASEM (3 olas, 6 payloads) contra los 15 coeficientes, y universo de llaves declarado en las 9 `SIN-RUTA` | A | única entrada que puede mover un **denominador**; la nombró el propio censo v1.0 | ninguno | `RECALCULADO — CAMBIA` — PR #198. Universo declarado: `gobernanza:623` (ADR-57(c), las tres llaves, citadas íntegras) + 6 payloads ENASEM 2018/2021/2024 (`data/manifiesto.yaml`, hashes 6/6 `COINCIDE`, diccionarios abiertos completos — 6,471 variables) + 28 celdas de `data/abrir4-variables-2026-08-08.tsv` + `data/curacion-registro/relaciones.tsv` (filas N12/N13/N14), @ `dcc4f6a`. Tres filas del censo (12·`familismo_apoyo`, 13·`familismo_obligacion`, 14·`radio_confianza`) reclasifican `SIN-RUTA` → `RUTA-C` — ninguna a `RUTA-I` (regla de precedencia: co-observación sin llave de identificación no es identificación; verificada contra la definición propia de `RUTA-C` en `censo-estimabilidad-coeficientes-v1_0.md` §1, que ya cubre el caso sin necesidad de clase nueva). Cruce directo de ENASEM: negativo definitivo para `radio_confianza` (0/6,471 variables de confianza en las tres rondas) y para `familismo_obligacion` (único candidato es un ítem de personalidad general, no de deber familiar); precondición de panel real para `familismo_apoyo` (`G17`, tres olas, `UNHHIDNP` persistente) propuesta como candidata a diseño futuro, no ejercida. Reparto: `RUTA-A`=3 (sin cambio) · `RUTA-C`=2→5 · `RUTA-I`=1 (sin cambio, `CAL-G3` no tocada) · `SIN-RUTA`=9→6. Universo de llaves declarado en las 9 filas `SIN-RUTA` originales de v1.0: **9 de 9** (v1.0: 1 de 9). Contador `llaves de identificación ejercidas: 0 de 2` (`registro-llaves-identificacion-v1_0.md`) **no se mueve** — ninguna fila alcanzó `SELLADA_NO_EJERCIDA`, sin propuesta de movimiento. Detalle completo: `forense/censo-estimabilidad-coeficientes-v1_1.md`, `forense/notas/2026-08-13-censo-v1_1-abrir4-enasem.md`. *(Texto copiado verbatim de esa nota §12, per encargo. Propagado con `ADR-74(b)` (redactado como `ADR-73`, renumerado tras colisión con `PR #203`/ALIAS-P, que selló su propio `ADR-73` y fusionó primero) — regla de §1 vigente de este registro: `RECALCULADO — CAMBIA` se propaga con su propio ADR. Nota sobre el `0 de 2` citado arriba: seguía exacto cuando `censo-v1_1` lo escribió, el 13/ago antes de este acto; en el mismo ACTO ADJ-4, `R5.1-D2` mueve el contador a `1 de 2` por firma separada y ajena a esta entrada — `forense/registro-llaves-identificacion-v1_0.md` §5. No hay contradicción: son dos eventos concurrentes sobre el mismo contador, ninguno causado por el otro.)* |
| 2 | **ADR-52 A y ADR-54** — reapertura acotada de las dos búsquedas cerradas | C | gatean 4 `SIN-RUTA`; APERTURA-ISSP produce el reporte con el que se deciden | reporte de APERTURA-ISSP fusionado | `RECALCULADO — SIN CAMBIO` — ACTO E2 (cierre formal), 13/ago/2026, sobre el barrido completo de `PR #207` (`REAPERTURA-52A-54`, dos commits). Universo declarado: **104 payloads** con `archivo`+`sha256` en `data/manifiesto.yaml`, priorizados por `data/censo-explotacion-2026-08-13.tsv` (`REFERENCIADO-NO-ABIERTO`=4 + `SIN-DEMANDA` con diccionario=100) — no 5 —, @ `1e6e6a9` (SHA declarado en COMMIT 1, contra el que corrió el barrido, antes de fusionar `origin/main`). De esos 104: **94 diccionarios abiertos nuevos** en este barrido (94×2 necesidades = 188 celdas) + 10 ya cubiertos hoy por actos concurrentes (`APERTURA-ISSP` PR#200, `CENSO-v1.1` PR#198 — citados, no reabiertos: 20 celdas más). **208 celdas evaluadas (104 payloads × 2 necesidades `sens_estatus`/`aversion_riesgo`), 208/208 `NO-ENCONTRADO`** — verificado directo de `data/reapertura-52a-54-variables-2026-08-13.tsv` (`Counter` sobre la columna `clasificacion_a4`: `{'NO-ENCONTRADO': 208}`; cero vocabulario prohibido). 2,477 coincidencias crudas revisadas término por término (tras corregir un defecto de subcadena, `\bTERM` con límite de palabra); ninguna sobrevivió revisión de contexto. Los seis casos examinados con mayor detalle por ser los más plausibles a priori — todos descartados con argumento propio, no por conteo: Módulo de Movilidad Social/INEGI (mide movilidad objetiva por categoría ocupacional, no sensibilidad a estatus), WVS7 México, `za5900_questionnaire_development_report` (informe de diseño, no instrumento con reactivos), codebook de CSES5 (metadato de codificación), LAPOP AmericasBarometer (79 de 81 coincidencias son percepción de inseguridad ante el crimen — constructo de victimización, no riesgo financiero), MOCIBA×9 ediciones (experiencia de victimización atribuida al agresor, no disposición propia) — detalle: `forense/notas/2026-08-13-reapertura-52a-54-commit2-barrido.md` §5. Gate cumplido: `APERTURA-ISSP` (PR#200) fusionado — `data/apertura-issp-variables-2026-08-13.tsv` (14 celdas, verificado) + `forense/notas/2026-08-13-apertura-issp.md`. Sostiene el cierre original de ADR-52 A/ADR-54 ("búsqueda cerrada, límite de régimen") sobre un universo ~20× mayor que el régimen original (5 instrumentos, 0.52% de 958 programas). Por §2 de este mismo registro, `SIN CAMBIO` no sella ADR nuevo — precedente Entradas 0 y 4. Desbloquea la Entrada 3 (gate "entradas 1 y 2", ambas cerradas). Detalle completo y dos correcciones de cifras frente al encargo de cierre: `forense/notas/2026-08-13-e2-cierre.md`. |
| 3 | **Los 7 veredictos `D` del Hito D** — uno por acto, ficha B-bis propia | B | archivo por hueco de diseño: hay que preguntar si el hueco era de instrumento y el instrumento estaba en disco | entradas 1 y 2 | `ABIERTA` |
| 4 | **Censo de explotación** — apertura por payload sobre los 550 | A | instituye el contador del ADR y evita la repetición | ninguno | `RECALCULADO — SIN CAMBIO` — PR #201. Universo declarado: 550 payloads (`archivo`+`sha256` del manifiesto) × `data/abrir4-variables-2026-08-08.tsv`+`data/verif3-variables-2026-08-08.tsv` × `relaciones.tsv`, @ `dcc4f6a`..`5f90757` (idéntico en las cuatro tablas, verificado). `EXPLOTADO`+`ABIERTO-SIN-HALLAZGO` = 8/550 = 1.45%, confirma exacto el valor que este mismo registro cita en §2. Nominal completo (estado por payload, 4 estados) en `data/censo-explotacion-2026-08-13.tsv`; corrige además, como hallazgo no pedido por §2, la cifra de `SIN-DEMANDA` que el encargo de este acto estimaba en ≈321/550(58%) a **538/550 (97.82%)** medido — detalle en `forense/notas/2026-08-13-censo-explotacion.md`. |
| 5 | **ADR-50 / ADR-51 / ADR-57(c)** | D | dependen del reparto de la entrada 1 | entrada 1 | `ABIERTA` |

**Cada entrada cierra con:** el acto que la cerró (PR), el veredicto de los tres, y **el universo declarado en la misma línea.**

---

## 2 · Contador instituido por ADR-72

`payloads con apertura registrada / payloads en manifiesto` — al sellar ADR-72, **8 de 550 = 1.45%**.

Derivación (verbatim de ADR-72): cruzar los `id_manifiesto` de los TSV de apertura a nivel variable (`data/abrir4-variables-2026-08-08.tsv` + `data/verif3-variables-2026-08-08.tsv`) contra las entradas con `archivo`+`sha256` del manifiesto.

Este es un contador de medición sobre México y **ADR-72 no lo mueve — lo instituye.** Su primer movimiento, si lo hay, lo produce la entrada 4 de este registro.

---

## 3 · Segundo contador, instituido por la enmienda de COMMIT 2 (ADDENDA 5)

`filas de consolidación en desacuerdo entre sí` — el censo de coeficientes (`forense/censo-estimabilidad-coeficientes-v1_0.md`) y el registro de demanda (`data/curacion-registro/relaciones.tsv`) no se cotejan entre sí por ningún mecanismo (`grep -rln "censo-estimabilidad" tests/ tools/` → 0), y hoy se contradicen en al menos **3 filas verificadas**: las filas 12/13/14 del censo (`familismo_apoyo`/`familismo_obligacion`/`radio_confianza`) declaran `SIN-RUTA`, "Ninguna llave aplica", mientras `relaciones.tsv` trae `capa4_apertura_mapeo=EXISTE-SATISFACE` + `clasificacion_relacion=CONFIRMADA` para las mismas necesidades (`N12`/`N13`/`N14` — `REL-4a609c6633a4bafac14a6930` · `REL-fe202a3fa76f0516a6e27f8b` · `REL-5741e12ce3e0a0e076ee48fc`).

**Cotejo parcial, declarado, no las 15.** El censo no trae un campo `necesidad_id` cruzable por máquina contra `relaciones.tsv` — identificar la necesidad conceptual de cada una de sus 15 filas exige la misma lectura semántica que resolvió estas tres. Ese cotejo completo es la entrada `0` de §1, que este mismo acto abre y no cierra — cerrarla aquí habría abierto el censo en bloque, lo que el Método de ADR-72 prohíbe ("un acto por entrada").

Justificación bajo el impuesto de v2.3 (misma que el contador de §2): el defecto ya ocurrió — tres coeficientes clasificados sin ruta mientras su reactivo estaba `EXISTE-SATISFACE` en la otra tabla consolidada del programa, sin que nada lo señalara — y es derivable con un cruce de dos archivos, no con un módulo de auditoría nuevo.

---

*Append-only. Nueva entrada = nueva fila, nunca edición de una fila existente salvo para llenar `estado` al cerrarla. Este archivo no adjudica — registra.*
