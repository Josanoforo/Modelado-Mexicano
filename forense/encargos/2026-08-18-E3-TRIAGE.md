# ENCARGO · E3-TRIAGE — lanzar la Entrada 3 de registro-recalculo (los 7 veredictos D del Hito D)

**SHA de redacción:** `93a4dd9` (`origin/main`, tras `ADR-100`/`ADR-101`, ACTO MESA-18AGO, 18/ago/2026)
**Entorno asignado:** **NUBE** (`cloud_default`, repo-only). No toca microdato — el Hito D corre contra el motor/corpus ya en el repo.
**Estado:** **CONSUMIDO** — `ACTO E3-TRIAGE`, 18/ago/2026, rama `claude/e3-triage-launcher-3c1oz7`, entorno NUBE (repo-only), sobre `origin/main = f3d3f95`. Entrada 3 cerrada `RECALCULADO — SIN CAMBIO` con universo declarado; 7 fichas B-bis de re-triage (`T-1`=2 · `T-2`=4 · `T-3`=2 · `T-4`=0, 0 reaperturas); `FP-14` → `FIRMADA`. **Tarea 1 contestada:** el índice E2 crudo no está en el repo (solo en `.barrido2/`, gitignorado) — el acto corrió contra el ledger durable de 672 filas, que decide a la granularidad que la pregunta de la entrada pide; **no requirió Ubuntu**. Contadores movidos: 0. ADR sellados: 0. Detalle: `forense/notas/2026-08-18-entrada3-triage-hitoD.md`.

**Estado original al redactarse:** VIVO — **DISPARADOR-A** de `FP-26` (`ADR-101(h)`): no consume semántica del barrido, puede arrancar ya. Gate propio (entradas 1 y 2 de `registro-recalculo`, ambas cerradas) ya está cumplido desde antes de este acto.
**Origen:** `FP-14`, firmada-condicional `ADR-91` (`PR #246`) — *"E3-TRIAGE corre automáticamente al cierre de BARRIDO-2, contra su índice E2."* `BARRIDO-2` cerró materialmente (`GATE-DURABLE-V7`, `PR #255`).

## Verificación de existencia (A.8), contestada por quien escribe

```
gate propio (entradas 1 y 2):       forense/registro-recalculo-v1_0.md §1 -- Entrada 1
                                     RECALCULADO-CAMBIA (PR #198), Entrada 2 RECALCULADO-SIN
                                     CAMBIO (ACTO E2) -- AMBAS CERRADAS                        SATISFECHO
Entrada 3, fila hoy:                forense/registro-recalculo-v1_0.md §1, fila 3: "Los 7
                                     veredictos D del Hito D -- uno por acto, ficha B-bis
                                     propia", estado ABIERTA                                   EXISTE
índice E2 de BARRIDO-2:             .barrido2/ (gitignorado, material sin curar) -- verificar
                                     si el encargo necesita el índice E2 completo o basta el
                                     ledger durable de data/curacion-universo/                 A VERIFICAR
                                     AL LANZAR (puede requerir Ubuntu si el índice completo
                                     solo vive en .barrido2/, fuera de git)
```

## Perímetro

ESCRIBE: `forense/registro-recalculo-v1_0.md` (Entrada 3, cerrada con su veredicto y universo declarado) · ficha B-bis propia por cada uno de los 7 (formato a derivar del propio Hito D) · nota del acto. NO ESCRIBE: `canon/`, `data/curacion-registro/**`, `milpa/`.

## Tarea

1. Verificar si el "índice E2" que el gate de `FP-14` cita vive en el repo (`data/curacion-universo/`) o solo en `.barrido2/` (gitignorado, requeriría Ubuntu con acceso al staging material).
2. Identificar los 7 veredictos `D` del Hito D pendientes de ficha B-bis (`canon/estado-programa-v1_10.md` §L5 los enumera por código `R#.#`).
3. Para cada uno: preguntar si el hueco era de instrumento y si el instrumento estaba en disco (la pregunta que la Entrada 3 nombra como su propio criterio), y escribir la ficha B-bis.
4. Cerrar la Entrada 3 con veredicto de los tres (`RECALCULADO — SIN CAMBIO/CAMBIA/INDECIDIBLE`) y universo declarado, mismo formato que las entradas 0/1/2/4/6 ya cerradas.

## Cierre

Entrada 3 cerrada con universo declarado · 7 fichas B-bis (o las que correspondan tras el triage) · `firmas-pendientes.tsv`: `FP-14` → `FIRMADA`, `ejecutada_en` este PR · `tests/check.py --baseline` VERDE · línea en `hallazgos.md` · encargo `CONSUMIDO`.
