ENCARGO FINAL · AUTOMATIZA-1 · E1 PERÍMETRO FÍSICO · E2 ESTADO COMÚN · E3 CIERRE MECÁNICO
dirección (Fable) · 6/sep/2026 · base `main = 76dcdbbc923f330b5ba75bad13c8face81468ae9` (#563) · ADR máx 355 · FP máx 325 · ramas vivas al redactar: `acto/maestra38-lote-ensanut`, `maestra38-l2-mps2012` (caja) · tres actos secuenciales, un PR cada uno, en NUBE, MODELO SUGERIDO Sonnet «ultracode» (puede subir a Opus, nunca bajar) · texto de mesa/dirección adoptado verbatim; firma de mesa = merge de cada PR
CABECERA DE LA CASA (aplica a los tres actos; se archiva con cada uno)

* Invoca `/acto` en cada elemento: ARRANQUE (5 puntos + A.2 tres partes — en nube `ls data/raw/` da ausente y se declara «no aplica: este acto no toca corpus ni red»), COMPUERTA verificada contra `origin/main`, guard de rama (`git ls-remote --heads origin | grep -i automatiza-1-e<n>` → coincidencia = PARA), 0-bis A.3: este documento entero archivado verbatim en `forense/encargos/2026-09-0X-AUTOMATIZA-1-E<n>.md` y empujado al primer minuto.
* Rótulos ADR/FP no vienen preasignados: se derivan en el 0-bis, contiguos, contra `main` + ramas vivas; renumera quien fusiona segundo. E3 los deriva con su propio tool en dry-run y lo pega.
* Compuertas: E1 ninguna · E2 = E1 fusionado · E3 = E2 fusionado. Verificar por producto (`git log origin/main --oneline | grep AUTOMATIZA-1-E<n-1>`), no por promesa.
* Concurrencia declarada: en caja corren `MAESTRA38-L2` y `MAESTRA38-LOTE-ENSANUT`; escriben `data/l2-*`, `data/l16-*`, `data/l17-*`, `milpa/tramite-ola5-propuesta-v0.yaml`, sus notas y la cascada. Ninguno toca `tests/manifiesto.py`, `tests/corpus.py`, `tests/test_manifiesto_alcance.py`, `tools/digesto_tramite.py`, `tools/tablero_programa.py`, `tests/check.py`, `.claude/commands/acto.md`. Quien fusiona segundo re-corre la cascada.
* Perímetro por elemento = la lista de archivos de su sección + notas + A.3 + cascada. Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.
* Cascada D-10 completa al cierre: ADR por comando de la casa, cabecera de gobernanza, recifrado L0, `registro-rotulos`, T25, `python3 tests/check.py --baseline` VERDE o PARO-reporta, `## CONSUMIDO` con el PR. (En E3, el primer cierre real se hace con el tool, dry-run y aplica pegados.)
* Reporte de cierre por acto (§ SOLICITUD AL EJECUTOR, abajo): qué cambió · qué quedó automatizado · qué sigue humano · qué medición demuestra que pagó. CONTADOR: medición de modelo cero en los tres.

TRES CORRECCIONES DE DIRECCIÓN CONTRA EL ÁRBOL (A.8, comando y salida; mandan sobre el texto de abajo donde choquen)
C1 · E1.B parte de una premisa falsa y se conserva igual. «Entradas históricas `raiz: downloads` = K»: hoy K = 0 — `grep -oE "^ raiz: [a-z_]+" data/manifiesto.yaml | sort | uniq -c` → `277 data_raw · 333 descargas_mx`, ninguna `downloads`. El guard en `cmd_verifica` (`tests/manifiesto.py:512`, antes de `resolver_raiz` en `:551`) se implementa como garantía por construcción; su test usa un fixture con una entrada `raiz: downloads`, y la nota de cierre dice que hoy no protege ninguna entrada real del manifiesto. C2 · E1 rompe un test existente que hay que reescribir, no dejar en rojo ni borrar en silencio. `tests/test_manifiesto_alcance.py` ejercita `--escanea downloads` (fixture `downloads_personal`, línea 79) para proteger la corrección MAP-1b (filtro por extensión sobre raíces no curadas). Con la frontera, `--escanea downloads` se rechaza antes de filtrar nada. El test se reescribe para afirmar el rechazo (`RAIZ_NO_ESCANEABLE: downloads`, código ≠ 0, cero `os.walk`), y su docstring registra que MAP-1b deja de tener superficie de ataque porque la raíz ya no se recorre. Entra al perímetro de E1 y a su COMMIT 1. C3 · Líneas verificadas hoy, para no buscarlas de nuevo (re-verifica en el 0-bis; si se movieron, corrige y sigue): `tests/manifiesto.py` `RAIZ_INTEGRADA` :279 · `raices_configuradas` :282 · `resolver_raiz` :296 · `cmd_verifica` :512 (I/O físico desde :551) · `RAICES_QUE_EXIGEN_GRUPO={"downloads"}` :789 (compuerta blanda actual: exige `--grupo`; queda inalcanzable desde `--escanea`, no se borra, docstring de una línea) · `os.walk` :843/:888 · `cmd_escanea` :896. `tests/corpus.py` `c1_huerfanos` :166 (walk de raíces :221-247) · `c3_entradas_sin_archivo` :289 (`resolver_raiz` :301, `exists` :305). `tools/digesto_tramite.py` `lee_tablero` :247 · `RE_ESTADO_ABIERTA` :263 · ramas :454-462. `tools/tablero_programa.py` ramas :74 (`git branch -r`, local) · ADR/FP :144-147 · `== "ABIERTA"` :151. `tests/check.py` `t15_adr_count` :617 (regex :622) · `t22_firmas` :1734 (`!= "ABIERTA"` :1744; `not in ("ABIERTA","FIRMADA")` :1784). `.claude/commands/acto.md` §4 CIERRE :158-215. `canon/estado-programa-v1_12.md` L0 :111 — una sola línea de ~100 000 caracteres: ancla única o PARO, nunca reescritura. `canon/gobernanza-v1_15.md` línea 2 `**N ADR**`. `tools/`: 0 de 70 archivos con lógica de cierre — E3 nace de cero.
Lo que queda superado de cualquier texto anterior de dirección sobre AUTOMATIZA-1 (abortos por transformación en E3, importar `_T25_*`, escribir `registro-rotulos`, `startswith("FIRMADA")` en T22): la propuesta de abajo gobierna.
PROPUESTA FINAL · AUTOMATIZA-1 (texto adoptado)
Objetivo general: automatizar tres operaciones repetitivas y deterministas que hoy todavía generan trabajo manual, divergencias o riesgo, sin crear un framework, orquestador, superagente ni nueva capa de gobernanza. Principio rector: automatizar operaciones mecánicas; mantener humanas las decisiones semánticas, epistemológicas y de mesa. Cada elemento produce un PR pequeño y fusionable. Orden: `E1 → E2 → E3`. E1 y E2 son independientes pero se ejecutan en secuencia para mantener el trabajo pequeño; E3 depende materialmente de E2.
REGLAS COMUNES

1. Trabajar desde `origin/main` vigente al iniciar cada acto. 2. Worktree/rama propia. 3. Re-derivar SHA, ADR/FP y ramas existentes antes de editar. 4. Un PR por elemento. 5. No tocar modelo, datos sustantivos, tiers, clasificaciones ni decisiones semánticas. 6. Reusar código existente antes de crear código nuevo. 7. Máximo un módulo nuevo por elemento, salvo prueba dirigida. 8. Ninguna clase nueva salvo imposibilidad material demostrada. 9. Ningún framework. 10. Tests sólo para proteger el defecto observado o la nueva frontera material. 11. `python3 tests/check.py --baseline` cierra sin regresión material nueva. 12. Si resolver un elemento exige ampliar significativamente el perímetro previsto, PARO y reporta la vía mínima alternativa. No convertir anomalías incidentales en trabajo adicional.

ELEMENTO 1 · ACTO AUTOMATIZA-1-E1 · PERÍMETRO FÍSICO DE RAÍCES
Objetivo. Hacer cumplir por código la frontera física vigente: raíces autorizadas para recorrido físico `data_raw` y `descargas_mx`; `downloads` explícitamente fuera. `downloads` puede seguir existiendo como nombre histórico, en entradas antiguas del manifiesto (hoy: ninguna — C1) y en `data/raices.local.yaml`; pero ningún automatismo del corpus debe caminar, listar, hacer `exists`, `stat`, hash o abrir archivos físicamente bajo `downloads`. La frontera aplica a todo I/O físico general de corpus, no sólo a `--escanea`. Por qué importa. `downloads` contenía información personal fuera del proyecto. Regla operativa: una descarga llega al navegador; si pertenece al proyecto, el usuario la mueve a `Descargas MX`; a partir de ahí sólo `data_raw` y `descargas_mx` forman parte del corpus. La seguridad no debe depender de recordar modificar un YAML local: queda garantizada por construcción. Se conserva. `RAIZ_INTEGRADA = "data_raw"`, `raices_configuradas()`, `resolver_raiz()`, referencias históricas del manifiesto, `data/raices.local.yaml`, verificación física normal de `data_raw` y `descargas_mx`. No borrar ni migrar automáticamente entradas `raiz: downloads`. No purgar historia. `RAICES_QUE_EXIGEN_GRUPO`/`--grupo` no se eliminan: quedan inalcanzables desde `--escanea` y se documenta en una línea. Cambio mínimo. En `tests/manifiesto.py`, junto a `RAIZ_INTEGRADA`: `RAICES_ESCANEABLES = frozenset({RAIZ_INTEGRADA, "descargas_mx"})` y `def raiz_escaneable(nombre: str) -> bool: return nombre in RAICES_ESCANEABLES`. El nombre exacto puede variar. No crear sistema de permisos. Cuatro vías físicas a proteger.

* E1.A `cmd_escanea`: antes de resolver físicamente la raíz, comprobar directorio, `os.walk`, hash o `stat`: si la raíz no está autorizada → `RAIZ_NO_ESCANEABLE: downloads` y salida con código no cero. La frontera se comprueba antes de informar si la raíz está configurada: `downloads` es no escaneable por política, no simplemente «no configurada».
* E1.B `cmd_verifica`: para cualquier entrada cuyo `raiz == "downloads"`: no resolver físicamente, no `exists`, no `stat`, no SHA, no imprimir nombre físico completo; clasificarla `FUERA_DE_PERIMETRO` (`[id] [downloads]: FUERA_DE_PERIMETRO — raíz histórica no inspeccionable`). El resumen puede contar cuántas entradas del manifiesto pertenecen a esa raíz (sólo lee YAML). (C1: hoy 0 entradas; test con fixture.)
* E1.C `tests/corpus.py::c1_huerfanos`: al construir `raices`, no recorrer ninguna que no satisfaga `raiz_escaneable()`; para `downloads` → `RAIZ_NO_ESCANEABLE: downloads — no recorrida`; sin `os.walk`, nombres, hashes ni conteo físico; no incluirla en `Total en disco`.
* E1.D `tests/corpus.py::c3_entradas_sin_archivo`: para una entrada de raíz no escaneable, omitir la comprobación física, no contabilizarla como AUSENTE, no tocar la ruta; opcionalmente `downloads: fuera de perímetro físico` sin nombres de archivo. Qué sí puede seguir leyéndose. Desde `data/manifiesto.yaml`: entradas por raíz (`data_raw = N · descargas_mx = M · downloads = K`). Es estado lógico del repo. `referencia histórica ≠ autorización para I/O físico`. Prueba mínima — `tests/test_perimetro_raices.py` (ejecutable con `python3 tests/test_perimetro_raices.py`, patrón de `tests/test_t_cron.py`): (1) `raiz_escaneable("data_raw")` True · (2) `("descargas_mx")` True · (3) `("downloads")` False · (4) `--escanea downloads` → código de rechazo, imprime `RAIZ_NO_ESCANEABLE`, cero llamadas a `os.walk` (monkeypatch contando) · (5) `--verifica` con una entrada de fixture `raiz: downloads` → `FUERA_DE_PERIMETRO`, cero `exists/stat/hash` sobre esa ruta · (6) `c1_huerfanos` con `downloads` configurada no camina esa carpeta · (7) C3 no llama `exists()` sobre ella. Sin pruebas de permisos, usuarios o ACL. Más C2: `tests/test_manifiesto_alcance.py` reescrito para afirmar el rechazo. Verificación. `python3 tests/test_perimetro_raices.py` · `python3 tests/test_manifiesto_alcance.py` · `python3 tests/test_manifiesto_clon.py` · `python3 tests/check.py --baseline`. Después del merge, en CAJA (un minuto, sin acto): `python3 tests/manifiesto.py --escanea downloads` → `RAIZ_NO_ESCANEABLE: downloads`; `--escanea descargas_mx` → censo normal; `python3 tests/corpus.py` → `downloads` no recorrida. Commits. COMMIT 1: frontera central + `cmd_escanea` + `cmd_verifica` + test nuevo + `test_manifiesto_alcance` reescrito. COMMIT 2: C1/C3 + documentación mínima (`data/INFRAESTRUCTURA-v1_0.md`, una línea en la fila de raíces) + cascada. Un PR. PARO si cerrar `downloads` exige modificar semántica del manifiesto; si algún consumidor material necesita abrir `downloads` para funcionar (reportar el consumidor concreto); si la frontera sólo puede implementarse con una arquitectura de permisos. No relajarlo silenciosamente. Fuera de perímetro. Borrar `downloads` del manifiesto; migrar entradas; inspeccionar contenido de `downloads`; ACLs; permisos por usuario; auditar historia de filtraciones; modificar `data/manifiesto.yaml`. Criterio de éxito. «`downloads` puede seguir existiendo como referencia histórica, pero ninguna herramienta general del corpus puede hacer I/O físico sobre esa raíz. Sólo `data_raw` y `descargas_mx` son recorribles.» CONTADOR: caminos generales de I/O físico sobre `downloads` 4 → 0 · tests +1 (y 1 reescrito).

ELEMENTO 2 · ACTO AUTOMATIZA-1-E2 · ESTADO COMÚN — COMPUERTA: E1 fusionado
Objetivo. Eliminar implementaciones divergentes de conceptos operativos puramente mecánicos repetidos entre digesto, tablero y cierre. Defecto observado: `ABIERTA -- glosa` es reconocida por el digesto, no por `tablero_programa.py` ni por partes de T22. Centralizar sólo conceptos con duplicación real. Módulo nuevo: `tools/estado_comun.py`. Sin clases, sin estado global; funciones con argumentos explícitos. (Si `tools/` necesita importar desde `tests/` o viceversa: dos líneas de `sys.path` como ya hacen `corpus.py`/`digesto`, no paquete nuevo.) Primitivas autorizadas. 3.1 `es_abierta(estado)` — mover sin reinterpretar la regla del digesto `^ABIERTA(\s|$)`: `ABIERTA`→True · `ABIERTA -- pendiente`→True · `ABIERTA-RECIBO`→False · `FIRMADA`→False · `FIRMADA-POR-MERGE`→False · `""`→False. No `startswith("ABIERTA")`. 3.2 `lee_tablero` — extraer la lectura TSV cruda del digesto; conserva tabulador literal, comillas literales, sin semántica CSV que cambie contenido. 3.3 `ramas_remotas_presentes()` — estrategia del digesto: `git ls-remote --heads origin`; si no responde, `git for-each-ref refs/remotes/origin`; devuelve también la fuente. Nombre `presentes`, no `vivas`: una rama en origin no implica PR abierto ni trabajo sin fusionar; la herramienta informa lo que sabe. 3.4 `adr_max()` — máximo ADR actual, para tablero, E3 y consumidores productivos. 3.5 `fp_max()` — primera columna de `firmas-pendientes.tsv`; E3 la necesita y no debe crear una segunda implementación. T15 mantiene independencia. `tests/check.py::t15_adr_count()` NO usa `adr_max()`: es el vigía de duplicados, huecos y secuencia; conserva derivación propia. Duplicación deliberada de verificación, no deuda. Migración de `digesto_tramite.py`: sólo las primitivas seleccionadas; salida existente conservada donde era correcta; no reescribir otras secciones. Migración de `tablero_programa.py`: (A) `git branch -r` → `ramas_remotas_presentes()`; la clave de salida puede conservar nombre, la nota debe ser exacta sobre lo que representa. (B) `adr_max()`. (C) `fp_max()`. (D) `estado == "ABIERTA"` → `es_abierta(estado)`. Nada más; el tablero no se reescribe. El comando de derivación puede quedar en el campo `cmd` como documentación; el valor viene de la función. Migración de T22. T22(a): `if es_abierta(estado)` — corrige el defecto. T22(c): conservar exactamente `estado == "FIRMADA"`; no `startswith` (el repo tiene `FIRMADA-POR-MERGE` y otros compuestos; no hay defecto que justifique colapsarlos). T22(b): `es_abierta(estado) or estado == "FIRMADA"`; sin normalizar otras variantes. Baseline. La corrección puede producir WARN adicionales en T22: efecto esperado del vigía corrigiéndose. Antes de recifrar: listar exactamente qué filas nuevas aparecen; demostrar que todas satisfacen `es_abierta()`; comprobar que no apareció otra causa. Sólo entonces actualizar `tests/baseline.json` si hace falta. Prueba mínima — `tests/test_estado_comun.py`: la tabla de `es_abierta` de arriba; `adr_max`/`fp_max` sobre fixture mínimo; `lee_tablero` sobre TSV con glosa; ramas con fuente de respaldo simulada si es barato. Sin tests por métrica del tablero. Paridad sobre el árbol real: digesto y tablero cuentan el mismo conjunto de filas `ABIERTA`, por IDs: `digesto abiertas: FP-…` · `tablero abiertas: FP-…` · `diferencia: ∅`. Pegar los dos listados antes y después. Commits. COMMIT 1: `estado_comun.py` + prueba. COMMIT 2: migración digesto y tablero. COMMIT 3: T22 + baseline sólo si hay delta explicado + cascada. Un PR. PARO si T22 produce WARN nuevos no explicables exclusivamente por `ABIERTA -- glosa` (pega `cut -f6 forense/firmas-pendientes.tsv | sort | uniq -c`); si la migración exige reescribir partes grandes del tablero; si aparecen primitivas repetidas fuera del perímetro (se anotan, no se incorporan). Fuera de perímetro. Normalizar todos los estados; `es_firmada()` genérico; cambiar el significado de `FIRMADA-POR-MERGE`; tiers; motor; centralizar todos los contadores; que T15 use la implementación que verifica; `tablero_vista.py`. Criterio de éxito. Una sola implementación productiva de los conceptos seleccionados y `digesto == tablero` para filas abiertas y ramas observables, con las pruebas críticas independientes. CONTADOR: implementaciones de `es_abierta` 3 → 1 · de ADR máx (productivas) 2 → 1 · de ramas 2 → 1 · discrepancia digesto/tablero N → ∅ · tests +1.
ELEMENTO 3 · ACTO AUTOMATIZA-1-E3 · CIERRE MECÁNICO — COMPUERTA: E2 fusionado
Objetivo. Retirar de `/acto` las operaciones de cierre puramente mecánicas (contar, buscar, recifrar, detectar, comparar, recordar comandos) sin automatizar decisiones: preflight + reconciliador mecánico, no autor de gobernanza. Dependencia. Reutiliza `es_abierta`, `lee_tablero`, `ramas_remotas_presentes`, `adr_max`, `fp_max`. No los reimplementa. Módulo nuevo: `tools/cierre_acto.py`, sin clases. `python3 tools/cierre_acto.py` = dry-run por defecto; `--aplica` = sólo reconciliación mecánica segura. Principio de diseño. El tool no incrementa por adelantado un ADR que no existe. Primero el humano crea la entrada ADR, la anotación L0, el rótulo si corresponde, `## CONSUMIDO` cuando corresponda. Después el tool reconcilia los derivados mecánicos contra la realidad del árbol (`ADR reales: 356 · cabecera declara 355 · L0 declara 355 → gobernanza 355→356 · L0 355→356`). Idempotente. Fase A · inspección (nunca escribe). Git: `HEAD`, `origin/main`, si HEAD deriva de main, ramas remotas presentes y su fuente. ADR: máximo real, siguiente candidato, si el candidato aparece ya en alguna rama remota accesible, con el límite explícito de que una rama puede existir sin haber redactado aún su ADR; no afirmar «PR abierto» sin evidencia. FP: máximo y filas abiertas (`es_abierta`). Conteos de gobernanza: número real de ADR vs `**N ADR**` de cabecera vs conteo de L0. Rótulo del acto: esperado/proporcionado y si ya está en `canon/registro-rotulos.tsv` — sólo reportar. `## CONSUMIDO`: presencia/ausencia en el encargo. Suite: ejecuta `python3 tests/check.py --baseline`, reporta código, VERDE/ROJO y últimas líneas. Sección «requiere juicio humano», siempre: redacción ADR, anotación L0, significado de rótulo nuevo, `donde_vive`, firmas, decisiones de mesa, merge. T25. No importar `_T25_ROTULO_BARE` ni `_T25_ARCHIVOS_CONOCIDOS` (internos de la suite). El mecanismo es correr `check.py --baseline`; un T25 nuevo se reporta como acción humana. No extraer T25 a módulo común preventivamente. `registro-rotulos.tsv`. El tool no escribe filas: `que_significa`/`donde_vive` son semánticos. Puede detectar que el rótulo falta, mostrar columnas requeridas y generar un esqueleto no aplicado (`RÓTULO: … · registro: AUSENTE · requiere humano: que_significa = ? · donde_vive = ?`). Fase B · `--aplica`, atómica. Regla dura: si cualquiera de las transformaciones no puede demostrarse antes de escribir, escribe cero archivos; sin éxito parcial. Escribe sólo: (A) cabecera de gobernanza `**N ADR**` al número real; (B) los dígitos del conteo ADR de L0 al valor real — ancla única, exactamente una sustitución, diferencia limitada a los dígitos; sin insertar anotación, sin tocar historia, sin reordenar. (C) Nada más: ni `registro-rotulos`, ni ADR, ni anotaciones, ni FP, ni encargo, ni baseline, ni PR. Proceso: leer objetivos → construir bytes candidatos en memoria → validar todas las anclas → comprobar que el cambio es exactamente el permitido → sólo entonces reemplazar. Si algo falla: `APLICACION_ABORTADA · 0 archivos escritos`. Prueba mínima — `tests/test_cierre_acto.py`: (A) fixture con 4 ADR reales, cabecera 3, L0 3 → dry-run `real=4 · gobernanza 3→4 · L0 3→4`. (B) `--aplica` actualiza ambos; sólo los dígitos previstos cambian. (C) ancla L0 rota → `APLICACION_ABORTADA · 0 archivos escritos`, gobernanza tampoco cambia. (D) ya reconciliado: segunda corrida → `sin cambios`. (E) rótulo ausente: lo reporta, no escribe `registro-rotulos.tsv`. Verificación histórica (manual, única, en la nota — no en CI): dry-run con `git worktree` en la base previa a `ADR-355` (padre de #559: `3fb0b8a`) debe proponer 355; en el árbol actual, el siguiente real. Registrar en la nota. Integración con `/acto`. Sólo la sección de cierre; los nueve conceptos se conservan. Pasos mecánicos → `python3 tools/cierre_acto.py` (preflight) y, tras escribir la sustancia, `python3 tools/cierre_acto.py --aplica`. El ejecutor sigue haciendo: ADR, anotación L0, rótulo, decisiones, anti-PR#77, `## CONSUMIDO`, push, PR. El tool: derivación, detección, reconciliación de conteos, verificación. Diff de `acto.md` pegado en la nota. Baseline. El tool no recifra `tests/baseline.json`; la suite informa; reglas vigentes. Commits. COMMIT 1: `cierre_acto.py` + prueba. COMMIT 2: integración mínima con `acto.md`. COMMIT 3: primer cierre real usando el tool, dentro del propio E3 (dry-run y aplica pegados). Un PR. PARO si L0 no tiene ancla única (pega los 200 caracteres alrededor de `L0 · Gobierno`, no inventes un segundo patrón); si reconciliar exige reescribir toda la línea L0; si el tool necesita entender semántica de ADR; si escribir `registro-rotulos` exige campos semánticos; si la aplicación no puede ser todo-o-nada; si importar internos de tests parece necesario. En esos casos: reportar y mantener el paso humano; no ampliar arquitectura. Fuera de perímetro. Redactar ADR; decidir si merece ADR; insertar anotaciones L0; escribir `que_significa`/`donde_vive`; firmar FP; decidir pendientes; tiers; fusionar/aprobar PR; crear encargos; `/despacha`; modelo; datos; recifrar baseline. Criterio de éxito. Un cierre ordinario deja de exigir contar ADR, comparar conteos, derivar ramas, detectar drift mecánico y recifrar a mano los dos contadores; sigue exigiendo redactar, decidir, explicar, firmar, fusionar. CONTADOR: pasos del cierre hechos a mano 9 → 3 (texto ADR/anotación, rótulo/`## CONSUMIDO`, push/PR) · tests +1 · primer cierre real con el tool: el de E3.
CRITERIO DE RETIRO (los tres)
Si tras uso real E1 no protege ninguna vía real, E2 no evita divergencias, o E3 no ahorra cierres o genera más trabajo del que elimina: retirar o simplificar la pieza. No añadir otra capa para salvar una automatización que no pagó. Mismo criterio que A.3/A.8/A.9/A.10/A.12/A.13: se revisa en un mes con el caso citado.
SOLICITUD AL EJECUTOR
Ejecutar los tres actos en secuencia. Para cada uno: verificar `main` vigente; corregir cualquier línea/ruta que haya cambiado respecto a C3 (y decirlo); hacer sólo el cambio mínimo del elemento; correr su prueba dirigida; correr baseline; abrir PR; no fusionar; reportar qué cambió, qué comportamiento quedó automatizado, qué sigue humano, y qué medición demuestra que pagó. No reabrir el diseño salvo defecto material nuevo — un defecto así es entregable, se reporta con el archivo y la línea, y se PARA.

## CONSUMIDO

Ejecutado: **ACTO AUTOMATIZA-1-E3 · CIERRE-MECANICO**, el tercero y último de `AUTOMATIZA-1`.

PR: https://github.com/Josanoforo/Modelado-Mexicano/pull/572 (rama `acto/automatiza-1-e3`, fusionado -- commit de merge `b1be1438634e87cfc68b5ee1e27db0bd92617ac1`).

**Nota de reconciliación** (defecto de proceso, no de contenido): esta sección `## CONSUMIDO` se había escrito y empujado a la rama del PR (commit `29c98d391a5a7dc72247723b073b9098d48b4909`) antes de que mesa fusionara, pero el merge se resolvió contra el commit anterior de la rama (`facfcfe6d096418103733d7f98d0fc38fa817d81`, COMMIT 3) por una carrera entre el push y el clic de merge -- el segundo padre del commit de merge `b1be143` es `facfcfe`, no `29c98d3`. El contenido sustantivo del acto (COMMIT 1/2/3: `tools/cierre_acto.py`, la integración con `.claude/commands/acto.md`, `ADR-367`, la reconciliación L0/cabecera) sí está íntegro en `main`; sólo esta sección de cierre faltaba. Se repone aquí, en un PR aparte, sin reabrir ni editar ningún otro punto del encargo.

Compuerta de entrada verificada por producto antes de tocar código (en la sesión original): `git log origin/main --oneline | grep AUTOMATIZA-1-E2` → `PR #569` (commit `4dfb4a5`), cumplida.

`python3 tests/check.py --baseline`: LÍNEA BASE VERDE (re-verificado al reponer esta sección).

