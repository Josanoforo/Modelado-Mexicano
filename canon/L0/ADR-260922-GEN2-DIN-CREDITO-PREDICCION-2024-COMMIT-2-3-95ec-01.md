**ADR-260922-GEN2-DIN-CREDITO-PREDICCION-2024-COMMIT-2-3-95ec-01** (raíz de acto: `95ec` = 4 hex del commit de 0-bis `95ecb59f`; no se renumera al fusionar), `ACTO GEN2-DIN-CREDITO-PREDICCION-2024-COMMIT-2-3 · LA SECCIÓN DE CRÉDITO DE ENIF 2024 SE ABRE: PERSISTENCIA SE SOSTIENE EN 5 DE 7 CONDUCTAS COMPARABLES, TENDENCIA GANA EN 2 (PROPUESTA-CON-RESERVA)`, 22/sep/2026, entorno **CAJA**, Sonnet 5 por mandato de mesa, sin sub-agentes, **MODO RÍGIDO**. Encargo archivado verbatim (A.3): `forense/encargos/2026-09-22-GEN2-DIN-CREDITO-PREDICCION-2024-COMMIT-2-3.md` + `.cuerpo.sha256` (`9eef778c…`). Sucesor de `#987`/COMMIT-1 (predicción congelada, backtest persistencia 7/9). Premisa del encargo (P1 "re-correr EMISIONES") verificada FALSA contra el árbol — ya sellado en `#987` — y replanteada (logística, no procedimiento): se siguió la secuencia real de `-ADJUDICACION-0001/spec.yaml` (`commit_3a` antes de abrir el dato, luego `commit_2`, luego `commit_3`), confirmado con dirección antes de tocar el dato.

**`commit_3a`:** fija la regla de adjudicación (`FP-…-7866-02` → FIRMADA): bootstrap sintético desde el SE analítico (ninguno de los dos lados expone réplicas), alimentado a `tools/duelo/cruces_familia.py::adjudica()` con `umbral_vence_pp=∞` — sin umbral de materialidad, `VENCE-RETADOR` inalcanzable por diseño. Nueva spec humana v1.1 sucede a v1.0 sin editarla (D-15).

**`commit_2`:** `corrida0 run CALC-DIN-CREDITO-PREDICCION-2024-ADJUDICACION-0001`, primera apertura real de la sección de crédito de ENIF 2024 (guardia `abre_conducta_2024`, 9 conductas autorizadas, K7/«crédito por app» fuera de la lista blanca), n=12 379 personas 18-70, 882 RESULT (81 nulos declarados), `exit_code=0`, `verify` → `REPRODUCE/IDENTICO`.

**`commit_3`:** `tools/duelo/credito_prediccion_2024.py` adjudica por conducta. `PROPUESTA-CON-RESERVA` en K1 (ΔMAE +1.90pp, IC95 [+0.62,+2.65]) y K6-P-TENEDORES (ΔMAE +2.62pp, IC95 [+0.45,+3.50]); `NADIE-VENCE` en K2-DEPARTAMENTAL/NOMINA/AUTOMOTRIZ, K3, K5; `NO-CONSTRUIBLE-SIN-RETADOR` en K4A/K4B (sólo 2 olas previas). **Defecto heredado hallado y declarado (código sellado en COMMIT-1, no reparado):** `_code()` (importado de `-EJES-0003`) quita el cero inicial de `niv`, incompatible con los códigos 2024 de dos dígitos — el eje escolaridad completo (4 de 16 celdas) queda excluido del ajuste en las 9 conductas (`NC-…-95ec-01`, `FP-…-95ec-01` a mesa).

**CONTADOR:** `N_corridas_selladas` +1 (`CALC-DIN-CREDITO-PREDICCION-2024-ADJUDICACION-0001`); `cuenta_gen2 = SI` (asentado en `decisiones.tsv`, E.2 — la etiqueta de la spec ya sellada no se edita); **no adopta**. Replay `REPRODUCE/IDENTICO` asentado en `forense/replay-evidencia.tsv`. `tests/check.py --rapido`: VERDE, 0 FAIL. `FP-…-7866-01` (K6-P-TENEDORES vs K6-PR) sigue ABIERTA, ajena a este acto. Nota: `forense/notas/nota-2026-09-22-gen2-din-credito-prediccion-2024-commit-2-3.md`.
