ENCARGO · ACTO MAESTRA35-L4 · R-v1_2-CIEGA — invoca /acto
SHA de redacción: 0fd6b4c (merge PR #472). Redacta dirección (Fable), 2/sep/2026, contra v2.12. Estado: GATED a PR #470 (ACTO MAESTRA35-L2) fusionado — verificación POR PRODUCTO: `forense/prereg-duelo-v2/espec-R-ciega-v1_2.tsv` presente en `origin/main` Y `python3 tools/arbitra.py --produce` reconocido en la ayuda del script (`grep -c '"--produce"' tools/arbitra.py` ≥ 1). Si falta cualquiera, la skill se niega con A.13 y cero commits. Rótulo: ACTO MAESTRA35-L2 nombró este sucesor "MAESTRA35-L3"; dirección lo renombra a MAESTRA35-L4 porque MAESTRA35-L3 es CIVICA-TIPO-DE-BOLETA (encargo de dirección, 2/sep) — el PR #470 corrige su censo antes de fusionar.
ENTORNO ASIGNADO: UBUNTU con corpus montado, en SESIÓN NUEVA que NO haya visto M ni el motor, y en CLON/WORKTREE PROPIO. NO se lanza en NUBE. Tercera parte de la firma de entorno: `ls data/raw/ | head -1` no vacío. MODELO SUGERIDO: Opus.

⚠️ SESIÓN CIEGA (ADR-46), con la lección de MAESTRA35-L2 ya pagada. Durante el lote NO se abre: `marco-M-sorteado-v1_2.tsv` ni ningún `marco-M-*` (incrustan `p` del motor en `razon_DD` exactamente en estas cuatro celdas) · `canon/gobernanza-v1_15.md` ni `canon/estado-programa-v1_11.md` (los ADR citan `p` verbatim) · `milpa/` · `corridas-M/` · `corridas-L/` · `scoreboard*` · `L-extraido*` · ningún encargo ni nota de MAESTRA34-L1/L5/N1/N4/N9 ni de MAESTRA35-N1/L1/L2/N2/N3/N4/N5/L5/L6 · `forense/prereg-duelo-v2/codificacion-R-v1_0.tsv` ENTERO — solo por `grep -P '^(FAM-M-0[567]|TRA-M-02)\t'`. El insumo del lado R es ÚNICAMENTE `forense/prereg-duelo-v2/espec-R-ciega-v1_2.tsv` (lista blanca de 14 columnas, 0 cifras de motor tras releer del disco — verificado por L2). Este encargo no contiene ningún `p`, `M`, `L` ni `z`. ORDEN OBLIGATORIO: los cuatro `corridas-R/*.json` se escriben y se COMMITEAN ANTES de cualquier paso de la cascada de cierre (ADR, cabecera, L0) — la cascada contamina y no importa, porque `produce()` nunca reescribe un R existente. Si por cualquier vía la sesión ve una cifra de motor antes de escribir un R, lo declara al inicio de la nota de cierre y esa celda sale de lote (precedente MAESTRA34-L2 §0).

CARRILES: MAESTRA35-L7/L8/L9 (caja: propuesta, medidores; no tocan `corridas-R`) · `/despacha` N3 → N5 (nube: N3 lee `corridas-R/` al puntuar — si este acto fusiona antes, puntúa más celdas; ninguno es compuerta del otro). Este acto escribe cuatro JSON nuevos en `corridas-R/` (ninguno existe), filas en `codificacion-R` (append; re-aplica quien fusiona segundo) y una nota en `notas-arbitra/`.

FIRMAS DE MESA — verbatim. El ejecutor propaga, no decide (SELLA-3).
- Mesa, 2/sep/2026 (a MAESTRA35-L2, a mitad de acto): opción **C** — reparar la herramienta y construir la proyección ciega en vez de computar contaminado. Este acto es la segunda mitad de C.
- FP-227 (FIRMADA): R por celda de las 14 del sorteado v1_2, en caja, dos commits.
- Firma de FP-243 (mesa, 2/sep/2026): la proyección ciega es el insumo obligatorio del lado R; `/arbitra` §COMMIT-1 se redacta contra `espec-R-ciega-<v>.tsv`; el marco sellado no se toca.
- Firma de FP-244 (mesa, 2/sep/2026): en actos ciegos la cascada de cierre va DESPUÉS de escribir `corridas-R/*.json`, y `/acto` §CIERRE lo dice en una línea.

═══ VERIFICACIÓN DE EXISTENCIA (A.8) — contestada por dirección contra 0fd6b4c + rama de PR #470; re-deriva al arrancar ═══
(1) ESTRUCTURA. Corredor R (`corridas-R/<id>.json`, escritos por `tools/arbitra.py --produce`; `regresion()`; `notas-arbitra/`), proyección ciega (`espec-R-ciega-v1_2.tsv`, producida por `tools/arbitra.py --proyecta-ciego <marco> <salida>`, PR #470), codificación (`codificacion-R-v1_0.tsv`, append-only).
(2) CONTENIDO:
- `ls corridas-R/ | grep -c -E "^(FAM-M-0[567]|TRA-M-02)\.json"` → 0. NO-ENCONTRADO ×4.
- `grep -c -P "^TRA-M-02\t" codificacion-R-v1_0.tsv` → 0: L2 no la escribió. Las tres de FAM-M-05/06/07 EXISTEN con `estado=PROPUESTA` y codificación de umbral («y=1 si remesas > 0; y=0 si remesas == 0»), que el parser reparado (`_PATRON_UMBRAL`) ya calza. → EXISTE-SATISFACE ×3, NO-ENCONTRADO ×1.
- Interfaz real del árbitro (L2, declarada): `arbitra.py <marco> "" <ids>` va a `main()` y escribiría `NO-EJECUTABLE-SIN-CODIFICACION`; la ruta que computa es `--produce <marco.tsv> <id1> … <id4>`. Regresión de L2: 12 de 12 COINCIDE; L5 añadió lector `.dta` y `DIN-M-01b` (la regresión sobre celdas sin fila de codificación falla por límite declarado, no por drift — no es PARO).
- FP y ADR: toma el primer libre al arrancar y dilo (main trae hoy ≥ FP-251 / ADR-301).
(3) COBERTURA RETROACTIVA. Todo lo que toca nació el 31/ago–2/sep; cubierto.

PIEZAS
P0 · UN COMMIT, antes de computar. (a) Confirma `sha256sum forense/prereg-duelo-v2/espec-R-ciega-v1_2.tsv` y pégalo — es el insumo, y su hash es la prueba de que se leyó eso y no el marco. (b) Control de ceguera de L2, repetido sobre los DOS insumos que abrirás — la proyección y las cuatro filas de `codificacion-R` por `grep` —: barrido de cifras tipo `0.dddddd` con los valores ENMASCARADOS en el propio comando (imprime conteo por archivo y columna, nunca el valor); esperado 0 en la proyección; si aparece alguna en las filas de codificación, PARA y reporta sin abrirla. (c) Fila `TRA-M-02` en `codificacion-R-v1_0.tsv`, escrita desde la proyección y el FD, sin abrir nada más: payload `encuci2020_bd_dbf` (manifiesto:992), tabla `SEC_4_5`, unidad persona seleccionada; universo con contacto = alguna de `AP5_16_1..AP5_16_10` == '1'; codificación COMPUESTA en la forma que el parser reparado calza: `y=1 si AP5_17=='1' o AP5_18=='1'; y=0 si AP5_17=='2' y AP5_18=='2'` (resto fuera); ponderador `FAC_SEL`, estrato `EST_DIS`, upm `UPM_DIS`; `fuente: espec-R-ciega-v1_2.tsv + FD encuci2020_fd_pdf (manifiesto:1022)`; `estado: PROPUESTA (ACTO MAESTRA35-L4)`. Los códigos de `AP5_16_x`, `AP5_17`, `AP5_18` se verifican en el FD; si el FD dice otra cosa que '1'/'2', manda el FD y se declara. Guardias para P2: `n_filas_leidas` = 21 519 y n con contacto = 13 435 (cifras de la propia proyección, columna universo); desviación → se reporta, no se ajusta.
COMMIT-1 (`/arbitra` §COMMIT-1, redactado ya contra la proyección): `notas-arbitra/2026-09-02-lote-fam-m-05-06-07-tra-m-02-ciego.md` con las cuatro filas de `espec-R-ciega-v1_2.tsv` copiadas verbatim (sha del archivo citado), las cuatro codificaciones tal como están en `codificacion-R`, y la frase «el primer resultado que produzca este procedimiento es el que se reporta».
P1 · `python3 tools/arbitra.py --produce forense/prereg-duelo-v2/espec-R-ciega-v1_2.tsv FAM-M-05 FAM-M-06 FAM-M-07 TRA-M-02`. Si `--produce` exige el sorteado en vez de la proyección, PARO y reporte (no se abre el sorteado para "hacerlo funcionar"); es un defecto del árbitro y se entrega como tal. Salida cruda pegada: estado por celda, `R`, `EE_R`, `n_efectivo`, `n_codigo_no_valido` (esperado 0 en FAM; si no, cuántos y qué valores, sin reclasificar), `n_estratos`, `n_upm_total`. COMMIT-2 = los cuatro JSON + la fila de estado `COMPUTADA (ACTO MAESTRA35-L4)` por append en `codificacion-R` (las filas PROPUESTA no se editan). ESTE COMMIT SE HACE ANTES DE TOCAR CANON. Una celda que PARA no tumba el lote (P1(c) de L2 lo garantiza: captura por celda).
P2 · CASCADA de `/acto`, ya con los R en disco y commiteados: ADR, cabecera, L0, registro-rótulos (censa `MAESTRA35-L4`), T25, `tests/check.py --baseline` VERDE o PARO-reporta. Nota de cierre con la lista de archivos abiertos (contrato de `/arbitra`) y el momento exacto en que la sesión dejó de ser ciega (el primer archivo de canon abierto), para que quien audite vea que fue DESPUÉS de COMMIT-2. Append de una línea a `.claude/commands/acto.md` §CIERRE si PR #470 no la trajo ya: «En actos R de sesión ciega, la cascada corre después de commitear `corridas-R/*.json`; nunca antes.» — verifica con `grep -c "sesión ciega" .claude/commands/acto.md` antes de escribirla, y no dupliques.

PERÍMETRO Y CONCURRENCIA: `forense/prereg-duelo-v2/corridas-R/{FAM-M-05,FAM-M-06,FAM-M-07,TRA-M-02}.json` (nuevos) · `forense/prereg-duelo-v2/notas-arbitra/` (una nota) · `forense/prereg-duelo-v2/codificacion-R-v1_0.tsv` (append: 1 fila TRA-M-02 + 4 filas de estado) · `forense/notas/` (cierre) · `.claude/commands/acto.md` (una línea, solo si falta) · `forense/hallazgos.md` · `forense/firmas-pendientes.tsv` (recibo) · A.3 · cascada. NO toca `tools/arbitra.py` (si algo falla ahí, PARO y reporte), `marco-M-*`, `milpa/`, `corridas-M/`, `corridas-L/`, scoreboards, `data/`. Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo.

FP/ADR CANDIDATOS: una fila de recibo (R 10 → hasta 14 del sorteado v1_2). Primer FP y ADR libres al arrancar.

CONTADOR: celdas del sorteado v1_2 con R: 10 → hasta 14 · familia con R 1 → 4 · trámite con R 2 → 3 · celdas puntuables L∩M∩R: +4 · cero estimaciones fuera de R. Declara el real.

Lo que este acto NO hace: no repara herramienta; no re-computa ninguna R existente; no toca M/L/scoreboard; no lee el motor antes de COMMIT-2; no edita instrucciones.

Sucesores declarados, no lanzados: `/despacha` decide si N3 se re-corre con 14 R o N5 las lee.

## CONSUMIDO

Ejecutado por ACTO MAESTRA35-L4 · R-v1_2-CIEGA, rama `acto/maestra35-l4-r-v1_2-ciega` (caja `/home/pc0/mm-maestra35-l4`, UBUNTU con corpus, base real `9cbd8d8`), 2/sep/2026. Commits: `da20d03` (0-bis A.3), `e96c25e` (`/arbitra` COMMIT-1, specs congeladas desde la proyección ciega + fila `TRA-M-02`), `deea16f` (COMMIT-2: cuatro `corridas-R/*.json` `COMPUTADO` + cuatro filas de estado, antes de tocar canon), más el commit de cascada (`ADR-302` en colisión declarada con `PR #486`, `FP-253`, L0, registro-rótulos, hallazgo, censo T22, `forense/notas/2026-09-02-MAESTRA35-L4-cierre.md`). Resultado: `FAM-M-05` R = 0.047459, `FAM-M-06` R = 0.047285, `FAM-M-07` R = 0.043775, `TRA-M-02` R = 0.126025; sorteado v1_2 14 de 14 con R; `--regresion` 4 de 4 COINCIDE. PR: el que abre esta rama contra `main` (número anotado en un commit posterior al abrirlo).
