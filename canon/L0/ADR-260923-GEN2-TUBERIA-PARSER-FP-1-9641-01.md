**ADR-260923-GEN2-TUBERIA-PARSER-FP-1-9641-01**, `ACTO GEN2-TUBERIA-PARSER-FP-1`, 23/sep/2026, NUBE, Opus 5 (subido de Sonnet, D-13: las dos premisas del encargo cayeron y hubo que re-derivar la gramática contra el tablero real), sin sub-agentes, MODO ABIERTO. `RE_FP_ID` de `tools/digesto_tramite.py` leía sólo `FP-\d+` y devolvía el prefijo de fecha de un id con raíz de acto (`findall(FP-260921-GEN2-TUBERIA-SUCESOR-1-6e60-01) -> ['FP-260921']`); su único consumidor (`_nc_a_fila_mesa`) perdía en silencio el plazo heredado y el residual de esas citas. Ensanchado a las dos épocas de D-24, más ancho que `nc_por_clase.RE_FP` porque el tablero real tiene 2 ids que D-24 admite y esa gramática rechaza. Medido con lector de CSV, universo 613×462: 41 filas NC cambian su lista de FP citadas, las citas que resuelven pasan de 95 a 141, desaparecen 3 ids fantasma. Cierra `NC-…-6e60-01`. Test `tests/test_digesto_ids_raiz.py` (13/13 OK), huérfano en CI vía `ci_guardias.py --censo`/`--ejecuta-huerfanos`, sin tocar `verify.yml` (D-21 manda sobre el pedido del encargo de cablearlo a mano). NC nuevas: `…-9641-01` (`nc_por_clase.py` fuera de perímetro), `…-9641-02` (`estado_comun.lee_tablero` cuenta líneas físicas, no filas), `…-9641-03` (ADENDA-1 de FIRMAS-8 devuelta sin archivar: su encargo vive en PR #1027). Nota: `forense/notas/2026-09-23-GEN2-TUBERIA-PARSER-FP-1-cierre.md`.
