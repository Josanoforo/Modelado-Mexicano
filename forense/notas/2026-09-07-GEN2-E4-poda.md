# GEN2-E4 · LIMPIEZA-C2 · PODA — ejecución D0-D8, resultados

Acto ejecutado en **UBUNTU (caja)**, modelo Sonnet, sin nube — tal como pide
la cabecera. Corre en worktree propio (`mm-gen2-e4-limpieza-c2`, rama
`acto/gen2-e4-limpieza-c2-poda`), creado desde `origin/main` (`f2f7739`,
merge de PR #602 · GEN2-E1).

## §0 · Discrepancia de encargo, declarada antes de todo lo demás

`forense/encargos/cola/2026-09-07-GEN2-E4-LIMPIEZA-C2-PODA.md` (el archivo
verbatim que `ACTO GEN2-E0 · ENCOLA` dejó en la cola, vía PR [COLA]) describe
un `GEN2-E4` **genérico y más corto**: borrar ramas remotas consumidas, `git branch
-d` de ramas locales fusionadas, podar worktrees de actos CONSUMIDO con
barrido de `data/raw` antes de remover, borrar clones extra sin trabajo
único, y activar *Automatically delete head branches* en GitHub. Su
COMPUERTA declarada es **doble**: "GEN2-E1 fusionado **y** la lista de poda de
su nota lleva la firma de mesa (fila FIRMADA en el tablero, o el merge de un
PR que la marque)". Verificado: `forense/tablero/TABLERO-PROGRAMA.md` **no
tiene ninguna fila** que mencione GEN2-E1/GEN2-E4 o LIMPIEZA — esa segunda mitad
de la compuerta, tal como la cola la redactó, **no estaba satisfecha** por
ningún artefacto committeado.

Lo que este acto en realidad ejecutó es un texto **distinto y mucho más
detallado**, entregado directamente por mesa en esta misma sesión,
etiquetado explícitamente **"v1.2 — con todas las decisiones tomadas"**, con
procedimiento D0-D8 (worktree propio, refs huérfanas, poda de 114+3
worktrees, `data/raices.local.yaml`, ICPSR, CRAN, clon base a `main`, restos
sueltos, diagnóstico de `.git`) y esta frase explícita sobre la compuerta:
*"Las decisiones D0–D8 quedan firmadas por el merge de este acto"* — es
decir, la propia mesa, hablando en vivo en esta sesión, sustituye la
compuerta de doble condición de la cola por su autorización directa. Este
acto **archiva el texto v1.2 verbatim en §10 de esta misma nota** — no en
un archivo nuevo bajo `forense/encargos/`: ese path ya casi colisiona una
vez (ver la propia `FP-340`, corrección T02 de `GEN2-E1`, que borró un
duplicado por nombre normalizado entre `forense/encargos/<slug>.md` y
`forense/encargos/cola/<slug>.md` y dejó el `## CONSUMIDO` en la fila de
FP, no en un archivo aparte) y aquí el contenido además **difiere de
verdad** del `.md` de la cola, así que superponer un segundo archivo con
el mismo slug normalizado sería el mismo defecto con una excusa peor. El
`## CONSUMIDO` de este acto queda en la fila de recibo nueva de
`forense/firmas-pendientes.tsv` (ver Cierre), mismo patrón que `GEN2-E1`/
`GEN2-E2` ya usaron.

La única mitad de la compuerta que sí se verificó mecánicamente, contra el
árbol y no de memoria:

```
$ git show origin/main:forense/notas/2026-09-07-GEN2-E1-limpieza-arboles.md | grep -c "arboles_fuera_de_politica = 114"
1
```

## §1 · D0 — lanzamiento propio, limpio

`git worktree list --porcelain | grep -iE "e4|poda"` y `git branch --list
"*e4*" "*poda*"` sobre el clon base: los dos únicos matches de "e4"
(`acto/e4-diseno-ensafi`, `maestra31-e4-orden-superior`) son tareas viejas
sin relación con esta poda; ningún match de "poda". **0 commits propios, sin
lanzamiento previo que limpiar.** Worktree nuevo creado limpio desde
`origin/main`.

**Hallazgo colateral en la propia creación (ver §8, coincide con E1 §4.2):**
`git worktree add` emitió dos `error: could not write config file
.git/config: Device or resource busy` — el worktree se creó igual (archivos
al día, HEAD correcto) pero el tracking `branch.<rama>.remote/.merge` quedó
sin escribir. Mismo defecto que `GEN2-E1` ya había encontrado en su propia
creación.

## §2 · D1 — refs huérfanas, FP-340

Verificado antes de tocar nada: `05e6bef` (2 commits, incluye
`forense/notas/2026-09-03-MAESTRA37-L2-spec.md`) y `fd6e5bc` (1 commit,
0-bis A.3) existen como commits alcanzables en el clon base, en las ramas
locales que la nota de `GEN2-E1` describe. Empujados:

```
git push origin 05e6bef:refs/heads/historico/maestra37-l2-mps-codebook-y-p3
git push origin fd6e5bc:refs/heads/historico/maestra38-v1-corrobora-y-reconcilia
```

`git ls-remote --heads origin 'historico/*'` → 2 filas, confirmando ambos
SHA exactos. Después, y solo después, se podaron los dos worktrees
(`mm-maestra37-l2-mps-codebook`, `mm-maestra38-v1`) — ver §3.

FP-340 pasa a **FIRMADA** (edición en `forense/firmas-pendientes.tsv`,
columna `firmada_en`): preservadas como `historico/*`; lista de 114
podada (§3); PR #602 ya estaba fusionado. `canon/registro-rotulos.tsv`
recibe la fila nueva para el prefijo `historico/` (no existía ninguna).

## §3 · D2 — poda de worktrees

**Corrección de método, declarada antes de podar nada:** el propio
algoritmo del encargo pide `git -C <ruta> log --branches --not --remotes
--oneline | wc -l` por cada worktree. Medido en un worktree ya confirmado
`MERGED` (`mm-e1-reloj-cruce`, PR #382): ese comando da **604** — falso
positivo repo-completo, porque GitHub borra la rama remota al fusionar y
la rama LOCAL de esa tarea (vieja, en el clon base) sigue apuntando a
commits que ya están en `main` pero que ningún *nombre* remoto vivo
referencia. Es exactamente la misma corrección que la propia nota de E1 ya
declaró para su Tabla 1 ("no `--branches --not --remotes` sin punto de
partida"). Control positivo: `git rev-list HEAD --not origin/main --count`
sobre el mismo worktree da **0**, y `git merge-base --is-ancestor HEAD
origin/main` confirma `YES`. Se usó esta segunda forma (ancestría contra
`origin/main`, no contra "algún remoto") como el verdadero criterio de "sin
riesgo de perder trabajo" en los 114 candidatos.

**Encuesta en frío de los 114** (`ruta`, `rama`, `status --porcelain`,
commits propios no en `origin/main`, tipo de `data/raw`), guardada en
`survey-114.tsv` (scratch, no se commitea): **110 SAFE**, **4 BLOQUEADOS**
por `git status --porcelain` no vacío (el propio algoritmo del encargo dice
"se lista, no se borra" — no hay excepción de juicio aquí):

| worktree | qué hay sucio |
|---|---|
| `mm-autoridad-semantica-enif` | 2 archivos `tools/curador_registro/*.py` modificados + 3 archivos nuevos sin trackear — trabajo real, nunca comiteado |
| `mm-autoridad-semantica-marco-produccion` | `.barrido2` sin trackear |
| `mm-llave2-decreto` | `scratchpad/` sin trackear |
| `mm-marco-produccion-total` | `.barrido2` sin trackear |

0 de los 114 tenía `data/raw` como directorio real (todos symlink o
ausente) — **0 invocaciones de `barrido_descargas_vs_manifiesto.py`
necesarias** por esta rama del algoritmo. 0 ausentes en disco.

Se pidió confirmación explícita a mesa antes de ejecutar el borrado masivo
(el propio clasificador de modo automático de la caja bloqueó el primer
intento de `git worktree remove` en bucle sobre 110 rutas, por ser una
acción de alto radio de impacto) — aprobado, ejecutado: **110/110
removidos sin fallo** (`git worktree remove`, uno por uno, log completo en
`remove-log.txt`, scratch).

**Tres casos especiales, fuera de la lista de 114, con regla propia en el
encargo:**
- `mm-gen2-e1-limpieza-c1` — excluida por la propia nota de `GEN2-E1` ("es la caja
  de este acto"), pero el encargo de `GEN2-E4` la reincorpora explícitamente
  ("entra a la poda, su PR #602 ya fusionó"). Verificada limpia (`status`=0,
  `HEAD` ancestro de `origin/main`) y removida.
- `mm-maestra37-l2-mps-codebook`, `mm-maestra38-v1` — las dos rescatadas en
  §2. Verificadas limpias (`status`=0 cada una) y sus commits confirmados
  alcanzables desde `refs/remotes/origin/historico/*` antes de remover.
  Removidas.

**Total removido: 113** (110 + 3). `git worktree prune` limpia metadata
residual. `mm-sello-3` (rama `claude/encargo-maestra38-sello-3-6y5e0z`)
**no estaba en la lista de 114 de `GEN2-E1`** — apareció después de que `GEN2-E1` tomó su
inventario (su rama remota, per `git fetch --prune`, ya fue borrada de
`origin`, señal de que el PR se fusionó, pero el worktree local sigue vivo
y el encargo de `GEN2-E4` no lo nombra) — **fuera de perímetro de D2, no se
tocó**; queda declarado aquí como residuo para un acto de higiene futuro,
no una omisión silenciosa.

**Ramas locales fusionadas** (`git branch --merged origin/main`, excluyendo
`main` y la propia): primera pasada (HEAD del clon base aún en la rama vieja
`acto/maestra38-c1-re-asiento`, 154 commits atrás de `origin/main`) borró
120 y falló 9 — 5 de las 9 fallaron porque `git branch -d` compara contra
`HEAD`, no contra `origin/main`, y el clon base no estaba en `main` todavía
(mismo defecto de fondo que motiva la advertencia "nunca asumas que el clon
base está en main"). Repetida **después** de D6 (clon base ya en `main`):
5 más borradas, quedan solo las que siguen legítimamente checked-out en
otro worktree (`+` en `git branch`) o no fusionadas de verdad. **Total:
125 ramas locales borradas.**

## §4 · D3 — `data/raices.local.yaml`, retira `downloads`

Ambos clones reales editados a mano (no hay mecanismo automático para esto):
retirada la clave `downloads` de `Modelado-Mexicano/data/raices.local.yaml`
(quedaba apuntando a `/mnt/c/Users/PC0/Downloads`); `mm-adq` no declaraba ni
`data_raw` ni `downloads` (divergencia que ya había medido `GEN2-E1`) — se le copió
el archivo completo del clon base. **Ambos ahora bit a bit idénticos:**

```
sha256 7d7ba2640e983940dad1c21cd59cba14787a6140c6bfa07803431d05893e2444
  Modelado-Mexicano/data/raices.local.yaml
  mm-adq/data/raices.local.yaml
```

`python3 tests/manifiesto.py --verifica` (fuera de sandbox, `/mnt/c` está
en la lista de denegación de lectura del sandbox, no del sistema) corrido en
ambos clones: ninguna línea menciona `downloads` en ninguno de los dos —
confirma que la raíz retirada no se reporta más.

`python3 tools/entorno.py`: corre limpio en el clon base (`git_status:
LIMPIO`, `raices_logicas: data_raw:SI descargas_mx:SI`). **En `mm-adq` no
corre** — `tools/entorno.py` no existe en su árbol (`git show
HEAD:tools/entorno.py` confirma ausencia): `mm-adq` está parado en
`acto/automatiza-2-e4-pdn-compara` (`62e9422`, 7 commits propios encima,
diverge de `origin/main`, nunca se le tocó por mandato explícito de D6 —
"mm-adq no se toca"), rama que nació **antes** de que `GEN2-E3` escribiera
`tools/entorno.py`. Premisa del encargo verificada contra el árbol y
declarada corregida aquí: no es un defecto de este acto, es que la
herramienta que el encargo pide correr "en los dos clones" no existía
cuando la rama viva de `mm-adq` se creó, y tocar `mm-adq` para arreglarlo
está explícitamente fuera de perímetro.

## §5 · D4 — `ICPSR_35024-V1.zip`, cuatro copias

Encontradas en `/mnt/c/Users/PC0/Downloads/`: `ICPSR_35024-V1.zip`,
`ICPSR_35024-V1 (1).zip`, `(2).zip`, `(3).zip`. `unzip` **no existe** en
este entorno (`/bin/bash: unzip: command not found`) — se usó
`python3 -m zipfile`/`zipfile.ZipFile` en su lugar, sin margen de error
silencioso. Los cuatro `sha256` son **todos distintos** entre sí y
distintos del ya registrado (`icpsr35024_mexico_panel_study_2012_paquete_v1_zip`,
`c1e7ba95…`) — pero **los cuatro tienen la misma lista de 10 miembros**
(cuestionarios, codebooks, manifest, `TermsOfUse.html`), comparados por
`CRC32`+tamaño miembro a miembro: idénticos salvo `TermsOfUse.html` en la
copia `(3)` (mismo tamaño, `CRC` distinto — versión de términos de uso
descargada en fecha distinta, 6/sep vs 2/sep). **Ninguna de las cuatro
copias contiene `35024-0001-Data.dta` ni equivalente** — son las cuatro
**documentación**, no el paquete restringido de datos.

Se copió la copia canónica (`ICPSR_35024-V1.zip`, sin sufijo, la más
antigua de las cuatro) a `descargas_mx/academico_icpsr35024/
icpsr35024_documentacion_v1.zip` y se registró vía el ciclo real de la
herramienta (`--registra` no acepta una raíz distinta de `data/raw/` —
premisa del encargo corregida, ver §8; el mecanismo correcto para
`descargas_mx` es `--escanea RAIZ --grupo/--url` seguido de `--promueve
--grupo`, que sí escribe el campo `raiz`):

```
id: icpsr35024_documentacion_v1
raiz: descargas_mx
sha256: 8e48c789c5841d6c964297a19562025744f6245d68b8abf2a91548e455abd13b
```

**FP-314 se anota** (no se cierra: sus otras 4 recetas siguen abiertas):
las cuatro copias de `ICPSR_35024-V1.zip` en `Downloads` son documentación;
el DUA para el paquete de datos sigue vigente, receta #1 sin resolver.

## §6 · D5 — `ACADEMICO-list-cran`

Confirmado clon git completo del paquete R `list::mexico`
(`SensitiveQuestions/list`, commit `e088e5f8`, 108 archivos). Los dos
archivos registrados (`list_cran_mexico_tab` = `data/mexico.tab`,
`list_cran_mexico_rd` = `man/mexico.Rd`) se copiaron a
`descargas_mx/list-cran/{mexico.tab,mexico.Rd}`, con `sha256` verificado
byte a byte idéntico al original antes de tocar el manifiesto. Se
actualizó el campo `archivo` de ambas entradas al nuevo path y
`tests/manifiesto.py --verifica --id <cada uno>` → **COINCIDE** en los
dos. Solo entonces se borró el clon completo (`rm -rf
ACADEMICO-list-cran`).

**El "archivo 138 que `GEN2-E1` no identificó":** re-escaneado con la versión
actual de `tests/manifiesto.py --escanea descargas_mx` (que desde
`ADR-310`/MAP-1b colapsa un clon git completo en una sola línea `CLON …`),
el reporte de hoy da `nuevos: 3` — dos metadatos `.json` de
`ACADEMICO-dataverse-mps2012` y una página `descargas.php`, los tres ya en
`data/manifiesto-staging.yaml` de un acto anterior, ninguno relacionado con
CRAN. **No hay ningún archivo huérfano "138" hoy**: esa cifra de E1 se
midió con un barrido distinto (`barrido_descargas_vs_manifiesto.py`, previo
a que `manifiesto.py` colapsara clones git en una línea), y por eso 137 de
los 138 eran justamente los miembros del clon que aquí se acaba de borrar.
Declarado como corrección de método, no como hallazgo nuevo.

## §7 · D6 — clon base a `main`

`git -C Modelado-Mexicano checkout main && git pull --ff-only`: estaba 154
commits atrás (branch previa: `acto/maestra38-c1-re-asiento`, PR #577 ya
fusionado); fast-forward limpio a `f2f7739`. `mm-adq` no se tocó —
`git status --porcelain` confirma exactamente la línea sucia esperada
(`data/manifiesto-staging.yaml`), nada más.

## §8 · D7 — restos sueltos de E1 §4.3

Los 5 ítems (`mm-corre-r10-1-SELLO/`, `respaldo-worktrees/`,
`BACKUP-mm-mirror-2026-08-10.tar.gz`, `barrido-completo-snapshot-
staging-2026-08-10.tar.gz`, `untracked_barrido.txt`) verificados presentes
en `$HOME`, movidos sin abrir ni examinar contenido más allá de lo que E1
ya declaró a `~/archivo-gen1/2026-09-07/`, con `LEEME.txt` que los
inventaría uno por uno con su procedencia. **Nada se borró.**

## §9 · D8 — diagnóstico del `.git` anómalo (sin reparar)

```
$ stat ~/.git/config ~/.git/hooks
  File: /home/pc0/.git/config    -> character special file, 0 bytes,
                                     Device 0,6, uid/gid nobody/nogroup
  File: /home/pc0/.git/hooks     -> idéntico
$ mount | grep -E "home|dev/null"
  none on /home/pc0/.git/config type devtmpfs (ro,...)
  none on /home/pc0/.git/hooks  type devtmpfs (ro,...)
  /dev/sdd on /home/pc0/Modelado-Mexicano/.git/config type ext4 (ro,...)
  /dev/sdd on /home/pc0/mm-adq/.git/config type ext4 (ro,...)
```

**Hallazgo, no reparado:** `~/.git/config` y `~/.git/hooks` son
bind-mounts de solo-lectura sobre `devtmpfs` (dispositivo de caracteres,
no archivos reales) — y el `.git/config` de **ambos clones reales**
(`Modelado-Mexicano`, `mm-adq`) también está bind-montado de solo-lectura
sobre su propio contenido real (`/dev/sdd`, mismo filesystem, mismo
archivo, pero re-montado `ro`). Esto **explica directamente** el `Device or
resource busy` que este mismo acto encontró al crear su propio worktree
(§1) y que E1 ya había encontrado (§4.2 de su nota): cualquier intento de
`git worktree add`/`branch --set-upstream` que necesite escribir en el
`.git/config` del **clon base** (no en el `.git` propio del worktree
nuevo, que sí es escribible) choca con este remount de solo lectura.
Patrón consistente en los dos clones reales y en el `.git` huérfano de
`$HOME` — lee como una política de sandbox de la propia caja (proteger
`.git/config`/`.git/hooks` de cualquier escritura de comando, en todo
`$HOME`), no como una corrupción de disco ni un defecto del proyecto.
Causa exacta (qué proceso arma el bind-mount) **no identificada** — fuera
del perímetro de este acto, que solo diagnostica.

`git -C Modelado-Mexicano config --get-regexp '^branch\.' | wc -l` → **72**
secciones `[branch "x"]`, muchas de ramas que ya no existen (confirma E1
§4.5, basura inofensiva, no se poda aquí — fuera de perímetro).

`branch.<rama>.remote` por cada worktree superviviente:

| worktree | rama | `branch.<rama>.remote` |
|---|---|---|
| `Modelado-Mexicano` | `main` | `origin` |
| `mm-autoridad-semantica-enif` | `codex/autoridad-semantica-enif` | **VACÍO** |
| `mm-autoridad-semantica-marco-produccion` | `codex/autoridad-semantica-marco-cobertura-total` | `origin` |
| `mm-gen2-e4-limpieza-c2` (esta caja) | `acto/gen2-e4-limpieza-c2-poda` | **VACÍO** (mismo EBUSY de §1) |
| `mm-llave2-decreto` | `llave2-decreto` | **VACÍO** |
| `mm-marco-produccion-total` | `marco-produccion-total` | **VACÍO** |
| `mm-sello-3` | `claude/encargo-maestra38-sello-3-6y5e0z` | **VACÍO** |

Fila ABIERTA (D8, diagnóstico puro): causa raíz del remount de solo-lectura
sobre `.git/config`/`.git/hooks` en `$HOME` y en ambos clones reales, no
identificada; efecto medido (tracking de rama sin escribir en worktrees
nuevos) documentado dos veces (`GEN2-E1`, `GEN2-E4`) pero no corregido.

## Autoadvertencia de perímetro

El perímetro declarado del encargo nombra `data/manifiesto.yaml`,
`canon/registro-rotulos.tsv`, `forense/firmas-pendientes.tsv`,
`forense/notas/` (1) y "cascada" — no nombra explícitamente
`tests/check.py`. Este acto sí lo tocó, para añadir esta misma nota a
`_T25_ARCHIVOS_CONOCIDOS` (ver arriba, T25 marcaba `FAIL` por el `E4`
pelado dentro del bloque verbatim de §10). Se juzga dentro de "cascada"
— es exactamente el mecanismo que `T25` documenta en su propio código
para este caso exacto (ver el precedente `MAESTRA38-M13` en la misma
lista) — pero se declara aquí en vez de darlo por implícito, en vez de
asumir que "cascada" cubre cualquier archivo. También se revirtió
`data/manifiesto-staging.yaml`: el `--escanea` de D4/D5 regeneró esa
raíz completa e incluyó de paso 3 archivos reales pero ajenos al
perímetro (dos metadatos de `ACADEMICO-dataverse-mps2012` y una página
`descargas.php`), descubiertos como efecto colateral, no buscados —
`git checkout -- data/manifiesto-staging.yaml` los deja donde estaban
(en disco, sin registrar) para que el acto que sí los toque los
encuentre en su propio escaneo, en vez de que este acto decida por él.

## Contadores de cierre

- `arboles_fuera_de_politica`: 114 → **4 residuo listado** (los 4
  BLOQUEADOS de §3; 110 podados + 3 casos especiales = 113 de los 117
  originalmente fuera-de-política, más 1 (`mm-gen2-e1-limpieza-c1`) que
  no contaba en los 114 pero también se podó).
- `ramas_consumidas_vivas`: **0** (`sello-3-6y5e0z` ya no tiene rama remota
  tras el `--prune`; el worktree local `mm-sello-3` sigue vivo mismo, ver
  §3, pero está fuera del perímetro de la lista de 114 de E1).
- `payloads_locales_no_manifestados` en `descargas_mx`: **0** tras §5/§6
  (el único nuevo, la copia documentación de ICPSR, quedó registrado; el
  clon CRAN completo ya no existe).
- `fp_abiertas`: **−1** (FP-340 FIRMADA).

## §10 · Encargo v1.2 ejecutado, verbatim (0-bis A.3)

Texto recibido directamente de mesa en esta sesión, íntegro, sin editar —
distinto del `.md` que quedó en `forense/encargos/cola/2026-09-07-GEN2-E4-
LIMPIEZA-C2-PODA.md` (ver §0):

> E4 · ACTO GEN2-E4 · LIMPIEZA-C2 PODA · v1.2 — con todas las decisiones tomadas
>
> Cabecera: UBUNTU (caja) · Sonnet · COMPUERTA por producto: git show origin/main:forense/notas/2026-09-07-GEN2-E1-limpieza-arboles.md | grep -c "arboles_fuera_de_politica = 114" → 1 (#602 fusionado). NO se lanza en NUBE. Las decisiones D0–D8 quedan firmadas por el merge de este acto.
>
> D0 · Limpia tu propio lanzamiento abortado, antes del 0-bis. git worktree list --porcelain | grep -iE "e4|poda" y git branch --list "*e4*" "*poda*" en el clon base: si existe el worktree o la rama del lanzamiento anterior, se verifica 0 commits propios y status vacío, se pega la salida, y se hace git worktree remove + git branch -D. Si tiene commits propios (no debería), PARA y reporta. Solo entonces arranca el acto en worktree nuevo desde origin/main. D1 · Refs huérfanas (histórico, sin PR) — cierra FP-340. git push origin 05e6bef:refs/heads/historico/maestra37-l2-mps-codebook-y-p3 (2 commits; contiene forense/notas/2026-09-03-MAESTRA37-L2-spec.md) y git push origin fd6e5bc:refs/heads/historico/maestra38-v1-corrobora-y-reconcilia (1 commit, 0-bis). git ls-remote --heads origin 'historico/*' → 2. Rótulo historico/ en canon/registro-rotulos.tsv = «rama preservada; no fusionable; no se abre PR sobre ella». FP-340 → FIRMADA (E4 D1): preservadas como historico/*; worktrees podados. Solo después se podan esos dos worktrees. D2 · Poda de los 114 worktrees de E1 §5, por cada uno y en este orden: git -C <ruta> status --porcelain | wc -l (>0 → se lista, no se borra, sigue) → git -C <ruta> log --branches --not --remotes --oneline | wc -l (>0 → ídem) → si data/raw es directorio real: tools/barrido_descargas_vs_manifiesto.py sobre él; lo no registrado se mueve al corpus compartido y se registra (tests/manifiesto.py --registra) antes de seguir → git worktree remove <ruta>. Al final git worktree prune; git branch -d de cada rama local en git branch --merged origin/main. Excluidos: Modelado-Mexicano, mm-adq, el worktree de este acto; mm-gen2-e1-limpieza-c1 entra a la poda (su PR #602 ya fusionó). D3 · downloads deja de ser raíz. data/raices.local.yaml de ambos clones queda con data_raw (inerte, documentado) y descargas_mx; sin downloads; ambos archivos idénticos (pegar sha256 de cada uno, no rutas). Confirmar con python3 tests/manifiesto.py --verifica (no reporta downloads) y python3 tools/entorno.py en los dos clones. Nada de esa carpeta se lista ni se copia, salvo D4. D4 · ICPSR_35024-V1.zip, cuatro copias. sha256sum de las cuatro (si difieren, se dice cuál se abre); unzip -l <copia> | grep -iE "Data\.(dta|tsv|sav|csv)". Si contiene 35024-0001-Data.dta o equivalente: copiar esa copia a descargas_mx/, registrar (--registra, raíz descargas_mx, sha256, url_origen: ICPSR 35024, descarga manual de mesa, fecha del archivo), y FP-314 → #1 SUPERADO por E4 (D4): paquete Data presente y registrado; NO-ACCESIBLE-por-DUA queda como historia 8/ago–7/sep. Si solo es documentación: registrar como icpsr35024_documentacion_zip y anotar en FP-314 «las cuatro copias son documentación; el DUA sigue vigente». No se abre el .dta, no se mide. D5 · descargas_mx/ACADEMICO-list-cran/. Copiar los dos registrados (list_cran_mexico_tab, list_cran_mexico_rd) a descargas_mx/list-cran/, --verifica --id de ambos → coincide; borrar el clon git. Nombrar y clasificar el archivo 138 que E1 no identificó (registrar o descartar, con razón). D6 · Clon base a main: git -C ~/Modelado-Mexicano checkout main && git pull --ff-only. mm-adq no se toca (línea sucia esperada: data/manifiesto-staging.yaml). D7 · Restos sueltos de $HOME (E1 §4.3) → ~/archivo-gen1/<fecha>/ con LEEME.txt que los inventaría; no se borran. D8 · Anomalía .git/config (E1 §4.1–4.2): solo diagnóstico — stat ~/.git/config ~/.git/hooks, lsattr, mount | grep -E "home|dev/null", git -C ~/Modelado-Mexicano config --get-regexp '^branch\.' | wc -l, y por cada worktree superviviente git config --get branch.<rama>.remote (los vacíos se listan). Fila ABIERTA con el diagnóstico; no se repara nada. Cierre: limpia_arbol.py --reporta antes y después, pegado; nota forense/notas/<fecha>-GEN2-E4-poda.md; ADR de registro que cubra el inventario de E1 (#602, sin ADR propio) y esta poda; recibo; suite VERDE; verifica_head_remoto.py. Perímetro: sistema de archivos local según D0–D7; en el repo: data/manifiesto.yaml (solo append por D4/D5) · canon/registro-rotulos.tsv (historico/) · forense/firmas-pendientes.tsv (FP-340, FP-314, ABIERTA de D8, recibo) · forense/notas/ (1) · cascada. No toca milpa/**, canon de reglas, specs ni data/corrida0/. Si te encuentras escribiendo fuera de esta lista, PARA — el perímetro estaba mal calculado y saberlo vale más que el atajo. Contador: arboles_fuera_de_politica 114 → 0 (o residuo listado); ramas_consumidas_vivas → 0; payloads_locales_no_manifestados → 0 en descargas_mx; fp_abiertas −1 (FP-340).
