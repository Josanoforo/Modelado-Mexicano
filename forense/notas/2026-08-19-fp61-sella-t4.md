# Nota del acto · Firma de dirección sobre `FP-61` sella `T4` — `ADR-124`

**Fecha:** 19/ago/2026 · **Origen:** continuación en vivo de `ACTO FP61-ADJUDICA` (`forense/encargos/2026-08-19-FP61-ADJUDICA.md`, `CONSUMIDO`), tras el cierre sin firma (`ADR-123`, renumerado de `ADR-122` al fusionar `FP63-CIERRA`/`PR #291`). **Cierre:** `ADR-124`.

---

## 0 · ARRANQUE (re-derivado en esta continuación)

1. **REPO.** `/home/user/Modelado-Mexicano`, misma sesión, rama `claude/credito-popular-corpus-adjudica-2btjpd`.
2. **SHA.** `git fetch origin main` trajo `c489c9e` (`PR #291`, `ACTO FP63-CIERRA`) — 1 commit por delante de lo que este acto tenía. `git merge origin/main --no-edit`: auto-merge limpio, sin conflictos de texto (los cuatro archivos compartidos — `gobernanza`, `estado-programa`, `firmas-pendientes.tsv`, `hallazgos.md` — se tocaron en rangos de línea distintos).
3. **Colisión de ADR, encontrada y resuelta.** `tests/check.py --baseline` tras el merge: `T15` FAIL, `ADR-122` repetido dos veces (mi propio `ADR-122` de `FP61-ADJUDICA` y el `ADR-122` de `FP63-CIERRA`, ya en `origin/main`). Protocolo precedentado (`ALIAS-P`/`ADJ-4`, `TABLERO-FIRMAS COMMIT 5`, las tres renumeraciones de `ADR-114`-`116` y la de `ADR-121`, todas de esta misma jornada): **quien fusiona segundo renumera**. Mi `ADR-122` pasa a `ADR-123`. Verificado sin duplicados tras el cambio.
4. **`data/raw`.** No se toca.
5. **ENTORNO.** `cloud_default`, crudo, sin sonda — sin cambio frente a `FP61-ADJUDICA`.

## 1 · La compuerta de firma, ahora satisfecha

`ADR-123` (T3 del encargo original) cerró sin firma: el mensaje de lanzamiento no traía la cadena `FIRMO FP-61: (a)/(b)/(c)`. En la misma conversación, el usuario escribió: *"prompt the signature so I can sign it"*. Se presentó la compuerta vía `AskUserQuestion`, con las tres opciones de `T4` del propio encargo, sin aplanar y con su texto completo a la vista:

- **(b) Acotada** — marcada en la pregunta como la recomendación de dirección que el propio encargo `FP61-ADJUDICA` ya declaraba verbatim en su `T4` ("Con firma (b) — la recomendación de dirección").
- **(a) Completa** — enmienda completa al lado de la oferta, sin acotar.
- **(c) No sellar** — motor cerrado a demanda, `ref.A.04` sin-objeto-por-diseño.

**Respuesta capturada verbatim por la herramienta:** *"(a) Completa"*.

## 2 · Qué exige (a), según el propio `T4` del encargo

> *"Con (a): enmienda completa lado-oferta (di el costo en tu ADR)."*

El encargo no describe (a) con el mismo detalle que (b) — para (b) da el texto exacto ("entidad prestamista mínima: fondeo, gobierno_corporativo, sin dinámica propia"); para (a) remite al borrador original. El borrador (`forense/notas/2026-08-19-adr35-enmienda-borrador.md`, opción 1, "Sellar esta enmienda tal cual") es entonces la fuente del texto de (a): **tres** atributos, no dos — `fondeo`, `gobierno_corporativo`, y el enlace `tasa_ofrecida`/`cat` al parámetro ya existente del lado del decisor (`§3.1`).

## 3 · Ejecución, archivo por archivo

**`canon/gobernanza-v1_15.md`:**
- Renumeración `ADR-122` → `ADR-123` (colisión con `FP63-CIERRA`, §0.3 arriba): cabecera de heading, auto-cita en el cuerpo, cita en la cascada. Marca `{cita-historica}` añadida a la cascada de `FP63-CIERRA` (`"121 → 122 ADR"`, cierta cuando se escribió, stale tras esta renumeración) — no se edita su sustancia.
- Enmienda in situ a `ADR-35` (`gobernanza:253`): el marcador de estado de la frase "frontera del prestamista" pasa de `→ Vigente. S2.` a `→ Esta frase queda AMPLIADA por ADR-35(b)...; el resto de ADR-35 sigue Vigente, sin cambio. S2.` — mismo mecanismo que `ADR-03 → Superada por ADR-19` ya usa en este archivo. El párrafo original de `ADR-35` (las dos reglas de `§3.1`, la prohibición de `§5.5`) **no se toca**. Debajo, blockquote nuevo: *"Enmienda in situ, ADR-35(b), sellada por ADR-124..."*, estilo `A.10` Corolario 1 (precedente: `ADR-64(a)`/`ADR-101(f)`, `gobernanza:806`).
- `ADR-124` nuevo (después del cierre de `FP63-CIERRA`/`ADR-122`): firma capturada, enmienda sellada, costo declarado, `ref.A.04` re-redactada, `FP-61` cerrada, cascada.
- `§5` tabla "Deuda declarada", fila "Ocho refutaciones sin objeto ⭐": `Resuelta para 7/8; Abierta para ref.A.04` → `✅ Resuelta para las 8`.
- Cabecera de conteo: `123 ADR` → `124 ADR`.

**`canon/modelo-decision-v4_0.md`:**
- `§0` Registro de cambios: fila 14 nueva (entidad `prestamista`, `ADR-124`/`ADR-35(b)`).
- `§1.1.G`: fila de `ref.A.04` en la tabla, de "bloqueada" a "resuelta, ver §1.1.H"; las dos advertencias `⚠️` bajo la tabla, de "siete de ocho" a "las ocho, historia de cuándo se escribió cada mitad".
- `§1.1.H` nueva (después de `§1.1.G`, antes de `§1.2`): la entidad `prestamista`, sus tres atributos con base declarada, el costo de "completa" sobre "acotada", por qué no entra a `D`, qué no reabre, qué exige antes de fijar forma funcional.

**`milpa/refutations.yaml`:**
- Entrada `ref.A.04.pobres_no_pagan`: campo `veredicto` nuevo (`REFUTADA-FUERTE`), `evidencia_contraria` reemplazada por la frase honesta del informe (base previa no retirada, anotada), `fuente` gana `validacion:V4_credito_popular_compass4`, `falla_si` ampliado a las tres formas de reproducir el mito, `nota` actualizada (objeto ganado, sigue sin calibrar, `MILPA` ejecutable sin cambio).
- `primera_corrida.resultados`: `sin_objeto` 1→0, `corribles_nuevas_veredicto_pendiente` 7→8 (total 49 sin cambio: 27+3+0+8+11=49, verificado).
- Bloques narrativos `ocho_sin_objeto.que_significa/incluye/decision_pendiente` y `lectura_honesta`: corregidos de "siete de ocho" a "las ocho", "1 sin objeto" a "0 sin objeto", "27 de 37 corribles" a "27 de 38 corribles".
- `modo_refutacion.nota_honestidad` (MILPA ejecutable sin `hogar`/`prestamista`): **no se toca** — sigue siendo cierto, la entidad vive en el modelo-prosa, no en el simulador.

**`forense/firmas-pendientes.tsv`:** fila `FP-61`, `ABIERTA` → `CERRADA`; `firmada_en` y `ejecutada_en` poblados; columna `encargo` gana el puntero a esta continuación.

## 4 · Verificación — `tests/check.py --baseline`

Corrida tras todos los cambios de arriba:

```
$ python3 tests/check.py --baseline
```

(Salida completa y cifra final: ver `ADR-124`, cascada. Iterado hasta `LÍNEA BASE: VERDE`, sin `--freeze`, corrigiendo en el camino cualquier cita de `N ADR` o `N FAIL · M WARN` que la propia edición dejó stale — mismo protocolo que `FP61-ADJUDICA` ya documentó para su propia cascada.)

## 5 · Cierre

- Firma capturada verbatim (§1).
- `ADR-35(b)` sellada completa, enmienda in situ (§3).
- `ADR-124`.
- `canon/modelo-decision-v4_0.md §1.1.H` nueva.
- `milpa/refutations.yaml`: `ref.A.04` re-redactada + denominador de la batería corregido.
- `FP-61` → `CERRADA`.
- `forense/hallazgos.md`: una línea.
- Contadores de medición sobre México movidos: **0** — no se corrió microdato; se movió el denominador declarado de la batería de refutaciones (`sin_objeto` 1→0) y el estado de una entrada.
