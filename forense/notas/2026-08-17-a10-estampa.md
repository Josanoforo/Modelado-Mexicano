# ACTO A10-ESTAMPA — nota del acto

**Fecha:** 17/ago/2026 · **Encargo:** `forense/encargos/2026-08-17-EA10-a10-estampa.md` (E-A10) ·
**Base:** `f3873c2` · **Rama:** `claude/a10-estampa-universo-83h68h` · **ADR:** `ADR-87` (derivado, ver §6) ·
**Entorno:** nube (`cloud_default`), repo-only, sin `data/raw`, sin microdato.

**Regla de honestidad de este acto, escrita antes de correr nada: el primer resultado que produzca este
procedimiento es el que se reporta.** Nada de lo de abajo se re-corrió hasta que salió el número que convenía;
donde una cifra heredada no reprodujo, se dice que no reprodujo y se escribe la que salió.

---

## 1. Arranque, contestado con salida cruda

| Punto | Resultado |
|---|---|
| 1 · REPO | Clon ya presente en `/home/user/Modelado-Mexicano`. `git log -1 --format="%h %s" origin/main` → `f3873c2 Merge pull request #240 from Josanoforo/claude/tablero-firmas-mecanismo-tocpwi`. `git status` → *working tree clean*. |
| 2 · SHA | Base declarada `f3873c2` = `origin/main` real = `HEAD` de la rama. Sin diferencia que reportar. **Incidente de ref, declarado:** al arrancar, el `origin/main` local apuntaba a `f8eb2e3` (PR #182); `git fetch origin main` lo corrigió con *forced-update* a `f3873c2`, y `git merge-base --is-ancestor f8eb2e3 f3873c2` responde que `f8eb2e3` **no** es ancestro. El estado autoritativo (`git ls-remote origin refs/heads/main` → `f3873c25d12ec3e26730901dc257788011e5ceea`) coincide con la base declarada; se trabajó sobre esa. |
| 3 · data/raw | Este acto **NO** toca microdato. Punto saltado, como el encargo indica. |
| 4 · ENTORNO | `echo ${CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE:-sin_variable}` → `cloud_default` (variable presente, firma correcta de nube). `curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://github.com/` → **400** en la primera corrida; contra `https://github.com/Josanoforo/Modelado-Mexicano` → **200**; `curl "$HTTPS_PROXY/__agentproxy/status"` → `enabled:true`, `recentRelayFailures: []`. Se reporta el 400 tal como salió, no se sustituye por el 200. |
| 5 · ESPEJO | Toda cifra de esta nota sale del clon, con el comando a la vista. Ninguna del espejo del proyecto. |

**Divergencia declarada respecto al encargo, punto 1.** El encargo pide la rama `claude/a10-estampa`. El
arnés de esta sesión designó `claude/a10-estampa-universo-83h68h` y prohíbe empujar a otra. Se usó la
designada. Mismo trabajo, distinto nombre de rama; se dice en vez de callarse.

---

## 2. Verificación de existencia, re-corrida contra el árbol (no heredada del encargo)

- `grep -c "A\.10" instrucciones-proyecto-v2_9.md` → **2**. **Corrección de la premisa del encargo:** el
  encargo sitúa esas dos ocurrencias en "≈líneas 296 y 326". En `instrucciones-proyecto-v2_9.md` las dos
  ocurrencias literales de `A.10` están en las **líneas 344 y 354** ("Por qué v2.9" y la nota de alcance);
  296 y 326 son las **dos notas de numeración de `A.7`**, que no contienen la cadena `A.10`. Son los números
  de línea de `instrucciones-proyecto-v2_8.md`, heredados de la fila `FP-07` del tablero. El encargo apunta
  a los párrafos correctos con los números equivocados; se editaron los párrafos, no los números.
- La sección `A.10` como regla: **NO-ENCONTRADO** en instrucciones antes de este acto. Confirmado.
- Texto fuente: **EXISTE-SATISFACE**. `canon/gobernanza-v1_15.md:866` (`ADR-67`) trae el párrafo verbatim.
- Autorización del rótulo: **EXISTE-SATISFACE**. `gobernanza:1173-1177` (`ADR-79(e)`), firma verbatim
  *"comitteemos el texto."*, que nombra `A10-ESTAMPA` como el acto que escribe la sección completa.
- `forense/firmas-pendientes.tsv`, fila `FP-09`: `ABIERTA` antes de este acto. Confirmado.

---

## 3. Qué se escribió

**`instrucciones-proyecto-v2_10.md` (nuevo).** `cp` de `instrucciones-proyecto-v2_9.md` verificado idéntico
antes de tocar nada, más:

- **"Por qué v2.10"** — una frase: el texto existía en `ADR-67` desde el 10/ago y el rótulo desde
  `ADR-79(e)`, pero vivía en un registro de decisión y no donde una regla gobierna una corrida.
- **`A.10 · Estampa de universo`** — el verbatim de `ADR-67` como núcleo citado; la regla operable (SHA,
  corpus, denominador cuando exista, y cuando no exista se escribe que no existe; `VENCIDO EN ALCANCE` como
  estado con nombre); **dos** corolarios, ambos derivados del propio `ADR-67` — re-sello contra el universo
  nuevo en vez de edición del viejo (precedente real: `ADR-82` sobre `ADR-67(a)`), y "la conclusión de un
  cierre no puede ser más ancha que su universo declarado" (`ADR-67(b)`, verbatim); **Falsador y caducidad**
  a tres meses; **El costo, contado** (los cierres de `ADR-52 A`/`ADR-54` sobre cinco instrumentos, con las
  cifras de `ADR-67`: 509 de 35,708 activos inspeccionados, 1.43%; universo desconocido sin denominador).
- **Contención verificada, no afirmada:** `diff instrucciones-proyecto-v2_9.md instrucciones-proyecto-v2_10.md`
  → **0 líneas suprimidas**, 28 añadidas, en tres tramos (`@@ -295 @@`, `@@ -325 @@`, `@@ -353 @@`). v2.10 ⊇ v2.9.
- `instrucciones-proyecto-v2_9.md` **no se borra** — protocolo de renombre-por-versión de
  `forense/encargos/convencion.md`.

**Tres enmiendas in situ fechadas, ninguna reescritura.** Las dos notas de numeración de `A.7` (la de v2.8 y
la de v2.7 dentro de `A.8`) y la nota de alcance de v2.9 conservan su texto original íntegro y ganan un
párrafo fechado debajo. El encargo pedía dos; se hicieron tres, y la tercera se declara aquí con su razón:
la nota de alcance de v2.9 dice "no escribe el texto de `A.10` … acto sucesor `A10-ESTAMPA` sin ejecutar", y
dejarla sin marca dentro del archivo que **sí** lo escribe deja una contradicción de hecho en el mismo
documento. Sigue siendo cierta de v2.9; la enmienda dice exactamente eso.

**Hallazgo propio: la verificación de `A.11` heredada no reproduce.** v2.9 declara `A.11` libre "verificado
(`grep -n "A\.11" canon/ forense/ instrucciones-proyecto-v2_8.md` → sin resultados)". Re-corrido hoy sobre el
universo equivalente, el grep devuelve **3 líneas**: `gobernanza-v1_15.md:1377`, `forense/hallazgos.md:301` y
la propia nota de alcance de v2.9 — las tres declarando que `A.11` **no** está reclamado, es decir, el
registro de que quedó libre, no un reclamo. Fuera de ese universo, `milpa/refutations.yaml:234` trae
`ref.A.11.rechazo_tecnologico`, que es un id de refutación del motor: espacio de nombres distinto, sin
colisión. **La conclusión de v2.9 sigue siendo correcta; su evidencia ya no.** En v2.10 se escribe la salida
real con su universo declarado, en vez de heredar la cifra — que es, literalmente, lo que `A.10` acaba de
volver regla.

**`canon/gobernanza-v1_15.md`.** `ADR-87` (append) con: el texto citado, la regla operable y sus dos
corolarios, la consecuencia sobre `A.7`, el movimiento del tablero, el desborde de perímetro declarado, y el
estado real de la suite. Cabecera de conteo, `86` → `87`.

**`canon/estado-programa-v1_10.md`.** Solo cascada de conteo: `:27` (Registro de artefactos) y `:101`
(historia de la numeración, `L0`). Ver §5 — es desborde de perímetro, declarado.

**`forense/firmas-pendientes.tsv`.** Ver §4.

---

## 4. Tablero de firmas — qué se movió y qué no

| Fila | Antes | Después | Por qué |
|---|---|---|---|
| `FP-09` | `ABIERTA` | **`FIRMADA`** | Su condición era "texto completo aún no escrito en instrucciones-proyecto". Está escrito. `firmada_en` = `ADR-87, PR #<n>, 2026-08-17`. |
| `FP-07` | `ABIERTA` | **`ABIERTA`** | El encargo abrió su ranura de firma con vehículo declarado — el mensaje de lanzamiento de mesa — y **ese mensaje no trajo firma adjunta sobre el punto**. Sin cita verbatim no hay adjudicación (`ADR-79(i)`). La fila gana nota: el segundo reclamo sobre `A.7` está retirado **de hecho** (la estampa vive en `A.10`), la decisión **formal** sigue sin firma. |
| `FP-25` | — | **`ABIERTA`** (nueva) | Pegado de `instrucciones-proyecto-v2_10.md` al proyecto de Claude, que `A.9` exige y este acto no puede ejecutar. Id derivado del máximo real del TSV con `awk` sobre la columna `id`, ordenado numéricamente por el sufijo (`sort -t- -k2 -n`), última fila → `FP-24`; no tecleado. |
| `FP-22` | `ABIERTA` | `ABIERTA` | **Satisfecha por contención cuando `FP-25` se firme**, no antes: v2.10 ⊇ v2.9 está verificado por `diff`, así que quien pegue v2.10 pega también v2.9 — pero nadie ha pegado nada todavía. Se marca `FIRMADA` en el mismo acto que `FP-25`, mismo mecanismo con que `ADR-81(c)` cerró `FP-08`/`A.9`. |

Tablero tras el acto: **25 filas · 19 `ABIERTA` · 6 `FIRMADA`**, derivado con
`awk -F'\t' 'NR>1 && NF>1 {print $6}' forense/firmas-pendientes.tsv | sort | uniq -c`. `ABIERTA` sin cambio
neto (−1 por `FP-09`, +1 por `FP-25`) — el tablero no mejoró de tamaño, y decirlo importa más que el número.

**Nota mecánica sobre `T22(b)`.** El encargo archivado contiene el marcador de ranura literal que `T22`
vigila. En vez de tocar `tests/check.py` (fuera de perímetro) para añadirlo a la lista de archivos conocidos,
se usó la salida que el propio test ya tiene: la fila `FP-07` cita ahora ese archivo en su columna `dónde`,
que es además donde semánticamente corresponde. `T22` queda en `[warn] 19 warn`, **0 FAIL**.

---

## 5. Perímetro — lo que se tocó, y el desborde declarado

Tocado, exactamente: `instrucciones-proyecto-v2_10.md` (nuevo) · `canon/gobernanza-v1_15.md` ·
`forense/firmas-pendientes.tsv` · `forense/encargos/2026-08-17-EA10-a10-estampa.md` (nuevo) ·
`forense/notas/2026-08-17-a10-estampa.md` (esta nota) · **`canon/estado-programa-v1_10.md`**.

El último **no** está en la lista cerrada del encargo. Se tocó igual, con el cambio mínimo posible (un dígito
en `:27`, un dígito más una cláusula de cascada en la oración histórica de `:101`), y la razón se declara en
vez de esconderse: la receta de `T15` escanea **todo** `canon/*.md`, no solo `gobernanza`. Sellar `ADR-87` sin
esa cascada deja tres `FAIL` de `T15` causados por este mismo acto — una regresión real, no heredada. El
precedente que resuelve exactamente esta tensión ya está escrito en el repo: `ADR-62` (`gobernanza:768`),
verbatim, prefiere el requisito de cierre sobre el perímetro para esta cascada concreta y lo declara. Se
siguió ese precedente. El encargo lo previó a medias — dice "cascada de cabecera **si su receta lo exige**",
y su receta la exige.

No se tocó: `data/` · `tools/` · `milpa/` · `tests/**` · el motor · ningún cierre previo · nada del perímetro
de colisión de BARRIDO-2 · ningún archivo de `forense/encargos/` distinto del propio (E-HIG no colisiona).

---

## 6. Numeración del ADR

`ADR-87`, derivado con la receta de `T15` contra `origin/main` = `f3873c2`: **únicos 86 · max 86 · huecos
`[]`** → siguiente disponible 87. Tras el append, la misma receta da **únicos 87 · max 87 · huecos `[]`**, y
`T15` corre `[ ok ]`.

El encargo advierte que el número no se anticipa y que hay colisión posible con el ADR de Codex. Derivarlo
contra el `main` que se tiene delante es lo correcto y **no evita** la colisión: es exactamente lo que pasó el
14/ago entre `ACTO TABLERO-FIRMAS` y `ACTO B2` con `ADR-84`, donde ninguno de los dos erró y renumeró quien
fusionó después. Si ocurre, la renumeración propaga a cinco sitios, listados aquí para que quien la haga no
los busque: `gobernanza-v1_15.md:2` · `estado-programa-v1_10.md:27,101` · las tres enmiendas in situ de
`instrucciones-proyecto-v2_10.md` · la columna `firmada_en` de `FP-09` · la fila `FP-25`.

---

## 7. Suite — el primer resultado, tal como salió

`python3 tests/check.py --baseline`, corrido **antes** de tocar nada, sobre la base `f3873c2` limpia:

```
20 FAIL · 131 WARN
LÍNEA BASE: ROJO — 19 entradas nuevas frente a tests/baseline.json (HEAD congelado 2af748ca…)
```

`python3 tests/check.py --baseline`, corrido **después** de todos los cambios de este acto:

```
20 FAIL · 131 WARN
LÍNEA BASE: ROJO — 19 entradas nuevas frente a tests/baseline.json (HEAD congelado 2af748ca…)
```

**La suite no está VERDE, y no lo estaba antes de este acto.** El encargo pide `--baseline` VERDE; la premisa
no se sostenía ya en la base declarada, y eso se reporta en vez de fabricarse. Qué son exactamente las 19
entradas, derivado y no supuesto: las 19 son de `T22`, y son las **mismas filas `ABIERTA` del tablero con su
contador de días corrido** — `tests/baseline.json` las congeló el 14/ago diciendo "(0 días)"; hoy, 17/ago,
dicen 3, 4, 6, 7 y 12 días. La línea siguiente del propio reporte lo confirma desde el otro lado: *"19
entradas de la línea base ya no aparecen"*. Es deriva de fecha por diseño de `T22`, no deuda nueva del
corpus: el baseline de este mecanismo no puede seguir verde de un día para otro sin recongelarse.

Comparación fila por fila de las dos corridas (`diff` sobre las entradas nuevas de cada una) — **la única
diferencia que este acto introduce**:

```
19d18
<   · T22: FP-09 ABIERTA desde 2026-08-13 (4 días): Rótulo A.10 autorizado …
30a30
>   · T22: FP-25 ABIERTA desde 2026-08-17 (0 días): A.9 aplica a instrucciones-proyecto-v2_10.md …
```

Sale `FP-09` (pasó a `FIRMADA`), entra `FP-25` (nueva). Nada más. El `diff` de la tabla de tests por test
entre las dos corridas es **idéntico**: `T02 [ ok ]`, `T03 [warn] 46`, `T15 [ ok ]`, `T22 [warn] 19`, cero
`FAIL` nuevos, cero clases nuevas. Ni un solo test cambió de estado por este acto.

**Por qué no se recongela.** `ADR-76(f)`, verbatim: *"recongelar la línea base exige ADR de mesa; un ejecutor
que encuentre drift lo reporta y no lo recongela. Sin condiciones adicionales."* Además `tests/**` está fuera
del perímetro. Se deja ROJO y se declara — mismo criterio con que los tres primeros commits de
`ACTO TABLERO-FIRMAS` dejaron su propio rojo sin tocar, antes de que `ADR-86` lo autorizara.

`git diff --check`: **dos hits, ambos en `forense/firmas-pendientes.tsv`, ambos estructurales y ninguno
corregible sin romper el tablero.** Son las filas `FP-07` y `FP-25`: el esquema del TSV tiene 7 columnas y una
fila `ABIERTA` deja vacía la última (`firmada_en`), de modo que la línea termina en tabulador. Verificado que
no es cosa de este acto — `git show HEAD:forense/firmas-pendientes.tsv` muestra que **todas** las filas
`ABIERTA` preexistentes terminan igual (`ABIERTA^I$`); `git diff --check` solo señala las líneas *añadidas*.
Y verificado que quitar ese tabulador sería un defecto, no una limpieza: el parser de `T22`
(`_t22_tabla`) descarta en silencio toda fila cuyo número de campos no iguale el de la cabecera, así que una
fila de 6 columnas **desaparecería del tablero sin aviso** — exactamente la clase de limbo que `A.12` existe
para impedir. Fuera del TSV, cero hits.

---

## 8. Qué NO hizo este acto

No adjudicó `A.7` sin firma. No tocó `data/`, `tools/`, `milpa/`, el motor ni `tests/`. No reabrió ningún
cierre previo — `A.10` gobierna hacia adelante y los sellos existentes conservan su estampa, o su ausencia,
como historia (`ADR-67`/`ADR-77`). No pegó nada al proyecto de Claude: eso es de mesa, con fecha, y por eso
existe `FP-25`. No borró `instrucciones-proyecto-v2_9.md` ni ninguna versión anterior. No reclamó `A.11`. No
recongeló la línea base. No se auto-fusionó.

**Contadores del programa movidos: 0.** `13 de 27` (Hito D) · `11 de 15` (condicionales) · `0 de 15`
(coeficientes en escala del modelo) · `1 de 2` (llaves ejercidas) · `4 de 144`. Ninguno se mueve — este acto
es de gobierno sobre el propio programa, no de medición sobre México.
