ENCARGO · ACTO GEN2-TUBERIA-SIDECAR-CUERPO-1 · EL SELLO DE UN ENCARGO CUBRE SU CUERPO, NACE EN EL 0-bis Y NO SE REGENERA NUNCA · LAS ADENDAS ATERRIZAN COMO ARCHIVO PROPIO · Y EL CI EMPIEZA A MIRAR LOS SIDECARS DE `forense/encargos/` Y `forense/notas/`
CABECERA · redactado contra `5a888bcb` (re-deriva al abrir; si `main` se movió no es PARO: refresca, fusiona hacia la rama, re-deriva y reporta) · ENTORNO: NUBE — Claude en la nube, `CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=cloud_default`, con credenciales de Git y publicación de PR funcionando; cero microdato, `data/raw` no hace falta · una sola sesión, rama propia (D-17) · MODO: ABIERTO (D-18) · MODELO SUGERIDO: Opus (se puede subir, nunca bajar) · CONTADOR: `cuenta_gen2 = NO` — no mide ninguna celda del programa · vehículo: `/acto`.
COMPUERTA — una, y protege un borrado (D-20): se retira exactamente un archivo, `forense/encargos/2026-09-20-GEN2-DIN-CREDITO-COMPARABILIDAD-TEXTO-1.md.sha256`, autorizado por la firma #932 de abajo, en el mismo commit que crea su sustituto. Ningún otro sidecar se borra, se edita ni se normaliza. El de `ENCARGOS-GEN2-v1_5` se conserva intacto como testigo.
EL PR NO SE FUSIONA EN ESTE ACTO. Se publica y queda propuesto; mesa central lo revisa y fusiona. El acto termina con `## CONSUMIDO` citando el número real de PR, verificado contra el HEAD remoto.
Ramas vivas al redactar: cinco (`acto/gen2-celda-d-piloto-3-commit-2-3`, `acto/gen2-din-credito-pisos-enif2021-1`, `acto/gen2-tramite-firmas-3-propagacion`, `claude/focused-archimedes-fjyqf5`, `claude/new-session-ccjtu7`). Re-deriva al abrir y declara el conteo (A.13). No necesita ventana. `claude/new-session-ccjtu7` es `GEN2-TUBERIA-SUCESOR-1`, en vuelo: comparte con este acto `forense/hallazgos.md`, `forense/no-corrido.tsv` y posiblemente la cascada de gobernanza; es concurrencia normal, se fusiona `main` hacia la rama y se re-deriva.
IDS DE ESTE ACTO.

* `ADR`: numérico, como siempre — `ADR-577` candidato, derivado hoy (máximo en `main`: `ADR-576`). Deriva al cierre, no heredes; renumera quien fusiona segundo.
* `NC` y `FP`: este acto no debería necesitar ninguna `FP` —todo lo que toca está firmado—. Si necesita `NC`, la regla depende de un hecho verificable al acuñar:
   * Si `main` ya contiene el merge de `GEN2-TUBERIA-SUCESOR-1` (existe en `forense/no-corrido.tsv` o `forense/firmas-pendientes.tsv` al menos una fila con raíz de acto, o su encargo en `main` trae `## CONSUMIDO`): acuña con la raíz congelada, `NC-<AAMMDD>-GEN2-TUBERIA-SIDECAR-CUERPO-1-<4 hex del commit de 0-bis>-<NN>`.
   * Si no: acuña con el esquema viejo. Por la firma D-4, `SUCESOR-1` es el primero que acuña con la raíz nueva y el que ensancha sus consumidores; acuñar antes que él dejaría la fila invisible al enrutamiento.
   * Declara en el cierre cuál de las dos ramas aplicó y por qué.

1 · OBJETIVO
Que al terminar: (a) `tools/sella_sha256.py` selle y verifique el cuerpo de un encargo con la regla firmada; (b) `/acto` selle cada encargo en el 0-bis, prohíba re-sellarlo al cierre, obligue a que el cierre se añada al final, y archive las adendas como archivo propio; (c) el CI verifique los sidecars de `forense/encargos/` y `forense/notas/`; (d) el sidecar de #932 esté restaurado y el de `ENCARGOS-GEN2-v1_5` re-sellado; (e) la convención, el hallazgo de método y las semillas queden asentadas.
Criterio de «hecho»: `python3 tests/check.py --baseline --parallel` en LÍNEA BASE VERDE, el verificador nuevo cableado en CI y en VERDE sobre `main`, este mismo encargo sellado con la regla nueva antes de escribir su cierre (P-i), PR publicado y propuesto a mesa, y `## CONSUMIDO` con el PR real.
2 · FIRMAS DE MESA, verbatim (21/sep/2026)
D-a1 — «Sí: la convención (a) se escribe con la normalización N(texto): se quita al final todo blanco y toda línea de solo `---`, y se termina en exactamente un salto de línea. Se aplica igual al sellar y al verificar.»
D-a2 — «Delimitador por hash que desambigua: candidatos = el prefijo antes de cada línea `^## NO-CORRIDO` más el archivo entero; pasa si alguno, normalizado, casa con el sidecar. Sin centinela.»
D-a5 — «Sí: los candidatos de D-a2 se amplían al prefijo antes de cualquier encabezado `^## `, más el archivo entero. Con una condición: el verificador reporta CUÁL candidato casó, y emite WARN —no FAIL— cuando lo que sigue al cuerpo sellado abre con un encabezado distinto de `## NO-CORRIDO` o `## CONSUMIDO`. Una adenda pegada al final pasa el sello del cuerpo, pero no pasa inadvertida (D-16: los WARN se listan, no adjudican).»
D-a3 — «Sufijo propio `X.cuerpo.sha256`. `sha256sum -c` no es su verificador, y el nombre lo dice.»
D-a4 — «Verificador para `forense/encargos/` y `forense/notas/`, cableado en CI. No se extiende a los 261. Entiende en LECTURA los tres formatos de sidecar que ya existen, para no dar falsas alarmas. NO reescribe ni normaliza ningún sidecar viejo. Los sidecars nuevos nacen en un solo formato.»
#932 — «Se restaura el sello del 0-bis (`8a847600…`). No se recalcula. El sidecar pasa a `.cuerpo.sha256` en el mismo commit y el viejo se retira declarándolo.»
ENCARGOS-GEN2-v1_5 — «Se re-sella declarando la edición del 15/sep y su NC-0050. El sidecar viejo queda como testigo VENCIDO EN ALCANCE (A.10); no se edita ni se borra. No se revierte el texto.»
acto.md — «TUBERÍA puede editar `.claude/commands/acto.md` en esta pieza, SOLO en dos sitios: el paso de sello en el 0-bis y la prohibición de re-sellar al cierre. Ese archivo cambió hace un día (GEN2-V215, sección 4 EJECUCIÓN): parte de la versión de main y no toques nada más.»
D-a6 — «Sí: tercer sitio autorizado en `.claude/commands/acto.md`, el paso 10, con la frase: «La sección de cierre se añade al final del encargo archivado; nada por encima de ella se edita.» Ningún otro cambio a ese archivo.»
Dónde vive el texto de (a) — «En acto.md ahora, y como semilla PARA-v2.16 en hallazgos para entrar a A.3 en la próxima versión de instrucciones. No se reabre la v2.15 por esto.»
ADENDAS — «Confirmada la regla con tus tres precisiones: `<encargo>-ADENDA-N.md` junto a su encargo, forma plana, con su propio `.cuerpo.sha256`, sellada al recibirse; N por max+1 (un solo escritor por encargo, D-17); el encargo cita sus adendas desde el CIERRE, nunca desde el cuerpo.»
Método — «La corrección de §0 —medir lo que el commit hizo él mismo, contra TODOS sus padres, y no contra el primero— es el hallazgo de método de la jornada: asiéntalo como semilla.»
Semillas sin ejecutar — «(1) … el cuerpo de un encargo no trae campos que se rellenen al cierre ni líneas de estado. Tocar PLANTILLA-ENCARGO-v2_0.md exige v2.1 y pegado en los dos lados (A.9): no es de esta pieza. (2) Las adendas sin rastro del 19 al 21/sep: no se archivan retroactivamente … Pero lista en tu nota las que SÍ puedas nombrar por los mensajes de commit … para que quede escrito qué actos corrieron bajo un texto que el repo no guarda.»
3 · LO QUE DIRECCIÓN SABE — cada línea con su rótulo

* `EJECUTADO` · #932. El sidecar vigente declara `b07f8ba3…` y falla. El sello del 0-bis, `8a847600…`, pasa hoy con la regla firmada (N y candidatos ante cualquier `^## `). Traza: `d4d472b7` (0-bis, sello correcto) → `d3ac15a9` (`## NO-CORRIDO`; la unión añade un `\n`) → `4893f53e` (el arreglo provisional re-sella sobre el texto final) → `847d12b0` (una renumeración reescribe la línea de `## CONSUMIDO` y rompe el re-sello).
* `EJECUTADO` · `ENCARGOS-GEN2-v1_5`. `forense/notas/ENCARGOS-GEN2-v1_5-aparato-antes-de-calcular-2026-09-08.md` y su `.sha256` existen; el sidecar casa con la versión de `7c6042e4` (8/sep); la edición posterior es `cf904d60` (15/sep, `GEN2-MANTENIMIENTO-Y-ARCHIVO-2`); `NC-0050` está CERRADA.
* `EJECUTADO` · Alcance. 261 sidecars versionados en el árbol; `sha256sum -c` ingenuo da 21 negativos, 17 de ellos falsas alarmas de formato. Los tres formatos que el verificador debe leer: `hash basename` relativo al directorio del sidecar · `hash ruta` relativa a la raíz del repo · hash pelado contra el archivo hermano. Dos convenciones de nombre: `X.sha256` y `X.md.sha256`. En `forense/encargos/` y `forense/notas/` hay dos rotos (los dos de arriba) y dos huérfanos (`instrucciones-proyecto-v2_13.md.sha256` en la raíz no entra al universo; el sello del insumo codex en `forense/notas/insumos-externos/pisos-enif2021-0002-rama-codex/` sí entra: cita un `sello.json` que no está junto a él).
* `EJECUTADO` · Ambigüedad del delimitador. De 189 encargos con `## NO-CORRIDO`, cuatro tienen más de una línea que empieza así: `PRE-E5`, `BANXICO-PRODUCTO-ATRASO-Y-COSTO`, `CHECADOR-2` (en prosa, línea 35) y `E3-1-1`. El hash los resuelve sin escoger.
* `EJECUTADO` · La amenaza hacia adelante. Sello simulado en el 0-bis sobre los 56 encargos tocados del 19 al 21/sep: 43 pasan con D-a2; 4 más con D-a5 (cierre bajo otro encabezado — deben salir WARN); 9 fallan porque el cuerpo se editó, cinco de ellos por la cascada de cierre del propio acto. D-a6 es el remedio.
* `EJECUTADO` · La renumeración casi nunca toca el cuerpo. Contando sólo lo que cada commit hizo él mismo —difiere de todos sus padres—: 35 ediciones a encargos archivados en 14 días, 34 en el cierre, 8 en el cuerpo; ids `ADR` cambiados en el cuerpo: cero.
* `LEÍDO` · `tools/sella_sha256.py` (7/sep): sella y verifica sidecars hermanos uno por uno, con tres estados que no se colapsan (`SELLO_COINCIDE` · `SIDECAR_AUSENTE` · `SELLO_NO_COINCIDE`) y exit codes 0/2/3. Su cabecera ya prohíbe re-sellar al cierre: «que el cierre reselle un archivo no vuelve aceptable un cambio accidental en una spec». Usa `X.sha256`; declara que `X.md.sha256` no es su convención. Lo importan `tools/corrida0.py` y `tests/check.py`: su comportamiento actual no se rompe; `--cuerpo` es un modo nuevo.
* `LEÍDO` · `.claude/commands/acto.md` en `main`: `## 3 · 0-bis A.3` en la línea 215; `## 5 · CIERRE` en la 288; el paso 10 (`## NO-CORRIDO / RESERVAS`) en torno a la 360, que hoy dice sólo que se escribe «en el mismo encargo archivado, antes de esa sección»; el paso 11 ya pone `## CONSUMIDO` al final.
* `LEÍDO` · CI: `.github/workflows/verify.yml` corre los tests adicionales uno por línea (`run: python3 tests/test_….py`, líneas ~77-133). Ahí se cablea el verificador.
* `EJECUTADO` · Adendas del 19 al 21/sep, nombrables por mensaje de commit en todas las refs: `GEN2-MARCADOR-REDISENO-1` (`07794972`, pegada al encargo tras el 0-bis) · `GEN2-CELDA-D-PILOTO-3-P0` (`4d4131df`, «Adenda de mesa del 20/sep: P0 deja de gatear el COMMIT-1»; `352c7aed`, «Adenda de mesa punto 4»; no está en el encargo ni como archivo propio, la nota del paro la refiere) · `GEN2-LIMPIEZA-RAMAS-LOCALES-3` (`2802e63b`, «tras adenda de dirección»; `adbdb3c0`, «hipótesis de dirección, adenda»; texto no encontrado) · `GEN2-TRAMITE-FIRMAS-3`, en vuelo (`00ac06e9`, «Adenda de mesa: reserva:envipe2026»; aterrizó en `forense/encargos/CUADERNO-DE-FIRMAS-2026-09-21.md`). Falso positivo descartado: `4fc8fcd1`. No son adendas: `LIMPIEZA-RAMAS-LOCALES-4` y `-6` (el INPUT DE MESA es el encargo). Mesa reportó unas seis; el repo nombra cuatro actos.
* `EJECUTADO` · La cifra contaminada no está en el árbol. «47 ediciones de cuerpo», «162 ediciones» y variantes: cero ocurrencias en `main` y en las ramas vivas. No hay archivo que corregir.

4 · YA HECHO / YA DECIDIDO — búsqueda por OBJETO (A.8)

* `EJECUTADO`, contenido: `--cuerpo` en `tools/sella_sha256.py`: 0 ocurrencias. Archivos `*.cuerpo.sha256` en el árbol: 0. Verificador de sidecars de `forense/encargos/` o `forense/notas/`: 0 de 307 archivos `tests/*.py` y `tools/*.py`. Vocabulario A.4: NO-ENCONTRADO, las tres.
* `EJECUTADO`, precedente de adendas: cinco archivadas como archivo propio en `forense/encargos/` entre el 12/ago y el 15/sep (`2026-08-12-adenda-adquisicion.md` … `2026-09-15-GEN2-RELEVO-USOS-1-ADENDA-REGLA-ADOPCION-EN-BLOQUE.md`) y dos en `cola/`. La regla firmada formaliza una práctica que existió y se perdió; no se renombran esas siete.
* Todo lo que este acto decide ya está firmado (§2). Nada se vuelve a preguntar a mesa.

5 · PIEZAS — resultado esperado, no receta
P0 · 0-bis (A.3) y chequeo de duplicado por CONTENIDO. Este encargo verbatim a `forense/encargos/`. El 0.c estándar busca el rótulo en nombres de rama, worktrees y PR; ~40 % de las ramas llevan nombre autogenerado sin rótulo, así que mira también si alguna rama viva archiva ya este encargo. Declara cuántas ramas examinaste. Si hay duplicado: PARA con cero commits.
P-a · El sellador de cuerpo. `tools/sella_sha256.py --cuerpo`: sella y verifica con N (D-a1) y candidatos = el prefijo antes de cada `^## `más el archivo entero (D-a2 + D-a5). Reporta cuál candidato casó. WARN, no FAIL, cuando lo que sigue al cuerpo sellado abre con un encabezado distinto de `## NO-CORRIDO` o `## CONSUMIDO`. Conserva los tres estados y sus exit codes. El sidecar nuevo se llama `X.cuerpo.sha256` (D-a3) y tiene un solo formato, el que ya usa el sellador (`<64 hex> <basename>\n`). El modo actual del sellador no cambia: `tools/corrida0.py` y `tests/check.py` lo importan.
P-b · `/acto` — exactamente dos lugares de `acto.md`, partiendo de la versión de `main`:

1. `## 3 · 0-bis A.3`: tras archivar el encargo verbatim, `python3 tools/sella_sha256.py --cuerpo <encargo>` en el mismo commit. Aquí se escriben, en este orden y sin más: el texto de (a) —N, candidatos por hash, sufijo `.cuerpo.sha256`—; la prohibición de re-sellar —«este sello no se regenera nunca: ni al cierre, ni tras una renumeración, ni para que un verificador pase»—, citando la advertencia del 7/sep del sellador; y la regla de adendas firmada.
2. `## 5 · CIERRE`, paso 10: la frase exacta de D-a6: «La sección de cierre se añade al final del encargo archivado; nada por encima de ella se edita.»

Ningún otro cambio a `acto.md`. Si al abrirlo la sección 3 o el paso 10 no están donde dice §3, re-deriva su ubicación por contenido; no reorganices nada.
P-c · El verificador (D-a4). Recorre los sidecars de `forense/encargos/` y `forense/notas/` y lee los tres formatos viejos más el nuevo, sin reescribir ni normalizar ninguno. Resultados por sidecar, sin colapsarlos: casa · no casa · objetivo ausente (huérfano) · testigo declarado. Un sidecar nuevo (`.cuerpo.sha256`) que no casa es FAIL; los viejos que ya no casan y están declarados como testigo o huérfano no adjudican. Cableado en `.github/workflows/verify.yml` junto a los demás tests adicionales. Probado por mutación, como mínimo: un sello de cuerpo que pasa · un cuerpo editado que falla · un cierre al final bajo `## NO-CORRIDO` que pasa limpio · un cierre bajo otro encabezado que pasa con WARN y nombra el candidato · uno de los cuatro encargos con doble `## NO-CORRIDO` que pasa sin elegir delimitador · un sidecar viejo en cada uno de los tres formatos que el verificador lee sin falsa alarma.
P-d · #932. Crea `forense/encargos/2026-09-20-GEN2-DIN-CREDITO-COMPARABILIDAD-TEXTO-1.md.cuerpo.sha256` con `8a847600…` restaurado del 0-bis (`git show d4d472b7:<ruta del .sha256>` o el hash que ese commit declaró). No se recalcula. En el mismo commit se retira `…TEXTO-1.md.sha256`, y el hallazgo lo declara con el hash retirado, `b07f8ba3…`. Es la única compuerta de este acto.
P-e · `ENCARGOS-GEN2-v1_5`. Sello nuevo `…2026-09-08.cuerpo.sha256` sobre el texto de hoy, declarando en el hallazgo la edición del 15/sep (`cf904d60`) y `NC-0050`. El `.sha256` viejo queda intacto y se declara testigo VENCIDO EN ALCANCE (A.10). No se revierte el texto.
P-f · Hallazgos y semillas en `forense/hallazgos.md`:

1. PARA-v2.16 · A.3 — la convención (a): N, candidatos por hash ante cualquier `^## `, WARN por encabezado ajeno, sufijo `.cuerpo.sha256`, sello en el 0-bis, prohibición de re-sellar, cierre al final.
2. PARA-v2.16 · A.3 — adendas como archivo propio, con las tres precisiones firmadas.
3. Método — una cuenta que atribuye cambios a commits en una historia con merges mide lo que el commit hizo él mismo —el archivo difiere de TODOS sus padres—, no la diferencia contra el primero. Motivo: la cifra de 162 ediciones contra las 35 reales.
4. PARA-v2.16 · quien redacta encargos — el cuerpo de un encargo no trae campos que se rellenen al cierre ni líneas de estado. Motivo: `NUBE-PILOTO-1` y `CORRIDA0-RENDIMIENTO-1`. Semilla sin ejecutar: la plantilla no se toca.
5. La cifra contaminada — una línea: no llegó al árbol.

P-g · Adendas sin rastro — en la nota de cierre, la tabla de actos del 19 al 21/sep que corrieron bajo una adenda que el repo no guarda, con los datos de §3. Nada se archiva retroactivamente. La diferencia entre las seis reportadas y las cuatro nombrables se declara, no se estima.
P-h · Huérfano dentro del universo. El sello del insumo codex en `forense/notas/insumos-externos/pisos-enif2021-0002-rama-codex/` cita un `sello.json` que no está junto a él. El verificador lo declara huérfano; el acto lo asienta como hallazgo. No se toca.
P-i · Este acto se sella con su propia regla. En cuanto P-a exista y antes de escribir cualquier sección de cierre, sella el cuerpo de este encargo archivado con `--cuerpo`. Su cuerpo sigue intacto desde el 0-bis, así que el sello es válido. Es el primer encargo del repo que nace con sello de cuerpo, y el verificador lo cubre desde su primera corrida.
P-j · Cierre. Cascada de `/acto`, `check.py --baseline --parallel` VERDE o PARO, `## NO-CORRIDO / RESERVAS` («Ninguno.» es obligatorio) al final del encargo —la regla que este acto escribe—, y `## CONSUMIDO` con el PR real verificado contra el HEAD remoto. La nota de cierre lleva basename distinto del encargo (`T02`) y todo rótulo nuevo se registra con su prefijo de espacio (`T25`): son los dos FAIL que topó el careo.
6 · LATITUD
El cómo es tuyo: el diseño interno de `--cuerpo`, dónde vive el verificador, cómo escribes las mutaciones, la redacción del texto de (a) dentro de los dos lugares autorizados. Un obstáculo reversible y barato se resuelve y se declara (D-19). Si `main` se mueve —con cinco ramas vivas, se moverá—, fusiona hacia la rama y re-deriva.
7 · PAROS — lista cerrada
Entorno sin credenciales de publicación · otra rama viva ya archiva este encargo · borrar, editar o normalizar cualquier sidecar distinto del `…TEXTO-1.md.sha256` de #932 · recalcular el sello de #932 en vez de restaurarlo · cambiar `acto.md` fuera de la sección 3 y del paso 10 · cambiar el comportamiento actual de `sella_sha256.py` de modo que `tools/corrida0.py` o `tests/check.py` fallen · objetivo inalcanzable. Fuera de esta lista no se para: se resuelve, o se pregunta a mesa con opciones y recomendación y se sigue con lo demás.
8 · PERÍMETRO Y CONCURRENCIA
Escribes en: `tools/sella_sha256.py` · `tests/test_sella_sha256.py` · el test del verificador · `.github/workflows/verify.yml` (una línea por test nuevo) · `.claude/commands/acto.md` sólo en la sección 3 y el paso 10 · los dos `.cuerpo.sha256` de P-d y P-e y el de este encargo (P-i) · el retiro de `…TEXTO-1.md.sha256` (P-d) · `forense/encargos/` (este encargo) · `forense/notas/` (la nota de cierre) · `forense/hallazgos.md` · `forense/no-corrido.tsv` · y la cascada de gobernanza que `cierre_acto.py --aplica` reconcilia. Más el perímetro de cierre permanente (D-21). Si te encuentras escribiendo fuera de esta lista, PARA.
No tocas: `milpa/` · ningún `CALC` · `tools/corrida0.py` · `tools/cierre_acto.py` · `tests/check.py` salvo lo que la cascada estándar exija · `PLANTILLA-ENCARGO-v2_0.md` · ningún sidecar de `data/`, `forense/prereg-caja/` ni `forense/prereg-duelo-v2/` · los siete archivos de adenda que ya existen · los testigos históricos de EDER · `instrucciones-proyecto-v2_13.md.sha256`.
9 · LO QUE NO HACE · SUCESORES
No re-sella ningún otro encargo · no extiende el verificador a los 261 · no archiva retroactivamente ninguna adenda · no toca la plantilla · no toca la renumeración ni el asignador de `ADR` (su careo sigue después, firma 1) · no reabre la v2.15 · no fusiona su propio PR.
Sucesores, en el orden de mesa: (3) `RES`/`CORR` con llave lógica sin ruta ni versión; (4) un archivo por entrada; (5) taxonomía de PR y regla de enrutamiento. Y las semillas PARA-v2.16 que este acto asienta, para la próxima versión de instrucciones.
10 · FALSADOR (§9)
Si en tres meses el verificador no ha emitido un solo FAIL ni un solo WARN, se anota y se revisa si valía el aparato. Si el WARN de D-a5 salta en más de uno de cada diez encargos nuevos, la frase de D-a6 no se está cumpliendo y se revisa `/acto`, no el verificador.

## NO-CORRIDO / RESERVAS

- **qué** — `P-h · Huérfano dentro del universo. El sello del insumo codex en `forense/notas/insumos-externos/pisos-enif2021-0002-rama-codex/` cita un `sello.json` que no está junto a él. El verificador lo declara huérfano; el acto lo asienta como hallazgo. No se toca.`
  **por qué** — `FUERA-DE-PERÍMETRO` — es del acto que vuelva a recibir o re-sellar el insumo `pisos-enif2021-0002` de la rama codex. El propio encargo lo ordena («No se toca») y es un insumo EXTERNO: editarle el sidecar es reescribir evidencia ajena. Medido aquí, además: el hermano `pisos-enif2021-0002-codex-2026-09-20-sello.json` tiene exactamente el sha256 declarado (`96fd07bc…`), así que lo roto es la CITA, no el payload.
  **impacto** — ningún contador. El payload está verificado y en su sitio; lo que no se mueve es la cita: cualquier verificador futuro que le tome la palabra al sidecar seguirá leyendo `HUÉRFANO` y depende de la declaración de testigo en `tools/verifica_sidecars.py` para no adjudicar. Si esa declaración se pierde, el FAIL reaparece.
  **sucesor** — `SIN-ASIGNAR` — el acto que re-reciba o re-selle el insumo codex `pisos-enif2021-0002`.
  Fila: `NC-260921-GEN2-TUBERIA-SIDECAR-CUERPO-1-3d08-01`.

- **qué** — `P-g · Adendas sin rastro — en la nota de cierre, la tabla de actos del 19 al 21/sep que corrieron bajo una adenda que el repo no guarda, con los datos de §3. Nada se archiva retroactivamente. La diferencia entre las seis reportadas y las cuatro nombrables se declara, no se estima.` **La tabla SÍ se entregó** (nota de cierre, §3); lo que no se corrió es la recuperación del TEXTO de dos de esas adendas.
  **por qué** — `NO-VERIFICABLE-AQUÍ` — el texto de las adendas de `GEN2-CELDA-D-PILOTO-3-P0` (`4d4131df`, `352c7aed`) y `GEN2-LIMPIEZA-RAMAS-LOCALES-3` (`2802e63b`, `adbdb3c0`) nunca llegó al árbol y no se deriva del mensaje de commit sin inventarlo (§2, regla de oro). La firma «Semillas sin ejecutar» ordena que no se archiven retroactivamente. Sólo mesa tiene el texto.
  **impacto** — ningún contador. Queda escrito qué actos corrieron bajo un texto que el repo no guarda: dos actos cuyo encargo archivado no es el texto completo bajo el que se ejecutaron. La regla firmada hoy (adenda = archivo propio, sellada al recibirse) lo impide hacia adelante; hacia atrás sólo se cierra si mesa aporta el texto.
  **sucesor** — `SIN-ASIGNAR` — sólo se cierra si mesa aporta el texto de esas dos adendas.
  Fila: `NC-260921-GEN2-TUBERIA-SIDECAR-CUERPO-1-3d08-02`.

## CONSUMIDO

**PR #946** — https://github.com/Josanoforo/Modelado-Mexicano/pull/946 · rama `claude/entorno-6pj9np` · `ADR-580` · 21/sep/2026.

Publicado y **propuesto a mesa; NO fusionado por el ejecutor** (el encargo lo exige y D-10/paso 9 de `/acto` lo confirma: el merge es la autorización, no un trámite).

`cuenta_gen2 = NO` · contadores movidos: **cero**.

## ADENDA DE CIERRE — tercera fila NO-CORRIDO, posterior al PR

Descubierta al correr la suite después de publicar `PR #946`. Se añade aquí,
al final, sin editar nada por encima (D-a6).

- **qué** — `Cascada de cierre, paso 3 · tools/cierre_acto.py --aplica deriva los tres contadores mecánicos del MÁXIMO de ADR, no del conteo de ÚNICOS.`
  **por qué** — `FUERA-DE-PERÍMETRO` — `tools/cierre_acto.py` está en la lista `No tocas` del §8 de este encargo, por nombre. Es de dirección.
  **impacto** — todo acto que tome un `ADR` no contiguo (es decir, todo acto que evite una colisión con otro en vuelo, el caso normal con cinco ramas vivas) cierra con dos FAIL de `T15` que debe corregir a mano, y el paso 3 de la cascada deja de ser todo-o-nada fiable.
  **sucesor** — dirección (bandeja), acto sucesor sin asignar; el arreglo es derivar el contador del conteo de `ADR` únicos —el mismo universo que `T15` usa— en vez de del máximo.
  Fila: `NC-260921-GEN2-TUBERIA-SIDECAR-CUERPO-1-3d08-03`.
