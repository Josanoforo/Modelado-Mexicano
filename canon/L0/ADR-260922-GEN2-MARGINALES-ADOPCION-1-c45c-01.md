**ADR-260922-GEN2-MARGINALES-ADOPCION-1-c45c-01**, `ACTO GEN2-MARGINALES-ADOPCION-1`, 22/sep/2026, NUBE, Sonnet 5, sin sub-agentes, MODO ABIERTO, COMPUERTA: ninguna. Ejecuta `FP-260921-GEN2-ARBITRO-MARGINALES-1-ed7d-02` (firma de mesa §2): de las 57 celdas marginales `EVALUADA`, ENVIPE 2025 se adopta (8/15 = 0.53 [0.30, 0.75]), ENIF 2024 se difiere (6/32 = 0.19 [0.09, 0.35]), ENCIG 2025 se veta en nivel (0/10) y queda como piso de orden — firma en `decisiones.tsv#adopcion:piso-t1-marginales-por-instrumento`. `tools/marcador_segmento.py` extendido (premisa `[SUPUESTO]` falsa, rama prevista): nueva clave `marginales` en `milpa/estimadores-por-segmento.yaml` (15 `ADOPTADO-POR-FIRMA` con `champion=PERSISTENCIA(t-1)` + `cobertura`, 42 `DIFERIDA`/`VETADA-EN-NIVEL` con `veto: <instrumento, cobertura>`), guardias T-RESERVA/T-EMISOR-NO-COMPARA/T-PISO-NO-CIRCULAR en verde, test nuevo `t_marginales_adopcion_por_instrumento`. `tools/corrida0.py::_estimadores_segmento_para_status` extendido para proyectar las 15 ADOPTADO-POR-FIRMA como uso activo. Mesa respondió `FP-…-c45c-01`: acreditar `origen_numerico` caso por caso en `decisiones.tsv` (objeto `origen:CALC-PISOS-ENVIPE2024-EJES-0002:<RESULT>`, 15 filas), sin editar el CALC sellado — verificado leyendo su spec: `envipe2024_csv` es `origen: manifiesto` (microdato real), el `INDETERMINADO` venía de que `PISOS-REJILLA-METADATOS` (tabla de identidad, no aporta cifra) no calzaba con el token `METADATO` de `_funcion_de_dependencia` — hueco de etiqueta, no de procedencia, como mesa diagnosticó. `tools/corrida0.py::_origen_numerico_decisiones()` nuevo (mismo patrón que `_validacion_independiente_resuelta`, precedencia de mesa por RESULT). `N_resultados_gen2_adoptados_activos`: **72 → 87**. `FP-…-c45c-01` FIRMADA, `NC-…-c45c-01` CERRADA. Sucesor del IC calibrado de ENIF declarado sin diseñar: `NC-260922-…-c45c-02` → `GEN2-ENIF-IC-CALIBRADO-1`, sigue ABIERTA. `celdas_validadas` sin tocar (vedado por el encargo). `data/corrida0/marcador-segmento.tsv` y `milpa/estimadores-por-segmento.yaml` NO viajan committeados en el PR (P4 §2(2), 21/sep/2026): las cifras arriba son proyección local, el job del push a `main` las re-deriva. `tests/check.py --rapido`: VERDE, 0 FAIL.
