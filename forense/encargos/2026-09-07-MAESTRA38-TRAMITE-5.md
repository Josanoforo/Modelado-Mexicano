ACTO MAESTRA38-TRAMITE-5 (FP-327, FP-328 a/b, benchmark al repo) — nube, Sonnet, invoca /acto

Ejecutor: Sonnet, invoca /acto, sin ADR propio salvo cascada. SHA base c275952.

(1) FP-327 → marcar EJECUTADA en el tablero: "el cuerpo v1.5 entró por PR #573; verificado grep -c \"v1.5\" forense/tablero/TABLERO-PROGRAMA.md → 3" (verifica tú mismo el grep antes de escribirlo; si el conteo real difiere, usa el valor real).

(2) FP-328 (a): las 4 filas ENFIH restantes (N12 ×2, N4 ×2) que citan el mismo id_manifiesto que ya se enlazó en C1 (PR #577) reciben id_manifiesto=enfih2019_bd_csv_zip + sha256_fuente, mismo mecanismo/script que usó C1 (busca en el repo qué script fue — probablemente algo como via_capa2.py --escribe o similar; localízalo en tools/ o donde corresponda y sigue su uso documentado). Si el script no existe o no aplica, documenta el enlace manualmente siguiendo el formato de las filas ya enlazadas en la tabla de relaciones/evidencias, y dilo explícitamente en el commit.

FP-328 (b): dar de alta MACU_INMUJERES como fuente canónica en data/curacion-registro/ (o la ruta real que gobierne fuentes según INFRAESTRUCTURA) usando el escritor canónico correspondiente (alta_relacion.py u otro — léelo de INFRAESTRUCTURA). La nota de la relación REL-2fa1c0dd debe pasar a citar el id de esta fuente en vez de prosa libre.

FP-328 (c) queda para el encargo N18 (spec de rama A) — no la toques aquí; en el tablero anota FP-328 → FIRMADA-PARCIAL con nota de que (c) se resuelve en spec S6/S7 v1.1.

(3) Crear forense/benchmark/BENCHMARK-MOTORES-COMPARABLES.md con el contenido del benchmark v1.2 (no tienes el texto verbatim del benchmark adjunto de mesa — busca si ya existe en el repo un archivo con "benchmark" y "v1.2" en el nombre o contenido, p.ej. agregado-v1_2-resultado.json y cualquier borrador de benchmark ya presente; si no encuentras un borrador v1.2 en prosa, construye el documento a partir de las cifras verificables del repo: agregado-v1_2-resultado.json, número de reglas/motor, ADR, manifiesto, tablero — cita solo lo que puedas verificar con grep/lectura directa, no inventes cifras). Añade cabecera de una línea "archivo canónico único, versión en la primera línea del cuerpo". Si existen v1.0/v1.1 del benchmark en el repo, muévelos a forense/historico/; si no existen en el repo, anota en el nuevo archivo que viven fuera del repo (en mesa) y no fue posible incluirlos.

(4) En el tablero: agregar fila nueva "D4 ABIERTA — procedimiento-scoring v1.2 con métrica secundaria (pp o Brier): firma de mesa" y fila "D1 FIRMADA — corredor P: no por ahora; se reabre cuando M deje de ser constante dentro de CIV".

PERÍMETRO permitido: tablero (TSV/MD), relaciones/evidencias (las 4 filas + 1 fuente), forense/benchmark/ (nuevo), INFRAESTRUCTURA, notas, A.3, cascada de ADR si aplica. NO TOCAR: milpa/**, canon/ (salvo cascada estrictamente necesaria), tests/*.py, tools/*.py (solo úsalos, no los edites), cron.

## CONSUMIDO

Ejecutado por Sonnet, 7/sep/2026, entorno **NUBE sin corpus** (`data/raw` ausente, verificado con `ls`), sobre `origin/main = 0a19d39` (rama `claude/pr-573-577-benchmark-m1fa61`, ancestro directo de `c275952`, la SHA base que el encargo cita — sincronizada por fast-forward antes de ejecutar).

**(1) FP-327 — corrección de premisa, no confirmación.** La premisa del encargo — "el cuerpo v1.5 entró por PR #573" — se verificó y es **FALSA**. `grep -c "v1.5" forense/tablero/TABLERO-PROGRAMA.md` → **3**, cifra correcta, pero las 3 apariciones son menciones del adjunto `TABLERO-PROGRAMA-v1_5.md` **ausente** (cabecera de consolidación de `TRAMITE-3` + nota de `PARO` de `TRAMITE-4`), no contenido nuevo. Verificado además con `git log --oneline 4b3a2f1..cb233ac` (rango real del PR #573, rama `claude/tablero-v1-5-update-khcia5`): exactamente 2 commits, `ACTO MAESTRA38-N18` (spec `S7-L17` v1.1, mapeo `a0927`) y `ACTO MAESTRA38-TRAMITE-4` (`PARO`, mismo adjunto declarado ausente) — ningún commit del PR toca el cuerpo del tablero con contenido `v1.5`. Van ya tres actos consecutivos (`TRAMITE-3`, `TRAMITE-4`, este) sin que el adjunto real llegue al repo. Siguiendo la instrucción explícita de usar el valor real cuando la premisa difiere, y dado que la propia fila `FP-327` ya ofrecía a mesa la opción (b) ("retira esta fila declarando v1.1 como contenido definitivo"), se cierra `FP-327` **por esa opción**, no por la premisa falsa del encargo — declarado así en `forense/firmas-pendientes.tsv` y en el tablero, no fabricado como si el PR #573 hubiera traído el cuerpo real.

**(2) FP-328(a) — mecanismo replicado a medias, discrepancia declarada.** El script que usó `C1` es `tools/curador_registro/via_capa2.py` (confirmado en `forense/notas/2026-09-07-MAESTRA38-C1-cierre.md §2(c)`: "el enlace se escribió como edición preservando texto, y `via_capa2.py --escribe` se corrió después"). Las 4 filas (`REL-04c3943ecb0df7bacdc3c994`/N12, `REL-987f01cb468fa0b51b2dffc6`/N4, `REL-a63dce9852d52046595e28f0`/N12, `REL-fb6a1baf579c6521a638b093`/N4) reciben `id_manifiesto=enfih2019_bd_csv_zip` + `sha256_fuente=be372533d5043920892142e8bf792b7293a5f20ab466a6441bc89925b42ef4d5` (verificado contra `data/manifiesto.yaml:4102-4116`), mismo criterio (edición preservando texto, sin `csv.DictWriter`). **Discrepancia declarada:** esta sesión es NUBE sin corpus — `via_capa2.py` (modo lectura) reporta `AUSENTE=97 RAIZ_NO_CONFIGURADA=117`, exit 1, 0 diffs propuestos; `--escribe` no promueve nada sin `data/raw` montada. `capa2_manifiesto`/`capa3_disco_real` de las 4 filas quedan **sin cambio** (`SI_O_REFERENCIADO`/`SI_O_PARCIAL`), a diferencia de las 4 filas que `C1` sí promovió a `SI`/`EXISTE;COINCIDE;INTEGRO` en `CAJA` con corpus. Queda para un acto `CAJA` sucesor correr `via_capa2.py --escribe` y completar la promoción.

**FP-328(b) — ejecutado con `tools/curador_registro/alta_relacion.py`.** Alta de `MACU_INMUJERES` como fuente canónica propia (`--dry-run` primero, luego aplicado): `REL-31513e0d2be4fb2d7fa98aeb` (`N36`/`R4.3`, `CANDIDATA`, `id_manifiesto=inmujeres_macu_indicadores_territoriales_xlsx`, `sha256_fuente=1ca57de22a68ce9fb2348e51380134ab53cadd1ecc385ae3194d6c6a7186fd94`). La nota de `REL-2fa1c0ddb0bd7c2776e43ea8` (y sus proyecciones en `evidencias.tsv`/`utilidad-modelo.tsv`) se editó para citar el id de la relación nueva en vez de la prosa libre "MACU no tiene fuente canónica propia…". `baseline.json` recifrado con `sync_bootstrap._freeze_manifest` (mismo escritor que usa `alta_relacion.py`); `python3 tools/curador_registro/baseline.py data/curacion-registro` → `{"ok": true, "errores": []}`.

**FP-328(c) — no tocado aquí**, como el encargo instruye; se resuelve en `forense/prereg-caja/S6-L16-spec-v1_1.md` (`ACTO MAESTRA38-N19`, acto separado inmediatamente después de este).

**(3) Benchmark — reconstruido, no transcrito.** No existe en el repo ningún borrador "benchmark v1.2" en prosa (`find . -iname "*benchmark*v1_2*" -o -iname "*agregado*v1_2*"` solo encuentra `forense/prereg-duelo-v2/agregado-v1_2-resultado.json` y su script) — mismo patrón de adjunto-que-nunca-llega que `TABLERO-PROGRAMA-v1_5.md`. Tampoco hay `v1.0`/`v1.1` de un "benchmark de motores" en el árbol para mover a `forense/historico/`; declarado en el propio documento nuevo que, si existen, viven fuera de este repo. `forense/benchmark/BENCHMARK-MOTORES-COMPARABLES.md` (v1.2, cabecera de una línea "archivo canónico único, versión en la primera línea del cuerpo") se construyó solo con cifras verificables: los tres corredores (`L_SOLO`/`L_CORPUS`/`M`) del `procedimiento-scoring-v1_1.md` sellado (`ADR-262`); comparación principal `L_SOLO_vs_M` = `INDETERMINADO` (punto −28.99, IC95 [−74.02, +9.40], 13 celdas pareadas de 14); hallazgo verificado de que `M` es constante dentro de `CIV` (6 celdas, un solo valor `0.294313`, leído directo de `agregado-v1_2-resultado.json`, sin recalcular nada).

**(4) Tablero — filas `D1`/`D4` del benchmark, en sección propia.** El tablero ya tenía, antes de este acto, una tabla de Discrepancias (§7) con filas rotuladas `D1`/`D3`/`D4` sobre un tema completamente distinto (defectos de documentación del propio tablero). Para no colisionar ni sobreescribir esas filas, las `D1`/`D4` que este encargo pide (numeración propia del benchmark, citada en su §5) se agregaron en una sección nueva y claramente separada ("Decisiones de mesa — benchmark de motores"), con nota explícita de que la letra coincide pero el tema no tiene relación. `D1 FIRMADA` — corredor `P`: no por ahora, se reabre cuando `M` deje de ser constante dentro de `CIV`. `D4 ABIERTA` — `procedimiento-scoring` v1.2 con métrica secundaria (pp o Brier): firma de mesa.

**Suite.** `python3 tools/curador_registro/baseline.py data/curacion-registro` → verde (`errores: []`). `python3 tools/cierre_acto.py --encargo forense/encargos/2026-09-07-MAESTRA38-TRAMITE-5.md` (preflight, dry-run) corrió `tests/check.py --baseline` y encontró **2 FAIL nuevos**, ambos ya corregidos o declarados en este mismo acto, sin editar `tests/*.py` (fuera de perímetro):

1. **T21 T-CAPA2-CAPA3** — la fila `REL-31513e0d2be4fb2d7fa98aeb` (alta de `MACU_INMUJERES`) traía `capa2_manifiesto=SI` con `capa3_disco_real=SI_O_PARCIAL`, combinación inválida (`capa2=SI` exige `capa3=EXISTE;COINCIDE;INTEGRO`, y esta sesión NUBE no puede afirmar eso sin corpus). **Corregido**: `capa2_manifiesto` bajado a `SI_O_REFERENCIADO` (el par válido para `SI_O_PARCIAL`) — honesto con lo que esta sesión sí verificó (resolución estructural contra el manifiesto) contra lo que no pudo verificar (disco). De paso se corrigieron dos coerciones booleanas de YAML no detectadas al construir la entrada de `alta_relacion.py` (`conflicto_material`/`requiere_decision: NO` se habían escrito como `False` en las tres tablas — YAML 1.1 interpreta `NO` como booleano — corregidas a la cadena `"NO"` que usa el resto del registro). `baseline.json` re-recifrado tras la corrección; `baseline.py` sigue en verde.
2. **T15** (tras abrir `ADR-373` en cascada, ver más abajo) — `canon/estado-programa-v1_12.md:27` seguía citando "372 ADR" en su tabla de artefactos canónicos, un segundo lugar (distinto de la línea `L0`) que declara el conteo. **Corregido**: `372` → `373`.
3. **T22 T-FIRMAS** — cerrar `FP-327` (que citaba `forense/tablero/TABLERO-PROGRAMA.md` en su `dónde` mientras estaba `ABIERTA`) deja a ese archivo sin ninguna fila `ABIERTA`/`FIRMADA` que lo cite, y `_T22_ARCHIVOS_CONOCIDOS` (`tests/check.py:1414-1422`) sigue listando solo el nombre viejo `TABLERO-PROGRAMA-v1_1.md` — exactamente el gap que el propio `MAESTRA38-TRAMITE-4` ya había identificado en su encargo (pieza (3)) sin llegar a ejecutarlo (`PARO`). **No corregido aquí** (`tests/*.py` fuera de perímetro de este acto) — declarado como `FP-330` nueva (recibo, ver `forense/firmas-pendientes.tsv`) para que un acto con `tests/*.py` en su perímetro añada `forense/tablero/TABLERO-PROGRAMA.md` a `_T22_ARCHIVOS_CONOCIDOS` con el mismo comentario que ya tiene el nombre viejo.

**A.8 · `tools/ya_medido.py` — apéndice del ejecutor, NO parte del texto verbatim** (el `objeto_modelo_origen: R4.3` de la entrada `MACU_INMUJERES` en FP-328(b) cita una regla del motor; T-YAMEDIDO exige la salida pegada, aunque este acto no clasifica/pre-registra/carga/sella la regla — solo cita su id como procedencia del objeto de evidencia):

```
$ python3 tools/ya_medido.py R4.3
=== ya_medido: R4.3 ===
  resuelto por canon: R4.3 -> id `salud.adherencia.desabasto_vs_cuidadora` (canon/modelo-decision-v4_0.md §3, tag **id:**)
-- milpa/tramite.yaml -- (sin apariciones)
-- milpa/tramite-ola5-propuesta-v0.yaml -- (sin apariciones)
-- canon/modelo-decision-v4_0.md §7 -- R4.3 | L243 | Desabasto -> abandono / familia cuidadora -> adherencia | [FUERTE / MEDIA] -- compuesta | Si
-- forense/notas/*-L*-*.md -- MAESTRA37-L1-censo.md:72, -remapeo.md:27; MAESTRA37-L3-BIS-veredictos.md:113,216,230,232,250,256,269; MAESTRA37-L3-veredictos.md:101,164,217,236,250,251; MAESTRA38-LOTE-ENSANUT-resultados.md:145
-- forense/prereg-caja/S*-spec-*.md -- S3-C1-spec-v1_0.md:42 (mencion de N36/R4.3, fuera de alcance de C1)
-- canon/registro-rotulos.tsv (alias) -- N MAESTRA34-N6, N MAESTRA36-N6, N MAESTRA37-N1, L MAESTRA37-L3, L MAESTRA37-L3-BIS
========================================
NUNCA-MEDIDA
```

`NUNCA-MEDIDA`, consistente: este acto no corre ningún falsador de `R4.3`, solo da de alta una relación `CANDIDATA` (evidencia documental) que cita la regla como procedencia. Medición del motor: **cero**.

**Cascada.** `ADR-373` (candidato re-derivado por `tools/cierre_acto.py`, máximo real `372`, contiguo) en `canon/gobernanza-v1_15.md §4` — sin enmienda de `modelo-decision-v4_0.md`, registro de decisión puro (el encargo declaraba "sin ADR propio salvo cascada"; se abre igual, por consistencia con el precedente de `TRAMITE-3`/`TRAMITE-4`, ambos también "sin ADR propio" y ambos registrados). `canon/estado-programa-v1_12.md` L0 (anotación nueva insertada antes de la anterior) y su tabla de artefactos canónicos (línea 27, "372 ADR" → "373 ADR", segundo lugar que `T15` audita). `canon/registro-rotulos.tsv`: censado `MAESTRA38-TRAMITE-5`. `python3 tools/cierre_acto.py --aplica` confirmó, tras mis ediciones manuales, "sin cambios -- cabecera y L0 ya declaran 373, igual al real".

**Perímetro respetado.** Tocado: `forense/tablero/TABLERO-PROGRAMA.md`, `forense/firmas-pendientes.tsv`, `data/curacion-registro/{relaciones,evidencias,utilidad-modelo,baseline}.tsv|.json`, `forense/benchmark/BENCHMARK-MOTORES-COMPARABLES.md` (nuevo), `canon/gobernanza-v1_15.md` (ADR-373, cascada), `canon/estado-programa-v1_12.md` (L0 + línea 27, cascada), `canon/registro-rotulos.tsv` (censo, cascada), `forense/encargos/` (A.3). No tocado: `milpa/**`, `canon/modelo-decision-v4_0.md` (ninguna regla del motor se toca), `tests/*.py`, `tools/*.py` (solo usados: `alta_relacion.py`, `via_capa2.py`, `baseline.py`, `sync_bootstrap._freeze_manifest`, `cierre_acto.py`, `ya_medido.py`, ninguno editado), `cron`. Medición de México: cero.
